# Local dev Prometheus config — scrapes ambush-relay metrics.
# The relay runs on the host (not in Docker), so we use host.docker.internal.
# Default metrics port is 9102; override with AMBUSH_METRICS_PORT.
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: ambush-relay
    static_configs:
      - targets: ["host.docker.internal:9102"]
