name: credential_access_lsass
description: LSASS memory access should trigger the credential access detector
seed_time_ms: 1700000300000
requested_by: replay-whisker
metadata:
  class: adversarial
  techniques:
    - T1003.001
  tags:
    - credential-access
    - lsass
    - memory

input:
  kind: events
  events:
    - action:
        type: isolate_host
        host_id: host-cred-1
      event:
        source: synthetic
        event_id: hunt-cred-1
        timestamp: 1700000300100
        host_id: host-cred-1
        payload:
          kind: registry_access
          process_name: rundll32.exe
          registry_path: MEMORY
          access_type: read
          target_process: lsass.exe

expectations:
  replay_bundle_count: 1
  max_detect_latency_us: 50000
