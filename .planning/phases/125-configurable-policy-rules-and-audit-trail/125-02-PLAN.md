---
phase: 125-configurable-policy-rules-and-audit-trail
plan: 02
type: execute
wave: 2
depends_on:
  - 125-01
files_modified:
  - crates/swarm-policy/src/static_gate.rs
  - crates/swarm-policy/src/lib.rs
autonomous: true
requirements:
  - POLICY-02
must_haves:
  truths:
    - "`StaticApprovalGate` denies same-scope bursts that exceed the configured one-minute limit."
    - "Rate-limit denials carry a stable rule name and human-readable reason."
    - "Old scope entries are pruned so the limiter is window-bounded rather than monotonically growing."
  artifacts:
    - path: "crates/swarm-policy/src/static_gate.rs"
      provides: "Per-scope rate limiting, stable static rule names, and unit coverage."
  key_links:
    - from: "crates/swarm-core/src/config.rs"
      to: "crates/swarm-policy/src/static_gate.rs"
      via: "max_actions_per_scope_per_minute"
      pattern: "max_actions_per_scope_per_minute"
---

<objective>
Harden the static policy fallback with deterministic per-scope burst limiting.

Purpose: Make the last-line-of-defense gate reject pathological action bursts even before configurable policy rules are considered.
Output: Windowed per-scope rate limiting in `StaticApprovalGate` with exact unit tests for denial and pruning behavior.
</objective>

<context>
@.planning/phases/125-configurable-policy-rules-and-audit-trail/125-CONTEXT.md
@.planning/phases/125-configurable-policy-rules-and-audit-trail/125-VALIDATION.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-policy/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-policy/src/static_gate.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add static per-scope rate limiting</name>
  <read_first>
    crates/swarm-policy/src/static_gate.rs
    crates/swarm-policy/src/lib.rs
  </read_first>
  <files>
    crates/swarm-policy/src/static_gate.rs
    crates/swarm-policy/src/lib.rs
  </files>
  <behavior>
    - `scope_rate_limit_denies_burst_for_same_scope` proves same-scope actions over the configured limit are denied.
    - `scope_rate_limit_prunes_old_entries` proves entries older than one minute stop counting against the limit.
    - Static decisions expose stable rule names suitable for structured logs and audit trails.
  </behavior>
  <action>Refactor `StaticApprovalGate` to build from `PolicyConfig`, track recent actions per scope in a one-minute window, and emit rule-attributed deny/allow/require-human decisions. Keep request validation and lease issuance behavior deterministic.</action>
  <verify>
    <automated>cargo test -p swarm-policy scope_rate_limit_denies_burst_for_same_scope -- --exact</automated>
  </verify>
  <done>The static fallback gate now denies bursty same-scope actions with stable audit metadata and bounded in-memory state.</done>
</task>

</tasks>

<verification>
Run the exact scope-limit test first, then the full `swarm-policy` crate tests to make sure the new stateful gate still preserves the existing baseline behavior.
</verification>

<output>
After completion, create `.planning/phases/125-configurable-policy-rules-and-audit-trail/125-02-SUMMARY.md`
</output>
