---
phase: 125-configurable-policy-rules-and-audit-trail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/config.rs
  - rulesets/default.yaml
autonomous: true
requirements:
  - POLICY-02
  - POLICY-03
must_haves:
  truths:
    - "Repository policy config can express static scope rate limits and ordered configurable YAML rules."
    - "Malformed or nonsensical policy config fails validation before runtime startup."
    - "The default repository ruleset demonstrates the new policy contract without requiring code changes."
  artifacts:
    - path: "crates/swarm-core/src/config.rs"
      provides: "Policy config contract and validation for static and configurable gate settings."
    - path: "rulesets/default.yaml"
      provides: "Repository-owned example rules proving the YAML contract is deployment-facing."
  key_links:
    - from: "rulesets/default.yaml"
      to: "crates/swarm-core/src/config.rs"
      via: "SwarmConfig deserialization"
      pattern: "policy:"
---

<objective>
Extend the policy config contract so Phase 125 can be driven entirely from the repository ruleset.

Purpose: Make configurable policy behavior a YAML-owned deployment concern rather than another hardcoded Rust branch.
Output: Validated `PolicyConfig` extensions, repository ruleset examples, and config-loading coverage for the new policy fields.
</objective>

<context>
@.planning/phases/125-configurable-policy-rules-and-audit-trail/125-CONTEXT.md
@.planning/phases/125-configurable-policy-rules-and-audit-trail/125-RESEARCH.md
@.planning/phases/125-configurable-policy-rules-and-audit-trail/125-VALIDATION.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/rulesets/default.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Phase 125 policy config contract</name>
  <read_first>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/config.rs
    rulesets/default.yaml
  </read_first>
  <files>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/config.rs
    rulesets/default.yaml
  </files>
  <behavior>
    - `PolicyConfig` exposes `max_actions_per_scope_per_minute` plus ordered configurable rules.
    - Rule config supports named allow/deny decisions, threat/action/severity selectors, UTC hour restrictions, and per-agent rate limits.
    - Validation rejects zero/empty limits, invalid UTC hour windows, and empty rule names.
  </behavior>
  <action>Add the new config types and validation logic in `crates/swarm-core/src/config.rs`, update any config fixtures or loader tests that construct `PolicyConfig`, and extend `rulesets/default.yaml` with representative policy rules that parse through the normal repository config path.</action>
  <verify>
    <automated>cargo test -p swarm-runtime config::tests::loads_repository_ruleset -- --exact</automated>
  </verify>
  <done>The repository ruleset and runtime config loader understand the new policy contract and reject malformed policy settings before runtime startup.</done>
</task>

</tasks>

<verification>
Run the repository config-load test after the contract lands, then use package checks to catch any remaining `PolicyConfig` constructor fallout in downstream crates.
</verification>

<output>
After completion, create `.planning/phases/125-configurable-policy-rules-and-audit-trail/125-01-SUMMARY.md`
</output>
