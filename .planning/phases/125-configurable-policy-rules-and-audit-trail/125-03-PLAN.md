---
phase: 125-configurable-policy-rules-and-audit-trail
plan: 03
type: execute
wave: 3
depends_on:
  - 125-01
  - 125-02
files_modified:
  - crates/swarm-policy/src/configurable_gate.rs
  - crates/swarm-policy/src/lib.rs
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/replay/core.inc
autonomous: true
requirements:
  - POLICY-03
must_haves:
  truths:
    - "`ConfigurableApprovalGate` denies by default when the ruleset is empty."
    - "Named YAML rules can allow or deny requests by action, threat class, severity thresholds, UTC hour restrictions, and per-agent rate limits."
    - "Configured runtime construction uses `ConfigurableApprovalGate`, not `StaticApprovalGate`, when loading repository config."
    - "Requests with no matching YAML rule fall through to `StaticApprovalGate`."
  artifacts:
    - path: "crates/swarm-policy/src/configurable_gate.rs"
      provides: "Ordered configurable rule evaluation and per-agent rate limiting."
    - path: "crates/swarm-runtime/src/service.rs"
      provides: "Configured runtime wiring for the new gate."
  key_links:
    - from: "crates/swarm-policy/src/configurable_gate.rs"
      to: "crates/swarm-policy/src/static_gate.rs"
      via: "static fallback on no YAML match"
      pattern: "fallback"
    - from: "crates/swarm-runtime/src/service.rs"
      to: "crates/swarm-policy/src/configurable_gate.rs"
      via: "from_config runtime gate construction"
      pattern: "ConfigurableApprovalGate"
---

<objective>
Implement the configurable policy gate and wire it into configured runtime construction.

Purpose: Move response authorization from hardcoded-only policy into ordered, deployment-owned YAML rules without creating a fail-open path.
Output: `ConfigurableApprovalGate`, unit coverage for its rule semantics, and runtime/service composition that instantiates it from repository config.
</objective>

<context>
@.planning/phases/125-configurable-policy-rules-and-audit-trail/125-CONTEXT.md
@.planning/phases/125-configurable-policy-rules-and-audit-trail/125-RESEARCH.md
@.planning/phases/125-configurable-policy-rules-and-audit-trail/125-VALIDATION.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-policy/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-policy/src/static_gate.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/service.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/replay/core.inc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed configurable-gate unit coverage</name>
  <read_first>
    crates/swarm-policy/src/lib.rs
    crates/swarm-policy/src/static_gate.rs
  </read_first>
  <files>
    crates/swarm-policy/src/configurable_gate.rs
  </files>
  <action>Create the exact tests `configurable_gate_denies_when_rules_are_empty`, `configurable_gate_applies_matching_allow_rule`, `configurable_gate_denies_outside_allowed_hours`, `configurable_gate_enforces_per_agent_rate_limit`, and `configurable_gate_falls_back_to_static_gate_when_no_rule_matches` before wiring the implementation. Keep the tests focused on gate semantics, not runtime service composition.</action>
  <verify>
    <automated>rg -n "configurable_gate_denies_when_rules_are_empty|configurable_gate_applies_matching_allow_rule|configurable_gate_denies_outside_allowed_hours|configurable_gate_enforces_per_agent_rate_limit|configurable_gate_falls_back_to_static_gate_when_no_rule_matches" crates/swarm-policy/src/configurable_gate.rs</automated>
  </verify>
  <done>The new configurable gate file contains exact unit tests for fail-closed empty rules, ordered matching, time windows, agent rate limits, and static fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the configurable gate and wire configured runtime construction</name>
  <read_first>
    crates/swarm-policy/src/configurable_gate.rs
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/replay/core.inc
  </read_first>
  <files>
    crates/swarm-policy/src/configurable_gate.rs
    crates/swarm-policy/src/lib.rs
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/replay/core.inc
  </files>
  <action>Implement ordered YAML rule evaluation in `ConfigurableApprovalGate`, including rule-local UTC hour checks and per-agent one-minute rate limits, deny on empty rulesets, and static fallback when no rule matches. Then switch configured runtime builders to instantiate `ConfigurableApprovalGate` from loaded `PolicyConfig` so repository config actually drives the live runtime path.</action>
  <verify>
    <automated>cargo test -p swarm-policy configurable_gate_applies_matching_allow_rule -- --exact</automated>
  </verify>
  <done>The repository-configured runtime stack now evaluates named YAML policy rules with fail-closed semantics and static fallback.</done>
</task>

</tasks>

<verification>
Run the exact configurable-gate test first, then the full `swarm-policy` suite and a runtime config smoke check to confirm the new gate composes cleanly with runtime builders.
</verification>

<output>
After completion, create `.planning/phases/125-configurable-policy-rules-and-audit-trail/125-03-SUMMARY.md`
</output>
