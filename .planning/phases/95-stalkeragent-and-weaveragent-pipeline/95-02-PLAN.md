---
phase: 95-stalkeragent-and-weaveragent-pipeline
plan: 02
type: execute
wave: 1
depends_on: [95-01]
files_modified:
  - crates/swarm-runtime/src/weaver_agent.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs
autonomous: true
requirements: [MULTI-05, MULTI-07]

must_haves:
  truths:
    - "`WeaverAgent` consumes investigation pheromones and assembles `CorrelatedIncident` records through the existing correlation engine"
    - "Serve mode registers `WhiskerAgent`, `StalkerAgent`, and `WeaverAgent` into the same live registry when the relevant config toggles are enabled"
    - "Integration coverage proves the bounded pipeline: detect -> pheromone deposit -> investigation -> correlation -> incident assembly"
  artifacts:
    - path: "crates/swarm-runtime/src/weaver_agent.rs"
      provides: "WeaverAgent implementation over correlation and incident stores"
      contains: "pub struct WeaverAgent"
    - path: "crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs"
      provides: "End-to-end multi-agent runtime proof"
      contains: "full_multi_agent_pipeline"
  key_links:
    - from: "crates/swarm-runtime/src/weaver_agent.rs"
      to: "CorrelationEngine"
      via: "correlate_hunt against completed investigation bundles"
      pattern: "correlate_hunt"
    - from: "crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs"
      to: "AgentDispatcher"
      via: "bounded dispatcher ticks over Whisker, Stalker, and Weaver"
      pattern: "AgentDispatcher"
---

<objective>
Implement `WeaverAgent` and prove the full multi-agent runtime pipeline end to end.

Purpose: Once investigations exist, the swarm needs a correlation agent that turns those completed investigations into durable incidents. This plan finishes the v1.32 promise by wiring `WeaverAgent` into the same live registry and adding integration coverage that exercises all three agents together.

Output: `weaver_agent.rs`, live serve-mode registration for all three agents, and an integration test proving detect -> investigate -> correlate in bounded dispatcher ticks.
</objective>
