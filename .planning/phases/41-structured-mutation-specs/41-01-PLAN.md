# Plan 41-01

1. Add a new runtime mutation module with durable mutation-spec storage and rendering.
2. Support mutation-spec creation from either a reviewed draft or a materialized candidate.
3. Add explicit variant append and reload commands to `swarmctl`.
4. Cover both source paths with runtime tests and document the operator flow.
