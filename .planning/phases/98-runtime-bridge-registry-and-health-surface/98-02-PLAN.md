---
phase: 98-runtime-bridge-registry-and-health-surface
plan: 02
type: execute
wave: 1
depends_on: [98-01]
files_modified:
  - crates/swarm-runtime/src/detection/metrics.rs
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/tests/ingest_integration.rs
autonomous: true
requirements: [BRIDGE-05]

must_haves:
  truths:
    - "Bridge event counts, error counts, and lag are visible on operator status and `/healthz`"
    - "Prometheus output exposes bridge health and activity per bridge name"
    - "Bridge worker failures degrade bridge health without breaking the existing hot detection path or HTTP ingest surface"
  artifacts:
    - path: "crates/swarm-runtime/src/service.rs"
      provides: "operator-visible bridge health report"
      contains: "OperatorStatusReport"
    - path: "crates/swarm-runtime/src/detection/metrics.rs"
      provides: "Prometheus bridge metrics"
      contains: "bridge"
  key_links:
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-runtime/src/service.rs"
      via: "shared bridge health snapshot surfaced through readiness and operator status"
      pattern: "BridgeHealth"
    - from: "crates/swarm-runtime/src/detection/metrics.rs"
      to: "crates/swarm-runtime/tests/ingest_integration.rs"
      via: "bridge counters and lag rendered on `/metrics` and `/healthz`"
      pattern: "bridge"
---

<objective>
Expose runtime bridge health and activity on the same operator and Prometheus surfaces that already report detector and substrate state.

Purpose: Phase 98 is only complete when operators can tell which bridges are alive, how much telemetry they have processed, and whether one bridge is degraded without guessing from logs.

Output: bridge-aware `/healthz`, operator status, Prometheus metrics, and integration coverage proving degraded bridge health does not corrupt the existing detection path.
</objective>
