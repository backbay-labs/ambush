---
phase: 121-network-connect-detector
plan: 02
type: execute
wave: 2
depends_on: ["121-01"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/src/canary.rs
  - crates/swarm-runtime/src/promotion.rs
  - crates/swarm-runtime/src/replay/core.inc
  - crates/swarm-runtime/src/drafting.rs
  - crates/swarm-runtime/src/mutation.rs
  - crates/swarm-runtime/src/detection/pipeline.rs
  - crates/swarm-runtime/tests/network_connect_integration.rs
  - crates/swarm-runtime/tests/critical_path_integration.rs
  - rulesets/default.yaml
autonomous: true
requirements:
  - NETWORK-01
  - NETWORK-02
  - NETWORK-03

must_haves:
  truths:
    - "`profiles.network_connect` merges defaults plus overrides through the existing runtime profile-resolution path"
    - "`build_composite_detector()`, canary baselines, promotion baselines, and replay candidate manifests can all construct `strategy: network_connect` without widening `DetectionStrategy::evaluate()`"
    - "Threat-intel IP matching for network findings is proven through the existing runtime enrichment path in `detect_and_deposit()`, and roadmap / requirements language is reconciled to that architecture"
    - "Single-strategy runtime coverage proves `network_connect` findings and signed deposits flow through the live detection pipeline"
    - "Existing runtime tooling that enumerates supported strategy families does not reject `network_connect` when canary or promotion features are enabled"
  artifacts:
    - path: "crates/swarm-runtime/src/config.rs"
      provides: "runtime profile merge and validation hook for `NetworkConnectProfile`"
      contains: "network_connect_profile"
    - path: "crates/swarm-runtime/src/control.rs"
      provides: "runtime detector factory support for `network_connect`"
      contains: "\"network_connect\""
    - path: "crates/swarm-runtime/src/canary.rs"
      provides: "canary baseline support for `network_connect` in existing rollout tooling"
      contains: "\"network_connect\""
    - path: "crates/swarm-runtime/src/promotion.rs"
      provides: "promotion baseline support for `network_connect` in existing rollout tooling"
      contains: "\"network_connect\""
    - path: "crates/swarm-runtime/src/replay/core.inc"
      provides: "replay manifest and switchboard support for `network_connect` baseline candidates"
      contains: "\"network_connect\""
    - path: "crates/swarm-runtime/src/drafting.rs"
      provides: "compile-safe handling for the new replay manifest variant in detector drafting flows"
      contains: "DetectorCandidateManifest"
    - path: "crates/swarm-runtime/src/mutation.rs"
      provides: "compile-safe handling for the new replay manifest variant in mutation flows"
      contains: "DetectorCandidateManifest"
    - path: "crates/swarm-runtime/src/detection/pipeline.rs"
      provides: "pipeline test proving network IP threat-intel enrichment"
      contains: "network_findings_are_enriched_by_matching_ip_threat_intel"
    - path: "crates/swarm-runtime/tests/network_connect_integration.rs"
      provides: "single-strategy runtime proof for `network_connect` findings and deposits"
      contains: "network_connect_strategy"
  key_links:
    - from: "crates/swarm-runtime/src/control.rs"
      to: "crates/swarm-whisker/src/network_connect.rs"
      via: "`build_single_detector()` constructs `NetworkConnectDetector` from the merged profile"
      pattern: "network_connect"
    - from: "crates/swarm-runtime/src/detection/pipeline.rs"
      to: "crates/swarm-whisker/src/network_connect.rs"
      via: "`detect_and_deposit()` enriches detector-produced network findings using destination-IP threat-intel matches"
      pattern: "ThreatIntelIndicatorType::IpAddress"
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "phase success criteria and requirement text explicitly describe runtime-owned threat-intel enrichment for `network_connect`"
      pattern: "runtime enrichment"
    - from: "crates/swarm-runtime/src/replay/core.inc"
      to: "crates/swarm-runtime/src/promotion.rs"
      via: "promotion baseline candidates can represent `network_connect` through the replay manifest surface"
      pattern: "DetectorCandidateManifest"
    - from: "rulesets/default.yaml"
      to: "crates/swarm-core/src/config.rs"
      via: "documented supported strategies and detector profile overrides include `network_connect`"
      pattern: "network_connect"
---

<objective>
Wire `NetworkConnectDetector` into the runtime config/factory path and prove Phase 121’s end-to-end behavior with focused runtime tests, while keeping threat-intel enrichment in the existing detection pipeline.

Purpose: Make `strategy: network_connect` build and run in the live runtime path, satisfy `NETWORK-02` through the architecture that already exists rather than forcing a detector-contract expansion, and reconcile the phase contract text to that implementation.
Output: Config/profile wiring, runtime factory support across control, rollout, and replay-manifest tooling, ruleset and requirement documentation, pipeline enrichment coverage for IP intel, and single-strategy runtime integration tests for `NetworkConnect`.
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/121-network-connect-detector/121-CONTEXT.md
@.planning/phases/121-network-connect-detector/121-RESEARCH.md
@.planning/phases/121-network-connect-detector/121-01-SUMMARY.md

<guardrails>
- Implement `NETWORK-02` as runtime-owned enrichment proof: detector emits a network finding first, then `detect_and_deposit()` boosts confidence via `query_threat_intel_entry()` for the destination IP.
- Do not make `DetectionStrategy::evaluate()` async, and do not inject `PheromoneSubstrate` into detector construction.
- Keep Phase 121 focused on live runtime support, but include every existing runtime surface that already hard-codes supported detector families and would reject `network_connect` in the shipped config.
- The workspace still has a known unrelated `evolution::tests::evolution_handoff_persists_pending_launch_packet` failure, so verification must stay targeted.
</guardrails>

<interfaces>
Current `DetectorProfilesConfig` in `crates/swarm-core/src/config.rs`:
```rust
pub struct DetectorProfilesConfig {
    pub suspicious_process_tree: Option<serde_json::Value>,
    pub dns_exfiltration: Option<serde_json::Value>,
    pub lateral_movement: Option<serde_json::Value>,
    pub credential_access: Option<serde_json::Value>,
    pub suspicious_scripting: Option<serde_json::Value>,
    pub persistence: Option<serde_json::Value>,
    pub supply_chain: Option<serde_json::Value>,
}
```

Current runtime enrichment path in `crates/swarm-runtime/src/detection/pipeline.rs`:
```rust
match &event.payload {
    TelemetryPayload::NetworkConnect(connection) => {
        let destination_ip = connection.destination_ip.trim().to_ascii_lowercase();
        if !destination_ip.is_empty() {
            candidates.insert((ThreatIntelIndicatorType::IpAddress, destination_ip));
        }
    }
    // ...
}
```

Current runtime factory seam in `crates/swarm-runtime/src/control.rs`:
```rust
pub fn build_composite_detector(config: &DetectionConfig) -> Result<CompositeDetector, ControlError>;
fn build_single_detector(strategy_name: &str, config: &DetectionConfig) -> Result<Box<dyn DetectionStrategy>, ControlError>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire `network_connect` through config, factory, rollout, and requirement surfaces</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/STATE.md
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/config.rs
    crates/swarm-runtime/src/control.rs
    crates/swarm-runtime/src/canary.rs
    crates/swarm-runtime/src/promotion.rs
    crates/swarm-runtime/src/replay/core.inc
    crates/swarm-runtime/src/drafting.rs
    crates/swarm-runtime/src/mutation.rs
    crates/swarm-runtime/tests/critical_path_integration.rs
    rulesets/default.yaml
  </files>
  <read_first>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/config.rs
    crates/swarm-runtime/src/control.rs
    crates/swarm-runtime/src/canary.rs
    crates/swarm-runtime/src/promotion.rs
    crates/swarm-runtime/src/replay/core.inc
    crates/swarm-runtime/tests/critical_path_integration.rs
    rulesets/default.yaml
    crates/swarm-whisker/src/network_connect.rs
  </read_first>
  <action>
    1. Update `crates/swarm-core/src/config.rs`:
       - add `network_connect: Option<serde_json::Value>` to `DetectorProfilesConfig`
       - keep serde defaults and backward compatibility intact

    2. Update `crates/swarm-runtime/src/config.rs`:
       - import `NetworkConnectProfile`
       - add `pub(crate) fn network_connect_profile(config: &DetectionConfig) -> Result<NetworkConnectProfile, DetectorProfileError>`
       - seed the profile from top-level `high_confidence_threshold` / `medium_confidence_threshold` just like the existing detectors
       - extend both `validate_detector_profiles()` and `validate_all_detector_profiles()` to include `network_connect`
       - add a focused unit test such as `network_connect_profile_merges_overrides` proving raw JSON overrides merge into the base profile correctly without touching unrelated detector profiles

    3. Update `crates/swarm-runtime/src/control.rs`:
       - import `NetworkConnectDetector`
       - extend `build_single_detector()` with a `"network_connect"` match arm that constructs the detector from `network_connect_profile(config)?`
       - keep the detector factory synchronous; do not thread substrate access into construction or evaluation

    4. Update `crates/swarm-runtime/src/canary.rs`, `crates/swarm-runtime/src/promotion.rs`, `crates/swarm-runtime/src/replay/core.inc`, `crates/swarm-runtime/src/drafting.rs`, and `crates/swarm-runtime/src/mutation.rs`:
       - extend the existing supported-detector switchboards / baseline builders so `network_connect` is accepted anywhere the shipped runtime can select a baseline strategy
       - extend `DetectorCandidateManifest` and replay-side family validation so promotion baselines can represent `network_connect` end to end
       - update exhaustive `DetectorCandidateManifest` matches in drafting and mutation code so the new variant compiles cleanly and routes through the existing unsupported-materialization path wherever full materialization is still intentionally out of scope
       - add focused tests or fixture updates that prove these surfaces no longer reject `network_connect`
       - keep rollout behavior otherwise unchanged; broader rollout scoping belongs to Phase 122

    5. Reconcile the Phase 121 contract text:
       - update `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` so `NETWORK-02` describes runtime-owned destination-IP threat-intel enrichment for `network_connect` findings rather than a substrate lookup inside `NetworkConnectDetector::evaluate()`
       - add a short note to `.planning/STATE.md` explaining the architectural rationale so later phases do not regress to the old wording

    6. Update `rulesets/default.yaml`:
       - add `network_connect` to the supported-strategy comment block
       - add a commented example profile override block for `profiles.network_connect` showing `suspicious_ports` and `process_port_allowlist`

    7. Update `crates/swarm-runtime/tests/critical_path_integration.rs`:
       - extend the runtime-strategy factory coverage list to include `"network_connect"`
       - do not broaden this file into a new scenario-fixture phase; a factory-coverage update is sufficient here
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/swarm-team-six && cargo test -p swarm-runtime network_connect_profile_merges_overrides && cargo test -p swarm-runtime --test critical_path_integration composite_detector_factory_covers_all_runtime_strategies && cargo test -p swarm-runtime --lib canary && cargo test -p swarm-runtime --lib promotion && cargo test -p swarm-runtime --lib replay && cargo clippy -p swarm-runtime --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "network_connect: Option<serde_json::Value>" crates/swarm-core/src/config.rs
    - grep -q "fn network_connect_profile" crates/swarm-runtime/src/config.rs
    - grep -q "network_connect_profile_merges_overrides" crates/swarm-runtime/src/config.rs
    - grep -q "profiles.network_connect" rulesets/default.yaml
    - grep -q "\"network_connect\" =>" crates/swarm-runtime/src/control.rs
    - grep -q "\"network_connect\"" crates/swarm-runtime/src/canary.rs
    - grep -q "\"network_connect\"" crates/swarm-runtime/src/promotion.rs
    - grep -q "\"network_connect\"" crates/swarm-runtime/src/replay/core.inc
    - grep -q "DetectorCandidateManifest" crates/swarm-runtime/src/drafting.rs
    - grep -q "DetectorCandidateManifest" crates/swarm-runtime/src/mutation.rs
    - grep -q "\"network_connect\"" crates/swarm-runtime/tests/critical_path_integration.rs
    - grep -q "runtime-owned" .planning/REQUIREMENTS.md
  </acceptance_criteria>
  <done>Runtime config/profile resolution, replay manifests, existing rollout tooling, and the phase contract documents all support `strategy: network_connect` without requiring an async detector-contract rewrite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove runtime-owned IP intel enrichment and single-strategy network detection</name>
  <files>
    crates/swarm-runtime/src/detection/pipeline.rs
    crates/swarm-runtime/tests/network_connect_integration.rs
  </files>
  <read_first>
    crates/swarm-runtime/src/detection/pipeline.rs
    crates/swarm-runtime/tests/persistence_supply_chain_integration.rs
    crates/swarm-whisker/src/network_connect.rs
    crates/swarm-runtime/src/control.rs
  </read_first>
  <behavior>
    - A network finding is enriched when destination-IP threat intel matches
    - Enrichment adds evidence plus confidence boost in the runtime pipeline, not the detector
    - `strategy: network_connect` builds successfully and produces `ThreatClass::CommandAndControl` findings through `detect_and_deposit()`
    - Stateful repeated `NetworkConnect` events can trigger the detector through the live runtime path
  </behavior>
  <action>
    1. Extend `crates/swarm-runtime/src/detection/pipeline.rs` tests with `network_findings_are_enriched_by_matching_ip_threat_intel`:
       - construct a `NetworkConnectDetector` configured to produce a finding on a synthetic event (a custom suspicious-port override is fine)
       - seed `ThreatIntelEntry { indicator_type: IpAddress, value: <destination_ip> }`
       - call `detect_and_deposit()`
       - assert:
         - one finding exists
         - finding confidence increased above its detector-only base
         - evidence includes `threat_intel_matches`
         - deposit confidence reflects the enriched value
       - keep the proof architecture-explicit: the detector should not change for the threat-intel test beyond producing the base finding

    2. Create `crates/swarm-runtime/tests/network_connect_integration.rs` modeled after `persistence_supply_chain_integration.rs`:
       - add a helper that loads `rulesets/default.yaml` and sets `config.detection.strategy = "network_connect"`
       - add `network_connect_strategy_detects_suspicious_port_and_deposits_command_and_control`:
         - configure a `profiles.network_connect` override that guarantees a suspicious-port hit
         - build the detector through `build_composite_detector()`
         - run `detect_and_deposit()`
         - assert:
           - `strategy_id == "network_connect"`
           - `threat_class == ThreatClass::CommandAndControl`
           - one signed deposit is written
       - add `network_connect_strategy_detects_low_jitter_beacon_sequence`:
         - reuse one detector instance across a short repeated event sequence with the same host/process/destination tuple
         - assert that the final event produces the beaconing finding and deposit

    3. Keep the scope tight:
       - do not add cross-strategy `distinct_sources` assertions here; that belongs to Phase 123
       - do not add new replay manifests in this task
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/swarm-team-six && cargo test -p swarm-runtime --test network_connect_integration && cargo test -p swarm-runtime network_findings_are_enriched_by_matching_ip_threat_intel && cargo clippy -p swarm-runtime --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "network_findings_are_enriched_by_matching_ip_threat_intel" crates/swarm-runtime/src/detection/pipeline.rs
    - grep -q "network_connect_strategy_detects_suspicious_port_and_deposits_command_and_control" crates/swarm-runtime/tests/network_connect_integration.rs
    - grep -q "network_connect_strategy_detects_low_jitter_beacon_sequence" crates/swarm-runtime/tests/network_connect_integration.rs
    - cargo test -p swarm-runtime --test network_connect_integration exits 0
    - cargo test -p swarm-runtime network_findings_are_enriched_by_matching_ip_threat_intel exits 0
  </acceptance_criteria>
  <done>Focused runtime tests prove `network_connect` runs through the live pipeline and that destination-IP threat-intel boosts confidence via the existing enrichment layer rather than a detector-contract rewrite.</done>
</task>

</tasks>

<verification>
1. `cargo test -p swarm-runtime --test network_connect_integration` passes
2. `cargo test -p swarm-runtime network_findings_are_enriched_by_matching_ip_threat_intel` passes
3. `cargo test -p swarm-runtime --test critical_path_integration composite_detector_factory_covers_all_runtime_strategies` passes
4. `cargo clippy -p swarm-runtime --all-targets -- -D warnings` is clean
5. `strategy: network_connect` builds through `build_composite_detector()` with merged profile overrides
6. Network destination-IP intel hits enrich confidence and evidence in the runtime pipeline without changing the detector contract
7. Existing canary and promotion baseline builders no longer reject `network_connect`
</verification>

<success_criteria>
- `DetectorProfilesConfig` and runtime profile resolution support `profiles.network_connect`
- `build_composite_detector()` can construct `network_connect` from validated config
- The default ruleset documents `network_connect` as a supported runtime strategy and shows profile-override shape
- Runtime tests prove a single-strategy `network_connect` finding flows through to a signed `CommandAndControl` deposit
- Runtime tests prove `ThreatIntelIndicatorType::IpAddress` enrichment boosts network-finding confidence in `detect_and_deposit()`
- Existing rollout tooling accepts `network_connect` anywhere the shipped runtime already enumerates supported detector families
- Phase 121 stays within NETWORK-01/02/03 scope without expanding into Phase 123 cross-strategy proof work
</success_criteria>

<output>
After completion, create `.planning/phases/121-network-connect-detector/121-02-SUMMARY.md`
</output>
