---
phase: 124-pounceagent-core-and-de-escalation
plan: 02
type: execute
wave: 2
depends_on:
  - 124-01
files_modified:
  - crates/swarm-runtime/src/pounce_agent.rs
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/tests/pounceagent_integration.rs
autonomous: true
requirements:
  - POUNCE-01
  - POUNCE-02
  - POUNCE-03
must_haves:
  truths:
    - "PounceAgent emits response requests only for elevated sessions, not on every repeated tick."
    - "PounceAgent skips same-tick duplicates when peer findings already cover the same response scope."
    - "Response selection comes from repo-owned playbook config instead of ad hoc hardcoded branching."
  artifacts:
    - path: "crates/swarm-runtime/src/pounce_agent.rs"
      provides: "PounceAgent implementation with session-bounded dedupe and playbook selection."
    - path: "crates/swarm-runtime/tests/pounceagent_integration.rs"
      provides: "Automated proof for emission, peer-finding dedupe, and playbook matching."
  key_links:
    - from: "crates/swarm-runtime/src/pounce_agent.rs"
      to: "crates/swarm-policy/src/static_gate.rs"
      via: "shared response-action scope helper"
      pattern: "scope_for_response_action"
    - from: "crates/swarm-runtime/src/pounce_agent.rs"
      to: "crates/swarm-core/src/config.rs"
      via: "ResponsePlaybookConfig selection"
      pattern: "ResponsePlaybookConfig"
---

<objective>
Implement the autonomous PounceAgent decision loop itself: deterministic playbook selection, lineage-preserving evidence, and duplicate suppression bounded to the current elevated-mode session.

Purpose: Give the runtime a real response-emitting agent before routing/execution wiring begins.
Output: `PounceAgent` source file plus focused integration coverage for POUNCE-01 through POUNCE-03.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-CONTEXT.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-RESEARCH.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-VALIDATION.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/types.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-policy/src/static_gate.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/whisker_agent.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build PounceAgent around elevated-session dedupe and deterministic playbook selection</name>
  <read_first>
    crates/swarm-core/src/agent.rs
    crates/swarm-core/src/config.rs
    crates/swarm-core/src/types.rs
    crates/swarm-policy/src/static_gate.rs
    crates/swarm-runtime/src/whisker_agent.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/pounce_agent.rs
    crates/swarm-runtime/src/lib.rs
  </files>
  <behavior>
    - Test 1: elevated `Alert` and `Incident` sessions produce `SwarmAction::RequestResponse` when the configured playbook matches the escalation context.
    - Test 2: repeated ticks inside the same elevated session do not re-emit the same escalation/action tuple once the bounded handled set has recorded it.
    - Test 3: returning to `SwarmMode::Normal` clears the handled session state so a later elevated session can act again.
  </behavior>
  <action>Create `crates/swarm-runtime/src/pounce_agent.rs` defining `PounceAgent` as a normal `SwarmAgent` with `AgentRole::Pouncer`, no direct `SwarmRuntime` reference, and an internal bounded seen-set keyed to the current elevated-mode session (`env.mode` plus `env.mode_transition_at()` or a stronger repo-visible session key if found during implementation). Implement deterministic playbook lookup from `ResponsePlaybookConfig`, build `SwarmAction::RequestResponse` values using real lineage-bearing `hunt_id` and `evidence`, and use the shared scope helper from `swarm-policy` so duplicate suppression matches policy scope semantics. Export the module from `crates/swarm-runtime/src/lib.rs`; do not introduce synthetic `hunt_id` values or a second scope model.</action>
  <acceptance_criteria>`rg -n "struct PounceAgent|AgentRole::Pouncer|RequestResponse|scope_for_response_action" crates/swarm-runtime/src/pounce_agent.rs` finds the agent seam, and `cargo test -p swarm-runtime --test pounceagent_integration response_playbook_selects_actions_by_threat_severity_and_confidence -- --exact` passes against the repo-owned playbook mapping.</acceptance_criteria>
  <verify>
    <automated>cargo test -p swarm-runtime --test pounceagent_integration response_playbook_selects_actions_by_threat_severity_and_confidence -- --exact</automated>
  </verify>
  <done>PounceAgent exists as a standalone swarm agent with deterministic playbook selection, shared scope semantics, and bounded elevated-session dedupe state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focused PounceAgent integration coverage for emission and peer-finding suppression</name>
  <read_first>
    crates/swarm-runtime/src/pounce_agent.rs
    .planning/phases/124-pounceagent-core-and-de-escalation/124-VALIDATION.md
    crates/swarm-runtime/tests/escalation_integration.rs
  </read_first>
  <files>
    crates/swarm-runtime/tests/pounceagent_integration.rs
  </files>
  <behavior>
    - Test 1: `pounceagent_emits_request_response_for_alert_and_incident` proves only elevated modes emit response requests.
    - Test 2: `pounceagent_skips_scope_present_in_peer_findings` proves same-tick peer findings suppress a second response for the same target scope.
    - Test 3: `response_playbook_selects_actions_by_threat_severity_and_confidence` proves ordered actions come from config, not hardcoded defaults.
  </behavior>
  <action>Create `crates/swarm-runtime/tests/pounceagent_integration.rs` with the exact test names from the validation contract and shared fixtures for `SwarmEnvironment`, representative pheromone/escalation context, and peer findings. Assert on emitted `SwarmAction::RequestResponse` values, ordered action sequences, and no-op behavior when the duplicate scope already exists in `env.peer_findings`. Keep these tests focused on agent behavior only; dispatcher routing belongs to Plan 03.</action>
  <acceptance_criteria>`cargo test -p swarm-runtime --test pounceagent_integration pounceagent_emits_request_response_for_alert_and_incident -- --exact` and `cargo test -p swarm-runtime --test pounceagent_integration pounceagent_skips_scope_present_in_peer_findings -- --exact` both pass, and the test file contains the three exact validation test names.</acceptance_criteria>
  <verify>
    <automated>cargo test -p swarm-runtime --test pounceagent_integration pounceagent_emits_request_response_for_alert_and_incident -- --exact && cargo test -p swarm-runtime --test pounceagent_integration pounceagent_skips_scope_present_in_peer_findings -- --exact</automated>
  </verify>
  <done>The new integration file proves elevated-mode emission, same-tick scope dedupe, and playbook-driven action ordering with no dispatcher/runtime shortcuts.</done>
</task>

</tasks>

<verification>
Run the three `pounceagent_integration` exact tests first, then `cargo test -p swarm-runtime --test pounceagent_integration` to confirm there is no hidden cross-test state leak in the bounded seen-set logic.
</verification>

<success_criteria>
PounceAgent exists, behaves deterministically from config, preserves lineage in emitted requests, and cannot spam duplicate responses within a single elevated session or same tick.
</success_criteria>

<output>
After completion, create `.planning/phases/124-pounceagent-core-and-de-escalation/124-02-SUMMARY.md`
</output>
