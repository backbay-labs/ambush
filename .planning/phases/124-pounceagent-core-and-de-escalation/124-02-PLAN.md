---
phase: 124-pounceagent-core-and-de-escalation
plan: 02
type: execute
wave: 3
depends_on:
  - 124-01
  - 124-05
files_modified:
  - crates/swarm-core/src/agent.rs
  - crates/swarm-runtime/src/escalation.rs
  - crates/swarm-runtime/tests/escalation_integration.rs
autonomous: true
requirements:
  - DEESC-01
  - DEESC-02
must_haves:
  truths:
    - "Swarm mode can move back down to Normal after sustained below-threshold pressure."
    - "De-escalation does not flap immediately on the first quiet evaluation."
    - "Downward mode transitions clear the old triggering threat lineage when the runtime relaxes."
  artifacts:
    - path: "crates/swarm-core/src/agent.rs"
      provides: "Explicit downward transition API on SwarmModeState."
    - path: "crates/swarm-runtime/src/escalation.rs"
      provides: "Cooldown-aware de-escalation logic in ConcentrationMonitor."
    - path: "crates/swarm-runtime/tests/escalation_integration.rs"
      provides: "Automated proof that cooldown-based de-escalation works."
  key_links:
    - from: "crates/swarm-runtime/src/escalation.rs"
      to: "crates/swarm-core/src/agent.rs"
      via: "SwarmModeState::transition_down"
      pattern: "transition_down"
    - from: "crates/swarm-runtime/src/escalation.rs"
      to: "crates/swarm-core/src/config.rs"
      via: "PheromoneConfig.deescalation_cooldown_secs"
      pattern: "deescalation_cooldown_secs"
---

<objective>
Implement the de-escalation behavior itself after the config seam is in place.

Purpose: Keep cooldown-driven mode relaxation isolated from config migration work so the behavior change is easy to verify and hard to regress.
Output: Explicit downward mode transitions plus integration coverage for cooldown-gated de-escalation.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-CONTEXT.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-RESEARCH.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-VALIDATION.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/escalation.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/tests/escalation_integration.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit downward transitions on `SwarmModeState`</name>
  <read_first>
    crates/swarm-core/src/agent.rs
    .planning/phases/124-pounceagent-core-and-de-escalation/124-VALIDATION.md
  </read_first>
  <files>
    crates/swarm-core/src/agent.rs
  </files>
  <behavior>
    - Test 1: `mode_state_transition_down_clears_triggering_threat_class` proves `transition_down()` lowers mode, updates `last_transition_at`, and clears `triggering_threat_class`.
  </behavior>
  <action>Add `SwarmModeState::transition_down(mode, now)` in `crates/swarm-core/src/agent.rs` and keep `transition_to()` upward-only. Extend the existing unit coverage in the same module or test section with the exact validation test name. Do not weaken upward escalation semantics or allow `transition_down()` to no-op-clear state when the target mode is not lower than current.</action>
  <verify>
    <automated>cargo test -p swarm-core agent::tests::mode_state_transition_down_clears_triggering_threat_class -- --exact</automated>
  </verify>
  <done>`SwarmModeState` has an explicit downward-only API and automated coverage proving it updates timestamps while clearing the old trigger class.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate de-escalation on sustained below-threshold cooldown</name>
  <read_first>
    crates/swarm-runtime/src/escalation.rs
    crates/swarm-runtime/tests/escalation_integration.rs
    .planning/phases/124-pounceagent-core-and-de-escalation/124-VALIDATION.md
  </read_first>
  <files>
    crates/swarm-runtime/src/escalation.rs
    crates/swarm-runtime/tests/escalation_integration.rs
  </files>
  <behavior>
    - Test 1: `concentration_monitor_deescalates_after_cooldown` proves `evaluate_all()` stays elevated until the below-threshold dwell time reaches `deescalation_cooldown_secs`, then returns to `Normal`.
    - Test 2: the same integration path proves there is no immediate one-tick flap after escalation.
  </behavior>
  <action>In `crates/swarm-runtime/src/escalation.rs`, track below-threshold dwell time for the active elevated mode, call `transition_down()` only when all threat classes remain below alert threshold for the configured cooldown, and reset dwell tracking whenever a fresh escalation occurs. Extend `crates/swarm-runtime/tests/escalation_integration.rs` with the exact validation test name; do not implement a one-tick downgrade or move cooldown behavior into policy/governance code.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test escalation_integration concentration_monitor_deescalates_after_cooldown -- --exact</automated>
  </verify>
  <done>De-escalation is explicit, cooldown-gated, and covered by integration proof that blocks oscillation on the first quiet evaluation.</done>
</task>

</tasks>

<verification>
Run the exact unit test first, then `cargo test -p swarm-runtime --test escalation_integration` to confirm the cooldown path does not regress adjacent escalation behavior.
</verification>

<success_criteria>
The runtime can safely return from Alert or Incident to Normal only after sustained quiet time, and the downward transition semantics are explicit in code and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/124-pounceagent-core-and-de-escalation/124-02-SUMMARY.md`
</output>
