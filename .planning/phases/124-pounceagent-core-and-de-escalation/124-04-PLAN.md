---
phase: 124-pounceagent-core-and-de-escalation
plan: 04
type: execute
wave: 4
depends_on:
  - 124-03
files_modified:
  - crates/swarm-runtime/src/dispatcher.rs
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
  - crates/swarm-runtime/tests/dispatch_integration.rs
autonomous: true
requirements:
  - POUNCE-04
  - POUNCE-05
  - POLICY-01
must_haves:
  truths:
    - "A PounceAgent request flows through the existing policy and guard pipeline instead of stopping at a dispatcher no-op."
    - "Dry-run autonomous responses use the same runtime path and produce simulated receipts."
    - "Expired capability leases fail closed before any adapter execution."
    - "Receipt and audit output preserve the originating `hunt_id` and lineage-bearing evidence end-to-end."
  artifacts:
    - path: "crates/swarm-runtime/src/dispatcher.rs"
      provides: "Dispatcher-owned routing seam for RequestResponse actions."
    - path: "crates/swarm-runtime/src/lib.rs"
      provides: "Fail-closed lease expiry enforcement in canonical runtime execution."
    - path: "crates/swarm-runtime/tests/dispatch_integration.rs"
      provides: "Automated proof of routed execution, dry-run parity, expired-lease denial, and lineage preservation."
  key_links:
    - from: "crates/swarm-runtime/src/dispatcher.rs"
      to: "crates/swarm-runtime/src/lib.rs"
      via: "authorize_and_execute"
      pattern: "authorize_and_execute"
    - from: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      to: "crates/swarm-runtime/src/dispatcher.rs"
      via: "dispatcher router wiring and PounceAgent registration"
      pattern: "PounceAgent"
    - from: "crates/swarm-runtime/src/lib.rs"
      to: "crates/swarm-runtime/tests/dispatch_integration.rs"
      via: "ResponseReceipt audit assertions"
      pattern: "hunt_id|evidence"
---

<objective>
Route autonomous response requests through the canonical runtime path, enforce lease expiry before any adapter call, and prove receipt lineage end-to-end.

Purpose: Close the detect-to-respond loop without bypassing policy, guards, receipts, dry-run behavior, or audit lineage.
Output: Dispatcher routing seam, runtime lease enforcement, live PounceAgent registration, and dispatch integration tests for Phase 124’s safety and lineage invariants.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-CONTEXT.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-RESEARCH.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-VALIDATION.md
@.planning/phases/116-agent-safety-hardening/116-02-SUMMARY.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/dispatcher.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/bin/swarm_detect.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/tests/dispatch_integration.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend dispatch integration coverage before wiring the runtime path</name>
  <read_first>
    crates/swarm-runtime/tests/dispatch_integration.rs
    .planning/phases/124-pounceagent-core-and-de-escalation/124-VALIDATION.md
    crates/swarm-runtime/src/pounce_agent.rs
  </read_first>
  <files>
    crates/swarm-runtime/tests/dispatch_integration.rs
  </files>
  <action>Extend `crates/swarm-runtime/tests/dispatch_integration.rs` with the exact validation tests `request_response_routes_through_authorize_and_execute`, `pounceagent_dry_run_routes_through_runtime_path`, and `expired_capability_lease_fails_closed_before_execution`, plus `receipt_preserves_original_hunt_id_and_lineage_evidence`. The lineage test must assert that the receipt and its audit-visible evidence preserve the original detection-backed `hunt_id` and lineage-bearing evidence payload instead of substituting synthetic identifiers. Keep this file focused on routed runtime behavior; do not duplicate the agent-local dedupe assertions from Plan 03.</action>
  <verify>
    <automated>rg -n "request_response_routes_through_authorize_and_execute|pounceagent_dry_run_routes_through_runtime_path|expired_capability_lease_fails_closed_before_execution|receipt_preserves_original_hunt_id_and_lineage_evidence" crates/swarm-runtime/tests/dispatch_integration.rs</automated>
  </verify>
  <done>The dispatch integration file contains the exact routed-execution, dry-run, lease-expiry, and lineage-preservation test cases that the implementation task must satisfy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route `RequestResponse` through runtime execution and enforce fail-closed leases</name>
  <read_first>
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-runtime/tests/dispatch_integration.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-runtime/tests/dispatch_integration.rs
  </files>
  <behavior>
    - Test 1: `request_response_routes_through_authorize_and_execute` proves dispatcher-routed autonomous requests hit the canonical runtime path.
    - Test 2: `pounceagent_dry_run_routes_through_runtime_path` proves detect-only mode still exercises policy, lease, guard, and receipt generation and returns `ResponseStatus::Simulated`.
    - Test 3: `expired_capability_lease_fails_closed_before_execution` proves `CapabilityLease.expires_at_ms <= now_ms` returns `ApprovalError::Denied("capability lease expired")` before any adapter call.
    - Test 4: `receipt_preserves_original_hunt_id_and_lineage_evidence` proves the emitted receipt/audit output carries the original detection-backed `hunt_id` and lineage-bearing evidence end-to-end.
  </behavior>
  <action>Replace the `SwarmAction::RequestResponse` no-op arm in `AgentDispatcher::apply_actions()` with a dispatcher-owned routing seam that can call into the configured runtime stack without leaking `SwarmRuntime<P, E>` generics into agents. Wire that seam from the live startup path in `crates/swarm-runtime/src/bin/swarm_detect.rs`, using `IngestState` accessors as needed, and register `PounceAgent` beside the existing Whisker/Stalker/Weaver agents only after the router is available. In `crates/swarm-runtime/src/lib.rs`, add explicit lease expiry enforcement immediately after `issue_lease()` and before any `response.execute()` call. Preserve the existing runtime-mode mapping to `ExecutionMode::DryRun`, keep `RequestResponse` centralized in the dispatcher, and ensure the receipt/audit path preserves the original `hunt_id` plus lineage-bearing evidence payload rather than minting autonomous-only identifiers.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test dispatch_integration request_response_routes_through_authorize_and_execute -- --exact && cargo test -p swarm-runtime --test dispatch_integration pounceagent_dry_run_routes_through_runtime_path -- --exact && cargo test -p swarm-runtime --test dispatch_integration expired_capability_lease_fails_closed_before_execution -- --exact && cargo test -p swarm-runtime --test dispatch_integration receipt_preserves_original_hunt_id_and_lineage_evidence -- --exact</automated>
  </verify>
  <done>Autonomous responses traverse the same runtime path in dry-run and live modes, expired leases fail closed before adapter execution, and routed receipts preserve original hunt lineage end-to-end.</done>
</task>

</tasks>

<verification>
Run the four exact `dispatch_integration` tests first, then `cargo test -p swarm-runtime --test dispatch_integration` to confirm routed execution, dry-run parity, lease enforcement, and lineage preservation coexist cleanly.
</verification>

<success_criteria>
`RequestResponse` is live-routed, PounceAgent is registered in the runtime startup path, dry-run receipts stay simulated through the canonical runtime path, expired leases are denied before execution, and receipt lineage remains traceable to the original hunt evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/124-pounceagent-core-and-de-escalation/124-04-SUMMARY.md`
</output>
