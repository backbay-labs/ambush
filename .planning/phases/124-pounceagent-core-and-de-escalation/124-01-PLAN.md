---
phase: 124-pounceagent-core-and-de-escalation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-policy/src/static_gate.rs
  - rulesets/default.yaml
  - crates/swarm-runtime/src/detection/pipeline.rs
  - crates/swarm-runtime/src/stalker_agent.rs
  - crates/swarm-runtime/src/whisker_agent.rs
  - crates/swarm-runtime/src/dispatcher.rs
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/src/evidence.rs
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/http/core.inc
  - crates/swarm-runtime/src/strategy.rs
  - crates/swarm-runtime/src/canary.rs
  - crates/swarm-runtime/src/promotion.rs
  - crates/swarm-runtime/tests/multi_strategy_integration.rs
  - crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs
  - crates/swarm-runtime/tests/operational_hardening_integration.rs
autonomous: true
requirements:
  - POUNCE-03
  - DEESC-02
must_haves:
  truths:
    - "The runtime config exposes cooldown and response-playbook seams before any PounceAgent or de-escalation logic lands."
    - "Policy and agent code share one response-action scope model instead of inventing separate dedupe semantics."
    - "Owned runtime/test constructors compile against the new `PheromoneConfig` shape immediately after the config seam lands."
  artifacts:
    - path: "crates/swarm-core/src/config.rs"
      provides: "Response playbook contracts and de-escalation cooldown config."
    - path: "crates/swarm-policy/src/static_gate.rs"
      provides: "Public response-action scope helper shared by policy and PounceAgent."
    - path: "rulesets/default.yaml"
      provides: "Repo-owned default playbook and cooldown examples."
  key_links:
    - from: "crates/swarm-core/src/config.rs"
      to: "rulesets/default.yaml"
      via: "ResponsePlaybookConfig serialization and defaults"
      pattern: "response_playbook"
    - from: "crates/swarm-policy/src/static_gate.rs"
      to: "crates/swarm-runtime/src/pounce_agent.rs"
      via: "scope_for_response_action"
      pattern: "scope_for_response_action"
---

<objective>
Land the shared config, policy-helper, and compile-safe constructor seams that every later Phase 124 slice builds on.

Purpose: Separate repo-owned playbook/cooldown setup from later behavior changes so PounceAgent and de-escalation implementation do not reopen config churn.
Output: Stable config contracts, one shared scope helper, default ruleset coverage, and updated runtime/test literals that compile with the new config shape.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-CONTEXT.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-RESEARCH.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-VALIDATION.md
@.planning/phases/122-cross-strategy-pheromone-signals-and-rollout-scoping/122-01-SUMMARY.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-policy/src/static_gate.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/rulesets/default.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared Phase 124 config contracts and the policy-owned scope helper</name>
  <read_first>
    crates/swarm-core/src/config.rs
    crates/swarm-policy/src/static_gate.rs
    rulesets/default.yaml
    .planning/phases/124-pounceagent-core-and-de-escalation/124-CONTEXT.md
  </read_first>
  <files>
    crates/swarm-core/src/config.rs
    crates/swarm-policy/src/static_gate.rs
    rulesets/default.yaml
  </files>
  <action>Add `deescalation_cooldown_secs: i64` to `PheromoneConfig` with repo-owned defaults and serde support, and add `ResponsePlaybookConfig` plus any supporting config structs required to map `(ThreatClass, Severity, confidence range)` to ordered `ResponseAction` sequences. Update `rulesets/default.yaml` with a concrete cooldown value and playbook examples. Expose a reusable public helper in `crates/swarm-policy/src/static_gate.rs` for deriving action scope so policy and PounceAgent share one scope model; do not add YAML policy-rule evaluation or TomAgent seams here because those are deferred to later phases.</action>
  <verify>
    <automated>cargo check -p swarm-core -p swarm-policy</automated>
  </verify>
  <done>The config contracts exist in `swarm-core`, the shared scope helper is public in `swarm-policy`, and the default ruleset documents the new cooldown/playbook seams without introducing deferred Phase 125 logic.</done>
</task>

<task type="auto">
  <name>Task 2: Update owned runtime and test constructors to the new config shape</name>
  <read_first>
    crates/swarm-runtime/src/detection/pipeline.rs
    crates/swarm-runtime/src/stalker_agent.rs
    crates/swarm-runtime/src/whisker_agent.rs
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/control.rs
    crates/swarm-runtime/src/evidence.rs
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/http/core.inc
    crates/swarm-runtime/src/strategy.rs
    crates/swarm-runtime/src/canary.rs
    crates/swarm-runtime/src/promotion.rs
    crates/swarm-runtime/tests/multi_strategy_integration.rs
    crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs
    crates/swarm-runtime/tests/operational_hardening_integration.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/detection/pipeline.rs
    crates/swarm-runtime/src/stalker_agent.rs
    crates/swarm-runtime/src/whisker_agent.rs
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/control.rs
    crates/swarm-runtime/src/evidence.rs
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/http/core.inc
    crates/swarm-runtime/src/strategy.rs
    crates/swarm-runtime/src/canary.rs
    crates/swarm-runtime/src/promotion.rs
    crates/swarm-runtime/tests/multi_strategy_integration.rs
    crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs
    crates/swarm-runtime/tests/operational_hardening_integration.rs
  </files>
  <action>Update every owned `PheromoneConfig { ... }` literal and related helper fixture in the listed runtime/test files so the new cooldown field compiles immediately across the existing runtime surface. Keep this slice mechanical: fix constructor shape, default builders, and any import fallout needed for the shared helper, but do not implement `transition_down()`, PounceAgent behavior, or dispatcher runtime routing in this task.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test escalation_integration --no-run</automated>
  </verify>
  <done>All listed runtime/test constructors compile against the updated `PheromoneConfig` shape, leaving later plans free to focus on behavior rather than missing-field churn.</done>
</task>

</tasks>

<verification>
Run `cargo check -p swarm-core -p swarm-policy` after Task 1, then `cargo test -p swarm-runtime --test escalation_integration --no-run` after Task 2 to prove the config seam is live and compile-safe before behavior plans begin.
</verification>

<success_criteria>
Phase 124 has stable playbook/cooldown config contracts, one shared action-scope helper, and no lingering runtime fixture breakage from the new config shape.
</success_criteria>

<output>
After completion, create `.planning/phases/124-pounceagent-core-and-de-escalation/124-01-SUMMARY.md`
</output>
