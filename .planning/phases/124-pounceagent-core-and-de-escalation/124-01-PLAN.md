---
phase: 124-pounceagent-core-and-de-escalation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/swarm-core/src/agent.rs
  - crates/swarm-core/src/config.rs
  - crates/swarm-policy/src/static_gate.rs
  - crates/swarm-runtime/src/escalation.rs
  - crates/swarm-runtime/tests/escalation_integration.rs
  - crates/swarm-runtime/src/detection/pipeline.rs
  - crates/swarm-runtime/src/stalker_agent.rs
  - crates/swarm-runtime/src/whisker_agent.rs
  - crates/swarm-runtime/src/dispatcher.rs
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/src/evidence.rs
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/http/core.inc
  - crates/swarm-runtime/src/strategy.rs
  - crates/swarm-runtime/src/canary.rs
  - crates/swarm-runtime/src/promotion.rs
  - crates/swarm-runtime/tests/multi_strategy_integration.rs
  - crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs
  - crates/swarm-runtime/tests/operational_hardening_integration.rs
  - rulesets/default.yaml
autonomous: true
requirements:
  - DEESC-01
  - DEESC-02
must_haves:
  truths:
    - "Swarm mode can move back down to Normal after sustained below-threshold pressure."
    - "De-escalation does not flap immediately on a single quiet tick."
    - "The runtime config exposes the cooldown and response-playbook seams needed by later PounceAgent work."
  artifacts:
    - path: "crates/swarm-core/src/agent.rs"
      provides: "Explicit downward transition API on SwarmModeState."
    - path: "crates/swarm-core/src/config.rs"
      provides: "De-escalation cooldown config and response playbook config contracts."
    - path: "crates/swarm-runtime/src/escalation.rs"
      provides: "Cooldown-aware de-escalation logic in ConcentrationMonitor."
    - path: "crates/swarm-runtime/tests/escalation_integration.rs"
      provides: "Automated proof that cooldown-based de-escalation works."
  key_links:
    - from: "crates/swarm-runtime/src/escalation.rs"
      to: "crates/swarm-core/src/agent.rs"
      via: "SwarmModeState::transition_down"
      pattern: "transition_down"
    - from: "crates/swarm-runtime/src/escalation.rs"
      to: "crates/swarm-core/src/config.rs"
      via: "PheromoneConfig.deescalation_cooldown_secs"
      pattern: "deescalation_cooldown_secs"
---

<objective>
Land the de-escalation foundation and the Phase 124 config seams before PounceAgent or dispatcher routing work starts.

Purpose: Make downward mode transitions explicit and cooldown-gated, while adding repo-owned config contracts that later plans consume directly.
Output: Downward mode APIs, cooldown-aware concentration monitoring, shared response-scope helper, and updated config/defaults with compiling runtime fixtures.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-CONTEXT.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-RESEARCH.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-VALIDATION.md
@.planning/phases/116-agent-safety-hardening/116-02-SUMMARY.md
@.planning/phases/122-cross-strategy-pheromone-signals-and-rollout-scoping/122-01-SUMMARY.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-policy/src/static_gate.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/escalation.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/tests/escalation_integration.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 124 config seams and compile-safe shared helpers</name>
  <read_first>
    crates/swarm-core/src/config.rs
    crates/swarm-policy/src/static_gate.rs
    rulesets/default.yaml
    crates/swarm-runtime/src/detection/pipeline.rs
    crates/swarm-runtime/src/stalker_agent.rs
    crates/swarm-runtime/src/whisker_agent.rs
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/control.rs
    crates/swarm-runtime/src/evidence.rs
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/http/core.inc
    crates/swarm-runtime/src/strategy.rs
    crates/swarm-runtime/src/canary.rs
    crates/swarm-runtime/src/promotion.rs
    crates/swarm-runtime/tests/multi_strategy_integration.rs
    crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs
    crates/swarm-runtime/tests/operational_hardening_integration.rs
  </read_first>
  <files>
    crates/swarm-core/src/config.rs
    crates/swarm-policy/src/static_gate.rs
    rulesets/default.yaml
    crates/swarm-runtime/src/detection/pipeline.rs
    crates/swarm-runtime/src/stalker_agent.rs
    crates/swarm-runtime/src/whisker_agent.rs
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/control.rs
    crates/swarm-runtime/src/evidence.rs
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/http/core.inc
    crates/swarm-runtime/src/strategy.rs
    crates/swarm-runtime/src/canary.rs
    crates/swarm-runtime/src/promotion.rs
    crates/swarm-runtime/tests/multi_strategy_integration.rs
    crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs
    crates/swarm-runtime/tests/operational_hardening_integration.rs
  </files>
  <action>Add `deescalation_cooldown_secs: i64` to `PheromoneConfig` with a repo-owned default and serde support, and add the Phase 124 `ResponsePlaybookConfig` contract in `crates/swarm-core/src/config.rs` so later plans can map `(ThreatClass, Severity, confidence range)` to ordered `ResponseAction` sequences without hardcoding. Update `rulesets/default.yaml` with a concrete default playbook section and cooldown value. Expose a reusable public helper in `crates/swarm-policy/src/static_gate.rs` for response-action scope derivation so PounceAgent and policy share one scope model. Update every direct `PheromoneConfig { ... }` literal in the listed runtime/test files so the workspace compiles immediately after the new field lands; do not leave the new config seam partially wired.</action>
  <acceptance_criteria>`rg -n "deescalation_cooldown_secs|ResponsePlaybookConfig|scope_for_response_action" crates/swarm-core/src/config.rs crates/swarm-policy/src/static_gate.rs rulesets/default.yaml` finds the new seams, and `cargo test -p swarm-runtime --test escalation_integration --no-run` succeeds without missing-field compiler errors from `PheromoneConfig` literals.</acceptance_criteria>
  <verify>
    <automated>cargo test -p swarm-runtime --test escalation_integration --no-run</automated>
  </verify>
  <done>The config contract exists, the default ruleset documents it, the shared scope helper is public, and all owned runtime/test constructors compile against the new `PheromoneConfig` shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement explicit downward transitions and cooldown-gated de-escalation</name>
  <read_first>
    crates/swarm-core/src/agent.rs
    crates/swarm-runtime/src/escalation.rs
    crates/swarm-runtime/tests/escalation_integration.rs
    .planning/phases/124-pounceagent-core-and-de-escalation/124-VALIDATION.md
  </read_first>
  <files>
    crates/swarm-core/src/agent.rs
    crates/swarm-runtime/src/escalation.rs
    crates/swarm-runtime/tests/escalation_integration.rs
  </files>
  <behavior>
    - Test 1: `mode_state_transition_down_clears_triggering_threat_class` proves `transition_down()` lowers mode, updates `last_transition_at`, and clears `triggering_threat_class`.
    - Test 2: `concentration_monitor_deescalates_after_cooldown` proves `evaluate_all()` stays elevated until the below-threshold dwell time reaches `deescalation_cooldown_secs`, then returns to `Normal`.
    - Test 3: the same integration path proves no immediate flap on the first quiet evaluation after escalation.
  </behavior>
  <action>Add `SwarmModeState::transition_down(mode, now)` in `crates/swarm-core/src/agent.rs` and keep `transition_to()` upward-only. In `crates/swarm-runtime/src/escalation.rs`, track below-threshold dwell for active elevated mode, call `transition_down()` only when all threat classes remain below alert threshold for the configured cooldown, and clear/reset dwell tracking whenever a fresh escalation event is observed. Extend `crates/swarm-runtime/tests/escalation_integration.rs` with exact coverage named in the validation contract; do not implement a one-tick downgrade or change upward escalation semantics.</action>
  <acceptance_criteria>`cargo test -p swarm-core mode_state_transition_down_clears_triggering_threat_class -- --exact` passes, `cargo test -p swarm-runtime --test escalation_integration concentration_monitor_deescalates_after_cooldown -- --exact` passes, and `rg -n "transition_down|deescalation_cooldown_secs" crates/swarm-core/src/agent.rs crates/swarm-runtime/src/escalation.rs` finds the explicit downward path.</acceptance_criteria>
  <verify>
    <automated>cargo test -p swarm-core mode_state_transition_down_clears_triggering_threat_class -- --exact && cargo test -p swarm-runtime --test escalation_integration concentration_monitor_deescalates_after_cooldown -- --exact</automated>
  </verify>
  <done>Downward transitions are explicit, cooldown-gated, and covered by automated unit/integration tests that enforce non-flapping behavior.</done>
</task>

</tasks>

<verification>
Run the plan-targeted de-escalation checks first, then run `cargo test -p swarm-runtime --test escalation_integration` to confirm no adjacent escalation regression slipped in with the cooldown logic.
</verification>

<success_criteria>
The Phase 124 cooldown and transition seams exist in code and config, `SwarmModeState` can de-escalate safely, and later plans can consume a stable playbook/scope contract without reopening config work.
</success_criteria>

<output>
After completion, create `.planning/phases/124-pounceagent-core-and-de-escalation/124-01-SUMMARY.md`
</output>
