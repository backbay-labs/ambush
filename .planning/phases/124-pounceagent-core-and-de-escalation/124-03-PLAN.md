---
phase: 124-pounceagent-core-and-de-escalation
plan: 03
type: execute
wave: 3
depends_on:
  - 124-01
  - 124-02
files_modified:
  - crates/swarm-runtime/src/dispatcher.rs
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
  - crates/swarm-runtime/tests/dispatch_integration.rs
autonomous: true
requirements:
  - POUNCE-04
  - POUNCE-05
  - POLICY-01
must_haves:
  truths:
    - "A PounceAgent request flows through the existing policy and guard pipeline instead of stopping at a dispatcher no-op."
    - "Dry-run autonomous responses use the same runtime path and produce simulated receipts."
    - "Expired capability leases fail closed before any adapter execution."
  artifacts:
    - path: "crates/swarm-runtime/src/dispatcher.rs"
      provides: "Dispatcher-owned routing seam for RequestResponse actions."
    - path: "crates/swarm-runtime/src/lib.rs"
      provides: "Fail-closed lease expiry enforcement in canonical runtime execution."
    - path: "crates/swarm-runtime/tests/dispatch_integration.rs"
      provides: "Automated proof of routed execution, dry-run parity, and expired-lease denial."
  key_links:
    - from: "crates/swarm-runtime/src/dispatcher.rs"
      to: "crates/swarm-runtime/src/lib.rs"
      via: "authorize_and_execute"
      pattern: "authorize_and_execute"
    - from: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      to: "crates/swarm-runtime/src/dispatcher.rs"
      via: "dispatcher router wiring and PounceAgent registration"
      pattern: "PounceAgent"
---

<objective>
Route autonomous response requests through the canonical runtime path and enforce lease expiry before any adapter call.

Purpose: Close the detect-to-respond loop without bypassing policy, guards, receipts, or dry-run behavior.
Output: Dispatcher routing seam, runtime lease enforcement, PounceAgent registration in the live runtime path, and dispatch integration tests for Phase 124’s safety invariants.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-CONTEXT.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-RESEARCH.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-VALIDATION.md
@.planning/phases/116-agent-safety-hardening/116-02-SUMMARY.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/dispatcher.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/bin/swarm_detect.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/tests/dispatch_integration.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route `RequestResponse` through the dispatcher/runtime seam and register PounceAgent</name>
  <read_first>
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-runtime/src/pounce_agent.rs
    crates/swarm-runtime/src/whisker_agent.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/bin/swarm_detect.rs
  </files>
  <action>Replace the `SwarmAction::RequestResponse` no-op arm in `AgentDispatcher::apply_actions()` with a dispatcher-owned routing seam that can call into the configured runtime stack without leaking `SwarmRuntime<P, E>` generics into agents. Wire that seam from the live startup path in `crates/swarm-runtime/src/bin/swarm_detect.rs`, using `IngestState` accessors as needed, and register `PounceAgent` beside the existing Whisker/Stalker/Weaver agents only after the router is available. Keep `RequestResponse` centralized in the dispatcher; do not call response executors directly from the agent.</action>
  <acceptance_criteria>`rg -n "RequestResponse" crates/swarm-runtime/src/dispatcher.rs | rg -v "agent-direct"` no longer describes the variant as a no-op, `rg -n "PounceAgent" crates/swarm-runtime/src/bin/swarm_detect.rs` shows live registration, and `cargo test -p swarm-runtime --test dispatch_integration request_response_routes_through_authorize_and_execute -- --exact` passes.</acceptance_criteria>
  <verify>
    <automated>cargo test -p swarm-runtime --test dispatch_integration request_response_routes_through_authorize_and_execute -- --exact</automated>
  </verify>
  <done>The dispatcher owns autonomous response routing, the live runtime registers PounceAgent, and `RequestResponse` no longer terminates at a no-op branch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce fail-closed lease expiry and prove dry-run parity through integration tests</name>
  <read_first>
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/tests/dispatch_integration.rs
    .planning/phases/124-pounceagent-core-and-de-escalation/124-VALIDATION.md
  </read_first>
  <files>
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/tests/dispatch_integration.rs
  </files>
  <behavior>
    - Test 1: `request_response_routes_through_authorize_and_execute` proves dispatcher-routed autonomous requests hit the canonical runtime path.
    - Test 2: `pounceagent_dry_run_routes_through_runtime_path` proves detect-only mode still exercises policy, lease, guard, and receipt generation and returns `ResponseStatus::Simulated`.
    - Test 3: `expired_capability_lease_fails_closed_before_execution` proves `CapabilityLease.expires_at_ms <= now_ms` returns `ApprovalError::Denied("capability lease expired")` before any adapter call.
  </behavior>
  <action>Add explicit lease expiry enforcement to `SwarmRuntime::authorize_and_execute()` and the audited execution path in `crates/swarm-runtime/src/lib.rs` immediately after `issue_lease()` and before any call to `response.execute()`. Extend `crates/swarm-runtime/tests/dispatch_integration.rs` with the exact test names from the validation contract to prove routed execution, dry-run parity, and fail-closed expiry. Preserve the existing runtime-mode mapping to `ExecutionMode::DryRun`; do not add a special autonomous-response shortcut.</action>
  <acceptance_criteria>`cargo test -p swarm-runtime --test dispatch_integration pounceagent_dry_run_routes_through_runtime_path -- --exact` passes, `cargo test -p swarm-runtime --test dispatch_integration expired_capability_lease_fails_closed_before_execution -- --exact` passes, and `rg -n "capability lease expired" crates/swarm-runtime/src/lib.rs` finds the exact denial string.</acceptance_criteria>
  <verify>
    <automated>cargo test -p swarm-runtime --test dispatch_integration pounceagent_dry_run_routes_through_runtime_path -- --exact && cargo test -p swarm-runtime --test dispatch_integration expired_capability_lease_fails_closed_before_execution -- --exact</automated>
  </verify>
  <done>Autonomous responses traverse the same runtime path in dry-run and live modes, and expired leases fail closed before any adapter call.</done>
</task>

</tasks>

<verification>
Run the three exact `dispatch_integration` tests first, then `cargo test -p swarm-runtime --test dispatch_integration` to confirm routed execution, dry-run parity, and lease enforcement coexist cleanly.
</verification>

<success_criteria>
`RequestResponse` is live-routed, PounceAgent is registered in the runtime startup path, dry-run receipts stay simulated through the canonical runtime path, and expired leases are denied before execution.
</success_criteria>

<output>
After completion, create `.planning/phases/124-pounceagent-core-and-de-escalation/124-03-SUMMARY.md`
</output>
