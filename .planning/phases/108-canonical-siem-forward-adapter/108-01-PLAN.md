---
phase: 108-canonical-siem-forward-adapter
plan: 01
type: execute
wave: 1
depends_on: [107-01, 107-02]
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-response/src/config.rs
  - crates/swarm-response/src/lib.rs
  - crates/swarm-response/src/siem.rs
autonomous: true
requirements: [SIEM-01]

must_haves:
  truths:
    - "A repo-owned `SiemForwardConfig` selects Splunk HEC, ELK bulk, or Chronicle forwarding"
    - "`SiemForwardAdapter` implements `ResponseExecutor` and emits one canonical `swarm_finding` payload schema"
    - "The adapter reuses existing retry, circuit-breaker, and dead-letter behavior through `ResilientExecutor`"
  artifacts:
    - path: "crates/swarm-response/src/siem.rs"
      provides: "canonical finding schema plus transport-specific SIEM adapter"
      contains: "SiemForwardAdapter"
    - path: "crates/swarm-core/src/config.rs"
      provides: "top-level SIEM forward config contract"
      contains: "SiemForwardConfig"
  key_links:
    - from: "crates/swarm-core/src/config.rs"
      to: "crates/swarm-response/src/siem.rs"
      via: "repo-owned SIEM transport config drives canonical finding delivery"
      pattern: "siem_forward"
    - from: "crates/swarm-response/src/siem.rs"
      to: "crates/swarm-response/src/resilience.rs"
      via: "adapter inherits retry and circuit-breaker behavior"
      pattern: "ResponseExecutor"
---

<objective>
Add the canonical SIEM forward config and adapter surface.

Purpose: External SOC delivery needs a first-class resilient finding forwarder instead of ad hoc webhook reuse.

Output: `SiemForwardConfig`, canonical `swarm_finding` payload types, and a `SiemForwardAdapter` that supports Splunk HEC, ELK bulk ingest, and Chronicle transport variants.
</objective>
