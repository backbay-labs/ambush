---
phase: 156-chaos-and-resilience-testing
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 155
files_modified:
  - crates/swarm-consensus/src/lib.rs
  - crates/swarm-runtime/src/tom_agent.rs
  - crates/swarm-runtime/tests/dispatch_integration.rs
  - crates/swarm-runtime/tests/governance_resilience_integration.rs
autonomous: true
requirements:
  - CHAOS-01
  - CHAOS-02
  - CHAOS-03
must_haves:
  truths:
    - "Phases 153 through 155 already shipped the consensus, signed-governance, and partition-authority seams, so Phase 156 should prove those paths under adversarial conditions rather than redesign them."
    - "Chaos work must stay deterministic enough for CI and milestone closeout; replayable harnesses and explicit fault injection are preferable to timing-dependent stress tests."
    - "Safety matters more than liveness in this phase: the key proof is that unauthorized destructive response never executes under Byzantine, partitioned, or cascading-failure conditions."
  artifacts:
    - path: "crates/swarm-consensus/src/lib.rs"
      provides: "Byzantine injection harnesses for equivocation, delay, and invalid-signature scenarios."
    - path: "crates/swarm-runtime/tests/dispatch_integration.rs"
      provides: "Partition expiry, redemption, and healing proofs on the live destructive-response path."
    - path: "crates/swarm-runtime/tests/governance_resilience_integration.rs"
      provides: "End-to-end cascading failure and recovery replay coverage across the runtime governance stack."
  key_links:
    - from: "crates/swarm-consensus/src/lib.rs"
      to: "crates/swarm-runtime/src/tom_agent.rs"
      via: "signed governance decisions and exclusion handling"
      pattern: "Byzantine behavior must never produce an unauthorized destructive approval"
    - from: "crates/swarm-runtime/src/tom_agent.rs"
      to: "crates/swarm-runtime/tests/dispatch_integration.rs"
      via: "partition-aware lease redemption"
      pattern: "partition simulation must keep observability alive while fail-closing destructive response without an active lease"
---

<objective>
Complete Phase 156 by proving the distributed-governance path remains safe under Byzantine behavior, partitions, and cascading failures.

Purpose: close the milestone with deterministic resilience evidence rather than only nominal-path functionality.
Output: a Byzantine injection harness, partition-expiry and healing integration proof, and end-to-end cascading-failure replay coverage for the multi-instance governance lane.
</objective>

<context>
@.planning/phases/156-chaos-and-resilience-testing/156-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-consensus/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/tom_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/dispatcher.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/tests/dispatch_integration.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject Byzantine consensus behavior and prove fail-closed safety</name>
  <files>
    crates/swarm-consensus/src/lib.rs
    crates/swarm-runtime/src/tom_agent.rs
  </files>
  <action>Extend the in-process consensus harness with equivocation, delayed-vote, and invalid-signature scenarios and assert that invalid committee behavior is excluded without creating unauthorized governance approvals.</action>
  <verify>
    <automated>cargo test -p swarm-consensus tests::byzantine_committee_rejects_equivocation_and_invalid_signatures -- --exact</automated>
  </verify>
  <done>The consensus harness proves Byzantine participants cannot force an unauthorized commit or bypass existing exclusion logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Simulate partitions, expiry, and healing on the runtime response path</name>
  <files>
    crates/swarm-runtime/src/tom_agent.rs
    crates/swarm-runtime/tests/dispatch_integration.rs
  </files>
  <action>Add partition simulation coverage for valid lease redemption, lease expiry, missing-lease denial, and healing reconciliation on the live dispatcher path.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test dispatch_integration partitioned_request_response_ -- --nocapture</automated>
  </verify>
  <done>Partition simulations prove detection continuity, destructive fail-closed behavior, and deterministic reconciliation when quorum returns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove cascading failure and recovery with deterministic replay</name>
  <files>
    crates/swarm-runtime/tests/governance_resilience_integration.rs
    crates/swarm-runtime/src/dispatcher.rs
  </files>
  <action>Add an end-to-end resilience integration harness that exercises agent crash, health degradation, partition authority, and recovery using deterministic replay and persisted governance artifacts.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test governance_resilience_integration -- --nocapture</automated>
  </verify>
  <done>The runtime proves that crash and recovery sequences preserve safety, restore governance state, and keep unauthorized execution blocked throughout the failure window.</done>
</task>

</tasks>

<verification>
Keep Phase 156 focused on resilience proof. Do not pull Calico, fileless detection, or broader detection-breadth work forward from v1.47.
</verification>
