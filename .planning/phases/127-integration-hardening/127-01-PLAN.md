---
phase: 127-integration-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - verifications/office-detector-safety-v1.yaml
  - crates/swarm-runtime/src/replay/core.inc
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/tests/dispatch_integration.rs
autonomous: true
requirements:
  - POUNCE-01
  - POUNCE-02
  - POUNCE-04
  - POLICY-01
  - POLICY-03
must_haves:
  truths:
    - "The canonical office verification artifact stays green under the current debug-test runtime envelope."
    - "A real routed PounceAgent request for the same escalation only executes once per elevated session."
    - "An empty configurable ruleset fails closed on the routed autonomous path."
    - "Expired leases produce auditable routed failure records without calling the executor."
  artifacts:
    - path: "verifications/office-detector-safety-v1.yaml"
      provides: "Repo-owned verification budget aligned with the milestone validation envelope."
    - path: "crates/swarm-runtime/src/lib.rs"
      provides: "Auditable routed failure path for expired leases."
    - path: "crates/swarm-runtime/tests/dispatch_integration.rs"
      provides: "End-to-end routed proofs for no-double-trigger, fail-closed policy, and auditable lease expiry."
  key_links:
    - from: "crates/swarm-runtime/src/dispatcher.rs"
      to: "crates/swarm-runtime/src/lib.rs"
      via: "routed request execution and audited failure persistence"
      pattern: "route_request"
    - from: "crates/swarm-runtime/src/pounce_agent.rs"
      to: "crates/swarm-runtime/tests/dispatch_integration.rs"
      via: "same-session dedupe proven through dispatcher-backed execution counts"
      pattern: "handled_actions"
    - from: "crates/swarm-policy/src/configurable_gate.rs"
      to: "crates/swarm-runtime/tests/dispatch_integration.rs"
      via: "fail-closed empty-rules gate on routed autonomous requests"
      pattern: "configurable.fail_closed.empty_ruleset"
---

<objective>
Close the routed-path hardening gaps that remained after Phases 124-126 landed.

Purpose: Prove the autonomous response path behaves correctly when driven through the dispatcher and runtime seams, and keep the canonical verification artifact from spuriously blocking proof-backed workflows.
Output: stabilized verification budget, routed no-double-trigger proof, routed fail-closed empty-rules proof, and auditable expired-lease failure coverage.
</objective>

<context>
@.planning/phases/127-integration-hardening/127-CONTEXT.md
@.planning/phases/127-integration-hardening/127-RESEARCH.md
@.planning/phases/127-integration-hardening/127-VALIDATION.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/verifications/office-detector-safety-v1.yaml
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/tests/dispatch_integration.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed routed hardening proofs in dispatch integration</name>
  <read_first>
    crates/swarm-runtime/tests/dispatch_integration.rs
    .planning/phases/127-integration-hardening/127-VALIDATION.md
  </read_first>
  <files>
    crates/swarm-runtime/tests/dispatch_integration.rs
  </files>
  <action>Add the exact tests `pounceagent_routes_same_escalation_only_once_per_session`, `empty_ruleset_policy_fails_closed_for_routed_pounce_request`, and `expired_lease_routing_records_failure_audit_without_execution`. Use a real `PounceAgent` for the first two tests. The lease-expiry test must assert both zero executor calls and a persisted failure-shaped audit artifact on the routed path.</action>
  <verify>
    <automated>rg -n "pounceagent_routes_same_escalation_only_once_per_session|empty_ruleset_policy_fails_closed_for_routed_pounce_request|expired_lease_routing_records_failure_audit_without_execution" crates/swarm-runtime/tests/dispatch_integration.rs</automated>
  </verify>
  <done>The routed hardening proof points exist before the runtime changes are finalized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stabilize canonical verification and harden routed lease failures</name>
  <read_first>
    verifications/office-detector-safety-v1.yaml
    crates/swarm-runtime/src/replay/core.inc
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/tests/dispatch_integration.rs
  </read_first>
  <files>
    verifications/office-detector-safety-v1.yaml
    crates/swarm-runtime/src/replay/core.inc
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/tests/dispatch_integration.rs
  </files>
  <action>Align the canonical office verification detect-latency budget with the current debug-test runtime envelope, then teach the audited routed runtime path to persist a failure-shaped audit record when a lease is already expired at execution time. Preserve zero executor calls, policy attribution, and lineage-bearing evidence on that failure path.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test dispatch_integration pounceagent_routes_same_escalation_only_once_per_session -- --exact</automated>
  </verify>
  <done>The routed autonomous path is stable, deduped, fail-closed on empty configurable rules, and auditable on expired leases.</done>
</task>

</tasks>

<verification>
Run the three exact routed hardening tests first, then `cargo test -p swarm-runtime --test dispatch_integration` to confirm the new proofs coexist with the existing dry-run, audit-lineage, and governance-veto coverage.
</verification>

<output>
After completion, create `.planning/phases/127-integration-hardening/127-01-SUMMARY.md`
</output>
