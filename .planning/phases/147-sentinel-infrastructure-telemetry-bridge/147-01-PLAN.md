---
phase: 147-sentinel-infrastructure-telemetry-bridge
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 146
files_modified:
  - crates/swarm-core/src/telemetry.rs
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/bridge_runtime.rs
  - crates/swarm-runtime/tests/bridge_registry_integration.rs
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-ingest-sentinel/Cargo.toml
  - crates/swarm-ingest-sentinel/src/lib.rs
  - rulesets/default.yaml
autonomous: true
requirements:
  - INFRA-01
  - INFRA-04
must_haves:
  truths:
    - "Phase 147 is an ingest and bridge-integration phase, not a detector phase."
    - "Sentinel must look like a first-class bridge source in runtime health and metrics rather than a special-case side channel."
    - "The normalized payload contract has to land in `swarm-core` before the new bridge crate can be useful."
  artifacts:
    - path: "crates/swarm-core/src/telemetry.rs"
      provides: "normalized infrastructure telemetry payloads"
    - path: "crates/swarm-core/src/config.rs"
      provides: "repo-owned sentinel bridge config surface"
    - path: "crates/swarm-ingest-sentinel/src/lib.rs"
      provides: "Sentinel `TelemetryBridge` implementation"
    - path: "crates/swarm-runtime/src/bridge_runtime.rs"
      provides: "runtime registry wiring and health publication for sentinel"
  key_links:
    - from: "crates/swarm-ingest-sentinel/src/lib.rs"
      to: "crates/swarm-runtime/src/bridge_runtime.rs"
      via: "TelemetryBridge instantiation"
      pattern: "sentinel source is built and surfaced through the existing bridge runtime"
    - from: "crates/swarm-core/src/config.rs"
      to: "rulesets/default.yaml"
      via: "repo-owned source kind"
      pattern: "operator config can describe a sentinel bridge without ad hoc parsing"
---

<objective>
Complete Phase 147 by adding the Sentinel bridge crate, normalizing Sentinel infrastructure payloads into the shared telemetry schema, and wiring the new bridge kind into the existing runtime bridge registry and health surfaces.

Purpose: give Swarm a durable infrastructure-signal ingest lane that later detectors can consume without inventing new runtime plumbing.
Output: new telemetry payload variants, sentinel bridge config support, repo-owned `swarm-ingest-sentinel` crate, and runtime registry integration proof.
</objective>

<context>
@.planning/phases/147-sentinel-infrastructure-telemetry-bridge/147-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/telemetry.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/bridge_runtime.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/docs/research/sentinel-convergence/05-TELEMETRY-BRIDGE-ARCHITECTURE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend shared telemetry and config contracts for Sentinel</name>
  <read_first>
    crates/swarm-core/src/telemetry.rs
    crates/swarm-core/src/config.rs
  </read_first>
  <files>
    crates/swarm-core/src/telemetry.rs
    crates/swarm-core/src/config.rs
    rulesets/default.yaml
  </files>
  <action>Add normalized infrastructure payload variants and a repo-owned `sentinel` bridge config shape that can be described inside `runtime.telemetry_sources`.</action>
  <verify>
    <automated>cargo test -p swarm-core telemetry::tests:: -- --nocapture</automated>
    <automated>cargo test -p swarm-core config::tests:: -- --nocapture</automated>
  </verify>
  <done>Sentinel-specific infrastructure payloads and config deserialize, validate, and stay owned by `swarm-core`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the Sentinel bridge crate</name>
  <read_first>
    crates/swarm-ingest-json/src/generic_json.rs
    docs/research/sentinel-convergence/05-TELEMETRY-BRIDGE-ARCHITECTURE.md
  </read_first>
  <files>
    crates/swarm-ingest-sentinel/Cargo.toml
    crates/swarm-ingest-sentinel/src/lib.rs
  </files>
  <action>Implement a deterministic `TelemetryBridge` that maps Sentinel infrastructure health, thermal anomaly, and resource exhaustion payloads into the shared normalized event schema with bridge-health accounting and schema validation.</action>
  <verify>
    <automated>cargo test -p swarm-ingest-sentinel -- --nocapture</automated>
  </verify>
  <done>The Sentinel bridge can poll fixture-backed input, emit valid normalized events, and report health like the existing bridge crates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Sentinel into runtime bridge orchestration</name>
  <read_first>
    crates/swarm-runtime/src/bridge_runtime.rs
    crates/swarm-runtime/tests/bridge_registry_integration.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/bridge_runtime.rs
    crates/swarm-runtime/tests/bridge_registry_integration.rs
    crates/swarm-runtime/src/config.rs
  </files>
  <action>Register the new bridge kind in `BridgeRuntimeRegistry`, prove it shows up in health and metrics, and add an integration test that emits normalized Sentinel telemetry through the runtime bridge worker path.</action>
  <verify>
    <automated>cargo test -p swarm-runtime bridge_runtime::tests:: -- --nocapture</automated>
    <automated>cargo test -p swarm-runtime --test bridge_registry_integration -- --nocapture</automated>
    <automated>cargo check -p swarm-runtime -p swarm-ingest-sentinel --tests -j 1 --message-format short</automated>
  </verify>
  <done>The runtime accepts a `sentinel` telemetry source, builds the bridge, and reports its health and output through the same registry and metrics path as the other bridge workers.</done>
</task>

</tasks>

<verification>
Land the shared schema and config first, then the standalone bridge crate, then runtime registry wiring. Keep infrastructure threat detection itself out of scope until Phase 148.
</verification>
