---
phase: 123-multi-strategy-integration-proof
plan: 01
type: execute
wave: 1
depends_on: [121-01, 121-02, 122-01, 122-02]
files_modified:
  - crates/swarm-runtime/tests
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements: [NETWORK-04, NETWORK-05]

must_haves:
  truths:
    - "A runtime-facing integration test proves `NetworkConnect` telemetry becomes a signed `ThreatClass::CommandAndControl` pheromone deposit"
    - "A composite integration test proves three distinct strategy-scoped deposits can drive `PheromoneConcentration.distinct_sources >= 3` and trigger escalation"
    - "Phase closeout updates roadmap and requirements state to reflect v1.38 completion"
  artifacts:
    - path: "crates/swarm-runtime/tests"
      provides: "milestone integration proof for network-connect deposits and cross-strategy escalation"
      contains: "CompositeDetector::new"
---

<objective>
Close v1.38 with executable proof for the two remaining requirements.

Purpose: Phase 121 already proved real `network_connect` runtime wiring, and Phase 122 already proved strategy-scoped deposits plus rollout scoping. This phase must connect those pieces into milestone-level proof without widening production scope.

Implementation note: the original 123 context suggested mixing `NetworkConnect`, suspicious scripting, and credential access in one sequence. That does not prove `min_sources_for_escalation`, because escalation is evaluated per `ThreatClass`. This plan therefore splits the proof into:
1. a real `network_connect` end-to-end deposit test for `NETWORK-04`
2. a focused composite escalation test that keeps all three corroborating strategies on one threat class for `NETWORK-05`
</objective>
