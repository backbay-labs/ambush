---
phase: 270-adversary-emulation-corpus
plan: 01
type: execute
depends_on:
  - 269-deployment-documentation
files_modified:
  - crates/swarm-runtime/src/evasion_coverage.rs
  - crates/swarm-runtime/src/bin/generate_adversary_emulation_report.rs
  - crates/swarm-runtime/tests/adversary_emulation_integration.rs
  - docs/benchmarks/adversary-emulation-coverage.md
  - tools/check-adversary-emulation-coverage.sh
autonomous: true
requirements:
  - EMULATION-01
  - EMULATION-02
  - EMULATION-03
---

<objective>
Turn the shipped evasion corpus into an operator- and CI-facing adversary
emulation proof surface with documented detector mapping and a measurable
coverage floor.
</objective>

<tasks>

<task>
  <name>Capture technique coverage in the runtime-owned report</name>
  <done>`crates/swarm-runtime/src/evasion_coverage.rs` now summarizes per-scenario and per-technique adversary-emulation coverage, including `detected`, `partial`, and `not_covered` states plus the overall mapped coverage percentage.</done>
</task>

<task>
  <name>Add CI-friendly report generation and enforcement</name>
  <done>`generate_adversary_emulation_report` emits the JSON report and `tools/check-adversary-emulation-coverage.sh` enforces the scenario-count, technique-count, and 60% coverage floor.</done>
</task>

<task>
  <name>Prove the mapped corpus through the full detection pipeline</name>
  <done>`crates/swarm-runtime/tests/adversary_emulation_integration.rs` replays the shipped corpus through the configured runtime and validates the documented scenario-to-detector mapping.</done>
</task>

</tasks>
