---
phase: 271-quickstart-command-and-end-to-end-validation
plan: 01
type: execute
depends_on:
  - 270-adversary-emulation-corpus
files_modified:
  - crates/swarm-cli/src/core.inc
  - crates/swarm-cli/src/tracing.rs
  - crates/swarm-runtime/src/control.rs
  - Dockerfile
  - docs/QUICKSTART.md
autonomous: true
requirements:
  - DEPLOY-03
---

<objective>
Close the operator-packaging milestone with one end-to-end first-run command
that validates config, injects the built-in synthetic attack path, waits for a
real finding, and prints the proof surface cleanly.
</objective>

<tasks>

<task>
  <name>Add the one-command quickstart surface</name>
  <done>`swarmctl quickstart` now validates the active config, runs the guided first-run walkthrough, and emits the finding, elapsed time, receipt-pack ID, and proof Merkle root in one report.</done>
</task>

<task>
  <name>Make the quickstart output operator-readable</name>
  <done>The CLI now defaults `swarmctl` logging to `warn` and only suggests follow-up commands that are coherent for the actual config, rather than pointing operators at incident lookup when the signed bootstrap still keeps incident state in memory.</done>
</task>

<task>
  <name>Validate the packaged path end-to-end</name>
  <done>The repo-owned Docker packaging path was rechecked after the bootstrap include-path fix and builder-stage `rulesets/` copy update so `swarmctl quickstart` remains coherent with the shipped image wrapper.</done>
</task>

</tasks>
