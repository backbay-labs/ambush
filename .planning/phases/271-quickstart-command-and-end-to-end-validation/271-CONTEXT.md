# Phase 271 Context

Phase 271 follows Phase 270 under `v1.75 Operator Packaging`.

Goal: `swarmctl quickstart` proves first-run success end-to-end by validating config, starting runtime, injecting a synthetic attack, waiting for detection, and reporting elapsed time.
