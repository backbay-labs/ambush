---
phase: 268-curated-defaults-and-operator-ux
plan: 01
type: execute
depends_on:
  - 267-agent-crate-extraction
files_modified:
  - crates/swarm-cli/src/core.inc
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/src/service/runtime_service.rs
  - crates/swarm-runtime/src/service/types.rs
autonomous: true
requirements:
  - DEFAULTS-01
  - DEFAULTS-02
  - OPEXP-01
  - OPEXP-02
---

<objective>
Turn the existing CLI and control-plane groundwork into a first-run detect-only
operator path that validates cleanly, surfaces bridge and config problems with
actionable remediation, and reports one-screen runtime summary data.
</objective>

<tasks>

<task>
  <name>Make `swarmctl init` emit a validate-able bootstrap bundle</name>
  <done>`swarmctl init --mode detect_only` writes a repo-owned bootstrap config plus matching signature sidecar behavior so `swarmctl validate` and `swarmctl readiness` can run on the generated path without hand editing.</done>
</task>

<task>
  <name>Add remediation-grade validation and readiness errors</name>
  <done>The validate and readiness paths now map signature, config-shape, detector-profile, endpoint, and bridge failures into concrete operator remediation guidance instead of raw parser or connection errors alone.</done>
</task>

<task>
  <name>Compress operator status into a one-screen summary</name>
  <done>`swarmctl status` now leads with runtime mode, active detectors, bridge health, recent findings, and escalation state while preserving the deeper review details below.</done>
</task>

</tasks>
