---
phase: 289-herd-memory
plan: 01C
type: execute
wave: 4
depends_on: [289-01B]
files_modified:
  - crates/swarm-runtime/src/herd_memory_factory.rs
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/tests/herd_memory_factory.rs
autonomous: true
requirements: [HERDMEM-01, HERDMEM-05]
must_haves:
  truths:
    - "Runtime projection/import startup obtains its resolver only from HerdMemoryConfig and the repository-owned FileOpaqueKeyProvider; no caller can inject bytes, an environment secret, or a process-local/test provider in a normal build."
    - "Missing, insecure, foreign-owned, tampered, retired, revoked, rotated, cross-tenant, cross-namespace, or mismatched resolver state fails before projection/import construction."
    - "The runtime factory never auto-bootstraps key custody; first-run provisioning is the explicit Plan 01B operation and startup remains fail closed."
  artifacts:
    - path: "crates/swarm-runtime/src/herd_memory_factory.rs"
      provides: "config-bound HerdMemoryRuntimeFactory and typed construction errors"
      exports: ["HerdMemoryRuntimeFactory"]
    - path: "crates/swarm-runtime/tests/herd_memory_factory.rs"
      provides: "startup, fallback, custody, scope, rotation, and restart tests"
      contains: "no_process_local_or_test_resolver_fallback"
  key_links:
    - from: "crates/swarm-runtime/src/herd_memory_factory.rs"
      to: "crates/swarm-core/src/config/state.rs"
      via: "validated HerdMemoryConfig is the sole startup input"
      pattern: "from_config"
    - from: "crates/swarm-runtime/src/herd_memory_factory.rs"
      to: "crates/swarm-spine/src/herd_key_resolver.rs"
      via: "factory opens FileOpaqueKeyProvider and never substitutes a provider"
      pattern: "OpaqueKeyResolverFactory::from_config"
---

<objective>
Bind runtime startup to the concrete provider and make the secure custody boundary executable at the projection/import construction seam.

Purpose: typed body/provider correctness is insufficient if runtime startup can bypass it with a default or test resolver.
Output: config-only runtime factory, public runtime export, and fail-closed startup tests.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/289-herd-memory/289-CONTEXT.md
@.planning/phases/289-herd-memory/289-01-PLAN.md
@.planning/phases/289-herd-memory/289-01B-PLAN.md
@crates/swarm-core/src/config/state.rs
@crates/swarm-core/src/config/validation.rs
@crates/swarm-runtime/src/lib.rs
@crates/swarm-spine/src/herd_key_resolver.rs
@crates/swarm-spine/src/herd_memory.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Construct the config-bound runtime factory with no implicit bootstrap or fallback</name>
  <files>crates/swarm-runtime/src/herd_memory_factory.rs, crates/swarm-runtime/src/lib.rs, crates/swarm-runtime/tests/herd_memory_factory.rs</files>
  <read_first>
    - crates/swarm-spine/src/herd_key_resolver.rs after Plan 01B
    - crates/swarm-core/src/config/state.rs and src/validation.rs
    - crates/swarm-runtime/src/lib.rs
    - crates/swarm-runtime/src/herd_memory_advisory.rs
  </read_first>
  <behavior>
    - HerdMemoryRuntimeFactory::from_config(&HerdMemoryConfig) validates enabled config and opens OpaqueKeyResolverFactory::from_config; missing root/key/descriptor/provider is a typed error, never an auto-bootstrap.
    - Normal-build construction has no constructor accepting raw key bytes, environment values, arbitrary provider objects, process-local maps, or test fixtures; fixture injection is cfg(test)-only and cannot be linked by production code.
    - Current key is required for new export; same-scope retired epochs are only an explicit verification path, and tenant/export namespace/key epoch/resolver reference mismatches fail before projection/import.
  </behavior>
  <action>
    Implement HerdMemoryRuntimeFactory::from_config as the only normal-build constructor and re-export it from swarm-runtime::lib. It calls the Plan 01B OpaqueKeyResolverFactory, propagates typed errors, and returns only a non-secret resolver/factory handle. Do not call provision_first_opaque_key implicitly: a first-run operator must provision custody explicitly, then restart/start the runtime.

    Write tests first for factory_requires_config_bound_file_provider, factory_rejects_missing_root, factory_rejects_insecure_or_foreign_root, factory_rejects_tampered_generation, factory_rejects_missing_or_retired_current_key, factory_preserves_same_scope_retired_verification, cross_tenant_or_namespace_lookup_fails_closed, restart_reopens_current_and_retired_epochs, and no_process_local_or_test_resolver_fallback. Add source-level mutation checks proving no env/process-local/raw-key fallback and no automatic bootstrap call.
  </action>
  <verify>
    <automated>cargo test -p swarm-runtime --test herd_memory_factory -- --test-threads=1 && cargo clippy -p swarm-runtime --all-targets -- -D warnings && cargo fmt --all -- --check && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The only normal-build constructor is config-bound and fails closed on all custody/scope/lifecycle failures.
    - Startup never creates, repairs, or downgrades key material and exports no secret bytes.
    - Tests prove current/retired behavior, restart durability, scope separation, and absence of every fallback.
  </acceptance_criteria>
  <done>Runtime projection/import construction cannot bypass the explicit FileOpaqueKeyProvider custody boundary.</done>
</task>
</tasks>

<verification>
Run the focused runtime factory tests, runtime clippy/format/diff, and the Plan 01B resolver tests. Confirm startup construction is config-bound and no implicit bootstrap or fallback path exists.
</verification>

<success_criteria>
- The runtime factory is a narrow config seam, not a second key provider.
- First-run provisioning and runtime startup are separate operations with fail-closed permissions and explicit operator control.
</success_criteria>

<output>After completion, create .planning/phases/289-herd-memory/289-01C-SUMMARY.md with factory wiring, custody failure, restart, and fallback evidence.</output>
