---
phase: 289-herd-memory
plan: 01
type: execute
wave: 2
depends_on: [289-00]
files_modified:
  - crates/swarm-core/src/config/state.rs
  - crates/swarm-core/src/config/defaults.rs
  - crates/swarm-core/src/config/root.rs
  - crates/swarm-core/src/config/validation.rs
  - crates/swarm-core/src/config/tests.rs
autonomous: true
requirements: [HERDMEM-01, HERDMEM-02, HERDMEM-05]
must_haves:
  truths:
    - "Herd memory is disabled by default and cannot start without explicit tenant, local swarm, source allowlist, epoch, registry, storage, retention, clock-skew, and opaque-key reference configuration."
    - "The enabled configuration is bounded, schema-validated, path-safe, and contains no secret key bytes, raw telemetry, response action, receipt, approval, or operator credential."
    - "Legacy configurations remain disabled and preserve existing memory defaults; unknown and unsafe enabled fields fail closed."
  artifacts:
    - path: "crates/swarm-core/src/config/state.rs"
      provides: "HerdMemoryConfig nested under MemoryConfig with fail-closed scope, retention, epoch, resolver-root, and resource settings"
      contains: "pub struct HerdMemoryConfig"
    - path: "crates/swarm-core/src/config/defaults.rs"
      provides: "disabled-by-default HerdMemoryConfig defaults"
      contains: "herd_memory"
    - path: "crates/swarm-core/src/config/validation.rs"
      provides: "field-specific enabled-config validation and path/relationship checks"
      contains: "herd_memory"
    - path: "crates/swarm-core/src/config/tests.rs"
      provides: "legacy, valid, invalid-field, and unknown-field configuration tests"
      contains: "herd_memory"
  key_links:
    - from: "crates/swarm-core/src/config/state.rs"
      to: "crates/swarm-core/src/config/validation.rs"
      via: "SwarmConfig validation rejects unsafe enabled HerdMemoryConfig"
      pattern: "herd_memory.*validate"
    - from: "crates/swarm-core/src/config/tests.rs"
      to: "rulesets/attestation.json"
      via: "read-only digest/attestation test proves the signed default ruleset bytes are unchanged"
      pattern: "bc63f0e53780325317f638b6e22f4d6f638048fc7ba177485c18592f6104c324"
---

<objective>
Define the disabled-by-default, bounded HerdMemoryConfig that later typed-body, resolver, trust, lifecycle, and runtime plans consume.

Purpose: no later implementation can repair an unscoped, path-unsafe, secret-bearing, or silently activated memory lane.
Output: validated configuration fields, defaults, signed-default preservation evidence, and strict configuration tests.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/289-herd-memory/289-CONTEXT.md
@.planning/phases/289-herd-memory/289-RESEARCH.md
@.planning/phases/289-herd-memory/289-00-PLAN.md
@crates/swarm-core/src/config/state.rs
@crates/swarm-core/src/config/root.rs
@crates/swarm-core/src/config/validation.rs
@crates/swarm-core/src/config/defaults.rs
@crates/swarm-core/src/config/tests.rs
@rulesets/default.yaml
@rulesets/attestation.json
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add disabled-by-default HerdMemoryConfig and fail-closed validation</name>
  <files>crates/swarm-core/src/config/state.rs, crates/swarm-core/src/config/defaults.rs, crates/swarm-core/src/config/root.rs, crates/swarm-core/src/config/validation.rs, crates/swarm-core/src/config/tests.rs</files>
  <read_first>
    - `crates/swarm-core/src/config/state.rs` (`MemoryConfig`, `IdentityConfig`, and existing `Default`/`validate` patterns)
    - `crates/swarm-core/src/config/root.rs` (`SwarmConfig.memory`)
    - `crates/swarm-core/src/config/validation.rs` (top-level validation order)
    - `crates/swarm-core/src/config/defaults.rs` (repo-owned default helpers)
    - `crates/swarm-core/src/config/tests.rs` (config fixture construction and unknown-field tests)
    - `rulesets/default.yaml` (signed/default config shape)
    - `.planning/phases/289-herd-memory/289-RESEARCH.md` (Open Questions: scope, TTL/skew, opaque-key epoch)
  </read_first>
  <behavior>
    - Deserializing an old config with no `memory.herd_memory` produces `enabled: false` and preserves all existing memory defaults/serialized behavior.
    - Enabling the lane with an empty tenant, local swarm, export namespace, source allowlist, registry path, import root, opaque-key root, or opaque-key resolver reference fails with the field-specific `ConfigValidationError`.
    - Enabled configuration requires `epoch > 0`, `opaque_key_epoch > 0`, `max_ttl_ms > 0`, `retention_ms >= max_ttl_ms`, `max_clock_skew_ms < max_ttl_ms`, and every resource limit > 0; unknown keys fail through `deny_unknown_fields`.
  </behavior>
  <action>
    Add `HerdMemoryConfig` under `MemoryConfig` as a `#[serde(default, deny_unknown_fields)]` nested field named `herd_memory`. Use these concrete fields: `enabled: bool` (default `false`), `tenant_id: String`, `local_swarm_id: String`, `export_namespace: String`, `accepted_source_swarm_ids: BTreeSet<String>`, `epoch: u64`, `issuer_registry_path: String`, `import_root: String`, `opaque_key_root: String`, `max_ttl_ms: u64`, `max_clock_skew_ms: u64`, `retention_ms: u64`, `opaque_key_epoch: u64`, `opaque_key_ref: String`, `opaque_key_resolver_ref: String`, `max_record_bytes: usize`, `max_transformations: usize`, and `max_index_entries: usize`. Keep key material out of config and records; `opaque_key_root` is an explicit local custody root and `opaque_key_ref`/`opaque_key_resolver_ref` are reference labels only. The resolver reference is carried into the importer/export adapter, never treated as key material.

    Add explicit default helper functions and rely on `#[serde(default)]` omission semantics; do not edit the signed `rulesets/default.yaml` or any attestation/signature file. Extend top-level validation so the new config is checked only when enabled but never silently activates. Reject duplicate/empty source IDs, a receiver that is also not configured as local, unsafe TTL/skew/retention relationships, zero limits, path traversal in `import_root`/`issuer_registry_path`/`opaque_key_root`, invalid tenant/export-namespace characters, and empty key references. Add round-trip, legacy-default, every-invalid-field, and unknown-field tests; add `signed_default_ruleset_bytes_and_attestation_remain_unchanged`, which reads `rulesets/default.yaml` and `rulesets/attestation.json`, recomputes the pinned `sha256`/size, and fails on any mutation. Do not use floating-point confidence or secret bytes in this config.
  </action>
  <verify>
    <automated>cargo test -p swarm-core config::tests::herd_memory --lib && cargo fmt --all -- --check && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `HerdMemoryConfig` is nested under `MemoryConfig`, defaults disabled, and contains every named scope/epoch/path/TTL/retention/opaque-key/resource field.
    - `cargo test -p swarm-core config::tests::herd_memory --lib` covers legacy omission, enabled valid config, every zero/empty/relationship failure, and an unknown key.
    - No secret key bytes, raw telemetry, response action, receipt, approval, or operator credential field is present in the config schema; resolver and registry values are opaque references/paths only.
    - Omission of `memory.herd_memory` deserializes as disabled while the signed `rulesets/default.yaml` remains byte-identical; the attestation digest `bc63f0e53780325317f638b6e22f4d6f638048fc7ba177485c18592f6104c324` and recorded byte size are revalidated by tests.
  </acceptance_criteria>
  <done>Configuration cannot enable an unscoped, unbounded, stale-prone, or secret-bearing herd-memory lane and legacy configs remain disabled by default.</done>
</task>

</tasks>

<verification>
Run the focused swarm-core configuration tests, format, and diff checks. Confirm disabled legacy configs remain byte-compatible and every enabled scope/path/retention/limit violation fails with a field-specific error before any resolver or importer can start.
</verification>

<success_criteria>
- HERDMEM-01/02/05 configuration inputs are explicit, bounded, disabled by default, and secret-free.
- Legacy omission remains disabled; unknown fields, path traversal, empty scopes, unsafe TTL/skew/retention, and zero limits fail closed.
- The signed default ruleset and its attestation are read-only inputs; no plan task edits their bytes.
- No body, provider, runtime factory, or response module is included in this plan; those files are owned by the dependent 01B/01C plans.
</success_criteria>

<output>After completion, create .planning/phases/289-herd-memory/289-01-SUMMARY.md with configuration schema, default/validation evidence, and no resolver or body implementation claims.</output>
