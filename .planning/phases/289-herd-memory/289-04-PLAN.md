---
phase: 289-herd-memory
plan: 04
type: execute
wave: 6
depends_on: [289-01C, 289-03]
files_modified:
  - crates/swarm-runtime/src/herd_memory_projection.rs
  - crates/swarm-runtime/src/sphinx_agent.rs
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/tests/herd_memory_projection.rs
autonomous: true
requirements: [HERDMEM-01]
must_haves:
  truths:
    - "A completed investigation/graph/strategy outcome exports only the five typed herd-memory abstractions and typed digest references through an explicit projection; rich source structs are never serialized or passed through as a generic view."
    - "Every digest-shaped body, source, evidence, lineage, tenant, namespace, entity, and transformation reference is an explicit lower-case 64-hex value; 0x-prefixed, upper-case, non-hex, wrong-length, and display-helper encodings are rejected before projection."
    - "Host/user/process/IP values, secrets, operator credentials, raw telemetry, URLs, command lines, receipts, actions, approval, and response authority cannot cross the projection boundary, including nested mutation cases."
    - "Opaque entity references are keyed HMACs scoped to tenant/key epoch/kind; the normalized source value is dropped before the body is serialized or logged."
    - "Every OpaqueEntityRef carries tenant_scope_digest and export_namespace_digest, and Sphinx obtains its resolver only from the config-bound production factory."
    - "The real Sphinx export seam is scanned and contains exactly one resolver-backed projection call; no alternate rich serialization path can bypass the allowlist."
    - "Phase 288's Arena-owned Phase287ArenaSynthesisAdapter is the only concrete producer of the Runtime-owned ArenaSynthesisInput; Phase 289 consumes only its typed ArenaSourceRef role/schema/canonical-ID/content/payload/partition digests plus the detached source digest, and Runtime never imports swarm-arena or recreates that adapter."
    - "The projection emits a complete detached ArenaLineage/SignedStateExpectation manifest; every adapter/DTO/source-role and signed-state field is typed, separately digested, and mutation-tested before export or evaluator use."
    - "Sphinx export calls the public opaque HerdMemoryLifecycleHandle::prepare_export then commit_export boundary under the lifecycle/registry lock; the private coordinator cannot be reached, and Sphinx cannot sign, write, or bypass durable generation/predecessor/source-highwater/nonce/fence/crash-journal state."
  artifacts:
    - path: "crates/swarm-runtime/src/herd_memory_projection.rs"
      provides: "explicit typed projection input, allowlist constructors, source digest/transformation handling, and HMAC ref wiring"
      exports: ["HerdMemoryProjectionInput", "project_herd_memory"]
    - path: "crates/swarm-runtime/src/sphinx_agent.rs"
      provides: "safe completed-investigation export seam that calls the dedicated projection"
      contains: "project_herd_memory"
    - path: "crates/swarm-runtime/tests/herd_memory_projection.rs"
      provides: "raw-field, nested-secret, HMAC, unknown-schema, and rich-source negative controls"
      contains: "projection_is_allowlist_only_and_hmac_opaque"
  key_links:
    - from: "crates/swarm-runtime/src/sphinx_agent.rs"
      to: "crates/swarm-runtime/src/herd_memory_projection.rs"
      via: "completed graph/investigation output is converted by explicit constructors"
      pattern: "project_herd_memory"
    - from: "crates/swarm-runtime/src/herd_memory_projection.rs"
      to: "swarm_spine::herd_memory::opaque_entity_ref"
      via: "keyed opaque reference construction before normalized-value drop"
      pattern: "opaque_entity_ref"
    - from: "crates/swarm-runtime/tests/herd_memory_projection.rs"
      to: "scenarios/herd-memory/manifest.yaml"
      via: "same independent prohibited-field mutation matrix"
      pattern: "prohibited|manifest"
    - from: "crates/swarm-arena/src/synthesis_adapter.rs"
      to: "crates/swarm-runtime/src/synthesis/arena_input.rs"
      via: "Phase287ArenaSynthesisAdapter is the sole Arena-owned producer of ArenaSynthesisInput; Runtime consumes the DTO without importing Arena"
      pattern: "Phase287ArenaSynthesisAdapter|ArenaSynthesisInput|ArenaSourceRef"
    - from: "crates/swarm-runtime/src/synthesis/arena_input.rs"
      to: "crates/swarm-runtime/src/herd_memory_projection.rs"
      via: "only typed ArenaSourceRef lineage becomes projection input after pin validation"
      pattern: "ArenaSynthesisInput|ArenaSourceRole|schema_version|canonical_id|content_digest|payload_digest|partition_digest"
    - from: "crates/swarm-runtime/src/herd_memory_projection.rs"
      to: "ArenaLineage|SignedStateExpectation"
      via: "detached complete typed lineage covers adapter/DTO/six roles/source and every signed-state field"
      pattern: "ArenaLineage|SignedStateExpectation|capture_digest|receipt_digest|preview_digest|rollback_digest"
    - from: "crates/swarm-runtime/src/sphinx_agent.rs"
      to: "HerdMemoryLifecycleHandle|HerdMemoryExportReservation"
      via: "Sphinx export uses the lifecycle handle's opaque reservation pair and never writes/signs directly"
      pattern: "HerdMemoryLifecycleHandle|prepare_export|commit_export|HerdMemoryExportReservation"
---

<objective>
Project completed local reasoning into the strict herd-memory body without leaking rich investigation or telemetry structures.

Purpose: the current Sphinx/investigation/evidence producers contain exactly the host, user, process, IP, secret, receipt, and operator data that the wire contract must exclude. A separate allowlist projection is the only safe export path.
Output: explicit runtime projection adapter, Sphinx export seam, keyed opaque refs, and nested privacy tests.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/289-herd-memory/289-CONTEXT.md
@.planning/phases/289-herd-memory/289-RESEARCH.md
@.planning/phases/289-herd-memory/289-01-PLAN.md
@.planning/phases/289-herd-memory/289-01B-PLAN.md
@.planning/phases/289-herd-memory/289-01C-PLAN.md
@.planning/phases/289-herd-memory/289-00-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00F-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-06-PLAN.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-CONTEXT.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-00-PLAN.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-00A-PLAN.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-02-PLAN.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-04-PLAN.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-05-PLAN.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-VALIDATION.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-VERIFICATION.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-07-PLAN.md
@crates/swarm-runtime/src/sphinx_agent.rs
@crates/swarm-runtime/src/investigation.rs
@crates/swarm-runtime/src/strategy.rs
@crates/swarm-spine/src/investigation.rs
@crates/swarm-spine/src/incident.rs
@crates/swarm-spine/src/herd_memory.rs
@crates/swarm-runtime/tests/herd_memory_oracle.rs
@tools/check-herd-memory-upstreams.sh
</context>

<projection_prerequisite>
Run `bash tools/check-herd-memory-upstreams.sh --run -- bash -c '<task command>'`
before projection/export work and before each bridge test. It must have accepted
the exact Phase 286 07B, Phase 287 06, and Phase 288 07 closure artifacts,
independent review/verification rows, recomputed zero findings, current
`HEAD^{tree}`, and canonical evidence digests; no path-only or partial ledger
counts. Phase 289 consumes only the actual resolved adapter/runtime DTO/Cargo
membership/corpus and Phase 288 source/tree/evidence pins in that gate record.
</projection_prerequisite>

<interfaces>
The projection may consume typed local records but may not serialize them wholesale. The Phase 288
Arena-owned adapter is external to this plan:
```rust
pub struct InvestigationBundle { /* rich local record; never a wire payload */ }
pub struct CorrelatedIncident { /* rich local record; never a wire payload */ }
pub struct HerdMemoryBody { /* five allowlisted families only */ }
pub struct OpaqueEntityRef {
    kind: OpaqueEntityKind,
    ref_digest: Digest64,
    key_epoch: u64,
    tenant_scope_digest: Digest64,
    export_namespace_digest: Digest64,
}
pub trait OpaqueKeyResolver { /* tenant/export-namespace-bound lookup */ }
pub fn opaque_entity_ref(resolver: &dyn OpaqueKeyResolver, key_ref: &str, tenant_id: &str, export_namespace: &str, key_epoch: u64, kind: OpaqueEntityKind, normalized: &str) -> OpaqueEntityRef;

use crate::synthesis::arena_input::{ArenaSynthesisInput, ArenaSourceRef, ArenaSourceRole};
pub struct HerdMemoryProjectionInput { /* explicit allowlisted fields plus validated arena lineage */ }
// The Plan 00 contract above is the normative schema. This plan is the sole
// production owner of its definitions in herd_memory_projection.rs; the Plan
// 00 oracle mirrors the schema only and no other plan may redeclare it or add a
// second constructor.
```

`Phase287ArenaSynthesisAdapter` in `crates/swarm-arena/src/synthesis_adapter.rs` is the only
concrete producer. It emits the Runtime-owned `ArenaSynthesisInput`; its closed
`ArenaSourceRole` values are exactly `BlueOutcome`, `ArenaIngestResult`,
`Phase286InvestigationCapture`, `ArenaArtifactStore`, `PairReport`, and
`SignedBlueLearnedState`. Phase 289 validates those refs and the accepted gate-resolved upstream pins but may
not define a second adapter, add a role variant, import `swarm-arena` from Runtime, or infer a
record from a guessed module path. The Phase 287 known-set remains the separately pinned oracle
registry and its exact tuple is consumed only through typed source lineage.

The Phase 287 conversion, tuple construction, canonical-byte recipe, aggregate
known-set digest, and source attribution are Arena-owned contracts in Plan 06's
existing `crates/swarm-arena/src/synthesis_adapter.rs` seam and its
`crates/swarm-arena/tests/herd_memory_benchmark_bridge.rs` tests. That adapter
consumes the actual pinned Phase 287 campaign-stage contract, derives
authenticated provenance from its `ArenaSourceRef` and accepted adapter/corpus
pins, and emits only the Runtime-owned `ArenaSynthesisInput`. Plan 04 has no
upstream-stage import, tuple constructor, fingerprint type, canonicalization
recipe, or Runtime-to-Arena dependency: it validates only the DTO's closed
roles/schema/IDs/digests and detached `ArenaLineage`.

The evaluator process receives the Arena-produced typed known-set result and
never receives a caller-supplied attribution or a Runtime fingerprint object.
Attribution remains outside the per-tuple digest but is included in the
authenticated Arena lineage and signed aggregate linkage described by Plan 06.
The projection receives the detached `ArenaLineage` with its one
`source_digest`, recomputes `canonical_bytes_without_source_digest()`, and
requires equality before producing a body. The adapter digest, projection
digest, candidate-freeze receipt `candidate_lineage_digest`, evaluator
receipt/request lineage fields, and capability lineage field must all equal
that same digest byte-for-byte; any subset, attribution-inclusive, or
evaluator-recomputed replacement is refused.

The Phase 288 closure is an external prerequisite, not an in-phase plan ID:
`288-VERIFICATION.md` must be complete with zero open counters and its reviewed
head/tree and typed adapter evidence must match the literal
`upstream-contract-pins.yaml` record before this projection consumes any Phase
288 record. A missing, planned, pending, substituted, or drifted Phase 288
closure is a typed refusal; this plan may not infer or regenerate the adapter
contract.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement explicit allowlist projection from completed local reasoning</name>
  <files>crates/swarm-runtime/src/herd_memory_projection.rs, crates/swarm-runtime/src/lib.rs, crates/swarm-runtime/tests/herd_memory_projection.rs</files>
  <read_first>
    - `tools/check-herd-memory-upstreams.sh` (accepted prerequisite and resolved Phase 287/288 adapter/Cargo/corpus pins)
    - `crates/swarm-spine/src/herd_memory.rs` (strict body/opaque-ref public contract)
    - `crates/swarm-spine/src/investigation.rs` and `src/incident.rs` (source records and prohibited rich fields)
    - `crates/swarm-runtime/src/sphinx_agent.rs` (graph/entity/contribution extraction seams)
    - `crates/swarm-runtime/src/strategy.rs` (strategy utility/provenance values)
    - `crates/swarm-runtime/src/investigation.rs` (completed investigation outcome fields)
    - `scenarios/herd-memory/manifest.yaml` and `crates/swarm-runtime/tests/herd_memory_oracle.rs`
  </read_first>
  <behavior>
    - `ArenaSynthesisInput` is accepted only when the Phase 288 Arena-owned `Phase287ArenaSynthesisAdapter` has supplied the six closed `ArenaSourceRole` refs with schema, canonical ID, content, payload, and partition digests; Arena separately owns the evaluator-only upstream-stage conversion, golden-vector tuple digest, sorted known-set aggregate, and authenticated attribution. Runtime consumes none of those private evaluator types. Phase 289 rejects aliases, unknown versions, duplicate canonical IDs, digest mismatch, raw telemetry, and missing upstream closure pins.
    - The accepted Arena adapter contract must expose the exhaustive typed upstream-stage conversion, literal five-field canonical bytes, Phase 287 golden vector, per-tuple digest, sorted `phase_287_known_evasion_set_sha256`, and derived attribution described in Plan 06; a missing conversion, guessed Runtime reconstruction, caller-supplied attribution, or path-only adapter pin is a prerequisite refusal.
    - A valid `HerdMemoryProjectionInput` yields body schema version 1 with attack abstractions, motifs, detector/response outcomes, strategy utility, source-corpus digest, bounded confidence, and transformation history only.
    - Projection is constructed from explicit typed fields and digests; it never calls `serde_json::to_value`/serializes `InvestigationBundle`, `ReplayBundle`, `CorrelatedIncident`, Sphinx `EntityNode`/contributions, generic evidence views, or arbitrary source JSON.
    - Runtime construction obtains the resolver only through `HerdMemoryRuntimeFactory::from_config`/`OpaqueKeyResolverFactory::from_config`, which opens the repository-owned `FileOpaqueKeyProvider` at the validated `HerdMemoryConfig.opaque_key_root`; a missing/insecure root or test/process-local fallback fails before projection.
    - Missing source digest, unbounded list/field, prohibited raw field, unsupported schema, or transformation mismatch returns a typed privacy/validation error without serializing partial output.
  </behavior>
  <action>
    Mandatory wrapper first: `bash tools/check-herd-memory-upstreams.sh --run -- bash -c '<task command>'`. No direct gate, cargo, fixture, or implementation command may precede it.
    Before projection, construct `[ArenaSourceRef; 6]` and call the Plan 00
    `ArenaLineage::from_phase288_sources` constructor with the adapter-derived
    `phase287_source_attribution_digest`. Do not assemble a
    `BTreeSet` or selected source manually: the constructor enforces the exact
    six-role set, cardinality, unique canonical IDs, selected-source
    membership, and canonical sorted aggregate digest. Its mutation table
    covers every source-role and signed-state field before the existing
    projection/export path can proceed.
    The mandatory wrapper above has already performed the accepted prerequisite
    gate; no second direct gate invocation occurs. Consume—not recreate—
    `ArenaSynthesisInput` emitted by the Phase 288 Arena-owned
    `Phase287ArenaSynthesisAdapter` in `crates/swarm-arena/src/synthesis_adapter.rs`.
    Revalidate its `ArenaSourceRef` values against the resolved pins: each must
    carry one of the six closed `ArenaSourceRole` variants, schema version,
    canonical ID, content digest, payload digest, and partition digest. The
    adapter has already validated the full `BlueOutcome`, `ArenaIngestResult`,
    `Phase286InvestigationCapture`, `ArenaArtifactStore`, `PairReport`, and
    `SignedBlueLearnedState` records and the full signed learned-state envelope;
    Phase 289 validates lineage/pin equality before projection. Build the
    detached `ArenaLineage` and complete `SignedStateExpectation` above; reject
    a mutation of every adapter/DTO/Cargo/corpus/detached source
    digest/selected source/role/schema/canonical-ID/content/payload/partition
    field, every `ArenaContractPins` path/digest, and every signed-state kind/signer/stream/sequence/generation/
    predecessor/fence/schema/signature/payload/content/capture/receipt/preview/
    rollback field. Require the accepted Arena adapter's exhaustive
    upstream-stage conversion, literal Phase 287 tuple bytes, golden vector,
    sorted aggregate digest, and derived `Phase287SourceAttribution` to be
    present in the resolved bridge contract; Runtime consumes only the
    resulting `ArenaSynthesisInput` and never reconstructs tuples, attribution,
    or fingerprints. Reject aliases, unknown/renamed fields, duplicated IDs, changed
    content/payload/partition digests, raw telemetry, and missing upstream
    closure pins. Do not add a Runtime-to-Arena dependency, define a second
    producer adapter, add a `KnownEvasionSet` role, accept a guessed module
    path, or regenerate the known set.

    Add `HerdMemoryProjectionInput` and `project_herd_memory` in a new runtime module, importing only the Runtime-owned `ArenaSynthesisInput`, `ArenaSourceRef`, and `ArenaSourceRole` types. The input must contain only typed attack abstraction IDs/enums, causal motif IDs/relations, detector/response outcome summaries, strategy utility basis points, typed source evidence digests, a bounded transformation list, and the `HerdMemoryConfig` tenant/export namespace plus resolver reference. Add explicit constructors from completed local investigation/graph/strategy state and adapter-produced lineage that copy only these fields and discard all raw values; do not implement `Serialize` for the rich source input and do not accept a generic map/value. Route every digest-shaped field through the single neutral-core `Digest64` parser/canonicalizer defined by Plan 01; public refs are literal lower-case 64-hex values, never `0x` strings, upper-case values, or alternate display/debug helpers.

    Validate all input bounds against `HerdMemoryConfig.max_record_bytes`/`max_transformations`, require a nonempty source-corpus digest and transformation lineage, enforce confidence `0..=10_000`, and construct the Plan 01 `HerdMemoryBody` with schema version 1. Resolve keys only through the Plan 01 `OpaqueKeyResolver` produced by `HerdMemoryRuntimeFactory::from_config`/`OpaqueKeyResolverFactory::from_config`, which opens the repository-owned `FileOpaqueKeyProvider` at the configured `opaque_key_root` and binds tenant, export namespace, resolver reference, and key epoch; do not accept arbitrary key bytes or a test/process-local resolver. Call the HMAC helper for host/user/process/IP-derived references before dropping normalized values. The serialized body must contain only HMAC digest/key epoch/`tenant_scope_digest`/`export_namespace_digest` values, never the normalized value or a reversible plain hash. Preserve response/detector outcomes as abstract typed outcomes; never add a receipt, action, approval, policy verdict, operator note, command line, URL, or raw evidence field.

    Write tests first for `arena_synthesis_input_accepts_phase287_lineage_and_phase288_adapter_evidence`, `arena_synthesis_input_rejects_wrong_role_schema_id_or_digest`, valid projection, each of the eleven independent prohibited categories, nested raw values hidden inside an otherwise valid source, missing/changed source digest, transformation mutation, unknown schema, over-limit input, HMAC tenant/namespace/key/epoch separation, resolver-reference mismatch, retired-key refusal, lower-case/0x/upper-case/wrong-length digest mutations, and canonical repeatability. The Arena bridge tests—not this Runtime test—own exhaustive upstream-stage conversion, golden-vector, attribution-derivation, tuple-digest, and aggregate-known-set coverage. Name the primary allowlist test `projection_is_allowlist_only_and_hmac_opaque` and the validation negative `projection_serialization_rejects_authority_and_raw_fields` because Plan 00 and the validation matrix consume those exact targets. Assert serialized JSON and `Debug` output contain none of the normalized host/user/process/IP values or secret/credential material.
  </action>
  <verify>
    <automated>bash tools/check-herd-memory-upstreams.sh --run -- bash -c 'cargo test -p swarm-runtime --test herd_memory_projection --locked --offline -- --exact arena_synthesis_input_accepts_phase287_lineage_and_phase288_adapter_evidence --test-threads=1 && cargo test -p swarm-runtime --test herd_memory_projection --locked --offline -- --exact projection_serialization_rejects_authority_and_raw_fields --test-threads=1 && cargo clippy -p swarm-runtime --all-targets --locked --offline -- -D warnings && git diff --check'</automated>
  </verify>
  <acceptance_criteria>
    - `Phase287ArenaSynthesisAdapter` is the sole Arena-owned producer of `ArenaSynthesisInput`; Phase 289 accepts only its six `ArenaSourceRole`/`ArenaSourceRef` records with matching schema/canonical-ID/content/payload/partition digests, plus the detached source digest and selected source ref, and upstream pins. Arena—not Runtime—owns the exact Phase 287 tuple bytes, golden vector, sorted `phase_287_known_evasion_set_sha256`, and derived attribution; no Phase 289-specific recipe, role, Runtime-to-Arena dependency, or guessed module path is accepted.
    - `project_herd_memory` has no generic `serde_json::Value`/raw telemetry input and no whole-source serialization call.
    - The projection test suite rejects all manifest prohibited categories, nested mutations, unknown fields, missing source/transformation digests, and over-limit records.
    - All digest-shaped refs are literal lower-case 64-hex values; `0x`, upper-case, non-hex, wrong-length, and alternate display encodings fail closed. Host/user/process/IP-derived refs are tenant/export-namespace-bound HMAC digests with key epoch plus `tenant_scope_digest` and `export_namespace_digest`; normalized inputs are absent from body bytes, JSON, `Debug`, and errors.
    - The resulting body contains only the five allowlisted families and typed digest/transform metadata.
    - Detector/response outcomes are closed, authority-free summaries: no action, policy, approval, receipt, capability, dispatcher, operator, URL, command, or local-corroboration field is representable in the exported body.
  </acceptance_criteria>
  <done>An explicit runtime allowlist projection produces privacy-minimized typed bodies and proves nested raw/secret/authority data cannot cross the export boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Sphinx completed-investigation export to the projection only</name>
  <files>crates/swarm-runtime/src/sphinx_agent.rs, crates/swarm-runtime/src/lib.rs, crates/swarm-runtime/tests/herd_memory_projection.rs</files>
  <read_first>
    - `crates/swarm-runtime/src/sphinx_agent.rs` (`extract_entities`, memory contributions, completed investigation and snapshot writes)
    - `crates/swarm-runtime/src/herd_memory_projection.rs` after Task 1
    - `crates/swarm-spine/src/investigation.rs` and `src/incident.rs`
    - `crates/swarm-spine/src/store.rs` (`ReplayBundle` raw/receipt-bearing record to exclude)
    - `crates/swarm-runtime/tests/negative_herd_memory_boundary.rs`
  </read_first>
  <behavior>
    - The only Sphinx herd export call reaches `project_herd_memory` with typed completed-state fields and returns a body/envelope input; no existing rich snapshot, entity contribution, evidence view, replay bundle, receipt, or action is serialized as herd memory.
    - A source containing a host/user/process/IP or operator/receipt/action field either yields a keyed opaque ref through the explicit constructor or a typed rejection; it never reaches a generic serializer.
    - If the projection fails, no partial memory ID/body is returned and the local investigation remains available for local use without producing a privacy-invalid import.
  </behavior>
  <action>
    Mandatory wrapper first: `bash tools/check-herd-memory-upstreams.sh --run -- bash -c '<task command>'`. No direct gate, cargo, fixture, or implementation command may precede it.
    Add one narrow `SphinxAgent::export_herd_memory` method (or an equivalent completed-investigation adapter with that exact seam name) that constructs `HerdMemoryProjectionInput` field by field from the completed typed graph/strategy outcome, obtains the resolver from `HerdMemoryRuntimeFactory::from_config`/`FileOpaqueKeyProvider` at the validated config root, passes the configured tenant/export namespace/key epoch/opaque-key resolver reference from `HerdMemoryConfig` to `project_herd_memory`, and returns only the typed body plus source-corpus/transformation metadata required by the Plan 02 envelope. Route the result exclusively to the public opaque `HerdMemoryLifecycleHandle::prepare_export` and then `HerdMemoryLifecycleHandle::commit_export` reservation pair; the handle-owned `HerdMemoryExportCoordinator` remains private to `swarm-spine`, and Sphinx may not sign, serialize, write, or mutate lifecycle state directly. Crash/retry/concurrency errors return typed lifecycle refusals without accepted partial exports. Do not add a second Sphinx serialization format, reuse `answer_memory_query`/`SphinxMemoryContribution` as a wire record, accept arbitrary key bytes, or expose raw entity values to the caller.

    Define the exact boundary scan for the real `crates/swarm-runtime/src/sphinx_agent.rs`: require a non-empty file, extract the balanced-brace body of `fn export_herd_memory`, require exactly one `project_herd_memory(` call and exactly one config-bound `HerdMemoryRuntimeFactory::from_config`/resolver construction within that body, and reject `serde_json::to_value`, `serde_json::json!`, `serde_json::to_string`, `ReplayBundle`, `SphinxMemoryContribution`, `EntityNode`, `ResponseAction`, receipt/action/approval/policy/dispatcher tokens, or any second serializer/export seam. Mutations deleting the method, duplicating the projection call, renaming the call, bypassing the factory, or serializing a rich source fixture must fail closed.

    Keep the export path enrichment-only: it may return an advisory body for the coordinator, but cannot invoke policy, approval, receipt, dispatcher, response adapter, or a live action. Extend the projection tests with a full Sphinx-shaped fixture containing nested raw entity values, `ReplayBundle` receipts/actions, and operator notes; assert the safe fields survive only as typed abstraction IDs/opaque refs and all forbidden values are absent. Mutate every `ArenaLineage` and `SignedStateExpectation` field independently and assert no mutation reaches the export coordinator or evaluator.
  </action>
  <verify>
    <automated>bash tools/check-herd-memory-upstreams.sh --run -- bash -c 'cargo test -p swarm-runtime --test herd_memory_projection --locked --offline -- --exact sphinx_export_uses_only_typed_projection --test-threads=1 && cargo test -p swarm-runtime --test negative_herd_memory_boundary --locked --offline -- --test-threads=1 && git diff --check'</automated>
  </verify>
  <acceptance_criteria>
    - `sphinx_agent.rs` contains exactly one `export_herd_memory` seam that calls `project_herd_memory` through the configured resolver; the Plan 00 boundary scan reads this real file, extracts its balanced body, requires one projection/factory call, and rejects direct serialization of rich Sphinx/investigation/evidence/replay types.
    - The full-source fixture proves receipts/actions/operator notes are not present in the output and that normalized identifiers are HMAC-only with both scope digests present.
    - The exported body has only the five approved abstraction families and lower-case 64-hex digest/reference fields; response outcomes are outcome-only and cannot affect local policy, response, dispatcher, or corroboration state.
    - Projection failure is typed and atomic: no partial body/memory ID is emitted, no local state is deleted, and no authority path is reachable.
  </acceptance_criteria>
  <done>Completed Sphinx reasoning can export only the dedicated privacy allowlist, with no raw telemetry or response-authority shortcut.</done>
</task>

</tasks>

<verification>
Run the projection target, its exact Sphinx export test, the independent privacy/authority boundary tests, runtime clippy, format, and diff checks. Inspect imports and serialized output for no raw telemetry, secrets, operator credentials, receipts, actions, approval, or response authority.
</verification>

<success_criteria>
- HERDMEM-01 is satisfied by a separate typed projection and nested mutation proof, not by redacting rich source JSON.
- HMAC opaque references prevent raw or low-entropy identifiers from crossing the export boundary and preserve key-epoch separation.
- Sphinx remains a local enrichment producer; projection failures cannot alter policy, response, receipt, or operator authority.
</success_criteria>

<output>After completion, create `.planning/phases/289-herd-memory/289-04-SUMMARY.md` with projection field inventory, source exclusions, HMAC evidence, and Sphinx integration tests.</output>
