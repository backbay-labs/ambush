---
phase: 289-herd-memory
plan: 01B
type: execute
wave: 3
depends_on: [289-01]
files_modified:
  - Cargo.lock
  - crates/swarm-spine/Cargo.toml
  - crates/swarm-spine/src/herd_memory.rs
  - crates/swarm-spine/src/herd_key_resolver.rs
  - crates/swarm-spine/src/lib.rs
  - crates/swarm-spine/tests/herd_memory_export.rs
  - crates/swarm-spine/tests/herd_key_resolver.rs
autonomous: true
requirements: [HERDMEM-01, HERDMEM-02, HERDMEM-05]
must_haves:
  truths:
    - "The public body is a versioned typed allowlist containing only the five approved abstraction families, bounded digests, transformations, and HMAC opaque references; raw telemetry, secrets, identifiers, credentials, receipts, actions, and arbitrary JSON are unrepresentable."
    - "The repository-owned FileOpaqueKeyProvider is the only production opaque-key provider, uses zeroized bytes, owner-only custody, atomic generations, and no environment/process-local/test fallback."
    - "First-run key provisioning is an explicit fail-closed operation; startup never silently creates or repairs an absent or insecure key root."
  artifacts:
    - path: "Cargo.lock"
      provides: "locked workspace resolution and checksum metadata for the workspace zeroize dependency"
      contains: "name = \"zeroize\""
    - path: "crates/swarm-spine/Cargo.toml"
      provides: "workspace-managed zeroize dependency for key-byte cleanup"
      contains: "zeroize.workspace = true"
    - path: "crates/swarm-spine/src/herd_memory.rs"
      provides: "strict body/opaque-reference contract"
      exports: ["HerdMemoryBody", "OpaqueEntityRef", "OpaqueKeyResolver", "opaque_entity_ref"]
    - path: "crates/swarm-spine/src/herd_key_resolver.rs"
      provides: "FileOpaqueKeyProvider, durable descriptor generations, rotation, bootstrap, and scoped resolver"
      exports: ["FileOpaqueKeyProvider", "ConfiguredOpaqueKeyResolver", "OpaqueKeyResolverFactory"]
    - path: "crates/swarm-spine/tests/herd_memory_export.rs"
      provides: "typed privacy/schema/HMAC mutation tests"
      contains: "typed_body_rejects_unknown_and_prohibited_fields"
    - path: "crates/swarm-spine/tests/herd_key_resolver.rs"
      provides: "provider custody, bootstrap, rotation, restart, and scope mutation tests"
      contains: "file_provider_bootstrap_requires_secure_root"
  key_links:
    - from: "crates/swarm-spine/Cargo.toml"
      to: "crates/swarm-spine/src/herd_key_resolver.rs"
      via: "workspace zeroize dependency is explicit and locked before key provider implementation"
      pattern: "zeroize.workspace = true"
    - from: "crates/swarm-spine/Cargo.toml"
      to: "Cargo.lock"
      via: "locked metadata/checksum resolution is updated and verified with --locked"
      pattern: "zeroize"
    - from: "crates/swarm-spine/src/herd_memory.rs"
      to: "swarm-crypto::hmac_sha256_hex"
      via: "domain-separated tenant/export-namespace/key-epoch opaque references"
      pattern: "hmac_sha256_hex"
    - from: "crates/swarm-spine/src/herd_key_resolver.rs"
      to: "crates/swarm-core/src/config/state.rs"
      via: "validated opaque_key_root, resolver reference, tenant, namespace, and epoch"
      pattern: "opaque_key_root"
---

<objective>
Implement the strict wire-body contract and the concrete repository-owned opaque-key provider after Plan 01 has frozen configuration.

Purpose: a privacy boundary and key custody seam must be executable before trust, projection, or importer code can consume it.
Output: typed body/HMAC references, an explicit zeroize dependency, and durable file-provider bootstrap/rotation tests.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/289-herd-memory/289-CONTEXT.md
@.planning/phases/289-herd-memory/289-RESEARCH.md
@.planning/phases/289-herd-memory/289-00-PLAN.md
@.planning/phases/289-herd-memory/289-01-PLAN.md
@crates/swarm-spine/Cargo.toml
@crates/swarm-spine/src/lib.rs
@crates/swarm-core/src/config/state.rs
@crates/swarm-core/src/config/secrets.rs
@crates/swarm-governance/src/lib.rs
@crates/swarm-crypto/src/hashing.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the versioned privacy allowlist body and keyed opaque references</name>
  <files>crates/swarm-spine/src/herd_memory.rs, crates/swarm-spine/src/lib.rs, crates/swarm-spine/tests/herd_memory_export.rs</files>
  <read_first>
    - crates/swarm-spine/src/investigation.rs and src/incident.rs
    - crates/swarm-core/src/telemetry.rs
    - crates/swarm-crypto/src/hashing.rs and src/lib.rs
    - scenarios/herd-memory/manifest.yaml
    - crates/swarm-runtime/tests/herd_memory_oracle.rs
  </read_first>
  <behavior>
    - Valid body records contain only AttackAbstraction, CausalMotif, DetectorOutcomeSummary, ResponseOutcomeSummary, StrategyUtilitySummary, bounded typed IDs/digests, and TransformationRecord values under schema version 1.
    - Every nested type denies unknown fields; generic maps, serde_json::Value, raw telemetry, credentials, receipts, actions, approval, policy, response, URLs, and command lines are impossible to serialize as HerdMemoryBody.
    - A fixed tenant/export namespace/key epoch/kind produces deterministic HMAC output; changing tenant, namespace, key, or epoch changes the digest, and normalized input is absent from bytes, Debug, errors, and persisted state.
  </behavior>
  <action>
    Create the strict schema with HERD_MEMORY_SCHEMA_V1, deny_unknown_fields on every nested record, bounded strings/collections, u16 confidence basis points, typed source-evidence digests, and transformation lineage. Define OpaqueKeyResolver and a non-debug/non-serializable ResolvedOpaqueKey; resolve must reject missing scope, cross-tenant lookup, unknown/retired epoch, and resolver-reference mismatch without exposing key bytes. Define OpaqueEntityRef with ref_digest, key_epoch, tenant_scope_digest, and export_namespace_digest. Construct the exact domain-separated HMAC message ambush.herd.entity-ref.v2:{tenant_id}:{export_namespace}:{key_epoch}:{kind}:{normalized}, using swarm_crypto::hmac_sha256_hex only through the resolver seam; never use plain hashes or raw values.

    Write strict-TDD tests for valid canonical round-trip, all eleven public prohibited categories including nested mutation, unknown schema/key, bounds, resolver-reference mismatch, tenant/namespace/key/epoch separation, retired-key refusal, and absence of normalized values in serialized and Debug output. Prove rich InvestigationBundle, CorrelatedIncident, Sphinx entities/contributions, generic evidence, and ReplayBundle types cannot be accepted as the wire body by type or field inspection.
  </action>
  <verify>
    <automated>cargo test -p swarm-spine --test herd_memory_export -- --test-threads=1 && cargo clippy -p swarm-spine --all-targets -- -D warnings && cargo fmt --all -- --check && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The body is a five-family typed allowlist with no generic value or authority member, and every prohibited mutation fails.
    - Opaque references are lower-case 64-hex keyed digests carrying both scope digests and key epoch; raw normalized values never cross the seam.
  </acceptance_criteria>
  <done>The typed body and resolver trait are fixed in the spine before trust or runtime projection consumes them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement durable FileOpaqueKeyProvider custody, secure first-run bootstrap, and rotation</name>
  <files>Cargo.lock, crates/swarm-spine/Cargo.toml, crates/swarm-spine/src/herd_key_resolver.rs, crates/swarm-spine/src/lib.rs, crates/swarm-spine/tests/herd_key_resolver.rs</files>
  <read_first>
    - crates/swarm-spine/src/herd_memory.rs after Task 1
    - crates/swarm-core/src/config/state.rs and src/validation.rs
    - crates/swarm-core/src/config/secrets.rs
    - crates/swarm-governance/src/lib.rs
    - crates/swarm-spine/Cargo.toml
  </read_first>
  <behavior>
    - The spine manifest declares zeroize.workspace = true; no ad hoc version, optional feature, environment secret, process-local map, test resolver, or hard-coded key can satisfy a production build.
    - An explicit provision_first_opaque_key(config) operation is the only first-run bootstrap. It creates a new root only with owner UID, directory mode 0700, no symlink/no-follow, and a generated key/descriptor file mode 0600; it uses OS CSPRNG bytes, create_new, bounded writes, file sync, atomic descriptor rename, and parent-directory sync. Startup open never auto-creates, repairs, or downgrades an absent/insecure root.
    - Existing roots with missing descriptor/key, foreign owner, group/world permissions, symlink, malformed generation, tampered digest, partial rotation, or wrong resolver/tenant/namespace/epoch fail closed.
    - Rotation writes a complete next generation and key file atomically, records predecessor/generation/status history, zeroizes loaded key bytes, and permits retired epochs only for same-scope verification of already persisted references; new exports require current epoch.
  </behavior>
  <action>
    Add zeroize.workspace = true under [dependencies] in crates/swarm-spine/Cargo.toml and keep the workspace lockfile authoritative. Implement FileOpaqueKeyProvider at HerdMemoryConfig.opaque_key_root with typed descriptor/generation metadata and epoch-scoped 0600 key files. Separate explicit provisioning from open: provision_first_opaque_key is an administrative/bootstrap seam that rejects an existing path, refuses insecure parent/root state, generates key bytes from the OS CSPRNG, writes with create_new and 0600 before any bytes are persisted, zeroizes temporary buffers, fsyncs the file, atomically installs the descriptor, and fsyncs the parent. If any permission/ownership/no-follow check fails, return a typed error and leave no usable partial state. The normal open/factory path must never call this bootstrap implicitly.

    Enforce owner-only 0700 root and 0600 descriptor/key files on every open; reject foreign owner, symlink, non-directory, traversal, unknown resolver/key reference, malformed canonical metadata, generation/predecessor mismatch, and key-file digest tamper. Rotation uses a new generation/key file plus atomic rename and parent sync; status transitions Current/Retired/Revoked and predecessor digest are committed together. Load key bytes only into zeroize::Zeroizing buffers and return a non-debug/non-serializable handle. External HSM/KMS is documented only as a future optional adapter, never a current dependency.

    Write tests first for file_provider_bootstrap_requires_secure_root, file_provider_bootstrap_is_atomic_and_restartable, file_provider_rejects_missing_key, file_provider_rejects_insecure_permissions, file_provider_rejects_foreign_owner, file_provider_rejects_symlink, file_provider_rejects_tampered_generation, file_provider_rotates_atomically, file_provider_reopen_preserves_epoch_status, retired_key_fails_for_new_export, rotation_requires_current_epoch_and_scope, cross_tenant_or_namespace_lookup_fails_closed, and no_env_process_local_or_test_resolver_fallback.
  </action>
  <verify>
    <automated>cargo test -p swarm-spine --test herd_key_resolver --locked -- --test-threads=1 && cargo check -p swarm-spine --locked && cargo clippy -p swarm-spine --all-targets --locked -- -D warnings && cargo fmt --all -- --check && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - crates/swarm-spine/Cargo.toml explicitly uses zeroize.workspace = true.
    - Cargo.lock records the resolved zeroize package/checksum and all focused provider/build checks pass with `--locked`; a manifest/lock mismatch fails before tests.
    - Bootstrap is explicit, owner-only, 0700/0600, CSPRNG-backed, atomic, restart-safe, and fail-closed; open never silently creates or repairs custody.
    - Missing/insecure/foreign/tampered/rotated/revoked/cross-scope cases fail closed and key bytes are zeroized; no process-local/test/env fallback exists.
  </acceptance_criteria>
  <done>The repository-owned provider has secure first-run provisioning and durable rotation evidence without an OS-keychain/HSM runtime dependency.</done>
</task>

</tasks>

<verification>
Run the spine body and resolver tests, inspect the explicit zeroize.workspace dependency, run spine clippy/format/diff, and confirm provider bootstrap/open/rotation mutations fail closed before runtime factory construction.
</verification>

<success_criteria>
- HERDMEM-01 body privacy and HMAC scope are typed and mutation-tested.
- FileOpaqueKeyProvider is the concrete production seam with explicit secure bootstrap, owner-only custody, zeroization, atomic generations, and no fallback.
- The runtime factory is intentionally deferred to Plan 01C; no unlisted runtime file is changed here.
</success_criteria>

<output>After completion, create .planning/phases/289-herd-memory/289-01B-SUMMARY.md with body schema, zeroize dependency, provider bootstrap, rotation, and custody evidence.</output>
