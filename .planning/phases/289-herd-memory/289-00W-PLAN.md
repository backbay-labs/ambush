---
phase: 289-herd-memory
plan: 00W
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/check-herd-memory-upstreams.sh
  - tools/sha256-root.sh
  - artifacts/phase289/upstream-prerequisite-gate.json
autonomous: true
requirements: [HERDMEM-01, HERDMEM-02, HERDMEM-03, HERDMEM-04, HERDMEM-05, HERDMEM-06]
must_haves:
  truths:
    - "Exactly one tools-only prerequisite gate runs before any Phase 289 fixture, oracle, compilation, benchmark, or production work; every later plan reaches this Wave 0 gate through a non-empty dependency chain."
    - "The accepted gate verifies the exact Phase 286 07B, Phase 287 06, and Phase 288 07 closure artifacts, typed reviewed HEAD/tree object IDs plus frozen-tree digests, canonical evidence digests, and recomputed zero findings; partial ledgers, path strings, planned records, stale trees, dirty/untracked trees, or substituted artifacts fail closed."
    - "After acceptance, the gate resolves actual Phase 287 adapter/DTO/Cargo/corpus pins and Phase 288 runtime-contract/source/tree/evidence pins; it never writes fixtures, baselines, evaluator manifests, test bodies, or production modules."
    - "The root SHA-256 helper has one unprefixed lowercase 64-hex output format and a format self-test; no alternate digest helper is introduced."
    - "Every non-Wave-0 Phase 289 task begins by invoking the mandatory --run wrapper on the accepted upstream gate; the wrapper executes the exact prerequisite check before forwarding one task command and cannot be bypassed by a direct cargo, fixture, oracle, or implementation invocation."
  artifacts:
    - path: "tools/check-herd-memory-upstreams.sh"
      provides: "tools-only external closure gate with accepted/rejected status and typed upstream pin resolution"
      contains: "--require-accepted"
    - path: "tools/sha256-root.sh"
      provides: "sole unprefixed lowercase-64 root digest helper and format self-test"
      contains: "^[0-9a-f]{64}$"
    - path: "artifacts/phase289/upstream-prerequisite-gate.json"
      provides: "retained accepted gate record with exact paths, typed reviewed head/tree object IDs, frozen-tree/artifact digests, and recomputed zero findings"
      contains: "prerequisite_status"
  key_links:
    - from: "tools/check-herd-memory-upstreams.sh"
      to: "artifacts/phase289/upstream-prerequisite-gate.json"
      via: "accepted gate record is written only after all external closure checks pass"
      pattern: "prerequisite_status.*accepted"
    - from: "tools/check-herd-memory-upstreams.sh"
      to: ".planning/phases/286-collective-hypothesis-graph/286-07B-SUMMARY.md|.planning/phases/287-adversarial-co-evolution-arena/287-06-SUMMARY.md|.planning/phases/288-autonomous-detector-response-synthesis/288-07-SUMMARY.md"
      via: "exact upstream closure sets, reviewed HEAD/tree, evidence digests, and zero-finding recomputation"
      pattern: "286-07B-SUMMARY|287-06-SUMMARY|288-07-SUMMARY|HEAD\\^\\{tree\\}|open_p0|open_p1|open_p2"
    - from: "tools/sha256-root.sh"
      to: "tools/check-herd-memory-upstreams.sh"
      via: "the gate uses the sole canonical unprefixed lowercase-64 digest interface"
      pattern: "sha256-root.sh|^[0-9a-f]{64}$"
---

<objective>
Establish and execute the external prerequisite gate before any Phase 289
fixture, oracle, compilation, benchmark, or implementation work. The same
Wave 0-owned script also exposes the mandatory `--run -- <command>` wrapper:
it performs `--require-accepted --locked-tree` as its first operation, then
executes exactly the forwarded command. Wave 0 Task 1 is the sole bootstrap
exception because it creates that gate and wrapper; every later task must use
the wrapper, even when its command is a shell script or a multi-command test.

Purpose: upstream closure must be real, current, independently reviewed, and
digest-pinned before downstream plans can author truth or consume contracts.
This is the sole `depends_on: []` plan; every other Phase 289 plan depends
directly or transitively on it.

Output: the canonical root digest helper, the executable upstream gate, and a
retained accepted/rejected gate record. This plan deliberately does not create
fixtures, baselines, evaluator manifests, tests, or production modules.
The Phase 287 corpus paths are exactly `catalog.yaml`, `partitions.yaml`,
`historical-attacks.yaml`, `benign-controls.yaml`, `counterexamples.yaml`,
`withheld-campaigns.yaml`, `baseline.json`, and `oracle-registry.json` under
`scenarios/adversarial-coevolution-arena/`; no guessed or legacy path is
accepted.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/289-herd-memory/289-CONTEXT.md
@.planning/phases/289-herd-memory/289-RESEARCH.md
@.planning/phases/286-collective-hypothesis-graph/286-07B-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-06-PLAN.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-07-PLAN.md
</context>

<task type="auto">
  <name>Task 1: Create and execute the tools-only upstream prerequisite gate</name>
  <files>
    <file>tools/sha256-root.sh</file>
    <file>tools/check-herd-memory-upstreams.sh</file>
    <file>artifacts/phase289/upstream-prerequisite-gate.json</file>
  </files>
  <action>
    Implement the sole root digest helper with a shell-safe unprefixed
    lowercase 64-hex output and an explicit format self-test. Implement the
    sole `tools/check-herd-memory-upstreams.sh` owner with `--self-test`,
    `--require-accepted --locked-tree`, and mandatory `--run -- <command>`
    modes. `--run` must execute the exact locked-tree gate first, reject a
    missing command or nested alternate gate, then `exec` only the forwarded
    command with its original argv. Before any other Phase 289 plan
    executes, run the gate against the exact Phase 286 07B, Phase 287 06, and
    Phase 288 07 closure summary/review/verification/validation/evidence sets.
    Require regular non-symlink files, complete status, exact task/requirement/
    artifact rows, distinct reviewer and implementer, current reviewed HEAD
    and `HEAD^{tree}`, canonical recomputed artifact/evidence digests, and one
    recomputed zero for every P0/P1/P2 class. Reject partial Phase 286
    validation ledgers, path-only or planned/pending records, stale or
    dirty/untracked trees, substituted artifacts, missing/duplicate rows, and
    any nonzero recomputed finding. The wrapper self-test must prove that a
    direct forwarded command, missing `--`, reordered gate, nested alternate
    checker, or empty argv is rejected and that a valid forwarded command runs
    only after the accepted record is revalidated. Only after acceptance resolve the actual
    Phase 287 adapter source/DTO/Cargo membership/eight corpus files/oracle
    registry and Phase 288 source/runtime contract/tree/evidence pins and
    write the retained record. Do not create or edit fixtures, baselines,
    evaluator manifests, oracle/test bodies, or production modules in this
    task; a rejected gate leaves all later plans blocked.
  </action>
  <verify>
    <automated>bash -n tools/sha256-root.sh && bash tools/sha256-root.sh --self-test && bash -n tools/check-herd-memory-upstreams.sh && bash tools/check-herd-memory-upstreams.sh --self-test && bash tools/check-herd-memory-upstreams.sh --require-accepted --locked-tree && git diff --check -- .planning/phases/289-herd-memory</automated>
    <manual>Confirm the retained record is accepted only from the exact current upstream closure artifacts and contains actual adapter/DTO/Cargo/corpus/runtime/evidence digests; confirm no fixture, evaluator, oracle, benchmark, or production file is authored by this Wave 0 task.</manual>
  </verify>
  <done>Both tools pass syntax and self-tests, the exact locked-tree prerequisite gate emits an accepted retained record with recomputed zero findings and all required upstream pins, and no later Phase 289 work can run without this accepted record.</done>
</task>
