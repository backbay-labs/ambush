---
phase: 276-crowdstrike-rtr-adapter
plan: 01
type: execute
depends_on:
  - 275-kubernetes-audit-detector-and-cross-cloud-integration-proof
files_modified:
  - crates/swarm-core/src/config/response.rs
  - crates/swarm-response/src/lib.rs
  - crates/swarm-response/src/config.rs
  - crates/swarm-response/src/dispatch.rs
  - crates/swarm-response/src/crowdstrike_rtr.rs
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-runtime/src/ingest/health.rs
autonomous: true
requirements:
  - EDRINT-01
  - EDRINT-02
  - EDRINT-03
---

<objective>
Add one repo-owned CrowdStrike RTR response adapter on the existing
`ResponseExecutor` seam with OAuth2 token exchange, bounded session management,
and mock-backed integration proof for host isolation, process kill, file
quarantine, and failure handling.
</objective>

<tasks>

<task>
  <name>Add a first-class `crowdstrike_rtr` response adapter config and health identity</name>
  <done>`swarm-core`, runtime secret resolution, and ingest health all recognize a dedicated CrowdStrike RTR adapter variant instead of overloading the generic HTTP wrapper.</done>
</task>

<task>
  <name>Implement the RTR adapter on the existing resilience contract</name>
  <done>`CrowdStrikeRtrAdapter` exchanges OAuth2 client credentials for a bearer token, creates bounded RTR sessions for command-backed actions, translates isolate/kill/quarantine requests onto the repo-owned mock API, and returns normalized receipts that fit `ResilientExecutor` dead-letter handling.</done>
</task>

<task>
  <name>Prove the shipped behavior against a repo-owned mock RTR surface</name>
  <done>Integration tests cover auth, session creation, host isolation, process kill, file quarantine, timeout handling, and final failed executions surfacing through the existing dead-letter contract.</done>
</task>

</tasks>
