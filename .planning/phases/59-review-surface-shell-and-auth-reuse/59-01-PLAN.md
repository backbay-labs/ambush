# Phase 59 Plan

1. Extend `operator_http` with a read-only HTML review shell route group that runs behind the existing bearer-auth middleware and shared operator state.
2. Add reusable HTML layout, escaping, and navigation helpers so later evidence and promotion review pages can compose into one local surface.
3. Expose a review home page that links to the authenticated JSON evidence API and the new HTML drill-down pages by stable ID.
4. Add focused operator-surface tests that verify auth reuse, HTML responses, and the read-only review shell behavior.
5. Update operator docs to document the local review entry points and explicitly keep the surface advisory and read-only.
