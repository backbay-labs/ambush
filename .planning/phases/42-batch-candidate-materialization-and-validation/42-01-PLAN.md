# Plan 42-01

1. Add durable materialization-batch and validation-batch artifacts to the mutation module.
2. Materialize all variants in one mutation spec into distinct repo-owned experiment manifests.
3. Refresh validation bundles across that batch through the existing replay, proof, shadow, and scorecard lanes.
4. Expose batch materialization and batch validation commands in `swarmctl` and cover both with runtime tests.
