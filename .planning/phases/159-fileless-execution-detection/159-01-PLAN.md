---
phase: 159-fileless-execution-detection
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 158
files_modified:
  - crates/swarm-core/src/telemetry.rs
  - crates/swarm-whisker/src/detector.rs
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-runtime/src/detector_factory.rs
  - crates/swarm-runtime/src/detection/pipeline.rs
autonomous: true
requirements:
  - FILELESS-01
  - FILELESS-03
  - FILELESS-05
must_haves:
  truths:
    - "Phase 159 needs a shared telemetry contract for memory access; detector-only evidence blobs would make replay, config, and downstream correlation brittle."
    - "The runtime already has a detector-factory and profile seam, so fileless execution should become one more repo-owned detector strategy instead of a runtime special case."
    - "Behavioral baselines are explicitly Phase 160, so Phase 159 should stay focused on deterministic fileless indicators and correct threat-class mapping."
  artifacts:
    - path: "crates/swarm-core/src/telemetry.rs"
      provides: "Shared telemetry schema and serialized payload ownership."
    - path: "crates/swarm-whisker/src/detector.rs"
      provides: "Existing detector trait, finding contract, and detector-export surface."
    - path: "crates/swarm-runtime/src/detector_factory.rs"
      provides: "Runtime detector selection and profile-backed construction seam."
  key_links:
    - from: "crates/swarm-core/src/telemetry.rs"
      to: "crates/swarm-whisker/src/detector.rs"
      via: "process_memory_access payload"
      pattern: "shared memory-access telemetry feeds deterministic fileless heuristics"
    - from: "crates/swarm-whisker/src/detector.rs"
      to: "crates/swarm-runtime/src/detector_factory.rs"
      via: "fileless_execution profile and runtime detector registration"
      pattern: "repo-owned detector config can enable fileless execution without special runtime wiring"
---

<objective>
Complete Phase 159 by adding first-class memory-access telemetry, a repo-owned `FilelessExecutionDetector`, and runtime profile wiring that produces threat-class-aware pheromone findings for reflective injection, encoded PowerShell, and syscall-gadget patterns.

Purpose: widen live detection coverage into fileless execution without waiting on the later behavioral-baseline work.
Output: `ProcessMemoryAccess` telemetry in the shared schema, a production runtime detector for fileless execution, and verification showing those events reach the existing finding and pheromone lane.
</objective>

<context>
@.planning/phases/159-fileless-execution-detection/159-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/telemetry.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-whisker/src/detector.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/detector_factory.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared memory-access telemetry and profile seams</name>
  <files>
    crates/swarm-core/src/telemetry.rs
    crates/swarm-runtime/src/config.rs
    rulesets/default.yaml
  </files>
  <action>Add a typed `ProcessMemoryAccess` telemetry payload with the required source, target, allocation, protection, region-size, and optional call-stack fields, then expose a repo-owned runtime profile seam for the fileless detector.</action>
  <verify>
    <automated>cargo test -p swarm-core telemetry::tests:: -- --nocapture</automated>
  </verify>
  <done>The shared schema can serialize and deserialize fileless memory-access events, and runtime config can describe the detector without ad hoc JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement FilelessExecutionDetector heuristics</name>
  <files>
    crates/swarm-whisker/src/detector.rs
  </files>
  <action>Implement a dedicated detector that identifies reflective DLL injection, encoded PowerShell with staged deobfuscation hints, and syscall-gadget patterns while mapping findings to `ThreatClass::DefenseEvasion` or `ThreatClass::PrivilegeEscalation` as appropriate.</action>
  <verify>
    <automated>cargo test -p swarm-whisker fileless -- --nocapture</automated>
  </verify>
  <done>The detector returns deterministic structured findings with the expected threat class, severity, confidence, and evidence for representative fileless attack inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire runtime factory support and prove pheromone flow</name>
  <files>
    crates/swarm-runtime/src/detector_factory.rs
    crates/swarm-runtime/src/detection/pipeline.rs
    crates/swarm-runtime/src/replay/core.inc
  </files>
  <action>Register the new detector through the runtime factory and prove fileless execution events flow through the existing detection and pheromone-deposit lane.</action>
  <verify>
    <automated>cargo test -p swarm-runtime fileless -- --nocapture</automated>
  </verify>
  <done>The runtime can construct the fileless detector from repo-owned config and emit signed pheromone deposits from representative memory-access telemetry.</done>
</task>

</tasks>

<verification>
Keep the phase bounded to fileless execution. Do not start per-host behavioral baseline persistence or decay logic until Phase 160.
</verification>
