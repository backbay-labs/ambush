---
phase: 100-escalation-records-and-mode-aware-environment
plan: 02
type: execute
wave: 1
depends_on: [100-01]
files_modified:
  - crates/swarm-core/src/agent.rs
  - crates/swarm-runtime/src/escalation.rs
  - crates/swarm-runtime/tests/escalation_integration.rs
autonomous: true
requirements: [SUBSTRATE-01, SUBSTRATE-02]

must_haves:
  truths:
    - "Runtime escalation monitoring persists records only for true upward mode transitions"
    - "Agents can read `current_mode()` and `mode_transition_at()` from `SwarmEnvironment` without reaching into raw fields"
    - "Integration coverage proves both the recorded escalation history and the mode-aware environment contract"
  artifacts:
    - path: "crates/swarm-runtime/src/escalation.rs"
      provides: "live escalation recording into the substrate"
      contains: "EscalationRecord"
    - path: "crates/swarm-core/src/agent.rs"
      provides: "mode-aware environment helper methods"
      contains: "current_mode"
  key_links:
    - from: "crates/swarm-runtime/src/escalation.rs"
      to: "crates/swarm-pheromone/src/substrate.rs"
      via: "monotonic mode transitions recorded as durable escalation records"
      pattern: "record_escalation"
    - from: "crates/swarm-core/src/agent.rs"
      to: "crates/swarm-runtime/tests/escalation_integration.rs"
      via: "mode-aware agent/runtime access contract"
      pattern: "mode_transition_at"
---

<objective>
Wire durable escalation recording into the live runtime and expose the mode-aware environment helpers agents will use in later substrate-aware phases.

Purpose: Phase 100 is not complete until durable escalation history matches real runtime transitions and agents have an explicit contract for reading current mode plus transition timing.

Output: persisted runtime escalation records, `SwarmEnvironment` helper accessors, and verification coverage proving the runtime and substrate agree on recorded transitions.
</objective>
