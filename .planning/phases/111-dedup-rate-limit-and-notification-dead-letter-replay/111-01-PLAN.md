---
phase: 111-dedup-rate-limit-and-notification-dead-letter-replay
plan: 01
type: execute
wave: 4
depends_on: [110-02]
files_modified:
  - crates/swarm-response/src/dead_letter.rs
  - crates/swarm-response/src/notification.rs
  - crates/swarm-runtime/src/service.rs
autonomous: true
requirements: [SIEM-05, SIEM-06]

must_haves:
  truths:
    - "Notification dedup merges findings sharing `strategy_id` and `ThreatClass` within `dedup_window_ms`"
    - "Each channel enforces an in-memory rate limit and writes suppressed alerts to a channel-specific dead-letter queue"
    - "Suppressed alert payloads remain replayable from the stored dead-letter record"
  artifacts:
    - path: "crates/swarm-response/src/notification.rs"
      provides: "dedup, rate limit, and dead-letter notification routing"
      contains: "dedup_window_ms"
    - path: "crates/swarm-response/src/dead_letter.rs"
      provides: "dead-letter read/list support for replay"
      contains: "read_entries"
---

<objective>
Add production-safe notification suppression behavior.

Purpose: Alert routing must control burst noise without dropping operator visibility permanently.

Output: deduplication, per-channel rate limiting, channel dead-letter persistence, and replay-ready stored notification payloads.
</objective>
