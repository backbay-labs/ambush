---
phase: 277-splunk-hec-adapter
plan: 01
type: execute
depends_on:
  - 276-crowdstrike-rtr-adapter
files_modified:
  - crates/swarm-core/src/config/response.rs
  - crates/swarm-response/src/lib.rs
  - crates/swarm-response/src/siem.rs
  - crates/swarm-response/src/splunk_hec.rs
  - crates/swarm-runtime/src/detection/metrics.rs
  - crates/swarm-runtime/src/service/runtime_service.rs
  - crates/swarm-runtime/src/ingest/health.rs
autonomous: true
requirements:
  - SIEMINT-01
  - SIEMINT-02
  - SIEMINT-03
---

<objective>
Turn the existing Splunk forwarder path into a repo-owned Splunk HEC adapter
with explicit CIM-aligned event mapping, bounded batching, metrics, and
mock-backed delivery proof without widening the outbound surface beyond the
current runtime contracts.
</objective>

<tasks>

<task>
  <name>Add explicit Splunk batching and delivery metadata to the shipped config path</name>
  <done>`splunk_hec` config carries bounded batch sizing and still resolves secrets through the current runtime secret provider.</done>
</task>

<task>
  <name>Implement a dedicated Splunk HEC adapter and batch-aware forwarder path</name>
  <done>`SplunkHecAdapter` emits CIM-aligned payloads, chunks batches on explicit limits, and returns delivery receipts that expose batch count, byte count, and transport metadata on the existing resilient executor seam.</done>
</task>

<task>
  <name>Expose delivery observability and prove it against a mock HEC endpoint</name>
  <done>Runtime Prometheus output and mock-backed tests validate auth, batching, field mapping, payload byte metrics, and degraded delivery behavior through the existing audit and dead-letter surfaces.</done>
</task>

</tasks>
