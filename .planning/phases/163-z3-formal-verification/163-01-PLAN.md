---
phase: 163-z3-formal-verification
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 162
files_modified:
  - crates/swarm-evolution/Cargo.toml
  - crates/swarm-evolution/src/evolution.rs
  - crates/swarm-runtime/src/evolution_status.rs
autonomous: true
requirements:
  - Z3-01
  - Z3-02
must_haves:
  truths:
    - "The `z3` path must be optional; a hard runtime dependency would violate the phase boundary."
    - "Solver output is only useful if counterexamples, timeout state, and proof signatures are durable artifacts, not transient logs."
    - "Replay and solver proof must compose: the solver lane augments invariant coverage but does not replace existing replay validation."
  artifacts:
    - path: "crates/swarm-evolution/Cargo.toml"
      provides: "Feature-gated solver dependency surface."
    - path: "crates/swarm-evolution/src/evolution.rs"
      provides: "Invariant compilation, solver execution, and proof artifact persistence."
    - path: "crates/swarm-runtime/src/evolution_status.rs"
      provides: "Shared surfaced status for optional solver proofs."
  key_links:
    - from: "crates/swarm-evolution/src/evolution.rs"
      to: "crates/swarm-runtime/src/evolution_status.rs"
      via: "durable signed proof artifacts"
      pattern: "solver-backed invariant proof becomes visible through the same status lane as replay and adversarial history"
---

<objective>
Complete Phase 163 by adding an optional `z3` verification tier that compiles strategy invariants into solver constraints, persists signed proof or counterexample artifacts, and surfaces the result through the shared evolution status lane.

Purpose: give evolved strategies one more formal proof tier for properties replay cannot exhaustively cover.
Output: feature-gated solver integration, durable proof artifacts with timeout and counterexample handling, and shared surfaced status for those artifacts.
</objective>

<context>
@.planning/phases/163-z3-formal-verification/163-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/evolution.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/evolution_status.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a feature-gated solver compilation seam</name>
  <files>
    crates/swarm-evolution/Cargo.toml
    crates/swarm-evolution/src/evolution.rs
  </files>
  <action>Add a `z3` feature flag and a typed invariant-to-constraint compilation seam so strategy invariants can be checked without leaking solver details through the rest of the runtime.</action>
  <verify>
    <automated>cargo test -p swarm-evolution z3_ -- --nocapture</automated>
  </verify>
  <done>The codebase can compile solver-backed invariant checks behind an explicit feature flag without affecting default builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist signed proof and counterexample artifacts</name>
  <files>
    crates/swarm-evolution/src/evolution.rs
  </files>
  <action>Persist durable signed proof artifacts for proved, failed, and timeout outcomes, including machine-readable counterexamples when solver checks fail.</action>
  <verify>
    <automated>cargo test -p swarm-evolution z3_proof -- --nocapture</automated>
  </verify>
  <done>Solver-backed proof outcomes survive restart and are consumable through the same artifact lane as the existing evolution proof surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface solver proof state through shared status</name>
  <files>
    crates/swarm-runtime/src/evolution_status.rs
  </files>
  <action>Expose the latest solver proof outcome, timeout state, and counterexample presence through the shared evolution status lane so operators can see whether a candidate passed formal proof.</action>
  <verify>
    <automated>cargo test -p swarm-runtime evolution_status::tests:: -- --nocapture</automated>
  </verify>
  <done>The optional solver lane is visible without inventing a separate status surface.</done>
</task>

</tasks>

<verification>
Keep the phase scoped to optional solver proof integration. Broader operator UX for solver browsing stays out of scope.
</verification>
