---
phase: 263-baseline-poisoning-resistance-and-staleness-policy
plan: 01
type: execute
depends_on:
  - 262-recruitment-kill-chain-benchmark
files_modified:
  - crates/swarm-whisker/src/behavioral_anomaly.rs
  - crates/swarm-runtime/src/detection/pipeline.rs
autonomous: true
requirements:
  - BASERES-03
---

<objective>
Reduce trust in aged behavioral baselines by applying a graduated confidence
penalty after restart instead of silently trusting stale learned state.
</objective>

<tasks>

<task>
  <name>Add one configurable staleness policy to the behavioral detector</name>
  <done>`BehavioralAnomalyProfile` now carries a bounded baseline-staleness policy with threshold, per-window confidence reduction, and a minimum confidence floor.</done>
</task>

<task>
  <name>Compose staleness with signed persistence</name>
  <done>The runtime pipeline now preserves the signed snapshot capture timestamp across persistence and uses it to reduce confidence when a reopened baseline is stale.</done>
</task>

<task>
  <name>Prove the stale path on the restart boundary</name>
  <done>Whisker and runtime tests now prove the stale baseline path lowers confidence, emits `baseline_staleness` evidence, and still hydrates correctly from the signed local-journal snapshot.</done>
</task>

</tasks>
