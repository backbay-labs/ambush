---
phase: 278-e2e-deployment-proof-compose-stack
plan: 01
type: execute
depends_on:
  - 277-splunk-hec-adapter
files_modified:
  - docker-compose.yml
  - deploy/integration-proof/runtime.integration.yaml
  - deploy/integration-proof/telemetry/attack-process.json
  - deploy/integration-proof/mocks/crowdstrike_rtr_mock.py
  - deploy/integration-proof/mocks/splunk_hec_mock.py
  - tools/run-integration-proof.sh
autonomous: true
requirements:
  - E2EPROOF-01
  - E2EPROOF-02
---

<objective>
Ship one reproducible Docker Compose proof stack that boots the runtime with a
bridge-backed telemetry source plus mocked CrowdStrike RTR and Splunk HEC
dependencies, then proves the full detect -> respond -> deliver loop through a
repo-owned verification script.
</objective>

<tasks>

<task>
  <name>Add a narrow integration-proof compose overlay and runtime config</name>
  <done>The repo-owned stack mounts one bridge-backed telemetry fixture, one live-response runtime config, one CrowdStrike RTR mock, and one Splunk HEC mock without altering the default operator bootstrap path.</done>
</task>

<task>
  <name>Drive one deterministic attack scenario through the running stack</name>
  <done>The proof script starts the stack, waits for readiness, verifies the telemetry bridge produced a finding, proves one RTR containment action fired, and proves the mapped finding arrived at the mock HEC sink.</done>
</task>

</tasks>
