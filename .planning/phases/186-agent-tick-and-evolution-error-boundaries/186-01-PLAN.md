---
phase: 186-agent-tick-and-evolution-error-boundaries
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 185
files_modified:
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/src/ingest/mod.rs
  - crates/swarm-runtime/src/sphinx_agent.rs
  - crates/swarm-runtime/src/stalker_agent.rs
  - crates/swarm-evolution/src/evolution.rs
  - crates/swarm-evolution/src/drafting.rs
autonomous: true
requirements:
  - PANIC-03
must_haves:
  truths:
    - "Agent tick and evolution-specific runtime seams must propagate typed failures instead of `String` reasons once work leaves the request boundary."
    - "Cross-crate runtime-to-evolution calls must preserve enough failure class to fail closed without crashing surrounding runtime loops."
    - "The runtime should keep its current outward behavior while tightening the internal error contract for agent and evolution work."
  artifacts:
    - path: "crates/swarm-runtime/src/lib.rs"
      provides: "Typed runtime-owned orchestration and agent-tick boundary errors."
    - path: "crates/swarm-runtime/src/ingest/mod.rs"
      provides: "Typed strategy-proposal and evolution-facing ingest boundary propagation."
    - path: "crates/swarm-evolution/src/evolution.rs"
      provides: "Evolution-facing error contracts that preserve failure class across crate seams."
---

<objective>
Complete Phase 186 by converting the remaining agent tick and evolution-specific
runtime seams from string-only or ad hoc propagation into typed error
boundaries.

Purpose: extend the Phase 185 request-boundary cleanup into the runtime’s
cross-crate orchestration paths.
Output: typed agent/evolution runtime boundary errors, fail-closed propagation
through the strategy-proposal lane and agent ticks, and focused regression
coverage proving those seams stay non-panicking.
</objective>

<context>
@.planning/phases/186-agent-tick-and-evolution-error-boundaries/186-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/mod.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/sphinx_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/stalker_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/evolution.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add typed agent-tick runtime boundaries</name>
  <files>
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/src/sphinx_agent.rs
    crates/swarm-runtime/src/stalker_agent.rs
  </files>
  <action>Replace agent-tick string-only propagation or hidden failure flattening with typed runtime-local enums that preserve the failing subsystem.</action>
  <done>Agent orchestration surfaces can distinguish runtime, policy, memory, or investigation failures without panicking or collapsing everything to one string reason.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert evolution-facing runtime seams to typed propagation</name>
  <files>
    crates/swarm-runtime/src/ingest/mod.rs
    crates/swarm-evolution/src/evolution.rs
    crates/swarm-evolution/src/drafting.rs
  </files>
  <action>Introduce typed errors for the strategy-proposal router and adjacent evolution harness calls so cross-crate runtime work preserves failure class.</action>
  <done>Evolution-specific runtime paths no longer depend on blanket `error.to_string()` adapters before the final trait or surface boundary.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Prove agent and evolution failures stay fail-closed and non-panicking</name>
  <files>
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/src/ingest/mod.rs
    crates/swarm-evolution/src/evolution.rs
  </files>
  <action>Add focused regression coverage for representative agent-tick and evolution failure cases.</action>
  <done>Verification shows typed agent/evolution errors propagate without panic and without losing the bounded failure context added in this phase.</done>
</task>

</tasks>

<verification>
Verify that representative agent-tick failures, evolution harness failures, and
strategy-proposal admission failures propagate through typed boundaries without
reintroducing panic-driven or string-only runtime contracts.
</verification>
