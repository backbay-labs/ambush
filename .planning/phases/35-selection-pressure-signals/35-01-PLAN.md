# Plan 35-01

1. Add durable selection-pressure report types and a file-backed store with stable IDs.
2. Build a harness that derives pressure reports from replay, verification, or strategy-memory evidence.
3. Add `swarmctl` commands to create and reload pressure reports.
4. Cover pressure persistence and stable-ID reload behavior with unit tests.
