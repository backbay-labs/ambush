---
phase: 174-assurance-gated-queue-canary-and-promotion
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 173
files_modified:
  - crates/swarm-evolution/src/evolution.rs
  - crates/swarm-evolution/src/canary.rs
  - crates/swarm-evolution/src/promotion.rs
  - crates/swarm-runtime/src/evolution_status.rs
autonomous: true
requirements:
  - ASSURE-03
must_haves:
  truths:
    - "Queue, canary, and promotion must fail closed when assurance policy is unsatisfied."
    - "Blocked rollout artifacts must preserve the exact assurance gate that denied progression."
    - "Rollout enforcement must reuse the shipped durable artifact flow instead of creating ad hoc gate state."
  artifacts:
    - path: "crates/swarm-evolution/src/evolution.rs"
      provides: "Queue and handoff fail-closed assurance enforcement."
    - path: "crates/swarm-evolution/src/canary.rs"
      provides: "Canary admission enforcement with assurance lineage."
    - path: "crates/swarm-evolution/src/promotion.rs"
      provides: "Promotion fail-closed enforcement with assurance lineage."
---

<objective>
Complete Phase 174 by applying assurance policy to the queue, canary, and promotion state machines so candidates fail closed when assurance requirements are not satisfied.

Purpose: make assurance a real rollout gate rather than advisory metadata.
Output: fail-closed enforcement across the rollout ladder plus durable blocked-state lineage on queue, handoff, canary, and promotion artifacts.
</objective>

<context>
@.planning/phases/174-assurance-gated-queue-canary-and-promotion/174-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/evolution.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/canary.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/promotion.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce assurance in queue review and handoff</name>
  <files>
    crates/swarm-evolution/src/evolution.rs
  </files>
  <action>Require satisfied assurance policy before proposals can be accepted for canary or handoff packets can be created or launched.</action>
  <done>Queue and handoff transitions fail closed with explicit assurance blocking reasons when requirements are unsatisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce assurance at canary and promotion entry</name>
  <files>
    crates/swarm-evolution/src/canary.rs
    crates/swarm-evolution/src/promotion.rs
  </files>
  <action>Apply the same assurance policy when entering canary and promotion so rollout cannot bypass the queue gate by stale or inconsistent artifacts.</action>
  <done>Canary and promotion runs fail closed on unsatisfied or missing assurance lineage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface blocked assurance transitions</name>
  <files>
    crates/swarm-runtime/src/evolution_status.rs
  </files>
  <action>Expose assurance gate failures in the shared evolution status summary so blocked rollout progress is operator-visible.</action>
  <done>The latest evolution status can explain whether assurance policy is currently blocking rollout progression.</done>
</task>

</tasks>

<verification>
Keep this phase rollout-enforcement focused. Waiver issuance and exported assurance review context belong to Phase 175.
</verification>
