---
phase: 161-evasion-test-corpus-and-coverage-metrics
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 160
files_modified:
  - crates/swarm-runtime/src/replay/core.inc
  - crates/swarm-runtime/src/evasion_coverage.rs
  - crates/swarm-runtime/src/detection/metrics.rs
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/lib.rs
  - scenario-suites/evasion-breadth-v1.yaml
  - rulesets/evasion/attack-technique-catalog.yaml
  - scenarios/evasion-execution-office-chains.yaml
  - scenarios/evasion-defense-evasion-fileless.yaml
  - scenarios/evasion-command-and-control-network.yaml
  - scenarios/evasion-data-exfiltration-dns.yaml
  - scenarios/evasion-lateral-movement-remote-admin.yaml
  - scenarios/evasion-credential-access-harvest.yaml
  - scenarios/evasion-persistence-autostart.yaml
autonomous: true
requirements:
  - EVASION-01
  - EVASION-02
  - EVASION-04
must_haves:
  truths:
    - "The evasion bench must be repo-owned and deterministic or later mutation pressure will drift with operator-local config and become non-reproducible."
    - "Coverage numbers have to come from the runtime detector factory, otherwise `/metrics` would describe a detector graph different from the one the runtime actually executes."
    - "The catalog needs to preserve intentional gaps with rationale; a raw catch-rate table is not enough to tell operators whether uncovered ATT&CK techniques are debt or deliberate scope."
  artifacts:
    - path: "crates/swarm-runtime/src/replay/core.inc"
      provides: "Typed scenario and suite loading for repo-owned evasion manifests."
    - path: "crates/swarm-runtime/src/evasion_coverage.rs"
      provides: "One shared coverage snapshot and ATT&CK-gap reporting surface."
    - path: "crates/swarm-runtime/src/ingest.rs"
      provides: "Authenticated API exposure and metrics refresh hook."
  key_links:
    - from: "scenario-suites/evasion-breadth-v1.yaml"
      to: "crates/swarm-runtime/src/evasion_coverage.rs"
      via: "repo-owned replay suite"
      pattern: "curated adversarial scenarios become deterministic detector coverage input"
    - from: "crates/swarm-runtime/src/evasion_coverage.rs"
      to: "crates/swarm-runtime/src/detection/metrics.rs"
      via: "EvasionCoverageSnapshot"
      pattern: "the same detector snapshot drives both Prometheus and API reporting"
---

<objective>
Complete Phase 161 by shipping a repo-owned evasion corpus, a documented technique-gap catalog, and a runtime coverage module that exposes per-detector catch rates through `/api/v1/evasion/coverage`, `/v2/api/evasion/coverage`, and `/metrics`.

Purpose: give the runtime one deterministic evasion benchmark that later mutation and formal-safety phases can consume without inventing new side-channel artifacts.
Output: repo-owned adversarial scenarios plus catalog, runtime coverage evaluation, authenticated coverage API, and Prometheus evasion gauges.
</objective>

<context>
@.planning/phases/161-evasion-test-corpus-and-coverage-metrics/161-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/replay/core.inc
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/detection/metrics.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the repo-owned evasion suite and catalog</name>
  <files>
    scenario-suites/evasion-breadth-v1.yaml
    rulesets/evasion/attack-technique-catalog.yaml
    scenarios/evasion-execution-office-chains.yaml
    scenarios/evasion-defense-evasion-fileless.yaml
    scenarios/evasion-command-and-control-network.yaml
    scenarios/evasion-data-exfiltration-dns.yaml
    scenarios/evasion-lateral-movement-remote-admin.yaml
    scenarios/evasion-credential-access-harvest.yaml
    scenarios/evasion-persistence-autostart.yaml
  </files>
  <action>Add a deterministic evasion replay suite with enough payload volume per threat class and document intentionally uncovered ATT&CK techniques per detector with rationale.</action>
  <verify>
    <automated>cargo test -p swarm-runtime evasion_coverage::tests::repo_evasion_snapshot_provides_ten_payloads_per_threat_class -- --exact</automated>
  </verify>
  <done>The repo contains a reusable evasion corpus and explicit intentional-gap catalog instead of only ad hoc unit-test fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Evaluate detector coverage and publish metrics</name>
  <files>
    crates/swarm-runtime/src/evasion_coverage.rs
    crates/swarm-runtime/src/detection/metrics.rs
    crates/swarm-runtime/src/lib.rs
  </files>
  <action>Implement a runtime-owned coverage evaluator that loads the evasion suite, runs every supported detector, and publishes per-detector plus per-threat-class catch-rate gauges.</action>
  <verify>
    <automated>cargo test -p swarm-runtime detection::metrics::tests::encode_metrics_renders_evasion_coverage_gauges -- --exact</automated>
  </verify>
  <done>The runtime can produce one typed evasion coverage snapshot and encode that result into Prometheus labels without a second reporting path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose authenticated API coverage and prove mounted-config behavior</name>
  <files>
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/evasion_coverage.rs
  </files>
  <action>Expose the coverage snapshot on the authenticated platform-read API and harden repo-root discovery so mounted configs outside the repo tree still resolve the repo-owned evasion suite.</action>
  <verify>
    <automated>cargo test -p swarm-runtime platform_evasion_coverage_endpoint -- --nocapture</automated>
    <automated>cargo test -p swarm-runtime ingest::tests::metrics_include_evasion_coverage_gauges -- --exact</automated>
  </verify>
  <done>The runtime can serve and metricize evasion coverage even when the config file is mounted outside the workspace tree.</done>
</task>

</tasks>

<verification>
Keep the phase bounded to the benchmark and reporting surface. Using measured gaps to drive mutation remains Phase 162.
</verification>
