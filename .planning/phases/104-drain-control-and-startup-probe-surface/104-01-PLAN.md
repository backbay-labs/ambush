---
phase: 104-drain-control-and-startup-probe-surface
plan: 01
type: execute
wave: 1
depends_on: [103-01, 103-02]
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
autonomous: true
requirements: [K8S-01]

must_haves:
  truths:
    - "Serve mode can enter drain state and reject new ingest requests before shutdown"
    - "The runtime waits for accepted in-flight ingest work to finish or for `drain_timeout_ms` to expire before terminating"
    - "The shutdown path still uses the existing Axum/Tokio graceful shutdown flow"
  artifacts:
    - path: "crates/swarm-runtime/src/ingest.rs"
      provides: "drain-state tracking, in-flight accounting, and PreStop coordination"
      contains: "prestop_handler"
    - path: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      provides: "serve-mode drain-aware shutdown sequencing"
      contains: "drain_timeout_ms"
  key_links:
    - from: "crates/swarm-core/src/config.rs"
      to: "crates/swarm-runtime/src/ingest.rs"
      via: "bounded drain timeout from runtime settings"
      pattern: "drain_timeout_ms"
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      via: "PreStop handler triggers clean shutdown"
      pattern: "begin_drain"
---

<objective>
Add drain-aware serve-mode lifecycle handling so Kubernetes rollouts can stop new ingest work, wait for accepted requests to finish, and then terminate cleanly.

Purpose: Rolling updates must not drop accepted detection or response work.

Output: request-scoped in-flight tracking, PreStop drain control, bounded shutdown waiting, and serve-mode lifecycle tests.
</objective>
