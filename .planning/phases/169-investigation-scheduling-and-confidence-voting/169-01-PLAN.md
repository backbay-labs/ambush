---
phase: 169-investigation-scheduling-and-confidence-voting
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 168
files_modified:
  - crates/swarm-runtime/src/investigation.rs
  - crates/swarm-runtime/src/stalker_agent.rs
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-spine/src/investigation.rs
  - crates/swarm-core/src/config.rs
autonomous: true
requirements:
  - SCHED-01
  - SCHED-02
must_haves:
  truths:
    - "Async work must be scheduled by value, freshness, and bounded queue budget instead of FIFO-only behavior."
    - "Ambiguous investigations must preserve competing interpretations and final confidence lineage."
    - "Starvation, backlog pressure, and queue-budget behavior must be explicit and testable."
  artifacts:
    - path: "crates/swarm-runtime/src/investigation.rs"
      provides: "Priority scheduling and vote-handling logic."
    - path: "crates/swarm-runtime/src/stalker_agent.rs"
      provides: "Lead submission with priority and ambiguity context."
    - path: "crates/swarm-runtime/src/service.rs"
      provides: "Runtime exposure of queue state and recent investigation outcomes."
---

<objective>
Complete Phase 169 by turning the async investigation queue into a bounded priority scheduler with speculate-plus-vote handling for ambiguous hunts.

Purpose: make the async lane selective and explainable before it becomes a first-class operator surface.
Output: prioritized investigation scheduling, durable confidence-vote lineage, and queue-budget verification.
</objective>

<context>
@.planning/phases/169-investigation-scheduling-and-confidence-voting/169-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/investigation.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/stalker_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/service.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bounded priority scheduling</name>
  <files>
    crates/swarm-runtime/src/investigation.rs
    crates/swarm-runtime/src/stalker_agent.rs
    crates/swarm-core/src/config.rs
  </files>
  <action>Replace FIFO-only queue behavior with priority scoring that accounts for criticality, freshness, learned value, and explicit queue budgets or starvation rules.</action>
  <done>The async queue selects work deterministically and exposes its budget or starvation behavior in tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist confidence-vote lineage for ambiguous leads</name>
  <files>
    crates/swarm-runtime/src/investigation.rs
    crates/swarm-spine/src/investigation.rs
  </files>
  <action>Allow ambiguous investigations to carry multiple candidate interpretations, vote or scoring lineage, and a final confidence decision through durable investigation artifacts.</action>
  <done>Ambiguous hunts can be replayed or reviewed with their competing interpretations preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface and verify scheduling outcomes</name>
  <files>
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/investigation.rs
  </files>
  <action>Add focused runtime tests and status shaping for queue depth, budget pressure, ambiguous-vote outcomes, and starvation prevention.</action>
  <done>The scheduler's bounded behavior is observable and regression-tested.</done>
</task>

</tasks>

<verification>
Keep the phase investigation-focused. New operator UI/API surfaces and milestone-wide end-to-end proof belong to Phase 171.
</verification>
