---
phase: 112-telemetry-persistence-payloads-and-detector-contracts
plan: 01
type: execute
wave: 1
depends_on: [111-01, 111-02]
files_modified:
  - crates/swarm-core/src/telemetry.rs
  - crates/swarm-core/src/pheromone.rs
  - crates/swarm-core/src/config.rs
  - crates/swarm-whisker/src/detector.rs
  - crates/swarm-whisker/src/lib.rs
autonomous: true
requirements: [PERSIST-03, PERSIST-04]

must_haves:
  truths:
    - "`TelemetryPayload` includes `RegistryPersistence` and `FilePersistence` with stable serde support"
    - "`ThreatClass` includes `SupplyChain` and shared label helpers can surface it"
    - "`DetectorProfilesConfig` can represent `persistence` and `supply_chain` profile overrides"
  artifacts:
    - path: "crates/swarm-core/src/telemetry.rs"
      provides: "shared normalized payload variants for persistence telemetry"
      contains: "RegistryPersistenceEvent"
    - path: "crates/swarm-core/src/pheromone.rs"
      provides: "shared threat taxonomy including supply-chain findings"
      contains: "SupplyChain"
    - path: "crates/swarm-core/src/config.rs"
      provides: "config contract for the new detector profile families"
      contains: "DetectorProfilesConfig"
  key_links:
    - from: "crates/swarm-core/src/telemetry.rs"
      to: "crates/swarm-whisker/src/detector.rs"
      via: "shared payload variants re-export into detector implementations"
      pattern: "TelemetryPayload"
    - from: "crates/swarm-core/src/config.rs"
      to: "crates/swarm-runtime/src/config.rs"
      via: "detector profile overrides resolve from repo-owned runtime config"
      pattern: "profiles"
---

<objective>
Add the shared payload, threat-class, and profile contracts needed by the persistence and supply-chain detectors.

Purpose: The later detector phases need stable event, taxonomy, and configuration contracts before any heuristics can be wired safely through the rest of the runtime.

Output: New persistence-oriented telemetry payload structs, `ThreatClass::SupplyChain`, and detector profile config entries for `persistence` and `supply_chain`.
</objective>
