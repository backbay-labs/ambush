# Phase 292 scoping (controller, 2026-09-08)

- **DCORE-03 is ENFORCE-only:** `cargo tree -p swarm-policy` is ALREADY clean of all 7 forbidden deps (axum/hyper/tokio-rustls/reqwest/opentelemetry*/clap/x509-parser). No cleanup — just add the boundary gate.
- **DCORE-01 (within swarm-policy):** rate-limit window state is `Arc<Mutex<HashMap<String, VecDeque<i64>>>>` at `static_gate.rs:18` (scope_windows) + `configurable_gate.rs:16` (agent_windows). Extract the approval/rate-limit logic into a new `crates/swarm-policy/src/formal_core.rs` as PURE TOTAL functions taking window state + `now_ms: i64` explicitly, returning updated state; the gates keep the Mutex at the edge but call the pure core.
- **DCORE-02 (cross-crate move):** the governance predicates — `GovernancePolicy::can_act`, `ContingencyLease::verify/can_redeem/redeem`, `governance_quorum_threshold`, and the internal `now_ms()` at `tom_agent.rs:508` — live in `crates/swarm-agents/src/tom_agent.rs`. swarm-policy deps = swarm-core only (swarm-agents depends on swarm-policy, so moving DOWN into swarm-policy/formal_core is cycle-free). Port the pure predicate logic into formal_core (no Mutex/fs/clock; now_ms an explicit param); the shared types (ContingencyLease, GovernancePolicy) go to swarm-core or swarm-policy as needed; swarm-agents then calls swarm-policy::formal_core. Preserve behaviour.
- **DCORE-04 path slip (repo convention):** the requirement names `docs/adr/ADR-0002-...` + `scripts/check-decision-core-boundary.sh`; repo convention is `docs/decisions/NNNN-...` (ADRs are 0001-0011; next = 0012) and `tools/check-*.sh` (check-gates-wired scans tools/). RULE: ADR at docs/decisions/0012-decision-core-boundary.md, gate at tools/check-decision-core-boundary.sh, wired into ci.yml. Note the slip (same as 283/285/291).
- **DCORE-05:** every static_gate/configurable_gate/tom_agent governance test passes UNCHANGED. The 285 fail-closed invariants live in swarm-policy (static_gate) — the extraction MUST preserve the `// INVARIANT:` markers + the negative tests + check-mapping. High stakes (TCB-adjacent).
- **Structure:** sequential (T1 formal_core + rate-limit extraction; T2 governance predicate cross-crate port; T3 boundary ADR+gate; T4 close). Not parallel (interdependent extraction). Careful review like phase 286 (TCB change).
