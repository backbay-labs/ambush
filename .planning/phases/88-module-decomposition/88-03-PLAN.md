---
phase: 88-module-decomposition
plan: 03
type: execute
wave: 2
depends_on: [88-02]
files_modified:
  - crates/swarm-runtime/src/workbench/mod.rs
  - crates/swarm-runtime/src/workbench/types.rs
  - crates/swarm-runtime/src/workbench/stores.rs
  - crates/swarm-runtime/src/workbench/harness.rs
  - crates/swarm-runtime/src/workbench/render.rs
  - crates/swarm-runtime/src/workbench/helpers.rs
  - crates/swarm-runtime/src/review_workbench.rs
  - crates/swarm-runtime/src/replay/mod.rs
  - crates/swarm-runtime/src/replay/types.rs
  - crates/swarm-runtime/src/replay/stores.rs
  - crates/swarm-runtime/src/replay/harness.rs
  - crates/swarm-runtime/src/replay/render.rs
  - crates/swarm-runtime/src/replay/validation.rs
  - crates/swarm-runtime/src/replay/helpers.rs
  - crates/swarm-runtime/src/lib.rs
autonomous: true
requirements: [REFAC-03, REFAC-04]

must_haves:
  truths:
    - "review_workbench.rs is replaced by focused workbench/ submodules with clear public API boundaries"
    - "replay.rs is replaced by focused replay/ submodules each under 1.5K lines"
    - "All existing tests (including replay tests) pass with zero regressions"
    - "cargo build && cargo test && cargo clippy passes"
    - "External consumers (http/ module, cli/ module) continue to compile via re-export facades"
  artifacts:
    - path: "crates/swarm-runtime/src/workbench/mod.rs"
      provides: "Workbench module root with pub use re-exports"
      contains: "pub mod types"
    - path: "crates/swarm-runtime/src/workbench/types.rs"
      provides: "All domain types, enums, structs, error types"
      min_lines: 500
    - path: "crates/swarm-runtime/src/workbench/stores.rs"
      provides: "All 7 file store implementations"
      min_lines: 500
    - path: "crates/swarm-runtime/src/workbench/harness.rs"
      provides: "DefaultReviewWorkbenchHarness struct and impl"
      min_lines: 500
    - path: "crates/swarm-runtime/src/workbench/render.rs"
      provides: "All render_review_session/capsule/delegation/handoff/readiness functions"
      min_lines: 100
    - path: "crates/swarm-runtime/src/workbench/helpers.rs"
      provides: "Internal helpers (normalization, lane classification, crypto, signature)"
      min_lines: 200
    - path: "crates/swarm-runtime/src/replay/mod.rs"
      provides: "Replay module root with pub use re-exports"
      contains: "pub mod types"
    - path: "crates/swarm-runtime/src/replay/types.rs"
      provides: "All domain types (scenarios, manifests, expectations, evaluation, experiment, verification, shadow, promotion review)"
      min_lines: 800
    - path: "crates/swarm-runtime/src/replay/stores.rs"
      provides: "Memory and file store implementations for runs, experiments, verifications, shadows, promotion reviews"
      min_lines: 500
    - path: "crates/swarm-runtime/src/replay/harness.rs"
      provides: "DefaultReplayHarness struct and impl"
      min_lines: 500
    - path: "crates/swarm-runtime/src/replay/render.rs"
      provides: "All render_replay_run/evaluation/suite/experiment/verification/shadow/promotion functions"
      min_lines: 200
    - path: "crates/swarm-runtime/src/replay/validation.rs"
      provides: "Manifest loaders, validators, path resolution"
      min_lines: 300
    - path: "crates/swarm-runtime/src/replay/helpers.rs"
      provides: "Comparison helpers, gate evaluation, metric computation, utility functions"
      min_lines: 200
  key_links:
    - from: "crates/swarm-runtime/src/review_workbench.rs"
      to: "crates/swarm-runtime/src/workbench/mod.rs"
      via: "review_workbench.rs becomes re-export facade"
      pattern: "pub use workbench::"
    - from: "crates/swarm-runtime/src/replay/harness.rs"
      to: "crates/swarm-runtime/src/replay/types.rs"
      via: "harness uses domain types"
      pattern: "use super::types::"
    - from: "crates/swarm-runtime/src/replay/harness.rs"
      to: "crates/swarm-runtime/src/replay/stores.rs"
      via: "harness constructs and uses stores"
      pattern: "use super::stores::"
---

<objective>
Split review_workbench.rs (3,805 lines) and replay.rs (5,345 lines) into focused submodule directories, keeping each submodule under 1.5K lines.

Purpose: These are the remaining two monolithic files. review_workbench.rs mixes 990 lines of types, 1,000 lines of store impls, 940 lines of harness logic, and 700 lines of helpers in one file with zero tests. replay.rs mixes types, stores, harness, rendering, validation, and helpers with tests scattered throughout. Splitting by concern makes each area independently navigable.

Output: `workbench/` directory (types, stores, harness, render, helpers) and `replay/` directory (types, stores, harness, render, validation, helpers). Old files become thin re-export facades.
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-module-decomposition/88-CONTEXT.md

@crates/swarm-runtime/src/review_workbench.rs
@crates/swarm-runtime/src/replay.rs
@crates/swarm-runtime/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split review_workbench.rs into workbench/ submodules</name>
  <files>
    crates/swarm-runtime/src/workbench/mod.rs,
    crates/swarm-runtime/src/workbench/types.rs,
    crates/swarm-runtime/src/workbench/stores.rs,
    crates/swarm-runtime/src/workbench/harness.rs,
    crates/swarm-runtime/src/workbench/render.rs,
    crates/swarm-runtime/src/workbench/helpers.rs,
    crates/swarm-runtime/src/review_workbench.rs,
    crates/swarm-runtime/src/lib.rs
  </files>
  <action>
    Create `crates/swarm-runtime/src/workbench/` directory and split review_workbench.rs mechanically:

    **types.rs** -- Move (lines 1-990 approximately):
    - All imports needed by type definitions
    - `ReviewArtifactRefKind` enum and impls (Display, FromStr)
    - `ReviewLane` enum and impls
    - `ReviewLaneSummary`, `ReviewLaneGap` structs
    - `ReviewArtifactRef`, `ReviewSessionCreateRequest`, `ReviewSessionReport`, `ReviewSessionRecord` (and impl)
    - `ReviewSessionLookup`, `ReviewSessionList`
    - `ReviewSessionExportBundle`, `ReviewSessionExportVerification`, `ReviewSessionExportPromotionPacket`, `ReviewSessionExport`, `ReviewSessionExportRecord` (and impl), `ReviewSessionExportLookup`, `ReviewSessionExportList`
    - `ReviewPromotionReadinessRecommendation` enum and impl
    - `ReviewSessionPromotionReadiness`, `ReviewSessionPromotionReadinessRecord` (and impl), lookup, list
    - `ReviewCapsuleSourceKind` enum and impls
    - `ReviewCapsule`, `ReviewCapsuleRecord` (and impl), lookup, list
    - `ReviewCapsuleImportTrustStatus` enum and impl
    - `ReviewCapsuleImport`, `ReviewCapsuleImportRecord` (and impl), lookup, list
    - `ReviewDelegationSourceKind` enum and impls
    - `ReviewDelegationPacket`, `ReviewDelegationPacketRecord` (and impl), lookup, list
    - `ReviewSessionMaintenanceActionResult`, `ReviewSessionMaintenanceHandoff`, `ReviewSessionMaintenanceHandoffRecord` (and impl), lookup, list
    - `ReviewSessionReverifyRequest`, `ReviewCapsuleImportRequest`, `ReviewDelegationCreateRequest`, `ReviewSessionResolved`
    - All store error enums: `ReviewSessionStoreError`, `ReviewSessionExportStoreError`, `ReviewCapsuleStoreError`, `ReviewCapsuleImportStoreError`, `ReviewDelegationPacketStoreError`, `ReviewSessionMaintenanceHandoffStoreError`, `ReviewSessionPromotionReadinessStoreError`
    - `ReviewWorkbenchError` enum

    **stores.rs** -- Move (lines ~1055-1987):
    - `FileReviewSessionStore` struct and impl
    - `FileReviewSessionExportStore` struct and impl
    - `FileReviewSessionPromotionReadinessStore` struct and impl
    - `FileReviewSessionMaintenanceHandoffStore` struct and impl
    - `FileReviewCapsuleStore` struct and impl
    - `FileReviewCapsuleImportStore` struct and impl
    - `FileReviewDelegationPacketStore` struct and impl
    - These use types from `super::types::*`

    **harness.rs** -- Move (lines ~1988-2929):
    - `DefaultReviewWorkbenchHarness` struct and its full `impl` block
    - This is the core business logic: session creation, resolution, export, capsule management, delegation, handoff, etc.
    - Uses types from `super::types::*` and stores from `super::stores::*`
    - Uses helpers from `super::helpers::*`

    **render.rs** -- Move (lines ~2930-3190):
    - `pub fn render_review_session_list`
    - `pub fn render_review_session`
    - `pub fn render_review_session_export`
    - `pub fn render_review_capsule`
    - `pub fn render_review_capsule_import`
    - `pub fn render_review_delegation_packet`
    - `pub fn render_review_session_handoff`
    - `pub fn render_review_session_promotion_readiness`

    **helpers.rs** -- Move (lines ~3191-3800):
    - `normalize_artifact_refs`, `normalize_optional_text`
    - `ensure_selected_refs_belong_to_session`
    - `derive_bundle_ids`, `push_unique_bundle`, `push_unique_verification`, `push_unique_packet`
    - `attach_bundle_dependencies`
    - `classify_subject_lane`, `build_lane_summaries`, `collect_lane_gaps`
    - `promotion_readiness_recommendation`
    - `collect_related_refs_from_export`, `collect_related_refs_from_resolved`, `push_unique_related_ref`
    - `review_capsule_signature_statement_bytes`, `review_delegation_signature_statement_bytes`
    - `signature_from_detached`, `signature_to_detached`, `resolve_trusted_key_id`, `empty_signature_placeholder`
    - `maintenance_status_label`, `now_ms`, `now_unix_nanos`, `sanitize_id`

    **mod.rs** -- Module root:
    ```rust
    pub mod types;
    pub mod stores;
    pub mod harness;
    pub mod render;
    mod helpers;

    pub use types::*;
    pub use stores::*;
    pub use harness::*;
    pub use render::*;
    ```
    Note: `helpers` is `mod` not `pub mod` since helpers are internal.

    **review_workbench.rs** -- Replace with thin re-export facade:
    ```rust
    //! Backward-compatible re-export facade.
    //! The review workbench now lives in `crate::workbench`.
    pub use crate::workbench::*;
    ```

    **lib.rs** -- Add `pub mod workbench;` to the module list. Keep `pub mod review_workbench;` for backward compat.

    IMPORTANT: Purely mechanical extraction. No logic changes. All existing `use crate::review_workbench::*` imports continue to work through the re-export facade.
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/swarm-team-six && cargo build --workspace 2>&1 | tail -3 && echo "--- workbench line counts ---" && wc -l crates/swarm-runtime/src/workbench/*.rs</automated>
  </verify>
  <done>workbench/ directory exists with types.rs, stores.rs, harness.rs, render.rs, helpers.rs. review_workbench.rs is a thin facade. `cargo build --workspace` succeeds. Each file under 1,500 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Split replay.rs into replay/ submodules and verify all constraints</name>
  <files>
    crates/swarm-runtime/src/replay/mod.rs,
    crates/swarm-runtime/src/replay/types.rs,
    crates/swarm-runtime/src/replay/stores.rs,
    crates/swarm-runtime/src/replay/harness.rs,
    crates/swarm-runtime/src/replay/render.rs,
    crates/swarm-runtime/src/replay/validation.rs,
    crates/swarm-runtime/src/replay/helpers.rs,
    crates/swarm-runtime/src/lib.rs
  </files>
  <action>
    IMPORTANT: replay.rs is currently declared as `pub mod replay;` in lib.rs, which resolves to `src/replay.rs`. When converting to a directory module, Rust resolves `pub mod replay;` to `src/replay/mod.rs`. So: create the `src/replay/` directory, move the module root to `src/replay/mod.rs`, and DELETE the old `src/replay.rs` file. Do NOT keep replay.rs as a re-export -- Rust cannot have both `src/replay.rs` and `src/replay/mod.rs`.

    Create `crates/swarm-runtime/src/replay/` directory:

    **types.rs** -- Move all domain type definitions:
    - `ReplayHarnessError` enum (lines 33-176)
    - `SupportedDetector` and its `DetectionStrategy` impl (lines 177-299)
    - `ReplayScenarioClass`, `ReplayScenarioMetadata`, `ReplaySuiteMetadata`
    - `ReplayScenarioManifest`, `ReplaySuiteManifest`
    - `ExperimentVerificationTarget`, `VerificationKnownBadCorpus`, `VerificationBenignControlCorpus`, `VerificationThreatClassTemplate`, `VerificationResourceBudgets`, `VerificationCorpusManifest`
    - `ReplayScenarioInput` and impl, `ReplayScenarioStep`, `ReplayExpectations`, `ExpectedHuntOutcome`, `ReplayInputKind`, `ActualHuntOutcome`
    - `ReplayDeterministicSummary` and impl
    - `ReplayRunBundle`, `ReplayRunRecord`, impl, `ReplayRunLookup`, `ReplayRunStoreHealth`, `ReplayRunStoreError`
    - `ReplayRunStore` trait
    - `ReplayEvaluationCheck`, `ReplayEvaluationReport`, `ReplaySuiteSourceKind`
    - `ReplayTechniqueGroupReport`, `ReplaySuiteScenarioReport`, `ReplaySuiteReport`
    - `ExperimentCorpusTarget`, `ExperimentLineage`, `ExperimentGateConfig` and Default impl
    - `DetectorCandidateManifest` and impl
    - `DetectorExperimentManifest`
    - `StrategyScenarioRegression`, `StrategyTechniqueRegression`, `StrategyExperimentMetrics`, `StrategyExperimentMetricDelta`, `StrategyExperimentComparison`, `ExperimentGateResult`, `StrategyExperimentReport`, `StrategyExperimentRecord` and impl, `StrategyExperimentLookup`
    - `VerificationCounterexample`, `VerificationInvariantResult`, `DetectorVerificationReport`, `DetectorVerificationRecord` and impl, `DetectorVerificationLookup`
    - `StrategyShadowReport`, `StrategyShadowRecord` and impl, `StrategyShadowLookup`
    - `PromotionReviewRecommendation`, `PromotionReviewBlockingReason`, `PromotionReviewPacket`, `PromotionReviewRecord` and impl, `PromotionReviewLookup`
    - Store error enums: `ExperimentStoreError`, `VerificationStoreError`, `ShadowStoreError`, `PromotionReviewStoreError`
    - `LoadedReplayScenario` struct (near line 4033)
    - The `default_require_known_bad_coverage` and `default_max_detect_latency_delta_us` default functions

    **stores.rs** -- Move all store implementations:
    - `MemoryReplayRunStore` struct and `ReplayRunStore` impl
    - `FileReplayRunStore` struct, `new()`, and `ReplayRunStore` impl
    - `FileExperimentStore` struct and impl
    - `FileVerificationStore` struct and impl
    - `FileShadowStore` struct and impl
    - `FilePromotionReviewStore` struct and impl

    **harness.rs** -- Move:
    - `DefaultReplayHarness` struct and its entire `impl` block (lines 1808-2652)
    - This is the core orchestration: run_scenario, run_suite, evaluate, experiment, verify, shadow, promotion_review, etc.

    **render.rs** -- Move all `pub fn render_*` functions:
    - `render_replay_run`, `render_evaluation_report`, `render_suite_report`
    - `render_experiment_report`, `render_verification_report`
    - `render_shadow_report`, `render_promotion_review_packet`

    **validation.rs** -- Move manifest loading and validation:
    - `load_scenario_manifest` (pub), `load_suite_manifest`, `load_experiment_manifest`, `load_detector_experiment_manifest` (pub), `load_verification_manifest` (pub)
    - `validate_manifest`, `validate_suite_manifest`, `validate_experiment_manifest`, `validate_verification_manifest`
    - `resolve_relative_path`, `scenario_paths_in_dir` (pub)

    **helpers.rs** -- Move internal computation helpers:
    - `supported_detector`, `detector_from_candidate`
    - `equality_check`, `latency_check`
    - `run_id_for_manifest`, `experiment_id_for_manifest`, `verification_id_for_experiment`, `shadow_id_for_report`, `promotion_review_id_for_packet`
    - `normalize_groups`, `technique_groups_from_suite`
    - `compare_suite_reports`, `suite_metrics`
    - `scenario_expected_positive`, `scenario_is_benign`, `scenario_detected`, `ratio`
    - `evaluate_experiment_gates`
    - `verify_known_bad_coverage`, `verify_canonical_templates`, `verify_false_positive_bound`, `verify_detect_latency_budget`, `verify_total_detection_budget`
    - `collect_review_blocking_reasons`
    - `offline_correlation_config`, `sanitize_id`, `sorted_recent_runs`, `now_ms`

    **mod.rs** -- Module root:
    ```rust
    pub mod types;
    pub mod stores;
    pub mod harness;
    pub mod render;
    pub mod validation;
    mod helpers;

    pub use types::*;
    pub use stores::*;
    pub use harness::*;
    pub use render::*;
    pub use validation::{load_scenario_manifest, load_detector_experiment_manifest, load_verification_manifest, scenario_paths_in_dir};

    #[cfg(test)]
    mod tests;
    ```

    Move the existing `mod tests` block from replay.rs into a separate `tests.rs` file inside `replay/`, OR keep it at the bottom of mod.rs. The tests use `super::*` imports so they work from mod.rs.

    After creating the directory module, DELETE the old `src/replay.rs` file. The `pub mod replay;` in lib.rs will now resolve to `src/replay/mod.rs`.

    Similarly for review_workbench: since `pub mod review_workbench;` already exists, and we want to keep the re-export facade approach, we need to handle the naming. The workbench/ directory uses a DIFFERENT name than the existing module, so:
    - lib.rs adds `pub mod workbench;` (new module)
    - lib.rs keeps `pub mod review_workbench;` (re-export facade file)
    - This works because `review_workbench` maps to `review_workbench.rs` and `workbench` maps to `workbench/mod.rs`

    After all splits, verify:
    - `cargo build --workspace` succeeds
    - `cargo test --workspace` passes all 319 tests
    - `cargo clippy --workspace -- -D warnings` passes
    - Every file in `replay/` is under 1,500 lines
    - Every file in `workbench/` is under 1,500 lines
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/swarm-team-six && cargo build --workspace && cargo test --workspace 2>&1 | tail -5 && cargo clippy --workspace -- -D warnings 2>&1 | tail -3 && echo "--- replay line counts ---" && wc -l crates/swarm-runtime/src/replay/*.rs && echo "--- workbench line counts ---" && wc -l crates/swarm-runtime/src/workbench/*.rs && echo "--- review_workbench.rs ---" && wc -l crates/swarm-runtime/src/review_workbench.rs</automated>
  </verify>
  <done>replay/ and workbench/ directories exist with focused submodules. Old replay.rs is deleted (replaced by replay/mod.rs). review_workbench.rs is a thin re-export facade. All submodules under 1,500 lines. All 319 tests pass. Clippy clean.</done>
</task>

</tasks>

<verification>
- `cargo build --workspace` succeeds
- `cargo test --workspace` passes all 319+ existing tests with zero regressions
- `cargo clippy --workspace -- -D warnings` produces zero warnings
- `wc -l crates/swarm-runtime/src/workbench/*.rs` shows each file under 1,500 lines
- `wc -l crates/swarm-runtime/src/replay/*.rs` shows each file under 1,500 lines
- `wc -l crates/swarm-runtime/src/review_workbench.rs` shows under 10 lines (facade)
- `ls crates/swarm-runtime/src/replay.rs` does not exist (replaced by directory)
</verification>

<success_criteria>
1. review_workbench.rs no longer contains implementation logic (thin facade or removed)
2. replay.rs no longer exists as a single file; replaced by replay/ directory
3. All workbench/ and replay/ submodules are under 1,500 lines
4. Zero test regressions
5. Zero clippy warnings
</success_criteria>

<output>
After completion, create `.planning/phases/88-module-decomposition/88-03-SUMMARY.md`
</output>
