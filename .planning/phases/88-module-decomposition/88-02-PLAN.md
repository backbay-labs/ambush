---
phase: 88-module-decomposition
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/swarm-runtime/src/http/mod.rs
  - crates/swarm-runtime/src/http/state.rs
  - crates/swarm-runtime/src/http/error.rs
  - crates/swarm-runtime/src/http/auth.rs
  - crates/swarm-runtime/src/http/control.rs
  - crates/swarm-runtime/src/http/review.rs
  - crates/swarm-runtime/src/http/evidence.rs
  - crates/swarm-runtime/src/http/approval.rs
  - crates/swarm-runtime/src/http/evolution.rs
  - crates/swarm-runtime/src/http/maintenance.rs
  - crates/swarm-runtime/src/http/helpers.rs
  - crates/swarm-runtime/src/http/render.rs
  - crates/swarm-runtime/src/operator_http.rs
  - crates/swarm-runtime/src/lib.rs
autonomous: true
requirements: [REFAC-02]

must_haves:
  truths:
    - "operator_http.rs is replaced by focused route modules each under 1.5K lines"
    - "The authenticated operator HTTP surface still serves all existing routes"
    - "All existing operator HTTP integration tests pass"
    - "cargo build && cargo test && cargo clippy passes with zero regressions"
  artifacts:
    - path: "crates/swarm-runtime/src/http/mod.rs"
      provides: "HTTP module root with LocalOperatorSurface, OperatorSurfacePaths, OperatorHttpError re-exports"
      contains: "pub struct LocalOperatorSurface"
    - path: "crates/swarm-runtime/src/http/state.rs"
      provides: "OperatorHttpState, OperatorSurfacePaths structs"
      max_lines: 300
    - path: "crates/swarm-runtime/src/http/error.rs"
      provides: "OperatorHttpError, OperatorApiError, OperatorReviewError types and IntoResponse impls"
      max_lines: 400
    - path: "crates/swarm-runtime/src/http/auth.rs"
      provides: "OperatorAuthState and require_bearer_auth middleware"
      max_lines: 200
    - path: "crates/swarm-runtime/src/http/control.rs"
      provides: "status, replay, investigation, incident handler functions"
      max_lines: 300
    - path: "crates/swarm-runtime/src/http/review.rs"
      provides: "All /v1/operator/review/* route handlers"
      max_lines: 1500
    - path: "crates/swarm-runtime/src/http/evidence.rs"
      provides: "All /v1/operator/evidence/* route handlers"
      max_lines: 400
    - path: "crates/swarm-runtime/src/http/approval.rs"
      provides: "All /v1/operator/approval-* route handlers"
      max_lines: 500
    - path: "crates/swarm-runtime/src/http/evolution.rs"
      provides: "All /v1/operator/evolution/* route handlers"
      max_lines: 500
    - path: "crates/swarm-runtime/src/http/maintenance.rs"
      provides: "All /v1/operator/maintenance/* route handlers"
      max_lines: 300
    - path: "crates/swarm-runtime/src/http/helpers.rs"
      provides: "Parse helpers, limit helpers, factory functions, shared utilities"
      max_lines: 500
    - path: "crates/swarm-runtime/src/http/render.rs"
      provides: "All HTML rendering functions (review_layout, session pages, evidence pages, etc.)"
      max_lines: 1500
  key_links:
    - from: "crates/swarm-runtime/src/http/mod.rs"
      to: "crates/swarm-runtime/src/http/control.rs"
      via: "router() method wires control handlers"
      pattern: "get\\(control::"
    - from: "crates/swarm-runtime/src/http/mod.rs"
      to: "crates/swarm-runtime/src/http/review.rs"
      via: "router() method wires review handlers"
      pattern: "review::"
    - from: "crates/swarm-runtime/src/operator_http.rs"
      to: "crates/swarm-runtime/src/http/mod.rs"
      via: "operator_http.rs becomes thin re-export facade"
      pattern: "pub use http::"
---

<objective>
Split the 5,392-line operator_http.rs into focused route modules under a new `src/http/` directory, with each module under 1.5K lines.

Purpose: The monolithic operator_http.rs mixes HTTP state management, authentication middleware, 6 domains of route handlers, parsing utilities, HTML rendering, and integration tests in one file. Splitting by concern makes each domain independently navigable and testable.

Output: An `http/` module directory with state.rs, error.rs, auth.rs, control.rs, review.rs, evidence.rs, approval.rs, evolution.rs, maintenance.rs, helpers.rs, and render.rs. The old operator_http.rs becomes a thin re-export facade.
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-module-decomposition/88-CONTEXT.md

@crates/swarm-runtime/src/operator_http.rs
@crates/swarm-runtime/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create http/ module directory and split operator_http.rs into focused submodules</name>
  <files>
    crates/swarm-runtime/src/http/mod.rs,
    crates/swarm-runtime/src/http/state.rs,
    crates/swarm-runtime/src/http/error.rs,
    crates/swarm-runtime/src/http/auth.rs,
    crates/swarm-runtime/src/http/control.rs,
    crates/swarm-runtime/src/http/review.rs,
    crates/swarm-runtime/src/http/evidence.rs,
    crates/swarm-runtime/src/http/approval.rs,
    crates/swarm-runtime/src/http/evolution.rs,
    crates/swarm-runtime/src/http/maintenance.rs,
    crates/swarm-runtime/src/http/helpers.rs,
    crates/swarm-runtime/src/http/render.rs,
    crates/swarm-runtime/src/lib.rs
  </files>
  <action>
    Create `crates/swarm-runtime/src/http/` directory. Split operator_http.rs into submodules by extracting each concern. This is mechanical extraction -- no logic changes.

    **state.rs** -- Move:
    - `OperatorSurfacePaths` struct (lines 54-77)
    - `OperatorHttpState` struct (the #[derive(Clone)] state shared across handlers, currently internal)
    - Make both `pub` so other submodules and mod.rs can use them

    **error.rs** -- Move:
    - `OperatorHttpError` enum (lines 81-125)
    - `OperatorApiError` enum and its `impl IntoResponse` (around lines 280-325)
    - `OperatorReviewError` enum and its `impl IntoResponse` (around lines 327-370)
    - `map_control_error`, `map_review_workbench_error`, `map_approval_error` functions

    **auth.rs** -- Move:
    - `OperatorAuthState` struct
    - `require_bearer_auth` async function (the middleware)

    **control.rs** -- Move route handler functions:
    - `status_handler` (lines 716-725)
    - `metrics_handler` (lines 727-740)
    - `replay_handler` (lines 742-753)
    - `investigation_handler` (lines 754-765)
    - `incident_handler` (lines 766-777)
    - The parse selector functions: `parse_replay_selector`, `parse_investigation_selector`, `parse_incident_selector`

    **review.rs** -- Move route handler functions for `/v1/operator/review/*`:
    - `review_home_handler` through all review session/capsule/delegation/handoff/evidence/promotion handlers
    - This is the largest handler group (~800 lines of handlers)
    - Include the review-specific filter types (`ReviewEvidenceVerificationFilter`, etc.)
    - Include `review_evidence_service`, `review_workbench_service`, and related factory helpers used ONLY by review handlers

    **evidence.rs** -- Move route handler functions for `/v1/operator/evidence/*`:
    - `evidence_bundle_handler`, `evidence_bundle_list_handler`, `evidence_verification_handler`, `promotion_evidence_packet_handler`
    - Include `evidence_service` factory helper

    **approval.rs** -- Move route handler functions for `/v1/operator/approval-*`:
    - `approval_set_handler`, `approval_set_list_handler`, `approval_set_create_handler`, `approval_vote_append_handler`, `approval_ledger_handler`, `approval_ledger_list_handler`
    - Include `approval_harness` factory helper

    **evolution.rs** -- Move route handler functions for `/v1/operator/evolution/*`:
    - `portfolio_handler`, `portfolio_list_handler`, `governance_packet_handler`, `packet_set_handler`, `packet_set_list_handler`, `portfolio_history_handler`, `portfolio_history_list_handler`
    - Include `portfolio_harness`, `governance_harness` factory helpers
    - Include `parse_portfolio_review_state`

    **maintenance.rs** -- Move route handler functions for `/v1/operator/maintenance/*`:
    - `maintenance_action_handler`, `maintenance_action_lookup_handler`, `maintenance_action_list_handler`
    - Include `maintenance_service` factory, `parse_maintenance_status`

    **helpers.rs** -- Move shared utilities:
    - `count_set`, `effective_limit`
    - All `limit_*` functions (limit_portfolio_list, limit_packet_set_list, etc.)
    - `parse_evidence_subject_kind`
    - `normalize_form_optional_text`
    - `parse_review_artifact_refs_text`

    **render.rs** -- Move ALL HTML rendering functions:
    - `escape_html`, `review_link`, `render_review_layout`, `render_status_pill`
    - All `render_*_options` functions
    - All `render_*_page` functions (render_review_session_list_page through render_review_promotion_packet_page)
    - `subject_api_path`, `render_related_ref_link`, `related_ref_path`
    - `render_maintenance_status_pill`, `render_review_home_page`

    **mod.rs** -- Contains:
    - All submodule declarations (`mod state; mod error; mod auth; ...`)
    - `pub use state::{OperatorSurfacePaths, OperatorHttpState};`
    - `pub use error::OperatorHttpError;`
    - The `LocalOperatorSurface` struct and its full `impl` block (from_path, from_config, from_paths, router, serve, bind_addr)
    - The `router()` method references handler functions from submodules: `control::status_handler`, `review::review_home_handler`, etc.
    - Move the existing tests into a `#[cfg(test)] mod tests` block at the bottom of mod.rs (they test the full router integration, so they belong at the module root)

    **lib.rs** -- Add `pub mod http;` to the module list. Keep `pub mod operator_http;` for now (it will become a re-export facade).

    Handler functions that are currently private (`async fn handler_name`) should become `pub(crate)` in their submodules so mod.rs `router()` can reference them. Alternatively, if the router is built in mod.rs, they can remain `pub(super)`.

    Each submodule imports types from siblings using `use super::state::*`, `use super::error::*`, `use super::helpers::*` etc.

    IMPORTANT: Do not change any handler logic, response types, route paths, or error handling. This is purely structural.
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/swarm-team-six && cargo build --workspace 2>&1 | tail -3</automated>
  </verify>
  <done>http/ directory exists with all submodules. `cargo build --workspace` succeeds. lib.rs exports `pub mod http`.</done>
</task>

<task type="auto">
  <name>Task 2: Replace operator_http.rs with thin re-export facade and verify all constraints</name>
  <files>
    crates/swarm-runtime/src/operator_http.rs,
    crates/swarm-runtime/src/bin/swarmctl.rs
  </files>
  <action>
    Replace operator_http.rs with a thin re-export module that preserves backward compatibility for any code importing `crate::operator_http::*`:

    ```rust
    //! Backward-compatible re-export facade.
    //!
    //! The operator HTTP surface now lives in `crate::http`. This module
    //! re-exports the public API so existing `use crate::operator_http::*`
    //! imports continue to compile.

    pub use crate::http::*;
    ```

    Check `swarmctl.rs` (or `cli/dispatch.rs` if Plan 01 already ran) for any `use swarm_runtime::operator_http::*` imports and update them to use `swarm_runtime::http::*` if it simplifies things. The re-export facade ensures either path works.

    After replacing, verify the full constraint set:
    - `cargo build --workspace` must succeed
    - `cargo test --workspace` must pass all existing tests
    - `cargo clippy --workspace -- -D warnings` must pass
    - Every file in `src/http/` must be under 1,500 lines (`wc -l`)
    - `operator_http.rs` is now under 10 lines (thin facade)

    If any submodule exceeds 1.5K lines, split further. The most likely candidate is render.rs (HTML rendering is ~1,400 lines) -- if it exceeds 1.5K, split into `render_sessions.rs` and `render_evidence.rs`.
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/swarm-team-six && cargo build --workspace && cargo test --workspace 2>&1 | tail -5 && cargo clippy --workspace -- -D warnings 2>&1 | tail -3 && echo "--- submodule line counts ---" && wc -l crates/swarm-runtime/src/http/*.rs && echo "--- operator_http.rs ---" && wc -l crates/swarm-runtime/src/operator_http.rs</automated>
  </verify>
  <done>operator_http.rs is a thin re-export facade. All http/ submodules are under 1,500 lines. All 319 tests pass. Clippy clean.</done>
</task>

</tasks>

<verification>
- `cargo build --workspace` succeeds
- `cargo test --workspace` passes all 319+ existing tests with zero regressions
- `cargo clippy --workspace -- -D warnings` produces zero warnings
- `wc -l crates/swarm-runtime/src/http/*.rs` shows each file under 1,500 lines
- `wc -l crates/swarm-runtime/src/operator_http.rs` shows under 10 lines
- All existing route paths (`/v1/operator/status`, `/v1/operator/review/sessions`, etc.) are preserved
</verification>

<success_criteria>
1. operator_http.rs no longer contains route handler implementations
2. Route handlers live in 4-5+ focused submodules each under 1.5K lines
3. Zero test regressions
4. Zero clippy warnings
</success_criteria>

<output>
After completion, create `.planning/phases/88-module-decomposition/88-02-SUMMARY.md`
</output>
