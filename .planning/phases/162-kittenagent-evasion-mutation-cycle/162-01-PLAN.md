---
phase: 162-kittenagent-evasion-mutation-cycle
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 161
files_modified:
  - crates/swarm-runtime/src/kitten_agent.rs
  - crates/swarm-evolution/src/mutation.rs
  - crates/swarm-runtime/src/evolution_status.rs
  - crates/swarm-runtime/src/evasion_coverage.rs
  - crates/swarm-runtime/tests/critical_path_integration.rs
autonomous: true
requirements:
  - EVASION-03
  - EVASION-05
must_haves:
  truths:
    - "Gap reporting only matters if it changes candidate selection; operator-visible metrics alone do not satisfy EVASION-03."
    - "Kitten already has replay, memory, and adversarial pressure lanes, so evasion pressure should join that same durable scoring path instead of bypassing mutation artifacts."
    - "The proof obligation is end to end: a measured evasion gap must cause a mutation proposal and reach the existing canary admission lane."
  artifacts:
    - path: "crates/swarm-runtime/src/evasion_coverage.rs"
      provides: "Measured evasion gap input and intentional-gap metadata."
    - path: "crates/swarm-runtime/src/kitten_agent.rs"
      provides: "Bounded mutation orchestration and proposal routing."
    - path: "crates/swarm-evolution/src/mutation.rs"
      provides: "Durable ranking, population, and episode persistence."
  key_links:
    - from: "crates/swarm-runtime/src/evasion_coverage.rs"
      to: "crates/swarm-runtime/src/kitten_agent.rs"
      via: "coverage snapshot and filtered detector gaps"
      pattern: "measured missed coverage becomes mutation pressure on the live Kitten loop"
    - from: "crates/swarm-runtime/src/kitten_agent.rs"
      to: "crates/swarm-evolution/src/mutation.rs"
      via: "proposal fitness and episode reporting"
      pattern: "gap-driven mutation survives restart through the existing durable evolution lane"
---

<objective>
Complete Phase 162 by teaching Kitten to react to the Phase 161 evasion benchmark, persist gap-driven pressure through the existing mutation artifacts, and prove the full evasion-gap-to-canary loop with integration coverage.

Purpose: turn the new benchmark from passive reporting into an active strategy-improvement driver.
Output: Kitten-driven mutation pressure from measured evasion gaps, durable episode and population artifacts carrying that pressure, and end-to-end tests showing the candidate reaches canary validation.
</objective>

<context>
@.planning/phases/162-kittenagent-evasion-mutation-cycle/162-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/kitten_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/mutation.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/evasion_coverage.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Translate coverage gaps into mutation pressure</name>
  <files>
    crates/swarm-runtime/src/evasion_coverage.rs
    crates/swarm-runtime/src/kitten_agent.rs
  </files>
  <action>Define the bounded gap-selection logic Kitten should use from the Phase 161 coverage snapshot, excluding intentionally uncovered techniques and prioritizing real detector misses.</action>
  <verify>
    <automated>cargo test -p swarm-runtime kitten_agent::tests::evasion_ -- --nocapture</automated>
  </verify>
  <done>Kitten can deterministically identify actionable evasion gaps from the shared runtime snapshot without re-parsing the corpus in a second code path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist evasion pressure through mutation and population artifacts</name>
  <files>
    crates/swarm-runtime/src/kitten_agent.rs
    crates/swarm-evolution/src/mutation.rs
    crates/swarm-runtime/src/evolution_status.rs
  </files>
  <action>Carry evasion-gap pressure into proposal fitness, durable ranking or episode artifacts, and any surfaced evolution-status summary so restart-safe mutation history reflects why candidates were proposed.</action>
  <verify>
    <automated>cargo test -p swarm-evolution mutation::tests::evasion_ -- --nocapture</automated>
  </verify>
  <done>The durable evolution lane records gap-driven pressure and candidate outcomes instead of dropping that reasoning after one in-memory tick.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove evasion gap to canary validation end to end</name>
  <files>
    crates/swarm-runtime/tests/critical_path_integration.rs
    crates/swarm-runtime/src/kitten_agent.rs
  </files>
  <action>Extend integration coverage so a measured evasion miss causes Kitten to propose a candidate that traverses the existing safety and canary pipeline.</action>
  <verify>
    <automated>cargo test -p swarm-runtime evasion_to_canary -- --nocapture</automated>
  </verify>
  <done>The repo has an end-to-end proof for EVASION-05 rather than only unit coverage around intermediate data structures.</done>
</task>

</tasks>

<verification>
Keep the phase focused on mutation pressure and canary flow. Optional Z3 proof compilation remains Phase 163.
</verification>
