---
phase: 287-adversarial-co-evolution-arena
plan: 00I
type: execute
wave: 5
depends_on: ["287-00F"]
files_modified:
  - tools/check-arena-corpus-truth.py
autonomous: true
requirements: [ARENA-01, ARENA-05, ARENA-07, ARENA-08]

must_haves:
  truths:
    - "The eight Plan 00F corpus truth files pass an independent strict schema/digest/partition/fingerprint/corpus-registry verifier before Plan 00B or any runtime loader proceeds; execution tests remain solely governed by tools/arena-test-registry.json."
    - "Every deletion, duplication, unknown-field, threshold, digest, fingerprint, partition, registry, or withheld-leak mutation fails closed under a run-owned temporary root."
  artifacts:
    - path: "tools/check-arena-corpus-truth.py"
      provides: "authoring-time strict eight-artifact schema, digest, partition, denominator, and mutation verifier"
  key_links:
    - from: "tools/check-arena-corpus-truth.py"
      to: "scenarios/adversarial-coevolution-arena/{catalog,partitions,historical,benign,counterexamples,withheld,baseline,oracle-registry}"
      via: "strict parser recomputes canonical corpus content/partition/fingerprint/oracle-manifest digests, proves disjointness and withheld separation, and checks nonzero denominators and integer thresholds"
---

<objective>
Independently verify the complete eight-artifact corpus authored by Plan 00F
before any runtime or oracle consumer proceeds.
</objective>

<ownership_boundary>
Plan 00I owns only `tools/check-arena-corpus-truth.py`; Plan 00F owns all eight
checked-in truth files and their bytes/metadata. Plan 00B remains the
independent runtime corpus oracle and Plan 00E remains the independent config
inventory. This verifier may use a run-owned temporary mutation root but must
never author, repair, regenerate, or copy checked-in truth files from
production output.
</ownership_boundary>

<corpus_contract>
The verifier's input set is exactly these eight Plan 00F paths:
`scenarios/adversarial-coevolution-arena/catalog.yaml`,
`scenarios/adversarial-coevolution-arena/partitions.yaml`,
`scenarios/adversarial-coevolution-arena/historical-attacks.yaml`,
`scenarios/adversarial-coevolution-arena/benign-controls.yaml`,
`scenarios/adversarial-coevolution-arena/counterexamples.yaml`,
`scenarios/adversarial-coevolution-arena/withheld-campaigns.yaml`,
`scenarios/adversarial-coevolution-arena/baseline.json`, and
`scenarios/adversarial-coevolution-arena/oracle-registry.json`. It must
reject a missing, extra, aliased, or unlisted artifact before parsing any
runtime output and must recompute each declared content, partition,
fingerprint, threshold, denominator, and corpus-oracle-manifest invariant from
those bytes; it must not contain or authorize execution test names/counts,
which are owned only by `tools/arena-test-registry.json`.
</corpus_contract>

<context>
@.planning/phases/287-adversarial-co-evolution-arena/287-00F-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-CONTEXT.md
@.planning/phases/287-adversarial-co-evolution-arena/287-RESEARCH.md
@.planning/phases/287-adversarial-co-evolution-arena/287-VALIDATION.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00B-PLAN.md
@crates/swarm-core/src/signed_state.rs
@crates/swarm-crypto/src/lib.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 287-00I-01: Implement the independent eight-artifact corpus verifier</name>
  <files>tools/check-arena-corpus-truth.py</files>
  <read_first>
    - `287-00F-PLAN.md` and all eight exact corpus paths before defining the closed schema or digest rules.
    - `287-CONTEXT.md`, `287-RESEARCH.md`, and `287-VALIDATION.md` for the partition, threshold, mutation, and preflight contracts.
    - `287-00B-PLAN.md` for the independent runtime corpus oracle and the prohibition on truth-owner duplication or repair.
    - `crates/swarm-core/src/signed_state.rs` and `crates/swarm-crypto/src/lib.rs` for canonical digest and detached-signature metadata conventions.
  </read_first>
  <behavior>
    - Test 1: `corpus_truth_check_accepts_exact_eight_artifacts` requires all eight files to be nonempty, parse the closed schema with unknown fields rejected, and expose canonical content/partition/fingerprint/corpus-oracle-manifest digests, detached-signature metadata, integer thresholds, and nonzero denominators.
    - Test 2: `corpus_truth_self_test_rejects_every_declared_mutation` deletes/duplicates entries, adds unknown fields, changes thresholds/digests/fingerprints/partitions/oracle-manifest metadata, attempts to add execution test names/counts, and leaks a withheld ID in a run-owned temporary copy; every mutation fails while the clean set passes.
  </behavior>
  <action>Implement only `tools/check-arena-corpus-truth.py` as an authoring-time credential-free strict verifier over Plan 00F's exact eight files. Use the repository's pinned YAML/JSON parser; reject missing or extra artifacts, unknown fields, duplicate IDs, and closed-vocabulary violations; recompute canonical bytes/content/partition/fingerprint/corpus-oracle-manifest digests; require detached-signature metadata, nonzero control/in-sample denominators, integer basis-point thresholds, disjoint partition membership, withheld separation, and closed `FixturePrimitive`/`FixtureTargetId` values. Treat `oracle-registry.json` as corpus-only: reject any attempt to use it as execution authority or to add test names/counts; `tools/arena-test-registry.json` remains the sole execution registry and is checked by Plan 00J/06. Implement `--check` and `--self-test`; every mutation runs under one `mkdtemp`/temporary root and must fail closed. Never author, repair, regenerate, or copy checked-in truth files from production output; Plan 00B remains the independent runtime corpus-oracle consumer and Plan 00E independently checks the complete config inventory.
  </action>
  <verify><automated>bash tools/check-arena-test-counts.sh --self-test && python3 tools/check-arena-corpus-truth.py --self-test && python3 tools/check-arena-corpus-truth.py --check && git diff --check</automated></verify>
  <acceptance_criteria>
    - The clean exact eight-artifact set passes both `--self-test` and `--check`; missing, extra, unknown, duplicate, digest, threshold, fingerprint, partition, registry, and withheld mutations fail.
    - No production loader or runtime output is a truth source; checked-in corpus bytes remain untouched by the verifier and all mutations stay under a run-owned temporary root.
  </acceptance_criteria>
  <done>The independent authoring-time verifier proves all eight Plan 00F artifacts structurally and digest-safe before Plan 00B proceeds.</done>
</task>
</tasks>

<verification>Run this verifier immediately after Plan 00F, then Plan 00B's independent oracle and Plan 00E's final inventory; no runtime consumer may proceed on a missing or mutated corpus.</verification>

<success_criteria>
- All eight Plan 00F truth files pass independent schema, digest, partition, fingerprint, registry, denominator, and threshold checks.
- Every declared mutation fails closed under a run-owned temporary root.
</success_criteria>

<output>Create `287-00I-SUMMARY.md` with all eight artifact digests, mutation outcomes, and exact verifier command evidence.</output>
