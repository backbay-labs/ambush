---
phase: 287-adversarial-co-evolution-arena
plan: 01
type: execute
wave: 6
depends_on: ["287-00", "287-00A", "287-00B", "287-00E"]
files_modified:
  - crates/swarm-arena-red/src/lib.rs
  - crates/swarm-arena-red/src/catalog.rs
  - crates/swarm-arena-red/src/grammar.rs
  - crates/swarm-arena-red/src/campaign.rs
  - crates/swarm-arena-red/src/scheduler.rs
  - crates/swarm-arena-red/tests/red_grammar.rs
  - crates/swarm-arena-red/tests/scheduler_bounds.rs
autonomous: true
requirements: [ARENA-01, ARENA-03, ARENA-08]

must_haves:
  truths:
    - "A Red campaign is a bounded, reusable sequence of catalogued fixture stages with deterministic event IDs, virtual timestamps, ordering keys, and no invented capability or live-target representation."
    - "Ready events and mutation choices are byte-identical for fixed seed/corpus/parent/outcome/generation inputs, independent of HashMap order, async order, host clock, or agent count."
    - "Every surviving child campaign records the exact persisted BlueOutcome and RedOutcomeProjection digests plus escape/falsification evidence that caused its allowed timing, ordering, or tactic-composition change."
    - "Generation, event/work, virtual-duration, mutation, plateau, coverage, and partition bounds terminate the loop with the first explicit stop reason; a wall watchdog is guard-only."
    - "Red uses only `GraphLogicalTime` and cannot name or implement the runtime `GraphClock` trait or any authority type reachable through `swarm-core`."
  artifacts:
    - path: "crates/swarm-arena-red/src/grammar.rs"
      provides: "deny-unknown-fields bounded fixture grammar and validation"
    - path: "crates/swarm-arena-red/src/campaign.rs"
      provides: "catalogued campaign materialization into deterministic arena-fixture telemetry"
    - path: "crates/swarm-arena-red/src/scheduler.rs"
      provides: "virtual clock, stable queue, budgets, plateau/coverage stop state"
    - path: "crates/swarm-arena-red/src/mutation.rs"
      provides: "RedOutcomeProjection-only causal mutation and lineage"
  key_links:
    - from: "crates/swarm-arena-red/src/campaign.rs"
      to: "crates/swarm-arena-red/src/catalog.rs"
      via: "catalog digest and fixture primitive lookup"
      pattern: "catalog_digest|FixturePrimitive"
    - from: "crates/swarm-arena-red/src/scheduler.rs"
      to: "crates/swarm-arena-red/src/campaign.rs"
      via: "virtual timestamp/order/event-id sorting"
      pattern: "virtual_timestamp|ordering_key|event_id"
    - from: "crates/swarm-arena-red/src/mutation.rs"
      to: "crates/swarm-arena-red/src/contracts.rs"
      via: "parent/outcome digest and changed-dimension lineage"
      pattern: "RedOutcomeProjection|projection_digest|RedMutationDecision"
    - from: "crates/swarm-arena-red/src/mutation.rs"
      to: "crates/swarm-runtime/src/red_swarm.rs"
      via: "explicit non-use of static replay adapters"
      pattern: "never.*SuiteRedSwarmAdapter|MockRedSwarm|DefaultReplayHarness"
---

<objective>
Implement the capability-minimal Red campaign interpreter, deterministic virtual scheduler, and measured RedOutcomeProjection-driven mutation.

Purpose: more Red workers or timestamp shuffling is not co-evolution. The Red side must express only finite catalogued fixture behavior and must be able to show why each surviving mutation occurred.
Output: validated campaigns, deterministic materialization/scheduling, explicit stop reasons, and causal mutation decisions that the Blue arena can consume. Full BlueOutcome remains Arena-owned and is never a Red input.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/287-adversarial-co-evolution-arena/287-CONTEXT.md
@.planning/phases/287-adversarial-co-evolution-arena/287-RESEARCH.md
@.planning/phases/287-adversarial-co-evolution-arena/287-VALIDATION.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00B-PLAN.md
@crates/swarm-runtime/src/red_swarm.rs
@crates/swarm-runtime/src/replay/harness.rs
@crates/swarm-runtime/src/replay/types.rs
@scenarios/adversarial-coevolution-arena/catalog.yaml

<interfaces>
From the Wave 0 contracts (`crates/swarm-arena-red/src/contracts.rs`):
```rust
Campaign { seed, corpus_digest, fixture_namespace, generation,
    virtual_start_ms, stages, budget }
CampaignStage { ordinal, tactic_id, technique_id, primitive,
    offset_ms, ordering_key }
RedOutcomeProjection { campaign_digest, outcome_digest, escape_event_ids,
    falsified_hypothesis_ids, graph_refs, telemetry_families, partition }
RedMutationDecision { parent_digest, outcome_digest, cause_refs,
    changed_dimensions, old_values, new_values, mutation_ordinal,
    stop_state }
```

The capability-neutral Phase 286 graph time value is:
```rust
pub struct swarm_core::hypothesis_graph::GraphLogicalTime(pub i64);
```
The runtime-only trait `swarm_runtime::hypothesis_graph::clock::GraphClock`
belongs to the Blue adapter in Plan 02. Red returns `GraphLogicalTime` values
from its virtual scheduler; it never depends on, implements, or exports that
runtime trait and never reimplements graph, task, memory, containment, or
authority stores.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement strict catalog grammar and fixture campaign materialization</name>
  <files>crates/swarm-arena-red/src/lib.rs, crates/swarm-arena-red/src/catalog.rs, crates/swarm-arena-red/src/grammar.rs, crates/swarm-arena-red/src/campaign.rs, crates/swarm-arena-red/tests/red_grammar.rs</files>
  <behavior>
    - Test 1: A valid catalogued multi-stage campaign yields deterministic `arena-fixture` telemetry with event IDs derived from canonical campaign bytes, stage ordinal, and fixture namespace.
    - Test 2: Unknown tactic/technique, unknown fields, unsupported primitive, invalid namespace, negative/overflow timing, depth/stage/event overages, path traversal, URL/IP/command/socket/process/file-write input, neutral/mixed class, and altered corpus digest are rejected before materialization.
    - Test 3: Reordering stages only through the declared ordering field changes the canonical campaign digest; arbitrary event/string replay input cannot be accepted as a campaign.
  </behavior>
  <action>Load the independently sealed Wave 0 catalog/registry with strict `deny_unknown_fields` records and validate version/digest before use. Model `FixturePrimitive` and `FixtureTargetId` as closed enums of repository-owned synthetic fixtures (not strings that can contain commands, URLs, paths, IPs, process callbacks, sockets, or writes). Enforce fixture namespace, tactic/technique membership, maximum stage depth/count, non-negative bounded offsets, deterministic stage IDs, and `source = arena-fixture` when converting to `swarm_core::TelemetryEvent`. Hash canonical typed bytes with `swarm_crypto::canonical_json_bytes` and `sha256_hex`; use no UUIDs, entropy, wall time, or existing `SuiteRedSwarmAdapter`, `MockRedSwarm`, `DefaultReplayHarness::run_loaded_scenario`, or `evaluate_suite_selection`. The mutation API accepts only `RedOutcomeProjection`, not full `BlueOutcome`; no Red field may contain policy, response, receipt, lease, dispatcher, governance, executor, network, process, filesystem, or other authority reachable through `swarm-core`. Keep all failures typed and fail closed; do not add a permissive fallback parser, `todo!`, `unimplemented!`, ignored test, or no-op/placeholder campaign.</action>
  <verify><automated>cargo test -p swarm-arena-red --lib --locked --offline grammar::tests::catalogued_campaign_rejects_unbounded_primitive -- --exact && cargo test -p swarm-arena-red --test red_grammar --locked --offline -- --test-threads=1</automated></verify>
  <done>Only finite, catalogued, digest-verified fixture campaigns can be constructed, and valid materialization is deterministic and safe to hand to Blue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement virtual scheduling and all independent budgets</name>
  <files>crates/swarm-arena-red/src/scheduler.rs, crates/swarm-arena-red/tests/scheduler_bounds.rs</files>
  <behavior>
    - Test 1: Ready events sort by virtual timestamp, ordering key, stage ordinal, then deterministic event ID; repeated runs produce identical queue/dequeue bytes.
    - Test 2: Separate `max_generations`, `max_events`, `max_stages`, `max_virtual_ms`, `max_mutations`, `plateau_window`, and coverage limits each stop the run at the first applicable bound with a named reason.
    - Test 3: A wall-clock watchdog can abort a deliberately hung test process but its observation is marked guard-only and cannot change fitness, mutation choice, or acceptance.
  </behavior>
  <action>Implement a virtual scheduler with explicit starting millisecond and monotonic advancement from event offsets/work units; emit `swarm_core::hypothesis_graph::GraphLogicalTime` values but do not implement or depend on `swarm_runtime::hypothesis_graph::clock::GraphClock`. The Blue adapter constructs that runtime bridge later. Use `BTreeMap`/`BTreeSet` and the documented four-key ready ordering. Track each budget independently and record the first stop reason plus seed/corpus/scheduler versions in the signed-ready contract. Define plateau as no improvement in the declared Blue score for `plateau_window` generations and coverage as a bounded frozen fingerprint/tactic obligation, not agent count. The watchdog must be a test/process guard only, never a scored latency or selection input. Reject authority symbols, raw telemetry, process/network/file effects, stubs, placeholders, ignored tests, and no-op success paths in both source and tests.</action>
  <verify><automated>cargo test -p swarm-arena-red --lib --locked --offline scheduler::tests::virtual_queue_order_is_canonical -- --exact && cargo test -p swarm-arena-red --test scheduler_bounds --locked --offline -- --test-threads=1</automated></verify>
  <done>All deterministic schedule and resource limits are independently testable; every bounded stop is explicit and no host timing can influence a campaign decision.</done>
</task>

</tasks>

<verification>
Run the Red grammar, scheduler, and capability-isolation tests here; supplementary Plan 01B owns mutation-causality tests. Run clippy, format, offline metadata, and diff checks. Inspect the dependency tree and source inventory for forbidden policy/response/runtime/ingest/agent/network/process/promotion capability; no wall-clock measurement may participate in a digest or stop/fitness decision.
</verification>

<success_criteria>
- Red emits only finite catalogued fixture telemetry with deterministic IDs and virtual schedule.
- Every declared bound terminates with an explicit first stop reason and no unbounded resource path.
- Mutation is impossible without a measured BlueOutcome-derived RedOutcomeProjection and cannot change an undeclared capability; causal lineage is byte-stable.
</success_criteria>

<output>After completion, create `.planning/phases/287-adversarial-co-evolution-arena/287-01-SUMMARY.md` with grammar, scheduler, mutation, isolation, and exact negative-test evidence.</output>
