---
phase: 287-adversarial-co-evolution-arena
plan: 00J
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/check-arena-test-counts.sh
  - tools/arena-test-registry.json
autonomous: true
requirements: [ARENA-01, ARENA-02, ARENA-03, ARENA-04, ARENA-05, ARENA-06, ARENA-07, ARENA-08]

must_haves:
  truths:
    - "Every Cargo-based TDD verification invokes one shared strict count helper with a task ID, exact expected test name, and immutable registry entry before the test is trusted."
    - "A missing, renamed, multiply matched, zero-executed, failed, or ignored test makes the helper exit nonzero even when Cargo itself exits zero for an empty filter."
    - "tools/arena-test-registry.json is the sole canonical execution authority: each (task, package, target, exact_name) record has expected_count=1, the canonical registry digest is recomputed before use, and Plan 06 independently proves complete 45-name coverage."
    - "The helper's execution registry is distinct from the sealed corpus oracle: oracle-registry.json contains corpus truth only and no second test-name/count authority may be consulted to authorize execution."
  artifacts:
    - path: "tools/check-arena-test-counts.sh"
      provides: "strict exact-test wrapper that executes one bounded command and independently counts matched/executed/passed/failed/ignored tests"
    - path: "tools/arena-test-registry.json"
      provides: "schema-versioned canonical records for every task/package/target/exact-name tuple, expected_count=1, and a recomputed registry digest"
  key_links:
    - from: "tools/check-arena-test-counts.sh"
      to: "tools/arena-test-registry.json"
      via: "required --registry/--task lookup, package/target/exact-name record equality, and digest verification"
      pattern: "--registry|--task|package|target|exact_name|expected_count|registry_digest"
    - from: "tools/check-arena-test-counts.sh"
      to: "287-VALIDATION.md"
      via: "every Cargo command is wrapped with the matching task ID and --expect name"
      pattern: "check-arena-test-counts.sh.*--task.*--expect"
---

<objective>
Install the shared non-vacuous test-count contract before any Phase 287 Cargo
TDD task runs. The helper is deliberately separate from the Plan 00B corpus
oracle: it verifies execution cardinality for one named command, while the
corpus oracle verifies immutable scenario truth. The registry records every
exact Cargo test name used by the Phase 287 TDD validation map. This document
is the sole execution registry; corpus truth remains a separate, non-execution
authority.
</objective>

<ownership_boundary>
Plan 00J is the sole initial owner of the shared count helper and its
task-to-test registry. Later plans may add stricter corpus/oracle cross-checks
to the helper only through a serial dependency; they may not add a second
counter implementation, replace exact names with a whole-binary filter, or
change registry entries to make a failing/renamed test pass. Plan 00B remains
the sole owner of the separate `oracle-registry.json` corpus truth. That file
may carry corpus schema, fixture, partition, baseline, fingerprint, and
signature digests, but it does not authorize a Cargo test and must not carry a
competing test-name/count registry. Plan 00B, Plan 05, and Plan 06 use
`tools/arena-test-registry.json` for every positive test execution; any corpus
oracle read is limited to corpus truth validation.
</ownership_boundary>

<registry_contract>
The implementation must write one canonical JSON document at
`tools/arena-test-registry.json`, with this schema. The digest is SHA-256 over
the canonical UTF-8 JSON bytes of the object containing `schema_version` and
sorted `records`, excluding the `registry_digest` value itself; canonical JSON
means lexicographically sorted object keys, the compact separators `,` and `:`,
and no trailing newline:

```json
{
  "schema_version": 1,
  "records": [
    {"task_id":"287-00-01","package":"swarm-arena-red","target":"lib","exact_name":"...","expected_count":1}
  ],
  "registry_digest": "sha256:<canonical-records-digest>"
}
```

The canonical sort key is `(task_id, package, target, exact_name)` and every
record has the exact keys shown above. `expected_count` is the integer `1` for
every positive record. The following is the frozen canonical-record inventory
(pipe-delimited projection of the JSON records, in canonical sort order); the
implementation must write these exact values, not a wildcard or a prose list:

```text
287-00-01|swarm-arena|lib|arena_blue_contracts_round_trip_and_projection_boundary|1
287-00-01|swarm-arena-red|lib|arena_red_contracts_round_trip_and_authority_boundary|1
287-00-02|swarm-arena|lib|arena_config_package_boundary_is_canonical|1
287-00A-01|swarm-core|lib|arena_config_default_is_disabled_and_omission_preserves_signed_bytes|1
287-00A-01|swarm-core|lib|arena_config_rejects_every_bound_and_unsafe_run_root_mutation|1
287-00A-01|swarm-core|lib|arena_config_require_enabled_is_first_guard|1
287-00B-01|swarm-arena|arena_corpus_oracles|corpus_manifest_is_strict|1
287-00B-01|swarm-arena|arena_corpus_oracles|corpus_oracle_rejects_mutation|1
287-00B-02|swarm-arena-red|arena_red_isolation|red_capability_boundary_is_nonvacuous|1
287-00D-01|swarm-ingest-runtime|lib|second_half_swarm_config_literals_use_canonical_arena_defaults|1
287-01-01|swarm-arena-red|lib|grammar::tests::catalogued_campaign_rejects_unbounded_primitive|1
287-01-01|swarm-arena-red|red_grammar|red_grammar_accepts_catalogued_multistage_campaign|1
287-01-01|swarm-arena-red|red_grammar|red_grammar_rejects_catalog_digest_mutation|1
287-01-01|swarm-arena-red|red_grammar|red_grammar_rejects_forbidden_fixture_and_replay_inputs|1
287-01-02|swarm-arena-red|lib|scheduler::tests::virtual_queue_order_is_canonical|1
287-01-02|swarm-arena-red|scheduler_bounds|scheduler_enforces_independent_budget_precedence|1
287-01-02|swarm-arena-red|scheduler_bounds|scheduler_orders_virtual_events_canonically|1
287-01-02|swarm-arena-red|scheduler_bounds|scheduler_watchdog_is_guard_only|1
287-01B-01|swarm-arena-red|mutation_causality|mutation_changes_from_measured_escape_or_falsification|1
287-01B-01|swarm-arena-red|mutation_causality|mutation_rejects_static_replay_agent_count_and_authority_inputs|1
287-01B-01|swarm-arena-red|mutation_causality|mutation_requires_persisted_blue_outcome_and_projection|1
287-02-01|swarm-ingest-runtime|lib|arena_clock_injected_ingest_preserves_normalization|1
287-02-02|swarm-arena|blue_runtime|blue_runtime_real_phase286_capture|1
287-02-02|swarm-runtime|collective_hypothesis_graph_contract|public_phase286_graph_contract_compiles|1
287-02-02|swarm-runtime|phase286_bridge_handoff|no_action_returns_zero_authority_calls|1
287-02-02|swarm-runtime|phase286_bridge_handoff|one_graph_result_feeds_outcome_and_planner|1
287-02-03|swarm-arena|blue_safety|blue_safety_rejects_live_mode_and_missing_admission|1
287-02B-01|swarm-ingest-runtime|arena_virtual_clock|arena_disabled_refuses_before_construction|1
287-02B-01|swarm-ingest-runtime|arena_virtual_clock|arena_virtual_clock_replays_identically|1
287-02B-01|swarm-ingest-runtime|arena_virtual_clock|arena_virtual_clock_uses_injected_stack_and_phase286_worker|1
287-03-01|swarm-arena|candidate_lineage|candidate_lineage_rejects_unsafe_or_noop_inputs|1
287-03-01|swarm-arena|candidate_lineage|candidate_selection_is_canonical_and_bounded|1
287-03-01|swarm-arena|candidate_lineage|learned_state_mapping_changes_real_inputs|1
287-03-01|swarm-arena|candidate_lineage|learned_state_materially_applies_detector_profile|1
287-03-02|swarm-arena|evaluation_partitions|evaluation_partitions_reject_digest_or_withheld_mutation|1
287-03-03|swarm-arena|signed_artifacts|signed_artifacts_reject_tamper_replay_and_fence|1
287-04-01|swarm-arena|adversarial_coevolution_arena|adaptive_mutation_is_causal|1
287-04-01|swarm-arena|adversarial_coevolution_arena|arena_disabled_refuses_before_ingest|1
287-04-01|swarm-arena|arena_ingest_capture_handoff|arena_ingest_capture_is_single_source|1
287-04-02|swarm-arena|adversarial_coevolution_arena|acceptance_metrics_meet_arena_07|1
287-04-02|swarm-arena|adversarial_coevolution_arena|blue_uses_real_runtime|1
287-04-02|swarm-arena|adversarial_coevolution_arena|single_agent_control_is_identical_stream|1
287-04-03|swarm-arena|arena_teardown|bounded_teardown|1
287-04-03|swarm-arena|arena_teardown|reproducible_artifacts|1
287-04-03|swarm-arena|arena_teardown|teardown_rejects_tamper_replay_and_leak|1
```

There are exactly 45 records and 45 unique exact names. A record is unique by
the full tuple `(task_id, package, target, exact_name)`; duplicate records or
any `expected_count != 1` fail closed. Multi-test tasks and cross-package or
cross-target tasks are represented by multiple records, never by collapsing a
task to one package or one binary. The helper binds each invocation's task,
Cargo package, Cargo target, exact filter, and `expected_count` to one matching
record. The aggregate workspace test in final-audit Task 287-05-03 is a
separate full-suite gate, not a named TDD test invocation, and is the only
positive direct Cargo test command exempt from this exact-name wrapper.

The `287-00J-01` zero-test `-- printf ...` mutation is a mutation-only
negative fixture, not a positive Cargo row, and is excluded from the
45-record coverage set. It exists only to prove that the helper rejects a
zero-executed child; Plan 06 classifies it as a mutation fixture rather than a
duplicate or missing execution record.
</registry_contract>

<execution_registry_consistency>
`tools/arena-test-registry.json` is the one execution oracle. The helper
canonicalizes records and recomputes `registry_digest` on every invocation;
Plan 05 reads this same file for all positive test names and Plan 06 reparses
the same bytes. Plan 05 and Plan 06 independently extract positive
helper-wrapped Cargo invocations from the task-level plan `<verify>` blocks
and from `287-VALIDATION.md` as two separate canonical source projections.
For each projection, emit the same schema-1 sorted records with
`expected_count=1`, then compute an independent canonical SHA-256 digest using
the registry's exact JSON canonicalization. Let `R` be the registry record set
and digest, `P` the plan-source record set and digest, and `V` the
validation-source record set and digest. Require `P == R`, `V == R`,
`digest(P) == digest(R)`, and `digest(V) == digest(R)` independently; a union
of sources is never sufficient. The positive plan and validation sets must
each contain all 45 records with no missing, extra, duplicate, or reassigned
tuple. The omission mutations remove one positive row from plans only or from
validation only while leaving the other source and registry intact; each must
fail closed. The 287-00J `-- printf` mutation and the 287-05-03 aggregate are
excluded from both positive source projections under their explicit exception
contracts. `scenarios/.../oracle-registry.json` is checked only by the corpus
verifier for schema/content/partition/fingerprint truth; it cannot supply test
names, counts, or execution approval. This single-registry rule eliminates a
second helper/oracle authority.
</execution_registry_consistency>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/287-adversarial-co-evolution-arena/287-CONTEXT.md
@.planning/phases/287-adversarial-co-evolution-arena/287-RESEARCH.md
@.planning/phases/287-adversarial-co-evolution-arena/287-VALIDATION.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00B-PLAN.md
@tools/check-gates-wired.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 287-00J-01: Create the strict exact-test count helper and registry</name>
  <files>tools/check-arena-test-counts.sh, tools/arena-test-registry.json</files>
  <read_first>
    - `.planning/phases/287-adversarial-co-evolution-arena/287-VALIDATION.md` and every TDD `<verify>` command before freezing the registry entries.
    - `tools/check-arena-oracles.sh` conventions and the Plan 00B exact-count/oracle boundary; do not merge corpus truth into this registry.
    - Existing shell self-test conventions; all mutation roots must be one `mktemp -d` owned by the helper and removed by a trap.
  </read_first>
  <behavior>
    - Test 1: The clean registry has schema version 1, a recomputable canonical digest, exactly 45 unique records, one `expected_count=1` record per `(task, package, target, exact_name)`, and no wildcard/empty/duplicate value; a task/package/target/name/count mismatch fails closed.
    - Test 2: Wrapping an exact Cargo command records one matching tuple, executed > 0, passed > 0, failed == 0, and ignored == 0; multi-test/cross-package tasks are checked record-by-record and a renamed/missing/duplicate output or whole-binary filter fails nonzero.
    - Test 3: A Cargo-like zero-test result (`0 passed; 0 failed; 0 ignored`) and an ignored-test result are deliberate mutations and must be rejected even when the wrapped command exits 0; a command exit failure is never converted into success, and the 00J mutation is classified separately from positive registry rows.
  </behavior>
  <action>Write the canonical registry first with schema version 1, sorted records, `expected_count: 1` for each of the exact 45 records above, and a digest over the canonical records. Implement `tools/check-arena-test-counts.sh` as credential-free POSIX/Bash: require `--registry`, `--task`, one or more `--expect`, and `-- <command>`; canonicalize and validate the registry before execution; parse the Cargo package and target from the child command; require each supplied expectation to equal exactly one registry record for the full `(task, package, target, exact_name)` tuple and require its count to be 1; require Cargo commands to carry `--locked --offline`, an explicit target, and `--exact` with the expected name; reject unfiltered whole-binary commands rather than trusting a zero-match exit. Multiple records for one task are supported by separate invocations and cross-package/cross-target records are never merged. Execute only the supplied command under one `mktemp -d` capture root, parse each exact `test <name> ... ok|FAILED|ignored` line and its summary, and independently require every expected name to match exactly once, `executed = passed + failed + ignored > 0`, `passed > 0`, `failed == 0`, and `ignored == 0`. Treat missing/renamed/duplicate/ignored/zero-result output or a nonzero child status as a nonzero helper result. Add `--self-test` mutations for registry digest/record/package/target/name/count changes, missing/renamed/duplicate names, zero-test output, ignored output, whole-binary filters, child failure, and an absent command; every mutation must fail while the clean fixture passes. The self-test must identify the 287-00J zero-test `-- printf` child as a mutation-only fixture, not as one of the 45 positive Cargo records. Keep all temporary paths explicit and owned; never delete a repository/worktree path, use credentials/network/process side effects, or make a fallback count from a broad suite.
  </action>
  <verify><automated>bash -n tools/check-arena-test-counts.sh && bash tools/check-arena-test-counts.sh --self-test && if bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-00-01 --expect arena_red_contracts_round_trip_and_authority_boundary -- printf 'test result: ok. 0 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out\n'; then exit 1; fi && git diff --check</automated></verify>
  <acceptance_criteria>
    - The helper's self-test proves nonzero rejection for zero executed/passed, renamed/missing/duplicate, ignored, failed, stale-digest, wrong package/target, whole-binary, and absent-command mutations; clean exact commands pass only with the matching full-tuple registry entry.
    - Every Cargo-based TDD row listed in the registry uses `--locked --offline`, an explicit target, `--exact`, and the same exact name in the registry, plan verify, and `287-VALIDATION.md`.
    - The helper has one task-owned implementation, one mutation-controlled temporary root, no `|| true`, no ignored allowance, no watch mode, and no broad suite fallback.
  </acceptance_criteria>
  <done>Before Plan 00 starts, Phase 287 has one immutable task-to-exact-test registry and one strict shared count helper that rejects every vacuous or ignored Cargo result.</done>
</task>

</tasks>

<verification>
Run the helper syntax check, its complete self-test, the explicit zero-test
mutation command, and `git diff --check`. Inspect every later Cargo TDD row
for the matching `--registry`, `--task`, `--expect`, package, target, and
`--exact` wrapper; Plan 06 must independently parse the full 45-name
task/package/target/name set and reject any row that bypasses this helper.
</verification>

<success_criteria>
- The shared helper exists before all Phase 287 Cargo TDD tasks through the
  Wave 1 dependency and all dependent waves use it.
- Exact names, positive execution/passed counts, zero failures, and
  ignored==0 are independently enforced per canonical record; Cargo's
  zero-match success cannot satisfy a task, and the independent 45-name
  set/digest equality prevents omitted records from hiding behind valid rows.
</success_criteria>

<output>After completion, create `.planning/phases/287-adversarial-co-evolution-arena/287-00J-SUMMARY.md` with the registry digest, task/name inventory, and zero/renamed/ignored mutation evidence.</output>
