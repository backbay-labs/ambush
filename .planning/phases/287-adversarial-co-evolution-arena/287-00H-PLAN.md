---
phase: 287-adversarial-co-evolution-arena
plan: 00H
type: execute
wave: 5
depends_on: ["287-00D"]
files_modified:
  - crates/swarm-runtime/src/service/tests_support.rs
  - crates/swarm-runtime/src/strategy.rs
  - crates/swarm-runtime/src/threat_intel_runtime.rs
autonomous: true
requirements: [ARENA-02, ARENA-06, ARENA-08]

must_haves:
  truths:
    - "The three remaining second-half SwarmConfig sites use the one canonical ArenaConfig default and retain service, strategy, and threat-intelligence behavior."
    - "The second-half helper and final AST scanner own these paths exactly once and reject alternate default values."
  artifacts:
    - path: "crates/swarm-runtime/src/service/tests_support.rs"
      provides: "service support migration"
    - path: "crates/swarm-runtime/src/strategy.rs"
      provides: "strategy migration"
    - path: "crates/swarm-runtime/src/threat_intel_runtime.rs"
      provides: "threat-intelligence migration"
---

<objective>Finish the second-half config migration ownership split without duplicating defaults.</objective>

<ownership_boundary>
This plan runs strictly after 287-00D and is the only owner of the three
supplementary second-half source paths. It may not duplicate or replace the
00D helper; its serial extension is the sole way those paths enter the live
second-half inventory before Plan 00E's independent scan.
</ownership_boundary>

<tasks>
<task type="auto" tdd="true">
  <name>Task 287-00H-01: Migrate service, strategy, and threat-intel literals</name>
  <files>crates/swarm-runtime/src/service/tests_support.rs, crates/swarm-runtime/src/strategy.rs, crates/swarm-runtime/src/threat_intel_runtime.rs</files>
  <read_first>
    - The three owned Rust paths and their current live `SwarmConfig` expressions.
    - `tools/check-arena-config-literals-second-half.sh` and `287-00D-PLAN.md` for second-half discovery/ownership rules.
    - `crates/swarm-core/src/config/defaults.rs` and `root.rs` for the one disabled ArenaConfig default.
  </read_first>
  <behavior>
    - Test 1: `second_half_config_literals_are_exactly_owned` discovers all three owned live paths and matches each direct `SwarmConfig` expression exactly once.
    - Test 2: `second_half_config_literal_mutations_fail_closed` rejects omission, alternate default, ownership drift, changed explicit value, and missing/extra path mutations.
  </behavior>
  <action>Write mutation tests first, then add the canonical disabled-by-default `arena: ArenaConfig::default()` field/helper to every live owned direct or loader-owned `SwarmConfig` site discovered in the three files. Preserve explicit runtime/policy values and signed bytes. The Plan 00D helper (owned by 00D) must enumerate current tracked Rust files and fail for omission, alternate defaults, path ownership drift, and changed literal values; 00E remains the independent combined-tree oracle. This supplementary plan owns no scanner/helper file and must not duplicate the 00D tool path.</action>
  <verify><automated>bash tools/check-arena-test-counts.sh --self-test && bash tools/check-arena-config-literals-second-half.sh --self-test && bash tools/check-arena-config-literals-second-half.sh && cargo fmt --all -- --check && git diff --check</automated></verify>
  <acceptance_criteria>
    - The second-half helper discovers and fingerprints all three live paths exactly once and rejects omission, alternate default, ownership drift, and changed-literal mutations.
    - `cargo fmt --all -- --check` and `git diff --check` exit 0; explicit runtime/policy values and signed ruleset bytes remain unchanged.
  </acceptance_criteria>
  <done>All three supplementary second-half paths are migrated and the final inventory can prove the complete 23-path ownership.</done>
</task>
</tasks>

<verification>Run the 00D helper and final 00E scanner after both migration halves are present.</verification>
<success_criteria>Every owned literal has one canonical arena default and no alternate constructor.</success_criteria>
<output>Create a concise supplementary migration summary.</output>
