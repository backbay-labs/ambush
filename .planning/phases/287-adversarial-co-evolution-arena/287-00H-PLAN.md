---
phase: 287-adversarial-co-evolution-arena
plan: 00H
type: execute
wave: 4
depends_on: ["287-00D"]
files_modified:
  - crates/swarm-runtime/src/service/tests_support.rs
  - crates/swarm-runtime/src/strategy.rs
  - crates/swarm-runtime/src/threat_intel_runtime.rs
autonomous: true
requirements: [ARENA-02, ARENA-06, ARENA-08]

must_haves:
  truths:
    - "The three remaining second-half SwarmConfig sites use the one canonical ArenaConfig default and retain service, strategy, and threat-intelligence behavior."
    - "The second-half helper and final AST scanner own these paths exactly once and reject alternate default values."
  artifacts:
    - path: "crates/swarm-runtime/src/service/tests_support.rs"
      provides: "service support migration"
    - path: "crates/swarm-runtime/src/strategy.rs"
      provides: "strategy migration"
    - path: "crates/swarm-runtime/src/threat_intel_runtime.rs"
      provides: "threat-intelligence migration"
---

<objective>Finish the second-half config migration ownership split without duplicating defaults.</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Migrate service, strategy, and threat-intel literals</name>
  <files>crates/swarm-runtime/src/service/tests_support.rs, crates/swarm-runtime/src/strategy.rs, crates/swarm-runtime/src/threat_intel_runtime.rs</files>
  <action>Write mutation tests first, then add the canonical disabled-by-default arena field/helper to every owned direct or loader-owned SwarmConfig site. Preserve explicit runtime/policy values and signed bytes. The Plan 00D helper (owned by 00D) must fail for omission, alternate defaults, path ownership drift, and changed literal values; 00E remains the independent combined-tree oracle. This supplementary plan owns no scanner/helper file and must not duplicate the 00D tool path.</action>
  <verify><automated>bash tools/check-arena-config-literals-second-half.sh --self-test && cargo fmt --all -- --check && git diff --check</automated></verify>
  <done>All three supplementary second-half paths are migrated and the final inventory can prove the complete 23-path ownership.</done>
</task>
</tasks>

<verification>Run the 00D helper and final 00E scanner after both migration halves are present.</verification>
<success_criteria>Every owned literal has one canonical arena default and no alternate constructor.</success_criteria>
<output>Create a concise supplementary migration summary.</output>
