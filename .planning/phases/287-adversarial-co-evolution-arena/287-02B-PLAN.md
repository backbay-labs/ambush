---
phase: 287-adversarial-co-evolution-arena
plan: 02B
type: execute
wave: 11
depends_on: ["287-02"]
files_modified:
  - crates/swarm-ingest-runtime/tests/arena_virtual_clock.rs
autonomous: true
requirements: [ARENA-02, ARENA-03, ARENA-08]

must_haves:
  truths:
    - "The virtual-clock integration test proves raw fixture normalization, injected detector/stack identity, deterministic logical time, and disabled-first refusal."
  artifacts:
    - path: "crates/swarm-ingest-runtime/tests/arena_virtual_clock.rs"
      provides: "real ingest seam and clock/control negative tests"
---

<objective>Own the injected virtual-clock integration test split from the arena ingest implementation plan.</objective>

<ownership_boundary>
Plan 02B runs strictly after 287-02 and owns only the ingest-runtime
integration test file. It does not add a second ingest seam, clock, parser,
fixture resolver, or runtime stack; all implementation remains owned by Plan
02 and all tests use the exact public injected API.
</ownership_boundary>

<tasks>
<task type="auto" tdd="true">
  <name>Task 287-02B-01: Prove real virtual-clock ingest and injection boundaries</name>
  <files>crates/swarm-ingest-runtime/tests/arena_virtual_clock.rs</files>
  <read_first>
    - `crates/swarm-ingest-runtime/src/ingest/mod.rs` and `crates/swarm-arena/src/{clock.rs,fixture.rs,blue.rs}` from Plan 02.
    - `crates/swarm-runtime/tests/phase286_bridge_handoff.rs` and the 06B compile contract for one-shot capture/cardinality.
    - `287-CONTEXT.md` and `287-VALIDATION.md` for disabled-first, no-action, and deterministic clock requirements.
  </read_first>
  <behavior>
    - Test 1: `arena_virtual_clock_replays_identically` proves identical raw fixture bytes, GraphClock input, six-argument Phase 286 stack configuration, and signer produce identical normalized/capture lineage bytes.
    - Test 2: `arena_virtual_clock_uses_injected_stack_and_phase286_worker` proves distinct detector/stack spies are used, the fixed IngestState stack is not used, and an actionable event produces exactly one existing coordinator submit, worker bridge capture, and Phase 286 capture.
    - Test 3: `arena_disabled_refuses_before_construction` proves ArenaConfig refusal precedes target, detector, stack, worker, authority, and ingest construction, while no-action produces zero authority calls.
  </behavior>
  <action>Write failing integration tests first. Use raw fixture bytes through the one `normalize_event` Result contract, replay with identical and perturbed GraphLogicalTime, install distinct detector/stack spies, and assert the fixed IngestState stack is never used. Treat fixed-stack substitution, clock drift, duplicate submit, and post-refusal construction as explicit mutation cases that fail. Prove default-disabled `ArenaConfig::require_enabled()` happens before target, detector, stack, or ingest construction. Assert the single-event Phase286StackResult has actionable capture or no-action DTO, with no replay rerun and event-local invocation deltas.</action>
  <verify><automated>bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-02B-01 --expect arena_virtual_clock_replays_identically -- cargo test -p swarm-ingest-runtime --test arena_virtual_clock --locked --offline -- --exact arena_virtual_clock_replays_identically --nocapture --test-threads=1 && bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-02B-01 --expect arena_virtual_clock_uses_injected_stack_and_phase286_worker -- cargo test -p swarm-ingest-runtime --test arena_virtual_clock --locked --offline -- --exact arena_virtual_clock_uses_injected_stack_and_phase286_worker --nocapture --test-threads=1 && bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-02B-01 --expect arena_disabled_refuses_before_construction -- cargo test -p swarm-ingest-runtime --test arena_virtual_clock --locked --offline -- --exact arena_disabled_refuses_before_construction --nocapture --test-threads=1 && git diff --check</automated></verify>
  <acceptance_criteria>
    - The three registry-bound helper invocations for `arena_virtual_clock_replays_identically`, `arena_virtual_clock_uses_injected_stack_and_phase286_worker`, and `arena_disabled_refuses_before_construction` each execute exactly one named test, pass with a positive count, and report `ignored == 0`.
    - Identical raw fixture bytes/clock produce identical normalized digest and invocation deltas; clock perturbation changes only declared logical time; a distinct injected detector/stack is observed and the fixed stack is not called.
    - Default-disabled construction fails before target/detector/stack/ingest calls, and actionable/no-action results are mutually exclusive with no replay rerun.
  </acceptance_criteria>
  <done>The real injected ingest path and its negative controls are independently proven.</done>
</task>
</tasks>

<verification>Run the exact arena virtual-clock test with locked offline dependencies and no generated `__pycache__`/temporary artifacts.</verification>
<success_criteria>Clock perturbation changes only declared logical-time fields and default-disabled runs perform zero downstream calls.</success_criteria>
<output>Create the virtual-clock seam summary.</output>
