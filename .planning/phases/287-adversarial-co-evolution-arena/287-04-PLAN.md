---
phase: 287-adversarial-co-evolution-arena
plan: 04
type: execute
wave: 13
depends_on: ["287-03"]
files_modified:
  - crates/swarm-arena/src/lib.rs
  - crates/swarm-arena/src/runner.rs
  - crates/swarm-arena/tests/adversarial_coevolution_arena.rs
  - crates/swarm-arena/tests/arena_ingest_capture_handoff.rs
  - crates/swarm-arena/tests/arena_teardown.rs
autonomous: true
requirements: [ARENA-02, ARENA-03, ARENA-04, ARENA-05, ARENA-07, ARENA-08]

must_haves:
  truths:
    - "The arena executes an actual immutable-seed -> real Blue -> measured BlueOutcome -> causal Red mutation -> bounded child loop; static replay or agent-count changes alone cannot produce a surviving child."
    - "Three consecutive fixed-seed runs each find at least one previously unseen normalized evasion fingerprint, while paired learning/control medians meet containment-time, blast-radius, baseline-improvement, and withheld-generalization thresholds."
    - "The arena stops on the first generation/budget/virtual-time/plateau/coverage/partition bound and records it; no live target, enforced response, promotion, or withheld feedback is reachable."
    - "Repeated runs produce byte-identical decisions, lineage, scores, and signed report content, and teardown proves no run-owned files, sockets, child tasks, or mutable withheld handles leak."
    - "A configured run root contains every arena store/artifact/temporary handle; cleanup is ownership-scoped and the clean-run-root/worktree control fails if any unowned residue remains."
    - "Stop precedence is deterministic: hard safety/authority refusal, partition/withheld boundary, generation, event, work, virtual-time, mutation, plateau, coverage, then completed; no later mutation or feedback occurs after the first reason."
  artifacts:
    - path: "crates/swarm-arena/src/runner.rs"
      provides: "bounded adaptive Red/Blue co-evolution loop and first-stop orchestration"
    - path: "crates/swarm-arena/tests/adversarial_coevolution_arena.rs"
      provides: "real-runtime adaptive integration and ARENA-07 acceptance assertions"
    - path: "crates/swarm-arena/tests/arena_ingest_capture_handoff.rs"
      provides: "single-source typed Phase 286 capture handoff integration proof"
    - path: "crates/swarm-arena/tests/arena_teardown.rs"
      provides: "determinism, tamper/replay, bound, and resource cleanup controls"
  key_links:
    - from: "crates/swarm-arena/src/runner.rs"
      to: "swarm_arena_red::mutation and swarm_arena::blue"
      via: "only the signed RedOutcomeProjection derived from a persisted BlueOutcome reaches Red mutation"
      pattern: "red_projection|outcome_digest|mutat|BlueRuntimeAdapter"
    - from: "crates/swarm-arena/src/runner.rs"
      to: "crates/swarm-arena/src/evaluation.rs"
      via: "paired control, bounded BlueLearnedState, and partition-separated scoring"
      pattern: "single_agent|learned_state|withheld|threshold|PairDigest"
    - from: "crates/swarm-arena/tests/adversarial_coevolution_arena.rs"
      to: "crates/swarm-runtime/src/service/stack.rs"
      via: "observable exact ingest -> runtime -> graph-enabled -> policy -> rehearsal traversal assertions"
      pattern: "process_bridge_event_at|process_event_with_phase286_capture|Phase286InvestigationCapture|ConfiguredRuntimeStack|policy|Simulated|dispatcher|graph"
    - from: "crates/swarm-arena/tests/arena_teardown.rs"
      to: "crates/swarm-arena/src/artifacts.rs"
      via: "byte-identical signed artifact CAS/fence replay and run-root cleanup assertions"
      pattern: "ArenaArtifactStore|fence|tamper|sequence|cleanup|identical"
---

<objective>
Implement and exercise the complete bounded adaptive arena loop, then prove its acceptance metrics, determinism, and teardown behavior.

Purpose: this is the phase's actual intelligence claim. It must show measured Red adaptation and Blue learning through the real runtime, not static replay, response theater, or a larger worker count.
Output: one bounded runner, three seeded acceptance runs, paired controls, unseen-evasion evidence, and adversarial cleanup/determinism tests.
</objective>

<ownership_boundary>
Plan 04 runs strictly after Plan 03 and is the only owner of the adaptive
runner and its three integration/teardown tests. It may extend the shared
Arena crate root only serially after Plan 00/02 exports exist; it does not
rebuild graph/planner/memory stores or create a second runtime, baseline, or
artifact store. Every scoreable result must come from the real Blue path and
the sealed partitions; no synthetic, cloned, digest-only, or role-count output
may be promoted to evidence.
</ownership_boundary>

<future_references>
The runner consumes the exact Phase 286 06B handoff and the Plan 03 learning /
artifact APIs. These are expected future references until their compile and
runtime contracts pass. A missing capture, changed cardinality, or absent
real-runtime trace is a hard stop, not a reason to fabricate a BlueOutcome or
fall back to static replay.
</future_references>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/287-adversarial-co-evolution-arena/287-CONTEXT.md
@.planning/phases/287-adversarial-co-evolution-arena/287-RESEARCH.md
@.planning/phases/287-adversarial-co-evolution-arena/287-VALIDATION.md
@.planning/phases/287-adversarial-co-evolution-arena/287-01-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-02-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-03-PLAN.md
@.planning/phases/286-collective-hypothesis-graph/286-04-PLAN.md
@.planning/phases/286-collective-hypothesis-graph/286-06-PLAN.md
@.planning/phases/286-collective-hypothesis-graph/286-07-PLAN.md

<interfaces>
From prior plans:
```rust
Red campaign -> materialize() -> Vec<TelemetryEvent>
BlueRuntimeAdapter::run(
    campaign: &Campaign,
    learned_state: &impl BlueLearnedStateInput,
    target: &FixtureTarget,
    partition: PartitionKind,
    clock: &mut VirtualArenaClock,
) -> Result<BlueOutcome, BlueRuntimeError>
BlueOutcome::red_projection()
    -> RedOutcomeProjection
RedMutation::survive(parent, RedOutcomeProjection, ordinal)
    -> RedMutationDecision + validated child Campaign | StopReason
CandidateSynthesizer::from_outcome(BlueOutcome) -> Vec<Candidate>
BlueLearnedState::admit_candidate(...) -> bounded learned candidate projection
Evaluator::compare_learning_and_single_agent(ControlInput) -> PairReport
```

`BlueLearnedStateInput` is the Plan 02 composition contract. Plan 03's
`SignedBlueLearnedState` is the concrete bounded implementation passed to this
generic method; the empty/frozen control and the direct outcome-change proof
remain owned by Plan 03, so this runner has no forward dependency on its
implementation file.

`ArenaIngestResult` is the single handoff boundary into this plan:
```rust
pub struct BlueOutcome {
    pub phase286_capture: Option<
        swarm_runtime::hypothesis_graph::service::Phase286InvestigationCapture,
    >,
    pub no_action: Option<swarm_runtime::service::NoActionFindingReplay>,
    pub policy_receipt: Option<swarm_runtime::service::DryRunTraversalReceipt>,
    // campaign, findings, policy, preview, rollback, metrics, ...
}
impl BlueOutcome {
    pub fn from_ingest_result(
        ingest: swarm_ingest_runtime::ingest::ArenaIngestResult,
        policy_receipt: Option<swarm_runtime::service::DryRunTraversalReceipt>,
    ) -> Result<Self, BlueRuntimeError>;
}
```
`from_ingest_result` moves (not clones from a side channel) the one
`ingest.phase286_capture: Option<Phase286InvestigationCapture>` and the legal
`ingest.no_action: Option<NoActionFindingReplay>` into the outcome. It rejects
a `None` capture for a finding-bearing scoreable/actionable event, but retains
the explicit no-action DTO for the legal no-action finding case. It never calls
`Phase286StrategyBridge::investigate_once`, `HypothesisGraphCoordinator`, or
`ContainmentPlanner`; those have already run once inside
`process_bridge_event_at`. The compile/integration test
`arena_ingest_capture_is_single_source` compares the capture graph digest,
investigation outcome, decision, simulation, and invocation counters in the
ingest result and Blue outcome, and asserts exactly one detector, coordinator,
planner, request builder, policy, receipt, dispatcher, recording response-adapter,
and rehearsal traversal.
`policy_receipt` is `Some(_)` exactly when the single `request_builder` returned
an action and the single rehearsal traversal completed; it is `None` for the
legal no-action finding case, which retains `no_action` evidence and performs no policy,
receipt, dispatcher, or response-adapter traversal. A scoreable actionable
outcome may not carry `Some(action)` without `Some(policy_receipt)`.
Replacing the typed field with a rerun, digest-only copy, optional second
replay, or `audit_authorize_and_execute` duplicate is a mutation failure.

The Phase 286 coordinator remains the graph/planning implementation. The
runner consumes only the public `Phase286InvestigationCapture` produced by the
post-Plan-06B one-shot bridge/stack handoff; it never invokes
`HypothesisGraphCoordinator::investigate` or `ContainmentPlanner::simulate`
independently. The existing fenced graph/task/memory stores remain Phase 286
owned. Arena artifacts use the Arena-owned
`ArenaArtifactStore`; there is no Phase 286 artifact-store dependency. The
runner may not rebuild graph nodes, replace the coordinator with
`SummaryInvestigator`, or promote a candidate.

The runner must call `SwarmConfig::arena.require_enabled()` before it creates
`CoEvolutionArena`, `BlueRuntimeAdapter`, `IngestState`, a target resolver, a
detector, or a configured runtime stack. A default-disabled config is a typed
refusal with zero runner/ingest construction or downstream calls. The only
configured investigation strategy passed to the Blue stack is the Phase 286
owned `swarm_runtime::hypothesis_graph::service::Phase286StrategyBridge`,
which implements the existing `swarm_runtime::investigation::InvestigationStrategy`
trait; `SummaryInvestigator` is not a legal fallback.

`ArenaRunConfig::run_root` is required and is the only root for artifact,
learned-state, partition-copy, task, and temporary-resource paths. The runner
records a run-root ownership token, refuses a non-empty/unowned root, and
returns a cleanup report. The final gate checks that exact root is empty and
that no process/socket/task remains; it never runs broad `git clean` or deletes
the repository worktree.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 287-04-01: Orchestrate the bounded adaptive Red/Blue runner</name>
  <files>crates/swarm-arena/src/lib.rs, crates/swarm-arena/src/runner.rs, crates/swarm-arena/tests/arena_ingest_capture_handoff.rs</files>
  <read_first>
    - `crates/swarm-arena/src/{contracts.rs,blue.rs,learning.rs,partitions.rs,evaluation.rs,artifacts.rs}` from Plans 02/03.
    - `crates/swarm-arena-red/src/{campaign.rs,scheduler.rs,mutation.rs}` for the only Red input/output boundary.
    - `crates/swarm-runtime/src/service/stack.rs`, Phase 286 bridge handoff tests, and `287-CONTEXT.md` for one-capture/runtime authority rules.
    - The empty `crates/swarm-arena/tests/arena_ingest_capture_handoff.rs` target path before creating the single-capture integration contract.
  </read_first>
  <behavior>
    - Test 1: A seeded run starts from an immutable catalogued campaign, invokes real Blue, records a signed BlueOutcome, and supplies that exact outcome digest to Red mutation before any child is accepted.
    - Test 2: The compile/integration contract `crates/swarm-arena/tests/arena_ingest_capture_handoff.rs::arena_ingest_capture_is_single_source` proves the exact typed `ArenaIngestResult.phase286_capture` is moved once into `BlueOutcome` and no coordinator/planner/bridge rerun or side-channel replay occurs.
    - Test 3: A measured escape/falsification changes a permitted child dimension and records causal lineage; a static replay vector, changed agent count, or absent outcome cannot produce a child.
    - Test 4: The first generation/event/work/virtual-time/mutation/plateau/coverage/partition bound terminates the loop with its explicit stop reason and no later mutation/feedback.
    - Test 5: `ArenaConfig::default().require_enabled()` refuses before `CoEvolutionArena`, target resolver, detector, configured stack, or `IngestState` construction; the disabled-run spy records zero downstream calls.
  </behavior>
  <action>Write the failing `arena_ingest_capture_is_single_source` integration test first in `crates/swarm-arena/tests/arena_ingest_capture_handoff.rs`, then implement `CoEvolutionArena::run` as a deterministic state machine: call `SwarmConfig::arena.require_enabled()` first and fail before runner, target, detector, stack, or ingest construction when disabled; then require `ArenaRunConfig::run_root`, acquire its ownership token, load/freeze corpus, detached signature, oracle registry, known-fingerprint set, signer, and limits; materialize one Red campaign; resolve its safe `FixtureTarget`; run Blue through `BlueRuntimeAdapter`; move the exact `ArenaIngestResult.phase286_capture` through `BlueOutcome::from_ingest_result` once; persist the full outcome in `ArenaArtifactStore`; synthesize evidence-only candidates; admit only bounded candidates to `BlueLearnedState`; derive `RedOutcomeProjection` and pass only that signed projection to Red mutation; validate the child; and repeat until the first declared stop condition. The one finding path has exactly one detector evaluation, one request-builder invocation, one Phase 286 coordinator/planner bridge capture, one `ArenaActionContext`, and one Phase 286 `BoundContainmentRequest`/authority traversal when actionable. Use the locked six-argument `ConfiguredRuntimeStack::from_graph_components(config, policy, response, bridge, authority, signer_binding)` with graph enabled, `RuntimeMode::DetectOnly`, `investigation.enabled=true`, and the configured real worker/queue; an actionable replay enters the existing coordinator through one `submit(&ReplayBundle)` and one worker bridge capture. Use Phase 286's public coordinator, authority, signer, containment, and store DTOs without duplicating them, and use the exact `Phase286StrategyBridge` as the existing runtime investigation strategy inside `ConfiguredRuntimeStack`; `SummaryInvestigator`, `new_without_worker`, direct bridge calls, or any static fallback is a construction/test failure. Use `ArenaArtifactStore` for arena artifacts with expected generation/predecessor/fence checks. Carry generation/seed/corpus/clock/scheduler/budget/run-root/target digests into every decision. Use one learning lane and one single-agent control lane over identical frozen common bytes; the learning lane may differ only by its signed learned-state projection, while the fixed Phase 286 worker count and queue scheduling are included in common inputs and are observational only, never verdict inputs. Apply stop precedence exactly: hard safety/authority refusal -> partition/withheld boundary -> generation -> event -> work -> virtual time -> mutation -> plateau -> coverage -> completed. Do not route withheld outcomes into mutation or learned state, do not call old replay/static Red helpers, do not call `audit_authorize_and_execute` or its instrumented duplicate, and do not invoke enforced response, external adapters, promotion, or canary. A stop decision is terminal and the runner must make no later mutation/feedback call.
  </action>
  <verify><automated>bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-04-01 --expect arena_ingest_capture_is_single_source -- cargo test -p swarm-arena --test arena_ingest_capture_handoff --locked --offline -- --exact arena_ingest_capture_is_single_source --test-threads=1 && bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-04-01 --expect arena_disabled_refuses_before_ingest -- cargo test -p swarm-arena --test adversarial_coevolution_arena --locked --offline -- --exact arena_disabled_refuses_before_ingest --test-threads=1 && bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-04-01 --expect adaptive_mutation_is_causal -- cargo test -p swarm-arena --test adversarial_coevolution_arena --locked --offline -- --exact adaptive_mutation_is_causal --test-threads=1</automated></verify>
  <acceptance_criteria>
    - The three exact runner/handoff tests execute once, pass with `--locked --offline`, and report `ignored == 0`.
    - A child is accepted only after the exact persisted BlueOutcome digest and authority-free RedOutcomeProjection cause a permitted timing/order/catalog mutation; static replay, event-ID/agent-count-only changes, missing outcome, synthetic/cloned outcome, and post-stop feedback fail closed.
    - Disabled `ArenaConfig` refuses before runner/target/detector/stack/ingest construction; the actionable path records one detector, request builder, Phase 286 capture, action context, policy/rehearsal traversal, and typed receipt, while no-action retains evidence with zero authority calls.
  </acceptance_criteria>
  <done>The runner is a real bounded feedback loop whose surviving mutations are causally linked to measured Blue outcomes and whose stop/lineage artifacts are deterministic and signed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 287-04-02: Prove real-runtime behavior, three-run unseen evasion, and all ARENA-07 thresholds</name>
  <files>crates/swarm-arena/tests/adversarial_coevolution_arena.rs</files>
  <read_first>
    - `crates/swarm-arena/src/runner.rs`, `blue.rs`, `evaluation.rs`, and sealed scenario partitions/baseline.
    - `crates/swarm-ingest-runtime/src/ingest/mod.rs`, `crates/swarm-runtime/src/service/stack.rs`, and Phase 286 bridge contract tests.
    - `287-CONTEXT.md`, `287-RESEARCH.md`, and `287-VALIDATION.md` for real-runtime trace, fingerprint, threshold, and withheld rules.
  </read_first>
  <behavior>
    - Test 1: `blue_uses_real_runtime` proves the exact injected ingest entry point, composite detection, `ConfiguredRuntimeStack` finding/replay evidence, graph-enabled Phase 286 coordinator, containment simulation, policy result, Pouncer/Tom/operator/receipt/dispatcher traversal, simulated sandbox rehearsal, and no direct-detector/static-replay substitution.
    - Test 2: `acceptance_metrics_meet_arena_07` runs the fixed seed set three consecutive times, requires at least one genuinely unseen normalized tactic/technique/primitive/order/timing fingerprint not in the frozen known set in each run, and asserts the integer 1,500 bp time, <=0 asset-impact median delta, 1,000 bp paired improvement, and <=500 bp withheld-loss thresholds with nonzero denominators.
    - Test 3: `single_agent_control_is_identical_stream` proves every paired digest matches before scoring and proves changing only worker/agent count cannot improve a result or alter verdict bytes.
  </behavior>
  <contract>The control invokes `BlueRuntimeAdapter::run(&campaign, &SignedBlueLearnedState::empty_frozen(), &target, partition, &mut clock)` through the same real path. The learning lane may differ only by a verified, bounded signed state; that state must alter an admitted detector profile or Phase 286 `InvestigationStrategyAdapter` graph-policy digest and a subsequent Blue outcome field beyond event IDs. Before arithmetic, assert `control_score_bp > 0`; the actual control score is the denominator, and zero/absent denominators are hard failures and mutation-test cases.</contract>
  <action>Drive the full runner on the sealed historical, benign, counterexample, and frozen withheld partitions using real generated fixture campaigns and the real Blue runtime; do not clone a learned outcome, precompute metrics, or substitute a mock detector. Assert the trace contains `process_bridge_event_at` normalization, composite detector observations, the six-argument `ConfiguredRuntimeStack::from_graph_components` with `GraphAuthorityHandoff` and `GraphSignerBinding`, one `submit(&ReplayBundle)`/worker capture, `SwarmConfig::hypothesis_graph.enabled=true`, `investigation.enabled=true`, Phase 286 graph IDs/hypothesis/falsification/containment-planning references, a content-derived `BoundContainmentRequest`, policy verdict, Pouncer/Tom/operator approval, bound receipt, dispatcher admission, simulated rehearsal, rollback, and blast-radius preview. Define unseen evasion only as normalized `tactic|technique|fixture_primitive|order_relation|timing_bucket` absent from the frozen known set and observed as an escape/falsification; generation-scoped event IDs, cloned outputs, worker counts, and agent counts are not fingerprints. Run exactly three consecutive fixed seeds and record each result; require one genuine measured unseen fingerprint per run and all five ARENA-07 thresholds using integer formulas: `time_improvement_bp = (control_median_virtual_ms - learned_median_virtual_ms) * 10_000 / control_median_virtual_ms >= 1_500`, `learned_median_asset_impact_units - control_median_asset_impact_units <= 0`, `learning_improvement_bp = (learned_score_bp - control_score_bp) * 10_000 / control_score_bp >= 1_000`, and `withheld_loss_bp = (in_sample_score_bp - withheld_score_bp) * 10_000 / in_sample_score_bp <= 500`, with checked arithmetic and nonzero denominators. Run the single-agent control through the same Blue path and identical frozen campaign/clock/config/graph/policy/signer/scheduler/target/partition/worker inputs with learning disabled. The common-input digest intentionally excludes both learned-state digest and projection; the learning lane receives a deliberately different signed bounded learned state, while the control receives `SignedBlueLearnedState::empty_frozen()`. Reject any pair whose common digest differs or whose learned-state digests are equal. Reject any mismatch before scoring and assert agent count is not a metric. Keep withheld evaluation after candidate/learned-state lineage freeze, emit no withheld `BlueOutcome` into Red or candidate selection, assert no Red mutation call receives withheld outcome data, and prove a changed measured Blue outcome changes the next bounded learned-state candidate set beyond event IDs.
  </action>
  <verify><automated>bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-04-02 --expect blue_uses_real_runtime -- cargo test -p swarm-arena --test adversarial_coevolution_arena --locked --offline -- --exact blue_uses_real_runtime --test-threads=1 && bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-04-02 --expect acceptance_metrics_meet_arena_07 -- cargo test -p swarm-arena --test adversarial_coevolution_arena --locked --offline -- --exact acceptance_metrics_meet_arena_07 --test-threads=1 && bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-04-02 --expect single_agent_control_is_identical_stream -- cargo test -p swarm-arena --test adversarial_coevolution_arena --locked --offline -- --exact single_agent_control_is_identical_stream --test-threads=1</automated></verify>
  <acceptance_criteria>
    - The three exact arena tests execute once, pass with `--locked --offline`, and report `ignored == 0`; all metric values are computed from live trace evidence and integer denominators.
    - Each of the three fixed seeds has a measured escape/falsification fingerprint absent from the frozen known set; event-ID, clone, worker-count, or role-count changes alone fail the unseen-evasion assertion.
    - Historical/benign/counterexample/withheld results remain separate, the single-agent control shares every frozen common-input digest and differs only in signed learned state, and withheld evaluation occurs after lineage freeze with no Red feedback.
  </acceptance_criteria>
  <done>Three seeded runs provide genuine measured unseen evasions and satisfy containment-time, blast-radius, baseline-improvement, and withheld-generalization gates using the real runtime and identical-stream control.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 287-04-03: Prove byte-identical replay, bounds, tamper refusal, and resource cleanup</name>
  <files>crates/swarm-arena/tests/arena_teardown.rs</files>
  <read_first>
    - `crates/swarm-arena/src/{runner.rs,artifacts.rs,learning.rs,evaluation.rs}` for ownership-scoped stores and canonical outputs.
    - `crates/swarm-core/src/signed_state.rs` and `crates/swarm-crypto/src/lib.rs` for tamper/replay checks.
    - `287-VALIDATION.md` for stop precedence, deterministic-byte, run-root, and cleanup requirements.
  </read_first>
  <behavior>
    - Test 1: `teardown_rejects_tamper_replay_and_leak` proves tampered signed artifacts, replayed sequence, changed catalog/withheld digest, stale store predecessor, missing cleanup ownership, leaked resources, and dirty run roots fail before scoring or mutation.
    - Test 2: `reproducible_artifacts` proves re-running fixed seed/catalog/config/signer/clock/store inputs produces byte-identical campaign decisions, BlueOutcome, mutation lineage, candidate IDs, pair digests, metrics, and final report; only explicitly excluded wall observations may differ.
    - Test 3: `bounded_teardown` proves generated event/virtual-time/work/generation/plateau/coverage bounds terminate; the run-owned temp root is removed and no child task, socket, process, mutable withheld handle, or external adapter remains. A wall watchdog is reported as guard-only.
  </behavior>
  <action>Build run-owned teardown probes around the real runner, `ArenaArtifactStore`, `BlueLearnedState`, and configured run-root APIs. Repeat the same run in fresh isolated roots and compare canonical report bytes and all signed lineage payloads; separately label non-gating wall observations. Force every budget/stop condition in the declared precedence order, tamper/replay each artifact stream, mutate withheld/index bytes, and exercise stale CAS/fence errors. Track only owned Tokio tasks, child processes, sockets, temporary files, partition handles, and run-root paths; await/abort only owned tasks, remove only the exact owned run root after verifying the ownership token, and assert cleanup before the test returns. Add a deliberately leaked-resource and dirty-run-root control that must fail the teardown oracle. The gate checks `git status --porcelain -- <configured-run-root>` is empty after cleanup but never runs broad recursive deletion, host-wide cleanup, `git clean`, or assumes the unrelated worktree is clean.
  </action>
  <verify><automated>bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-04-03 --expect teardown_rejects_tamper_replay_and_leak -- cargo test -p swarm-arena --test arena_teardown --locked --offline -- --exact teardown_rejects_tamper_replay_and_leak --test-threads=1 && bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-04-03 --expect reproducible_artifacts -- cargo test -p swarm-arena --test arena_teardown --locked --offline -- --exact reproducible_artifacts --test-threads=1 && bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-04-03 --expect bounded_teardown -- cargo test -p swarm-arena --test arena_teardown --locked --offline -- --exact bounded_teardown --test-threads=1</automated></verify>
  <acceptance_criteria>
    - The full teardown suite and its exact determinism/bounds tests pass once with `--locked --offline` and report `ignored == 0`.
    - Fresh owned roots with identical seed/corpus/config/signer/clock produce byte-identical canonical decisions, outcomes, lineage, pair metrics, and reports; only explicitly labeled guard observations may differ.
    - Every stop condition, tamper/replay/CAS/fence mutation, dirty-root control, leaked-resource control, and changed withheld/index mutation fails closed; cleanup removes only the exact ownership-token root and leaves no task/socket/process/handle.
  </acceptance_criteria>
  <done>Determinism, bounds, tamper/replay resistance, and run-owned cleanup are machine-proven; no state leakage or wall-clock path can silently change acceptance.</done>
</task>

</tasks>

<verification>
Run all exact real-runtime, causal-loop, three-run threshold, identical-control, determinism, and teardown tests twice. Then run Red/arena clippy, `cargo fmt --all -- --check`, `git diff --check`, the Wave 0 self-test, and all Phase 286 graph/store/planning tests. Inspect that no test accepts a changed event ID as an unseen evasion, no threshold uses wall time, no withheld outcome reaches mutation, and no candidate invokes promotion/live response.
</verification>

<success_criteria>
- The complete loop adapts from measured Blue evidence and never falls back to static replay or agent-count theater.
- Three consecutive seeded runs meet every ARENA-07 threshold with real runtime and identical-stream control evidence.
- Repeated artifacts are byte-identical, all bounds/negative controls fire, and teardown leaves no run-owned residue.
</success_criteria>

<output>After completion, create `.planning/phases/287-adversarial-co-evolution-arena/287-04-SUMMARY.md` with run seeds, exact metric denominators/values, real-runtime trace, deterministic byte comparisons, and teardown evidence.</output>
