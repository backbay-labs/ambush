---
phase: 287-adversarial-co-evolution-arena
plan: 00F
type: execute
wave: 4
depends_on: ["287-00", "287-00A"]
files_modified:
  - scenarios/adversarial-coevolution-arena/catalog.yaml
  - scenarios/adversarial-coevolution-arena/partitions.yaml
  - scenarios/adversarial-coevolution-arena/historical-attacks.yaml
  - scenarios/adversarial-coevolution-arena/benign-controls.yaml
  - scenarios/adversarial-coevolution-arena/counterexamples.yaml
  - scenarios/adversarial-coevolution-arena/withheld-campaigns.yaml
  - scenarios/adversarial-coevolution-arena/baseline.json
  - scenarios/adversarial-coevolution-arena/oracle-registry.json
autonomous: true
requirements: [ARENA-01, ARENA-05, ARENA-07, ARENA-08]

must_haves:
  truths:
    - "Catalog, four partitions, baseline, known-evasion fingerprints, corpus-oracle schema, and signatures are independently sealed outside production loaders; the sole execution registry is tools/arena-test-registry.json."
    - "Controls, counterexamples, and withheld campaigns are disjoint, synthetic, closed-target, and contain nonzero denominators with canonical threshold units."
  artifacts:
    - path: "scenarios/adversarial-coevolution-arena/catalog.yaml"
      provides: "closed tactic/technique/FixturePrimitive/FixtureTargetId catalog with canonical content digest"
    - path: "scenarios/adversarial-coevolution-arena/partitions.yaml"
      provides: "four-partition manifest with canonical membership and partition digests"
    - path: "scenarios/adversarial-coevolution-arena/historical-attacks.yaml"
      provides: "immutable historical attack corpus with entry and partition digests"
    - path: "scenarios/adversarial-coevolution-arena/benign-controls.yaml"
      provides: "immutable benign-control corpus with nonzero control denominators"
    - path: "scenarios/adversarial-coevolution-arena/counterexamples.yaml"
      provides: "immutable counterexample corpus with closed targets and safety labels"
    - path: "scenarios/adversarial-coevolution-arena/withheld-campaigns.yaml"
      provides: "disjoint generalization corpus never fed back as learning state"
    - path: "scenarios/adversarial-coevolution-arena/baseline.json"
      provides: "integer basis-point/asset-impact baseline formulas and nonzero denominators"
    - path: "scenarios/adversarial-coevolution-arena/oracle-registry.json"
      provides: "corpus-only fixture/partition/fingerprint/schema digest manifest; it does not authorize test execution"
  key_links:
    - from: "scenarios/adversarial-coevolution-arena/catalog.yaml"
      to: "scenarios/adversarial-coevolution-arena/oracle-registry.json"
      via: "canonical content and signature digests"
---

<objective>
Freeze the arena corpus and independent oracle inputs before runtime behavior exists.
</objective>

<ownership_boundary>
Plan 00F is the sole owner of these eight checked-in truth files and their
canonical bytes, detached-signature metadata, fingerprint set, threshold
formula, and corpus `oracle-registry.json` digest. Plan 00I owns the independent authoring-time
verifier. Plan 00B may independently parse and mutation-test the same inputs
but must not author, regenerate, or copy any truth file; Plan 00E may
independently verify their paths/digests but owns only its scanner tools.
The canonical execution test registry is instead owned only by Plan 00J at
`tools/arena-test-registry.json`; this corpus plan must not duplicate its
test-name/count records.
</ownership_boundary>

<truth_boundary>
The files are authored once from the reviewed scenario specification and are
never generated from production output, Red mutations, Blue outcomes, or
worker roles. Their detached signature metadata authenticates the canonical
manifest/index claim; it is not a claim that the YAML bytes are themselves a
live cryptographic signature. Plan 00I is the authoring-time gate immediately
after this plan, and Plan 00B is the first runtime consumer; each must fail
closed on every mutation before any runtime loader is trusted.
</truth_boundary>

<tasks>
<task type="auto" tdd="false">
  <name>Task 287-00F-01: Author strict catalog, partitions, baseline, and registry</name>
  <files>scenarios/adversarial-coevolution-arena/catalog.yaml, scenarios/adversarial-coevolution-arena/partitions.yaml, scenarios/adversarial-coevolution-arena/historical-attacks.yaml, scenarios/adversarial-coevolution-arena/benign-controls.yaml, scenarios/adversarial-coevolution-arena/counterexamples.yaml, scenarios/adversarial-coevolution-arena/withheld-campaigns.yaml, scenarios/adversarial-coevolution-arena/baseline.json, scenarios/adversarial-coevolution-arena/oracle-registry.json</files>
  <read_first>
    - `287-CONTEXT.md`, `287-RESEARCH.md`, and the ARENA-01/05/07/08 requirements before authoring any truth value.
    - `rulesets/evasion/attack-technique-catalog.yaml` and the existing scenario schema for tactic/technique vocabulary; do not copy arbitrary command or target fields.
    - `crates/swarm-core/src/signed_state.rs` and `crates/swarm-crypto/src/lib.rs` for detached signature metadata and canonical digest conventions.
  </read_first>
  <acceptance_criteria>
    - All eight listed truth files exist, are nonempty, use the closed schema, and have canonical content/partition/fingerprint/corpus-registry digests with nonzero denominators and integer basis-point thresholds.
    - Plan 00I's independent verifier and Plan 00B's runtime corpus oracle reject deletion, duplication, threshold/digest/fingerprint/partition/corpus-registry, unknown-field, and withheld-leak mutations before runtime loading.
    - `git diff --check` exits 0 and no production source, signed ruleset, or generated Blue/Red output is used to author the truth files.
  </acceptance_criteria>
  <action>Author deny-unknown-fields YAML/JSON truth in exactly the eight listed files, with closed tactic/technique/primitive/FixtureTargetId enums, bounded stage/order/timing values, disjoint content-addressed partitions, frozen `known_evasion_fingerprints` and digest, exact denominator/threshold formulas, and a separately sealed corpus-only oracle manifest. The oracle manifest may record corpus schema/content/fixture/partition/fingerprint/signature digests and owner metadata, but must not repeat execution test names or expected counts; those belong only to `tools/arena-test-registry.json` owned by Plan 00J. Include detached signature metadata without pretending to sign live data. Forbid paths, URLs, IPs, sockets, processes, commands, external targets, neutral/mixed labels, and withheld leakage. Do not implement a verifier or generate any truth file from production output: Plan 00I independently validates these eight bytes, while Plan 00B remains the independent runtime corpus-oracle consumer and Plan 00E independently checks the complete config inventory. None may repair these truth files.
  </action>
  <verify><automated>test -s scenarios/adversarial-coevolution-arena/catalog.yaml && test -s scenarios/adversarial-coevolution-arena/partitions.yaml && test -s scenarios/adversarial-coevolution-arena/historical-attacks.yaml && test -s scenarios/adversarial-coevolution-arena/benign-controls.yaml && test -s scenarios/adversarial-coevolution-arena/counterexamples.yaml && test -s scenarios/adversarial-coevolution-arena/withheld-campaigns.yaml && test -s scenarios/adversarial-coevolution-arena/baseline.json && test -s scenarios/adversarial-coevolution-arena/oracle-registry.json && test "$(find scenarios/adversarial-coevolution-arena -maxdepth 1 -type f -print | wc -l | tr -d ' ')" = 8 && git diff --check</automated></verify>
  <done>Independent corpus inputs are frozen, safe, disjoint, value-addressed, and ready for the separate oracle parser.</done>
</task>
</tasks>

<verification>Plan 00F's check is a narrowly exempt authoring/ownership check: it proves that exactly the eight declared truth paths are nonempty and changed only by this authoring task, but it does not claim schema, digest, partition, denominator, or mutation validity. Plan 00I is a mandatory independent gate immediately after this plan and must run `python3 tools/check-arena-corpus-truth.py --self-test` and `--check` before Plan 00B or any runtime loader proceeds; Plan 00F may not be marked preflight-green from file existence alone. Then run Plan 00B's parser/self-tests and Plan 00E's combined inventory after migrations; never make production loaders the source of truth.</verification>

<success_criteria>
- The corpus `oracle-registry.json` and fingerprint set are independent of implementation source; positive execution names/counts remain solely in `tools/arena-test-registry.json`.
- The authoring task is not the corpus seal: Plan 00I's independent schema/digest/mutation gate is mandatory and owns the only seal verdict.
- Withheld data cannot enter learned state or scoring inputs.
</success_criteria>

<output>Create a corpus sealing summary with all digests and partition ownership.</output>
