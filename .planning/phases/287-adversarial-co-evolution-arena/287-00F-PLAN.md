---
phase: 287-adversarial-co-evolution-arena
plan: 00F
type: execute
wave: 3
depends_on: ["287-00", "287-00A"]
files_modified:
  - scenarios/adversarial-coevolution-arena/catalog.yaml
  - scenarios/adversarial-coevolution-arena/partitions.yaml
  - scenarios/adversarial-coevolution-arena/historical-attacks.yaml
  - scenarios/adversarial-coevolution-arena/benign-controls.yaml
  - scenarios/adversarial-coevolution-arena/counterexamples.yaml
  - scenarios/adversarial-coevolution-arena/withheld-campaigns.yaml
  - scenarios/adversarial-coevolution-arena/baseline.json
  - scenarios/adversarial-coevolution-arena/oracle-registry.json
autonomous: true
requirements: [ARENA-01, ARENA-05, ARENA-07, ARENA-08]

must_haves:
  truths:
    - "Catalog, four partitions, baseline, known-evasion fingerprints, exact test registry, and signatures are independently sealed outside production loaders."
    - "Controls, counterexamples, and withheld campaigns are disjoint, synthetic, closed-target, and contain nonzero denominators with canonical threshold units."
  artifacts:
    - path: "scenarios/adversarial-coevolution-arena/oracle-registry.json"
      provides: "separate exact test/fixture/partition/fingerprint registry and digest"
    - path: "scenarios/adversarial-coevolution-arena/withheld-campaigns.yaml"
      provides: "disjoint generalization corpus never fed back as learning state"
  key_links:
    - from: "scenarios/adversarial-coevolution-arena/catalog.yaml"
      to: "scenarios/adversarial-coevolution-arena/oracle-registry.json"
      via: "canonical content and signature digests"
---

<objective>
Freeze the arena corpus and independent oracle inputs before runtime behavior exists.
</objective>

<ownership_boundary>
Plan 00F is the sole owner of these eight checked-in truth files and their
canonical bytes, detached-signature metadata, fingerprint set, threshold
formula, and registry digest. Plan 00B may parse and mutation-test them but
must not author, regenerate, or copy any truth file; Plan 00E may independently
verify their paths/digests but owns only its scanner tools.
</ownership_boundary>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Author strict catalog, partitions, baseline, and registry</name>
  <files>scenarios/adversarial-coevolution-arena/catalog.yaml, scenarios/adversarial-coevolution-arena/partitions.yaml, scenarios/adversarial-coevolution-arena/historical-attacks.yaml, scenarios/adversarial-coevolution-arena/benign-controls.yaml, scenarios/adversarial-coevolution-arena/counterexamples.yaml, scenarios/adversarial-coevolution-arena/withheld-campaigns.yaml, scenarios/adversarial-coevolution-arena/baseline.json, scenarios/adversarial-coevolution-arena/oracle-registry.json</files>
  <action>Author deny-unknown-fields YAML/JSON truth with closed tactic/technique/primitive/FixtureTargetId enums, bounded stage/order/timing values, disjoint content-addressed partitions, frozen `known_evasion_fingerprints` and digest, exact denominator/threshold formulas, and a separately sealed exact test registry. Include detached signature metadata without pretending to sign live data. Forbid paths, URLs, IPs, sockets, processes, commands, external targets, neutral/mixed labels, and withheld leakage. Mutation vectors delete/duplicate/alter entries, defaults, thresholds, digests, fingerprints, partitions, or registry names and must fail in Plan 00B/00E.
  </action>
  <verify><automated>python3 -c 'import pathlib; assert all(pathlib.Path(p).exists() for p in ["scenarios/adversarial-coevolution-arena/catalog.yaml","scenarios/adversarial-coevolution-arena/oracle-registry.json"])' && git diff --check</automated></verify>
  <done>Independent corpus inputs are frozen, safe, disjoint, value-addressed, and ready for the separate oracle parser.</done>
</task>
</tasks>

<verification>Run the Plan 00B parser/self-tests and Plan 00E combined inventory after migrations; never make production loaders the source of truth.</verification>

<success_criteria>
- The registry and fingerprint set are independent of implementation source.
- Withheld data cannot enter learned state or scoring inputs.
</success_criteria>

<output>Create a corpus sealing summary with all digests and partition ownership.</output>
