---
phase: 287-adversarial-co-evolution-arena
plan: 00B
type: execute
wave: 4
depends_on: ["287-00", "287-00A", "287-00F"]
files_modified:
  - crates/swarm-arena-red/tests/arena_red_isolation.rs
  - crates/swarm-arena-red/tests/ui/forbidden_response_import.rs
  - crates/swarm-arena-red/tests/ui/forbidden_response_import.stderr
  - crates/swarm-arena/tests/arena_corpus_oracles.rs
  - tools/check-arena-oracles.sh
autonomous: true
requirements: [ARENA-01, ARENA-05, ARENA-06, ARENA-07, ARENA-08]

must_haves:
  truths:
    - "Wave 0 preflight truth is independently sealed by Plan 00F; this plan only parses/verifies the catalog, four partitions, baseline, oracle registry, exact test registry, and all content/signature digests outside production loaders."
    - "The Red normal dependency closure and source inventory reject every authority type reachable through `swarm-core`, and a deliberately broken consumer-style import fails while the clean fixture passes."
    - "The oracle rejects missing/duplicate partitions, changed digests, withheld leakage, removed thresholds, unknown fields, zero matched tests, ignored tests, wrong execution counts, missing artifacts, stubs/placeholders/no-op success, and unsafe targets."
    - "Fixture targets are closed enum mappings to run-owned synthetic fixtures; no catalog input can name a path, URL, IP, socket, process, shell, network, or external target."
  artifacts:
    - path: "crates/swarm-arena/tests/arena_corpus_oracles.rs"
      provides: "test-local strict parser/verifier and mutation-controlled corpus/threshold/target oracle for Plan 00F truth files"
    - path: "crates/swarm-arena-red/tests/arena_red_isolation.rs"
      provides: "resolved dependency/source inventory and non-vacuous Red authority boundary"
    - path: "tools/check-arena-oracles.sh"
      provides: "credential-free exact-count, ignored==0, digest, registry, and boundary self-test"
  key_links:
    - from: "scenarios/adversarial-coevolution-arena/oracle-registry.json"
      to: "tools/check-arena-oracles.sh"
      via: "independent parser verifies Plan 00F's registry digest and expected named-test count"
      pattern: "registry_digest|expected_test_count|ignored_count"
    - from: "crates/swarm-arena/tests/arena_corpus_oracles.rs"
      to: "scenarios/adversarial-coevolution-arena/{catalog,partitions,baseline}.yaml/json"
      via: "test-local deny_unknown_fields parser, canonical bytes, and pinned digest checks"
      pattern: "deny_unknown_fields|partition_digest|known_evasion_fingerprint"
    - from: "crates/swarm-arena-red/tests/arena_red_isolation.rs"
      to: "swarm-core authority symbols"
      via: "resolved normal closure and source scan plus trybuild clean/broken controls"
      pattern: "ActionRequest|ResponseAction|Approval|Lease|Receipt|Dispatcher|Executor|Governance"
---

<objective>
Install the independent Wave 0 preflight oracle, immutable corpus, and Red
isolation boundary. This is separate from Plan 00 contract ownership and must
remain unchanged while Red/Blue behavior is implemented.

The plan intentionally has fewer than fifteen owned files. It must not add a
second production parser, replay harness, artifact store, or authority path.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/287-adversarial-co-evolution-arena/287-CONTEXT.md
@.planning/phases/287-adversarial-co-evolution-arena/287-RESEARCH.md
@.planning/phases/287-adversarial-co-evolution-arena/287-VALIDATION.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00-PLAN.md
@crates/swarm-core/src/signed_state.rs
@tools/check-workspace-layering.sh
@tools/check-gates-wired.sh
</context>

<interfaces>
The catalog maps finite `TacticId`/`TechniqueId` pairs to a closed
`FixturePrimitive` and closed `FixtureTargetId`. The only runtime target
constructor permitted later is `swarm_arena::fixture::FixtureTarget::from_id`,
which returns an in-memory/run-owned synthetic target and rejects every unknown
ID. No fixture target is a path or network address.

The partition manifest carries canonical entry/content/partition digests plus a
detached signature envelope (`state_kind=arena.partition.index`, signer key
ID, sequence, signature). The YAML is not itself claimed to be a signature;
the arena admission verifier checks the envelope and canonical bytes. This
keeps the static oracle's digest claim consistent with the runtime signature
claim.

The baseline fixes integer units and arithmetic:

```text
time_improvement_bp = (control_median_virtual_ms - learned_median_virtual_ms)
                      * 10_000 / control_median_virtual_ms; require control > 0 and >= 1_500 bp
blast_radius_delta_units = learned_median_asset_impact_units - control_median_asset_impact_units;
                           require <= 0 units
learning_improvement_bp = (learned_score_bp - control_score_bp)
                          * 10_000 / control_score_bp
require control_score_bp > 0 and learning_improvement_bp >= 1_000 bp
withheld_loss_bp = (in_sample_score_bp - withheld_score_bp)
                   * 10_000 / in_sample_score_bp; require in_sample > 0 and <= 500 bp
```

All division uses checked integer arithmetic with declared rounding (floor for
improvement, ceiling for loss). A zero or absent control/in-sample denominator
is a hard oracle failure; no fallback denominator is permitted. The
known-evasion set is frozen by digest and
uses `tactic_id|technique_id|fixture_primitive|order_relation|timing_bucket`;
event IDs, worker counts, and generation numbers are never fingerprints.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze catalog, partitions, baseline, fingerprint set, and oracle registry</name>
  <files>crates/swarm-arena/tests/arena_corpus_oracles.rs</files>
  <behavior>
    - Test 1: The strict test-local oracle loads all four named partitions, checks canonical IDs/content/partition digests, detached-signature metadata, disjoint membership, closed FixtureTarget mappings, fixed fingerprint-set digest, nonzero denominators, and exact integer thresholds.
    - Test 2: Removing/duplicating an entry or partition, changing any digest/signature/registry value, leaking a withheld ID, deleting a threshold/denominator/fingerprint, adding an unknown field, path alias, live target, neutral/mixed class, or raw command/URL/socket/process/file-write field returns a named failure.
    - Test 3: The corpus contains multi-stage catalogued fixtures and the oracle's own exact test registry is separately digest-checked; a renamed/zero-match/ignored test cannot satisfy the registry.
  </behavior>
  <action>Do not author or mutate the catalog, partition, baseline, fingerprint, or registry truth files here; Plan 00F owns those sealed inputs. Implement only the strict test-local parser/verifier and load those exact files through `deny_unknown_fields` structs and canonical bytes. Verify closed tactic/technique/primitive/FixtureTargetId mappings, bounded stage/order/timing values, disjoint content-addressed partitions, detached partition-signature metadata, frozen fingerprint digest, exact denominator/threshold units, and the separately sealed registry digest/counts. Ensure the parser never treats YAML as cryptographically signed, never creates a target, and never accepts a raw path/URL/IP/command/socket/process/file-write field. Add mutation cases for every listed failure and assert a nonzero exact test count. The shell helper performs only independent parsing/counting; it does not become a second truth owner.
  </action>
  <verify><automated>cargo test -p swarm-arena --test arena_corpus_oracles --locked --offline -- --exact corpus_manifest_is_strict && cargo test -p swarm-arena --test arena_corpus_oracles --locked --offline -- --exact corpus_oracle_rejects_mutation</automated></verify>
  <done>The independently parsed corpus, registry, fingerprint set, signature metadata, denominators, and threshold arithmetic are frozen and every declared mutation fails closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install non-vacuous Red isolation and exact-count shell self-tests</name>
  <files>crates/swarm-arena-red/tests/arena_red_isolation.rs, crates/swarm-arena-red/tests/ui/forbidden_response_import.rs, crates/swarm-arena-red/tests/ui/forbidden_response_import.stderr, tools/check-arena-oracles.sh</files>
  <behavior>
    - Test 1: A clean Red consumer fixture compiles with the declared capability-minimal closure; a broken fixture importing `swarm_response::SandboxExecutor`, `swarm_policy::ActionRequest`, or authority types reachable via `swarm-core` fails through trybuild/source-boundary validation.
    - Test 2: The shell self-test rejects zero matched tests, wrong exact count, `ignored > 0`, missing/extra report fields, corpus/registry/fingerprint/partition digest mutation, removed denominator/threshold, withheld leakage, authority import, wall-clock fitness, missing artifact, TODO/unimplemented/placeholder/no-op source, and absent arena directory; clean controls pass.
    - Test 3: Every deliberate mutation is created under one owned temporary root, observed failing, and removed by the trap without deleting a repository/worktree path.
  </behavior>
  <action>Use trybuild and a real miniature Cargo fixture with consumer-style imports; never let an absent directory or zero-match grep count as a passing isolation proof. Inventory the complete Red normal dependency closure with `cargo metadata --locked --offline`, reject response/policy/runtime/ingest/agents/network/process/filesystem/promotion authorities and symbols reachable through `swarm-core`, and scan for `todo!`, `unimplemented!`, `TODO`, `FIXME`, empty success branches, `#[ignore]`, or placeholder constructors. Implement `tools/check-arena-oracles.sh` as credential-free POSIX/Bash with a run-owned `mktemp -d`, exact registry-driven named-test matching/execution/passed/ignored counters, `ignored == 0`, independent digest/schema checks, and mutation controls. It may report that later behavior is not yet implemented, but missing behavior tests/artifacts must fail rather than be skipped. Do not run live targets, network, process execution, response adapters, promotion, or wall-clock scoring.
  </action>
  <verify><automated>bash -n tools/check-arena-oracles.sh && bash tools/check-arena-oracles.sh --self-test && cargo test -p swarm-arena-red --test arena_red_isolation --locked --offline -- --exact red_capability_boundary_is_nonvacuous && git diff --check</automated></verify>
  <done>Preflight has an independently sealed oracle registry, exact nonzero test/ignored-count controls, compiler-backed Red isolation, and mutation-tested failure behavior with no destructive cleanup.</done>
</task>

</tasks>

<verification>
Run both strict corpus mutations, Red compile-fail/clean fixtures, the shell self-test, offline metadata, format, and diff checks. Confirm the registry and all fixture/baseline/fingerprint digests are checked independently, no authority symbol reachable through `swarm-core` is in Red, and no test can pass because a target, directory, or test is absent.
</verification>

<success_criteria>
- Independent corpus/oracle truth and exact-test registry are sealed before behavior implementation.
- FixtureTarget mappings are closed, synthetic, run-owned, and incapable of naming live resources.
- Red isolation and shell gates fail closed on authorities, stubs, placeholders, ignored/zero tests, altered truth, or missing artifacts.
</success_criteria>

<output>After completion, create `.planning/phases/287-adversarial-co-evolution-arena/287-00B-SUMMARY.md` with corpus/registry/fingerprint/signature digests, exact counts, and mutation evidence. Do not mark execution waves complete; `287-VALIDATION.md` uses `wave_0_complete` only for this preflight bundle.</output>
