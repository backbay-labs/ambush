---
phase: 287-adversarial-co-evolution-arena
plan: 00G
type: execute
wave: 5
depends_on: ["287-00C"]
files_modified:
  - crates/swarm-runtime/src/mutation/test_support.rs
  - crates/swarm-runtime/src/replay/tests.rs
  - crates/swarm-runtime/src/selection.rs
  - tools/check-arena-config-literals.sh
autonomous: true
requirements: [ARENA-02, ARENA-06, ARENA-08]

must_haves:
  truths:
    - "The two remaining first-half SwarmConfig direct sites use the canonical ArenaConfig default and preserve all explicit replay/selection behavior."
    - "The first-half scanner and final AST inventory count these paths exactly once, including field values/default semantics."
  artifacts:
    - path: "tools/check-arena-config-literals.sh"
      provides: "serial extension of the scanner created by Plan 00C; it is never recreated or replaced"
    - path: "crates/swarm-runtime/src/replay/tests.rs"
      provides: "canonical arena field in replay fixtures"
    - path: "crates/swarm-runtime/src/selection.rs"
      provides: "canonical arena field in selection fixtures"
---

<objective>Finish the first-half config migration ownership split without creating a second default.</objective>

<ownership_boundary>
This plan runs strictly after 287-00C and is the only owner of the three
supplementary first-half source paths. It may extend the existing
`tools/check-arena-config-literals.sh` only in that serial wave; it must fail if
the helper is absent and must not recreate, replace, or duplicate it. It must
not edit any 00C-owned source path. Plan 00E consumes the resulting live
inventory after this plan completes.
</ownership_boundary>

<tasks>
<task type="auto" tdd="true">
  <name>Task 287-00G-01: Extend the scanner and migrate supplementary first-half literals</name>
  <files>crates/swarm-runtime/src/mutation/test_support.rs, crates/swarm-runtime/src/replay/tests.rs, crates/swarm-runtime/src/selection.rs, tools/check-arena-config-literals.sh</files>
  <read_first>
    - `crates/swarm-runtime/src/mutation/test_support.rs`, `crates/swarm-runtime/src/replay/tests.rs`, and `crates/swarm-runtime/src/selection.rs` with their current `SwarmConfig` expressions.
    - `tools/check-arena-config-literals.sh` and `287-00C-PLAN.md` for the first-half discovery/ownership contract.
    - `crates/swarm-core/src/config/defaults.rs` and `root.rs` for the canonical disabled ArenaConfig default.
  </read_first>
  <behavior>
    - Test 1: `first_half_config_literals_are_exactly_owned` discovers all three supplementary live paths and matches each direct `SwarmConfig` expression exactly once.
    - Test 2: `first_half_config_literal_mutations_fail_closed` rejects omission, alternate default, ownership drift, changed explicit value, and missing/extra path mutations.
  </behavior>
  <action>Write mutation tests first, assert that `tools/check-arena-config-literals.sh` already exists from Plan 00C, then extend that same helper's expected ownership partition and add the canonical disabled-by-default `arena: ArenaConfig::default()` field/helper to every live direct or loader-owned `SwarmConfig` site discovered in the three supplementary files. Preserve explicit values and signed bytes. The extension must fail for omission, alternate defaults, path ownership drift, changed literal values, or a missing/extra owned path, and Plan 00E's AST digest must change under any alternate-default mutation. Do not create a second scanner owner, replace the helper, or overwrite the seven-path 00C manifest.</action>
  <verify><automated>bash tools/check-arena-test-counts.sh --self-test && bash tools/check-arena-config-literals.sh --self-test && bash tools/check-arena-config-literals.sh && cargo fmt --all -- --check && git diff --check</automated></verify>
  <acceptance_criteria>
    - The first-half helper discovers and fingerprints both live paths exactly once, and its self-test fails for omission, alternate default, ownership drift, and changed literal mutations.
    - `cargo fmt --all -- --check` and `git diff --check` exit 0; no signed ruleset bytes change.
  </acceptance_criteria>
  <done>Both supplementary first-half paths are migrated and scanner ownership is complete.</done>
</task>
</tasks>

<verification>Run the 00C helper and final 00E scanner after both migration halves are present.</verification>
<success_criteria>Every owned literal has one canonical arena default and no alternate constructor.</success_criteria>
<output>Create a concise supplementary migration summary.</output>
