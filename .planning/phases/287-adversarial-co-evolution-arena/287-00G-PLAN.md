---
phase: 287-adversarial-co-evolution-arena
plan: 00G
type: execute
wave: 4
depends_on: ["287-00C"]
files_modified:
  - crates/swarm-runtime/src/replay/tests.rs
  - crates/swarm-runtime/src/selection.rs
  - tools/check-arena-config-literals.sh
autonomous: true
requirements: [ARENA-02, ARENA-06, ARENA-08]

must_haves:
  truths:
    - "The two remaining first-half SwarmConfig direct sites use the canonical ArenaConfig default and preserve all explicit replay/selection behavior."
    - "The first-half scanner and final AST inventory count these paths exactly once, including field values/default semantics."
  artifacts:
    - path: "crates/swarm-runtime/src/replay/tests.rs"
      provides: "canonical arena field in replay fixtures"
    - path: "crates/swarm-runtime/src/selection.rs"
      provides: "canonical arena field in selection fixtures"
---

<objective>Finish the first-half config migration ownership split without creating a second default.</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Migrate replay and selection literals</name>
  <files>crates/swarm-runtime/src/replay/tests.rs, crates/swarm-runtime/src/selection.rs, tools/check-arena-config-literals.sh</files>
  <action>Write mutation tests first, then add the canonical disabled-by-default arena field/helper to every direct or loader-owned SwarmConfig site in these two files. Preserve explicit values and signed bytes. Own the first-half scanner entry point here: it must fail for omission, alternate defaults, path ownership drift, changed literal values, or a missing/extra owned path; Plan 00E's AST digest must change under any alternate-default mutation.</action>
  <verify><automated>bash tools/check-arena-config-literals.sh --self-test && cargo fmt --all -- --check && git diff --check</automated></verify>
  <done>Both supplementary first-half paths are migrated and scanner ownership is complete.</done>
</task>
</tasks>

<verification>Run the 00C helper and final 00E scanner after both migration halves are present.</verification>
<success_criteria>Every owned literal has one canonical arena default and no alternate constructor.</success_criteria>
<output>Create a concise supplementary migration summary.</output>
