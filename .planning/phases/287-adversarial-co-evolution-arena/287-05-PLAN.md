---
phase: 287-adversarial-co-evolution-arena
plan: 05
type: execute
wave: 14
depends_on: ["287-04"]
files_modified:
  - tools/check-adversarial-coevolution-arena.sh
  - tools/parse-arena-report.py
  - tools/check-arena-isolation.sh
  - .github/workflows/ci.yml
  - .planning/phases/287-adversarial-co-evolution-arena/287-VALIDATION.md
  - artifacts/phase287/final-gate/arena-report.json
  - artifacts/phase287/final-gate/arena-lineage.json
  - artifacts/phase287/final-gate/final-gate-evidence.json
autonomous: true
requirements: [ARENA-01, ARENA-02, ARENA-03, ARENA-04, ARENA-05, ARENA-06, ARENA-07, ARENA-08]

must_haves:
  truths:
    - "One credential-free final gate runs the exact non-vacuous arena tests, validates report schema/denominators/thresholds, repeats deterministic output, and fails on every declared oracle, isolation, tamper, withheld, wall-clock, or cleanup mutation."
    - "CI executes the arena gate and isolation gate on the combined workspace with locked dependencies; a workflow banner, zero-test filter, isolated worktree, or local-only result cannot be reported as acceptance."
    - "Full workspace, layering, negative-registry, fixture-freshness, panic-contract, authority, and run-cleanup gates pass together, with no live target, enforced response, promotion, or external credential path."
    - "Validation records every task command and status truthfully; Wave 0 is marked complete only after its independent corpus/oracle/self-test/compile-fail artifacts pass on the final combined tree."
    - "The final report is parsed by an independent strict parser, not the arena implementation; positive exact-test execution uses only tools/arena-test-registry.json, with full task/package/target/name/count equality and ignored==0."
    - "The final independent review is a separate Plan 06 gate after this plan's combined-tree checks; verification is PASS only when open_p0, open_p1, and open_p2 are all zero on the combined tree."
    - "The configured per-run arena run_root and temporary capture roots are ephemeral and cleaned only within their ownership boundary; the three final-gate evidence files live in a disjoint immutable retained store and are never deleted, truncated, or replaced."
  artifacts:
    - path: "tools/check-adversarial-coevolution-arena.sh"
      provides: "exact-test-count, report-threshold, deterministic-repeat, mutation-controlled phase gate"
    - path: "tools/check-arena-isolation.sh"
      provides: "Red dependency/source boundary plus Blue policy/receipt/preview/live-action isolation gate"
    - path: ".github/workflows/ci.yml"
      provides: "real combined-tree CI invocation of arena/authority/full gates"
    - path: ".planning/phases/287-adversarial-co-evolution-arena/287-VALIDATION.md"
      provides: "truthful Nyquist task map and final evidence statuses"
    - path: "tools/parse-arena-report.py"
      provides: "independent strict ArenaReport schema, denominator, threshold, and observation parser"
    - path: "artifacts/phase287/final-gate/arena-report.json"
      provides: "retained immutable canonical final ArenaReport bytes consumed by Plan 06"
    - path: "artifacts/phase287/final-gate/arena-lineage.json"
      provides: "retained immutable canonical final signed lineage bytes consumed by Plan 06"
    - path: "artifacts/phase287/final-gate/final-gate-evidence.json"
      provides: "retained manifest containing explicit report/lineage paths and final_gate_evidence_digest"
  key_links:
    - from: "tools/check-adversarial-coevolution-arena.sh"
      to: "crates/swarm-arena/tests/adversarial_coevolution_arena.rs"
      via: "exact named test execution and nonzero count assertion"
      pattern: "acceptance_metrics_meet_arena_07|exact|count"
    - from: "tools/check-adversarial-coevolution-arena.sh"
      to: "tools/check-arena-isolation.sh"
      via: "structural/runtime boundary gate"
      pattern: "check-arena-isolation"
    - from: "tools/check-adversarial-coevolution-arena.sh"
      to: "tools/parse-arena-report.py"
      via: "independent strict parser, not a grep or production serializer"
      pattern: "parse-arena-report|ArenaReport|ignored_count"
    - from: "tools/check-adversarial-coevolution-arena.sh"
      to: "artifacts/phase287/final-gate/final-gate-evidence.json"
      via: "retains immutable report/lineage paths and computes final_gate_evidence_digest without deleting evidence"
      pattern: "final_gate_evidence_digest|arena-report.json|arena-lineage.json"
    - from: "tools/arena-test-registry.json"
      to: "tools/check-adversarial-coevolution-arena.sh"
      via: "sole canonical execution registry for every positive exact task/package/target/name/count row"
      pattern: "arena-test-registry|expected_count|registry_digest"
    - from: ".github/workflows/ci.yml"
      to: "tools/check-adversarial-coevolution-arena.sh"
      via: "credential-free combined-tree run step"
      pattern: "check-adversarial-coevolution-arena"
    - from: "287-VALIDATION.md"
      to: "all 287 task IDs and artifacts"
      via: "status only changes from executed evidence"
      pattern: "287-00J-01|287-00-01|287-00-02|287-00A-01|287-00B-01|287-00B-02|287-00C-01|287-00D-01|287-00E-01|287-00F-01|287-00G-01|287-00H-01|287-00I-01|wave_0_complete|preflight_complete"
---

<objective>
Close Phase 287 with an exact, mutation-controlled, combined-tree gate and truthful CI/validation wiring.

Purpose: a benchmark this adversarial is not accepted by a green workflow banner, a zero-match cargo filter, or isolated plan branches. The final gate must prove the actual arena, authority boundaries, deterministic artifacts, and cleanup all hold together.
Output: credential-free arena/isolation scripts, CI wiring, complete workspace checks, and validation evidence without any deployment or promotion action.
</objective>

<ownership_boundary>
Plan 05 runs strictly after the complete runner and owns only final gate/
isolation scripts, one CI invocation, and validation evidence. It cannot make
implementation green by weakening tests, accepting missing artifacts, or
claiming hosted/release evidence. Plan 06 is the independent reviewer and may
write its review artifacts only after this plan's combined-tree gate passes.
</ownership_boundary>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/287-adversarial-co-evolution-arena/287-CONTEXT.md
@.planning/phases/287-adversarial-co-evolution-arena/287-RESEARCH.md
@.planning/phases/287-adversarial-co-evolution-arena/287-VALIDATION.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00B-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00F-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00I-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00J-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00A-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00C-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00D-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00E-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00G-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00H-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-01-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-02-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-03-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-04-PLAN.md
@tools/check-gates-wired.sh
@tools/check-workspace-layering.sh
@tools/check-negative-registry.sh
@tools/check-fixture-freshness.sh
@tools/check-runtime-panic-contract.sh
@.github/workflows/ci.yml

<interfaces>
The final gate consumes these already-planned artifacts:
```text
tools/check-arena-test-counts.sh --self-test
tools/check-arena-oracles.sh --self-test
tools/check-arena-isolation.sh
crates/swarm-arena/tests/{arena_corpus_oracles,blue_runtime,blue_safety,
candidate_lineage,evaluation_partitions,signed_artifacts,
adversarial_coevolution_arena,arena_teardown}.rs
Signed canonical ArenaReport with explicit denominators, threshold values,
partition/control digests, stop reasons, and labeled non-gating observations.
```

The gate must run the sole canonical execution registry's exact named tests
through `tools/check-arena-test-counts.sh --registry
tools/arena-test-registry.json` with the task ID and each `--expect` name; the
helper independently observes the matching package/target/exact-name record,
matched/executed/passed/failed/ignored counts, and requires
`expected_count == 1`, executed/passed > 0, and `ignored == 0`, not a bare
cargo filter. `oracle-registry.json` is corpus-only and cannot authorize a
test. It must invoke
`tools/parse-arena-report.py` as an independent
strict parser, not trust the arena serializer or grep. It may call existing
workspace checks but must not rewrite their contracts or claim
hosted/protected evidence.

The final gate's retained evidence contract is explicit and immutable:

```text
artifacts/phase287/final-gate/arena-report.json
artifacts/phase287/final-gate/arena-lineage.json
artifacts/phase287/final-gate/final-gate-evidence.json
```

The first two files are canonical UTF-8 report/lineage bytes after removing
only fields explicitly labeled `observation_only`; the manifest records their
paths, the current HEAD/tree provenance, and
`final_gate_evidence_digest = SHA256(report_bytes || "\n" || lineage_bytes)`.
The gate must create these paths with exclusive/atomic writes, fail if an
existing path has different bytes, and never delete, truncate, or replace a
retained evidence file. Plan 06 receives all three paths plus the manifest's
digest and recomputes the same formula before independent review.
</interfaces>
</context>

<preflight_contract>
Plan 05 must name and audit the complete eleven-plan preflight set exactly:
`287-00J`, `287-00`, `287-00A`, `287-00B`, `287-00C`, `287-00D`, `287-00E`,
`287-00F`, `287-00G`, `287-00H`, and `287-00I`. Its task-level inventory must
also identify `287-00J-01`, `287-00-01`, `287-00-02`, `287-00A-01`,
`287-00B-01`, `287-00B-02`, `287-00C-01`, `287-00D-01`, `287-00E-01`,
`287-00F-01`, `287-00G-01`, `287-00H-01`, and `287-00I-01`, each exactly once
in `287-VALIDATION.md`. `287-00F-01` is authoring-only and may pass only when
the mandatory independent `287-00I-01` corpus seal is present; no other
preflight row is reduced to file existence.
</preflight_contract>

<evidence_store_boundary>
`SwarmConfig::arena.run_root` and each per-run child/capture directory are
ephemeral execution roots. The runner owns an explicit token for that root,
removes only the exact owned child during teardown, and may never use it as a
retained evidence location. All mutable ArenaArtifactStore/runtime artifacts
remain below that configured run_root as required by the phase context; the
three final-gate files below are immutable evidence-publication records, not
runtime artifacts, and are intentionally kept in a separate retained store.
The immutable retained evidence store is exactly
`artifacts/phase287/final-gate/`, with the three explicit report/lineage/manifest
paths from `<interfaces>`; Plan 05 must assert those paths are not descendants
of the ephemeral run_root or any `mktemp -d` root. Exclusive/atomic creation
must fail on a pre-existing different file, and cleanup must leave retained
bytes untouched; tests deliberately point cleanup at the retained store and
delete a retained path, and both mutations fail closed. Temporary parser and
compile-probe roots are separately ephemeral and are the only paths removed by
their traps/finally blocks.
</evidence_store_boundary>

<tasks>

<task type="auto" tdd="true">
  <name>Task 287-05-01: Build the exact final arena gate and its mutation self-test</name>
  <files>tools/check-adversarial-coevolution-arena.sh, tools/parse-arena-report.py, artifacts/phase287/final-gate/arena-report.json, artifacts/phase287/final-gate/arena-lineage.json, artifacts/phase287/final-gate/final-gate-evidence.json</files>
  <read_first>
    - `tools/check-arena-config-inventory.sh`, `tools/check-arena-oracles.sh`, and the shared `tools/check-arena-test-counts.sh` as independent gate inputs.
    - The expected report schema and parser boundary in `287-VALIDATION.md`, `287-CONTEXT.md`, and the eight corpus files; this task creates and owns `tools/parse-arena-report.py` rather than treating it as a pre-existing implementation.
    - `tools/arena-test-registry.json` as the sole positive execution registry; `scenarios/adversarial-coevolution-arena/oracle-registry.json` is corpus-only and supplies no test names/counts; and the signed report schema from Plans 00F/03/04.
    - `287-VALIDATION.md`, `287-CONTEXT.md`, and every final arena test name before writing exact-count parsing.
  </read_first>
  <behavior>
    - Test 1: Gate proves the complete eleven-plan preflight set and all thirteen named preflight task IDs from `<preflight_contract>` exactly once, runs `python3 tools/check-arena-corpus-truth.py --check` immediately before every arena run, independently canonicalizes/digests the registry set `R`, the positive plan `<verify>` set `P`, and the positive validation set `V`, requires `P == R`, `V == R`, and both source digests equal the registry digest, then runs every positive registry record exactly once/nonzero, verifies the signed report schema, all nonzero denominators, five ARENA-07 thresholds, partition disjointness, paired digests, three unseen-evasion runs, and explicit stop/cleanup evidence.
    - Test 2: `--self-test` constructs run-owned temporary mutations for zero/renamed tests, wrong execution count, plan-only omission, validation-only omission, wrong source digest, missing/extra report fields, removed threshold/denominator, changed catalog/withheld digest, inverted verdict, static replay, wall-clock fitness, authority import, missing receipt/preview, and leaked cleanup; each exits nonzero while clean controls pass.
    - Test 3: Gate rejects `RuntimeMode::LiveResponse`, external target/process/network use, promotion/canary calls, withheld feedback, unsigned/replayed artifacts, and an empty/missing arena crate rather than treating them as skipped coverage.
    - Test 4: The combined-tree CI step invokes `tools/check-arena-config-inventory.sh`, whose final scanner invokes both `tools/check-arena-config-literals.sh` and `tools/check-arena-config-literals-second-half.sh` before the independent AST-aware 24-site oracle.
    - Test 5: `tools/parse-arena-report.py --self-test` rejects every declared report/schema/denominator/threshold/observation mutation while the clean canonical report parses successfully.
    - Test 6: Two final gate runs retain exactly the same canonical report and lineage bytes at the explicit paths, and the manifest's `final_gate_evidence_digest` changes under any non-observation mutation or path deletion; retained evidence is never deleted or overwritten.
  </behavior>
  <action>Implement `tools/parse-arena-report.py` first as a separate strict schema/canonical-JSON parser with `--self-test` and the signed-report CLI, then write the strict POSIX/Bash credential-free gate using temporary files created under `mktemp -d` and a trap that removes only that directory. Its Python compile probe must use a `try/finally` cleanup of an explicitly supplied temporary `cfile`, then assert no repository `__pycache__` exists; a failed compile must still clean the temp root. Read `tools/arena-test-registry.json` as the sole execution registry; for every positive exact test record, independently observe the command's package/target/exact-name tuple and matched/executed/passed/ignored counts, require `expected_count == 1` and `ignored == 0`, and reject zero/renamed/multiply executed tests. Independently canonicalize/digest `R` from the registry, `P` from every positive plan task `<verify>` command, and `V` from every positive `287-VALIDATION.md` row using the same schema-1 sorted tuple projection; name these projections `registry_set`, `plan_set`, and `validation_set`, and require `plan_set == registry_set`, `validation_set == registry_set`, `plan_digest == registry_digest`, and `validation_digest == registry_digest` separately, with plan-only and validation-only omission mutations failing closed. Read `oracle-registry.json` only as corpus truth and fail if it is used as a second test-name/count authority. Invoke `python3 tools/check-arena-corpus-truth.py --check` immediately before each arena run so the final gate revalidates all eight corpus artifacts and their schema/digest/partition invariants; invoke the final `tools/check-arena-config-inventory.sh` direct-construction oracle before arena tests. Invoke the new parser on the signed report; it must require all nonzero denominators and exact integer bp/asset-impact thresholds, require `control_score_bp > 0` and divide by that actual control denominator (never `max(1, ...)`), reject zero-denominator mutations, unknown/missing fields, wall-clock decision fields, static replay, mutable withheld feedback, and false pass/fail inversions. Compare two final deterministic report/lineage payloads byte-for-byte after removing only explicitly labeled observation metadata, then exclusively/atomically retain them at `artifacts/phase287/final-gate/arena-report.json` and `artifacts/phase287/final-gate/arena-lineage.json`. Compute `final_gate_evidence_digest` as SHA-256 of canonical report bytes, one newline byte, and canonical lineage bytes; write `artifacts/phase287/final-gate/final-gate-evidence.json` with those paths, digest, HEAD, and HEAD tree. Assert the retained store is outside the configured ephemeral `SwarmConfig::arena.run_root`, every per-run child, and every temporary capture/compile root; cleanup may remove only those ephemeral roots and must fail if it touches retained evidence. Fail if any retained path already contains different bytes; never delete, truncate, or replace retained evidence, and pass all three paths plus the digest to Plan 06. Invoke `tools/check-arena-oracles.sh --self-test` plus structural/runtime isolation and run-root cleanup gates. Add source/dependency scans for every forbidden Red import reachable through `swarm-core`, direct detector/static replay use, live/enforced response, promotion/canary, network/process calls, stubs/placeholders/no-op success, ignored tests, wall-clock decision fields, and mutable withheld feedback. Every deliberate mutation, including zero control score, denominator fallback, report/lineage byte mutation, manifest/path deletion, cleanup targeting the retained store, and parser bypass, must be observed failing before the clean control is accepted; no credentials, live target, deploy, promotion, `git clean`, or broad deletion may appear in the script.</action>
  <verify><automated>bash tools/check-arena-test-counts.sh --self-test && bash -n tools/check-adversarial-coevolution-arena.sh && python3 tools/parse-arena-report.py --self-test && python3 -c 'import pathlib,py_compile,shutil,tempfile; repo=pathlib.Path("."); tmp=pathlib.Path(tempfile.mkdtemp(prefix="arena-gate-")); exec("try:\n py_compile.compile(\"tools/parse-arena-report.py\", cfile=str(tmp / \"parse-arena-report.pyc\"), doraise=True)\nfinally:\n shutil.rmtree(tmp, ignore_errors=False)"); assert not any(repo.rglob("__pycache__"))' && bash tools/check-arena-config-inventory.sh --self-test && bash tools/check-adversarial-coevolution-arena.sh --self-test && python3 tools/check-arena-corpus-truth.py --check && bash tools/check-adversarial-coevolution-arena.sh</automated></verify>
  <acceptance_criteria>
    - `bash tools/check-adversarial-coevolution-arena.sh --self-test` observes every listed mutation failing, including zero control score and denominator fallback, while clean controls pass; `bash tools/check-adversarial-coevolution-arena.sh` then runs all registry-named tests exactly once with `ignored == 0`.
    - The independent parser rejects unknown/missing fields, zero denominators, static/cloned output, mutable withheld feedback, wall-clock decision fields, authority/live/promotion paths, and inverted verdicts; two canonical reports compare byte-for-byte after removing only labeled observations.
    - The gate invokes `python3 tools/check-arena-corpus-truth.py --check` immediately before each arena run, then the live-inventory `check-arena-config-inventory.sh` before arena tests, and cleans only one temporary root; the Python compile probe leaves no repository `__pycache__`.
    - The gate retains `artifacts/phase287/final-gate/arena-report.json`, `arena-lineage.json`, and `final-gate-evidence.json` with exclusive/atomic writes, records `final_gate_evidence_digest = SHA256(report || newline || lineage)`, and never deletes or overwrites evidence.
    - The configured `SwarmConfig::arena.run_root`/per-run child and all `mktemp` capture/compile roots are explicitly ephemeral and disjoint from the immutable retained evidence store; cleanup mutations targeting retained paths fail closed.
    - The preflight inventory names all eleven plan IDs and all thirteen task IDs exactly once, with 287-00F-01 accepted only alongside the mandatory 287-00I-01 seal.
    - The final gate independently computes `R`, `P`, and `V` canonical 45-record sets/digests and rejects plan-only or validation-only omission mutations; it never relies on a union to hide a missing source row.
  </acceptance_criteria>
  <done>The final gate is non-vacuous, mutation-controlled, deterministic, and fail-closed across exact tests, reports, artifacts, partitions, authority, and cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 287-05-02: Wire structural/runtime isolation and full combined-tree checks in CI</name>
  <files>tools/check-arena-isolation.sh, .github/workflows/ci.yml</files>
  <read_first>
    - `tools/check-arena-oracles.sh`, `tools/check-arena-test-counts.sh`, `tools/check-workspace-layering.sh`, `tools/check-gates-wired.sh`, and the current `.github/workflows/ci.yml`.
    - `crates/swarm-arena-red/Cargo.toml` plus the full `cargo metadata --format-version 1 --locked --offline` resolve graph.
    - `crates/swarm-core/src/{lib.rs,types.rs,hypothesis_graph.rs,telemetry.rs}` to distinguish authority symbols from passive telemetry values.
  </read_first>
  <behavior>
    - Test 1: `arena_isolation_self_test_rejects_broken_dependency_and_authority_fixtures` proves `bash tools/check-arena-isolation.sh --self-test` rejects absent/empty Red closures, forbidden normal dependencies, authority imports reachable through `swarm-core`, and runtime/static-replay shortcuts while the clean fixture passes.
    - Test 2: `combined_tree_ci_step_contains_every_locked_offline_gate_once` proves the CI checker requires the arena, config-inventory, isolation, report parser, workspace, layering, negative-registry, fixture, panic, gate-wiring, and diff checks with no workflow-only shortcut, live target, credential, or broad cleanup.
  </behavior>
  <action>Implement `tools/check-arena-isolation.sh` as a credential-free source/Cargo metadata boundary gate: inventory `swarm-arena-red`'s complete transitive `kind=normal` dependency closure from `cargo metadata --format-version 1 --locked --offline`, reject forbidden workspace packages while allowing production-compatible third-party transitive dependencies, and tokenize Red imports to reject authority-bearing `swarm_core` exports without falsely banning passive telemetry names. Compile clean and deliberately broken Red fixtures; scan Arena/Blue modules for direct executor, lease minting, policy bypass, live mode, external target, promotion, static replay, stubs/placeholders/no-op success, or ignored tests; and run runtime missing-policy/receipt/preview negatives. Make the checker self-testing with a temporary broken fixture so an absent directory or no-match grep cannot pass. Add one real credential-free combined-tree CI step invoking the arena gate, final `tools/check-arena-config-inventory.sh`, isolation gate, independent report parser, full workspace tests/clippy/format, `check-workspace-layering.sh`, `check-negative-registry.sh`, `check-fixture-freshness.sh`, `check-runtime-panic-contract.sh`, `check-gates-wired.sh`, and `git diff --check`, all with `--locked --offline` and `CARGO_NET_OFFLINE=true` where Cargo runs. Use the repository's pinned runner/toolchain and sanitized environment; do not add a live target, network service, external secret, deployment, promotion, `git clean`, or workflow-only success shortcut.</action>
  <verify><automated>bash tools/check-arena-test-counts.sh --self-test && bash -n tools/check-arena-isolation.sh && bash tools/check-arena-isolation.sh --self-test && python3 -c 'import pathlib,py_compile,shutil,tempfile; repo=pathlib.Path("."); tmp=pathlib.Path(tempfile.mkdtemp(prefix="arena-isolation-")); exec("try:\n py_compile.compile(\"tools/parse-arena-report.py\", cfile=str(tmp / \"parse-arena-report.pyc\"), doraise=True)\nfinally:\n shutil.rmtree(tmp, ignore_errors=False)"); assert not any(repo.rglob("__pycache__"))' && bash tools/check-arena-config-inventory.sh && bash tools/check-gates-wired.sh && git diff --check</automated></verify>
  <acceptance_criteria>
    - `bash tools/check-arena-isolation.sh --self-test` passes only when the clean transitive closure is allowed and the deliberately broken package/import/authority fixtures fail; an absent directory or zero-match scan fails.
    - The CI step contains `CARGO_NET_OFFLINE=true`, `--locked --offline` for every Cargo invocation, and the arena, live config-inventory, isolation, parser, workspace, authority, cleanup, panic, gate-wiring, and diff checks exactly once.
    - `bash tools/check-gates-wired.sh` and `git diff --check` exit 0 without credentials, network service, live target, enforced response, promotion, or broad cleanup.
  </acceptance_criteria>
  <done>CI runs the real arena/isolation/full-workspace gates on the combined tree, and the isolation checker proves both clean and deliberately broken authority paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 287-05-03: Complete validation evidence and final full-suite audit (aggregate gate)</name>
  <files>.planning/phases/287-adversarial-co-evolution-arena/287-VALIDATION.md</files>
  <read_first>
    - `287-VALIDATION.md`, all 287 plan files, and every named command/artifact before changing statuses.
    - `tools/check-arena-config-inventory.sh`, `tools/check-adversarial-coevolution-arena.sh`, and the independent report parser.
    - `.planning/STATE.md` and `.planning/ROADMAP.md` only for evidence context; do not edit them from this task.
  </read_first>
  <behavior>
    - Test 1: `validation_map_has_one_exact_command_per_task` rejects missing/duplicate `Task 287-...` rows, empty commands, unlocked Cargo commands, `--ignored`, `|| true`, and file-exists-only shortcuts, except for the explicitly labeled authoring-only `287-00F-01` row whose mandatory independent `287-00I-01` seal row remains present.
    - Test 2: `final_audit_preserves_pending_or_red_evidence` keeps preflight/phase status pending whenever a named gate, denominator, threshold, config inventory, or combined-tree artifact is not green and never converts isolated/workflow/hosted claims into evidence.
  </behavior>
  <action>Run the final gate twice, compare deterministic report/lineage bytes at the retained explicit paths, recompute and preserve `final_gate_evidence_digest`, then execute every named exact-test command in the validation map plus the aggregate full workspace/authority/cleanup suite. Before changing any status, parse every `Task 287-...` name in every plan and require a one-to-one matching validation row with a nonempty exact command; require every named Cargo test command to be wrapped by `check-arena-test-counts.sh` with `--registry tools/arena-test-registry.json`, `--task`, `--expect`, the parsed package/target tuple, and `--exact`, while permitting only this explicitly labeled aggregate `cargo test --workspace --all-targets --locked --offline` final gate as the direct Cargo exception. Independently canonicalize the positive plan-source set `P` from every plan task `<verify>` command and the positive validation-source set `V` from every validation row, compare each set and its independently recomputed digest to the complete 45-record registry set/digest `R`, and fail closed for a plan-only or validation-only omission; never use a source union to hide a missing row. Classify the 287-00J zero-test `-- printf` mutation as mutation-only, not a positive Cargo row. Require every Cargo command to contain `--locked --offline`, every mutation gate to fail closed, and no command to contain `|| true`, an ignored test, or a missing-artifact shortcut. The only file-exists-only exception is the explicitly labeled authoring-only `287-00F-01` row; the audit must require `287-00I-01` immediately after it and must never treat 00F as the corpus seal. Run the independent `tools/check-arena-config-inventory.sh` final scanner in the same combined tree before changing any preflight status. Assert the configured/per-run ephemeral run_root and all temporary capture roots are disjoint from the immutable final-gate store and that cleanup cannot delete retained evidence. Update only task statuses, wave sign-off, and `wave_0_complete` after corresponding evidence is present; never delete, truncate, or replace the retained report, lineage, or manifest, and preserve explicit red/pending status for blocked or unverified claims. Verify all ARENA-01..08 requirements have at least one green exact test, all five thresholds use nonzero denominators, all wall-clock values are non-gating observations, the exact 24-site/23-path direct-construction SwarmConfig scan is green, and the report says results are evidence for Phase 288 synthesis only. Do not edit ROADMAP/REQUIREMENTS from this task, do not mark deployment/promotion, and do not turn an isolated branch or workflow banner into hosted/release evidence.</action>
  <verify><automated>bash tools/check-arena-test-counts.sh --self-test && bash tools/check-arena-config-inventory.sh && python3 tools/check-arena-corpus-truth.py --check && CARGO_NET_OFFLINE=true bash tools/check-adversarial-coevolution-arena.sh && python3 tools/check-arena-corpus-truth.py --check && CARGO_NET_OFFLINE=true bash tools/check-adversarial-coevolution-arena.sh && cargo test --workspace --all-targets --locked --offline && cargo clippy --workspace --all-targets --all-features --locked --offline -- -D warnings && cargo fmt --all -- --check && bash tools/check-workspace-layering.sh && bash tools/check-negative-registry.sh && bash tools/check-fixture-freshness.sh && bash tools/check-runtime-panic-contract.sh && bash tools/check-gates-wired.sh && bash tools/check-arena-isolation.sh && git diff --check</automated></verify>
  <acceptance_criteria>
    - Every plan task ID `287-...` appears exactly once in `287-VALIDATION.md` with a nonempty locked/offline automated command; missing/duplicate IDs or a command containing `|| true`, `--ignored`, or an unverified artifact fails the audit.
    - The final config inventory, arena gate twice, full workspace tests/clippy/format, authority/cleanup/panic/layering/fixture/gate checks, and `git diff --check` all exit 0 before any status flips.
    - `wave_0_complete` is set only after all eleven preflight plans' exact mutation/self-tests pass, and no deployment, promotion, hosted, or release claim is recorded.
  </acceptance_criteria>
  <done>Validation is an evidence-backed final record: every task has an executed command, Wave 0 is marked complete only after its self-tests pass, all ARENA requirements are covered, and no unverified live/promotion/hosted claim is made.</done>
</task>

</tasks>

<verification>
Run `tools/check-arena-oracles.sh --self-test`, both exact arena/isolation self-tests, the independent report parser, both final gate runs, all named integration tests, full workspace tests/clippy/format with `--locked --offline`, workspace layering, negative registry, fixture freshness, runtime panic contract, gate wiring, run-root cleanup, and diff checks. Re-run after any combined-tree merge. Confirm no external credential, live target, enforced executor, promotion/canary, response-adapter, or network/process side effect is present. Confirm the eleven named preflight plans and thirteen preflight task IDs are each represented exactly once, with 00F presence gated by 00I's independent seal. Confirm the three retained paths `artifacts/phase287/final-gate/arena-report.json`, `arena-lineage.json`, and `final-gate-evidence.json` remain present and immutable, are outside every configured/per-run ephemeral run_root and temporary capture root, and that the manifest digest is recomputed from their canonical bytes before Plan 06 receives the paths/digest. Plan 06 owns the independent P0/P1/P2 review/verification artifacts after this plan's gate is green.
</verification>

<compile_probe>
The gate's compile probe is independently specified as:
```python
import pathlib, py_compile, shutil, tempfile
repo = pathlib.Path('.')
tmp = pathlib.Path(tempfile.mkdtemp(prefix='arena-pycompile-'))
try:
    py_compile.compile('tools/parse-arena-report.py', cfile=str(tmp / 'parse-arena-report.pyc'), doraise=True)
finally:
    shutil.rmtree(tmp, ignore_errors=False)
assert not any(repo.rglob('__pycache__'))
```
The shell self-test mutates the compile path and requires the `finally` cleanup
and repository `__pycache__ == 0` assertion to remain present.
</compile_probe>

<success_criteria>
- Phase gate proves all ARENA-01..08 behavior with exact non-vacuous tests, independent mutation controls, and signed deterministic evidence.
- Combined-tree CI runs the same arena, isolation, authority, cleanup, and workspace gates; green status is not inferred from a missing/renamed test or isolated worktree.
- Validation status is truthful and Phase 287 stops at synthesis evidence without deployment or promotion.
</success_criteria>

<output>After completion, create `.planning/phases/287-adversarial-co-evolution-arena/287-05-SUMMARY.md`; Plan 06 owns the independent review and verification artifacts. Do not commit or modify roadmap/requirements from this plan.</output>
