---
phase: 287-adversarial-co-evolution-arena
plan: 01B
type: execute
wave: 9
depends_on: ["287-01"]
files_modified:
  - crates/swarm-arena-red/src/mutation.rs
  - crates/swarm-arena-red/tests/mutation_causality.rs
autonomous: true
requirements: [ARENA-03, ARENA-04, ARENA-08]

must_haves:
  truths:
    - "Red mutation choices are causally derived from persisted BlueOutcomeProjection evidence, bounded, deterministic, and not static replay or agent-count variation."
    - "Mutation tests are strict TDD and fail for absent outcome, invalid digest, withheld leakage, budget exhaustion, stubs, placeholders, ignored tests, or no-op success."
  artifacts:
    - path: "crates/swarm-arena-red/src/mutation.rs"
      provides: "RedOutcomeProjection-driven bounded mutation and lineage"
    - path: "crates/swarm-arena-red/tests/mutation_causality.rs"
      provides: "causal/deterministic mutation controls"
---

<objective>Implement the Red causal mutation engine as a separately reviewable plan.</objective>

<ownership_boundary>
Plan 01B runs strictly after 287-01 and owns only `mutation.rs` and its
causality tests. It consumes the immutable RedOutcomeProjection contract but
does not deserialize or receive the full BlueOutcome, and it does not edit
catalog, scheduler, runtime, or authority code.
</ownership_boundary>

<tasks>
<task type="auto" tdd="true">
  <name>Task 287-01B-01: Derive and test bounded causal mutations</name>
  <files>crates/swarm-arena-red/src/mutation.rs, crates/swarm-arena-red/tests/mutation_causality.rs</files>
  <read_first>
    - `crates/swarm-arena-red/src/contracts.rs`, `campaign.rs`, and `scheduler.rs` for immutable campaign/projection/budget fields.
    - `crates/swarm-arena-red/src/lib.rs` after Plan 01 to preserve module ownership and exports.
    - `.planning/phases/287-adversarial-co-evolution-arena/287-CONTEXT.md` and `287-VALIDATION.md` for allowed dimensions and stop precedence.
  </read_first>
  <behavior>
    - Test 1: `mutation_requires_persisted_blue_outcome_and_projection` rejects absent, unsigned, stale, or digest-only outcome/projection lineage and records the exact causal digests for every surviving child.
    - Test 2: `mutation_changes_from_measured_escape_or_falsification` proves different observed Blue escapes/falsified hypotheses change a surviving timing/order/catalogued tactic child.
    - Test 3: `mutation_rejects_static_replay_agent_count_and_authority_inputs` rejects event-ID/agent-count-only variation, raw telemetry, withheld feedback, response/policy authority, stubs, placeholders, ignored tests, and no-op success.
  </behavior>
  <action>Write failing tests first. Derive choices from `(seed, parent_digest, generation, outcome_digest, mutation_ordinal)` and accept only timing, ordering, or catalogued tactic/technique changes. Require nonempty escape/falsified-hypothesis references, old/new canonical values, persisted BlueOutcome and RedOutcomeProjection digests, budget/stop state, deterministic replay, and a closed vocabulary. Different measured escapes/falsifications must change a surviving child; static event/agent-count changes alone must fail. Reject raw telemetry and all authority types; run source/no-stub/ignored==0 controls.</action>
  <verify><automated>bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-01B-01 --expect mutation_requires_persisted_blue_outcome_and_projection -- cargo test -p swarm-arena-red --test mutation_causality --locked --offline -- --exact mutation_requires_persisted_blue_outcome_and_projection --nocapture --test-threads=1 && bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-01B-01 --expect mutation_changes_from_measured_escape_or_falsification -- cargo test -p swarm-arena-red --test mutation_causality --locked --offline -- --exact mutation_changes_from_measured_escape_or_falsification --nocapture --test-threads=1 && bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-01B-01 --expect mutation_rejects_static_replay_agent_count_and_authority_inputs -- cargo test -p swarm-arena-red --test mutation_causality --locked --offline -- --exact mutation_rejects_static_replay_agent_count_and_authority_inputs --nocapture --test-threads=1 && git diff --check</automated></verify>
  <acceptance_criteria>
    - The three registry-bound helper invocations for `mutation_requires_persisted_blue_outcome_and_projection`, `mutation_changes_from_measured_escape_or_falsification`, and `mutation_rejects_static_replay_agent_count_and_authority_inputs` each execute exactly one named test, pass with a positive count, and report `ignored == 0`.
    - Every surviving child records parent, persisted outcome, projection, escape/falsification, changed dimension, old/new canonical values, budget, and stop state; different measured evidence changes the child while event-ID/agent-count-only changes fail.
    - Source and test scans reject raw telemetry, withheld feedback, authority types, static replay, stubs, placeholders, ignored tests, and no-op success paths.
  </acceptance_criteria>
  <done>Red mutation is evidence-driven, deterministic, bounded, and independently mutation-tested.</done>
</task>
</tasks>

<verification>Run exact causal, static-replay, authority-isolation, and offline tests; the arena oracle consumes only the narrow RedOutcomeProjection.</verification>
<success_criteria>Every accepted child has an auditable causal lineage and no live authority capability.</success_criteria>
<output>Create the mutation summary with deterministic vectors and negative controls.</output>
