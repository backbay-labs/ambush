---
phase: 287-adversarial-co-evolution-arena
plan: 00D
type: execute
wave: 4
depends_on: ["287-00", "287-00A"]
files_modified:
  - crates/swarm-ingest-runtime/src/control.rs
  - crates/swarm-ingest-runtime/src/ingest/tests.rs
  - crates/swarm-runtime-http/src/http/tests.rs
  - crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs
  - crates/swarm-runtime/tests/operational_hardening_integration.rs
  - crates/swarm-runtime/tests/promotion_solver_gate.rs
  - crates/swarm-runtime/tests/shipped_alert_threshold_reachability.rs
  - tools/check-arena-config-literals-second-half.sh
autonomous: true
requirements: [ARENA-02, ARENA-06, ARENA-08]

must_haves:
  truths:
    - "Every SwarmConfig construction/loader site in this second inventory uses the same Phase 287-owned ArenaConfig serde default or explicit ArenaConfig::default(); no test helper silently omits the field."
    - "HTTP, ingest, control, runtime, and integration tests preserve their existing runtime mode/policy assertions while carrying the centralized disabled-by-default arena configuration."
    - "This plan's seven-path migration helper fails closed if any owned path is removed, a new matching path appears, or a direct literal lacks the arena field; Plan 00E owns the final combined 23-path inventory."
  artifacts:
    - path: "crates/swarm-ingest-runtime/src/ingest/tests.rs"
      provides: "explicit arena field in ingest test and secret-token test configurations"
    - path: "crates/swarm-runtime-http/src/http/tests.rs"
      provides: "explicit arena field in operator HTTP test configuration"
    - path: "crates/swarm-runtime/tests/operational_hardening_integration.rs"
      provides: "explicit arena field in both hardening integration literals"
    - path: "tools/check-arena-config-literals-second-half.sh"
      provides: "exact ten-path migration manifest helper owned by Plan 00D"
  key_links:
    - from: "crates/swarm-runtime/tests/promotion_solver_gate.rs"
      to: "crates/swarm-core/src/config/defaults.rs"
      via: "ruleset loader uses the canonical arena default without rewriting signed bytes"
      pattern: "with_arena_defaults|ArenaConfig::default"
---

<objective>
Close the second half of the live `SwarmConfig` site inventory and make the
preflight config scan a complete combined-tree gate. No arena implementation is
owned here; only explicit construction/default migration is.
</objective>

<ownership_boundary>
The seven paths named here are a discovered ownership partition, not a
hard-coded substitute for live source inspection. The second-half helper must
enumerate and classify current tracked Rust expressions before comparing its
manifest; new or missing expressions fail closed. Plan 00H owns the three
supplementary paths and runs only after this plan; Plan 00E independently
recomputes the complete combined-tree inventory.
</ownership_boundary>

<context>
@.planning/phases/287-adversarial-co-evolution-arena/287-00-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00C-PLAN.md
@crates/swarm-core/src/config/defaults.rs
</context>

<interfaces>
Plan 00 owns `swarm_core::config::ArenaConfig`, `SwarmConfig::arena`, serde
defaulting, and the one `SwarmConfig::with_arena_defaults` helper. A direct
literal must spell `arena: ArenaConfig::default()` or use the canonical struct
update. A loader/mutator must preserve an explicit arena value and may not
author a module-local `ArenaConfig` or a second `enabled/run_root/max_*`
default.

The full live source inventory is 24 direct `SwarmConfig` struct-expression
constructions in 23 matching paths. Declarations, signatures, `impl` headers,
comments, and strings are excluded by the dependent AST-aware scanner. Plan 00
owns the three core config paths; Plan 00C owns seven paths and Plan 00G owns
three supplementary paths; this plan owns these remaining seven source paths:

```text
crates/swarm-ingest-runtime/src/control.rs
crates/swarm-ingest-runtime/src/ingest/tests.rs
crates/swarm-runtime-http/src/http/tests.rs
crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs
crates/swarm-runtime/tests/operational_hardening_integration.rs
crates/swarm-runtime/tests/promotion_solver_gate.rs
crates/swarm-runtime/tests/shipped_alert_threshold_reachability.rs
```

Plan 00H owns the three supplementary source paths
`crates/swarm-runtime/src/service/tests_support.rs`,
`crates/swarm-runtime/src/strategy.rs`, and
`crates/swarm-runtime/src/threat_intel_runtime.rs`; the final checker must
reject duplicate ownership rather than silently choosing one plan.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 287-00D-01: Migrate the second exact SwarmConfig site inventory</name>
  <files>crates/swarm-ingest-runtime/src/control.rs, crates/swarm-ingest-runtime/src/ingest/tests.rs, crates/swarm-runtime-http/src/http/tests.rs, crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs, crates/swarm-runtime/tests/operational_hardening_integration.rs, crates/swarm-runtime/tests/promotion_solver_gate.rs, crates/swarm-runtime/tests/shipped_alert_threshold_reachability.rs, tools/check-arena-config-literals-second-half.sh</files>
  <read_first>
    - The seven owned Rust paths and the current direct `SwarmConfig` expressions in them before editing.
    - `crates/swarm-core/src/config/root.rs`, `defaults.rs`, and `validation.rs` for the canonical ArenaConfig owner and loader semantics.
    - `287-00D-PLAN.md`, `287-00E-PLAN.md`, and the live tracked-file inventory; never rely on a stale grep list.
  </read_first>
  <behavior>
    - Test 1: both ingest literals, both hardening literals, and every other direct literal in this list carry the canonical arena field/default.
    - Test 2: deserialized ruleset loaders preserve signed bytes and explicit runtime/policy values while applying only the serde arena default.
    - Test 3: the complete scanner's missing-path, extra-path, omitted-field, and alternate-default mutations all fail closed.
    - Test 4: `second_half_swarm_config_literals_use_canonical_arena_defaults` constructs the owned second-half fixture configurations and proves the canonical disabled arena default is present without changing explicit runtime or policy values.
  </behavior>
  <action>Write the second-half migration tests, including `second_half_swarm_config_literals_use_canonical_arena_defaults` in `crates/swarm-ingest-runtime/src/ingest/tests.rs`, and migrate every direct `SwarmConfig` expression discovered in the seven owned source paths to the Phase 287-owned `arena: ArenaConfig::default()` field without changing explicit values or signed ruleset bytes. The helper must enumerate `git ls-files 'crates/**/*.rs'`, tokenize comments/strings/attributes away, and compare the discovered expression fingerprints with the seven-path ownership partition; omission, alternate default, path drift, unowned construction, or changed literal fails. Plan 00H owns the remaining three second-half source paths; this plan owns the helper manifest and scanner contract for all ten paths, while Plan 00E remains the independent combined-tree 24-site oracle. Use temporary mutation copies and safe cleanup only.</action>
  <verify><automated>bash tools/check-arena-test-counts.sh --self-test && bash tools/check-arena-config-literals-second-half.sh --self-test && bash tools/check-arena-config-literals-second-half.sh && bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-00D-01 --expect second_half_swarm_config_literals_use_canonical_arena_defaults -- cargo test -p swarm-ingest-runtime --lib --locked --offline -- --exact second_half_swarm_config_literals_use_canonical_arena_defaults --test-threads=1 && git diff --check</automated></verify>
  <acceptance_criteria>
    - `bash tools/check-arena-config-literals-second-half.sh` discovers all live direct expressions in the second-half ownership partition and fails for any omitted, extra, unowned, alternate-default, or changed-value expression.
    - `bash tools/check-arena-config-literals-second-half.sh --self-test` observes missing-path, added-expression, ownership, and literal mutations failing in an ownership-scoped temporary root.
    - The registry-bound helper invocation for `second_half_swarm_config_literals_use_canonical_arena_defaults` executes exactly one non-ignored test with a positive pass count, and `git diff --check` exits 0 with signed ruleset bytes unchanged.
  </acceptance_criteria>
  <done>The second-half seven owned live source paths are migrated and the combined-tree scanner proves all 23 sites use the one ArenaConfig owner.</done>
</task>

</tasks>

<verification>
Run this migration-helper self-test and Plan 00E's AST-aware combined-tree
scan after the combined tree is assembled, then run config-focused tests with
locked offline dependencies and `git diff --check`.
</verification>

<success_criteria>
- The seven named paths are explicit and behavior-compatible.
- The ten second-half paths have one source owner and no alternate default; Plan 00E proves
  the complete 23-path direct-construction inventory.
</success_criteria>

<output>After completion, create `287-00D-SUMMARY.md` with the complete path ownership and mutation evidence.</output>
