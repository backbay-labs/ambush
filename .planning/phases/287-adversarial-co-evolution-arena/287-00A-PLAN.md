---
phase: 287-adversarial-co-evolution-arena
plan: 00A
type: execute
wave: 3
depends_on: ["287-00"]
files_modified:
  - crates/swarm-core/src/config/arena.rs
  - crates/swarm-core/src/config/mod.rs
  - crates/swarm-core/src/config/root.rs
  - crates/swarm-core/src/config/defaults.rs
  - crates/swarm-core/src/config/validation.rs
  - crates/swarm-core/src/config/tests.rs
  - crates/swarm-arena/src/contracts.rs
  - crates/swarm-arena/src/lib.rs
autonomous: true
requirements: [ARENA-02, ARENA-06, ARENA-08]

must_haves:
  truths:
    - "ArenaConfig is a single Phase 287-owned serde-defaulted module, disabled by default, and validated before any runner, fixture, detector, stack, or ingest construction."
    - "All limits, run-root ownership, signed-ruleset omission semantics, and direct SwarmConfig literal defaults are tested without changing existing non-arena config bytes."
  artifacts:
    - path: "crates/swarm-core/src/config/arena.rs"
      provides: "bounded ArenaConfig, serde defaults, run-root and require_enabled contract"
    - path: "crates/swarm-core/src/config/tests.rs"
      provides: "default-disabled, signed-ruleset, limit, and mutation tests"
    - path: "crates/swarm-arena/src/contracts.rs"
      provides: "Arena-owned ArenaRunConfig/run-root extension and post-00A package-boundary assertions over the config-neutral full Blue contract"
  key_links:
    - from: "crates/swarm-core/src/config/root.rs"
      to: "crates/swarm-core/src/config/arena.rs"
      via: "one SwarmConfig::arena field and canonical default"
    - from: "crates/swarm-arena/src/contracts.rs"
      to: "crates/swarm-core/src/config/arena.rs"
      via: "serial package-boundary assertion consumes the sole ArenaConfig owner"
---

<objective>
Own the complete arena configuration module and its fail-closed default/validation contract, then extend the initial Arena contracts with ArenaRunConfig/run-root and close the Arena package boundary against that canonical config in a serial post-00A task.
</objective>

<ownership_boundary>
Plan 00A owns the six listed `swarm-core` configuration files and, in its
post-configuration task, the serial Arena contract module, package-root export,
and package-boundary assertions. It does not edit any migration-site or
scanner file; Plans 00C/00D/00G/00H consume the config owner strictly after
Plan 00A completes. The checked-in signed ruleset remains untouched, and a
missing live construction is a migration failure rather than permission to
invent a new default site.
</ownership_boundary>

<context>
Plan 00 owns the initial workspace/crate boundaries and config-neutral typed Red/Blue contracts. This plan owns all six config files plus the serial ArenaRunConfig/run-root extension and package-boundary assertion, and no migration-site files; Plans 00C/00D/00G/00H migrate remaining literals.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 287-00A-01: Add disabled-by-default ArenaConfig and prove admission ordering</name>
  <files>crates/swarm-core/src/config/arena.rs, crates/swarm-core/src/config/mod.rs, crates/swarm-core/src/config/root.rs, crates/swarm-core/src/config/defaults.rs, crates/swarm-core/src/config/validation.rs, crates/swarm-core/src/config/tests.rs</files>
  <read_first>
    - `crates/swarm-core/src/config/root.rs`, `defaults.rs`, `validation.rs`, and `tests.rs` before changing the config schema or constructors.
    - `crates/swarm-core/src/signed_state.rs` and `rulesets/default.yaml` to verify omission preserves signed bytes; do not edit the ruleset.
    - `.planning/phases/287-adversarial-co-evolution-arena/287-CONTEXT.md` and `287-VALIDATION.md` for disabled-first and run-root requirements.
  </read_first>
  <behavior>
    - Test 1: `arena_config_default_is_disabled_and_omission_preserves_signed_bytes` proves serde omission yields the complete disabled shape without changing signed ruleset bytes.
    - Test 2: `arena_config_require_enabled_is_first_guard` proves disabled refusal occurs before runner, target, detector, configured-stack, and ingest spies record any call.
    - Test 3: `arena_config_rejects_every_bound_and_unsafe_run_root_mutation` rejects zero/contradictory/absolute/repository-root/unowned roots, unknown fields, and every independent limit mutation.
  </behavior>
  <action>Write failing tests first. Define serde `ArenaConfig` with bounded generation/event/work/virtual-time/mutation/plateau limits, relative run_root ownership, explicit `enabled`, and `require_enabled()` that fails closed before constructing any runner, target, detector, configured stack, or ingest state. Make omission resolve to the complete disabled shape; preserve signed ruleset bytes and every explicit non-arena value. Centralize `SwarmConfig::with_arena_defaults` without overwriting explicit arena fields. Reject zero/contradictory/absolute/repository-root/unowned roots, alternate defaults, unknown fields, and unsafe limits. Add a construction-order spy test proving disabled refusal precedes every downstream constructor and a mutation test for every default/limit field. Do not edit checked-in signed rulesets.
  </action>
  <verify><automated>bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-00A-01 --expect arena_config_default_is_disabled_and_omission_preserves_signed_bytes -- cargo test -p swarm-core --lib --locked --offline -- --exact arena_config_default_is_disabled_and_omission_preserves_signed_bytes --test-threads=1 && bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-00A-01 --expect arena_config_require_enabled_is_first_guard -- cargo test -p swarm-core --lib --locked --offline -- --exact arena_config_require_enabled_is_first_guard --test-threads=1 && bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-00A-01 --expect arena_config_rejects_every_bound_and_unsafe_run_root_mutation -- cargo test -p swarm-core --lib --locked --offline -- --exact arena_config_rejects_every_bound_and_unsafe_run_root_mutation --test-threads=1 && cargo fmt --all -- --check && git diff --check</automated></verify>
  <acceptance_criteria>
    - The three registry-bound helper invocations for `arena_config_default_is_disabled_and_omission_preserves_signed_bytes`, `arena_config_require_enabled_is_first_guard`, and `arena_config_rejects_every_bound_and_unsafe_run_root_mutation` each execute exactly one named non-ignored config test with a positive pass count.
    - `ArenaConfig::default()` is disabled, `require_enabled()` rejects before downstream constructor spies record any call, and every accepted `run_root` is relative, owned, and bounded.
    - `git diff --check` and `cargo fmt --all -- --check` exit 0; `rulesets/default.yaml` has the pre-task SHA-256 and byte count.
  </acceptance_criteria>
  <done>ArenaConfig has one owner, complete disabled serde defaults, strict bounded validation, and a fail-closed first guard with signed/default compatibility evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 287-00-02: Freeze the package boundary against the canonical ArenaConfig surface (post-00A)</name>
  <files>crates/swarm-arena/src/contracts.rs, crates/swarm-arena/src/lib.rs</files>
  <read_first>
    - `crates/swarm-arena/src/lib.rs` after Plan 00's package task; this task creates/extends the Arena contract module serially after the package root exists.
    - `crates/swarm-core/src/config/root.rs`, `crates/swarm-core/src/config/defaults.rs`, and `crates/swarm-core/src/config/validation.rs` after Task 287-00A-01.
    - `crates/swarm-arena-red/src/contracts.rs` to ensure the cross-package projection remains one-way.
  </read_first>
  <behavior>
    - Test 1: the Arena-owned contract file compiles against the exact six-file `ArenaConfig` surface, including `#[serde(default, deny_unknown_fields)]`, disabled defaults, bounded relative `run_root`, and `require_enabled()`.
    - Test 2: removing `arena`, changing a default, or allowing a runtime/Red authority type through the Arena contract is a deliberate mutation that fails the boundary test; config implementation and signed-ruleset tests remain owned by Task 287-00A-01.
    - Test 3: `arena_config_package_boundary_is_canonical` proves the post-00A ArenaRunConfig/run-root extension consumes the sole canonical ArenaConfig and leaves the existing full BlueOutcome/ArenaReport projection one-way into Red.
  </behavior>
  <action>After Task 287-00A-01 completes, extend the initial Arena-owned `crates/swarm-arena/src/contracts.rs` module and its serial `crates/swarm-arena/src/lib.rs` export with strict ArenaRunConfig/run-root semantics and the package-boundary assertions. Preserve the already-owned full BlueOutcome, Candidate, PairDigest, and ArenaReport canonical contracts and their one-way `red_projection()`; this task adds no duplicate full contract or default. Freeze the six canonical ArenaConfig module paths as a cross-package contract. The assertions prove `swarm_core::config::arena::ArenaConfig` is the sole config owner consumed by Arena contracts, that `require_enabled()` is called before runner/target/detector/stack/ingest construction, and that Red still receives only `GraphLogicalTime` rather than `GraphClock` or authority-bearing values. Name the exact test `arena_config_package_boundary_is_canonical` so validation targets this post-00A extension rather than Task 287-00A-01's config tests.</action>
  <verify><automated>cargo check -p swarm-arena-red -p swarm-arena --locked --offline && bash tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json --task 287-00-02 --expect arena_config_package_boundary_is_canonical -- cargo test -p swarm-arena --lib --locked --offline -- --exact arena_config_package_boundary_is_canonical --test-threads=1 && git diff --check</automated></verify>
  <acceptance_criteria>
    - The existing `arena_blue_contracts_round_trip_and_projection_boundary` test remains green after the serial extension, and `cargo test -p swarm-arena --lib --locked --offline -- --exact arena_config_package_boundary_is_canonical --test-threads=1` executes exactly one non-ignored post-00A boundary test and exits 0.
    - The contract test names `swarm_core::config::arena::ArenaConfig`, `ArenaConfig::require_enabled`, and `swarm_core::hypothesis_graph::GraphLogicalTime`, while no second ArenaConfig type or default constructor is introduced.
    - `git diff --check` exits 0 and the checked-in signed ruleset is byte-identical to its pre-task digest.
  </acceptance_criteria>
  <done>The post-00A ArenaRunConfig/run-root extension and package boundary compile against all six canonical ArenaConfig paths without duplicating the initial full contract, config default, or authority.</done>
</task>
</tasks>

<verification>Run focused config tests with locked offline dependencies, inspect signed ruleset bytes, run the existing Blue contract regression plus the post-00A package-boundary test, run the direct-literal migration scanners only after 00C/00D/00G/00H, and check no duplicate ArenaConfig constructor exists.</verification>

<success_criteria>
- No arena runner or dependency is constructed before `require_enabled()` succeeds, and the initial full Blue contract remains intact through the serial extension.
- Explicit arena values survive canonical default normalization.
</success_criteria>

<output>Create a summary containing config schema, default digest, validation mutations, admission-order evidence, and the post-00A package-boundary result.</output>
