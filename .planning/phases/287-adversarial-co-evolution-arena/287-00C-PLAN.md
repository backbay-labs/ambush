---
phase: 287-adversarial-co-evolution-arena
plan: 00C
type: execute
wave: 4
depends_on: ["287-00", "287-00A"]
files_modified:
  - crates/swarm-evolution/src/evidence.rs
  - crates/swarm-evolution/src/portfolio.rs
  - crates/swarm-runtime/src/calico_agent.rs
  - crates/swarm-runtime/src/canary.rs
  - crates/swarm-runtime/src/drafting.rs
  - crates/swarm-runtime/src/evolution/tests.rs
  - crates/swarm-runtime/src/promotion.rs
  - tools/check-arena-config-literals.sh
autonomous: true
requirements: [ARENA-02, ARENA-06, ARENA-08]

must_haves:
  truths:
    - "Every SwarmConfig construction/loader site in this inventory uses the Phase 287-owned ArenaConfig serde default or explicit ArenaConfig::default(); no test or helper authors a second arena default."
    - "The migration preserves existing signed ruleset bytes and explicit non-arena config values while making the arena field structurally visible to every direct literal."
    - "The migration manifest is complete for this half; the dependent final inventory scanner owns the combined-tree direct-literal count and fails on an omitted arena field, an alternate default, an unlisted SwarmConfig construction, or a new site silently added outside the two migration plans."
  artifacts:
    - path: "tools/check-arena-config-literals.sh"
      provides: "exact live SwarmConfig site inventory and centralized ArenaConfig default scanner"
    - path: "crates/swarm-evolution/src/evidence.rs"
      provides: "explicit arena field in the evidence test configuration"
    - path: "crates/swarm-runtime/src/promotion.rs"
      provides: "explicit arena field in promotion test configuration and loader helpers"
  key_links:
    - from: "tools/check-arena-config-literals.sh"
      to: "crates/swarm-core/src/config/defaults.rs"
      via: "all migrated sites use SwarmConfig::with_arena_defaults/ArenaConfig::default from one owner"
      pattern: "with_arena_defaults|ArenaConfig::default"
---

<objective>
Create the first-half config scanner and close its first seven live
`SwarmConfig` sites without changing production behavior or the signed
ruleset. This is a plan-only ownership map: the implementation must create the
scanner before using it, migrate every named site, and prove the source scan is
complete. Plan 00G serially extends this scanner for the remaining three
first-half paths.
</objective>

<ownership_boundary>
The ten first-half paths are split into seven paths owned here and three
supplementary paths owned by Plan 00G. This is an ownership partition, not an
imagined source inventory. The helper must be created here before it discovers
current tracked Rust files and classifies real `SwarmConfig { ... arena: ... }`
expressions; a newly added live construction outside the combined partition is
a hard failure. Plan 00G only extends this helper in its serial wave, while
Plan 00E remains the independent combined-tree authority.
</ownership_boundary>

<context>
@.planning/phases/287-adversarial-co-evolution-arena/287-00-PLAN.md
@crates/swarm-core/src/config/defaults.rs
@crates/swarm-core/src/config/root.rs
@crates/swarm-core/src/config/validation.rs
</context>

<interfaces>
Plan 00 owns `swarm_core::config::ArenaConfig`, `SwarmConfig::arena`, serde
defaulting, and the single `SwarmConfig::with_arena_defaults` helper. A direct
literal must spell `arena: ArenaConfig::default()` (or use a struct update
whose `Default` is the same owner). A function that loads an existing
`SwarmConfig` must call `with_arena_defaults()` only as a centralized
normalization assertion; it must not replace an explicitly enabled/validated
arena configuration. This plan owns no arena schema and no ruleset bytes.

The live migration inventory is intentionally explicit. It currently contains
24 direct `SwarmConfig` struct-expression constructions in 23 source paths;
declarations, type signatures, `impl` headers, function bodies without a
`SwarmConfig` expression, comments, and strings are not construction sites.
This plan owns these seven source paths:

```text
crates/swarm-evolution/src/evidence.rs
crates/swarm-evolution/src/portfolio.rs
crates/swarm-runtime/src/calico_agent.rs
crates/swarm-runtime/src/canary.rs
crates/swarm-runtime/src/drafting.rs
crates/swarm-runtime/src/evolution/tests.rs
crates/swarm-runtime/src/promotion.rs
```

Plan 00G owns the three supplementary first-half paths:

```text
crates/swarm-runtime/src/mutation/test_support.rs
crates/swarm-runtime/src/replay/tests.rs
crates/swarm-runtime/src/selection.rs
```

The scanner also lists the three canonical core owner paths
(`swarm-core/src/config/root.rs`, `validation.rs`, and `tests.rs`) but does not
claim their implementation ownership from Plan 00.

Its full expected path set is the union of those three core paths, the seven
paths above, the three Plan 00G paths, and these ten second-half paths owned by
Plan 00D/00H:

```text
crates/swarm-ingest-runtime/src/control.rs
crates/swarm-ingest-runtime/src/ingest/tests.rs
crates/swarm-runtime-http/src/http/tests.rs
crates/swarm-runtime/src/service/tests_support.rs
crates/swarm-runtime/src/strategy.rs
crates/swarm-runtime/src/threat_intel_runtime.rs
crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs
crates/swarm-runtime/tests/operational_hardening_integration.rs
crates/swarm-runtime/tests/promotion_solver_gate.rs
crates/swarm-runtime/tests/shipped_alert_threshold_reachability.rs
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 287-00C-01: Create the first-half scanner and migrate its exact site inventory</name>
  <files>crates/swarm-evolution/src/evidence.rs, crates/swarm-evolution/src/portfolio.rs, crates/swarm-runtime/src/calico_agent.rs, crates/swarm-runtime/src/canary.rs, crates/swarm-runtime/src/drafting.rs, crates/swarm-runtime/src/evolution/tests.rs, crates/swarm-runtime/src/promotion.rs, tools/check-arena-config-literals.sh</files>
  <read_first>
    - The seven owned Rust paths and every current `SwarmConfig` expression in them before adding the arena field.
    - `crates/swarm-core/src/config/root.rs`, `defaults.rs`, and `validation.rs` for the sole `ArenaConfig::default()`/`with_arena_defaults()` owner.
    - `287-00C-PLAN.md`, `287-00E-PLAN.md`, and the live `git ls-files '*.rs'` inventory to distinguish ownership from discovery.
  </read_first>
  <behavior>
    - Test 1: every direct `SwarmConfig { ... }` in these paths contains `arena: ArenaConfig::default()` or the canonical struct update.
    - Test 2: every loaded/mutated config calls the canonical helper without changing explicit arena values or signed ruleset bytes.
    - Test 3: removing one arena field, adding an alternate default, or moving one path out of this inventory makes the checker fail.
  </behavior>
  <action>Write the scanner mutation tests first, create `tools/check-arena-config-literals.sh`, then migrate every live direct `SwarmConfig` expression discovered in the seven owned source paths to the Phase 287-owned `arena: ArenaConfig::default()` field without changing explicit values or signed ruleset bytes. The initial helper must enumerate `git ls-files 'crates/**/*.rs'`, tokenize comments/strings/attributes away, and prove that each discovered expression in these paths is listed exactly once; the expected path list is only an ownership partition. Fail for any live direct expression lacking `arena`, any alternate default, any expression in an unowned path, or any path omitted from discovery. Plan 00G may extend this exact helper only after this task and owns the three remaining first-half source paths; Plan 00E is the independent combined-tree 24-site oracle. Use temporary mutation copies and safe cleanup only.</action>
  <verify><automated>bash tools/check-arena-test-counts.sh --self-test && test -x tools/check-arena-config-literals.sh && bash tools/check-arena-config-literals.sh --self-test && bash tools/check-arena-config-literals.sh && git diff --check</automated></verify>
  <acceptance_criteria>
    - `bash tools/check-arena-config-literals.sh` discovers the live tracked Rust tree, reports the seven owned paths with every direct expression, and fails on an omitted/extra/unowned construction or changed literal.
    - `bash tools/check-arena-config-literals.sh --self-test` observes deletion, alternate-default, path-move, and changed-value mutations failing under one temporary root.
    - `git diff --check` exits 0 and signed ruleset bytes plus all explicit non-arena fields are unchanged.
  </acceptance_criteria>
  <done>The initial first-half scanner is created, its seven owned live source paths are migrated to one config owner, and its mutation self-test proves no omission or alternate default.</done>
</task>

</tasks>

<verification>
Run this migration-helper self-test and the dependent 00E combined-tree
scanner with the combined tree, then run the affected package config tests
offline and `git diff --check`.
</verification>

<success_criteria>
- The seven 00C-owned paths and the three serial 00G-owned paths are explicitly
  migrated and remain behavior-compatible.
- The ten first-half paths are migrated; the exact full-workspace inventory is
  checked by dependent Plan 00E after Plan 00D/00G/00H complete.
</success_criteria>

<output>After completion, create `287-00C-SUMMARY.md` with the sorted inventory and mutation evidence.</output>
