---
phase: 287-adversarial-co-evolution-arena
plan: 06
type: execute
wave: 15
depends_on: ["287-05"]
files_modified:
  - tools/check-phase287-review.py
  - .planning/phases/287-adversarial-co-evolution-arena/287-P0-P2-REVIEW.md
  - .planning/phases/287-adversarial-co-evolution-arena/287-VERIFICATION.md
autonomous: true
requirements: [ARENA-01, ARENA-02, ARENA-03, ARENA-04, ARENA-05, ARENA-06, ARENA-07, ARENA-08]

must_haves:
  truths:
    - "An independent reviewer whose identity is disjoint from every implementation, helper, corpus, final-gate, and parser owner (including Plan 00J and the Plan 06 parser owner) inspects the complete combined tree and records every P0/P1/P2 finding with file/line, evidence, disposition, and verifier."
    - "Verification is PASS only when open_p0, open_p1, and open_p2 each occur exactly once as zero and every ARENA-01..08 requirement maps to executed combined-tree evidence."
    - "Missing artifacts, isolated-only evidence, untrusted workflow banners, config-scan failure, nonzero denominators, stale review counters, or any unresolved severity leave Phase 287 open."
    - "The independent review parser binds both artifacts to the exact current HEAD and HEAD tree, recomputes open severities from structured findings, and rejects stale or isolated evidence."
    - "The independent review parser receives the retained Plan 05 report/lineage paths and final_gate_evidence_digest, recomputes SHA256(report || newline || lineage), and rejects deleted, altered, replaced, or digest-mismatched final evidence."
    - "The parser uses only tools/arena-test-registry.json as execution authority, requires exact canonical 45-record set/digest equality, and treats the 287-00J zero-test mutation as a separate negative fixture rather than a positive Cargo row."
  artifacts:
    - path: "tools/check-phase287-review.py"
      provides: "fail-closed review/verification schema parser with current HEAD/tree binding and mutation self-tests"
    - path: ".planning/phases/287-adversarial-co-evolution-arena/287-P0-P2-REVIEW.md"
      provides: "independent severity review with provenance, structured findings, and one anchored zero counter per severity"
    - path: ".planning/phases/287-adversarial-co-evolution-arena/287-VERIFICATION.md"
      provides: "goal-backward requirement-to-evidence verification with exact zero-open proof"
  key_links:
    - from: "287-P0-P2-REVIEW.md"
      to: "287-05-PLAN.md"
      via: "review starts only after final gate, config inventory, and full combined-tree checks"
      pattern: "check-adversarial-coevolution-arena|check-arena-config-inventory|open_p0"
    - from: "tools/check-phase287-review.py"
      to: "287-P0-P2-REVIEW.md"
      via: "parser recomputes severity counters, validates current HEAD/tree provenance, and rejects stale/isolated evidence"
      pattern: "reviewed_head|source_tree_digest|open_p0|findings"
    - from: "tools/check-phase287-review.py"
      to: "artifacts/phase287/final-gate/final-gate-evidence.json"
      via: "CLI receives explicit report/lineage/manifest paths plus final_gate_evidence_digest and recomputes the retained evidence digest"
      pattern: "final-report|final-lineage|final-evidence-manifest|final-gate-evidence-digest|final_gate_evidence_digest"
    - from: "287-VERIFICATION.md"
      to: "287-VALIDATION.md"
      via: "each ARENA requirement points to an exact executed command and artifact"
      pattern: "ARENA-01|ARENA-08|open_p2"
---

<objective>
Close Phase 287 with a fail-closed independent review parser, severity review,
and goal-backward verification after all implementation/final-gate work is
complete. The parser task owns the schema, provenance binding, and mutation
controls before the reviewer writes the two evidence artifacts.
</objective>

<ownership_boundary>
Plan 06 runs strictly after Plan 05 and owns the independent review parser, the
two review artifacts, and its summary. The parser binds both artifacts to
`git rev-parse --verify HEAD` and `git rev-parse --verify HEAD^{tree}`; it
must reject any stale provenance, isolated-only evidence, unmatched task row,
or severity-counter mismatch. The reviewer must not have implemented Plans
00-05 or any preflight/final-gate helper, including Plan 00J, must inspect the
combined tree rather than isolated worker output, and
must leave the phase open on any missing runtime evidence, stale validation
row, or P0/P1/P2 finding.
</ownership_boundary>

<context>
@.planning/phases/287-adversarial-co-evolution-arena/287-VALIDATION.md
@.planning/phases/287-adversarial-co-evolution-arena/287-05-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00J-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-RESEARCH.md
@.planning/phases/287-adversarial-co-evolution-arena/287-CONTEXT.md
</context>

<strict_count_contract>Independently parse the one canonical
`tools/arena-test-registry.json` document and every positive named Cargo-based
TDD verification. Require schema version 1, a recomputed registry digest, and
exactly the 45 unique records keyed by `(task_id, package, target, exact_name)`
with `expected_count=1`; no record may be missing, extra, duplicated, or
reassigned. For each positive Cargo command, derive its package and target from
the command and require exactly one helper invocation
`tools/check-arena-test-counts.sh --registry tools/arena-test-registry.json
--task &lt;task-id&gt; --expect &lt;exact-name&gt; -- &lt;cargo command&gt;` whose full tuple
matches one canonical record, whose `--exact` filter matches the same name,
and whose helper enforces executed/passed &gt; 0, failed == 0, ignored == 0, and
`expected_count == 1`. Multi-test tasks and cross-package/target tasks are
supported by multiple record-level rows; they must not be collapsed to one
task or binary. Independently canonicalize three source projections using the
same schema-1 sorted records with `expected_count=1`: the registry projection
`R`, the task-level plan `<verify>` projection `P`, and the
`287-VALIDATION.md` projection `V`. Compute an independent canonical digest
for each source and require `P == R`, `V == R`, `digest(P) == digest(R)`, and
`digest(V) == digest(R)` separately; a plan/validation union is not accepted.
Omission mutations removing one positive row from plans only or validation only
must fail even when the other source and registry remain complete.
`oracle-registry.json` is corpus-only and cannot supply execution names/counts.
The sole explicit aggregate exception is final-audit Task
287-05-03's `cargo test --workspace --all-targets --locked --offline`, which is
not a named test and must remain paired with the helper self-test and every
exact registry-bound row. Classify the 287-00J `-- printf` zero-test helper
mutation separately from positive Cargo rows: it must be required to fail but
must not create a duplicate 287-00-01 record. Reject missing/renamed/duplicate
expected names, wrong package/target, zero executed/passed tests,
`ignored &gt; 0`, child failure, stale registry digest, direct Cargo bypass,
dual-oracle substitution, and whole-binary/module filters. The parser
self-test mutates each condition and proves nonzero rejection.</strict_count_contract>

<reviewer_independence_contract>The review artifact must carry a nonempty
`reviewer_id` and a complete normalized `implementer_ids` set. That set must
enumerate the owners of Plans `287-00J`, `287-00`, `287-00A`, `287-00B`,
`287-00C`, `287-00D`, `287-00E`, `287-00F`, `287-00G`, `287-00H`, `287-00I`,
`287-01`, `287-01B`, `287-02`, `287-02B`, `287-03`, `287-04`, and `287-05`,
including the shared count helper/registry owner and every parser/oracle/final
gate owner. Normalize case/whitespace and reject aliases; fail closed whenever
`reviewer_id` equals or is an alias of any implementation/parser owner,
including Plan 00J or the Plan 06 parser task. The independent parser's own
author is not eligible to be the reviewer.</reviewer_independence_contract>

<parser_registry_and_evidence_contract>The parser implementation must
independently canonicalize the sole `tools/arena-test-registry.json` using
lexicographically sorted object keys, compact `,`/`:` separators, UTF-8 bytes,
and no trailing newline; require schema version 1, the recomputed digest, and exactly 45 unique records keyed by
`(task_id, package, target, exact_name)` with `expected_count=1`, and join every
positive helper-wrapped Cargo command to exactly one full-tuple record. Build
three independent canonical projections: `R` from the registry bytes, `P`
from positive plan `<verify>` commands, and `V` from positive validation rows.
Require the named `plan_set == registry_set` and `validation_set == registry_set`
equalities, plus `plan_digest == registry_digest` and
`validation_digest == registry_digest`, separately for all 45 records; never
use a plan/validation union to hide an omission. Multi-test and cross-package/target
tasks remain separate records. `oracle-registry.json` is corpus-only and
cannot provide execution names, counts, or approval. The 287-00J `-- printf` zero-test invocation is a
mutation-only negative fixture: require it to fail, but exclude it from the
positive Cargo set so it cannot create a duplicate 287-00-01 record.

The parser must require normalized-disjoint `reviewer_id` and
`implementer_ids`, where implementer IDs enumerate every implementation,
helper, corpus/oracle, final-gate, and parser owner, including Plan 00J and the
Plan 06 parser task. Reject empty IDs, case/whitespace aliases, and self-review.
Require retained evidence paths to have the exact final-gate parent and to be
outside configured/per-run ephemeral `SwarmConfig::arena.run_root` and every
temporary capture root. Its self-test must mutate registry records/digest,
package/target/name equality, positive-vs-zero-test classification, reviewer
aliases (including 00J), implementation ownership, run_root containment, and
all provenance/counter/task/requirement/runtime evidence fields; every
mutation fails while clean fixtures pass.</parser_registry_and_evidence_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 287-06-01: Implement the independent review/verification parser and mutation controls</name>
  <files>tools/check-phase287-review.py</files>
  <read_first>
    - `287-VALIDATION.md`, every `287-*-PLAN.md` task name, and the Phase 286 `tools/check-phase286-review.py` contract for strict provenance/findings parsing.
    - `287-05-PLAN.md` and its final gate/parser outputs for exact combined-tree evidence anchors.
    - `287-CONTEXT.md` and `287-RESEARCH.md` for the real Phase 286 bridge/service path and no isolated/static evidence rule.
  </read_first>
  <path_contract>The parser must assert that `--plans-dir` is the existing directory `.planning/phases/287-adversarial-co-evolution-arena` (basename and resolved parent are checked), that the validation/review/verification files are regular files below that same directory, and that all retained final-evidence paths have the exact `artifacts/phase287/final-gate/` parent. It must also verify from the manifest/run metadata that the retained store is not a descendant of the configured ephemeral `SwarmConfig::arena.run_root`, any per-run child, or any temporary capture root; cleanup may not remove retained evidence. Its mutation self-test must replace the directory basename with a hyphen-less typo, point final evidence inside run_root, and prove each path violation is rejected.</path_contract>
  <structural_path_assertion>The structural validation command must first assert `test -d .planning/phases/287-adversarial-co-evolution-arena`, exact basename `287-adversarial-co-evolution-arena`, and exact parent `.planning/phases`; it must reject a hyphen-less typo or sibling directory before parsing any review evidence.</structural_path_assertion>
  <behavior>
    - Test 1: `review_parser_accepts_current_zero_open_pair` accepts exactly two artifact kinds (`p0-p2-review` and `verification`) only when both carry the current `reviewed_head` from `git rev-parse --verify HEAD`, current `source_tree_digest` from `git rev-parse --verify HEAD^{tree}`, explicit retained `--final-report`, `--final-lineage`, and `--final-evidence-manifest` paths outside ephemeral run_root, the passed `--final-gate-evidence-digest`, the same recomputed canonical final-gate evidence digest, reviewer/implementer identities with normalized disjointness including 00J, findings/evidence rows, and one anchored `open_p0: 0`, `open_p1: 0`, and `open_p2: 0` line.
    - Test 2: `review_parser_recomputes_findings_and_validation_rows` parses every `Task 287-...` from all phase plans, requires exactly one matching validation row with a nonempty locked/offline mutation-controlled command, independently builds `R`, `P`, and `V` canonical 45-record projections and digests, requires `P == R` and `V == R` plus digest equality separately, and rejects a plan-only omission or validation-only omission even when the other source is complete. It permits only the explicitly labeled authoring-only `287-00F-01` presence check when the mandatory `287-00I-01` independent seal row is present, classifies the 287-00J zero-test `-- printf` command as mutation-only, requires ARENA-01..08 evidence rows, and recomputes open P0/P1/P2 counts from structured findings rather than trusting counters.
    - Test 3: `review_parser_rejects_provenance_and_evidence_mutations` fails for stale HEAD/tree, mismatched artifact provenance, deleted/altered/replaced report or lineage paths, retained evidence inside ephemeral run_root, wrong final_gate_evidence_digest, unknown severity/status, missing/duplicate/unanchored counters, prose/history counter matches, unmatched/duplicate task IDs or registry tuples, missing requirements, missing command evidence, isolated-only evidence, dual-oracle substitution, zero-test mutation classified as positive, and reviewer identity equal/aliased to any implementation/parser owner including 00J.
    - Test 4: `review_parser_rejects_phase_directory_typo` accepts only the existing `.planning/phases/287-adversarial-co-evolution-arena` directory with the expected basename and `.planning/phases` parent; a hyphen-less basename, sibling directory, missing directory, or path outside that parent fails closed.
  </behavior>
  <action>Implement `tools/check-phase287-review.py` as a credential-free stdlib parser with schema `phase287-review-v1` and exactly two artifact kinds: `p0-p2-review` and `verification`. Require each artifact's `phase=287`, `artifact_kind`, `schema_version`, `reviewed_head`, `source_tree_digest`, shared `final_gate_evidence_digest`, independent `reviewer_id`/`implementer_ids`, structured findings table, and exactly one anchored integer counter for each open severity. Require the CLI arguments `--final-report`, `--final-lineage`, `--final-evidence-manifest`, and `--final-gate-evidence-digest`; require the three paths to be the retained Plan 05 paths, require the manifest to carry the same digest, recompute `SHA256(canonical_report_bytes || newline || canonical_lineage_bytes)`, and fail for missing, deleted, replaced, altered, or digest-mismatched evidence. Require `final_gate_evidence_digest` to be that canonical digest, not a prose claim. Findings require `finding_id`, `severity` in `P0|P1|P2|P3|none`, `status` in `open|closed|not_applicable`, file/line, evidence anchor, disposition, and verifier. Provide the exact CLI `--review-file`, `--verification-file`, `--validation-file`, and `--plans-dir` in addition to the final-evidence arguments; recompute counters from findings, require zero open P0/P1/P2, require both artifacts to agree on current HEAD/tree and retained final-gate evidence digest, and reject prose/history counter matches. Discover every `Task 287-...` name from all phase plans and require exactly one validation row; independently canonicalize and digest `R` from the registry, `P` from positive plan task `<verify>` commands, and `V` from positive validation rows, require `P == R`, `V == R`, and both source digests equal the registry digest, and run omission mutations against each source separately. Require every row's command to be nonempty, Cargo-locked/offline where applicable, mutation-controlled, and free of watch/ignored/shortcut forms except the explicitly labeled authoring-only `287-00F-01` presence row, which is valid only when mandatory `287-00I-01` seal coverage is present. Require every ARENA-01..08 row to identify an executed combined-tree command and artifact, reject isolated/workflow-only evidence, and verify the real six-argument Phase 286 stack plus GraphAuthorityHandoff/GraphSignerBinding/BoundContainmentRequest traversal is named in the evidence. The `--self-test` must mutate severity/status, counters, HEAD/tree values, reviewer identity, evidence anchors, task IDs, commands, requirements, positive registry records/digests, plan-only omissions, validation-only omissions, real-runtime evidence, retained paths, and final_gate_evidence_digest; every mutation must fail while the current clean fixtures pass. Bind provenance only to `git rev-parse --verify HEAD` and `git rev-parse --verify HEAD^{tree}`; never substitute an index/worktree digest or a summary-only claim.
  </action>
  <verify><automated>PHASE_DIR=.planning/phases/287-adversarial-co-evolution-arena && test -d "$PHASE_DIR" && test "$(basename "$PHASE_DIR")" = "287-adversarial-co-evolution-arena" && test "$(dirname "$PHASE_DIR")" = ".planning/phases" && bash tools/check-arena-test-counts.sh --self-test && python3 tools/check-phase287-review.py --self-test && python3 -c 'import pathlib,py_compile,shutil,tempfile; repo=pathlib.Path("."); tmp=pathlib.Path(tempfile.mkdtemp(prefix="arena-review-parser-")); exec("try:\n py_compile.compile(\"tools/check-phase287-review.py\", cfile=str(tmp / \"check-phase287-review.pyc\"), doraise=True)\nfinally:\n shutil.rmtree(tmp, ignore_errors=False)"); assert not any(repo.rglob("__pycache__"))' && git diff --check</automated></verify>
  <acceptance_criteria>
    - The parser's clean fixture passes only with current HEAD/tree provenance, two independent artifact kinds, recomputed zero open P0/P1/P2 counts, exact task/validation one-to-one mapping, all ARENA requirements, and a matching canonical digest of retained combined-tree gate/report/lineage evidence.
    - Every declared provenance, schema, findings, counter, task-row, requirement-row, command, reviewer-independence, isolated-evidence, and real Phase 286 traversal mutation fails closed in `--self-test`.
    - No parser path accepts workflow banners, isolated worker output, stale HEAD/tree values, summary-only zeros, prose counters, or generic/static replay evidence.
    - Structural validation runs `test -d .planning/phases/287-adversarial-co-evolution-arena`, checks the exact basename and `.planning/phases` parent before parser execution, and rejects the hyphen-less directory mutation.
  </acceptance_criteria>
  <done>A dedicated parser independently proves review/verification provenance, findings, counters, task rows, requirement evidence, and real-runtime evidence before Plan 06 can close the phase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 287-06-02: Perform independent P0/P1/P2 review and requirement verification</name>
  <files>.planning/phases/287-adversarial-co-evolution-arena/287-P0-P2-REVIEW.md, .planning/phases/287-adversarial-co-evolution-arena/287-VERIFICATION.md</files>
  <read_first>
    - `287-VALIDATION.md`, all `287-*-PLAN.md` files, and Plan 05's final gate output.
    - The combined-tree source/tests for Red closure, real ingest/runtime/Phase 286 capture, candidate application, partitions, controls, metrics, artifacts, and teardown.
    - `tools/check-arena-config-inventory.sh`, `tools/check-adversarial-coevolution-arena.sh`, `tools/check-phase287-review.py`, and existing independent review/parser conventions.
  </read_first>
  <behavior>
    - Test 1: Review evidence includes the Red normal closure including authority reachable through `swarm-core`, GraphLogicalTime/GraphClock ownership, exact Phase 286 DTO paths and real traversal, full BlueOutcome versus RedOutcomeProjection, single-capture/request-builder cardinality, candidate-to-learned-state bound, and closed deterministic patch vocabulary.
    - Test 2: Review evidence covers identical-control frozen-input formula, partition/signature/registry digests, canonical threshold arithmetic and actual `control_score_bp > 0` denominator, frozen unseen-fingerprint formula, stop precedence, run-root CAS/fence/cleanup, ignored==0/no-stub controls, offline CI, final config inventory, and independent report parser.
    - Test 3: Each ARENA-01..08 requirement maps to an exact executed green command/artifact from the combined tree; missing, isolated, hosted-only, or workflow-banner evidence fails the verifier.
    - Test 4: A parser mutation with duplicate/missing/nonzero `open_p0`, `open_p1`, or `open_p2` counters fails, while exactly one anchored `open_p0: 0`, `open_p1: 0`, and `open_p2: 0` in each artifact passes.
  </behavior>
<review_contract>Parse every `Task 287-...` name from all phase plans and require exactly one matching row in `287-VALIDATION.md`; require every row's automated command to be nonempty, Cargo-locked/offline where Cargo is used, mutation-controlled, and current to this combined tree. Parse the sole `tools/arena-test-registry.json`, recompute its digest, and require exactly 45 unique `(task, package, target, exact_name, expected_count=1)` records. Every positive named Cargo test must be helper-wrapped with `--registry tools/arena-test-registry.json`, `--task`, `--expect`, the matching package/target, and `--exact`; independently canonicalize the positive plan-source projection `P` from task-level plan `<verify>` blocks and the positive validation-source projection `V` from `287-VALIDATION.md`, then require `P == R` and `V == R` separately against registry set `R`, with `digest(P) == digest(R)` and `digest(V) == digest(R)` independently. A union is never sufficient: plan-only and validation-only omission mutations must each fail even when the other source and registry remain complete. Support multi-test/cross-package tasks record-by-record. The 287-00J `-- printf` zero-test invocation is a separately classified mutation fixture that must fail and must not count as a positive Cargo row; `oracle-registry.json` is corpus-only and cannot authorize execution. Permit only the explicitly labeled 287-05-03 aggregate full-workspace gate as a direct Cargo command, and reject any other broad or bypass command. Require normalized reviewer/implementer identity disjointness across every implementation/helper/parser owner including 00J. The independent parser must reject unmatched task IDs or registry tuples, duplicate/missing counters, prose/history counter matches, missing requirements, isolated-only evidence, stale HEAD/tree provenance, retained evidence under ephemeral run_root, dual-oracle substitution, and generic replay evidence. The review evidence must name the actual six-argument Phase 286 `ConfiguredRuntimeStack::from_graph_components(config, policy, response, bridge, authority, signer_binding)` composition with `investigation.enabled=true`, its existing bounded worker/queue/one-submit path, `GraphAuthorityHandoff`, `GraphSignerBinding`, and content-derived `BoundContainmentRequest` service traversal.</review_contract>
  <action>After Plan 05's combined-tree gate is green, assign a named reviewer whose normalized identity is absent from the complete `implementer_ids` set. That set must include every implementation, corpus/oracle, helper, final-gate, and parser owner for Plans 00J, 00, 00A, 00B, 00C, 00D, 00E, 00F, 00G, 00H, 00I, 01, 01B, 02, 02B, 03, 04, and 05; Plan 00J and the Plan 06 parser owner are explicit disjointness checks. The reviewer inspects the complete combined diff, executed test counts, the retained immutable `artifacts/phase287/final-gate/arena-report.json`, `arena-lineage.json`, and `final-gate-evidence.json`, the manifest's `final_gate_evidence_digest`, final AST-aware config scan, and the real raw-event -> detector -> six-argument Phase 286 `ConfiguredRuntimeStack::from_graph_components(config, policy, response, bridge, authority, signer_binding)` path with graph enabled, DetectOnly mode, `investigation.enabled=true`, bounded concrete worker/queue settings, exactly one coordinator `submit(&ReplayBundle)`, and exactly one worker bridge capture. For an actionable finding the reviewer confirms the existing `GraphAuthorityHandoff`/`GraphSignerBinding` path validates a content-derived `BoundContainmentRequest` before `RuntimeService::rehearse_selected_simulation(binding, &finding, &approval_context)` traverses policy/Pouncer/Tom/operator/receipt/dispatcher/rehearsal; raw ActionRequest, a second queue, direct bridge call, `new_without_worker`, SummaryInvestigator, and generic replay are failures. The reviewer also inspects Red authority boundaries, candidate application, paired controls, partitions, thresholds, fingerprints, stop/cleanup controls, and offline CI. Record every finding with severity, file/line, command/evidence, disposition, and verifier. Write both artifacts with exactly one anchored integer line per severity counter; the review artifact has `open_p0: 0`, `open_p1: 0`, `open_p2: 0` only after all findings close, and the verification artifact maps ARENA-01..08 to executed combined-tree commands and the same evidence digest. Confirm final evidence paths remain outside configured/per-run ephemeral run_root and all temporary roots; cleanup cannot delete retained evidence. Run the final config inventory and arena gate again without deleting or replacing the retained final report/lineage/manifest, then invoke the independent parser with `--final-report artifacts/phase287/final-gate/arena-report.json --final-lineage artifacts/phase287/final-gate/arena-lineage.json --final-evidence-manifest artifacts/phase287/final-gate/final-gate-evidence.json --final-gate-evidence-digest <manifest.final_gate_evidence_digest>`; it rejects duplicate counters, prose/history matches, absent counters, any nonzero value, missing requirements, stale HEAD/tree, altered final evidence, run_root containment, reviewer alias, and isolated-only evidence. Do not advance roadmap/state, claim hosted/release evidence, or commit from this plan if any check is red.
  </action>
  <verify><automated>PHASE_DIR=.planning/phases/287-adversarial-co-evolution-arena && test -d "$PHASE_DIR" && test "$(basename "$PHASE_DIR")" = "287-adversarial-co-evolution-arena" && test "$(dirname "$PHASE_DIR")" = ".planning/phases" && bash tools/check-arena-test-counts.sh --self-test && bash tools/check-arena-config-inventory.sh && CARGO_NET_OFFLINE=true bash tools/check-adversarial-coevolution-arena.sh && FINAL_GATE_DIGEST=$(python3 -c 'import json; print(json.load(open("artifacts/phase287/final-gate/final-gate-evidence.json"))["final_gate_evidence_digest"])') && python3 tools/check-phase287-review.py --self-test && python3 tools/check-phase287-review.py --review-file .planning/phases/287-adversarial-co-evolution-arena/287-P0-P2-REVIEW.md --verification-file .planning/phases/287-adversarial-co-evolution-arena/287-VERIFICATION.md --validation-file .planning/phases/287-adversarial-co-evolution-arena/287-VALIDATION.md --plans-dir .planning/phases/287-adversarial-co-evolution-arena --final-report artifacts/phase287/final-gate/arena-report.json --final-lineage artifacts/phase287/final-gate/arena-lineage.json --final-evidence-manifest artifacts/phase287/final-gate/final-gate-evidence.json --final-gate-evidence-digest "$FINAL_GATE_DIGEST" && python3 - <<'PY'
import pathlib, py_compile, shutil, tempfile
repo = pathlib.Path('.')
tmp = pathlib.Path(tempfile.mkdtemp(prefix='arena-review-validation-'))
try:
    py_compile.compile('tools/check-phase287-review.py', cfile=str(tmp / 'check-phase287-review.pyc'), doraise=True)
finally:
    shutil.rmtree(tmp, ignore_errors=False)
assert not any(repo.rglob('__pycache__'))
PY
git diff --check</automated></verify>
  <acceptance_criteria>
    - The independent reviewer is named and normalized-disjoint from every implementation/helper/parser owner in the complete implementer set, including Plan 00J and the Plan 06 parser; the review records every P0/P1/P2 finding with file/line, evidence, disposition, and verifier.
    - Each validation task ID and `ARENA-01` through `ARENA-08` has exactly one executed combined-tree evidence row; unmatched IDs, isolated-only evidence, missing artifacts, duplicate counters, or stale commands fail.
    - Each review artifact contains exactly one anchored `open_p0: 0`, `open_p1: 0`, and `open_p2: 0`; any nonzero severity leaves Phase 287 open.
  </acceptance_criteria>
  <done>Independent review and verification record exactly zero open P0/P1/P2 findings and executable evidence for ARENA-01..08; otherwise Phase 287 remains open.</done>
</task>

</tasks>

<verification>
Run the final config scanner and arena gate on the combined tree, then run the
independent counter/requirement parser with the explicit retained report,
lineage, manifest, and `final_gate_evidence_digest` arguments; recompute the
digest from the retained bytes and run `git diff --check`. No roadmap or
requirements advancement is permitted from this plan.
</verification>

<success_criteria>
- Both independent artifacts are present, each has exactly one zero counter
  line for every severity, and every ARENA requirement has green evidence.
- No P0/P1/P2 issue, stale counter, missing artifact, or untrusted evidence
  remains.
</success_criteria>

<output>After completion, create `287-06-SUMMARY.md` with reviewer identity,
commands, findings, and requirement evidence.</output>
