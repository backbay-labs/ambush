---
phase: 04-audit-and-hardening
plan: 01
type: standard
wave: 1
depends_on:
  - 03-02
files_modified:
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-spine/src/lib.rs
autonomous: true
requirements:
  - AUD-01
  - AUD-02
must_haves:
  truths:
    - Receipt trail includes detection, policy, and response steps.
    - A saved artifact can replay detect -> authorize -> execute.
  artifacts:
    - path: crates/swarm-runtime/src/service.rs
      provides: Replay bundle persistence and loading helpers.
    - path: crates/swarm-spine/src/lib.rs
      provides: Shared audit-trail or receipt-chain types.
  key_links:
    - from: crates/swarm-runtime/src/lib.rs
      to: crates/swarm-spine/src/lib.rs
      via: Runtime emits typed audit records into replay bundles.
    - from: crates/swarm-runtime/src/service.rs
      to: crates/swarm-runtime/src/lib.rs
      via: Replay consumes the same runtime-produced artifact types.
---

# Phase 4 Plan 01: Receipt Trail And Replay Bundle

<objective>
Add a typed audit trail that spans detection, policy, and response, then persist and replay that bundle from disk.
</objective>

<read_first>
- .planning/phases/04-audit-and-hardening/04-CONTEXT.md
- .planning/REQUIREMENTS.md
- crates/swarm-runtime/src/lib.rs
- crates/swarm-runtime/src/service.rs
- crates/swarm-response/src/lib.rs
- crates/swarm-spine/src/lib.rs
</read_first>

<tasks>
<task number="1" type="auto">
<description>Add typed audit records for the detection, policy, and response steps in the critical path.</description>
<acceptance_criteria>
- The runtime can return or persist one complete trail for a handled event.
- Detection, policy, and response records are distinct and typed.
</acceptance_criteria>
</task>

<task number="2" type="auto">
<description>Persist and reload replay bundles from disk.</description>
<acceptance_criteria>
- Replay bundle serialization is test-covered.
- A saved bundle can be read back into typed records.
</acceptance_criteria>
</task>

<task number="3" type="auto">
<description>Add replay tests that reconstruct a full detect-authorize-execute flow from saved artifacts.</description>
<acceptance_criteria>
- Replay tests prove the artifact contains enough information to inspect the full flow.
- Replay does not require JetStream or external services.
</acceptance_criteria>
</task>
</tasks>

<verification>
- cargo test -p swarm-runtime -p swarm-spine
</verification>

<success_criteria>
- Audit trail and replay bundle are real and test-covered.
- The runtime can reconstruct a handled event from saved artifacts.
</success_criteria>
