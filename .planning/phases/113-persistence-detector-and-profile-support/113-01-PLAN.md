---
phase: 113-persistence-detector-and-profile-support
plan: 01
type: execute
wave: 1
depends_on: [112-01, 112-02]
files_modified:
  - crates/swarm-whisker/src/persistence.rs
  - crates/swarm-whisker/src/lib.rs
autonomous: true
requirements: [PERSIST-01, PERSIST-04]

must_haves:
  truths:
    - "`PersistenceDetector` implements `DetectionStrategy` over persistence-specific payloads"
    - "`PersistenceProfile` validates suspicious paths and confidence thresholds consistently"
    - "Findings include `ThreatClass::Persistence` plus `mitre_technique_id` evidence"
  artifacts:
    - path: "crates/swarm-whisker/src/persistence.rs"
      provides: "persistence heuristics for run keys, cron, systemd timers, and scheduled tasks"
      contains: "PersistenceDetector"
  key_links:
    - from: "crates/swarm-whisker/src/persistence.rs"
      to: "crates/swarm-whisker/src/detector.rs"
      via: "detector evaluates shared `TelemetryPayload` variants"
      pattern: "TelemetryPayload"
---

<objective>
Implement the profile and heuristics for the persistence detector.

Purpose: The runtime needs direct persistence coverage rather than treating foothold artifacts as generic registry or file activity.

Output: `PersistenceProfile` plus a `PersistenceDetector` that produces ATT&CK-tagged persistence findings from suspicious registry and file persistence events.
</objective>
