# Plan 54-01

1. Extend the control layer with authenticated JSON views for status plus replay, investigation, incident, packet-set, and portfolio-history lookups and lists.
2. Add HTTP endpoints and query selectors that map cleanly onto the existing control-plane and governance-prep harness methods.
3. Document the authenticated review surface in `docs/CONFIGURATION.md` with example `curl` flows and local-token usage.
4. Verify the new read endpoints with router tests and workspace checks.
