---
phase: 109-finding-enrichment-and-delivery-metadata
plan: 01
type: execute
wave: 2
depends_on: [108-02]
files_modified:
  - crates/swarm-runtime/src/detection/pipeline.rs
  - crates/swarm-runtime/src/service.rs
autonomous: true
requirements: [SIEM-02]

must_haves:
  truths:
    - "`FindingEnrichmentService` decorates every finding with `parent_process_ancestry`, `host_metadata`, and `time_to_detect_ms`"
    - "Enrichment happens before replay persistence and outbound delivery"
    - "The enrichment path is deterministic and uses only live event context"
  artifacts:
    - path: "crates/swarm-runtime/src/service.rs"
      provides: "runtime-owned finding enrichment before delivery"
      contains: "FindingEnrichmentService"
  key_links:
    - from: "crates/swarm-runtime/src/service.rs"
      to: "crates/swarm-runtime/src/detection/pipeline.rs"
      via: "shared detection output flows through enrichment before persistence and delivery"
      pattern: "time_to_detect_ms"
---

<objective>
Add deterministic finding enrichment ahead of outbound delivery.

Purpose: SIEM and notification sinks should receive one consistent finding shape with operationally useful metadata attached.

Output: a shared enrichment service plus runtime wiring that decorates every finding before replay persistence and outbound delivery.
</objective>
