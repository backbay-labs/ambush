---
phase: 155-partition-authority-and-contingency-leases
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 154
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/tom_agent.rs
  - crates/swarm-runtime/src/dispatcher.rs
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
  - crates/swarm-runtime/src/service.rs
autonomous: true
requirements:
  - PARTITION-01
  - PARTITION-02
  - PARTITION-03
  - PARTITION-04
must_haves:
  truths:
    - "Phase 154 already established signed governance receipts and destructive-action gating, so partition authority should extend that path instead of bypassing it."
    - "Detection must continue during partition, but destructive response must fail closed unless a bounded contingency lease explicitly authorizes the action."
    - "Partition state and redeemed leases must be durable so healing and restart paths can reconcile what happened while quorum was unavailable."
  artifacts:
    - path: "crates/swarm-runtime/src/tom_agent.rs"
      provides: "Partition detection, lease issuance, and quorum-aware governance state."
    - path: "crates/swarm-runtime/src/dispatcher.rs"
      provides: "Lease-aware destructive routing and fail-closed partition enforcement."
    - path: "crates/swarm-core/src/config.rs"
      provides: "Repo-owned partition and contingency-lease configuration."
  key_links:
    - from: "crates/swarm-consensus/src/lib.rs"
      to: "crates/swarm-runtime/src/tom_agent.rs"
      via: "signed governance and lease receipts"
      pattern: "contingency authority is issued by the same consensus path as destructive-action approval"
    - from: "crates/swarm-runtime/src/dispatcher.rs"
      to: "crates/swarm-runtime/src/service.rs"
      via: "lease-gated execution"
      pattern: "destructive actions fail closed during partition unless covered by an active lease"
---

<objective>
Complete Phase 155 by adding quorum-loss detection and bounded contingency leases on top of the shipped governance path.

Purpose: let the runtime keep detecting during partition while preserving strict safety bounds around destructive response.
Output: partition-state tracking, consensus-issued contingency leases, fail-open detection with fail-closed destructive response, and durable reconciliation state for healing.
</objective>

<context>
@.planning/phases/155-partition-authority-and-contingency-leases/155-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/tom_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/dispatcher.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/service.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Detect partition state and publish quorum-aware governance status</name>
  <files>
    crates/swarm-runtime/src/tom_agent.rs
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-core/src/config.rs
  </files>
  <action>Add heartbeat/quorum-based partition detection with repo-owned thresholds and durable runtime-visible partition state.</action>
  <verify>
    <automated>cargo test -p swarm-runtime tom_agent::tests:: -- --nocapture</automated>
  </verify>
  <done>Tom governance can detect quorum loss and represent partition state explicitly instead of inferring it from generic health degradation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Issue and redeem bounded contingency leases</name>
  <files>
    crates/swarm-runtime/src/tom_agent.rs
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/service.rs
  </files>
  <action>Implement consensus-issued contingency leases with blast-radius caps and enforce them when destructive actions are attempted during partition.</action>
  <verify>
    <automated>cargo test -p swarm-runtime dispatcher::tests:: -- --nocapture</automated>
    <automated>cargo test -p swarm-runtime --test dispatch_integration -- --nocapture</automated>
  </verify>
  <done>Destructive actions during partition are blocked unless a valid active lease covers the requested scope and duration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile partition activity on healing</name>
  <files>
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/dispatcher.rs
  </files>
  <action>Persist lease redemption and partition-era actions, then surface authorized versus unauthorized activity when quorum returns.</action>
  <verify>
    <automated>cargo test -p swarm-runtime -- --nocapture</automated>
  </verify>
  <done>Healing paths preserve authorized actions, flag unauthorized ones, and leave durable evidence for later audit.</done>
</task>

</tasks>

<verification>
Keep Phase 155 focused on partition authority and lease behavior. Byzantine chaos injection and adversarial simulation remain Phase 156 work and should not be mixed into this implementation pass.
</verification>
