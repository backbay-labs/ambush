---
phase: 154-byzantine-safety-and-multi-instance-governance
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 153
files_modified:
  - crates/swarm-consensus/src/lib.rs
  - crates/swarm-runtime/src/tom_agent.rs
  - crates/swarm-runtime/src/dispatcher.rs
  - crates/swarm-runtime/src/agent_identity.rs
  - crates/swarm-pheromone/src/substrate.rs
  - crates/swarm-runtime/src/ingest.rs
autonomous: true
requirements:
  - CONSENSUS-03
  - GOVERN-01
  - GOVERN-02
  - GOVERN-03
must_haves:
  truths:
    - "Phase 153 already shipped the reusable round engine, so Phase 154 should add authenticity, exclusion, and runtime governance routing on top of that engine instead of replacing it."
    - "The project already has durable Ed25519 identities and an admission registry, so consensus trust should root in those existing artifacts rather than introducing parallel key management."
    - "Single-node governance must remain a degenerate case of the same consensus path, otherwise runtime behavior will diverge between local and multi-instance deployments."
  artifacts:
    - path: "crates/swarm-consensus/src/lib.rs"
      provides: "Signed message verification, equivocation detection, and exclusion receipts."
    - path: "crates/swarm-runtime/src/tom_agent.rs"
      provides: "TomAgent governance routing through the consensus committee with 1-of-1 fallback."
    - path: "crates/swarm-pheromone/src/substrate.rs"
      provides: "Registry-backed rejection of deposits from unadmitted identities."
  key_links:
    - from: "crates/swarm-runtime/src/agent_identity.rs"
      to: "crates/swarm-consensus/src/lib.rs"
      via: "persistent identity and registry admission"
      pattern: "only admitted identities can sign and participate in governance rounds"
    - from: "crates/swarm-runtime/src/tom_agent.rs"
      to: "crates/swarm-runtime/src/dispatcher.rs"
      via: "governance decision path"
      pattern: "response actions execute only after consensus approval receipts"
---

<objective>
Complete Phase 154 by turning the unsigned consensus core into the live governance path for TomAgent and the multi-instance runtime.

Purpose: make governance decisions authentic, admit only trusted participants, and route live response approval through BFT agreement with single-instance fallback.
Output: signed consensus messages, equivocation-triggered exclusion receipts, TomAgent BFT approval routing, registry-backed deposit rejection, and signed governance receipts persisted through the runtime audit path.
</objective>

<context>
@.planning/phases/154-byzantine-safety-and-multi-instance-governance/154-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-consensus/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/tom_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/dispatcher.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/agent_identity.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-pheromone/src/substrate.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add signed messages, verification, and equivocation exclusion</name>
  <files>
    crates/swarm-consensus/src/lib.rs
    crates/swarm-runtime/src/agent_identity.rs
  </files>
  <action>Extend the consensus envelope with Ed25519 signatures, verify admitted senders before processing, and emit signed exclusion receipts when one agent equivocates within the same round.</action>
  <verify>
    <automated>cargo test -p swarm-consensus -- --nocapture</automated>
  </verify>
  <done>Consensus messages are signed and verified, and conflicting signed votes from the same agent trigger an exclusion receipt instead of silent acceptance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route TomAgent governance through consensus with 1-of-1 fallback</name>
  <files>
    crates/swarm-runtime/src/tom_agent.rs
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/ingest.rs
  </files>
  <action>Replace local-only destructive-action veto with a consensus-backed governance decision path while preserving degenerate single-instance approval in standalone mode.</action>
  <verify>
    <automated>cargo test -p swarm-runtime tom_agent::tests:: -- --nocapture</automated>
    <automated>cargo test -p swarm-runtime dispatcher::tests:: -- --nocapture</automated>
  </verify>
  <done>Response actions requiring governance approval now execute only after consensus approval receipts, and single-node mode still functions through the same API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce registry-backed participation and persist governance receipts</name>
  <files>
    crates/swarm-pheromone/src/substrate.rs
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/ingest.rs
  </files>
  <action>Reject deposits from unadmitted identities in the multi-instance path and persist signed governance receipts carrying round, committee, and tally metadata into the audit trail.</action>
  <verify>
    <automated>cargo test -p swarm-pheromone -- --nocapture</automated>
    <automated>cargo test -p swarm-runtime -- --nocapture</automated>
  </verify>
  <done>Unadmitted identities cannot participate in governance or deposit on the multi-instance path, and governance receipts are durable and auditable.</done>
</task>

</tasks>

<verification>
Keep the phase focused on authenticity, admission, and runtime governance routing. Partition behavior and adversarial chaos belong to the next two phases and should not leak into this change set.
</verification>
