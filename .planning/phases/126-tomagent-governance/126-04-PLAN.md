---
phase: 126-tomagent-governance
plan: 04
type: execute
wave: 4
depends_on:
  - 126-01
  - 126-02
  - 126-03
files_modified:
  - crates/swarm-runtime/src/dispatcher.rs
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-response/src/lib.rs
  - crates/swarm-runtime/tests/dispatch_integration.rs
autonomous: true
requirements:
  - TOM-02
must_haves:
  truths:
    - "Governance-veto intent routes through the dispatcher runtime seam without invoking the executor."
    - "Vetoed actions produce a durable receipt id carrying the rejected action kind, veto reason, and governing agent id."
    - "The existing operator/audit receipt lookup path can still find vetoed actions by receipt id."
  artifacts:
    - path: "crates/swarm-runtime/src/lib.rs"
      provides: "Synthetic governance-veto audit and receipt construction."
    - path: "crates/swarm-response/src/lib.rs"
      provides: "Receipt audit payload for governance provenance."
    - path: "crates/swarm-runtime/tests/dispatch_integration.rs"
      provides: "Proof that veto receipts persist without any executor call."
  key_links:
    - from: "crates/swarm-runtime/src/dispatcher.rs"
      to: "crates/swarm-runtime/src/lib.rs"
      via: "governance veto routing into synthetic audit construction"
      pattern: "governance"
    - from: "crates/swarm-runtime/src/lib.rs"
      to: "crates/swarm-response/src/lib.rs"
      via: "receipt audit governance payload"
      pattern: "audit"
---

<objective>
Persist governance vetoes as first-class, queryable receipts.

Purpose: Make TOM-02 operationally real by leaving behind a durable artifact every time governance blocks a destructive autonomous response.
Output: Dispatcher/router/runtime support for governance-veto receipts plus integration coverage proving the executor is never called.
</objective>

<context>
@.planning/phases/126-tomagent-governance/126-CONTEXT.md
@.planning/phases/126-tomagent-governance/126-VALIDATION.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/dispatcher.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-response/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/tests/dispatch_integration.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend dispatch integration coverage for governance veto receipts</name>
  <read_first>
    crates/swarm-runtime/tests/dispatch_integration.rs
    crates/swarm-runtime/src/lib.rs
  </read_first>
  <files>
    crates/swarm-runtime/tests/dispatch_integration.rs
  </files>
  <action>Add the exact test `governance_veto_records_failure_receipt_without_execution` to `dispatch_integration`. It must prove the veto receipt includes governance provenance and that the executor invocation count remains zero.</action>
  <verify>
    <automated>rg -n "governance_veto_records_failure_receipt_without_execution" crates/swarm-runtime/tests/dispatch_integration.rs</automated>
  </verify>
  <done>The runtime integration suite has a pinned proof point for auditable pre-execution veto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route governance vetoes into synthetic audit and receipt output</name>
  <read_first>
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/lib.rs
    crates/swarm-response/src/lib.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/lib.rs
    crates/swarm-response/src/lib.rs
    crates/swarm-runtime/tests/dispatch_integration.rs
  </files>
  <action>Extend the dispatcher router seam to record governance-veto actions, add synthetic veto receipt construction in the runtime, and persist governing-agent provenance in receipt audit metadata without invoking the response executor.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test dispatch_integration governance_veto_records_failure_receipt_without_execution -- --exact</automated>
  </verify>
  <done>Governance vetoes now leave receipt-id-bearing audit artifacts that existing operator lookup paths can query, while the executor remains untouched.</done>
</task>

</tasks>

<verification>
Run the exact governance-veto dispatch integration test first, then the full `dispatch_integration` suite, then the full package verification sweep.
</verification>

<output>
After completion, create `.planning/phases/126-tomagent-governance/126-04-SUMMARY.md`
</output>
