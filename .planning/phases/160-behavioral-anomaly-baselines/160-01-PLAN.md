---
phase: 160-behavioral-anomaly-baselines
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 159
files_modified:
  - crates/swarm-core/src/pheromone.rs
  - crates/swarm-core/src/config.rs
  - crates/swarm-pheromone/src/substrate.rs
  - crates/swarm-pheromone/src/jetstream.rs
  - crates/swarm-whisker/src/behavioral_anomaly.rs
  - crates/swarm-whisker/src/detector.rs
  - crates/swarm-whisker/src/composite.rs
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-runtime/src/detector_factory.rs
  - crates/swarm-runtime/src/detection/pipeline.rs
  - rulesets/default.yaml
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - FILELESS-02
  - FILELESS-04
  - FILELESS-06
must_haves:
  truths:
    - "Per-host behavioral baselines are useless if they reset on restart, so persistence has to live in the durable substrate rather than one process-local map."
    - "Hydration and persistence must happen in the shared runtime detection lane or composite detectors and runtime-selected strategies will diverge."
    - "Strategy-scoped detector IDs are already part of the pheromone distinct-source model, so durable signature validation must tolerate that scoped suffix when the base signing identity matches."
  artifacts:
    - path: "crates/swarm-whisker/src/behavioral_anomaly.rs"
      provides: "Stateful anomaly scoring, decay, and snapshot serialization."
    - path: "crates/swarm-pheromone/src/substrate.rs"
      provides: "Durable baseline snapshot persistence across in-memory and local-journal backends."
    - path: "crates/swarm-runtime/src/detection/pipeline.rs"
      provides: "One-time detector hydration and dirty-state persistence during live evaluation."
  key_links:
    - from: "crates/swarm-whisker/src/behavioral_anomaly.rs"
      to: "crates/swarm-pheromone/src/substrate.rs"
      via: "BehavioralBaselineSnapshot"
      pattern: "stateful detector snapshots survive restart on the same durable substrate"
    - from: "crates/swarm-runtime/src/detection/pipeline.rs"
      to: "crates/swarm-pheromone/src/substrate.rs"
      via: "hydrate before evaluate, persist after evaluate"
      pattern: "runtime-owned detection keeps stateful baselines coherent without detector-specific side files"
---

<objective>
Complete Phase 160 by shipping a repo-owned `BehavioralAnomalyDetector`, durable baseline snapshot persistence across substrate backends, and runtime hydration proof showing baselines survive restart and still drive anomaly findings.

Purpose: close the detection-breadth milestone with a restart-safe behavioral baseline lane instead of a stateless heuristic detector.
Output: typed baseline snapshots in the substrate, a runtime-wired behavioral anomaly strategy, and verification covering both detector behavior and durable restart semantics.
</objective>

<context>
@.planning/phases/160-behavioral-anomaly-baselines/160-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-whisker/src/behavioral_anomaly.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-pheromone/src/substrate.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/detection/pipeline.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed behavioral baseline snapshot persistence</name>
  <files>
    crates/swarm-core/src/pheromone.rs
    crates/swarm-pheromone/src/substrate.rs
    crates/swarm-pheromone/src/jetstream.rs
  </files>
  <action>Add a durable `BehavioralBaselineSnapshot` contract and teach all substrate backends to store and query one snapshot per strategy.</action>
  <verify>
    <automated>cargo test -p swarm-pheromone substrate::tests::local_journal_recovers_behavioral_baseline_snapshots_after_reopen -- --exact</automated>
  </verify>
  <done>Behavioral baselines survive process restart through the same durable substrate family already used for pheromones and threat intel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the stateful behavioral anomaly detector and config surface</name>
  <files>
    crates/swarm-whisker/src/behavioral_anomaly.rs
    crates/swarm-runtime/src/config.rs
    rulesets/default.yaml
    docs/CONFIGURATION.md
  </files>
  <action>Ship the detector, repo-owned profile merge path, and default documentation for per-host ancestry, binary novelty, role-tool anomalies, and decay.</action>
  <verify>
    <automated>cargo test -p swarm-whisker behavioral_anomaly -- --nocapture</automated>
  </verify>
  <done>The detector learns a baseline, flags deviations once the host is warm, and the runtime can configure it without ad hoc JSON handling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove runtime hydration and durable restart behavior</name>
  <files>
    crates/swarm-runtime/src/detection/pipeline.rs
    crates/swarm-pheromone/src/substrate.rs
  </files>
  <action>Hydrate behavioral detectors from the substrate before evaluation, persist dirty snapshots after evaluation, and fix any durable-substrate signature invariants that block strategy-scoped runtime deposits.</action>
  <verify>
    <automated>cargo test -p swarm-runtime detection::pipeline::tests::behavioral_anomaly_detector_hydrates_persisted_baseline_after_restart -- --exact</automated>
  </verify>
  <done>A fresh runtime process can resume a learned baseline from durable storage and still emit the expected anomaly finding on the next event.</done>
</task>

</tasks>

<verification>
Keep the phase bounded to restart-safe behavioral baselines. Broader evasion corpus work remains in v1.48.
</verification>
