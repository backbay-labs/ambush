---
phase: 97-cloudtrail-and-generic-json-bridges
plan: 01
type: execute
wave: 1
depends_on: [96-01, 96-02]
files_modified:
  - Cargo.toml
  - crates/swarm-ingest-json/Cargo.toml
  - crates/swarm-ingest-json/src/lib.rs
  - crates/swarm-ingest-json/src/cloudtrail.rs
  - crates/swarm-ingest-json/src/source.rs
autonomous: true
requirements: [BRIDGE-03]

must_haves:
  truths:
    - "`CloudTrailBridge` implements `TelemetryBridge` and maps CloudTrail JSON records into normalized shared telemetry"
    - "CloudTrail bridge input handling is reusable for later runtime registry wiring instead of being hard-coded to one test-only path"
    - "CloudTrail unit tests cover authentication and non-authentication record mappings plus malformed input rejection"
  artifacts:
    - path: "crates/swarm-ingest-json/src/cloudtrail.rs"
      provides: "CloudTrail bridge implementation"
      contains: "impl TelemetryBridge for CloudTrailBridge"
    - path: "crates/swarm-ingest-json/src/source.rs"
      provides: "shared JSON record source helpers reusable by multiple bridges"
      contains: "JsonRecordSource"
  key_links:
    - from: "crates/swarm-ingest-json/src/cloudtrail.rs"
      to: "crates/swarm-core/src/telemetry.rs"
      via: "shared TelemetryBridge and TelemetryPayload contract"
      pattern: "TelemetryBridge"
    - from: "crates/swarm-ingest-json/src/cloudtrail.rs"
      to: "crates/swarm-ingest-json/src/source.rs"
      via: "shared JSON record polling"
      pattern: "JsonRecordSource"
---

<objective>
Implement `CloudTrailBridge` as the second concrete bridge adapter on top of the shared telemetry contract.

Purpose: Tetragon proved the bridge trait for streaming gRPC ingestion. This plan proves that pull-based JSON records can use the same contract while staying reusable for later runtime bridge orchestration.

Output: a new JSON ingest crate with reusable record-source helpers and a tested `CloudTrailBridge`.
</objective>
