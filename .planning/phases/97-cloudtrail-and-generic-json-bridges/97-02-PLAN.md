---
phase: 97-cloudtrail-and-generic-json-bridges
plan: 02
type: execute
wave: 1
depends_on: [97-01]
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/config.rs
  - docs/CONFIGURATION.md
  - crates/swarm-ingest-json/src/lib.rs
  - crates/swarm-ingest-json/src/generic_json.rs
autonomous: true
requirements: [BRIDGE-04]

must_haves:
  truths:
    - "`GenericJsonBridge` implements `TelemetryBridge` and maps arbitrary JSON into shared telemetry based on config-loaded field mappings"
    - "`FieldMappingConfig` and related bridge config shapes deserialize from `SwarmConfig` and validate fail closed"
    - "Generic JSON bridge tests cover successful payload mapping plus invalid pointer, missing field, and schema-rejection paths"
  artifacts:
    - path: "crates/swarm-ingest-json/src/generic_json.rs"
      provides: "config-driven generic JSON bridge implementation"
      contains: "pub struct FieldMappingConfig"
    - path: "crates/swarm-core/src/config.rs"
      provides: "repo-owned YAML config shapes for bridge mappings"
      contains: "TelemetryBridgeConfig"
  key_links:
    - from: "crates/swarm-core/src/config.rs"
      to: "crates/swarm-ingest-json/src/generic_json.rs"
      via: "shared `FieldMappingConfig` and bridge constructor inputs"
      pattern: "FieldMappingConfig"
    - from: "crates/swarm-runtime/src/config.rs"
      to: "docs/CONFIGURATION.md"
      via: "load-time validation and documented YAML shape"
      pattern: "telemetry_sources"
---

<objective>
Implement the config-driven generic JSON bridge and land the config surface that the runtime registry will consume in the next phase.

Purpose: The bridge architecture is only reusable if new sources can be described from repository-owned config rather than new Rust code. This plan turns field mapping into data and validates that `SwarmConfig` can carry it safely.

Output: `GenericJsonBridge`, `FieldMappingConfig`, parse-time config validation, and documented YAML examples for bridge mappings.
</objective>
