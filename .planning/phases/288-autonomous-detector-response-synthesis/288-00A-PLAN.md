---
phase: 288-autonomous-detector-response-synthesis
plan: 00A
type: execute
wave: 2
depends_on: [288-00]
files_modified:
  - scenarios/autonomous-detector-response-synthesis/manifest.yaml
  - scenarios/autonomous-detector-response-synthesis/historical-attacks.yaml
  - scenarios/autonomous-detector-response-synthesis/benign-controls.yaml
  - scenarios/autonomous-detector-response-synthesis/counterexamples.yaml
  - scenarios/autonomous-detector-response-synthesis/withheld-campaigns.yaml
  - crates/swarm-runtime/tests/synthesis_contract.rs
  - .planning/phases/288-autonomous-detector-response-synthesis/288-VALIDATION.md
  - tools/check-phase288-validation-map.py
autonomous: true
requirements: [SYNTH-01, SYNTH-02, SYNTH-03, SYNTH-04, SYNTH-05, SYNTH-06]

must_haves:
  truths:
    - "Historical, benign, executable-counterexample, and withheld lanes are independently authored, nonempty, disjoint, digest-addressed, and scored with separate nonzero denominators."
    - "The lane manifest is an oracle fixed before candidates; removing a truth row, expected relation, executable counterexample, digest, denominator, or threshold fails closed."
    - "Withheld content is evaluator-owned and crosses into evaluation only as an opaque handle/digest after candidate lineage is frozen."
  artifacts:
    - path: "scenarios/autonomous-detector-response-synthesis/manifest.yaml"
      provides: "Immutable lane ownership, class, denominator, Phase 287 partition digest, and threshold manifest"
    - path: "scenarios/autonomous-detector-response-synthesis/historical-attacks.yaml"
      provides: "Historical attack truth relations bound to Arena campaign IDs"
    - path: "scenarios/autonomous-detector-response-synthesis/benign-controls.yaml"
      provides: "Benign false-positive truth relations bound to Arena evidence"
    - path: "scenarios/autonomous-detector-response-synthesis/counterexamples.yaml"
      provides: "Executable counterexample truth and expected causal/response relations"
    - path: "scenarios/autonomous-detector-response-synthesis/withheld-campaigns.yaml"
      provides: "Opaque evaluator-owned withheld handles and digests"
  key_links:
    - from: "scenarios/autonomous-detector-response-synthesis/manifest.yaml"
      to: "scenarios/adversarial-coevolution-arena/partitions.yaml"
      via: "declared Phase 287 partition/content digest, never copied fixture bytes"
      pattern: "partition_digest|campaign_digest"
    - from: "crates/swarm-runtime/tests/synthesis_contract.rs"
      to: "scenarios/autonomous-detector-response-synthesis/withheld-campaigns.yaml"
      via: "opaque handle only; resolver capability remains outside synthesis"
      pattern: "opaque|resolver|withheld"
---

<objective>
Split the independent corpus/oracle from the strict contracts so Wave 0 is
small, immutable, and incapable of accepting a candidate-generated truth set.
</objective>

<context>
@.planning/phases/288-autonomous-detector-response-synthesis/288-CONTEXT.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-00-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-CONTEXT.md
@.planning/phases/287-adversarial-co-evolution-arena/287-03-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-04-PLAN.md
@scenarios/adversarial-coevolution-arena/partitions.yaml
</context>

<interfaces>
The manifest is an oracle over actual Phase 287 campaign/partition/content
digests. It does not duplicate `BlueOutcome` bytes or become a replay fixture.
Later evaluation must obtain `ArenaIngestResult`, `Phase286InvestigationCapture`,
`BlueOutcome`, `PairReport`, and signed learned-state evidence through the
Arena-owned adapter described in 288-CONTEXT. The withheld resolver is not a
Runtime or synthesizer dependency.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author four disjoint Phase 287-bound evaluation lanes</name>
  <files>scenarios/autonomous-detector-response-synthesis/manifest.yaml, scenarios/autonomous-detector-response-synthesis/historical-attacks.yaml, scenarios/autonomous-detector-response-synthesis/benign-controls.yaml, scenarios/autonomous-detector-response-synthesis/counterexamples.yaml, scenarios/autonomous-detector-response-synthesis/withheld-campaigns.yaml</files>
  <behavior>
    - Each lane has explicit class, owner, nonzero denominator, canonical IDs, content digest, Phase 287 partition/campaign digest, expected detection/causal/response relation, and resource ceiling; no row is `Mixed` or shared across lanes.
    - Counterexamples name executable Arena campaign handles and expected failure relations; withheld rows contain only opaque handles/digests and no path, bytes, resolver, or feedback field.
    - Improvement targets are fixed to integer basis-point formulas with a 1,000-bps minimum and explicit safety ceilings for false positives, false causal edges, work, blast radius, counterexample recall, withheld recall, and rollback completeness.
  </behavior>
  <action>Author the manifest and lane files before candidate code. Bind each row to the current Phase 287 `partitions.yaml`/campaign digest, Arena schema, and truth owner; keep source/evidence references typed and digest-only. Declare independent nonzero denominators and exact relative-delta formulas using checked integer arithmetic: reject a zero baseline before computing `10_000 * difference / baseline`, rather than substituting a denominator floor. Label wall-clock latency as observational while gating deterministic work units. Ensure withheld handles are evaluator-issued and cannot be resolved by synthesis before lineage freeze.</action>
  <verify><automated>cargo test -p swarm-runtime --test synthesis_contract four_lane_manifest_is_disjoint_and_digest_bound --locked --offline -- --exact</automated></verify>
  <done>All four lanes are complete, disjoint, Phase 287-bound, independently owned, and non-vacuous before candidate implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mutation-test the oracle, denominators, and withheld boundary</name>
  <files>crates/swarm-runtime/tests/synthesis_contract.rs, scenarios/autonomous-detector-response-synthesis/manifest.yaml, scenarios/autonomous-detector-response-synthesis/counterexamples.yaml, scenarios/autonomous-detector-response-synthesis/withheld-campaigns.yaml</files>
  <behavior>
    - Removing/duplicating/overlapping a lane, changing a source or expected digest, setting a denominator to zero, changing the formula/threshold, omitting an executable counterexample, or weakening a safety ceiling fails.
    - A path alias, raw withheld bytes, resolver import, mutable handle, or pre-freeze withheld feedback fails; a clean opaque-handle control passes.
    - The oracle cannot be replaced by a generated report, aggregate score, `max(1, denominator)` fallback, or timestamp-only mutation.
  </behavior>
  <action>Write table-driven mutations against the checked-in files and validate every failure through the strict contracts from Plan 00. Test exact partition disjointness, actual nonzero denominators, formula bytes, executable counterexample presence, Phase 287 digest drift, and evaluator-only withheld capability. Require before/after canonical bytes to differ for every mutation and reject any no-op or unbound mutation.</action>
  <verify><automated>cargo test -p swarm-runtime --test synthesis_contract synthesizer_cannot_dereference_withheld_handle --locked --offline -- --exact && cargo test -p swarm-runtime --test synthesis_contract oracle_mutations_fail_closed --locked --offline -- --exact</automated></verify>
  <done>Oracle weakening, empty/overlapping partitions, denominator fallbacks, missing executable counterexamples, and withheld capability leakage are all mutation-tested failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register corpus tasks and preserve Wave 0 truthfulness</name>
  <files>.planning/phases/288-autonomous-detector-response-synthesis/288-VALIDATION.md, scenarios/autonomous-detector-response-synthesis/manifest.yaml, tools/check-phase288-validation-map.py</files>
  <behavior>
    - The validation map has exactly one locked/offline automated command for each 288-00A task and marks the fixture/test files as Wave 0 inputs, not executed evidence.
    - `wave_0_complete` remains false and no status claims a candidate or hosted/release result before the execution workflow records it.
  </behavior>
  <action>Update only the Phase 288 validation map with the 00A task rows, file ownership, wave numbering, and exact commands. Add the manifest's canonical corpus digest and declared owner metadata to the map; do not mark files green merely because they exist and do not modify roadmap or requirements.</action>
  <verify><automated>python3 tools/check-phase288-validation-map.py --plans-dir .planning/phases/288-autonomous-detector-response-synthesis --validation .planning/phases/288-autonomous-detector-response-synthesis/288-VALIDATION.md --require-wave-0-pending --require-exact-task-rows</automated></verify>
  <done>The split Wave 0 plan is represented exactly in validation and remains pending until real execution evidence exists.</done>
</task>

</tasks>

<verification>
Run the exact lane, withheld, oracle-mutation, and validation-map checks. Confirm
lane bytes are an independent oracle and every runtime evaluation later binds to
actual Phase 287 Arena evidence rather than copying these fixture files.
</verification>

<success_criteria>
- Plan 00A has seven owned files and independently seals all four lanes.
- Every denominator, threshold, counterexample, expected relation, and withheld boundary is non-vacuous and mutation-tested.
- Validation records the new Wave 2 dependency without changing `wave_0_complete: false`.
</success_criteria>

<output>After completion, create `288-00A-SUMMARY.md`; no candidate implementation or roadmap update belongs here.</output>
