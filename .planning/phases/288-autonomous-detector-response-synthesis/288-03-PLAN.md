---
phase: 288-autonomous-detector-response-synthesis
plan: 03
type: execute
wave: 4
depends_on: [288-02]
files_modified:
  - crates/swarm-runtime/src/synthesis/mod.rs
  - crates/swarm-runtime/src/synthesis/response.rs
  - crates/swarm-runtime/tests/synthesis_response.rs
  - crates/swarm-runtime/tests/negative_synthesis_authority_boundary.rs
autonomous: true
requirements: [SYNTH-02]

must_haves:
  truths:
    - "A response-plan candidate can name only an action already present in the configured typed playbook and policy vocabulary."
    - "Every response plan carries the existing policy verdict/approval requirement, simulated rehearsal evidence, blast-radius scope, reversibility, concrete rollback expectation, runtime mode, and configuration digest."
    - "Synthesis never constructs an executable request, calls a response adapter/executor/dispatcher, mutates policy or playbook configuration, or turns a preview lease into authority."
  artifacts:
    - path: "crates/swarm-runtime/src/synthesis/response.rs"
      provides: "Advisory configured-playbook response-plan resolver and preview assembler"
      min_lines: 200
    - path: "crates/swarm-runtime/tests/synthesis_response.rs"
      provides: "Configured vocabulary, preview, invalid-scope, and multi-action negative tests"
    - path: "crates/swarm-runtime/tests/negative_synthesis_authority_boundary.rs"
      provides: "Response synthesis source/dependency isolation coverage"
  key_links:
    - from: "crates/swarm-runtime/src/synthesis/response.rs"
      to: "crates/swarm-runtime/src/service/runtime_service.rs"
      via: "playbook_preview is the only action-resolution path"
      pattern: "playbook_preview"
    - from: "crates/swarm-runtime/src/synthesis/response.rs"
      to: "crates/swarm-policy/src"
      via: "policy/approval projection is copied from the existing preview, not synthesized"
      pattern: "PolicyVerdict"
---

<objective>
Synthesize advisory response plans only by resolving configured typed playbook actions and rendering existing policy/rehearsal previews.

Purpose: response synthesis is an evidence projection, not a second authority. The only valid output is a reviewable simulated plan that retains existing governance, blast-radius, and rollback semantics and cannot execute.
Output: a preview-backed response-plan builder and negative tests proving no adapter, policy mutation, dispatch, live event processing, or multi-action truncation path is reachable.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-CONTEXT.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-RESEARCH.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-02-PLAN.md
@crates/swarm-runtime/src/synthesis/contracts.rs
@crates/swarm-runtime/src/service/runtime_service.rs
@crates/swarm-runtime/src/service/preview.rs
@crates/swarm-runtime/src/service/types.rs
@crates/swarm-core/src/types.rs
@crates/swarm-policy/src/lib.rs
@crates/swarm-response/src/adapters.rs
@crates/swarm-response/src/containment.rs
@crates/swarm-response/src/rollback.rs

<interfaces>
Use `RuntimeService::playbook_preview(ResponsePlaybookPreviewRequest, prepared_at_ms)` and the existing preview/rehearsal types. The preview exposes configured action kind, `PolicyVerdict`/rule and approval projection, `ResponseRehearsalPreview` with `simulated_only`, `ResponseBlastRadiusPreview`, and `ResponseRollbackPreview`. Existing multi-action rules are deliberately refused by live action selection; synthesis may retain the full ordered preview as advisory evidence but must not truncate it into an executable one-action request. `ResponseAction::requires_governance_receipt` and governed-action classification remain canonical.</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve response plans from configured typed vocabulary only</name>
  <files>crates/swarm-runtime/src/synthesis/mod.rs, crates/swarm-runtime/src/synthesis/response.rs, crates/swarm-runtime/tests/synthesis_response.rs</files>
  <behavior>
    - A valid response candidate references an existing playbook rule/action ID and returns the configured `ResponseAction` kind plus its policy/playbook configuration digest; arbitrary action strings, unknown action kinds, and unconfigured rules are rejected.
    - Approval requirements come from the existing `PolicyVerdict`/governance projection; synthesis cannot invent a policy, approval gate, lease, target scope, or capability.
    - A plan contains no executable `ActionRequest`, executor handle, dispatch token, or mutation method and is marked advisory/simulated-only at the type boundary.
  </behavior>
  <action>Write failing resolver tests first, then implement response-plan synthesis against the existing `ResponsePlaybookConfig` and strict contracts. Resolve actions by stable configured identifiers and preserve their typed kind; do not create a new `ResponseAction` variant or duplicate policy rules. Include source finding/graph/arena lineage, approval projection, runtime mode, and config digest in the plan. Do not import `ResponseExecutor`, `DispatchingExecutor`, adapter modules, or policy/playbook mutation APIs into the synthesis module.</action>
  <verify>
    <automated>cargo test -p swarm-runtime synthesis_response_plan_uses_configured_playbook --lib</automated>
  </verify>
  <done>Response candidates are references to configured typed playbook entries and cannot contain authority-bearing or invented response vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render policy-bound simulated rehearsal, blast-radius, and rollback evidence</name>
  <files>crates/swarm-runtime/src/synthesis/response.rs, crates/swarm-runtime/tests/synthesis_response.rs</files>
  <behavior>
    - Every accepted plan comes from `playbook_preview`/rehearsal preview with `simulated_only == true`, a concrete policy verdict/approval summary, nonempty target scope, typed blast-radius limits, reversibility, and addressable rollback steps.
    - Missing policy match, denied/ambiguous verdict, invalid or empty target scope, missing rollback metadata, non-simulated rehearsal, stale config digest, and unsupported multi-action live conversion block the plan.
    - Preview generation performs no adapter call, receipt mint, lease acquisition, event processing, external I/O, policy write, or playbook write.
  </behavior>
  <action>Call only the existing `RuntimeService::playbook_preview` and typed preview helpers in detect-only/simulated mode. Copy the existing policy/approval, rehearsal, blast-radius, rollback, and governance classification into the advisory response-plan record and hash the exact configured vocabulary/preview bytes. Preserve the full ordered multi-action preview for review while returning a blocking error if any caller tries to convert it to the single-action live path. Use a recording/spy boundary in tests if needed, but do not add an executor dependency to synthesis production code.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test synthesis_response response_preview_is_simulated_and_policy_bound -- --exact</automated>
  </verify>
  <done>Response efficacy can consume a complete simulated preview with policy, scope, rollback, and reversibility evidence, while every non-preview or malformed case fails closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the response authority boundary is non-reachable</name>
  <files>crates/swarm-runtime/tests/negative_synthesis_authority_boundary.rs, crates/swarm-runtime/tests/synthesis_response.rs</files>
  <behavior>
    - The clean synthesis module passes source/dependency inspection and the deliberately broken module fails when it imports an adapter/executor/dispatcher or mutates policy/playbook configuration.
    - A test recording the existing response boundary observes no adapter invocation during plan synthesis; governed live action without dispatcher admission remains rejected by the existing policy/response path.
    - Removing the boundary assertion or replacing a simulated preview with an executable call causes the exact negative test/gate to fail.
  </behavior>
  <action>Extend the Wave 0 mutation-tested scanner and integration fixtures to inspect the actual synthesis source plus a generated broken fixture. Exercise existing negative containment/policy behavior for missing receipt, missing preview, invalid scope, and governed action without dispatcher admission. Keep the assertion structural and runtime-backed: a passing boolean or a plan status must not be accepted as proof that no adapter was called. Do not weaken the checker to accommodate imports needed by a test-only spy.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test negative_synthesis_authority_boundary response_synthesis_cannot_invoke_adapter -- --exact</automated>
  </verify>
  <done>Both static and runtime controls demonstrate that response synthesis remains advisory and cannot reach response, policy, governance, or dispatch authority.</done>
</task>

</tasks>

<verification>
Run the exact response resolver, simulated-preview, and authority-boundary tests; rerun `cargo test -p swarm-response --test negative_containment_and_rollback`, `cargo clippy -p swarm-runtime --all-targets -- -D warnings`, format, and diff checks. Confirm no synthesis path imports or calls an adapter/executor or writes configuration.
</verification>

<success_criteria>
- Response plans are configured vocabulary references with existing policy/approval, simulated rehearsal, blast-radius, rollback, and reversibility evidence.
- Invalid, stale, denied, multi-action-live, or non-simulated plans fail closed.
- The source/dependency and runtime probes prove synthesis cannot execute, dispatch, authorize, or mutate response behavior.
</success_criteria>

<output>After completion, create `.planning/phases/288-autonomous-detector-response-synthesis/288-03-SUMMARY.md` with preview/policy evidence and authority-boundary results.</output>
