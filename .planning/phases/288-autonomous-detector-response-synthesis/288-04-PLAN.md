---
phase: 288-autonomous-detector-response-synthesis
plan: 04
type: execute
wave: 5
depends_on: [288-02, 288-03]
files_modified:
  - crates/swarm-runtime/src/replay/harness.rs
  - crates/swarm-runtime/src/replay/metrics.rs
  - crates/swarm-runtime/src/synthesis/mod.rs
  - crates/swarm-runtime/src/synthesis/evaluation.rs
  - crates/swarm-runtime/tests/synthesis_evaluation.rs
autonomous: true
requirements: [SYNTH-03, SYNTH-06]

must_haves:
  truths:
    - "Historical attacks, benign controls, executable counterexamples, and withheld campaigns run through the same real detect-only replay/runtime path for baseline and candidate."
    - "Reports retain per-lane scenario class, corpus/version/digest, source adapter/schema, virtual-time/seed, work counters, and baseline/candidate lineage; withheld outcomes never feed synthesis."
    - "Detector quality and response efficacy are separate metric records and deltas; wall-clock latency is observational while deterministic work/resource units are the only resource gate."
    - "A candidate cannot pass the measurement contract without a 10% target improvement, no safety-ceiling regression, executable counterexample pass, and independent withheld pass."
  artifacts:
    - path: "crates/swarm-runtime/src/synthesis/evaluation.rs"
      provides: "Four-lane evaluator and separated detector/response metrics"
      min_lines: 340
    - path: "crates/swarm-runtime/src/replay/harness.rs"
      provides: "Crate-private generalized real replay suite seam reused by synthesis"
    - path: "crates/swarm-runtime/tests/synthesis_evaluation.rs"
      provides: "Real replay, lane isolation, deterministic work, and threshold tests"
  key_links:
    - from: "crates/swarm-runtime/src/synthesis/evaluation.rs"
      to: "crates/swarm-runtime/src/replay/harness.rs"
      via: "baseline and candidate use the same load/validation/process_event/investigation/correlation summary path"
      pattern: "evaluate_suite_selection"
    - from: "crates/swarm-runtime/src/synthesis/evaluation.rs"
      to: "crates/swarm-runtime/src/synthesis/response.rs"
      via: "response efficacy consumes simulated preview/rehearsal, never detector catch counts"
      pattern: "ResponseEfficacyMetrics"
---

<objective>
Evaluate detector and advisory response candidates over four independent corpus lanes through the real replay path and emit deterministic, separately gated metrics.

Purpose: aggregate catch rate or machine latency cannot establish a safe synthesis gain. The evaluator must prove real runtime behavior, explicit denominators, causal evidence, response safety, and withheld generalization against an identical baseline.
Output: a reusable crate-private replay seam, four-lane evaluation report, deterministic work model/counters, separated detector/response metrics, and threshold/partition tests.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-CONTEXT.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-RESEARCH.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-02-PLAN.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-03-PLAN.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-VALIDATION.md
@.planning/phases/287-adversarial-co-evolution-arena/287-02-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-03-PLAN.md
@crates/swarm-runtime/src/replay/harness.rs
@crates/swarm-runtime/src/replay/types.rs
@crates/swarm-runtime/src/replay/metrics.rs
@crates/swarm-runtime/src/replay/verification.rs
@crates/swarm-runtime/src/service/runtime_service.rs

<interfaces>
`DefaultReplayHarness` already runs a real `RuntimeService` in `RuntimeMode::DetectOnly`; its suite path loads/validates manifests, builds the candidate detector, processes events, runs investigation/correlation, and emits deterministic summaries. Expose only a `pub(crate)` generalized suite method rather than a second runner or direct `detector.evaluate` shortcut. Preserve `ReplayScenarioClass` fail-closed behavior: missing and `Mixed` classes cannot contribute to a verdict. Existing latency fields are observations; the new deterministic work model must be versioned and count content-derived operations.</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose one reusable real replay seam for synthesis evaluation</name>
  <files>crates/swarm-runtime/src/replay/harness.rs, crates/swarm-runtime/src/replay/metrics.rs, crates/swarm-runtime/src/synthesis/mod.rs, crates/swarm-runtime/src/synthesis/evaluation.rs, crates/swarm-runtime/tests/synthesis_evaluation.rs</files>
  <behavior>
    - A synthesis suite run and the existing default replay run consume identical scenario loading, class validation, detector factory, runtime service, event processing, investigation/correlation, and deterministic summary logic.
    - Direct detector-only evaluation, static event loops, `MockRedSwarm`, and `DefaultReplayHarness::run_loaded_scenario` as a substitute for the real candidate path are rejected by tests or source checks.
    - Every run receives an injected deterministic seed/virtual clock and emits a versioned work counter; `Instant`/wall time is recorded only as an observation.
  </behavior>
  <action>Write seam-parity tests first, then extract the smallest crate-private replay method needed by synthesis from `DefaultReplayHarness::evaluate_suite_selection`/`run_loaded_scenario`. Keep existing callers byte-compatible and reuse existing verification/class invariants. Add deterministic work counting for event evaluations, findings/deposits, replay bundles, graph/evidence operations, policy evaluations, and response-plan operations with an explicit work-model version; never use wall-clock latency as fitness or acceptance input.</action>
  <verify>
    <automated>cargo test -p swarm-runtime replay::tests::synthesis_suite_seam_matches_default_replay --lib</automated>
  </verify>
  <done>Synthesis and ordinary replay share one real runtime execution seam, and deterministic resource work is available without changing existing latency-observation semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run and preserve the four independent corpus lanes</name>
  <files>crates/swarm-runtime/src/synthesis/evaluation.rs, crates/swarm-runtime/tests/synthesis_evaluation.rs</files>
  <behavior>
    - Baseline and candidate execute historical attacks, benign controls, replayable counterexamples, and withheld campaigns over identical campaign bytes, detector/config, seed, virtual clock, source adapters, and work-model version.
    - Historical/withheld lanes retain attack-chain/catch and generalization results; benign retains false-positive and false-causal-edge denominators; counterexamples require executable fixtures and their expected regression relation; withheld runs occur only after candidate lineage is frozen and do not emit learning feedback.
    - Missing/duplicate/overlapping/tampered lane entries, missing classes, absent executable counterexamples, changed partition digest, or withheld content access blocks the evaluation before scoring.
  </behavior>
  <action>Implement `SynthesisEvaluationCorpus` loading from the Wave 0 manifest and evaluate each lane independently through the shared replay seam. Pair baseline/candidate reports and reject any mismatch in corpus/config/ruleset/seed/clock/source/work-model digests. Preserve per-scenario evidence IDs, graph edge coverage, causal claims, response preview references, and first blocking reason; do not collapse lanes into aggregate catch rate or allow withheld results to mutate detector/response candidates.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test synthesis_evaluation four_lanes_execute_through_real_replay -- --exact</automated>
  </verify>
  <done>All four lanes are real, disjoint, independently scored, reproducible, and fail closed on missing, stale, mixed, or leaked corpus evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Separate detector quality and response efficacy with gated deltas</name>
  <files>crates/swarm-runtime/src/synthesis/evaluation.rs, crates/swarm-runtime/tests/synthesis_evaluation.rs</files>
  <behavior>
    - `DetectorMetrics` reports attack-chain recall, catch rate, false-positive rate, false-causal-edge rate, causal-evidence coverage, deterministic work/resource use, and latency observation with explicit denominators.
    - `ResponseEfficacyMetrics` reports virtual time to containment/plan, blast-radius scope, affected capabilities, reversibility, rollback completeness/cost, approval/governance posture, response work, and latency observation without borrowing detector counters.
    - A candidate passes only if one manifest-declared target (higher `attack_chain_recall_bps`, higher `evidence_coverage_bps`, or lower `median_time_to_containment_work_units`) improves by at least 1,000 bps under the frozen checked-integer formula: `10_000 * (candidate - baseline) / max(baseline, 1)` for higher-is-better, or `10_000 * (baseline - candidate) / max(baseline, 1)` for lower-is-better. Benign false positives do not regress and remain at most 500 bps; false causal edges do not regress and remain at most 1,000 bps; duplicate work does not regress and remains at most 500 bps; counterexample recall is at least 9,000 bps; withheld recall is at least 8,000 bps; evidence coverage is 10,000 bps; containment work and blast radius do not regress, blast radius is at most one declared scope, and reversibility plus rollback completeness are 10,000 bps. The improvement cannot be based on wall-clock latency.
  </behavior>
  <action>Compute baseline-relative deltas from the paired lane reports and response previews, keep detector and response records structurally separate, and enforce only the exact target allowlist and numeric safety ceilings frozen in the independent manifest. Encode safety, withheld, counterexample, and denominator verdicts as independent gates. Use integer/bounded representations or explicit finite values for gating metrics; retain wall-clock latency only under an observation field. Add threshold mutation tests for undeclared target selection, every weakened or removed ceiling/denominator, inverted verdicts, aggregate-only catch-rate scoring, and any attempt to claim containment improvement from detector metrics.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test synthesis_evaluation detector_and_response_metrics_are_separate -- --exact</automated>
  </verify>
  <done>The report provides auditable detector and response quality/safety deltas and rejects candidates that lack the 10% improvement plus all independent safety and generalization gates.</done>
</task>

</tasks>

<verification>
Run seam-parity, four-lane, and metric-separation exact tests; rerun relevant replay/verification tests, repeat the evaluator with identical inputs and compare report bytes, then run clippy, format, and diff checks. Verify no `Instant` or latency observation is read by a gate and no lane is represented by a zero denominator.
</verification>

<success_criteria>
- Candidate and baseline both traverse the real detect-only replay/runtime path across all four lanes.
- Detector quality and response efficacy remain separate with explicit denominators, deterministic resource work, and observational wall-clock latency.
- The 10% improvement/no-safety-regression/withheld/counterexample contract is fail-closed and reproducible.
</success_criteria>

<output>After completion, create `.planning/phases/288-autonomous-detector-response-synthesis/288-04-SUMMARY.md` with repeated report bytes, per-lane metrics, and baseline delta evidence.</output>
