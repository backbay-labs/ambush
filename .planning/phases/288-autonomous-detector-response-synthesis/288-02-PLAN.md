---
phase: 288-autonomous-detector-response-synthesis
plan: 02
type: execute
wave: 3
depends_on: [288-01]
files_modified:
  - crates/swarm-runtime/src/synthesis/mod.rs
  - crates/swarm-runtime/src/synthesis/detector.rs
  - crates/swarm-runtime/tests/synthesis_integration.rs
autonomous: true
requirements: [SYNTH-01]

must_haves:
  truths:
    - "Graph gaps, typed hypothesis-edge gaps, arena escapes, and falsifier findings produce bounded detector candidates only when every source ID and digest resolves through the typed adapter."
    - "Every detector candidate records signal features, one supported detector family, addressed hypothesis edges, source evidence, graph/arena lineage, bounded recipe, parent genome hashes, corpus/baseline references, and canonical candidate/genome digests."
    - "Changing source evidence, graph/arena lineage, family, profile, seed, or recipe changes the candidate digest; raw telemetry and free-form executable code cannot enter synthesis."
  artifacts:
    - path: "crates/swarm-runtime/src/synthesis/detector.rs"
      provides: "Evidence-addressed bounded detector candidate synthesizer"
      min_lines: 240
    - path: "crates/swarm-runtime/tests/synthesis_integration.rs"
      provides: "Graph/arena/falsifier lineage and ten-family candidate integration tests"
  key_links:
    - from: "crates/swarm-runtime/src/synthesis/detector.rs"
      to: "crates/swarm-runtime/src/mutation/types.rs"
      via: "synthesized target_genome uses exhaustive typed family enum"
      pattern: "EvolutionDetectorGenome"
    - from: "crates/swarm-runtime/src/synthesis/detector.rs"
      to: "crates/swarm-runtime/src/synthesis/contracts.rs"
      via: "typed source references are resolved and digest-checked before candidate creation"
      pattern: "source_artifact_digests"
---

<objective>
Synthesize bounded detector candidates from Phase 286 graph gaps, Phase 287 arena escapes, and falsifier findings with complete evidence-addressed lineage.

Purpose: a detector profile without a reproducible reason, source digest, addressed causal edge, or parent genome is not a defensible candidate. This plan creates the typed synthesis boundary without inventing a graph schema or generating executable code.
Output: a deterministic detector input adapter, candidate builder, canonical IDs/digests, and integration tests covering every supported family and source type.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-CONTEXT.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-RESEARCH.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-00-PLAN.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-01-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-03-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-04-PLAN.md
@crates/swarm-runtime/src/synthesis/contracts.rs
@crates/swarm-runtime/src/mutation/types.rs
@crates/swarm-runtime/src/mutation/autonomous.rs
@crates/swarm-runtime/src/mutation/fitness.rs
@crates/swarm-runtime/src/evasion_coverage.rs
@rulesets/evasion/attack-technique-catalog.yaml

<interfaces>
The Wave 0 contracts expose bounded typed source references and adapter resolution by canonical ID/schema/content digest. The genome boundary now exposes ten supported `EvolutionDetectorGenome` variants and `to_candidate_manifest`; use `target_genome` for all generated candidates. Canonical IDs must use `swarm_crypto::canonical_json_bytes` followed by `swarm_crypto::sha256_hex`, excluding display-only timestamps. The Phase 286/287 implementation names are not assumed: adapter inputs are typed IDs/digests and unresolved/unknown records block construction.</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve graph, arena, and falsifier evidence through a typed bounded adapter</name>
  <files>crates/swarm-runtime/src/synthesis/mod.rs, crates/swarm-runtime/src/synthesis/detector.rs, crates/swarm-runtime/tests/synthesis_integration.rs</files>
  <behavior>
    - A graph gap/edge, arena escape, or falsifier reference is accepted only when the adapter returns the declared typed record, schema version, producer role, source evidence IDs, and matching content digest.
    - Empty evidence, unknown IDs, stale/tampered source bytes, duplicate/conflicting lineage, cross-family parent references, unbounded feature/parent lists, and withheld content access fail closed.
    - The adapter exposes normalized signal-feature/schema references rather than raw telemetry, vendor payloads, commands, paths, or generated source code.
  </behavior>
  <action>Write adapter tests first, then implement the synthesis detector module against the Wave 0 typed contract. Define a narrow provider trait for graph gaps/hypothesis edges, arena escapes, falsifier findings, and source evidence; validate all IDs/digests and enforce the declared source/feature/parent budgets before any mutation runs. Accept graph/arena/falsifier records as evidence pressure only; do not duplicate Phase 286 graph storage, call an arena red capability, read withheld fixtures, or infer causal edges from a display string or wall-clock order.</action>
  <verify>
    <automated>cargo test -p swarm-runtime synthesis_detector_input_validation --lib</automated>
  </verify>
  <done>Only digest-verified, typed, bounded source records can reach detector synthesis, and all missing/stale/withheld/raw-input mutations fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build evidence-addressed detector candidates with canonical lineage</name>
  <files>crates/swarm-runtime/src/synthesis/detector.rs, crates/swarm-runtime/src/synthesis/mod.rs</files>
  <behavior>
    - A valid request yields a candidate containing detector family, signal features, addressed hypothesis-edge IDs, source evidence IDs/digests, graph/arena input digest, bounded recipe, seed/generator version, parent strategy/genome hashes, baseline/corpus references, and target genome.
    - Candidate ID and content digests change when any source, feature, edge, parent, recipe, profile, seed, corpus, or baseline lineage changes, while display timestamps do not change them.
    - Cross-family crossover, runtime-only strategies, empty addressed edges/evidence, invalid profile thresholds, and missing typed target genomes are rejected rather than downgraded to legacy overrides.
  </behavior>
  <action>Implement deterministic builders for gap-driven, escape-driven, and falsifier-driven candidates using the per-family bounded recipes from Plan 01. Preserve a causal `source_kind` and changed-dimension record for each candidate, require at least one typed graph/arena/falsifier trigger and source evidence, and keep parent genome lineage separate from the configured rollout baseline lineage. Compute canonical genome and candidate digests with existing crypto helpers; store no `created_at_ms` in ID material. Ensure candidate materialization delegates to the exhaustive typed mutation/factory path and never emits an LLM prompt, arbitrary predicate, or response authority.</action>
  <verify>
    <automated>cargo test -p swarm-runtime synthesis_detector_candidates_preserve_typed_lineage --lib</automated>
  </verify>
  <done>Detector candidates are reproducible, evidence-addressed, bounded, and typed; every candidate has enough lineage for an operator to reproduce why it exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove all ten detector families materialize from real source pressure</name>
  <files>crates/swarm-runtime/tests/synthesis_integration.rs</files>
  <behavior>
    - Representative graph-gap, arena-escape, and falsifier inputs produce candidates for each of the ten supported families and each candidate constructs through `build_detector_from_candidate`.
    - Replaying the same typed input/seed produces byte-identical candidate and lineage bytes; changing the measured source outcome produces a different candidate digest and recorded mutation dimension.
    - Runtime-only families, empty/withheld source pressure, raw telemetry injection, and an unbound graph edge are rejected before replay.
  </behavior>
  <action>Build a table-driven integration fixture from the independent Wave 0 corpus and typed source adapters. Assert all ten family IDs, strategy descriptions, profile JSON, thresholds, parent hashes, source digests, candidate IDs, and factory construction; compare repeated serialization byte-for-byte. Add negative cases for every unsupported/unsafe input and a causal test proving a changed escape/falsifier changes the child rather than merely shifting a timestamp.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test synthesis_integration detector_candidates_materialize_all_ten_families -- --exact</automated>
  </verify>
  <done>The detector synthesis path is exercised end-to-end for all ten supported families and cannot survive on descriptive, stale, raw, or unbound evidence.</done>
</task>

</tasks>

<verification>
Run the three exact detector tests, the complete typed mutation suite, `cargo clippy -p swarm-runtime --all-targets -- -D warnings`, format, and diff checks. Inspect that no source adapter accepts withheld content or raw telemetry and no candidate path uses `EvolutionMutationProfileOverrides` as a general fallback.
</verification>

<success_criteria>
- Graph gaps, arena escapes, and falsifiers are all valid typed synthesis causes with digest-verified evidence.
- Ten-family detector candidates round-trip, materialize, and construct through the real factory with canonical reproducible lineage.
- No unrestricted code generation, cross-family merge, raw telemetry, stale reference, or legacy override can produce a candidate.
</success_criteria>

<output>After completion, create `.planning/phases/288-autonomous-detector-response-synthesis/288-02-SUMMARY.md` with candidate lineage examples, ten-family evidence, and rejected-input results.</output>
