---
phase: 288-autonomous-detector-response-synthesis
plan: 05
type: execute
wave: 6
depends_on: [288-04]
files_modified:
  - crates/swarm-runtime/src/synthesis/mod.rs
  - crates/swarm-runtime/src/synthesis/evaluation.rs
  - crates/swarm-runtime/src/synthesis/controls.rs
  - crates/swarm-runtime/tests/synthesis_controls.rs
autonomous: true
requirements: [SYNTH-04]

must_haves:
  truths:
    - "Candidate-rule removal, source-adapter swap, expected-verdict mutation, event metamorphic transforms, and candidate-profile mutations are explicit typed controls over the same replay verifier."
    - "Every control records before/after canonical input digests, the intended relation, observed relation, source/corpus digests, documented regression, and pass/block result."
    - "A no-op mutation, unchanged expected input, stale/tampered fixture, or unobserved documented regression blocks the candidate before any score can be accepted."
  artifacts:
    - path: "crates/swarm-runtime/src/synthesis/controls.rs"
      provides: "Typed mutation/differential/metamorphic control runner"
      min_lines: 260
    - path: "crates/swarm-runtime/tests/synthesis_controls.rs"
      provides: "Adversarial control and no-op/tamper tests"
  key_links:
    - from: "crates/swarm-runtime/src/synthesis/controls.rs"
      to: "crates/swarm-runtime/src/synthesis/evaluation.rs"
      via: "controls invoke the same paired four-lane replay/evaluation path"
      pattern: "SynthesisEvaluation"
    - from: "crates/swarm-runtime/src/synthesis/controls.rs"
      to: "crates/swarm-crypto/src/lib.rs"
      via: "before/after bytes use canonical JSON SHA-256 digests"
      pattern: "canonical_json_bytes"
---

<objective>
Prove detector/response candidate gains are not artifacts of a weakened oracle, changed adapter semantics, mutated expected verdicts, or no-op controls.

Purpose: a candidate must survive independent adversarial controls over its input, source adapter, expected outcome, event representation, and profile. Controls must fail closed when the mutation did not actually happen or when the expected regression is absent.
Output: typed control records, deterministic before/after digests, documented relation evaluation, and mutation/differential/metamorphic tests integrated into candidate admission.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-CONTEXT.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-RESEARCH.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-04-PLAN.md
@crates/swarm-runtime/src/synthesis/contracts.rs
@crates/swarm-runtime/src/synthesis/evaluation.rs
@crates/swarm-runtime/src/replay/verification.rs
@crates/swarm-runtime/src/replay/metrics.rs
@crates/swarm-runtime/tests/negative_runtime_fail_closed.rs

<interfaces>
The four-lane evaluator accepts immutable corpus references and paired baseline/candidate inputs, and its reports carry explicit detector/response metrics and work-model digests. The Wave 0 `ControlResult` contract requires a control kind, canonical before/after input digests, expected/observed relation, documented regression, source digests, and a blocking status. Controls must call the same real replay/evaluation path; they are not independent score shortcuts.</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement typed control mutations with no-op rejection</name>
  <files>crates/swarm-runtime/src/synthesis/mod.rs, crates/swarm-runtime/src/synthesis/controls.rs, crates/swarm-runtime/tests/synthesis_controls.rs</files>
  <behavior>
    - Candidate-rule removal changes the candidate/profile input and records the expected detection/evidence regression; source-adapter swap records an approved equivalence or documented regression; expected-verdict mutation reaches the verifier and blocks when the relation is not valid.
    - Event order/duplicate/normalization metamorphic transformations declare equality, subset, or bounded-delta relations and reject unexplained changes; candidate-profile mutation changes canonical profile bytes before evaluation.
    - Every control rejects `before_digest == after_digest`, absent source/corpus digests, mutation outside the run-owned copy, or a transformation that cannot be shown to have changed its input.
  </behavior>
  <action>Write red tests for each control kind, then implement deterministic typed transformations over cloned candidate/corpus/expected-verdict inputs. Use canonical JSON and SHA-256 for before/after bytes; record the exact changed field/fixture, source adapter/schema, expected relation, and virtual-time/seed. Candidate-rule removal must target the synthesized rule/feature, source-adapter swap must use an explicitly admitted equivalent or report the expected regression, and event metamorphic controls must preserve the declared semantic class. Never mutate the independent truth files, withheld handle, policy configuration, or response authority.</action>
  <verify>
    <automated>cargo test -p swarm-runtime synthesis_controls_mutations_change_input_digest --lib</automated>
  </verify>
  <done>All five control classes produce typed, digest-addressed mutations and no-op or out-of-scope mutation attempts fail before scoring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Evaluate documented regressions and block oracle weakening</name>
  <files>crates/swarm-runtime/src/synthesis/evaluation.rs, crates/swarm-runtime/src/synthesis/controls.rs, crates/swarm-runtime/tests/synthesis_controls.rs</files>
  <behavior>
    - A real rule removal that loses its claimed detection/evidence relation is observed and recorded; an unchanged candidate, weakened verifier, or mutated expected verdict that still reports success is blocked.
    - Source-adapter equivalence is checked against identical replay inputs and work-model/corpus digests; unexplained divergence or equivalence claims without changed adapter/input bytes block.
    - Controls run before the 10% improvement decision and their failures cannot be hidden inside aggregate metrics or withheld results.
  </behavior>
  <action>Integrate control execution into synthesis evaluation after candidate lineage is frozen and before threshold admission. Run each mutated input through the same historical/benign/counterexample path (and withhold only where the control contract permits, without feedback), compare the declared relation to the observed detector/response evidence, and return a blocking control report when a documented regression is absent. Add differential fixtures for removed rule, swapped source adapter, expected-verdict tamper, and candidate-profile mutation; add metamorphic fixtures for event order, duplicate events, and normalized equivalent representation. Ensure the verifier itself is covered by a mutation test so deleting a control or switching to a permissive oracle fails.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test synthesis_controls differential_and_metamorphic_controls_fail_closed -- --exact</automated>
  </verify>
  <done>Candidate admission requires real documented control relations and blocks oracle weakening, source drift, expected-verdict tampering, and unobserved mutations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add tamper, stale-lineage, and before/after audit regressions</name>
  <files>crates/swarm-runtime/tests/synthesis_controls.rs</files>
  <behavior>
    - Tampering a source/corpus/adapter digest, reusing a stale control result, changing a control kind, or replaying a prior sequence is rejected before relation evaluation.
    - Removing the before/after inequality assertion, changing a declared expected relation, or omitting a control from the report is caught by the exact test/gate.
    - A clean run emits stable control report bytes across repeated executions with the same seed and corpus.
  </behavior>
  <action>Complete integration tests around the typed controls and signed/digest-addressed corpus references. Mutate every control-result field that could turn a failed regression into a pass, then assert fail-closed behavior; compare repeated reports byte-for-byte excluding only explicitly observational wall-clock fields. Keep all controls bounded by the Wave 0 work budget and ensure no test is ignored, filtered to zero, or satisfied by a hand-written boolean.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test synthesis_controls no_op_or_tampered_control_blocks -- --exact</automated>
  </verify>
  <done>Controls are auditable, reproducible, mutation-resistant, and mandatory for candidate admission.</done>
</task>

</tasks>

<verification>
Run all three exact control tests, the four-lane evaluator, relevant replay verification tests, clippy, format, and diff checks. Confirm every control has distinct before/after digests and that wall-clock observations are never used to declare a control relation or candidate verdict.
</verification>

<success_criteria>
- Candidate-rule, adapter, expected-verdict, event-metamorphic, and profile mutations all run through the real verifier.
- No-op, stale, tampered, unexplained, or oracle-weakened controls block admission.
- The phase report contains typed control evidence with stable input digests and documented observed relations.
</success_criteria>

<output>After completion, create `.planning/phases/288-autonomous-detector-response-synthesis/288-05-SUMMARY.md` with each control's before/after digest, expected/observed relation, and blocking evidence.</output>
