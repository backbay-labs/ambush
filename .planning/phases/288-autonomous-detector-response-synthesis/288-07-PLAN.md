---
phase: 288-autonomous-detector-response-synthesis
plan: 07
type: execute
wave: 8
depends_on: [288-06]
files_modified:
  - crates/swarm-runtime/tests/synthesis_phase_gate.rs
  - tools/check-autonomous-detector-response-synthesis.sh
  - .planning/phases/288-autonomous-detector-response-synthesis/288-VALIDATION.md
  - .planning/phases/288-autonomous-detector-response-synthesis/288-P0-P2-REVIEW.md
  - .planning/phases/288-autonomous-detector-response-synthesis/288-VERIFICATION.md
autonomous: true
requirements: [SYNTH-01, SYNTH-02, SYNTH-03, SYNTH-04, SYNTH-05, SYNTH-06]

must_haves:
  truths:
    - "The phase gate runs every exact synthesis, four-lane, control, authority, packet, and handoff test with nonzero/exact execution counts; a missing or renamed test cannot pass."
    - "The final report contains all four lane results, separate detector/response deltas, deterministic work/resource evidence, observational wall-clock labels, every control relation, packet signature/digest lineage, Proved solver state, canary/operator/quorum decision, and retained baseline."
    - "The combined workspace gate is green only with all thresholds and negative controls intact; no isolated local pass is presented as hosted/release evidence."
  artifacts:
    - path: "crates/swarm-runtime/tests/synthesis_phase_gate.rs"
      provides: "Exact non-vacuous phase integration/gate assertions"
    - path: "tools/check-autonomous-detector-response-synthesis.sh"
      provides: "Final exact test/count/schema/mutation/full-evidence gate"
    - path: ".planning/phases/288-autonomous-detector-response-synthesis/288-VALIDATION.md"
      provides: "Nyquist task map and truthful execution status with wave_0_complete false"
    - path: ".planning/phases/288-autonomous-detector-response-synthesis/288-P0-P2-REVIEW.md"
      provides: "Independent severity review with explicit zero-open counters"
    - path: ".planning/phases/288-autonomous-detector-response-synthesis/288-VERIFICATION.md"
      provides: "Goal-backward SYNTH-01..06 verification linked to executable evidence"
  key_links:
    - from: "tools/check-autonomous-detector-response-synthesis.sh"
      to: "crates/swarm-runtime/tests/synthesis_phase_gate.rs"
      via: "exact named test and execution-count assertion"
      pattern: "synthesis_phase_gate"
    - from: "tools/check-autonomous-detector-response-synthesis.sh"
      to: "288-VALIDATION.md"
      via: "gate checks all mapped tasks, thresholds, lane fields, and negative controls"
      pattern: "wave_0_complete"
    - from: ".planning/phases/288-autonomous-detector-response-synthesis/288-P0-P2-REVIEW.md"
      to: ".planning/phases/288-autonomous-detector-response-synthesis/288-VERIFICATION.md"
      via: "zero-open independent review is required before requirement verification can pass"
      pattern: "open_p0|open_p1|open_p2"
---

<objective>
Close Phase 288 with an exact, mutation-controlled, full combined-tree gate and truthful validation artifact.

Purpose: the phase is complete only when all synthesis behavior, safety boundaries, controls, packet/handoff gates, and deterministic reports execute together. A local green subset, zero-test match, or mutable passed flag is insufficient.
Output: final exact integration assertions, complete gate wiring, repeatability evidence, full workspace/clippy/format/diff validation, and status updates that preserve `wave_0_complete: false` as required by the planning contract.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-CONTEXT.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-RESEARCH.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-VALIDATION.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-00-PLAN.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-06-PLAN.md
@tools/check-gates-wired.sh
@tools/check-workspace-layering.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the exact non-vacuous synthesis phase gate</name>
  <files>crates/swarm-runtime/tests/synthesis_phase_gate.rs, tools/check-autonomous-detector-response-synthesis.sh</files>
  <behavior>
    - The gate runs the named strict-contract, four-lane, ten-family, response-authority, replay/metric, control, packet, handoff, and phase integration tests and asserts each matched/executed exactly as declared.
    - The gate validates required report fields, explicit denominators/thresholds, four-lane disjointness, deterministic work-model version, separate detector/response metrics, control before/after inequality, packet/solver/canary/operator/quorum/baseline evidence, and wall-clock observational labeling.
    - Gate self-mutations for zero tests, test rename/ignore, field/threshold removal, oracle weakening, authority leakage, no-op control, tampered packet, non-Proved solver, missing quorum, and auto-promotion all fail.
  </behavior>
  <action>Write the phase-level integration assertions first, then finalize the Wave 0 checker to invoke exact test names with nonzero/exact execution counts and parse the deterministic report/packet schema rather than grep a passing boolean. Chain the clean/broken authority fixtures, all mutation/differential/metamorphic controls, four-lane and withheld/counterexample gates, signed packet revalidation, real assurance/solver/canary/promotion handoff, and retained-baseline assertion. Keep the gate credential-free and bounded; never soften it for missing files, use an aggregate score, or accept a missing/Disabled solver. Do not modify roadmap or requirements, and keep the validation frontmatter `wave_0_complete: false`.</action>
  <verify>
    <automated>bash tools/check-autonomous-detector-response-synthesis.sh --self-test && cargo test -p swarm-runtime --test synthesis_phase_gate -- --test-threads=1</automated>
  </verify>
  <done>The dedicated phase gate is exact, non-vacuous, mutation-tested, authority-aware, and exercises all SYNTH-01..06 acceptance evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Run deterministic repeatability and the full combined-tree gate</name>
  <files>.planning/phases/288-autonomous-detector-response-synthesis/288-VALIDATION.md</files>
  <action>Run the dedicated gate twice with the same fixed corpus/seed/signer and compare canonical report/packet/control bytes, allowing only explicitly labeled observational wall-clock fields to differ. Then run the workspace all-targets locked tests, all-features clippy with `-D warnings`, format check, layering/authority checks, and `git diff --check`. Update only the per-task statuses/evidence in the validation document from actual commands; preserve `nyquist_compliant: true`, `wave_0_complete: false`, and truthful pending/blocked notes for anything not run. Do not infer hosted/release correctness from this local combined-tree result and do not edit roadmap, requirements, research, or production code in this task.</action>
  <verify>
    <automated>bash tools/check-autonomous-detector-response-synthesis.sh && bash tools/check-autonomous-detector-response-synthesis.sh && cargo test --workspace --all-targets --locked && cargo clippy --workspace --all-targets --all-features -- -D warnings && cargo fmt --all -- --check && bash tools/check-workspace-layering.sh && bash tools/check-gates-wired.sh && git diff --check</automated>
  </verify>
  <done>Two identical deterministic runs and the full combined-tree quality gate pass, the validation map reflects actual evidence, and no unsupported hosted/release claim or Wave 0 flag change is introduced.</done>
</task>

<task type="auto">
  <name>Task 3: Require independent P0/P1/P2 closure and goal-backward verification</name>
  <files>.planning/phases/288-autonomous-detector-response-synthesis/288-P0-P2-REVIEW.md, .planning/phases/288-autonomous-detector-response-synthesis/288-VERIFICATION.md</files>
  <action>After Tasks 1 and 2 are green, the Plan 07 implementer must not self-approve. The root orchestrator assigns a separate reviewer to inspect the complete combined Phase 288 diff, four-lane oracle ownership, withheld capability isolation, ten-family closure, advisory-response authority boundary, metric thresholds, mutation/differential/metamorphic controls, signed packet lineage, assurance/Proved/canary/operator/quorum handoff, baseline retention, and exact gate evidence. Record every P0/P1/P2 finding and disposition. Link SYNTH-01..06 to exact executable evidence in `288-VERIFICATION.md`. The phase remains open unless both artifacts exist and `open_p0`, `open_p1`, and `open_p2` all equal zero.</action>
  <verify>
    <automated>bash tools/check-autonomous-detector-response-synthesis.sh && python3 -c 'import pathlib,re; text="\n".join(pathlib.Path(p).read_text() for p in [".planning/phases/288-autonomous-detector-response-synthesis/288-P0-P2-REVIEW.md", ".planning/phases/288-autonomous-detector-response-synthesis/288-VERIFICATION.md"]); assert all(re.search(rf"{key}:\s*0\b", text) for key in ("open_p0", "open_p1", "open_p2"))' && git diff --check</automated>
  </verify>
  <done>An independent combined-tree review has zero open P0/P1/P2 issues and goal-backward verification links every SYNTH requirement to executed evidence.</done>
</task>

</tasks>

<verification>
Run the gate self-test and exact phase integration test, run the final gate twice and compare report/packet digests, then run the full workspace tests, clippy, format, layering/authority checks, and diff check. Confirm all required negative controls fail when mutated and all positive paths remain non-vacuous.
</verification>

<success_criteria>
- SYNTH-01..SYNTH-06 have exact executable evidence, including four-lane replay, separated metrics, controls, signed packets, Proved solver, canary, operator/quorum, and retained baseline.
- The phase gate cannot pass from missing tests, removed fields/thresholds, weakened oracle, no-op mutation, stale/tampered lineage, response authority, non-Proved solver, or auto-promotion.
- Full combined-tree evidence is truthful and `288-VALIDATION.md` retains `wave_0_complete: false`.
- Independent review and verification artifacts exist with `open_p0: 0`, `open_p1: 0`, and `open_p2: 0`.
</success_criteria>

<output>After completion, create `.planning/phases/288-autonomous-detector-response-synthesis/288-07-SUMMARY.md`, `288-P0-P2-REVIEW.md`, and `288-VERIFICATION.md`; record final evidence without changing roadmap/requirements ownership until independent closure passes.</output>
