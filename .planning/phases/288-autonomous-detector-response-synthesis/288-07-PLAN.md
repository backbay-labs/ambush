---
phase: 288-autonomous-detector-response-synthesis
plan: 07
type: execute
wave: 9
depends_on: [288-06]
files_modified:
  - crates/swarm-runtime/tests/synthesis_phase_gate.rs
  - tools/check-autonomous-detector-response-synthesis.sh
  - .planning/phases/288-autonomous-detector-response-synthesis/288-VALIDATION.md
  - .planning/phases/288-autonomous-detector-response-synthesis/288-P0-P2-REVIEW.md
  - .planning/phases/288-autonomous-detector-response-synthesis/288-VERIFICATION.md
autonomous: true
requirements: [SYNTH-01, SYNTH-02, SYNTH-03, SYNTH-04, SYNTH-05, SYNTH-06]

must_haves:
  truths:
    - "The final gate executes every Phase 288 exact task/test and every Phase 287 config inventory, parser, isolation, oracle, and exact Arena suite on the same combined tree with nonzero/ignored-zero counts."
    - "Two fixed-seed/signer runs compare canonical report, packet, control, lineage, and pair bytes mechanically, allowing only explicitly labeled wall-clock observations."
    - "Independent per-artifact review and verification bind current HEAD/tree, disjoint owner/reviewer, exact structured rows, all six requirements, and zero open P0/P1/P2; response authority and baseline replacement remain impossible."
    - "The final evidence remains local combined-tree evidence only; no workflow banner, isolated branch, hosted result, or release claim closes the phase."
  artifacts:
    - path: "crates/swarm-runtime/tests/synthesis_phase_gate.rs"
      provides: "Exact non-vacuous Phase 288 integration assertions"
    - path: "tools/check-autonomous-detector-response-synthesis.sh"
      provides: "Full Phase 287 + Phase 288 gate, mutation, parser, and repeatability wiring"
    - path: ".planning/phases/288-autonomous-detector-response-synthesis/288-P0-P2-REVIEW.md"
      provides: "Independent per-artifact severity rows and zero-open counters"
    - path: ".planning/phases/288-autonomous-detector-response-synthesis/288-VERIFICATION.md"
      provides: "Goal-backward SYNTH-01..06 evidence map bound to exact commands"
  key_links:
    - from: "tools/check-autonomous-detector-response-synthesis.sh"
      to: "tools/check-arena-corpus-truth.py|tools/check-arena-config-inventory.sh|tools/check-adversarial-coevolution-arena.sh|tools/parse-arena-report.py"
      via: "Phase 287 gates/parsers/config inventory run in combined tree before Phase 288 acceptance"
      pattern: "check-arena-corpus-truth|check-arena-config-inventory|check-adversarial-coevolution-arena|parse-arena-report"
    - from: "tools/check-autonomous-detector-response-synthesis.sh"
      to: "tools/parse-synthesis-review.py|tools/compare-synthesis-artifacts.py"
      via: "independent exact review parser and mechanical canonical-byte comparison"
      pattern: "current-head|combined-tree|compare-synthesis-artifacts|open_p0"
    - from: ".planning/phases/288-autonomous-detector-response-synthesis/288-VERIFICATION.md"
      to: ".planning/phases/288-autonomous-detector-response-synthesis/288-P0-P2-REVIEW.md"
      via: "same current-head/tree/evidence digest and independent reviewer"
      pattern: "SYNTH-01|head_oid|combined_tree_digest|reviewer"
---

<objective>
Close Phase 288 only with an exact, mutation-controlled, combined-tree gate
that consumes the real Phase 287 Arena interfaces and proves every SYNTH
requirement without response authority or unsupported evidence.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-CONTEXT.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-00-PLAN.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-00A-PLAN.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-01-PLAN.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-02-PLAN.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-03-PLAN.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-04-PLAN.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-05-PLAN.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-06-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-CONTEXT.md
@.planning/phases/287-adversarial-co-evolution-arena/287-00I-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-02-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-03-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-04-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-05-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-06-PLAN.md
@tools/check-gates-wired.sh
@tools/check-workspace-layering.sh
</context>

<interfaces>
The gate must prove actual Phase 287 evidence: `BlueRuntimeAdapter::run` with
candidate and `SignedBlueLearnedState::empty_frozen()` control, the six-argument
Phase 286 stack, `ArenaIngestResult`/`Phase286InvestigationCapture`,
`ArenaArtifactStore`, `PairReport`, and full signed-envelope validation. It must
run Phase 287's current `tools/check-arena-config-inventory.sh`,
`tools/check-arena-oracles.sh --self-test`,
`tools/check-arena-isolation.sh`, `tools/check-adversarial-coevolution-arena.sh`,
`tools/check-arena-corpus-truth.py --self-test` and `--check`,
`tools/parse-arena-report.py`, and `tools/check-phase287-review.py`, plus the
exact Arena suites `arena_corpus_oracles`, `blue_runtime`, `blue_safety`,
`candidate_lineage`, `evaluation_partitions`, `signed_artifacts`,
`adversarial_coevolution_arena`, and `arena_teardown`. The final inventory also
invokes the exact Red isolation test
`cargo test -p swarm-arena-red --test arena_red_isolation --locked --offline -- --exact red_capability_boundary_is_nonvacuous`; it must observe one
matched/executed/passed test and `ignored == 0`, and absence, zero matches, or a
non-running test is a failure. Missing/renamed/zero or ignored tests,
static/generic replay, stale lineage, or isolated evidence fails.

The exact run gate CLI is (and must remain identical for `run-2` after replacing
the run ID and artifact directory):

```text
bash tools/check-autonomous-detector-response-synthesis.sh \
  --run-id run-1 \
  --artifact-dir artifacts/synthesis/run-1 \
  --output-manifest artifacts/synthesis/run-1/manifest.json \
  --tree-scope . \
  --require-current-head \
  --require-frozen-tree
```

`--run-id` is nonempty and matches `[a-z0-9-]+`; `--artifact-dir` is a clean,
run-owned child of the configured Phase 287 `SwarmConfig::arena.run_root`, is
not the source or tree scope, and cannot overlap another run. The
`--output-manifest` path is exactly under that artifact directory. `--tree-scope
.` means repository root plus the locked `phase288-implementation-allowlist-v1`
below, not recursive inclusion: `crates/swarm-runtime/src/lib.rs`,
`crates/swarm-runtime/src/synthesis/**`, the typed mutation sources/tests,
pure-preview sources/tests, the named Phase 288 Runtime integration tests,
`crates/swarm-arena/src/{lib.rs,synthesis_adapter.rs}`, the three Arena
synthesis tests, the four lane files plus `manifest.yaml`, and the five owned
Phase 288 gate/parser
sources. The tree helper excludes generated/self-referential
reports, review/verification/validation/summary documents, all run roots,
`.git`, `target`, caches, symlinks, and worktrees, and rejects any path outside
the allowlist. `--require-current-head` binds `git rev-parse --verify HEAD` and
`--require-frozen-tree` binds the canonical sorted tree digest plus the
immutable `phase287_evidence_digest` over the exact three retained Phase 287
final-gate JSON bytes defined below; corpus-truth, config-inventory, and
review/parser commands are independently rerun and their paths/commands are
bound as evidence, not silently substituted for those retained bytes.

Each manifest is schema `phase288-synthesis-run-v1` and must contain `run_id`,
`artifact_dir`, `owner_id`, `tree_scope`, `tree_digest` (the canonical
implementation-tree digest, copied verbatim as `combined_tree_digest` in both
review artifacts), `head_oid`,
`phase287_evidence_digest`, the canonical paths for `arena_report`,
`synthesis_packet`, `controls`, `pair_report_view`, `review`, and
`verification`, an exhaustive `owned_paths` list, and explicit
`observation_only_paths`, plus an `ownership` object with
`arena_producer=swarm-arena`, `runtime_consumer=swarm-runtime`,
`run_owner=phase288-gate`, and `independent_reviewer`. It must state that
concrete Blue runs, CAS, and `PairReport` are Arena-owned, Runtime owns only
DTO views, and generated output paths are excluded from the implementation
digest.

The required path values are deterministic: for `run-1`,
`arena_report=artifacts/synthesis/run-1/arena-report.signed.json`,
`synthesis_packet=artifacts/synthesis/run-1/synthesis-packet.signed.json`,
`controls=artifacts/synthesis/run-1/controls.json`, and
`pair_report_view=artifacts/synthesis/run-1/pair-report-view.json`;
`review=.planning/phases/288-autonomous-detector-response-synthesis/288-P0-P2-REVIEW.md`;
`verification=.planning/phases/288-autonomous-detector-response-synthesis/288-VERIFICATION.md`.
`run-2`
uses the same filenames below `artifacts/synthesis/run-2`. `owned_paths` must
contain only that run's manifest/report/packet/control/view and explicitly
declared observation files; the review/verification paths are
`observation_only_paths`, not owned run outputs, and no other run, source, or
generated path may be claimed by the manifest.

The retained Phase 287 evidence paths and review-parser invocation are fixed.
The gate must pass the exact `--final-report
artifacts/phase287/final-gate/arena-report.json`, `--final-lineage
artifacts/phase287/final-gate/arena-lineage.json`, `--final-evidence-manifest
artifacts/phase287/final-gate/final-gate-evidence.json`, and
`--final-gate-evidence-digest "$FINAL_GATE_DIGEST"` arguments to
`python3 tools/check-phase287-review.py`, together with the retained Phase 287
review/verification/validation paths and plans directory. Here
`FINAL_GATE_DIGEST` is read from the manifest's
`final_gate_evidence_digest`, which must equal the Phase 287 canonical digest
`SHA256(canonical_json_bytes(arena-report.json) || "\n" ||
canonical_json_bytes(arena-lineage.json))`. Phase 288 defines
`phase287_evidence_digest` as
`SHA256(canonical_json_bytes(arena-report.json) || "\n" ||
canonical_json_bytes(arena-lineage.json) || "\n" ||
canonical_json_bytes(final-gate-evidence.json))`; the manifest, packet,
signed report, and both review artifacts must carry this value and the gate
must recompute it from those three retained bytes. Canonical JSON bytes use
UTF-8, sorted object keys, no insignificant whitespace, and reject duplicate
keys; missing, replaced, altered, or noncanonical retained bytes fail closed.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire exact Phase 287 and Phase 288 non-vacuous gates</name>
  <files>crates/swarm-runtime/tests/synthesis_phase_gate.rs, tools/check-autonomous-detector-response-synthesis.sh</files>
  <behavior>
    - The gate observes exact named Phase 288 tests for strict contracts, lane/oracle, ten-family typed genomes, concrete Arena adapter, response authority, four-lane metrics, controls, packet/handoff, and phase closure, requiring declared nonzero/exact counts and `ignored == 0`.
    - It runs the Phase 287 config inventory, oracle self-test, red isolation self-test and normal command, independent signed report parser/review parser, arena gate, and all eight exact Arena suites on the combined tree; missing or empty files are failures. The Phase 287 review parser receives all four explicit final-evidence arguments and the recomputed retained-path digest, not a generic or omitted evidence reference.
    - Its final inventory invokes `cargo test -p swarm-arena-red --test arena_red_isolation --locked --offline -- --exact red_capability_boundary_is_nonvacuous` and independently parses a nonzero matched/executed/passed count with `ignored == 0`; absent, zero-match, or non-running Red isolation evidence fails.
    - Any mutation, deletion, replacement, noncanonical rewrite, or digest change in the three retained Phase 287 final-gate JSON files makes `final_gate_evidence_digest` or `phase287_evidence_digest` mismatch and fails the parser/gate.
    - It validates all report fields/denominators/thresholds, actual `PairReport`/empty-frozen control, full signed envelope, mutation/differential/metamorphic controls, Proved solver/canary/operator/quorum/baseline, no response authority, and no wall-clock verdict.
  </behavior>
  <action>Write integration assertions first. Parse and enforce the exact run CLI `--run-id run-1 --artifact-dir artifacts/synthesis/run-1 --output-manifest artifacts/synthesis/run-1/manifest.json --tree-scope . --require-current-head --require-frozen-tree` (and the identical run-2 form), including configured run-root ownership, the frozen `phase288-implementation-allowlist-v1`, manifest paths/owner/tree/evidence fields, and the positional signed-report command `python3 tools/parse-arena-report.py artifacts/synthesis/run-1/arena-report.signed.json`. Then make the credential-free gate read current Phase 287 registry/parser contracts and invoke `python3 tools/check-arena-corpus-truth.py --self-test && python3 tools/check-arena-corpus-truth.py --check`, `check-arena-config-inventory.sh`, `check-arena-oracles.sh --self-test`, `CARGO_NET_OFFLINE=true bash tools/check-arena-isolation.sh --self-test` followed by normal `CARGO_NET_OFFLINE=true bash tools/check-arena-isolation.sh`, `check-adversarial-coevolution-arena.sh --self-test` and normal mode, and `parse-arena-report.py` on the signed combined-tree report. Set `FINAL_GATE_DIGEST=$(python3 -c 'import json; print(json.load(open("artifacts/phase287/final-gate/final-gate-evidence.json"))["final_gate_evidence_digest"])')`, then invoke `python3 tools/check-phase287-review.py --review-file .planning/phases/287-adversarial-co-evolution-arena/287-P0-P2-REVIEW.md --verification-file .planning/phases/287-adversarial-co-evolution-arena/287-VERIFICATION.md --validation-file .planning/phases/287-adversarial-co-evolution-arena/287-VALIDATION.md --plans-dir .planning/phases/287-adversarial-co-evolution-arena --final-report artifacts/phase287/final-gate/arena-report.json --final-lineage artifacts/phase287/final-gate/arena-lineage.json --final-evidence-manifest artifacts/phase287/final-gate/final-gate-evidence.json --final-gate-evidence-digest "$FINAL_GATE_DIGEST"`; recompute and bind `phase287_evidence_digest` from those three exact retained canonical bytes. Then run `python3 tools/check-phase288-validation-map.py --plans-dir .planning/phases/288-autonomous-detector-response-synthesis --validation .planning/phases/288-autonomous-detector-response-synthesis/288-VALIDATION.md --require-wave-0-pending --require-exact-task-rows` as the corpus/plan truth command, before Phase 288 tests. Run each exact suite with its literal command: `cargo test -p swarm-arena --test arena_corpus_oracles --locked --offline -- --test-threads=1`, `cargo test -p swarm-arena --test blue_runtime --locked --offline -- --test-threads=1`, `cargo test -p swarm-arena --test blue_safety --locked --offline -- --test-threads=1`, `cargo test -p swarm-arena --test candidate_lineage --locked --offline -- --test-threads=1`, `cargo test -p swarm-arena --test evaluation_partitions --locked --offline -- --test-threads=1`, `cargo test -p swarm-arena --test signed_artifacts --locked --offline -- --test-threads=1`, `cargo test -p swarm-arena --test adversarial_coevolution_arena --locked --offline -- --test-threads=1`, and `cargo test -p swarm-arena --test arena_teardown --locked --offline -- --test-threads=1`; independently require the registry's named test counts to be nonzero with `ignored == 0`. Parse matched/executed/passed/ignored counts, strict reports, Arena evidence, and canonical source/tree provenance; reject zero/renamed/duplicate tests, missing/extra fields, generic/static replay, response/policy/dispatch authority, empty denominators, weakened thresholds, no-op controls, mutable withheld feedback, missing signed fields, absent/disabled/malformed/non-Proved solver invariant artifact, missing quorum, and auto-promotion. Use bounded temporary roots and no network, credentials, live target, deploy, broad deletion, or `|| true`.</action>
  <verify><automated>bash -n tools/check-autonomous-detector-response-synthesis.sh && bash tools/check-autonomous-detector-response-synthesis.sh --self-test && cargo test -p swarm-runtime --test synthesis_phase_gate --locked --offline -- --exact phase_gate_requires_real_arena_evidence && git diff --check</automated></verify>
  <done>The gate is exact, mutation-tested, Phase 287-complete, authority-aware, and cannot pass on missing/isolated/generic evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Repeat the combined-tree run and compare canonical bytes mechanically</name>
  <files>.planning/phases/288-autonomous-detector-response-synthesis/288-VALIDATION.md</files>
  <behavior>
    - Two runs use the same corpus/partition/config/ruleset, seed, virtual clock, signer, candidate, and `empty_frozen()` control; canonical report, packet, control, source-lineage, Runtime `PairReportView`/artifact-view bytes compare exactly while concrete Blue/CAS/`PairReport` bytes remain Arena-owned evidence.
    - Only explicitly labeled observational wall-clock fields may differ; changed candidate/control/evidence/packet/tree bytes, missing artifacts, or changed current HEAD/tree fail.
    - Full combined-tree tests, clippy, format, layering, negative registry, fixture freshness, panic contract, gate wiring, and diff check run after the repeated Phase 287/288 gate.
  </behavior>
  <action>Run the exact gate twice with fixed inputs using these complete commands: <code>bash tools/check-autonomous-detector-response-synthesis.sh --run-id run-1 --artifact-dir artifacts/synthesis/run-1 --output-manifest artifacts/synthesis/run-1/manifest.json --tree-scope . --require-current-head --require-frozen-tree</code> and the same command with <code>run-2</code> paths. Require each manifest to list ownership, canonical artifact paths, current HEAD, frozen implementation-tree digest, and immutable Phase 287 evidence digest. Invoke <code>python3 tools/parse-arena-report.py artifacts/synthesis/run-1/arena-report.signed.json</code> and the run-2 equivalent with the signed report as a required positional argument, then <code>python3 tools/compare-synthesis-artifacts.py --first artifacts/synthesis/run-1 --second artifacts/synthesis/run-2 --allow-observation-fields</code> and require byte equality for every deterministic report, packet, control, pair-view, source-lineage, and manifest field. Then run all named Phase 287 gates/suites, workspace all-targets locked/offline tests, all-features clippy <code>-D warnings</code>, format, layering, negative-registry, fixture freshness, panic contract, gate wiring, and diff checks. Record only actual combined-tree evidence in validation; preserve <code>nyquist_compliant: true</code> and <code>wave_0_complete: false</code> until the execution workflow records Wave 0.</action>
  <verify><automated>bash tools/check-autonomous-detector-response-synthesis.sh --run-id run-1 --artifact-dir artifacts/synthesis/run-1 --output-manifest artifacts/synthesis/run-1/manifest.json --tree-scope . --require-current-head --require-frozen-tree && bash tools/check-autonomous-detector-response-synthesis.sh --run-id run-2 --artifact-dir artifacts/synthesis/run-2 --output-manifest artifacts/synthesis/run-2/manifest.json --tree-scope . --require-current-head --require-frozen-tree && python3 tools/parse-arena-report.py artifacts/synthesis/run-1/arena-report.signed.json && python3 tools/parse-arena-report.py artifacts/synthesis/run-2/arena-report.signed.json && python3 tools/compare-synthesis-artifacts.py --first artifacts/synthesis/run-1 --second artifacts/synthesis/run-2 --allow-observation-fields && cargo test --workspace --all-targets --locked --offline && cargo clippy --workspace --all-targets --all-features --locked --offline -- -D warnings && cargo fmt --all -- --check && bash tools/check-workspace-layering.sh && bash tools/check-negative-registry.sh && bash tools/check-fixture-freshness.sh && bash tools/check-runtime-panic-contract.sh && bash tools/check-gates-wired.sh && git diff --check</automated></verify>
  <done>Two fixed-input combined-tree runs have mechanically identical canonical evidence, all full-suite gates pass, and validation contains no unsupported hosted/release claim.</done>
</task>

<task type="auto">
  <name>Task 3: Require independent exact per-artifact P0/P1/P2 closure</name>
  <files>.planning/phases/288-autonomous-detector-response-synthesis/288-P0-P2-REVIEW.md, .planning/phases/288-autonomous-detector-response-synthesis/288-VERIFICATION.md, .planning/phases/288-autonomous-detector-response-synthesis/288-VALIDATION.md</files>
  <behavior>
    - A reviewer who did not author the implementation inspects the complete combined Phase 288 diff and exact Phase 287 evidence, including concrete Blue/Arena bridge, signed envelope, typed genomes, four lanes, controls, response authority, solver/canary/operator/quorum, and baseline retention.
    - `parse-synthesis-review.py` requires exactly one structured `review_row` for every fixed key `task:288-00-01..03`, `task:288-00A-01..03`, `task:288-01-01..03`, `task:288-02-01..03`, `task:288-03-01..03`, `task:288-04-01..03`, `task:288-05-01..03`, `task:288-06-01..03`, and `task:288-07-01..03`, `requirement:SYNTH-01..06`, and `artifact:288-P0-P2-REVIEW.md` plus `artifact:288-VERIFICATION.md`, with no missing or duplicate key; it also requires exactly one anchored `open_p0: 0`, `open_p1: 0`, and `open_p2: 0` in each artifact; current HEAD/tree binding; distinct owner/reviewer; and matching Phase 287 evidence digest.
    - Parser mutations for duplicate/missing/nonzero counters/rows, stale tree/head, owner=reviewer, missing evidence, prose-only counters, isolated-only output, and changed canonical bytes all fail.
  </behavior>
  <action>After Task 2 is green, assign an independent reviewer. Record exactly one structured `review_row` for each of the fixed 27 task keys (including 288-00A), six SYNTH requirement keys, and the two artifact keys `artifact:288-P0-P2-REVIEW.md` and `artifact:288-VERIFICATION.md`; record every P0/P1/P2 finding with exact file/line, command, evidence digest, owner, reviewer, disposition, and current combined-tree digest in both artifacts. Map SYNTH-01..06 to exact green Phase 288 and Phase 287 commands and concrete Arena evidence. Run `python3 tools/parse-synthesis-review.py --review-file .planning/phases/288-autonomous-detector-response-synthesis/288-P0-P2-REVIEW.md --verification-file .planning/phases/288-autonomous-detector-response-synthesis/288-VERIFICATION.md --require-current-head --require-combined-tree --require-independent-review --require-exact-rows`; close only after it reports exactly one zero counter per severity per artifact and no open finding. Do not advance roadmap/state or claim hosted/release evidence.</action>
  <verify><automated>python3 tools/parse-synthesis-review.py --self-test && python3 tools/parse-synthesis-review.py --review-file .planning/phases/288-autonomous-detector-response-synthesis/288-P0-P2-REVIEW.md --verification-file .planning/phases/288-autonomous-detector-response-synthesis/288-VERIFICATION.md --require-current-head --require-combined-tree --require-independent-review --require-exact-rows && python3 tools/compare-synthesis-artifacts.py --first artifacts/synthesis/run-1 --second artifacts/synthesis/run-2 --allow-observation-fields && git diff --check</automated></verify>
  <done>Independent combined-tree review and goal-backward verification have exact current-tree-bound rows, disjoint reviewer/owner, zero open P0/P1/P2, and all SYNTH requirements linked to executed evidence.</done>
</task>

</tasks>

<verification>
Run the Phase 287 config inventory, oracle/isolation/review/parser/arena gates
and all exact Arena suites; run the Phase 288 gate twice, compare canonical
bytes, run full locked/offline tests/clippy/format/layering/registry/fixture/
panic/wiring/diff checks, and run the independent review parser. A green local
combined tree is not hosted or release evidence.
</verification>

<success_criteria>
- SYNTH-01..06 have exact combined-tree evidence from concrete Phase 287 interfaces.
- Canonical bytes match across two fixed runs and every mutation/authority/solver/approval control is non-vacuous.
- Independent review and verification artifacts contain exact rows and zero open P0/P1/P2 with current HEAD/tree binding.
</success_criteria>

<output>After completion, create `288-07-SUMMARY.md`, `288-P0-P2-REVIEW.md`, and `288-VERIFICATION.md`; do not alter roadmap/requirements ownership.</output>
