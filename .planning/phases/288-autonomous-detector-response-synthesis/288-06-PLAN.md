---
phase: 288-autonomous-detector-response-synthesis
plan: 06
type: execute
wave: 7
depends_on: [288-05]
files_modified:
  - crates/swarm-runtime/src/synthesis/mod.rs
  - crates/swarm-runtime/src/synthesis/packet.rs
  - crates/swarm-runtime/src/synthesis/handoff.rs
  - crates/swarm-runtime/tests/synthesis_handoff.rs
autonomous: true
requirements: [SYNTH-05, SYNTH-06]

must_haves:
  truths:
    - "A synthesis review packet is canonical, signed, content-addressed, immutable by packet ID, sequence-checked, and revalidated at every review/handoff boundary."
    - "Promotion evidence is produced by the real assurance evaluator and formal solver path; only `EvolutionSolverProofStatus::Proved` can satisfy the production solver gate."
    - "Canary, verification, shadow, baseline lineage, operator approval, quorum/signature requirements, and packet/control/metric digests all match before handoff; missing, stale, contradictory, or tampered evidence fails closed."
    - "The old detector baseline remains retained as rollback/fallback and no synthesis result auto-promotes or silently replaces it."
  artifacts:
    - path: "crates/swarm-runtime/src/synthesis/packet.rs"
      provides: "Signed immutable review-packet schema/store and revalidation"
      min_lines: 300
    - path: "crates/swarm-runtime/src/synthesis/handoff.rs"
      provides: "Assurance/proof/canary/operator/quorum handoff orchestration"
      min_lines: 320
    - path: "crates/swarm-runtime/tests/synthesis_handoff.rs"
      provides: "Positive and fail-closed handoff tests with baseline retention"
  key_links:
    - from: "crates/swarm-runtime/src/synthesis/packet.rs"
      to: "crates/swarm-core/src/signed_state.rs"
      via: "SignedStateEnvelope binds packet digest, signer, stream, and monotonic sequence"
      pattern: "SignedStateEnvelope"
    - from: "crates/swarm-runtime/src/synthesis/handoff.rs"
      to: "crates/swarm-runtime/src/promotion.rs"
      via: "existing promotion gate receives exact packet-bound assurance/solver/canary/baseline lineage"
      pattern: "EvolutionSolverProofStatus::Proved"
---

<objective>
Assemble signed immutable synthesis review packets and hand accepted candidates through real assurance, solver, canary, operator, quorum, and production promotion gates without auto-promotion or baseline loss.

Purpose: reports and mutable stores are not attestations. This plan binds every source, corpus, control, metric, replay, assurance, proof, canary, and promotion artifact to a signed packet and revalidates it before each authority transition.
Output: immutable packet store, full handoff coordinator, Proved-only solver enforcement, operator/quorum gate integration, and tamper/baseline-retention tests.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-CONTEXT.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-RESEARCH.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-05-PLAN.md
@crates/swarm-core/src/signed_state.rs
@crates/swarm-crypto/src/lib.rs
@crates/swarm-runtime/src/evolution/assurance.rs
@crates/swarm-runtime/src/evolution/formal_safety.rs
@crates/swarm-runtime/src/evolution/harnesses.rs
@crates/swarm-runtime/src/canary.rs
@crates/swarm-runtime/src/promotion.rs
@crates/swarm-runtime/tests/promotion_solver_gate.rs

<interfaces>
Use `swarm_crypto::canonical_json_bytes`/`sha256_hex` for packet and artifact content IDs, and `SignedStateEnvelope::sign`/`verify(SignedStateExpectation)` for signer, stream, sequence, and payload checks. The existing assurance path owns private decision/provenance fields; call `evaluate_proposal_assurance` and never construct a passed summary literal. The production promotion gate independently requires `EvolutionSolverProofStatus::Proved` when configured, plus matching verification/shadow/canary/baseline and explicit operator approval/quorum/signature. Existing canary/promotion stores are mutable handoff records, so bind and rehash their content inside the immutable packet.</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist a signed immutable content-addressed review packet</name>
  <files>crates/swarm-runtime/src/synthesis/mod.rs, crates/swarm-runtime/src/synthesis/packet.rs, crates/swarm-runtime/tests/synthesis_handoff.rs</files>
  <behavior>
    - A packet binds candidate/response plan, graph/arena/falsifier lineage, four-lane corpus/config/ruleset digests, separated metrics, controls, baseline/experiment lineage, verification/shadow, assurance/proof/solver, canary, promotion, operator/quorum state, signer, stream, sequence, and packet digest.
    - Signature, signer, state kind, stream, accepted sequence, packet digest, nested candidate/genome/source/control digests, and expected schema are rechecked on load; same packet ID with different content is rejected and exact duplicate content is idempotent.
    - Missing, stale, replayed, conflicting, tampered, non-simulated response, incomplete evidence, or withheld/counterexample disjointness failures cannot be represented as accepted packet state.
  </behavior>
  <action>Write packet/store tests first, then implement canonical packet payload and an append-only immutable store using the existing `SignedStateEnvelope` pattern. Exclude display timestamps and machine-dependent wall-clock fields from packet IDs; include all deterministic work/metric/control/lineage digests. Require an admitted caller signer in production and a fixed deterministic test key only in tests. On every read and handoff recompute nested content digests and reject packet-ID conflicts, sequence replay, signer mismatch, stale baseline, or changed corpus/withheld handles. Do not reuse plain replacement stores as if they were immutable attestations.</action>
  <verify>
    <automated>cargo test -p swarm-runtime synthesis_packet_is_signed_and_immutable --lib</automated>
  </verify>
  <done>Review packets are durable signed immutable evidence; packet and nested digest tampering or sequence conflicts fail before any rollout decision.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chain real assurance, solver, canary, promotion, and quorum handoff</name>
  <files>crates/swarm-runtime/src/synthesis/handoff.rs, crates/swarm-runtime/src/synthesis/packet.rs, crates/swarm-runtime/src/synthesis/mod.rs</files>
  <behavior>
    - Handoff revalidates packet/candidate/baseline/corpus/control/replay/verification/shadow digests, calls the real assurance evaluator, attaches formal proof artifacts, and rejects every solver status except `Proved` where promotion requires it.
    - Canary admission requires exact matching experiment/verification/shadow/assurance/packet/baseline lineage and passes only through the existing bounded canary harness; failed/active/mismatched canaries cannot reach promotion.
    - Production promotion requires explicit operator review and configured quorum/signature evidence, retains the previous baseline/fallback and rollback record, and never invokes an auto-promote or silent replacement path.
  </behavior>
  <action>Implement a coordinator that consumes the signed packet and existing replay/preview outputs, invokes `evaluate_proposal_assurance` and the existing formal-safety/solver harness, then hands exact verified references to `DefaultCanaryHarness` and `DefaultProductionPromotionHarness`. Apply the production solver gate independently of any assurance allowed-status list: `Disabled`, `Counterexample`, `Timeout`, `ResourceLimit`, `Error`, missing, or fabricated proof all block. Require packet revalidation immediately before canary and again before promotion, explicit operator decision/quorum/signature according to config, and retained baseline identity/rollback. Synthesis may produce a review-ready handoff; it must not call a response adapter, mutate policy/admission/governance, or promote itself.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test synthesis_handoff requires_proved_solver_canary_and_operator_quorum -- --exact</automated>
  </verify>
  <done>Only a fully revalidated, Proved, canary-ready, operator/quorum-approved packet can enter the existing promotion gate, with old baseline retained and no auto-promotion path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove tamper rejection and baseline retention across handoff boundaries</name>
  <files>crates/swarm-runtime/tests/synthesis_handoff.rs</files>
  <behavior>
    - Mutating any nested source/corpus/control/metric/verification/shadow/assurance/solver/canary/promotion digest, packet signature/sequence, baseline ID, operator vote, or quorum result blocks handoff.
    - Solver `Disabled`, `Counterexample`, `Timeout`, `ResourceLimit`, `Error`, missing proof, failed assurance, failed shadow/verification/canary, missing approval/quorum, and stale baseline all fail closed.
    - An accepted candidate records explicit retained baseline/fallback and rollback; there is no path in which a packet silently changes the active baseline.
  </behavior>
  <action>Build adversarial handoff fixtures over a known-good signed packet and mutate one boundary at a time, reloading/revalidating through the real coordinator. Assert each refusal reason is durable and typed rather than a copied `passed` boolean. Exercise the existing promotion solver negative tests as a regression, compare active baseline before/after accepted review, and assert that no auto-promotion occurs without the explicit operator/quorum action required by configuration.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test synthesis_handoff rejects_tampered_lineage_and_retains_baseline -- --exact</automated>
  </verify>
  <done>Every required handoff evidence and approval boundary is mutation-tested, and baseline preservation/no-auto-promotion is observable in the accepted path.</done>
</task>

</tasks>

<verification>
Run packet immutability and both exact handoff tests, `cargo test -p swarm-runtime --test promotion_solver_gate`, relevant evolution/canary tests, clippy, format, and diff checks. Verify the coordinator calls real assurance/proof/canary/promotion APIs, not fabricated summaries or status booleans, and that packet bytes are revalidated at every boundary.
</verification>

<success_criteria>
- Signed immutable packets bind all source, corpus, metric, control, assurance, solver, canary, promotion, approval, and baseline evidence.
- Only real Proved solver evidence plus matching canary/operator/quorum gates can reach production promotion.
- No auto-promotion, adapter execution, authority mutation, stale/tampered handoff, or baseline replacement is possible.
</success_criteria>

<output>After completion, create `.planning/phases/288-autonomous-detector-response-synthesis/288-06-SUMMARY.md` with packet digest/signature evidence, refusal matrix, solver/canary/quorum results, and baseline retention proof.</output>
