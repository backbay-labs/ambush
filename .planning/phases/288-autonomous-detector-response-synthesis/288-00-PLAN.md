---
phase: 288-autonomous-detector-response-synthesis
plan: 00
type: execute
wave: 1
depends_on: [287-05]
files_modified:
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/src/synthesis/mod.rs
  - crates/swarm-runtime/src/synthesis/contracts.rs
  - crates/swarm-runtime/tests/synthesis_contract.rs
  - crates/swarm-runtime/tests/negative_synthesis_authority_boundary.rs
  - scenarios/autonomous-detector-response-synthesis/manifest.yaml
  - scenarios/autonomous-detector-response-synthesis/historical-attacks.yaml
  - scenarios/autonomous-detector-response-synthesis/benign-controls.yaml
  - scenarios/autonomous-detector-response-synthesis/counterexamples.yaml
  - scenarios/autonomous-detector-response-synthesis/withheld-campaigns.yaml
  - tools/check-autonomous-detector-response-synthesis.sh
  - .planning/phases/288-autonomous-detector-response-synthesis/288-VALIDATION.md
autonomous: true
requirements: [SYNTH-01, SYNTH-02, SYNTH-03, SYNTH-04, SYNTH-05, SYNTH-06]

must_haves:
  truths:
    - "The four evaluation lanes have an independently authored, strict, digest-addressed oracle before any candidate implementation can influence them."
    - "A missing class, executable counterexample, denominator, source digest, partition boundary, or expected verdict fails closed rather than disappearing from a report."
    - "A clean synthesis boundary passes while deliberate imports or calls toward policy mutation, dispatch, response adapters, or live execution fail the boundary probe."
    - "The phase gate cannot pass from zero matched tests, renamed tests, removed thresholds, a weakened oracle, or a wall-clock-only resource metric."
  artifacts:
    - path: "crates/swarm-runtime/src/synthesis/contracts.rs"
      provides: "Strict synthesis input, candidate, corpus, metric, control, packet, and handoff contracts"
      min_lines: 260
    - path: "scenarios/autonomous-detector-response-synthesis/manifest.yaml"
      provides: "Four-lane truth manifest with disjointness, denominators, digests, limits, and thresholds"
    - path: "crates/swarm-runtime/tests/synthesis_contract.rs"
      provides: "Independent schema/oracle tests and mutation cases"
    - path: "tools/check-autonomous-detector-response-synthesis.sh"
      provides: "Credential-free exact-test, report, mutation, and self-test gate"
  key_links:
    - from: "crates/swarm-runtime/src/synthesis/contracts.rs"
      to: "scenarios/autonomous-detector-response-synthesis/manifest.yaml"
      via: "strict parser validates lane IDs, class, digest, denominator, and resource limits"
      pattern: "SynthesisCorpusManifest"
    - from: "crates/swarm-runtime/tests/negative_synthesis_authority_boundary.rs"
      to: "tools/check-autonomous-detector-response-synthesis.sh"
      via: "clean and deliberately broken fixtures are both exercised by the phase gate"
      pattern: "boundary_checker_rejects_broken_fixture"
---

<objective>
Establish the non-vacuous contracts, four-lane corpus/oracle, authority-boundary mutation fixture, and exact gate before implementing synthesis behavior.

Purpose: later plans must consume an immutable truth surface and a mechanically failing boundary; neither candidate scores nor a missing implementation may define its own acceptance criteria.
Output: strict `swarm-runtime::synthesis` contracts, four lane fixtures, negative authority tests, a self-testing phase gate, and an aligned validation map with `wave_0_complete: false`.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-CONTEXT.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-RESEARCH.md
@.planning/phases/288-autonomous-detector-response-synthesis/288-VALIDATION.md
@.planning/phases/286-collective-hypothesis-graph/286-VALIDATION.md
@.planning/phases/287-adversarial-co-evolution-arena/287-RESEARCH.md
@.planning/phases/287-adversarial-co-evolution-arena/287-02-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-03-PLAN.md
@.planning/phases/287-adversarial-co-evolution-arena/287-04-PLAN.md
@crates/swarm-core/src/signed_state.rs
@crates/swarm-crypto/src/lib.rs
@crates/swarm-runtime/src/replay/types.rs
@crates/swarm-runtime/src/service/preview.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define strict synthesis contracts and independent input boundaries</name>
  <files>crates/swarm-runtime/src/lib.rs, crates/swarm-runtime/src/synthesis/mod.rs, crates/swarm-runtime/src/synthesis/contracts.rs, crates/swarm-runtime/tests/synthesis_contract.rs</files>
  <behavior>
    - Unknown fields, missing schema/version, empty bounded collections, duplicate IDs, invalid digests, `Mixed`/missing scenario classes, and zero denominators are rejected.
    - Graph gaps, hypothesis edges, arena escapes, falsifier findings, source evidence, and corpus references are typed IDs plus verified digests; no raw telemetry, free-form code, or `serde_json::Value` enters a synthesis request.
    - Detector and response candidate contracts, detector/response metric records, control results, deterministic work model, and review-packet references have explicit budgets and canonical digest inputs.
  </behavior>
  <action>Write the failing contract tests first, then add the `synthesis` module and strict `deny_unknown_fields` records. Define versioned `SynthesisCorpusManifest`, four lane/partition records, graph/arena/falsifier source-reference types, bounded synthesis budgets, deterministic work counters (with a version ID), detector candidate and advisory response-plan envelopes, separate detector/response metric records, control-result relations, and the review/handoff reference contract. Constructors must validate caps, canonical IDs, source digests, explicit classes, denominators, and baseline identity. Expose narrow adapter traits keyed by typed IDs/schema/digest so later Phase 286/287 implementations can plug in without guessing or duplicating their graph schemas. Do not introduce an LLM/code-generation path, wildcard deserialization, raw telemetry fields, wall-clock fitness, or authority-bearing response objects.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test synthesis_contract strict_contract_rejects_unknown_fields -- --exact</automated>
  </verify>
  <done>The crate exports validated contracts for every later plan, malformed/over-limit inputs fail closed, and the contract tests fail if strictness or typed lineage is removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze the four-lane corpus and independent truth oracle</name>
  <files>scenarios/autonomous-detector-response-synthesis/manifest.yaml, scenarios/autonomous-detector-response-synthesis/historical-attacks.yaml, scenarios/autonomous-detector-response-synthesis/benign-controls.yaml, scenarios/autonomous-detector-response-synthesis/counterexamples.yaml, scenarios/autonomous-detector-response-synthesis/withheld-campaigns.yaml, crates/swarm-runtime/tests/synthesis_contract.rs</files>
  <behavior>
    - Historical attacks, benign controls, executable counterexamples, and withheld campaigns have nonempty, explicit, non-`Mixed` classes, stable IDs, canonical content digests, and separate denominators.
    - Withheld entries are disjoint from all generation/source entries and are supplied only as an immutable digest/handle; their content cannot become detector or response synthesis input.
    - Only the evaluator owns the capability that resolves a withheld handle; the synthesizer cannot import the resolver, inspect a withheld path, or access withheld content before candidate lineage is frozen.
    - Removing a lane, truth entry, denominator, executable counterexample, expected relation, or digest changes the manifest verdict to failure.
  </behavior>
  <action>Create complete fixed fixtures and a strict manifest parser around the contracts from Task 1. Include historical adversarial scenarios, benign controls with false-positive truth, replayable counterexample fixtures that use the same real replay shape, and an operator-owned/withheld suite represented by an immutable handle plus digest. Record corpus version, scenario class, expected detection/causal/response relations, source graph/arena evidence IDs, baseline references, metric denominators, resource ceilings, and disjoint partition digests. Freeze allowed improvement targets to higher `attack_chain_recall_bps`, higher `evidence_coverage_bps`, or lower `median_time_to_containment_work_units`. Compute higher-is-better relative improvement as `10_000 * (candidate - baseline) / max(baseline, 1)` and lower-is-better as `10_000 * (baseline - candidate) / max(baseline, 1)`, using checked integer arithmetic; require at least 1,000 bps. Freeze benign false positives at no regression and at most 500 bps, false causal edges at no regression and at most 1,000 bps, duplicate work at no regression and at most 500 bps, counterexample recall at least 9,000 bps, withheld recall at least 8,000 bps, evidence coverage at 10,000 bps, containment work/blast radius at no regression with blast radius at most one declared scope, and reversibility/rollback completeness at 10,000 bps. The manifest must bind each threshold and denominator before synthesis begins. Add tests that mutate or overlap each partition, alias a path, omit a counterexample executable fixture, change a source/expected digest, choose an undeclared target metric, change the relative-delta denominator/formula, or weaken any ceiling and prove rejection. Add a structural capability test showing synthesis receives only an opaque handle/digest and cannot name or invoke the evaluator-only resolver before frozen-lineage evaluation. This is independent oracle data, not a generated report and not a production success fixture.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test synthesis_contract four_lane_manifest_is_disjoint_and_digest_bound -- --exact && cargo test -p swarm-runtime --test synthesis_contract synthesizer_cannot_dereference_withheld_handle -- --exact</automated>
  </verify>
  <done>The checked-in corpus has four independently scored lanes, executable counterexamples, a sealed withheld boundary, explicit denominators/thresholds, and mutation tests that prevent vacuous evaluation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Install the authority-boundary probe and self-testing exact gate</name>
  <files>crates/swarm-runtime/tests/negative_synthesis_authority_boundary.rs, tools/check-autonomous-detector-response-synthesis.sh, .planning/phases/288-autonomous-detector-response-synthesis/288-VALIDATION.md</files>
  <behavior>
    - The clean fixture passes, while a generated broken fixture importing/calling response execution, adapter dispatch, policy/governance mutation, or live `process_event` fails.
    - `--self-test` rejects zero matched tests, wrong execution counts, renamed/ignored tests, missing or extra report fields, removed thresholds, changed oracle truth, withheld-resolver capability leakage, broken authority boundaries, and wall-clock gating.
    - Normal gate mode requires all later exact test names and fails specifically for missing implementation rather than treating absent files as success.
  </behavior>
  <action>Implement a reusable source/dependency scanner and prove it with run-owned clean and deliberately broken fixtures; never let an absent synthesis directory make the scan pass. Its forbidden-capability mutation must prove that synthesis cannot import the evaluator-only withheld resolver, dereference withheld paths, or inspect withheld content before candidate lineage freeze. Write the credential-free phase checker with exact test-name/count assertions, strict report schema and denominator checks, deterministic repeat comparison, all declared mutation probes, `git diff --check`, and a `--self-test` mode that exercises failure paths before production behavior exists. The normal gate may remain red for later missing behavior, but its syntax and self-test must be green now. Initialize/update the validation document with every plan/task command while preserving `wave_0_complete: false`; do not claim phase execution evidence or edit roadmap/requirements/code outside the listed test/gate files.</action>
  <verify>
    <automated>bash -n tools/check-autonomous-detector-response-synthesis.sh && bash tools/check-autonomous-detector-response-synthesis.sh --self-test && cargo test -p swarm-runtime --test negative_synthesis_authority_boundary boundary_checker_rejects_broken_fixture -- --exact && git diff --check</automated>
  </verify>
  <done>The boundary scanner demonstrably detects authority leakage, the gate's own failure modes are mutation-tested, and the validation artifact is Nyquist-complete without falsely marking Wave 0 complete.</done>
</task>

</tasks>

<verification>
Run both exact Wave 0 tests, the gate self-test, shell syntax, format check, and `git diff --check`. Confirm normal gate mode fails only because later synthesis tests/behavior are not implemented, never because the oracle, boundary fixture, or exact-test checker is vacuous.
</verification>

<success_criteria>
- All later plans have typed contracts, independent four-lane truth, source/digest lineage, and exact test targets.
- The authority boundary fails on a real broken fixture and the gate cannot be weakened by deleting tests, fields, thresholds, or deterministic controls.
- No production stub, placeholder pass, ignored test, live adapter path, or wall-clock verdict is introduced.
</success_criteria>

<output>After completion, create `.planning/phases/288-autonomous-detector-response-synthesis/288-00-SUMMARY.md` with exact Wave 0 test evidence; leave `wave_0_complete: false` in the validation artifact until the execution workflow records its own completion.</output>
