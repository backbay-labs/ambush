---
phase: 269-deployment-documentation
plan: 01
type: execute
depends_on:
  - 268-curated-defaults-and-operator-ux
files_modified:
  - docs/QUICKSTART.md
  - docs/DEPLOYMENT.md
  - docs/CONFIGURATION.md
  - README.md
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
---

<objective>
Write the repo-owned operator packaging documentation so a first-time operator
can reach one visible detection quickly and can also see every supported
deployment path, required config seam, and verification command in one place.
</objective>

<tasks>

<task>
  <name>Publish the zero-to-first-detection quickstart</name>
  <done>`docs/QUICKSTART.md` walks the operator through the Docker Compose image path, validates the signed bootstrap bundle, runs `swarmctl quickstart`, and calls out the exact success output that proves first detection.</done>
</task>

<task>
  <name>Document every supported deployment path</name>
  <done>`docs/DEPLOYMENT.md` now covers Docker single-container, Docker Compose bootstrap, Docker Compose with NATS, Helm, and bare-metal binaries with prerequisites, config entrypoints, and verification steps.</done>
</task>

<task>
  <name>Align the canonical docs index</name>
  <done>`README.md` and `docs/CONFIGURATION.md` now link to the quickstart and deployment references so the active contract set exposes the new operator packaging guidance.</done>
</task>

</tasks>
