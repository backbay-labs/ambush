---
phase: 106-heap-pressure-metrics-and-readiness-gates
plan: 01
type: execute
wave: 3
depends_on: [105-01, 105-02]
files_modified:
  - Cargo.toml
  - crates/swarm-runtime/Cargo.toml
  - crates/swarm-runtime/src/detection/metrics.rs
  - crates/swarm-runtime/src/ingest.rs
autonomous: true
requirements: [K8S-05]

must_haves:
  truths:
    - "`CriticalPathMetrics` exports heap bytes and heap pressure ratio gauges"
    - "The gauges are populated from the live process instead of a constant or config placeholder"
    - "Metrics encoding includes the new heap gauges"
  artifacts:
    - path: "crates/swarm-runtime/src/detection/metrics.rs"
      provides: "heap gauge registration and observation helpers"
      contains: "heap_pressure_ratio"
  key_links:
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-runtime/src/detection/metrics.rs"
      via: "metrics and readiness share one heap measurement path"
      pattern: "observe_heap"
---

<objective>
Add live heap-pressure gauges to the runtime Prometheus surface.

Purpose: Operators and Kubernetes both need explicit memory-pressure visibility before the process is OOM-killed.

Output: heap metrics registration, live memory sampling, and metrics tests proving the new gauges are exported.
</objective>
