---
phase: 120-composite-detector-and-config-migration
plan: 02
type: execute
wave: 2
depends_on: ["120-01"]
files_modified:
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/whisker_agent.rs
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/tests/composite_integration.rs
autonomous: true
requirements:
  - COMPOSE-01
  - COMPOSE-02

must_haves:
  truths:
    - "IngestState builds and hot-reloads a CompositeDetector instead of SupportedDetector"
    - "WhiskerAgent accepts and uses a CompositeDetector for detection"
    - "Multi-strategy config produces findings from all configured strategies in an integration test"
    - "Legacy single-strategy config continues to work end-to-end through the ingest path"
  artifacts:
    - path: "crates/swarm-runtime/src/ingest.rs"
      provides: "Ingest runtime using CompositeDetector"
      contains: "CompositeDetector"
    - path: "crates/swarm-runtime/src/whisker_agent.rs"
      provides: "WhiskerAgent using CompositeDetector"
      contains: "CompositeDetector"
    - path: "crates/swarm-runtime/tests/composite_integration.rs"
      provides: "Integration test proving multi-strategy detection"
      contains: "composite"
  key_links:
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-runtime/src/control.rs"
      via: "build_composite_detector called from build_runtime"
      pattern: "build_composite_detector"
    - from: "crates/swarm-runtime/src/whisker_agent.rs"
      to: "crates/swarm-whisker/src/composite.rs"
      via: "WhiskerAgent stores Arc<CompositeDetector>"
      pattern: "CompositeDetector"
    - from: "crates/swarm-runtime/tests/composite_integration.rs"
      to: "crates/swarm-whisker/src/composite.rs"
      via: "Integration test constructs CompositeDetector with multiple strategies"
      pattern: "CompositeDetector::new"
---

<objective>
Wire CompositeDetector into the live runtime paths (IngestState, WhiskerAgent) replacing SupportedDetector, remove the SupportedDetector enum, and add an integration test proving multi-strategy detection with merged findings and substrate deposits.

Purpose: Complete the runtime migration so that all configured strategies evaluate every event, proving the composition works end-to-end.
Output: Runtime fully using CompositeDetector, integration test, SupportedDetector removed.
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/120-composite-detector-and-config-migration/120-01-SUMMARY.md

<interfaces>
<!-- Types created in Plan 01 that this plan depends on -->

From crates/swarm-whisker/src/composite.rs (created in 120-01):
```rust
pub struct CompositeDetector {
    strategies: Vec<Box<dyn DetectionStrategy>>,
}

impl CompositeDetector {
    pub fn new(strategies: Vec<Box<dyn DetectionStrategy>>) -> Self;
}

impl DetectionStrategy for CompositeDetector {
    fn id(&self) -> &str; // returns "composite"
    fn evaluate(&self, event: &TelemetryEvent) -> Vec<DetectionFinding>;
}
```

From crates/swarm-core/src/config.rs (updated in 120-01):
```rust
pub struct DetectionConfig {
    pub strategy: String,
    pub high_confidence_threshold: f64,
    pub medium_confidence_threshold: f64,
    #[serde(default)]
    pub strategies: Vec<String>,
    #[serde(default)]
    pub profiles: DetectorProfilesConfig,
}

impl DetectionConfig {
    pub fn active_strategies(&self) -> Vec<String>;
}
```

From crates/swarm-runtime/src/control.rs (updated in 120-01):
```rust
pub fn build_composite_detector(config: &DetectionConfig) -> Result<CompositeDetector, ControlError>;
```

From crates/swarm-runtime/src/ingest.rs (current):
```rust
pub struct IngestState {
    stack: Arc<ArcSwap<IngestRuntimeStack>>,
    detector: Arc<ArcSwap<SupportedDetector>>,
    // ...
}
```

From crates/swarm-runtime/src/whisker_agent.rs (current):
```rust
pub struct WhiskerAgent {
    detector: Arc<SupportedDetector>,
    // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate IngestState and WhiskerAgent to CompositeDetector</name>
  <files>
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/whisker_agent.rs
    crates/swarm-runtime/src/control.rs
  </files>
  <read_first>
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/whisker_agent.rs
    crates/swarm-runtime/src/control.rs
    crates/swarm-whisker/src/composite.rs
  </read_first>
  <action>
    1. Update `crates/swarm-runtime/src/ingest.rs`:
       - Replace import: `use crate::control::{ControlError, SupportedDetector, supported_detector};` with `use crate::control::{ControlError, build_composite_detector};` and `use swarm_whisker::CompositeDetector;`
       - Change `IngestState.detector` field type from `Arc<ArcSwap<SupportedDetector>>` to `Arc<ArcSwap<CompositeDetector>>`
       - Update `build_runtime` return type and body: call `build_composite_detector(&config.detection)` instead of `supported_detector(&config.detection)`
       - Update `current_detector` return type from `Arc<SupportedDetector>` to `Arc<CompositeDetector>`
       - Update `reload` method: the strategy status log should call `.id()` on the new CompositeDetector (it returns "composite"), or better: log the list of active strategy names. Use the config's `active_strategies()` to build the status string, e.g. `strategies.join(", ")`
       - Update `DetectorRuntimeStatus.strategy` to reflect multi-strategy: store the joined strategy names string
       - Fix all type references throughout the file. Since `CompositeDetector` implements `DetectionStrategy`, the generic `<D: DetectionStrategy>` usage in `process_event` and `detect_and_deposit` remains unchanged.
       - Update the `WhiskerAgent::new` call in the ingest path to pass `Arc<CompositeDetector>` instead of `Arc<SupportedDetector>`

    2. Update `crates/swarm-runtime/src/whisker_agent.rs`:
       - Replace `use crate::control::SupportedDetector;` with `use swarm_whisker::CompositeDetector;`
       - Change `WhiskerAgent.detector` field from `Arc<SupportedDetector>` to `Arc<CompositeDetector>`
       - Update `WhiskerAgent::new` parameter type from `Arc<SupportedDetector>` to `Arc<CompositeDetector>`
       - The `tick()` body is unchanged since it calls `detect_and_deposit(self.detector.as_ref(), ...)` which works with any `D: DetectionStrategy`

    3. Update `crates/swarm-runtime/src/control.rs`:
       - Remove the `SupportedDetector` enum entirely
       - Remove the `impl DetectionStrategy for SupportedDetector` block
       - Remove the `supported_detector` public function (it was kept in Plan 01 for backward compat; Plan 02 migrates all callers)
       - Keep `build_composite_detector` and `build_single_detector` as the only detector construction API
       - Update any remaining references to `SupportedDetector` in the file

    4. Verify all compile: `cargo build --workspace`
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/swarm-team-six && cargo build --workspace && cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - grep -rq "SupportedDetector" crates/swarm-runtime/src/ returns NO matches (enum fully removed)
    - grep -q "CompositeDetector" crates/swarm-runtime/src/ingest.rs
    - grep -q "CompositeDetector" crates/swarm-runtime/src/whisker_agent.rs
    - grep -q "build_composite_detector" crates/swarm-runtime/src/ingest.rs
    - cargo build --workspace exits 0
    - cargo test --workspace exits 0
  </acceptance_criteria>
  <done>IngestState and WhiskerAgent use CompositeDetector. SupportedDetector enum removed from control.rs. All existing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-strategy integration test</name>
  <files>
    crates/swarm-runtime/tests/composite_integration.rs
  </files>
  <read_first>
    crates/swarm-runtime/tests/persistence_supply_chain_integration.rs
    crates/swarm-runtime/src/detection/pipeline.rs
    crates/swarm-whisker/src/composite.rs
    crates/swarm-core/src/config.rs
  </read_first>
  <behavior>
    - CompositeDetector with 2 strategies (suspicious_process_tree + dns_exfiltration) evaluates a ProcessStart event and returns findings only from the matching strategy
    - CompositeDetector with 2 strategies evaluates a DnsQuery event and returns findings only from the matching strategy
    - CompositeDetector feeds both event types through detect_and_deposit and produces pheromone deposits from both strategies
    - DetectionConfig with strategies: ["suspicious_process_tree", "dns_exfiltration"] parsed from YAML produces a working CompositeDetector via build_composite_detector
    - DetectionConfig with only strategy: "suspicious_process_tree" (no strategies field) produces a working single-detector CompositeDetector via build_composite_detector
  </behavior>
  <action>
    Create `crates/swarm-runtime/tests/composite_integration.rs`:

    1. Follow the pattern from `persistence_supply_chain_integration.rs` for test structure.

    2. Test `composite_detector_merges_findings_from_multiple_strategies`:
       - Construct `CompositeDetector::new(vec![Box::new(SuspiciousProcessTreeDetector::default()), Box::new(DnsExfiltrationDetector::default())])`
       - Create a suspicious ProcessStart event (winword -> powershell -enc)
       - Evaluate: assert 1 finding with strategy_id "suspicious_process_tree"
       - Create a suspicious DnsQuery event (long subdomain label for exfil)
       - Evaluate: assert 1 finding with strategy_id "dns_exfiltration"

    3. Test `composite_detector_deposits_from_both_strategies`:
       - Use `detect_and_deposit` with the composite detector, InMemoryPheromoneSubstrate, and both event types
       - Assert deposits exist in substrate for both threat classes (Execution from process tree, DataExfiltration from DNS)

    4. Test `build_composite_from_multi_strategy_config`:
       - Construct a `DetectionConfig` with `strategies: vec!["suspicious_process_tree".into(), "dns_exfiltration".into()]`
       - Call `build_composite_detector(&config)`
       - Assert Ok, then evaluate a ProcessStart event and assert finding exists

    5. Test `build_composite_from_legacy_single_strategy_config`:
       - Construct a `DetectionConfig` with `strategy: "suspicious_process_tree"` and empty `strategies` vec
       - Call `build_composite_detector(&config)`
       - Assert Ok, evaluate ProcessStart event, assert finding exists
       - Evaluate DnsQuery event, assert NO findings (only process tree detector active)

    6. Ensure all tests use `#[tokio::test]` where async (detect_and_deposit tests) and `#[test]` for sync evaluate tests.
    7. Run `cargo clippy --workspace -- -D warnings` at the end.
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/swarm-team-six && cargo test --test composite_integration && cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "composite_detector_merges_findings" crates/swarm-runtime/tests/composite_integration.rs
    - grep -q "composite_detector_deposits_from_both" crates/swarm-runtime/tests/composite_integration.rs
    - grep -q "build_composite_from_multi_strategy_config" crates/swarm-runtime/tests/composite_integration.rs
    - grep -q "build_composite_from_legacy_single_strategy_config" crates/swarm-runtime/tests/composite_integration.rs
    - cargo test --test composite_integration exits 0
    - cargo test --workspace exits 0
    - cargo clippy --workspace -- -D warnings exits 0
  </acceptance_criteria>
  <done>Integration tests prove multi-strategy CompositeDetector evaluates events through all contained strategies, deposits pheromones for both, and constructs correctly from both legacy and new config formats. Full workspace tests and clippy pass.</done>
</task>

</tasks>

<verification>
1. `cargo test --workspace` passes (all tests including new composite integration)
2. `cargo clippy --workspace -- -D warnings` is clean
3. No references to `SupportedDetector` remain in `crates/swarm-runtime/src/`
4. Multi-strategy integration test proves merged findings from 2+ strategies
5. Legacy single-strategy config still works end-to-end
6. IngestState reload produces a new CompositeDetector on config change
</verification>

<success_criteria>
- SupportedDetector enum is completely removed; CompositeDetector is the only detector type in the runtime
- IngestState.detector is Arc<ArcSwap<CompositeDetector>>
- WhiskerAgent.detector is Arc<CompositeDetector>
- Integration test proves CompositeDetector with 2 strategies produces merged findings and pheromone deposits
- Legacy single-strategy config produces a single-detector CompositeDetector that works identically
- cargo test --workspace and cargo clippy --workspace -- -D warnings both clean
</success_criteria>

<output>
After completion, create `.planning/phases/120-composite-detector-and-config-migration/120-02-SUMMARY.md`
</output>
