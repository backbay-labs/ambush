---
phase: 148-infrastructure-anomaly-detection-and-cross-signal-correlation
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 147
files_modified:
  - crates/swarm-whisker/src/infrastructure_anomaly.rs
  - crates/swarm-whisker/src/lib.rs
  - crates/swarm-whisker/src/detector.rs
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-runtime/src/detector_factory.rs
  - crates/swarm-runtime/tests/multi_strategy_integration.rs
  - rulesets/default.yaml
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03
must_haves:
  truths:
    - "Phase 148 is detector and correlation work; bridge transport is already complete."
    - "Cross-signal escalation should reuse the existing pheromone distinct-source model instead of adding a second correlation engine."
    - "Infrastructure findings need to land in existing threat classes that already participate in escalation and response routing."
  artifacts:
    - path: "crates/swarm-whisker/src/infrastructure_anomaly.rs"
      provides: "stateful infrastructure anomaly detector and profile"
    - path: "crates/swarm-runtime/src/detector_factory.rs"
      provides: "runtime strategy wiring for infrastructure detection"
    - path: "crates/swarm-runtime/tests/multi_strategy_integration.rs"
      provides: "cross-signal escalation proof through the shared distinct-source path"
  key_links:
    - from: "crates/swarm-whisker/src/infrastructure_anomaly.rs"
      to: "crates/swarm-runtime/src/detector_factory.rs"
      via: "repo-owned strategy activation"
      pattern: "runtime can build the detector from config without special cases"
    - from: "crates/swarm-whisker/src/infrastructure_anomaly.rs"
      to: "crates/swarm-runtime/tests/multi_strategy_integration.rs"
      via: "shared pheromone deposit path"
      pattern: "infrastructure execution pressure and behavioral execution findings create distinct-source alerting"
---

<objective>
Complete Phase 148 by adding a stateful `InfrastructureAnomalyDetector`, wiring it into the runtime detector surface, and proving that infrastructure findings flow through the shipped pheromone and escalation path with cross-signal distinct-source correlation.

Purpose: turn the Sentinel bridge from passive telemetry into live infrastructure-signal detection without creating a parallel response pipeline.
Output: new detector/profile surface, runtime config/factory wiring, and end-to-end correlation proof.
</objective>

<context>
@.planning/phases/148-infrastructure-anomaly-detection-and-cross-signal-correlation/148-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-whisker/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/detector_factory.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/tests/multi_strategy_integration.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/docs/research/sentinel-convergence/02-PREDICTIVE-FAILURE-AS-THREAT-SIGNAL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the infrastructure anomaly detector and profile</name>
  <read_first>
    crates/swarm-whisker/src/network_connect.rs
    docs/research/sentinel-convergence/02-PREDICTIVE-FAILURE-AS-THREAT-SIGNAL.md
  </read_first>
  <files>
    crates/swarm-whisker/src/infrastructure_anomaly.rs
    crates/swarm-whisker/src/lib.rs
    crates/swarm-whisker/src/detector.rs
  </files>
  <action>Implement a bounded stateful detector that consumes `InfrastructureHealth`, `ThermalAnomaly`, and `ResourceExhaustion` and emits execution, impact, or defense-evasion findings for cryptominer, destructive exhaustion, and low-noise memory abuse patterns.</action>
  <verify>
    <automated>cargo test -p swarm-whisker infrastructure_anomaly::tests:: -- --nocapture</automated>
    <automated>cargo test -p swarm-whisker tests::default_detectors_construct_without_panic -- --exact</automated>
  </verify>
  <done>The detector produces deterministic infrastructure findings and exposes a repo-owned profile surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire infrastructure detection into runtime config and factory surfaces</name>
  <read_first>
    crates/swarm-runtime/src/config.rs
    crates/swarm-runtime/src/detector_factory.rs
  </read_first>
  <files>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/config.rs
    crates/swarm-runtime/src/detector_factory.rs
    rulesets/default.yaml
  </files>
  <action>Expose `infrastructure_anomaly` as a supported detector profile and runtime strategy so multi-strategy configs can activate it without manual wiring.</action>
  <verify>
    <automated>cargo test -p swarm-runtime config::tests::infrastructure_anomaly_profile_merges_overrides -- --exact</automated>
    <automated>cargo check -p swarm-runtime -p swarm-whisker --tests -j 1 --message-format short</automated>
  </verify>
  <done>The runtime can build the infrastructure detector from repo-owned config and supported-strategy docs are updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove cross-signal escalation through the existing pheromone lane</name>
  <read_first>
    crates/swarm-runtime/tests/multi_strategy_integration.rs
    crates/swarm-runtime/src/detection/pipeline.rs
  </read_first>
  <files>
    crates/swarm-runtime/tests/multi_strategy_integration.rs
  </files>
  <action>Add an integration test that combines infrastructure execution pressure with a behavioral execution signal and verifies that strategy-scoped deposits produce `distinct_sources >= 2` in the shared escalation lane.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test multi_strategy_integration infrastructure_and_behavioral_execution_signals_share_alert_lane -- --exact</automated>
  </verify>
  <done>Infrastructure and behavioral execution signals can jointly trigger alerting through the existing concentration monitor and deposit pipeline.</done>
</task>

</tasks>

<verification>
Keep the bridge and detector boundaries separate. The detector should only consume normalized infrastructure payloads and reuse the shipped deposit, escalation, and signature paths.
</verification>
