---
phase: 110-notification-channels-and-routing-rules
plan: 01
type: execute
wave: 3
depends_on: [109-02]
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-response/src/config.rs
  - crates/swarm-response/src/notification.rs
  - rulesets/default.yaml
autonomous: true
requirements: [SIEM-03]

must_haves:
  truths:
    - "`SwarmConfig` exposes `notification_channels` with target URL, auth token, rate limit, and quiet hours"
    - "Notification channel auth tokens support `@secret:` resolution"
    - "Notification sender config validates fail closed and remains repo-owned"
  artifacts:
    - path: "crates/swarm-core/src/config.rs"
      provides: "notification channel and routing config types"
      contains: "notification_channels"
    - path: "crates/swarm-runtime/src/config.rs"
      provides: "notification secret resolution path"
      contains: "notification_channels"
---

<objective>
Add the repo-owned notification channel configuration surface.

Purpose: Alert routing needs named channels and secret-aware outbound config before runtime rule matching can exist.

Output: config types, validation, secret resolution, and notification sender scaffolding.
</objective>
