---
phase: 185-ingest-and-service-panic-free-conversion
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 184
files_modified:
  - crates/swarm-runtime/src/ingest/mod.rs
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime/src/http/core.inc
autonomous: true
requirements:
  - PANIC-03
must_haves:
  truths:
    - "Ingest, service, and HTTP entrypoints must propagate typed failures instead of `String` reasons or blanket `error.to_string()` flattening."
    - "Malformed input and storage-adjacent errors must fail closed without reopening panic paths in the runtime entrypoints."
    - "The operator and ingest surfaces should keep stable response behavior while improving the internal error contract."
  artifacts:
    - path: "crates/swarm-runtime/src/ingest/mod.rs"
      provides: "Typed ingest helper and request-path error propagation."
    - path: "crates/swarm-runtime/src/service.rs"
      provides: "Service-layer typed conversions for entrypoint failures."
    - path: "crates/swarm-runtime/src/http/core.inc"
      provides: "Operator-surface mapping from typed runtime errors to stable API responses."
---

<objective>
Complete Phase 185 by replacing string-only error propagation in the ingest,
service, and HTTP entrypoints with typed runtime errors that fail closed on
malformed input and storage-adjacent failures.

Purpose: turn the Phase 184 audit into real panic-free entrypoint contracts.
Output: typed ingest and service error seams, operator-surface mapping that no
longer relies on blanket `to_string()`, and regression coverage for malformed
input and boundary failures.
</objective>

<context>
@.planning/phases/185-ingest-and-service-panic-free-conversion/185-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/mod.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/service.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/http/core.inc
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace string-only ingest helper errors with typed variants</name>
  <files>
    crates/swarm-runtime/src/ingest/mod.rs
  </files>
  <action>Convert helper seams that currently return `Result<_, String>` into typed error variants that preserve failure class and context.</action>
  <done>Malformed input and context-token helper failures propagate as typed ingest errors rather than anonymous strings.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Align service and HTTP entrypoints with typed propagation</name>
  <files>
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/http/core.inc
  </files>
  <action>Map service and operator-surface boundary failures from typed errors into stable response envelopes without flattening everything through `error.to_string()`.</action>
  <done>Service and HTTP entrypoints preserve explicit boundary context while keeping the shipped surface behavior fail-closed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Prove malformed input no longer relies on panic or string-only fallback</name>
  <files>
    crates/swarm-runtime/src/ingest/mod.rs
    crates/swarm-runtime/src/http/core.inc
  </files>
  <action>Add focused regression coverage for malformed input and representative boundary failures.</action>
  <done>Phase 185 verification demonstrates typed entrypoint failures rather than panic or opaque string-only propagation.</done>
</task>

</tasks>

<verification>
Verify that malformed input, config-adjacent helper failures, and representative
operator-surface boundary errors propagate through typed `Result` paths without
reintroducing panic-driven behavior.
</verification>
