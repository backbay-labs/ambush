---
phase: 157-calico-agent-core-and-deception-playbook
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 152
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/calico_agent.rs
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
  - rulesets/default.yaml
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - CALICO-01
  - CALICO-02
must_haves:
  truths:
    - "The repo already has `AgentRole::Calico` and `ResponseAction::DeployDecoy`, so the phase should extend those seams instead of creating parallel agent or action contracts."
    - "Phase 157 only needs the core deception lane: typed playbook config, baseline deploy requests, and high-confidence decoy-hit deposits."
    - "Calico is Tier 1 autonomous, so baseline decoy deployment should remain in the existing non-destructive routed response path."
  artifacts:
    - path: "crates/swarm-core/src/config.rs"
      provides: "Typed repository-owned config surface and fail-closed validation."
    - path: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      provides: "Serve-mode agent registration and identity admission seam."
    - path: "crates/swarm-runtime/src/stalker_agent.rs"
      provides: "Reference pattern for signed direct pheromone deposits from a runtime-owned agent."
  key_links:
    - from: "crates/swarm-core/src/config.rs"
      to: "rulesets/default.yaml"
      via: "deception playbook schema"
      pattern: "repo-owned YAML controls decoy deployment and monitoring rules"
    - from: "crates/swarm-runtime/src/calico_agent.rs"
      to: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      via: "serve-mode registration"
      pattern: "Calico becomes a real admitted runtime agent when deception is enabled"
---

<objective>
Complete Phase 157 by shipping a runtime-owned `CalicoAgent`, typed deception playbook config, baseline decoy deployment requests, and high-confidence deception-trigger pheromone deposits.

Purpose: add a first-class deception lane to the swarm without waiting on the later lifecycle and evolution integrations.
Output: validated `deception` config, registered Calico runtime agent, routed `DeployDecoy` bootstrap requests, and signed `InitialAccess` / `LateralMovement` deposits when monitored decoys are touched.
</objective>

<context>
@.planning/phases/157-calico-agent-core-and-deception-playbook/157-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/bin/swarm_detect.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/stalker_agent.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repo-owned deception playbook config and validation</name>
  <files>
    crates/swarm-core/src/config.rs
    rulesets/default.yaml
    docs/CONFIGURATION.md
  </files>
  <action>Add a top-level `deception` config section with typed playbook entries, placement strategies, and monitoring rules, plus fail-closed validation and a checked-in default ruleset example.</action>
  <verify>
    <automated>cargo test -p swarm-core config::tests::deception_ -- --nocapture</automated>
  </verify>
  <done>`SwarmConfig` loads and validates a repo-owned deception playbook, and invalid monitoring rules are rejected before runtime startup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CalicoAgent baseline deploy and decoy-hit deposit flow</name>
  <files>
    crates/swarm-runtime/src/calico_agent.rs
    crates/swarm-runtime/src/lib.rs
  </files>
  <action>Build `CalicoAgent` with baseline `DeployDecoy` requests derived from the playbook and signed high-confidence `DepositPheromone` actions when monitored file paths, honeypot ports, or canary credentials are touched in existing pheromone evidence.</action>
  <verify>
    <automated>cargo test -p swarm-runtime calico_agent::tests:: -- --nocapture</automated>
  </verify>
  <done>Calico emits one bootstrap deploy request per playbook entry and generates `InitialAccess` or `LateralMovement` deposits with confidence >= 0.95 for matching decoy interactions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire serve-mode registration and admission</name>
  <files>
    crates/swarm-runtime/src/bin/swarm_detect.rs
  </files>
  <action>Register Calico behind `deception.enabled` using the existing persisted-identity and registry-admission seam so serve mode runs the deception lane when configured.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --bin swarm_detect tests::serve_mode_registers_calico_when_deception_is_enabled -- --exact</automated>
  </verify>
  <done>Serve mode registers an admitted Calico agent with a stable `swarm:ed25519:<hex>` identity when deception is enabled.</done>
</task>

</tasks>

<verification>
Keep the phase narrow: prove config validation, direct Calico unit behavior, and serve-mode registration. Sphinx lifecycle and Kitten fitness integration remain explicitly out of scope until Phase 158.
</verification>
