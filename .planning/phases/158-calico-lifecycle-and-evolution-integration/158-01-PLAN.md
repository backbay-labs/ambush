---
phase: 158-calico-lifecycle-and-evolution-integration
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 157
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/calico_agent.rs
  - crates/swarm-runtime/src/sphinx_agent.rs
  - crates/swarm-runtime/src/kitten_agent.rs
  - crates/swarm-evolution/src/mutation.rs
autonomous: true
requirements:
  - CALICO-03
  - CALICO-04
must_haves:
  truths:
    - "Phase 157 already proves the playbook and high-confidence trigger seam, so Phase 158 should add lifecycle and downstream integrations on top of that lane instead of reworking the core agent."
    - "Decoy inventory has to survive restart and be attributable, which means explicit durable metadata rather than transient in-memory deployment markers."
    - "Kitten fitness should ingest deception interactions through existing durable scoring artifacts, not a separate ad hoc side channel."
  artifacts:
    - path: "crates/swarm-runtime/src/calico_agent.rs"
      provides: "Core playbook execution and interaction detection seam."
    - path: "crates/swarm-runtime/src/sphinx_agent.rs"
      provides: "Durable typed graph and persistence layer for decoy registration."
    - path: "crates/swarm-runtime/src/kitten_agent.rs"
      provides: "Runtime-owned evolution fitness entry point."
  key_links:
    - from: "crates/swarm-runtime/src/calico_agent.rs"
      to: "crates/swarm-runtime/src/sphinx_agent.rs"
      via: "deployed decoy inventory"
      pattern: "Calico lifecycle state is visible to the memory lane for correlation and forensic attribution"
    - from: "crates/swarm-runtime/src/calico_agent.rs"
      to: "crates/swarm-runtime/src/kitten_agent.rs"
      via: "deception interaction signal"
      pattern: "attacker interaction with a decoy boosts the strategy fitness tied to that placement"
---

<objective>
Complete Phase 158 by adding durable decoy lifecycle state, Sphinx graph registration for deployed decoys, and a Kitten fitness path for deception-trigger validation.

Purpose: turn Calico from a bootstrap-only deception lane into a lifecycle-managed subsystem that materially informs memory and evolution.
Output: persisted decoy inventory with rotate / cleanup handling, Sphinx-visible decoy metadata, and deception interactions reflected in durable Kitten fitness.
</objective>

<context>
@.planning/phases/158-calico-lifecycle-and-evolution-integration/158-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/calico_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/sphinx_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/kitten_agent.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add durable Calico lifecycle state</name>
  <files>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/calico_agent.rs
  </files>
  <action>Add deploy / monitor / rotate / cleanup lifecycle state for decoys, including durable metadata for type, placement, and creation time.</action>
  <verify>
    <automated>cargo test -p swarm-runtime calico_agent::tests::lifecycle_ -- --nocapture</automated>
  </verify>
  <done>Calico can recover deployed decoy state after restart and deterministically rotate or clean up stale assets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register decoys in Sphinx for memory and attribution</name>
  <files>
    crates/swarm-runtime/src/calico_agent.rs
    crates/swarm-runtime/src/sphinx_agent.rs
  </files>
  <action>Persist deployed decoy metadata into Sphinx's typed knowledge graph so other agents can correlate against durable deception inventory.</action>
  <verify>
    <automated>cargo test -p swarm-runtime sphinx_agent::tests::deception_ -- --nocapture</automated>
  </verify>
  <done>Sphinx retains decoy metadata across restart and associates later interactions with the registered deployment record.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Feed deception interactions into Kitten fitness</name>
  <files>
    crates/swarm-runtime/src/kitten_agent.rs
    crates/swarm-evolution/src/mutation.rs
  </files>
  <action>Thread decoy-trigger signals into the durable fitness path so strategy placements that attract attacker interaction receive positive evolutionary weight.</action>
  <verify>
    <automated>cargo test -p swarm-runtime kitten_agent::tests::deception_ -- --nocapture</automated>
  </verify>
  <done>Deception interactions change durable proposal fitness and survive restart through existing evolution artifacts.</done>
</task>

</tasks>

<verification>
Keep the phase bounded to lifecycle and downstream integration. Do not start fileless execution or behavioral-baseline work until the deception lane is durable and connected.
</verification>
