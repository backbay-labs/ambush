---
phase: 102-threat-intel-cache-and-operator-query-surface
plan: 02
type: execute
wave: 1
depends_on: [102-01]
files_modified:
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/src/http/core.inc
autonomous: true
requirements: [SUBSTRATE-04]

must_haves:
  truths:
    - "Authenticated operator endpoints can seed threat-intel entries without direct backend file edits"
    - "Operators can query threat-intel entries by indicator type and value through the same control surface"
    - "Verification proves the operator surface and substrate share one threat-intel source of truth"
  artifacts:
    - path: "crates/swarm-runtime/src/control.rs"
      provides: "control-plane helpers for threat-intel upsert and exact lookup"
      contains: "ThreatIntelEntry"
    - path: "crates/swarm-runtime/src/http/core.inc"
      provides: "authenticated operator API routes for threat-intel seed and query"
      contains: "/v1/operator/threat-intel"
  key_links:
    - from: "crates/swarm-runtime/src/control.rs"
      to: "crates/swarm-runtime/src/http/core.inc"
      via: "authenticated threat-intel management through the existing operator boundary"
      pattern: "query_threat_intel_entry"
    - from: "crates/swarm-runtime/src/http/core.inc"
      to: "crates/swarm-pheromone/src/substrate.rs"
      via: "operator seed/query routes backed by the substrate source of truth"
      pattern: "store_threat_intel_entry"
---

<objective>
Expose authenticated operator routes for seeding and querying threat-intel entries and verify that those routes hit the same substrate-backed cache the runtime will use later.

Purpose: Phase 102 is incomplete until operators can manage threat-intel through the existing control surface instead of editing storage files directly.

Output: operator threat-intel seed/query endpoints plus verification covering API behavior and substrate-backed lookup.
</objective>
