---
phase: 144-red-blue-pressure-and-episode-logging
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 143
files_modified:
  - crates/swarm-runtime/src/kitten_agent.rs
  - crates/swarm-runtime/src/red_swarm.rs
  - crates/swarm-evolution/src/mutation.rs
  - crates/swarm-runtime/src/evolution_status.rs
  - crates/swarm-runtime/src/runtime_events.rs
autonomous: true
requirements:
  - HELLCAT-02
  - HELLCAT-03
must_haves:
  truths:
    - "Phase 143 already created a deterministic suite-backed red corpus seam, so Phase 144 should consume that seam rather than rebuilding corpus logic inside Kitten."
    - "Replay, Sphinx retrieval, and adversarial pressure should resolve into one generation-scoped fitness record instead of separate ad hoc score files."
    - "Episode persistence must preserve corpus version and genome lineage so operators can replay why one generation passed or failed."
  artifacts:
    - path: "crates/swarm-runtime/src/kitten_agent.rs"
      provides: "Generation-scoped adversarial pressure wired into candidate evaluation."
    - path: "crates/swarm-evolution/src/mutation.rs"
      provides: "Durable evolution episode records and frozen corpus metadata."
    - path: "crates/swarm-runtime/src/evolution_status.rs"
      provides: "Operator-visible status for corpus version and episode progress."
  key_links:
    - from: "crates/swarm-runtime/src/red_swarm.rs"
      to: "crates/swarm-runtime/src/kitten_agent.rs"
      via: "generation-scoped corpus snapshot"
      pattern: "red pressure enters blue fitness"
    - from: "crates/swarm-evolution/src/mutation.rs"
      to: "crates/swarm-runtime/src/evolution_status.rs"
      via: "durable episode summaries"
      pattern: "auditable generation history"
---

<objective>
Complete Phase 144 by scoring candidate strategies against a frozen adversarial corpus snapshot per generation and persisting durable red-blue episode records.

Purpose: finish v1.43 by making adversarial pressure first-class in the evolution lane and preserving the evidence needed to explain each generation after restart.
Output: generation-scoped red corpus usage inside Kitten fitness, durable `EvolutionEpisode` persistence, and operator-visible corpus plus episode status.
</objective>

<context>
@.planning/phases/144-red-blue-pressure-and-episode-logging/144-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/kitten_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/red_swarm.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/mutation.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze one adversarial corpus snapshot per generation</name>
  <read_first>
    crates/swarm-runtime/src/kitten_agent.rs
    crates/swarm-runtime/src/red_swarm.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/kitten_agent.rs
    crates/swarm-runtime/src/red_swarm.rs
  </files>
  <action>Teach Kitten to materialize one generation-scoped adversarial corpus snapshot through `RedSwarmAdapter`, reuse that snapshot across candidate evaluations in the same generation, and record the frozen corpus version in candidate fitness artifacts.</action>
  <verify>
    <automated>cargo test -p swarm-runtime kitten_agent::tests:: -- --nocapture</automated>
  </verify>
  <done>Adversarial pressure is reproducible within a generation and refreshed only when the generation changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist durable red-blue evolution episodes</name>
  <read_first>
    crates/swarm-evolution/src/mutation.rs
  </read_first>
  <files>
    crates/swarm-evolution/src/mutation.rs
    crates/swarm-runtime/src/runtime_events.rs
  </files>
  <action>Add repo-owned `EvolutionEpisode` records and a file-backed store that captures episode ID, generation, corpus version, genome hash, per-threat-class coverage, and blue versus red fitness vectors for each evaluated candidate.</action>
  <verify>
    <automated>cargo check -p swarm-runtime -p swarm-evolution --tests -j 1 --message-format short</automated>
  </verify>
  <done>Red-blue episode history survives restart and exposes enough metadata to explain generation outcomes offline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface corpus and episode state through evolution status</name>
  <read_first>
    crates/swarm-runtime/src/evolution_status.rs
    crates/swarm-runtime/src/kitten_agent.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/evolution_status.rs
    crates/swarm-runtime/src/runtime_events.rs
  </files>
  <action>Extend the existing evolution status surface so runtime events and operator status can report the active corpus version, latest episode summary, and any generation-level pressure results without duplicating mutation state elsewhere.</action>
  <verify>
    <automated>cargo test -p swarm-runtime evolution_status::tests:: -- --nocapture</automated>
  </verify>
  <done>Operators can see which adversarial corpus and episode history produced the current evolution state.</done>
</task>

</tasks>

<verification>
Wire corpus freezing into Kitten first, then persist durable episode records, then surface the new state through the existing evolution-status channel. Keep the phase bounded to generation pressure and episode logging; do not reopen Phase 143 retention or adapter concerns unless verification shows a regression.
</verification>
