---
phase: 286-collective-hypothesis-graph
plan: 07B
type: execute
wave: 13
depends_on: [286-07]
files_modified:
  - tools/check-phase286-review.py
  - .planning/phases/286-collective-hypothesis-graph/286-P0-P2-REVIEW.md
  - .planning/phases/286-collective-hypothesis-graph/286-VERIFICATION.md
  - .planning/phases/286-collective-hypothesis-graph/286-07B-SUMMARY.md
autonomous: true
requirements: [COG-08]

must_haves:
  truths:
    - "Final review and verification are independent of the implementation agent; an exact parser recomputes severity counts from each artifact's finding rows and checks the anchored open_p0/open_p1/open_p2 counters rather than trusting self-attested totals."
    - "The complete combined tree, including Plans 06B, 06C, and this final gate, is verified before Phase 286 advances."
    - "Both artifacts are provenance-bound to the current HEAD and retain the exact locked/offline gate, bridge, authority, and mutation evidence that was reviewed."
  artifacts:
    - path: ".planning/phases/286-collective-hypothesis-graph/286-P0-P2-REVIEW.md"
      provides: "independent severity review"
    - path: ".planning/phases/286-collective-hypothesis-graph/286-VERIFICATION.md"
      provides: "goal-backward combined-tree verification"
    - path: ".planning/phases/286-collective-hypothesis-graph/286-07B-SUMMARY.md"
      provides: "independent closure summary with exact commands, evidence paths, and retained provenance"
    - path: "tools/check-phase286-review.py"
      provides: "exact non-circular severity parser and mutation self-test"
  key_links:
    - from: "tools/check-phase286-review.py"
      to: ".planning/phases/286-collective-hypothesis-graph/286-P0-P2-REVIEW.md"
      via: "recomputed open-severity counts from structured findings, not declared totals"
      pattern: "open_p0|open_p1|open_p2|finding_id"
    - from: "tools/check-phase286-review.py"
      to: ".planning/phases/286-collective-hypothesis-graph/286-VERIFICATION.md"
      via: "independent artifact-kind, current HEAD/tree, report digest, and exact command evidence"
      pattern: "reviewed_head|source_tree_digest|collective-report-one.json"
---

<objective>
Perform the independent final P0/P1/P2 review and verification split from Plan 07.
</objective>

<context>
Plan 07 owns benchmark metrics, CI wiring, the validation matrix, retained
reports, and `286-07-SUMMARY.md`. This plan owns the final review parser,
`286-P0-P2-REVIEW.md`, `286-VERIFICATION.md`, and the required
`286-07B-SUMMARY.md`; no other plan may create or update the verification
artifact. This plan must not self-approve implementation: the reviewer and
verifier are independent of the Plan 07 implementer, and the parser derives
open-severity counts from structured finding rows in both independent
artifacts.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Run independent severity review and combined-tree verification</name>
  <files>tools/check-phase286-review.py, .planning/phases/286-collective-hypothesis-graph/286-P0-P2-REVIEW.md, .planning/phases/286-collective-hypothesis-graph/286-VERIFICATION.md, .planning/phases/286-collective-hypothesis-graph/286-07B-SUMMARY.md</files>
  <action>Assign an independent reviewer to inspect the frozen combined tree at the current HEAD, not an implementation-agent assertion. Bind `reviewed_head` to `git rev-parse --verify HEAD` and `source_tree_digest` to `git rev-parse --verify HEAD^{tree}`; do not silently substitute a worktree or index digest. Inspect frozen oracle ownership, GraphLogicalTime/GraphClock boundary, one-shot bridge capture, no-action zero-call path, authority traversal, disabled compatibility, privacy, determinism, mutation controls, retained report digests, and exact gate evidence. Write both artifacts with an explicit schema/version, artifact kind, `reviewed_head`, `source_tree_digest`, independent reviewer/implementer identifiers, an exact structured findings table (`finding_id`, `severity` in `P0|P1|P2|P3|none`, `status` in `open|closed|not_applicable`, and evidence anchor), and exactly one anchored `open_p0: N`, `open_p1: N`, and `open_p2: N` line. Implement `tools/check-phase286-review.py` as a fail-closed parser that independently parses both artifacts, recomputes each open count from findings rows, rejects missing/extra/duplicate fields, unknown severities/statuses, duplicate counters, stale or non-current HEAD/tree digest, missing evidence anchors, missing retained report digests, and any summary-only or self-attested zero. It must never parse the Plan 07 summary as authority and must require the two independent artifact kinds to agree on current provenance and the exact Phase 286 gate evidence. Its `--self-test` must mutate a finding severity/status, remove or duplicate an anchored counter, alter HEAD/tree digest, remove an evidence/report anchor, and replace the bridge/authority command with a weaker command; every mutation must fail. Run the complete gate and workspace checks, then write the required `286-07B-SUMMARY.md` with exact commands, parser output, evidence paths, current HEAD, and disposition; keep Phase 286 open on any nonzero count or missing evidence.</action>
  <verify><automated>python3 tools/check-phase286-review.py --root . --self-test && bash tools/check-collective-hypothesis-graph.sh && cargo test --workspace --all-targets --locked --offline && cargo clippy --workspace --all-targets --all-features --locked --offline -- -D warnings && cargo fmt --all -- --check && git diff --check</automated></verify>
  <done>The independent parser proves both artifacts have recomputed zero open P0/P1/P2 findings, current-HEAD provenance, retained report/command evidence, and complete combined-tree coverage; the required 07B summary records the result.</done>
</task>
</tasks>

<verification>Run `python3 tools/check-phase286-review.py --root . --self-test` directly (no shell recursion), then run the phase gate twice with its retained reports, compare deterministic report bytes and SHA-256 values, run the strict validation matrix, full workspace checks, and the exact severity-counter parser against the frozen current tree. The parser must independently derive counters from both artifact finding tables and reject any artifact that merely claims `open_p*: 0` without matching closed rows and evidence.

Final commands, in this order, are:

```text
python3 tools/check-phase286-validation-matrix.py --strict --self-test --cwd .
bash tools/check-collective-hypothesis-graph.sh
python3 tools/check-phase286-review.py --root . --self-test
cargo test --workspace --all-targets --locked --offline
cargo clippy --workspace --all-targets --all-features --locked --offline -- -D warnings
cargo fmt --all -- --check
git diff --check
```

The Phase 286 CI job runs the same direct commands and uploads the retained
reports/evidence; it does not invoke another workflow, itself, or a wrapper
that invokes the phase gate recursively.</verification>

<success_criteria>
- Final review is independent and anchored.
- No phase advancement occurs without all severity counters at zero.
</success_criteria>

<output>Record independent review, verification, exact commands, retained report/evidence paths, current HEAD/tree digest, and parser output in `286-07B-SUMMARY.md`. Do not mark the phase complete or update roadmap/requirements until the parser and all severity counters pass.</output>
