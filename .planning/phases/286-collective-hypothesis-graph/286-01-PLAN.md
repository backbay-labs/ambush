---
phase: 286-collective-hypothesis-graph
plan: 01
type: execute
wave: 2
depends_on: [286-00]
files_modified:
  - crates/swarm-core/src/hypothesis_graph.rs
  - crates/swarm-core/src/config/hypothesis.rs
  - crates/swarm-core/src/config/mod.rs
  - crates/swarm-core/src/config/root.rs
  - crates/swarm-core/src/config/defaults.rs
  - crates/swarm-core/src/config/validation.rs
  - crates/swarm-core/src/config/tests.rs
  - crates/swarm-core/src/lib.rs
  - rulesets/default.yaml
autonomous: true
requirements: [COG-01, COG-02, COG-03, COG-04, COG-05, COG-06, COG-07]

must_haves:
  truths:
    - "Graph, evidence, hypothesis, task, kill-chain, containment, and strategy-memory artifacts are strict typed records with explicit schema versions and resource limits."
    - "Ambiguous evidence can preserve competing live hypotheses, explicit contradictions, uncertainty, and append-only decisions."
    - "Confidence and metric-critical values use bounded integer representations, never NaN-capable floats."
  artifacts:
    - path: "crates/swarm-core/src/hypothesis_graph.rs"
      provides: "shared collective-reasoning contracts and fail-closed validation"
    - path: "crates/swarm-core/src/config/hypothesis.rs"
      provides: "repo-owned graph/task/memory resource limits"
  key_links:
    - from: "crates/swarm-core/src/hypothesis_graph.rs"
      to: "crates/swarm-core/src/types.rs"
      via: "base AgentId identity and existing response preview data contracts"
      pattern: "AgentId"
---

<objective>
Define the strict shared contract for collective cyber reasoning before persistence or orchestration is added.

Purpose: every later phase component must agree on canonical typed evidence, graph, hypothesis, task, kill-chain, containment-simulation, memory, and metric records without relying on free-form JSON.
Output: validated public records, stable ID helpers, resource limits, and focused property/negative tests in `swarm-core`.
</objective>

<context>
@.planning/phases/286-collective-hypothesis-graph/286-CONTEXT.md
@.planning/phases/286-collective-hypothesis-graph/286-RESEARCH.md
@.planning/phases/286-collective-hypothesis-graph/286-VALIDATION.md
@crates/swarm-core/src/telemetry.rs
@crates/swarm-core/src/signed_state.rs
@crates/swarm-core/src/types.rs
@crates/swarm-core/src/config/mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict canonical evidence and graph contracts</name>
  <files>crates/swarm-core/src/hypothesis_graph.rs, crates/swarm-core/src/lib.rs</files>
  <action>Write failing tests first, then implement typed IDs, five required node kinds, typed relations, immutable evidence envelopes, lineage/clock/ordering/witness records, conflicts, causal edges, graph versions, admission errors, and canonical SHA-256 ID derivation through `swarm-crypto`. Use `deny_unknown_fields`, `BTreeMap`/`BTreeSet`, integer basis points, explicit schema constants, and constructors that validate all nested limits before admission. Same ID with different bytes and edges without existing endpoints/evidence must fail closed.</action>
  <verify><automated>cargo test -p swarm-core hypothesis_graph --lib</automated></verify>
  <done>All required graph/evidence kinds serialize deterministically, round-trip strictly, reject malformed/unproven/over-limit records, and retain explicit partial or unknown ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add competing hypothesis, task, kill-chain, simulation, memory, and metric records</name>
  <files>crates/swarm-core/src/hypothesis_graph.rs</files>
  <action>Add state-machine tests first, then implement confidence distributions summing to 10,000, uncertainty and contradiction records, append-only decision history, adjudication references, typed task kinds/scopes/leases/fences/states/completions, an explicit integer `GraphLogicalTime`/scheduler-key contract, evidence-linked kill-chain claims and missing-evidence records, simulation-only containment options, raw-telemetry-free strategy memory, and deterministic collective metric reports with explicit denominators. Do not add `SystemTime`, `Instant`, `ResponseAction`, executor, lease-authority, arbitrary `Value`, raw command line, or raw vendor payload fields to deterministic graph records or memory.</action>
  <verify><automated>cargo test -p swarm-core hypothesis_graph --lib</automated></verify>
  <done>An ambiguous seed can represent two live hypotheses; task and memory artifacts cannot become authority; every kill-chain/simulation/metric claim is typed and evidence-linked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire repo-owned limits and fail-closed config validation</name>
  <files>crates/swarm-core/src/config/hypothesis.rs, crates/swarm-core/src/config/mod.rs, crates/swarm-core/src/config/root.rs, crates/swarm-core/src/config/defaults.rs, crates/swarm-core/src/config/validation.rs, crates/swarm-core/src/config/tests.rs, rulesets/default.yaml</files>
  <action>Add a disabled-by-default `hypothesis_graph` config with bounded nodes, edges, evidence bytes, evidence references per edge, hypotheses, decisions, tasks, lease duration, retries, memory records, graph depth/fan-out, and benchmark work units. Reject zero, contradictory, or unsafe limits. Add the checked-in default shape without changing existing runtime mode or response defaults.</action>
  <verify><automated>cargo test -p swarm-core config::tests::hypothesis_graph --lib && cargo fmt --all -- --check && git diff --check</automated></verify>
  <done>The repo can express a bounded collective graph and rejects resource-exhausting or internally inconsistent configurations before startup.</done>
</task>

</tasks>

<verification>
Run `cargo test -p swarm-core hypothesis_graph --lib`, the focused config tests, clippy for `swarm-core`, format, and diff checks. Mutation controls must prove that removing evidence, widening confidence, changing schema, or exceeding each major cap fails.
</verification>

<success_criteria>
- Strict shared contracts cover all COG artifact families without a second runtime or free-form graph schema.
- Ambiguous hypotheses remain concurrently representable and cannot be deleted without an evidence-linked decision.
- Resource bounds and stable canonical identities are enforced at construction and deserialization boundaries.
</success_criteria>

<output>After completion, create `286-01-SUMMARY.md` with exact tests, changed files, and any contract deviation.</output>
