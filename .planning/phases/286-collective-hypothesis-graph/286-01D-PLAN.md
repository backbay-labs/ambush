---
phase: 286-collective-hypothesis-graph
plan: 01D
type: execute
wave: 7
depends_on: [286-01C]
files_modified:
  - crates/swarm-core/src/hypothesis_graph.rs
  - crates/swarm-core/src/lib.rs
  - crates/swarm-core/src/config/tests.rs
autonomous: true
requirements: [COG-01, COG-03, COG-04, COG-08]

must_haves:
  truths:
    - "Post-Plan-02 boundary regressions preserve the existing source-record EventNode identity, semantic typed-payload validation, checked graph-version overflow, and signed-or-untrusted witness semantics without adding duplicate APIs."
    - "Distinct same-kind records at the same logical millisecond cannot collide, while exact source-record retries remain idempotent."
    - "Malformed direct payloads, unsigned witness metadata, and graph-version exhaustion fail closed without mutating canonical state."
  artifacts:
    - path: "crates/swarm-core/src/hypothesis_graph.rs"
      provides: "adversarial regression coverage for existing EventNode, EvidenceWitness, TypedEvidencePayload, and HypothesisGraph contracts"
    - path: "crates/swarm-core/src/lib.rs"
      provides: "public regression-test imports for existing core graph contracts"
    - path: "crates/swarm-core/src/config/tests.rs"
      provides: "cross-module mutation fixtures and canonical-byte preservation assertions"
  key_links:
    - from: "crates/swarm-core/src/hypothesis_graph.rs"
      to: "dccbcd5 core contracts"
      via: "tests exercise existing APIs rather than defining replacement identity, witness, payload, or version types"
      pattern: "EventNode::new|EvidenceWitness|TypedEvidencePayload|bump_version"
---

<objective>
Preserve the current dccbcd5 core hardening as executable adversarial
regressions after the additive 01C contracts land. This is a narrow follow-up
plan: it does not retroactively rewrite historical Plan 01/02/03 ownership and
does not add a duplicate identity, witness, payload, or version API.
</objective>

<context>
@.planning/phases/286-collective-hypothesis-graph/286-01C-PLAN.md
@.planning/phases/286-collective-hypothesis-graph/286-02-PLAN.md
@crates/swarm-core/src/hypothesis_graph.rs
@crates/swarm-core/src/lib.rs
@crates/swarm-core/src/config/tests.rs
</context>

<interfaces>
The current tree already owns the source-record/evidence-bound
`EventNode::new(event_kind, source_record_id, observed_at)` identity, signed
`EvidenceWitness` role/scope binding, semantically validated direct
`TypedEvidencePayload` deserialization, and checked
`HypothesisGraph::bump_version`. This plan consumes those exact public APIs;
it must not introduce a second validator, a new source-record ID type, an
unsigned trust shortcut, or an unchecked version increment. Plan 02 remains
the owner of the CloudTrail event-scoped unknown-identity and lower-priority
identity-digest regression; this plan preserves that boundary without copying
the parser test.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve source-record identity and witness trust invariants</name>
  <files>crates/swarm-core/src/hypothesis_graph.rs, crates/swarm-core/src/lib.rs</files>
  <action>Write the adversarial regressions first against the existing dccbcd5 contracts. Assert that two same-kind events at one `GraphLogicalTime` with different source-record IDs produce distinct `GraphNodeId` values, while an exact source-record retry is idempotent. Assert that role and scope are trusted only when covered by the existing canonical witness signature and key-derived producer identity; an unsigned/default witness is explicitly untrusted and cannot be admitted. Add named tests `event_node_same_time_different_source_records_are_distinct` and `unsigned_witness_is_untrusted`. Mutation tests must fail if source-record material is removed, role/scope is taken from unsigned fields, or the witness identity is replaced by a role/scoped alias. Do not create or rename any core API.</action>
  <verify><automated>cargo test -p swarm-core --lib --locked --offline hypothesis_graph::tests::event_node_same_time_different_source_records_are_distinct -- --exact --test-threads=1 && cargo test -p swarm-core --lib --locked --offline hypothesis_graph::tests::unsigned_witness_is_untrusted -- --exact --test-threads=1</automated></verify>
  <done>Existing source-record identity and signed-or-untrusted witness semantics are regression-tested with no duplicate implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve direct payload validation and version-overflow atomicity</name>
  <files>crates/swarm-core/src/hypothesis_graph.rs, crates/swarm-core/src/config/tests.rs</files>
  <action>Write direct serde and overflow mutations first. Deserialize `TypedEvidencePayload` directly through serde and require its existing semantic validator to reject malformed entities, empty or bad digests, family mismatches, invalid confidence, and invalid logical expiry. Exercise the existing checked `HypothesisGraph::bump_version` at `u64::MAX`; require refusal and byte-identical canonical state before and after the attempt. Add named tests `typed_evidence_payload_direct_deserialize_is_validated` and `graph_version_overflow_is_fail_closed`. Keep Plan 02's CloudTrail unknown-identity/digest-completeness regression as the sole parser ownership and do not duplicate it here.</action>
  <verify><automated>cargo test -p swarm-core --lib --locked --offline hypothesis_graph::tests::typed_evidence_payload_direct_deserialize_is_validated -- --exact --test-threads=1 && cargo test -p swarm-core --lib --locked --offline hypothesis_graph::tests::graph_version_overflow_is_fail_closed -- --exact --test-threads=1 && cargo test -p swarm-core config::tests::hypothesis_graph --lib --locked --offline && cargo fmt --all -- --check && git diff --check</automated></verify>
  <done>Direct deserialization and version exhaustion fail closed without accepting malformed evidence or mutating graph bytes.</done>
</task>

</tasks>

<verification>
Run both exact regression gates with `--locked --offline`, then format and diff
checks. Confirm the combined tree still has one CloudTrail unknown-identity
owner in historical Plan 02 and no duplicate source-record, witness, payload,
or version API.
</verification>

<success_criteria>
- Existing dccbcd5 identity, witness, payload, and version contracts remain executable and mutation-tested.
- No historical Plan 01/02/03 claim is rewritten and no second parser/validator/identity API is introduced.
</success_criteria>

<output>After completion, create `286-01D-SUMMARY.md` with regression and mutation evidence.</output>
