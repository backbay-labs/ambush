---
phase: 286-collective-hypothesis-graph
plan: 03
type: execute
wave: 5
depends_on: [286-01, 286-01B]
files_modified:
  - crates/swarm-spine/Cargo.toml
  - crates/swarm-spine/src/hypothesis_graph_store.rs
  - crates/swarm-spine/src/strategy_memory.rs
  - crates/swarm-spine/src/lib.rs
autonomous: true
requirements: [COG-01, COG-03, COG-07]

must_haves:
  truths:
    - "Graph mutations and task claims are durable, signed/digest chained, compare-and-swap protected, and fenced across restart."
    - "Memory and local-file backends implement identical logical semantics."
    - "A stale lease holder can never complete after expiry/reclaim, and tampered persisted state never contributes to reasoning."
  artifacts:
    - path: "crates/swarm-spine/src/hypothesis_graph_store.rs"
      provides: "graph snapshot and durable fenced task ledger stores"
    - path: "crates/swarm-spine/src/strategy_memory.rs"
      provides: "signed append-only privacy-minimized strategy memory store"
  key_links:
    - from: "crates/swarm-core/src/hypothesis_graph.rs"
      to: "crates/swarm-spine/src/hypothesis_graph_store.rs"
      via: "validated graph/task records and explicit logical time drive durable CAS transitions"
      pattern: "GraphLogicalTime"
    - from: "crates/swarm-spine/src/hypothesis_graph_store.rs"
      to: "crates/swarm-runtime/src/hypothesis_graph/service.rs"
      via: "durable graph snapshots and fenced task ledger are orchestrated by the runtime service"
      pattern: "HypothesisGraphStore"
---

<objective>
Build the durable epistemic state and stigmergic claim ledger with backend-parity, replay, and fencing guarantees.

Purpose: process-local sets and plain JSON writes cannot prevent duplicate hunts or stale-worker commits.
Output: in-memory and local-file graph/task/memory stores with signed generation/digest CAS and restart-safe semantics.
</objective>

<context>
@.planning/phases/286-collective-hypothesis-graph/286-RESEARCH.md
@crates/swarm-governance/src/lib.rs
@crates/swarm-core/src/signed_state.rs
@crates/swarm-spine/src/investigation.rs
@crates/swarm-spine/src/store.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement graph snapshot and task-ledger state machines</name>
  <files>crates/swarm-spine/src/hypothesis_graph_store.rs, crates/swarm-spine/src/lib.rs</files>
  <action>Start with shared operation-log tests against the already-owned `swarm_spine::hypothesis_graph_store::HypothesisGraphStore` contract; do not introduce a `GraphStore` alias or second trait. Its exact read/CAS API is `snapshot() -> Result<GraphStoreSnapshot, GraphStoreError>` and `compare_and_swap(&GraphStoreRevision, GraphStoreState) -> Result<GraphStoreSnapshot, GraphStoreError>`. Preserve the existing `GraphStoreState::new(graph)` constructor and let Plan 04 extend state through its explicit reasoning-state adapter. Implement memory storage for immutable evidence/graph versions plus the Plan 03 graph/tasks/high-water state and task transitions. Every transition accepts explicit `GraphLogicalTime`; no store backend reads SystemTime/Instant. Bind each transition to generation, predecessor digest, lease ID, fencing token, admitted GraphRecordSigner verification, and nondecreasing high-water. Preserve terminal/history records; reject stale predecessor, same-generation/different-digest, stale fence/time, replay, invalid signature, and over-limit state. The Plan 03 contract test must compile the exact trait methods and the Plan 04 adapter, proving the runtime coordinator consumes this existing store without a renamed facade.</action>
  <verify><automated>cargo test -p swarm-spine hypothesis_graph_store --lib --locked --offline</automated></verify>
  <done>State-machine tests prove one logical claimant, explicit expiry/reclaim, and rejection of stale completions without mutating the stale caller.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add durable local-file CAS, lock binding, atomic persistence, and backend parity</name>
  <files>crates/swarm-spine/Cargo.toml, crates/swarm-spine/src/hypothesis_graph_store.rs</files>
  <action>Model the existing governance persistence protocol: lifetime file lock, no-follow/symlink checks on Unix, 0600 files, signed state kind/stream/signer expectations, generation plus predecessor digest, create-new temp files, file sync, atomic rename, parent-directory sync, and restart validation. Persist the Plan 03-owned `GraphStoreState` fields (`schema_version`, graph identity/generation/predecessor, graph, tasks, tombstones, fencing counter, and `logical_time_high_water`); do not claim ownership of the later Plan 04 reasoning extension. Plan 04/03B will add and test its migration/persistence adapter after this protocol is complete. A reopened store must never lower high-water. Run one deterministic logical operation vector with explicit logical timestamps against memory and reopened file stores and compare canonical state. Repeat with host-clock/insertion perturbations; canonical state must remain identical. Add two-handle stale write, corrupt/tampered state, missing anchor, lock contention, crash-boundary, replay, and lease fencing tests.</action>
  <verify><automated>cargo test -p swarm-spine hypothesis_graph_store --lib --locked --offline</automated></verify>
  <done>The file store is not last-writer-wins; it is restart-safe, tamper-evident, single-writer bound, and semantically identical to memory for the declared operation log.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist signed privacy-minimized strategy memories</name>
  <files>crates/swarm-spine/src/strategy_memory.rs, crates/swarm-spine/src/lib.rs</files>
  <action>Implement append-only signed memory storage, canonical dedupe, bounded listing/retrieval, provenance validation, and deterministic match ordering for the Plan 03 memory record. Enforce types that contain only graph/hypothesis/evidence IDs, bounded abstractions, utility, falsified alternatives, outcome and transformations—never raw telemetry, secrets, command lines, request objects, or arbitrary JSON. Add applicable/irrelevant/tampered/replayed/restart/privacy tests. A subsequent corrective pass may add logical TTL/expiry without changing this historical store boundary.</action>
  <verify><automated>cargo test -p swarm-spine strategy_memory --lib --locked --offline && cargo clippy -p swarm-spine --all-targets --locked --offline -- -D warnings</automated></verify>
  <done>Strategy memory survives restart, fails closed on tamper/replay, expires at a deterministic logical boundary, and cannot serialize raw telemetry by construction.</done>
</task>

</tasks>

<verification>
Run all `swarm-spine` tests and layering checks. Independently inspect that no transport, runtime, policy decision, response executor, or authority logic entered the spine.
</verification>

<success_criteria>
- Durable claims prove idempotency, CAS, fencing, expiry/reclaim, and backend parity.
- Persisted graph and memory artifacts are signed, bounded, replay-resistant, and revalidated on load.
- Strategy memory is advisory data only and has no raw-telemetry representation.
</success_criteria>

<output>After completion, create `286-03-SUMMARY.md` with the persistence protocol and adversarial test matrix.</output>
