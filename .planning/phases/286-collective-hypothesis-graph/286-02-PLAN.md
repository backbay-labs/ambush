---
phase: 286-collective-hypothesis-graph
plan: 02
type: execute
wave: 3
depends_on: [286-01]
files_modified:
  - crates/swarm-runtime/src/hypothesis_graph/mod.rs
  - crates/swarm-runtime/src/hypothesis_graph/normalize.rs
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/tests/collective_hypothesis_graph.rs
autonomous: true
requirements: [COG-01, COG-04]

must_haves:
  truths:
    - "Process, identity, Kubernetes, CloudTrail, network, and threat-intelligence records normalize into one strict evidence envelope without a second source parser."
    - "Conflicting signals remain separately attributable; ingestion time or legacy host_id never silently becomes causal truth."
    - "Witness signatures bind canonical bytes to an admitted base signing identity, not a scoped role label."
  artifacts:
    - path: "crates/swarm-runtime/src/hypothesis_graph/normalize.rs"
      provides: "bounded source-specific adapters into EvidenceEnvelope"
---

<objective>
Normalize Ambush's six required signal families into immutable, signed, lineage-aware evidence for the shared graph.

Purpose: the graph cannot reason causally if source identity, clock uncertainty, raw-field bounds, and contradictions are collapsed into strings.
Output: normalization coordinator, source adapters, witness admission, and a cross-telemetry integration fixture.
</objective>

<context>
@.planning/phases/286-collective-hypothesis-graph/286-RESEARCH.md
@crates/swarm-core/src/telemetry.rs
@crates/swarm-pheromone/src/substrate.rs
@crates/swarm-runtime/src/detection/pipeline.rs
@crates/swarm-runtime/src/threat_intel_runtime.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement strict telemetry and threat-intel normalization</name>
  <files>crates/swarm-runtime/src/hypothesis_graph/mod.rs, crates/swarm-runtime/src/hypothesis_graph/normalize.rs, crates/swarm-runtime/src/lib.rs</files>
  <action>Write adapter tests before implementation. Map existing `TelemetryEvent` payloads for process, authentication/identity, Kubernetes audit, CloudTrail, network/DNS and existing `ThreatIntelEntry` records into typed envelopes. Preserve observed/ingest time distinctions, source record IDs, partial/unknown order, AWS-account versus host semantics, bounded hashes/projections for raw request objects, and typed entity/relation proposals. Reuse current parsers; never parse vendor JSON again in this module.</action>
  <verify><automated>cargo test -p swarm-runtime hypothesis_graph::normalize --lib</automated></verify>
  <done>Every required source family produces a strict bounded envelope with correct source semantics and no unbounded raw JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce witness admission, idempotency, and visible conflict records</name>
  <files>crates/swarm-runtime/src/hypothesis_graph/mod.rs, crates/swarm-runtime/src/hypothesis_graph/normalize.rs, crates/swarm-runtime/tests/collective_hypothesis_graph.rs</files>
  <action>Add canonical-byte signature verification, key-derived base `AgentId` admission, exact-envelope idempotency, same-ID/different-content rejection, and deterministic conflict detection. Tests must cover tampered bytes, wrong/unadmitted keys, scoped role aliases, source-time conflict, same source record with different facts, and nested secret/raw-field exclusion.</action>
  <verify><automated>cargo test -p swarm-runtime --test collective_hypothesis_graph -- --exact cross_telemetry_fixture_preserves_conflicts --nocapture</automated></verify>
  <done>Corroboration and contradiction are both preserved with independent provenance; no role suffix can inflate source diversity.</done>
</task>

</tasks>

<verification>
Run normalization unit tests, the exact cross-telemetry integration test, clippy for `swarm-runtime`, format, and diff checks. The fixture must exercise all six source families with at least one corroborating and one conflicting observation each.
</verification>

<success_criteria>
- COG-04's six signal families share one typed evidence contract.
- CloudTrail/Kubernetes/process identity semantics are not conflated through legacy fields.
- Tampered, ambiguous, or conflicting evidence fails closed or remains explicitly contested rather than being silently averaged.
</success_criteria>

<output>After completion, create `286-02-SUMMARY.md` with exact source coverage and negative-test evidence.</output>
