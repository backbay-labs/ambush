---
phase: 286-collective-hypothesis-graph
plan: 02
type: execute
wave: 5
depends_on: [286-01, 286-01B]
files_modified:
  - crates/swarm-runtime/src/hypothesis_graph/mod.rs
  - crates/swarm-runtime/src/hypothesis_graph/clock.rs
  - crates/swarm-runtime/src/hypothesis_graph/normalize.rs
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/tests/collective_hypothesis_graph.rs
autonomous: true
requirements: [COG-01, COG-03, COG-04, COG-08]

must_haves:
  truths:
    - "Process, identity, Kubernetes, CloudTrail, network, and threat-intelligence records normalize into one strict evidence envelope without a second source parser."
    - "Conflicting signals remain separately attributable; ingestion time or legacy host_id never silently becomes causal truth."
    - "Witness signatures bind canonical bytes to an admitted base signing identity, not a scoped role label."
  artifacts:
    - path: "crates/swarm-runtime/src/hypothesis_graph/clock.rs"
      provides: "injected logical clock and deterministic scheduler used by every graph decision"
    - path: "crates/swarm-runtime/src/hypothesis_graph/normalize.rs"
      provides: "bounded source-specific adapters into EvidenceEnvelope"
  key_links:
    - from: "crates/swarm-core/src/hypothesis_graph.rs"
      to: "crates/swarm-runtime/src/hypothesis_graph/clock.rs"
      via: "runtime GraphClock emits the shared GraphLogicalTime contract consumed by stores"
      pattern: "GraphLogicalTime"
---

<objective>
Normalize Ambush's six required signal families into immutable, signed, lineage-aware evidence for the shared graph.

Purpose: the graph cannot reason causally if source identity, clock uncertainty, raw-field bounds, and contradictions are collapsed into strings.
Output: normalization coordinator, source adapters, witness admission, and a cross-telemetry integration fixture.
</objective>

<context>
@.planning/phases/286-collective-hypothesis-graph/286-RESEARCH.md
@crates/swarm-core/src/telemetry.rs
@crates/swarm-pheromone/src/substrate.rs
@crates/swarm-runtime/src/detection/pipeline.rs
@crates/swarm-runtime/src/threat_intel_runtime.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the injected graph clock and deterministic scheduler seam</name>
  <files>crates/swarm-runtime/src/hypothesis_graph/mod.rs, crates/swarm-runtime/src/hypothesis_graph/clock.rs, crates/swarm-runtime/src/lib.rs</files>
  <action>Write failing clock/scheduler tests, then publish exactly `swarm_runtime::hypothesis_graph::clock::GraphClock: Send + Sync { fn now_ms(&self) -> i64; }`, with `FixedGraphClock::new(start: swarm_core::hypothesis_graph::GraphLogicalTime) -> FixedGraphClock` and `FixedGraphClock` returning `start.as_millis()`. The only logical-time value type is `swarm_core::hypothesis_graph::GraphLogicalTime`; do not define a runtime replacement. Add the system-observation implementation only as a Blue/runtime wrapper and keep it out of Red's dependency surface. Add a deterministic scheduler ordered only by logical ready time, task kind, stable priority, and task ID. Every lease deadline, reclaim decision, task ordering decision, graph transition, and benchmark event receives explicit `GraphLogicalTime`; host `SystemTime`, `Instant`, Tokio wake order, and async arrival may be recorded only outside deterministic state. Add a perturbation test that varies host clock values, Tokio yield order, and insertion order while producing identical scheduler decisions.</action>
  <verify><automated>cargo test -p swarm-runtime hypothesis_graph::clock --lib --locked --offline</automated></verify>
  <done>The graph has one explicit logical-time/scheduler seam and deterministic decisions are invariant to host clock and async ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement strict telemetry and threat-intel normalization</name>
  <files>crates/swarm-runtime/src/hypothesis_graph/mod.rs, crates/swarm-runtime/src/hypothesis_graph/normalize.rs, crates/swarm-runtime/src/lib.rs</files>
  <action>Write adapter tests before implementation. Map existing `TelemetryEvent` payloads for process, authentication/identity, Kubernetes audit, CloudTrail, network/DNS and existing `ThreatIntelEntry` records into typed envelopes. Preserve observed/ingest time distinctions, source record IDs, partial/unknown order, AWS-account versus host semantics, bounded hashes/projections for raw request objects, and typed entity/relation proposals. Reuse current parsers; never parse vendor JSON again in this module.</action>
  <verify><automated>cargo test -p swarm-runtime hypothesis_graph::normalize --lib --locked --offline</automated></verify>
  <done>Every required source family produces a strict bounded envelope with correct source semantics and no unbounded raw JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce witness admission, idempotency, and visible conflict records</name>
  <files>crates/swarm-runtime/src/hypothesis_graph/mod.rs, crates/swarm-runtime/src/hypothesis_graph/normalize.rs, crates/swarm-runtime/tests/collective_hypothesis_graph.rs</files>
  <action>Create the separate behavior target `collective_hypothesis_graph.rs`; do not modify the sealed `collective_hypothesis_oracle.rs`. Add canonical-byte signature verification, key-derived base `AgentId` admission, exact-envelope idempotency, same-ID/different-content rejection, and deterministic conflict detection. Publish the exact runtime signer seam in `swarm_runtime::hypothesis_graph::GraphRecordSigner: Send + Sync` with `fn sign_edge(&self, edge: swarm_core::hypothesis_graph::CausalEdge, scoped_agent_id: &str) -> Result<swarm_core::hypothesis_graph::CausalEdge, swarm_core::hypothesis_graph::GraphAdmissionError>`, `fn sign_decision(&self, decision: swarm_core::hypothesis_graph::DecisionRecord, scoped_agent_id: &str) -> Result<swarm_core::hypothesis_graph::DecisionRecord, swarm_core::hypothesis_graph::GraphAdmissionError>`, `fn verify_edge(&self, edge: &swarm_core::hypothesis_graph::CausalEdge) -> Result<(), swarm_core::hypothesis_graph::GraphAdmissionError>`, and `fn verify_decision(&self, decision: &swarm_core::hypothesis_graph::DecisionRecord) -> Result<(), swarm_core::hypothesis_graph::GraphAdmissionError>`. Its repository implementation delegates to the existing core `CausalEdge::signed_with`/`DecisionRecord::signed_with` and `EvidenceWitness` validation over canonical bytes using the admitted `swarm_crypto::Keypair`; role/scope labels never replace key-derived identity. Plan 04 consumes this seam when appending edges/decisions, and the graph store revalidates both witnesses on reload. Tests must cover tampered bytes, wrong/unadmitted keys, scoped role aliases, source-time conflict, same source record with different facts, and nested secret/raw-field exclusion.</action>
  <verify><automated>cargo test -p swarm-runtime --test collective_hypothesis_graph --locked --offline -- --exact graph_record_signer_binds_edge_and_decision --nocapture && cargo test -p swarm-runtime --test collective_hypothesis_graph --locked --offline -- --exact cross_telemetry_fixture_preserves_conflicts --nocapture</automated></verify>
  <done>Corroboration and contradiction are both preserved with independent provenance; no role suffix can inflate source diversity.</done>
</task>

</tasks>

<verification>
Run normalization unit tests, the exact cross-telemetry integration test, clippy for `swarm-runtime`, format, and diff checks. The fixture must exercise all six source families with at least one corroborating and one conflicting observation each.
</verification>

<success_criteria>
- COG-04's six signal families share one typed evidence contract.
- CloudTrail/Kubernetes/process identity semantics are not conflated through legacy fields.
- Tampered, ambiguous, or conflicting evidence fails closed or remains explicitly contested rather than being silently averaged.
</success_criteria>

<output>After completion, create `286-02-SUMMARY.md` with exact source coverage and negative-test evidence.</output>
