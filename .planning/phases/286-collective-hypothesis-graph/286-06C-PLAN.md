---
phase: 286-collective-hypothesis-graph
plan: 06C
type: execute
wave: 11
depends_on: [286-06]
files_modified:
  - crates/swarm-runtime-http/src/http/control.rs
  - crates/swarm-runtime-http/src/http/state.rs
  - crates/swarm-runtime-http/src/lib.rs
  - crates/swarm-cli/src/core.inc
  - crates/swarm-cli/src/format.rs
  - crates/swarm-runtime/tests/collective_hypothesis_graph_surface.rs
autonomous: true
requirements: [COG-06, COG-07]

must_haves:
  truths:
    - "Operator graph projections are additive, authenticated, read-only, and unavailable when the graph is disabled."
    - "CLI rendering is deterministic and cannot approve, execute, or mutate a simulation."
    - "Every graph route is an authenticated bounded GET backed by the typed ingest projection; this plan has no authority, executor, lease, or mutation seam."
  artifacts:
    - path: "crates/swarm-runtime-http/src/http/control.rs"
      provides: "authenticated graph projection routes"
    - path: "crates/swarm-cli/src/core.inc"
      provides: "read-only graph inspection command"
  key_links:
    - from: "crates/swarm-runtime-http/src/http/control.rs"
      to: "crates/swarm-ingest-runtime/src/control.rs"
      via: "authenticated typed read-only projection lookup"
---

<objective>
Complete the HTTP and CLI surface split from Plan 06 without widening authority or legacy response shapes.
</objective>

<context>
Plan 06 owns the ingest-side handoff and service/runtime authority traversal. This plan owns only the five operator-surface files removed to keep each plan reviewable.
</context>

<file_ownership>
Plan 06C owns only the five HTTP/CLI implementation files and the independent
`collective_hypothesis_graph_surface.rs` test listed in its frontmatter. It
consumes the typed ingest projection from Plan 06 and never edits the graph
service, incident projection, authority seam, queue/bridge files, or shared
boundary scanner. Its routes and CLI are read-only projections, not a second
authority path.
</file_ownership>

<surface_boundary>
Plan 06C owns only the HTTP/CLI read surface and its independent surface test;
it does not modify the graph service, incident projection, authority handoff,
negative response boundary, ingest projection lookup, or existing runtime test
owned by Plan 04. The HTTP surface adds only bounded `GET` routes for the typed
ingest projection (graph summary, hypotheses/alternatives, task ledger,
kill-chain, containment simulations, and strategy-memory references). Each
route uses the existing bearer/schema authentication and operator-review scope
check, rejects disabled/missing/tampered graph state, and has no `POST`, `PUT`,
`PATCH`, or `DELETE` sibling. Handlers never receive a raw store or an
authority object and never call `RuntimeService::rehearse_selected_simulation`.

The CLI graph command performs the same authenticated GETs and renders only
deterministic typed DTOs. It has no approve/execute/apply flag, no response
executor, no lease or receipt mint, and no path to Pouncer, Tom, operator
approval, or dispatcher. The surface test is
`collective_hypothesis_graph_surface.rs`; it asserts route methods, auth and
schema failures, disabled/tampered refusal, stable rendering, and zero
authority calls while consuming the real Plan 06 projection types.
</surface_boundary>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add protected HTTP projections and deterministic CLI inspection</name>
  <files>crates/swarm-runtime-http/src/http/control.rs, crates/swarm-runtime-http/src/http/state.rs, crates/swarm-runtime-http/src/lib.rs, crates/swarm-cli/src/core.inc, crates/swarm-cli/src/format.rs, crates/swarm-runtime/tests/collective_hypothesis_graph_surface.rs</files>
  <read_first>crates/swarm-runtime-http/src/http/control.rs, crates/swarm-runtime-http/src/http/state.rs, crates/swarm-runtime-http/src/lib.rs, crates/swarm-cli/src/core.inc, crates/swarm-cli/src/format.rs, crates/swarm-ingest-runtime/src/control.rs, crates/swarm-runtime/tests/collective_hypothesis_graph.rs</read_first>
  <acceptance_criteria>
    - Every new graph endpoint is an authenticated bounded `GET` only, uses the existing bearer/schema/operator-review checks, and returns typed ingest projection data for summary, hypotheses/alternatives, tasks, kill-chain, simulations, or memory references.
    - No graph route or CLI path accepts `POST`, `PUT`, `PATCH`, `DELETE`, approve, execute, apply, lease, receipt-mint, or authority arguments; source checks show no call to `RuntimeService::rehearse_selected_simulation`, Pouncer, Tom, dispatcher, or a response executor.
    - Disabled, missing, malformed, stale, or tampered graph state returns the existing authenticated refusal shape without mutating a store; legacy non-graph response bytes remain unchanged.
    - CLI requests authenticated GET projections and renders deterministic typed output with stable ordering and no wall-clock fields.
  </acceptance_criteria>
  <action>Write failing end-to-end, route/parser, and render tests first. Consume the real typed ingest projection and exact Plan 06 graph DTOs, then add only authenticated bounded `GET` projections for the graph summary, hypotheses/alternatives, tasks, kill-chain, simulations, and memory references. Preserve existing response bytes, reject disabled/missing/tampered state, and expose no run/approve/execute/apply or mutation option. The CLI issues authenticated GETs and renders deterministic typed output; it never receives an authority handle. Keep this surface test independent from Plan 06's authority-boundary test and prove through route method assertions and recording adapters that HTTP/CLI cannot reach response authority.</action>
  <verify><automated>cargo test -p swarm-runtime-http hypothesis_graph --lib --locked --offline && cargo test -p swarm-cli hypothesis_graph --lib --locked --offline && cargo test -p swarm-runtime --test collective_hypothesis_graph_surface --locked --offline -- --exact authenticated_graph_surface_is_read_only --nocapture && cargo test -p swarm-runtime --test collective_hypothesis_graph_surface --locked --offline -- --exact disabled_or_tampered_graph_is_refused --nocapture && cargo fmt --all -- --check && git diff --check</automated></verify>
  <done>Authenticated operators can inspect the collective incident theory through additive surfaces, with disabled compatibility and no action side effects proven.</done>
</task>
</tasks>

<verification>Run HTTP auth/schema/disabled compatibility tests, CLI parser/render tests, and the independent `collective_hypothesis_graph_surface` route-method/authority-zero tests with `--locked --offline`; do not edit or duplicate Plan 06's authority-boundary test. Finish with format and diff checks.</verification>

<success_criteria>
- The five surface files and the owned surface test contain only additive read-only behavior; the existing Plan 04 graph integration test is untouched.
- The graph-enabled runtime path and exact dry-run receipt remain owned by Plans 06 and 06B.
</success_criteria>

<output>Create the supplementary summary with route/CLI evidence and independent authority-boundary results.</output>
