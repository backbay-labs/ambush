---
phase: 286-collective-hypothesis-graph
plan: 06
type: execute
wave: 6
depends_on: [286-02, 286-03, 286-04, 286-05]
files_modified:
  - crates/swarm-runtime/src/hypothesis_graph/service.rs
  - crates/swarm-runtime/src/hypothesis_graph/mod.rs
  - crates/swarm-runtime/src/service/runtime_service.rs
  - crates/swarm-spine/src/incident.rs
  - crates/swarm-ingest-runtime/src/control.rs
  - crates/swarm-runtime-http/src/http/control.rs
  - crates/swarm-runtime-http/src/http/state.rs
  - crates/swarm-runtime-http/src/lib.rs
  - crates/swarm-cli/src/core.inc
  - crates/swarm-cli/src/format.rs
  - crates/swarm-runtime/tests/collective_hypothesis_graph.rs
autonomous: true
requirements: [COG-01, COG-02, COG-03, COG-04, COG-05, COG-06, COG-07]

must_haves:
  truths:
    - "A seed signal runs through existing ingest/investigation seams into the shared graph and returns an evidence-linked incident projection."
    - "Operators can inspect graph, alternatives, contradictions, tasks, kill chain, simulations, and memory through additive authenticated surfaces."
    - "Existing investigation/incident response shapes remain backward compatible and no public platform API is silently widened."
---

<objective>
Complete the first end-to-end product slice and expose its persisted reasoning artifacts to operators without changing existing API contracts or action authority.
</objective>

<context>
@crates/swarm-runtime/src/service/runtime_service.rs
@crates/swarm-ingest-runtime/src/control.rs
@crates/swarm-runtime-http/src/http/control.rs
@crates/swarm-cli/src/core.inc
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Orchestrate seed-to-graph-to-incident through the existing runtime</name>
  <files>crates/swarm-runtime/src/hypothesis_graph/service.rs, crates/swarm-runtime/src/hypothesis_graph/mod.rs, crates/swarm-runtime/src/service/runtime_service.rs, crates/swarm-spine/src/incident.rs, crates/swarm-runtime/tests/collective_hypothesis_graph.rs</files>
  <action>Add a coordinator guarded by the exact repo-owned `SwarmConfig.hypothesis_graph.enabled` key introduced in Plan 01. The checked-in and serde default is `false`. When enabled, the existing processing/investigation path normalizes admitted evidence, seeds/contests hypotheses, persists graph/tasks, converges only through adjudication, reconstructs the kill chain, ranks simulations, and persists strategy memory. Extend incident records additively with defaulted typed graph/kill-chain references. When disabled, do not construct a graph store, emit graph artifacts, alter the legacy detect-only flow, or change the serialized investigation/incident shapes. Failure to load/validate required graph state while enabled must be explicit and fail closed for graph-derived claims while preserving legacy detect-only observability.</action>
  <verify><automated>cargo test -p swarm-runtime --test collective_hypothesis_graph -- --exact seed_signal_converges_through_real_runtime --nocapture && cargo test -p swarm-runtime --test collective_hypothesis_graph -- --exact disabled_hypothesis_graph_preserves_legacy_runtime --nocapture</automated></verify>
  <done>A checked-in suspicious seed produces inspectable competing hypotheses, claimed evidence work, a converged evidence-linked incident, ranked dry-run containment, and reusable memory through the real runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add authenticated additive operator graph projections</name>
  <files>crates/swarm-ingest-runtime/src/control.rs, crates/swarm-runtime-http/src/http/control.rs, crates/swarm-runtime-http/src/http/state.rs, crates/swarm-runtime-http/src/lib.rs</files>
  <action>Add protected `/v1/operator/investigations/{id}/hypothesis-graph` and `/v1/operator/incidents/{id}/hypothesis-graph` read routes using the existing operator bearer/schema negotiation and configured store state. Return `ControlEnvelope` projections without altering existing singular investigation/incident response shapes. When `SwarmConfig.hypothesis_graph.enabled` is false, the new projections are unavailable and all pre-existing routes and response bytes remain unchanged. Enforce exact IDs, list/resource bounds, missing/tampered artifact errors, and no mutation scope.</action>
  <verify><automated>cargo test -p swarm-runtime-http hypothesis_graph --lib && cargo test -p swarm-runtime-http disabled_hypothesis_graph_preserves_existing_operator_shapes --lib</automated></verify>
  <done>Operators can inspect persisted reasoning through authenticated backward-compatible routes; unauthenticated, malformed, missing, or tampered reads fail correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add CLI inspection and concise typed rendering</name>
  <files>crates/swarm-cli/src/core.inc, crates/swarm-cli/src/format.rs</files>
  <action>Add a read-only `hypothesis-graph` command that resolves an investigation/incident graph from configured stores, supports stable `--json`, and renders hypotheses, contradictions, unresolved/leased tasks, evidence coverage, kill-chain gaps, containment simulation ranking, and memory references. It must not contain a run/approve/execute option.</action>
  <verify><automated>cargo test -p swarm-cli hypothesis_graph --lib</automated></verify>
  <done>The collective incident theory is an operator-visible capability rather than an internal data structure, with no action side effect.</done>
</task>

</tasks>

<verification>
Run exact end-to-end, HTTP auth/schema, and CLI parser/render tests. Re-run existing operator investigation/incident tests to prove JSON compatibility.
</verification>

<success_criteria>
- One real runtime flow constructs, contests, converges, persists, and exposes a causal incident theory.
- The operator surface is additive, authenticated, bounded, and read-only.
- Existing policy/governance and response contracts remain the sole live-action route.
</success_criteria>

<output>After completion, create `286-06-SUMMARY.md` with end-to-end and compatibility evidence.</output>
