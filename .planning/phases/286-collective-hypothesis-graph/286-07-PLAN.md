---
phase: 286-collective-hypothesis-graph
plan: 07
type: execute
wave: 12
depends_on: [286-06B, 286-06C]
files_modified:
  - crates/swarm-runtime/src/hypothesis_graph/benchmark.rs
  - crates/swarm-runtime/tests/collective_hypothesis_graph.rs
  - tools/check-collective-hypothesis-graph.sh
  - .github/workflows/ci.yml
  - .planning/phases/286-collective-hypothesis-graph/286-VALIDATION.md
  - .planning/phases/286-collective-hypothesis-graph/286-VALIDATION-EVIDENCE.md
  - .planning/phases/286-collective-hypothesis-graph/286-07-SUMMARY.md
autonomous: true
requirements: [COG-01, COG-02, COG-03, COG-04, COG-05, COG-06, COG-07, COG-08]

must_haves:
  truths:
    - "Collective and single-agent controls consume the same adjudicated corpus, fixed logical clock, limits, and truth manifest."
    - "The five intelligence metrics have explicit denominators and deterministic verdicts; wall-clock observations never gate."
    - "Every report carries exact denominator, miss, and censoring maps tied to the frozen manifest; omitted, false, or extra maps cannot be treated as zero."
    - "The report and gate bind the exact seed 286001, recompute every fixture and baseline digest from current bytes, compare the single-agent control and immutable thresholds, and fail closed on missing, extra, false, or mutated fields."
    - "The gate proves exact tests ran, including the one-shot bridge and response-authority negative controls, and fails on missing source families, truth, thresholds, metrics, provenance, or renamed tests."
    - "A credential-free CI job runs the closure gate on the current commit with locked/offline commands and retains the machine-readable reports and structured evidence ledger."
  artifacts:
    - path: "tools/check-collective-hypothesis-graph.sh"
      provides: "final exact-test, baseline/digest/provenance, mutation, bridge, and authority gate"
    - path: ".github/workflows/ci.yml"
      provides: "credential-free Phase 286 CI job with retained report/evidence uploads"
    - path: ".planning/phases/286-collective-hypothesis-graph/286-VALIDATION-EVIDENCE.md"
      provides: "structured exact command, result-count, report-digest, and current-HEAD ledger"
  key_links:
    - from: "crates/swarm-runtime/src/hypothesis_graph/benchmark.rs"
      to: "crates/swarm-runtime/tests/collective_hypothesis_oracle.rs"
      via: "read-only frozen corpus and digest validation; oracle target is never modified after Plan 00"
      pattern: "collective-hypothesis-graph-baseline.json"
    - from: "crates/swarm-runtime/src/hypothesis_graph/benchmark.rs"
      to: "tools/check-collective-hypothesis-graph.sh"
      via: "strict deterministic machine-readable report"
      pattern: "COLLECTIVE_HYPOTHESIS_REPORT_PATH"
    - from: "tools/check-collective-hypothesis-graph.sh"
      to: ".planning/phases/286-collective-hypothesis-graph/286-VALIDATION-EVIDENCE.md"
      via: "retained report SHA-256, exact command counts, and current HEAD provenance"
      pattern: "collective-report-one.json|collective-report-two.json|head_commit"
    - from: ".github/workflows/ci.yml"
      to: "tools/check-collective-hypothesis-graph.sh"
      via: "credential-free direct Phase 286 closure job with locked/offline execution"
      pattern: "check-collective-hypothesis-graph.sh"
---

<objective>
Prove that collective epistemology improves causal reasoning against a single-agent baseline and close Phase 286 with adversarial, deterministic evidence.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/286-collective-hypothesis-graph/286-VALIDATION.md
@crates/swarm-runtime/src/replay/metrics.rs
@tools/check-mapping.sh
@.planning/phases/286-collective-hypothesis-graph/286-06B-PLAN.md

Plan 00 owns the semantic fixtures, sealed oracle target, and initial gate
self-test. Plan 07 owns the final gate enforcement and CI wiring: it may
harden `tools/check-collective-hypothesis-graph.sh` without changing the
sealed oracle, fixture bytes, baseline thresholds, or truth manifest. Plan 07
also owns the structured validation evidence ledger and its summary. Plan 07
does not create or update `286-VERIFICATION.md`; that independent artifact is
owned exclusively by Plan 07B.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Consume and revalidate the frozen independent corpus</name>
  <files>crates/swarm-runtime/src/hypothesis_graph/benchmark.rs, crates/swarm-runtime/tests/collective_hypothesis_graph.rs</files>
  <action>Load the immutable semantic truth fixtures and baseline authored in Plan 00 without rewriting them or the independently owned `collective_hypothesis_oracle.rs` target. Revalidate their recorded digests, fixed logical timestamps, six source families, corroboration/conflict, ambiguous alternatives, expected evidence/node/edge/stage IDs, withheld disjointness, resource limits, 100 logical tasks, denominators, censoring rules, and thresholds. The benchmark implementation may emit run reports, but must fail if any Plan 00 truth, baseline, or pinned oracle digest changes after implementation. Every report carries `schema_version: 1`, `seed: 286001` exactly (not merely nonzero), the current 40-lowercase-hex `head_commit`, and independently recomputed lowercase-hex SHA-256 values for the manifest, both fixtures, baseline bytes, canonical config, and lane inputs; a report with a missing/extra field, false digest, stale HEAD, or altered seed is rejected before metrics are evaluated.</action>
  <verify><automated>cargo test -p swarm-runtime hypothesis_graph::benchmark::tests::manifest --lib --locked --offline</automated></verify>
  <done>The implementation consumes a frozen independent oracle and detects any post-implementation truth/baseline drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute and gate five deterministic intelligence metrics</name>
  <files>crates/swarm-runtime/src/hypothesis_graph/benchmark.rs, crates/swarm-runtime/tests/collective_hypothesis_graph.rs</files>
  <action>Run both lanes over identical inputs through the injected fixed `GraphClock` and deterministic scheduler, then calculate median logical time to correct causal hypothesis, attack-chain recall, false causal-edge rate including rejected edges, duplicate actual work, and adjudicated evidence coverage. Require >=20% lower median hypothesis time, +10 percentage points recall, <=10% false edges, <=5% duplicate work, and >=90% evidence coverage. Persist exact top-level `denominators`, `misses`, and `censored` maps with the manifest's complete metric-key set (including explicit zeroes), plus seed/corpus/config/baseline/lane digests, deterministic work units, and separately labeled non-gating wall-clock observations. The single-agent lane must equal the checked-in `single_agent_baseline` values and the verifier must recompute all deltas and verdicts from lane values and immutable thresholds; no candidate-provided verdict, denominator, miss count, or censoring value is authoritative. Repeated runs, memory/file backends, reversed insertion order, altered Tokio yields, and wildly different host-clock observations must produce the same deterministic report/verdict bytes aside from explicitly excluded observation metadata.</action>
  <verify><automated>cargo test -p swarm-runtime --test collective_hypothesis_graph --locked --offline -- --exact collective_reasoning_beats_single_agent_baseline --nocapture && cargo test -p swarm-runtime --test collective_hypothesis_graph --locked --offline -- --exact host_clock_and_scheduler_perturbation_do_not_change_verdict --nocapture</automated></verify>
  <done>The report measures intelligence, not agent count or machine speed, and meets every COG-08 threshold on the adjudicated corpus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add an exact, mutation-controlled phase gate and complete Nyquist validation</name>
  <files>tools/check-collective-hypothesis-graph.sh, .github/workflows/ci.yml, .planning/phases/286-collective-hypothesis-graph/286-VALIDATION.md, .planning/phases/286-collective-hypothesis-graph/286-VALIDATION-EVIDENCE.md, .planning/phases/286-collective-hypothesis-graph/286-07-SUMMARY.md</files>
  <action>Finalize the credential-free checker installed and self-tested in Plan 00 without changing its oracle, thresholds, semantic fixtures, or baseline. Every cargo invocation made by the checker and CI job must contain both `--locked` and `--offline`; no wrapper may invoke the checker recursively or use a shell self-call. The gate must run exact named tests with an exact one-test execution count, re-compute manifest/fixture/baseline/config/lane SHA-256 digests from bytes, require `seed == 286001` and `head_commit == git rev-parse --verify HEAD`, compare the single-agent lane exactly to the frozen baseline, reject missing or extra top-level/nested report fields, false/missing denominators, misses, thresholds, source families, or verdict inputs, and recompute deltas/verdicts instead of trusting report claims. Its self-test must mutate and prove failure for every declared oracle digest, baseline metric, seed, HEAD, source-family, false/missing field, denominator/miss, threshold, verdict, renamed/zero/duplicate test, wall-clock gate input, one-shot bridge path, and response-authority boundary. The normal gate must explicitly execute the Plan 06B bridge tests (`no_action_returns_zero_authority_calls`, `one_graph_result_feeds_outcome_and_planner`, `second_event_uses_independent_deltas`, `no_finding_returns_no_capture`) and Plan 06 authority/negative tests (`selected_simulation_requires_full_existing_authority_chain`, `production_graph_modules_preserve_response_authority_boundary`, `boundary_checker_rejects_broken_fixture`) in addition to the benchmark tests. Run it twice, retain the exact JSON files at `artifacts/phase286/collective-report-one.json` and `artifacts/phase286/collective-report-two.json`, record each SHA-256, current-HEAD provenance, exact command, passed count, and failure count in `286-VALIDATION-EVIDENCE.md`, retain exact command/passed-count/failure-count evidence for every green matrix row, add one credential-free Phase 286 CI job to `.github/workflows/ci.yml`, and update validation statuses only after every reference exists and passes. The CI job must use a fresh credential-free checkout (`persist-credentials: false`, `fetch-depth: 0`), the repository Rust toolchain, and a cached locked dependency set; it runs the direct matrix/gate/parser commands in separate `run:` steps, uploads both reports plus the validation ledger as artifacts, and fails if the offline cache is incomplete instead of falling back to an unlocked/network resolution or a recursive workflow invocation.</action>
  <verify><automated>python3 tools/check-phase286-validation-matrix.py --strict --self-test --cwd . && bash tools/check-collective-hypothesis-graph.sh && cargo test --workspace --all-targets --locked --offline && cargo clippy --workspace --all-targets --all-features --locked --offline -- -D warnings && cargo fmt --all -- --check && git diff --check</automated></verify>
  <done>The phase gate proves all required tests executed, all metrics meet thresholds, negative controls fire, authority remains fail closed, and the full combined tree is green.</done>
</task>

</tasks>

<verification>
Run the dedicated gate twice and compare deterministic report content, then run the strict validation-matrix self-test, full workspace, clippy, format, diff, layering, fixture-freshness, logical-clock perturbation, full authority-handoff, one-shot bridge, and response-authority controls on the combined tree. Every command is explicit, locked, and offline where Cargo is involved; no command calls this gate from another shell wrapper. Retain the two reports and their byte digests in the evidence ledger with the current HEAD. The independent reviewer, not the Plan 07 implementer, owns the P0/P1/P2 and `286-VERIFICATION.md` artifacts.
</verification>

<success_criteria>
- All COG-01..COG-08 requirements have executable evidence.
- Collective reasoning beats the single-agent baseline by the declared thresholds without safety or determinism regressions.
- No P0, P1, or P2 finding remains open; wall-clock and hosted status are reported truthfully.
</success_criteria>

<output>After completion, create `286-07-SUMMARY.md` and update `286-VALIDATION.md` plus `286-VALIDATION-EVIDENCE.md` only from retained command/report evidence. Do not create or edit `286-VERIFICATION.md`; Plan 07B owns that artifact and the final severity review.</output>
