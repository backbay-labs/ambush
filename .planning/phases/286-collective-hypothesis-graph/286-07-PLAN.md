---
phase: 286-collective-hypothesis-graph
plan: 07
type: execute
wave: 7
depends_on: [286-06]
files_modified:
  - crates/swarm-runtime/src/hypothesis_graph/benchmark.rs
  - crates/swarm-runtime/tests/collective_hypothesis_graph.rs
  - .planning/phases/286-collective-hypothesis-graph/286-VALIDATION.md
autonomous: true
requirements: [COG-01, COG-02, COG-03, COG-04, COG-05, COG-06, COG-07, COG-08]

must_haves:
  truths:
    - "Collective and single-agent controls consume the same adjudicated corpus, fixed logical clock, limits, and truth manifest."
    - "The five intelligence metrics have explicit denominators and deterministic verdicts; wall-clock observations never gate."
    - "The gate proves exact tests ran and fails on missing source families, truth, thresholds, metrics, or renamed tests."
---

<objective>
Prove that collective epistemology improves causal reasoning against a single-agent baseline and close Phase 286 with adversarial, deterministic evidence.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/286-collective-hypothesis-graph/286-VALIDATION.md
@crates/swarm-runtime/src/replay/metrics.rs
@tools/check-mapping.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Consume and revalidate the frozen independent corpus</name>
  <files>crates/swarm-runtime/src/hypothesis_graph/benchmark.rs, crates/swarm-runtime/tests/collective_hypothesis_graph.rs</files>
  <action>Load the immutable semantic truth fixtures and baseline authored in Plan 00 without rewriting them. Revalidate their recorded digests, fixed logical timestamps, six source families, corroboration/conflict, ambiguous alternatives, expected evidence/node/edge/stage IDs, withheld disjointness, resource limits, 100 logical tasks, denominators, censoring rules, and thresholds. The benchmark implementation may emit run reports, but must fail if any Plan 00 truth or baseline byte changes after implementation.</action>
  <verify><automated>cargo test -p swarm-runtime hypothesis_graph::benchmark::tests::manifest --lib</automated></verify>
  <done>The implementation consumes a frozen independent oracle and detects any post-implementation truth/baseline drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute and gate five deterministic intelligence metrics</name>
  <files>crates/swarm-runtime/src/hypothesis_graph/benchmark.rs, crates/swarm-runtime/tests/collective_hypothesis_graph.rs</files>
  <action>Run both lanes over identical inputs and calculate median logical time to correct causal hypothesis, attack-chain recall, false causal-edge rate including rejected edges, duplicate actual work, and adjudicated evidence coverage. Require >=20% lower median hypothesis time, +10 percentage points recall, <=10% false edges, <=5% duplicate work, and >=90% evidence coverage. Persist exact denominators, misses, seed/corpus/config digests, deterministic work units, and separately labeled non-gating wall-clock observations. Repeated runs and memory/file backends must produce the same verdict bytes aside from explicitly excluded generation metadata.</action>
  <verify><automated>cargo test -p swarm-runtime --test collective_hypothesis_graph -- --exact collective_reasoning_beats_single_agent_baseline --nocapture</automated></verify>
  <done>The report measures intelligence, not agent count or machine speed, and meets every COG-08 threshold on the adjudicated corpus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add an exact, mutation-controlled phase gate and complete Nyquist validation</name>
  <files>.planning/phases/286-collective-hypothesis-graph/286-VALIDATION.md</files>
  <action>Run the complete credential-free checker installed and self-tested in Plan 00 without changing its oracle, thresholds, semantic fixtures, or baseline. It must run exact named tests, assert nonzero/exact execution count, validate report schema and all five metrics/denominators, compare the frozen baseline, and rerun negative mutations for source-family omission, truth/threshold removal, verdict inversion, test rename, authority-boundary break, and nondeterministic wall-clock scoring. Update validation statuses only after every reference exists and passes.</action>
  <verify><automated>bash tools/check-collective-hypothesis-graph.sh && cargo test --workspace --all-targets --locked && cargo clippy --workspace --all-targets --all-features -- -D warnings && cargo fmt --all -- --check && git diff --check</automated></verify>
  <done>The phase gate proves all required tests executed, all metrics meet thresholds, negative controls fire, authority remains fail closed, and the full combined tree is green.</done>
</task>

</tasks>

<verification>
Run the dedicated gate twice and compare deterministic report content, then run the full workspace, clippy, format, diff, layering, fixture-freshness, and response-authority controls on the combined tree. Conduct an independent P0/P1/P2 review before phase closure.
</verification>

<success_criteria>
- All COG-01..COG-08 requirements have executable evidence.
- Collective reasoning beats the single-agent baseline by the declared thresholds without safety or determinism regressions.
- No P0, P1, or P2 finding remains open; wall-clock and hosted status are reported truthfully.
</success_criteria>

<output>After completion, create `286-07-SUMMARY.md` and `286-VERIFICATION.md`, then update roadmap/state/requirements only from verified evidence.</output>
