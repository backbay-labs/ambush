---
phase: 286-collective-hypothesis-graph
plan: 01B
type: execute
wave: 4
depends_on: [286-01]
files_modified:
  - crates/swarm-runtime/src/canary.rs
  - crates/swarm-runtime/src/promotion.rs
  - crates/swarm-runtime/src/service/tests_support.rs
autonomous: true
requirements: [COG-01, COG-02, COG-03]

must_haves:
  truths:
    - "Runtime test/support configuration literals use the same disabled-by-default hypothesis-graph config without changing existing response or runtime defaults."
    - "The supplementary ownership split preserves the completed 286-01 evidence and does not create a second config schema."
  artifacts:
    - path: "crates/swarm-runtime/src/service/tests_support.rs"
      provides: "runtime test constructors carrying the canonical graph config"
    - path: "crates/swarm-runtime/src/canary.rs"
      provides: "canary configuration migration"
  key_links:
    - from: "crates/swarm-runtime/src/canary.rs"
      to: "crates/swarm-core/src/config/root.rs"
      via: "canonical SwarmConfig hypothesis_graph field"
---

<objective>
Finish the runtime-side configuration migration split from Plan 01 with strict tests and no duplicate defaults.
</objective>

<context>
Plan 01 owns swarm-core schema and validation. This plan owns only the three runtime support files removed from its oversized Task 3.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Migrate runtime support literals and guard signed/default compatibility</name>
  <files>crates/swarm-runtime/src/canary.rs, crates/swarm-runtime/src/promotion.rs, crates/swarm-runtime/src/service/tests_support.rs</files>
  <action>Write failing focused tests first, then add the explicit canonical `HypothesisGraphConfig::default()`/serde-default field at every owned literal. Assert the signed default ruleset bytes remain unchanged, arena/graph config is disabled by default, and existing runtime mode, policy, and response fields are byte-identical. Scan these files for alternate constructors or hidden test-only defaults.</action>
  <verify><automated>cargo test -p swarm-runtime --lib --locked --offline canary::tests::canary_support_config_preserves_disabled_graph_and_legacy_runtime_bytes -- --exact --nocapture && cargo test -p swarm-runtime --lib --locked --offline promotion::tests::promotion_support_config_preserves_disabled_graph_and_legacy_runtime_bytes -- --exact --nocapture && cargo test -p swarm-runtime --lib --locked --offline service::tests::service_support_config_preserves_disabled_graph_and_legacy_runtime_bytes -- --exact --nocapture && cargo fmt --all -- --check && git diff --check</automated></verify>
  <done>All three runtime support sites use the canonical config and their compatibility/mutation tests fail on omission or alternate default.</done>
</task>
</tasks>

<verification>Run focused runtime tests, offline metadata, format, and diff checks; the combined Phase 286 gate remains owned by Plan 07B.</verification>

<success_criteria>
- No runtime support literal omits or redefines the graph config.
- Historical Plan 01 summary remains valid and is not rewritten.
</success_criteria>

<output>Create the supplementary execution summary with exact runtime sites and tests.</output>
