---
phase: 286-collective-hypothesis-graph
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/swarm-runtime/tests/collective_hypothesis_graph.rs
  - crates/swarm-runtime/tests/negative_graph_response_boundary.rs
  - scenarios/collective-hypothesis-graph/manifest.yaml
  - scenarios/collective-hypothesis-graph/ambiguous-cross-telemetry.yaml
  - scenarios/collective-hypothesis-graph/withheld-kill-chain.yaml
  - docs/benchmarks/collective-hypothesis-graph.md
  - docs/benchmarks/collective-hypothesis-graph-baseline.json
  - tools/check-collective-hypothesis-graph.sh
  - .planning/phases/286-collective-hypothesis-graph/286-VALIDATION.md
autonomous: true
requirements: [COG-01, COG-02, COG-03, COG-04, COG-05, COG-06, COG-07, COG-08]

must_haves:
  truths:
    - "The phase starts from independent adjudicated fixtures, exact test targets, and a non-vacuous authority-boundary checker."
    - "Later implementation cannot silently weaken truth, omit a signal family, rename away a test, or turn the benchmark into a zero-test pass."
    - "Wave 0 infrastructure is useful and tested on its own; it contains no production stubs or placeholder success."
  artifacts:
    - path: "scenarios/collective-hypothesis-graph/manifest.yaml"
      provides: "strict independent truth and benchmark/control contract"
    - path: "crates/swarm-runtime/tests/negative_graph_response_boundary.rs"
      provides: "mutation-proven response-authority source boundary checker"
    - path: "tools/check-collective-hypothesis-graph.sh"
      provides: "complete exact-test and report gate installed before behavior"
---

<objective>
Establish non-vacuous test, truth-corpus, authority-boundary, and exact-gate infrastructure before implementing the graph.

Purpose: strict TDD requires the independent oracle and failure modes to exist before production behavior can satisfy them.
Output: validated fixtures, a concrete integration target, a mutation-tested boundary probe, a complete future phase checker, and aligned Nyquist state.
</objective>

<context>
@.planning/phases/286-collective-hypothesis-graph/286-CONTEXT.md
@.planning/phases/286-collective-hypothesis-graph/286-RESEARCH.md
@.planning/phases/286-collective-hypothesis-graph/286-VALIDATION.md
@.planning/REQUIREMENTS.md
@crates/swarm-runtime/tests/negative_runtime_fail_closed.rs
@tools/check-mapping.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create and strictly validate the adjudicated corpus</name>
  <files>scenarios/collective-hypothesis-graph/manifest.yaml, scenarios/collective-hypothesis-graph/ambiguous-cross-telemetry.yaml, scenarios/collective-hypothesis-graph/withheld-kill-chain.yaml, docs/benchmarks/collective-hypothesis-graph.md, docs/benchmarks/collective-hypothesis-graph-baseline.json, crates/swarm-runtime/tests/collective_hypothesis_graph.rs</files>
  <action>Create complete fixed fixtures and a test-local strict manifest schema using `deny_unknown_fields`. The manifest must enumerate all six source families, fixed logical times, ambiguous alternatives, corroboration and conflict per family, expected evidence/node/edge/stage IDs, withheld support, 100 task identities, single-agent and collective controls, resource limits, metric denominators, and exact thresholds. Add mutation cases proving missing/duplicate family, duplicate ID, absent truth, overlapping withheld/training data, missing denominator/threshold, unbounded work, and unknown fields fail. This is independent truth infrastructure, not a graph implementation stub.</action>
  <verify><automated>cargo test -p swarm-runtime --test collective_hypothesis_graph benchmark_manifest_is_strict -- --exact</automated></verify>
  <done>The checked-in corpus is complete, independently validated, and fails for every declared oracle mutation before graph production code exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install a non-vacuous response-authority boundary checker</name>
  <files>crates/swarm-runtime/tests/negative_graph_response_boundary.rs</files>
  <action>Implement a reusable source/dependency boundary scanner with explicit forbidden graph-module imports for response executors/adapters, capability/containment lease stores, receipt minting, dispatcher, governance authority, and policy mutation. Prove the scanner itself with a generated deliberately broken fixture and a clean fixture, so an absent graph directory is never the only reason it passes. Plan 04 will point the same checker at real graph modules.</action>
  <verify><automated>cargo test -p swarm-runtime --test negative_graph_response_boundary boundary_checker_rejects_broken_fixture -- --exact</automated></verify>
  <done>The boundary probe demonstrably fails on authority leakage and is ready to cover production graph files as they land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Install the complete exact-test/report gate and align validation</name>
  <files>tools/check-collective-hypothesis-graph.sh, .planning/phases/286-collective-hypothesis-graph/286-VALIDATION.md</files>
  <action>Write the final-form credential-free checker now: exact test names and execution-count assertions, strict report field/denominator validation, threshold checks, deterministic repeat comparison, and negative mutations for truth, source families, thresholds, verdict, test name, authority boundary, and wall-clock gating. It is expected to block until later plans supply all behavior; Wave 0 gates its syntax and built-in self-test/mutation routines rather than weakening missing-test failures. Mark Wave 0 complete only after the fixture and boundary tests pass.</action>
  <verify><automated>bash -n tools/check-collective-hypothesis-graph.sh && bash tools/check-collective-hypothesis-graph.sh --self-test && cargo test -p swarm-runtime --test collective_hypothesis_graph benchmark_manifest_is_strict -- --exact && cargo test -p swarm-runtime --test negative_graph_response_boundary boundary_checker_rejects_broken_fixture -- --exact && git diff --check</automated></verify>
  <done>All later plan dependencies have concrete test/fixture/gate files, and the final checker cannot pass from a missing or zero-match test.</done>
</task>

</tasks>

<verification>
Run the two exact Wave 0 tests, shell syntax, and `--self-test`. The self-test must use run-owned temporary inputs to prove rejection of zero matched tests, wrong execution count, missing/extra report fields, threshold removal/failure, every declared oracle mutation, authority-boundary mutation, and wall-clock gating. Then run format, clippy for the new integration targets, and diff checks. Confirm the normal full checker fails specifically because production graph behavior is not implemented—not because of syntax, missing fixture, or a vacuous test.
</verification>

<success_criteria>
- Every missing validation reference has an owned Wave 0 creation task.
- The oracle, boundary scanner, and checker have mutation controls proving they can fail.
- No production stub, ignored test, placeholder pass, or weakened threshold is introduced.
</success_criteria>

<output>After completion, create `286-00-SUMMARY.md` and set only `wave_0_complete: true`; requirement statuses remain pending.</output>
