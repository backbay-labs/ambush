---
phase: 286-collective-hypothesis-graph
plan: 03B
type: execute
wave: 7
depends_on: [286-01C]
files_modified:
  - crates/swarm-spine/src/hypothesis_graph_store.rs
  - crates/swarm-spine/src/strategy_memory.rs
  - crates/swarm-spine/src/lib.rs
  - crates/swarm-spine/tests/reasoning_state_contract.rs
autonomous: true
requirements: [COG-03, COG-07, COG-08]

must_haves:
  truths:
    - "The existing Plan 03 HypothesisGraphStore::{snapshot, compare_and_swap} seam remains unchanged while the post-Plan-03 core contracts are revalidated at persistence boundaries."
    - "Logical StrategyMemory TTL/expiry survives memory/file restart and exact-expiry retrieval is identical; wall-clock values never affect applicability."
    - "Spine owns no runtime hypothesis coordinator, policy, response, or second task ledger; Plan 04 owns durable hypotheses/decision histories and the terminal outbox extension."
  artifacts:
    - path: "crates/swarm-spine/src/hypothesis_graph_store.rs"
      provides: "typed validation/persistence adapter for the new task identity, lineage, claim, and scheduler records"
    - path: "crates/swarm-spine/src/strategy_memory.rs"
      provides: "logical-time TTL persistence and deterministic applicable-memory retrieval"
    - path: "crates/swarm-spine/tests/reasoning_state_contract.rs"
      provides: "cross-backend compile and mutation proof after core contract extension"
  key_links:
    - from: "crates/swarm-core/src/hypothesis_graph.rs"
      to: "crates/swarm-spine/src/hypothesis_graph_store.rs"
      via: "validation at the existing HypothesisGraphStore CAS boundary"
      pattern: "TaskId|TaskCompletion|TaskCapabilityProof"
    - from: "crates/swarm-core/src/hypothesis_graph.rs"
      to: "crates/swarm-spine/src/strategy_memory.rs"
      via: "created_at/expires_at and injected GraphLogicalTime"
      pattern: "expires_at|is_applicable_at|GraphLogicalTime"
---

<objective>
Adapt the historical spine implementation to the strict post-Plan-03 core
contracts without changing its public store protocol or taking ownership of
Plan 04's durable hypotheses, decision histories, or terminal outbox.
</objective>

<context>
@.planning/phases/286-collective-hypothesis-graph/286-03-PLAN.md
@.planning/phases/286-collective-hypothesis-graph/286-01C-PLAN.md
@crates/swarm-spine/src/hypothesis_graph_store.rs
@crates/swarm-spine/src/strategy_memory.rs
</context>

<ownership_boundary>
Plan 03 remains the historical owner of the existing public
`HypothesisGraphStore::{snapshot, compare_and_swap}` protocol and its original
graph/tasks/tombstones/fencing/high-water state. This sequential 03B adapter
may extend validation and serialization for the new core task/memory records,
but it must not rename the trait, add `load_snapshot`/`commit`, or create a
second ledger. Plan 04 owns the `GraphStoreState.hypotheses`, append-only
decision histories, cross-links, result/operator projections, migration marker,
and typed `terminal_outbox`; 03B only preserves unknown future state bytes
until that extension is admitted by Plan 04.
</ownership_boundary>

<interfaces>
```rust
pub trait swarm_spine::hypothesis_graph_store::HypothesisGraphStore: Send + Sync {
    fn snapshot(&self) -> Result<GraphStoreSnapshot, GraphStoreError>;
    fn compare_and_swap(
        &self,
        expected: &GraphStoreRevision,
        state: GraphStoreState,
    ) -> Result<GraphStoreSnapshot, GraphStoreError>;
}
pub fn validate_task_terminal_envelope(
    task: &swarm_core::hypothesis_graph::TaskRecord,
    envelope: &swarm_core::hypothesis_graph::TaskTerminalEnvelope,
    capability: &swarm_core::hypothesis_graph::TaskCapabilityProof,
) -> Result<(), GraphStoreError>;
pub fn applicable_strategy_memory(
    memories: &[swarm_core::hypothesis_graph::StrategyMemoryExpiryEnvelope],
    now: swarm_core::hypothesis_graph::GraphLogicalTime,
) -> Vec<&swarm_core::hypothesis_graph::StrategyMemoryExpiryEnvelope>;
```
The adapter validates `TaskId` identity from `derive_logical_task_id`, existing
claimant-scoped `IdempotencyKey`s, key-bound `TaskCapabilityProof`, exact task/
completion-kind mapping, full `TaskDecisionLink` challenge/falsification
lineage, scheduler rank/budget, and monotonic logical time before CAS. It never
performs evidence work or publishes an outbox entry itself.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Revalidate post-Plan-03 task records at the existing CAS seam</name>
  <files>crates/swarm-spine/src/hypothesis_graph_store.rs, crates/swarm-spine/src/lib.rs</files>
  <action>Write failing adapter tests first. Keep the exact public `snapshot` and `compare_and_swap` signatures and add validation for `derive_logical_task_id`, existing `TaskId`/`IdempotencyKey`, `TaskCapabilityProof`, `TaskDecisionLink`, `TaskTerminalEnvelope`, `GraphSchedulerKey` dispatch ordering, and per-tick budget. Reject wrong claimant/producer keys, wrong completion kind, duplicate logical task IDs, stale fences/times, missing challenge edge/falsification hypothesis, missing evidence/decision lineage, and any mutation that would lower logical high-water. Prove that Plan 04 can pass the same snapshot/revision through this adapter without a renamed store or side ledger.</action>
  <verify><automated>cargo test -p swarm-spine hypothesis_graph_store --lib --locked --offline -- --test-threads=1</automated></verify>
  <done>The historical store protocol accepts only the new typed task contracts and retains its CAS/fence semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist and retrieve logical StrategyMemory expiry</name>
  <files>crates/swarm-spine/src/strategy_memory.rs, crates/swarm-spine/src/lib.rs</files>
  <action>Write memory/file parity tests first. Leave the historical `StrategyMemory` bytes untouched and persist the signed version-1 `StrategyMemoryExpiryEnvelope` beside each new record, binding memory ID/digest, `created_at`, and `expires_at`. A missing envelope is legacy/quarantined and cannot be retrieved for priority; unknown version, digest/signature mismatch, invalid logical ordering, or over-limit TTL fails closed. Implement retrieval only for `created_at <= now < expires_at`. Test before-created, active, exact-expiry, after-expiry, tampered, replayed, and reopened-file memories; change host clock and insertion order to prove canonical bytes and applicability remain unchanged. Do not add raw telemetry or runtime/authority fields.</action>
  <verify><automated>cargo test -p swarm-spine strategy_memory --lib --locked --offline -- --test-threads=1 && cargo test -p swarm-spine --test reasoning_state_contract --locked --offline -- --exact strategy_memory_expiry_is_backend_identical --nocapture</automated></verify>
  <done>Expired memories cannot influence replay, and memory/file backends agree at the exact logical boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the post-Plan-03 compile and mutation boundary</name>
  <files>crates/swarm-spine/tests/reasoning_state_contract.rs</files>
  <action>Compile a minimal consumer against the exact public trait and new core records. Exercise a successful typed task transition and deliberately mutate claimant key, completion kind, lineage, scheduler direction, TTL boundary, and high-water. Assert every mutation fails before state publication and the successful operation leaves no runtime authority or duplicate ledger.</action>
  <verify><automated>cargo test -p swarm-spine --test reasoning_state_contract --locked --offline && cargo clippy -p swarm-spine --all-targets --locked --offline -- -D warnings && cargo fmt --all -- --check && git diff --check</automated></verify>
  <done>The core extension is executable at the existing spine boundary and has independent negative evidence.</done>
</task>

</tasks>

<verification>
Run the exact spine unit/integration tests and offline clippy/format/diff checks.
Confirm that no Plan 04 hypotheses, decisions, terminal outbox, runtime
coordinator, or authority types are implemented here.
</verification>

<success_criteria>
- The existing `HypothesisGraphStore` API remains canonical.
- TTL/expiry and task validation are persisted and fail closed across backends.
- Plan 04 remains the sole owner of durable hypothesis/decision/outbox state.
</success_criteria>

<output>After completion, create `286-03B-SUMMARY.md` with backend parity and mutation evidence.</output>
