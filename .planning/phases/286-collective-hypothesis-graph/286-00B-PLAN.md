---
phase: 286-collective-hypothesis-graph
plan: 00B
type: execute
wave: 2
depends_on: [286-00]
files_modified:
  - .planning/phases/286-collective-hypothesis-graph/286-VALIDATION.md
autonomous: true
requirements: [COG-08]

must_haves:
  truths:
    - "The phase validation matrix records preflight ownership separately from later execution waves; `wave_0_complete` means only that Plan 00 and this validation-document reconciliation are complete."
    - "Every 286 plan task has a validation row with exact test, artifact, mutation, and evidence expectations; no row treats a missing implementation as a green result."
  artifacts:
    - path: ".planning/phases/286-collective-hypothesis-graph/286-VALIDATION.md"
      provides: "complete per-plan/per-task Nyquist matrix and unambiguous preflight terminology"
  key_links:
    - from: ".planning/phases/286-collective-hypothesis-graph/286-VALIDATION.md"
      to: "286-00-PLAN.md"
      via: "preflight rows point to the independently sealed oracle and boundary checker"
---

<objective>
Reconcile the phase validation artifact after Plan 00 split its gate from the validation document, while preserving the completed oracle evidence and making preflight versus execution waves unambiguous.
</objective>

<context>
The completed Plan 00 summary remains historical evidence and is not rewritten. This supplementary plan owns only validation ownership and rows; implementation plans remain responsible for production behavior.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Expand the validation matrix per task and normalize wave terminology</name>
  <files>.planning/phases/286-collective-hypothesis-graph/286-VALIDATION.md</files>
  <action>Retain the existing COG-01..08 evidence and add one anchored row for every task in Plans 00, 00B, 01, 01B, 02, 03, 01C, 01D, 02B, 03B, 04, 05, 06, 06B, 06C, and 07/07B. Each row names the exact test or checker, owned artifact, negative/mutation control, and evidence output. Define `preflight_complete` as Plan 00 plus 00B only; define `wave_0_complete` as that preflight state and never as implementation completion. Label execution waves 3..13 separately, including supplementary 01C, 01D, 02B, and 03B. Add three dedicated 06B rows for the bridge compile contract, one-shot capture/counter proof, and legacy-worker suppression/no-action zero-call proof. Preserve prior findings and mark any gate as blocked until its named implementation and evidence exist.</action>
  <verify><automated>python3 tools/check-phase286-validation-matrix.py --strict --cwd . && git diff --check</automated></verify>
  <done>The validation artifact has complete per-task rows, exact 06B coverage, and no ambiguous preflight/execution status.</done>
</task>
</tasks>

<verification>
Run the strict matrix parser, confirm every referenced plan/task exists, and verify that historical Plan 00 evidence is preserved rather than silently promoted to implementation evidence.
</verification>

<success_criteria>
- 286-VALIDATION has one row per task including all 06B tasks.
- `wave_0_complete` cannot be confused with execution-wave completion.
- Missing production evidence remains fail-closed.
</success_criteria>

<output>Update 286-VALIDATION.md only; no production code or historical summary is changed.</output>
