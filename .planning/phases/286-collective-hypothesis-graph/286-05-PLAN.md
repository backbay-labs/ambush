---
phase: 286-collective-hypothesis-graph
plan: 05
type: execute
wave: 5
depends_on: [286-03, 286-04]
files_modified:
  - crates/swarm-agents/src/stalker_agent.rs
  - crates/swarm-agents/src/weaver_agent.rs
  - crates/swarm-runtime/src/hypothesis_graph/tasks.rs
  - crates/swarm-runtime/src/hypothesis_graph/memory.rs
  - crates/swarm-runtime/src/hypothesis_graph/mod.rs
  - crates/swarm-runtime/tests/collective_hypothesis_graph.rs
autonomous: true
requirements: [COG-03, COG-07]

must_haves:
  truths:
    - "Existing Stalker/Weaver runtime roles execute hunter/challenger/falsifier capabilities through the durable ledger; role proliferation does not replace coordination."
    - "Exactly one live lease owns each logical task and stale workers cannot publish completion after fencing."
    - "Applicable strategy memory changes deterministic task priority only; it never changes authority or silently alters evidence confidence."
  key_links:
    - from: "crates/swarm-agents/src/stalker_agent.rs"
      to: "crates/swarm-spine/src/hypothesis_graph_store.rs"
      via: "Hunter and Falsifier capability claims with lease fences"
      pattern: "InvestigationTaskKind"
    - from: "crates/swarm-agents/src/weaver_agent.rs"
      to: "crates/swarm-spine/src/hypothesis_graph_store.rs"
      via: "Challenger capability claims with lease fences"
      pattern: "InvestigationTaskKind"
    - from: "crates/swarm-runtime/src/hypothesis_graph/memory.rs"
      to: "crates/swarm-runtime/src/hypothesis_graph/tasks.rs"
      via: "signed applicable memories provide bounded deterministic task-priority adjustments only"
      pattern: "priority"
---

<objective>
Connect the collective-reasoning contracts to Ambush's real agents and make completed investigation memory improve the next task ordering.
</objective>

<context>
@crates/swarm-agents/src/stalker_agent.rs
@crates/swarm-agents/src/weaver_agent.rs
@crates/swarm-runtime/src/investigation.rs
@crates/swarm-runtime/src/dispatcher.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route hunter, challenger, and falsifier capabilities through durable claims</name>
  <files>crates/swarm-agents/src/stalker_agent.rs, crates/swarm-agents/src/weaver_agent.rs, crates/swarm-runtime/src/hypothesis_graph/tasks.rs</files>
  <action>Replace graph-path reliance on process-local queued/published sets with ledger-backed typed task claims. Use the explicit capability matrix `Stalker: Hunter + Falsifier`, `Weaver: Challenger`; do not add new `AgentRole` variants. Each capability gets a test proving claim, lease, renew, terminal evidence lineage, failure, expiry/reclaim, and stale-fence refusal through the injected logical clock/scheduler. Claim, lease, renew, complete/fail, expiry, and reclaim must carry the exact idempotency key and fence. Publishing an evidence result must first commit the task terminal record; duplicate or stale workers record a refusal without performing evidence work twice.</action>
  <verify><automated>cargo test -p swarm-agents stalker_agent --lib && cargo test -p swarm-agents weaver_agent --lib</automated></verify>
  <done>Real agent ticks coordinate unresolved graph work through durable state, preserving claim/challenge/falsification lineage across restart.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove 100-task stigmergic allocation, duplicate ceiling, and fencing</name>
  <files>crates/swarm-runtime/tests/collective_hypothesis_graph.rs</files>
  <action>Drive multiple existing agents against 100 logical tasks with barrier-controlled competing claims, idempotent retries, restart, explicit logical-time lease expiry, reclaim, and stale completion. Run the same operation set under reversed insertion and yield order. Count actual redundant evidence executions rather than requests. Require duplicate work <=5%, exactly one accepted terminal result per task, and byte-identical memory/file ledger outcomes.</action>
  <verify><automated>cargo test -p swarm-runtime --test collective_hypothesis_graph -- --exact duplicate_claim_fixture_100 --nocapture</automated></verify>
  <done>The fixture proves stigmergic allocation reduces duplicate investigation work and fences stale agents under restart/reclaim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Emit and retrieve strategy memory as deterministic prioritization</name>
  <files>crates/swarm-runtime/src/hypothesis_graph/memory.rs, crates/swarm-runtime/src/hypothesis_graph/mod.rs, crates/swarm-runtime/tests/collective_hypothesis_graph.rs</files>
  <action>On completed/adjudicated investigation, persist the typed hypothesis delta, evidence utility, falsified alternatives, outcome, and provenance. Retrieve only signed applicable context matches and use them as a bounded stable task-priority adjustment. Add replay tests for relevant, irrelevant, contradictory, tampered, and expired memories; assert evidence confidence and containment authorization are unchanged.</action>
  <verify><automated>cargo test -p swarm-runtime --test collective_hypothesis_graph -- --exact memory_replay_changes_priority_deterministically --nocapture</automated></verify>
  <done>Past investigations measurably reprioritize matching unresolved work without raw telemetry, confidence forgery, or response authority.</done>
</task>

</tasks>

<verification>
Run agent unit tests and exact allocation/memory integrations. Inspect that graph-path agent findings preserve typed references rather than depending on free-form peer summary matching.
</verification>

<success_criteria>
- Durable stigmergic work allocation operates through real agent roles with <=5% duplicate execution on the declared fixture.
- Lease fencing and terminal task history survive restart.
- Strategy memory affects only deterministic task priority and remains evidence-linked/advisory.
</success_criteria>

<output>After completion, create `286-05-SUMMARY.md` with allocation and memory benchmark evidence.</output>
