---
phase: 286-collective-hypothesis-graph
plan: 05
type: execute
wave: 9
depends_on: [286-03B, 286-04]
files_modified:
  - crates/swarm-agents/src/stalker_agent.rs
  - crates/swarm-agents/src/weaver_agent.rs
  - crates/swarm-runtime/src/hypothesis_graph/memory.rs
  - crates/swarm-runtime/tests/collective_hypothesis_graph.rs
autonomous: true
requirements: [COG-03, COG-07]

must_haves:
  truths:
    - "Existing Stalker/Weaver runtime roles execute hunter/challenger/falsifier capabilities through the durable ledger; role proliferation does not replace coordination."
    - "Exactly one live lease owns each logical task and stale workers cannot publish completion after fencing."
    - "Applicable strategy memory changes deterministic task priority only; it never changes authority or silently alters evidence confidence."
  key_links:
    - from: "crates/swarm-agents/src/stalker_agent.rs"
      to: "crates/swarm-spine/src/hypothesis_graph_store.rs"
      via: "Hunter and Falsifier capability claims with lease fences"
      pattern: "TaskKind|TaskTerminalEnvelope"
    - from: "crates/swarm-agents/src/weaver_agent.rs"
      to: "crates/swarm-spine/src/hypothesis_graph_store.rs"
      via: "Challenger capability claims with lease fences"
      pattern: "TaskKind|TaskTerminalEnvelope"
    - from: "crates/swarm-runtime/src/hypothesis_graph/memory.rs"
      to: "crates/swarm-runtime/src/hypothesis_graph/tasks.rs"
      via: "signed applicable memories provide bounded deterministic task-priority adjustments only"
      pattern: "priority"
---

<objective>
Connect the collective-reasoning contracts to Ambush's real agents and make completed investigation memory improve the next task ordering.
</objective>

<context>
@crates/swarm-agents/src/stalker_agent.rs
@crates/swarm-agents/src/weaver_agent.rs
@crates/swarm-runtime/src/investigation.rs
@crates/swarm-runtime/src/dispatcher.rs
@.planning/phases/286-collective-hypothesis-graph/286-01C-PLAN.md
@.planning/phases/286-collective-hypothesis-graph/286-02B-PLAN.md
@.planning/phases/286-collective-hypothesis-graph/286-03B-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route hunter, challenger, and falsifier capabilities through durable claims</name>
  <files>crates/swarm-agents/src/stalker_agent.rs, crates/swarm-agents/src/weaver_agent.rs</files>
  <action>Replace graph-path reliance on process-local queued/published sets with the Plan 04 ledger-backed typed task claims; `crates/swarm-runtime/src/hypothesis_graph/tasks.rs` is owned only by Plan 04 and is consumed through its public `TaskClaim`, `TaskTerminalOutboxEntry`, and `accept_terminal_once` APIs. Use the explicit capability matrix `Stalker: Hunter + Falsifier`, `Weaver: Challenger`; do not add new `AgentRole` variants. Each capability gets a test proving claim, lease, renew, terminal evidence lineage, failure, expiry/reclaim, and stale-fence refusal through the injected logical clock/scheduler. Claims are bound to the admitted producer key and deterministic `TaskId` from `derive_logical_task_id`; claimant retries use only the existing `IdempotencyKey`. Completion kind must match task kind and `TaskDecisionLink` must retain edge/hypothesis -> decision -> evidence lineage. Publishing an evidence result must first commit the task terminal record through one atomic outbox CAS; duplicate, stale, failed, wrong-key, or wrong-kind workers record a refusal without performing evidence work or publishing evidence/decision/memory. Producer admission is key-bound: the worker's base identity must be present in the immutable Plan 02 `swarm_runtime::hypothesis_graph::WitnessAdmission`, and graph record signing/verification must use `KeypairGraphRecordSigner::with_admission`; role or scoped labels alone never grant capability. Add a wrong-key and unadmitted-allowlist mutation that fails before evidence work.</action>
  <verify><automated>cargo test -p swarm-agents stalker_agent --lib --locked --offline && cargo test -p swarm-agents weaver_agent --lib --locked --offline</automated></verify>
  <done>Real agent ticks coordinate unresolved graph work through durable state, preserving claim/challenge/falsification lineage across restart.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove 100-task stigmergic allocation, duplicate ceiling, and fencing</name>
  <files>crates/swarm-runtime/tests/collective_hypothesis_graph.rs</files>
  <action>Drive multiple existing agents against 100 deterministic `TaskId`s from `derive_logical_task_id` with barrier-controlled competing claims, claimant-scoped idempotent retries, restart, explicit logical-time lease expiry, reclaim, stale completion, and per-tick `SchedulerBudget` work/claim ceilings. Run the same operation set under reversed insertion and yield order. Count actual redundant evidence executions rather than requests. Require duplicate work <=5%, exactly one accepted terminal result per task, one event-local work/claim delta per tick, and byte-identical memory/file ledger outcomes. Mutations that invert descending `GraphSchedulerKey.priority_basis_points`, reuse a claimant `IdempotencyKey` as a logical task ID, or bypass either budget must fail. Exercise the exact `DeterministicScheduler::pop_ready(now: swarm_core::hypothesis_graph::GraphLogicalTime) -> Result<Option<GraphSchedulerKey>, GraphAdmissionError>` path and assert a task with `ready_at > now` is not returned or consumed; after an earlier task is popped, the future task must remain byte-identical and become selectable only at its logical ready tick.</action>
  <verify><automated>cargo test -p swarm-runtime --test collective_hypothesis_graph --locked --offline -- --exact duplicate_claim_fixture_100 --nocapture && cargo test -p swarm-runtime --test collective_hypothesis_graph --locked --offline -- --exact scheduler_priority_and_budget_are_deterministic --nocapture</automated></verify>
  <done>The fixture proves stigmergic allocation reduces duplicate investigation work and fences stale agents under restart/reclaim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Emit and retrieve strategy memory as deterministic prioritization</name>
  <files>crates/swarm-runtime/src/hypothesis_graph/memory.rs, crates/swarm-runtime/tests/collective_hypothesis_graph.rs</files>
  <action>On a terminal result already accepted by Plan 04's atomic outbox, persist the typed hypothesis delta, evidence utility, falsified alternatives, outcome, provenance, `created_at`, and checked logical `expires_at`. Retrieve only signed applicable context matches at the injected `GraphLogicalTime` (`created_at <= now < expires_at`) and use them as a bounded stable task-priority adjustment; memory never changes evidence confidence, policy mode, containment authorization, or detector authority. Add replay tests for relevant, irrelevant, contradictory, tampered, exact-expiry, and expired memories, plus a forced-CAS-failure proof that no memory publishes.</action>
  <verify><automated>cargo test -p swarm-runtime --test collective_hypothesis_graph --locked --offline -- --exact memory_replay_changes_priority_deterministically --nocapture && cargo test -p swarm-runtime --test collective_hypothesis_graph --locked --offline -- --exact memory_expiry_is_logical_and_fail_closed --nocapture</automated></verify>
  <done>Past investigations measurably reprioritize matching unresolved work without raw telemetry, confidence forgery, or response authority.</done>
</task>

</tasks>

<verification>
Run agent unit tests and exact allocation/memory integrations with `--locked --offline`. Inspect that graph-path agent findings preserve typed references and atomic outbox lineage rather than depending on free-form peer summary matching or a duplicate task ledger.
</verification>

<success_criteria>
- Durable stigmergic work allocation operates through real agent roles with <=5% duplicate execution on the declared fixture.
- Lease fencing and terminal task history survive restart.
- Strategy memory affects only deterministic task priority and remains evidence-linked/advisory.
</success_criteria>

<output>After completion, create `286-05-SUMMARY.md` with allocation and memory benchmark evidence.</output>
