---
phase: 285-assurance-foundation-closure
plan: "07B"
type: execute
wave: 14
depends_on:
  - "285-07A"
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/phase285-independent-review.yml
  - tools/check-gates-wired.sh
  - tools/check-phase285-closure.sh
  - tools/check-phase285-evidence.sh
  - tools/check-phase285-external-tests.sh
  - tools/check-phase285-workspace-tests.sh
  - tools/schemas/phase285-evidence.schema.json
  - tools/check-single-governor-key.sh
  - tools/check-negative-registry.sh
autonomous: true
requirements:
  - ASSURE-01
  - ASSURE-02
  - ASSURE-03
  - ASSURE-04
  - ASSURE-05
  - ASSURE-06
must_haves:
  truths:
    - "The final local matrix runs on one explicit clean subject worktree and records the actual subject implementation commit/tree in `$PHASE285_EVIDENCE_DIR/285-LOCAL-EVIDENCE.json`; the final evidence task never substitutes ambient HEAD or undeclared C_s/T_s variables."
    - "The final hosted/review checker canonicalizes and confines the external evidence path, proves the subject is a distinct linked worktree recorded as detached by `git worktree list --porcelain`, verifies its tree and cleanliness, and validates hosted counts/reviewer identity against that exact subject without requiring evidence-tree equality."
    - "Authority and negative-registry inventories are recalculated only after all production/manifests/checkers are frozen, and deliberate stale-manifest/source/lock/checker mutations prove the refreshed baselines can fail."
    - "Both local and final closure ledgers expose the five named ASSURE-01 components with exact artifact digests, positive/mutation counts, and repeated subject commit/tree binding; a generic assurance boolean cannot satisfy closure."
    - "Evidence distinguishes wired, executed, passed, and protected-required; external GitHub App/repository protection remains deferred, trusted-volume/coordinated-anchor rollback remains out of scope, and Phases 286-289 remain blocked until exact closure."
    - "Before either workflow-dispatch event, ready bootstrap PRs land byte-identical workflow definitions on the default branch; API-fetched default-branch digests equal the frozen subject, and both runs execute an immutable tag whose head is the exact subject commit."
  artifacts:
    - path: "tools/check-phase285-closure.sh"
      provides: "Final local selector/gate orchestrator with explicit subject-worktree and external ledger binding."
      contains: "--local"
    - path: "tools/check-phase285-evidence.sh"
      provides: "Deny-unknown-fields exact-subject path, local-ledger, and final-closure modes with named ASSURE-01 records and explicit artifact/subject arguments."
      contains: "--final-closure"
    - path: "tools/check-phase285-workspace-tests.sh"
      provides: "Exact workspace-test orchestrator that runs ordinary tests without ignored summaries and binds each intentionally ignored external test to its owned exact selector receipt."
      contains: "--self-test"
    - path: "tools/check-phase285-external-tests.sh"
      provides: "Literal receipt-bearing registry for external-environment tests not owned by earlier selector families, including the complete standalone pheromone JetStream suites."
      contains: "--all"
    - path: ".github/workflows/ci.yml"
      provides: "Credential-free hosted Phase 285 lanes carrying exact selector names/counts, subject commit/tree, and mutation results."
      contains: "phase285"
    - path: ".github/workflows/phase285-independent-review.yml"
      provides: "Workflow-dispatch-only independent-review artifact attestation whose authenticated GitHub actor is bound to the frozen subject and disjoint author/implementer sets."
      contains: "phase285-independent-review"
    - path: "tools/schemas/phase285-evidence.schema.json"
      provides: "Recursive deny-unknown JSON Schema definitions for local, hosted, review, and closure artifacts using the exact fields and types in 285-VALIDATION.md."
      contains: "phase285-hosted-evidence"
    - path: "tools/check-single-governor-key.sh"
      provides: "Final exact authority/manifests/source/lock inventory plus mutation-sensitive stale-baseline controls."
      contains: "single_governor_inventory_rejects_stale_manifest_source_or_lock_digest"
    - path: "tools/check-negative-registry.sh"
      provides: "Final protected negative-registry inventory covering the complete Phase 285 authority/checker surface."
      contains: "negative_registry_rejects_stale_authority_manifest_source_or_checker_digest"
  key_links:
    - from: "$PHASE285_EVIDENCE_DIR/285-LOCAL-EVIDENCE.json"
      to: "explicit detached subject worktree"
      via: "jq extracts subject_commit and subject_tree, then git -C verifies HEAD/tree/status in that worktree"
      pattern: "subject_commit.*subject_tree.*git -C.*rev-parse"
    - from: "tools/check-phase285-evidence.sh --final-closure --local-evidence --hosted-evidence --hosted-attestation --review-evidence --review-attestation --closure-out"
      to: "hosted/reviewer artifacts"
      via: "explicit canonical artifact and Sigstore bundle paths, exact schemas/counts/mutations, GitHub OIDC workflow identities, and zero-P0/P1/P2 review bind to the ledger subject before closure output is written"
      pattern: "--final-closure.*--local-evidence.*--hosted-evidence.*--hosted-attestation.*--review-evidence.*--review-attestation.*--closure-out.*--commit.*--tree"
    - from: "tools/schemas/phase285-evidence.schema.json"
      to: "tools/check-phase285-evidence.sh"
      via: "the checker validates each artifact against the exact recursive schema before semantic or attestation checks"
      pattern: "phase285-evidence.schema.json.*localEvidence.*hostedEvidence.*reviewEvidence.*closureEvidence"
    - from: "C_e closure ledger"
      to: "C_s/T_s subject ledger"
      via: "evidence commit may contain results but never attests evidence-tree == subject-tree"
      pattern: "subject_commit.*subject_tree.*evidence_commit"
    - from: "tools/check-phase285-evidence.sh --verify-paths"
      to: "git worktree list --porcelain and git-common-dir"
      via: "canonical evidence/subject paths are non-overlapping and the exact subject record is a distinct detached linked worktree"
      pattern: "realpath.*worktree list --porcelain.*git-common-dir.*detached"
    - from: "assurance.assumption_registry, invariant_mapping, negative_registry, fixture_freshness, supply_chain"
      to: "subject_commit and subject_tree"
      via: "each named evidence component repeats the exact frozen subject and publishes its artifact digest plus positive and mutation-failure counts"
      pattern: "assurance.*assumption_registry.*invariant_mapping.*negative_registry.*fixture_freshness.*supply_chain.*subject_commit.*subject_tree.*mutation_failure_count"
---

<objective>
Freeze final local evidence and validate fresh hosted/independent closure against the exact subject tree.

Purpose: Phase 285 closes only when local, hosted, mutation, deployment, and independent review evidence all describe one immutable implementation commit/tree. The evidence task must be safe from detached worktrees and must not accidentally test ambient HEAD or a later evidence commit.
Output: Final checker/CI wiring, external subject-bound local ledger, explicit detached-subject hosted/review verification, and later C_e closure evidence.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/285-assurance-foundation-closure/285-CONTEXT.md
@.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md
@.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md
@.planning/phases/285-assurance-foundation-closure/285-VERIFICATION.md
@.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md
@.github/workflows/ci.yml
@tools/check-gates-wired.sh
@tools/check-phase285-closure.sh
@tools/check-phase285-evidence.sh
@tools/check-phase285-deployment.sh
@tools/check-phase285-witness-conformance.sh
@tools/check-phase285-governance-persistence.sh
@tools/check-phase285-plan-schema.sh
@tools/schemas/phase285-plan-frontmatter.schema.yaml
@tools/check-single-governor-key.sh
@tools/check-negative-registry.sh
@tools/check-mapping.sh
@tools/check-fixture-freshness.sh
@tools/generate-sbom.sh
@tools/check-supply-chain.sh
@Cargo.toml
@Cargo.lock

Plan01 owns initial checker registration; Plan05A owns the base six-selector governance wrapper and Plans05B/06B extend its exact registries serially; Plan06C owns through-06C checkpoint closure; Plan07B is the final owner of the exact external-test registry, workspace-test orchestrator, authority/negative-registry baseline refresh, hosted-evidence additions, and final checker handoff. All evidence artifacts are external to every repository/worktree via a canonicalized `$PHASE285_EVIDENCE_DIR`. The final task is execution-only and makes no repository edits.
</context>

<tasks>

<task type="auto">
  <wave>14</wave>
  <name>Task 285-07-03: Finalize exact-subject checker, closure orchestration, and hosted CI wiring</name>
  <files>.github/workflows/ci.yml, .github/workflows/phase285-independent-review.yml, tools/check-gates-wired.sh, tools/check-phase285-closure.sh, tools/check-phase285-evidence.sh, tools/check-phase285-external-tests.sh, tools/check-phase285-workspace-tests.sh, tools/schemas/phase285-evidence.schema.json</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; .planning/phases/285-assurance-foundation-closure/285-VERIFICATION.md; .planning/phases/285-assurance-foundation-closure/285-RESEARCH.md; .github/workflows/ci.yml; tools/check-gates-wired.sh; tools/check-phase285-closure.sh; tools/check-phase285-evidence.sh; tools/check-phase285-external-tests.sh; tools/check-phase285-workspace-tests.sh; tools/check-phase285-deployment.sh; tools/check-phase285-witness-conformance.sh; tools/check-phase285-governance-persistence.sh; tools/check-phase285-plan-schema.sh; tools/schemas/phase285-plan-frontmatter.schema.yaml; tools/check-mapping.sh; tools/check-fixture-freshness.sh; tools/generate-sbom.sh; tools/check-supply-chain.sh; Cargo.toml; Cargo.lock</read_first>
  <action>Work red-first by adding local-ledger mutations for dirty subject worktree, stale commit/tree, omitted exact selector, zero/ignored/skipped result, failed mutation, stale SBOM/lock graph, wrong Helm/live result, non-independent review, self-attesting evidence, and a slice checkpoint labeled complete. Add ASSURE-01 schema mutations that omit or add a named component/field, alter one component's subject commit/tree, replace a required artifact digest, set a positive count to zero, or make a mutation-failure count differ from its exact registry count. Add path mutations for a relative evidence directory, evidence inside or containing any repository/worktree root, an unregistered copied checkout, a subject path absent from `git worktree list --porcelain`, a branch-bound worktree, mismatched `git-common-dir`, and subject/evidence overlap. Add final evidence mutations for a different subject commit/tree, hosted banner without per-test/target counts, stale credential, false protected-required state, reviewer equal to author/implementer, missing P0/P1/P2 verdict, self-authored review JSON, local-only evidence, false App enforcement, advanced Phases 286-289, out-of-scope rollback detection claims, and any logic comparing evidence-tree to subject-tree. Complete all repository edits to `check-phase285-closure.sh`, `check-phase285-evidence.sh`, `check-phase285-external-tests.sh`, `check-phase285-workspace-tests.sh`, `check-gates-wired.sh`, hosted CI, the independent-review workflow, and `tools/schemas/phase285-evidence.schema.json` in this task; Tasks07-04 through 07-06 must not alter these files. The JSON Schema must encode the exact field/type/enumeration contracts under `Hosted, review, and closure artifact schemas` in `285-VALIDATION.md`, set `additionalProperties: false` recursively, and use separate `localEvidence`, `hostedEvidence`, `reviewEvidence`, and `closureEvidence` definitions plus the exact `SemanticReviewPacketV1` and `ReviewDispatchBundleV1` input definitions. Define `check-phase285-closure.sh --local --evidence-out <absolute-path> --subject-commit <C_s> --subject-tree <T_s>` as the only final-local mode: all three arguments are mandatory, the output path must pass confinement, the supplied subject must equal the executing clean detached worktree, and the ledger is written only after every required lane is green. Extend that mode with explicit exact invocations for all witness selectors (`response-failure-wire`, `candidate-verifier`, `protocol-checkpoint`, `atomic-store-contract`, `in-memory-differential`, `typed-proxy`, `transport-layering`, `jetstream-cas`, `jetstream-checkpoint`, `public-dispatcher`, `full-service-path`, `service-checkpoint`, `combined-checkpoint`), all governance selectors (`fixed-lanes`, `transaction-recovery`, `enforced-checkpoint`, `retention`, `offline-maintenance`, `detector-integration`, `cli-migration`), `bash tools/check-phase285-external-tests.sh --all`, `bash tools/check-phase285-workspace-tests.sh`, `bash tools/check-phase285-deployment.sh`, `bash tools/check-witness-dependency-closure.sh --all-targets`, `bash tools/check-phase285-plan-schema.sh`, mapping/fixture/gates-wired/layering/authority/negative-registry/supply-chain/SBOM checks, strict locked Clippy/fmt, immutable-subject diff checks, and clean-tree checks; reject undefined aggregate selectors. The final mode must invoke `bash tools/check-phase285-witness-integrity.sh combined-checkpoint` with the exact externally supplied launcher and manifest SHA-256 pins; that invocation must retain Plan06C's exact five selector-to-case mappings and emit one distinct `SelectorRecordV1` per `combined_checkpoint_*` case in both final-local and hosted evidence. Every selector/gate/target must report executed > 0, passed = executed, failed = 0, ignored = 0, and every mutation must demonstrate a deliberate red result. Both local and final evidence schemas must be deny-unknown-fields and include top-level `subject_commit`/`subject_tree` plus an `assurance` object with exactly `assumption_registry`, `invariant_mapping`, `negative_registry`, `fixture_freshness`, and `supply_chain`, using every mandatory field and component binding specified in `285-VALIDATION.md`; reject a generic assurance boolean. Implement three disjoint `check-phase285-evidence.sh` modes. `--verify-paths --evidence-dir <path> --subject-worktree <path> --commit <C_s> --tree <T_s>` canonicalizes existing paths, requires an absolute evidence directory, enumerates every linked worktree, rejects all repository/evidence/subject overlap, requires the subject exactly once as detached with no branch and the same `git-common-dir`, and verifies exact clean commit/tree. `--local-ledger --evidence-dir <path> --subject-worktree <path> --local-evidence <path> --commit <C_s> --tree <T_s>` accepts only `phase_status: final-local-pending-hosted`, the complete local schema/counts, exact subject, and absent hosted/review/closure claims; it cannot return final success. `--final-closure --evidence-dir <path> --subject-worktree <path> --local-evidence <path> --hosted-evidence <path> --hosted-attestation <path> --review-evidence <path> --review-attestation <path> --closure-out <path> --commit <C_s> --tree <T_s>` requires canonical confined existing input paths and the exact schemas. It must verify both Sigstore DSSE bundles with GitHub CLI exactly `2.87.3` against public Fulcio/Rekor trust roots and issuer `https://token.actions.githubusercontent.com`; hosted identity is restricted to repository `backbay-labs/ambush` and signer workflow `backbay-labs/ambush/.github/workflows/ci.yml`, while review identity is restricted to `backbay-labs/ambush/.github/workflows/phase285-independent-review.yml`; both source and signer digests equal the exact subject. It must cross-check attested artifact SHA-256 and certificate workflow identity, then fetch each recorded Actions run and an attempt-specific jobs page with `per_page=100`, reject `total_count` mismatch, more than 100 jobs, pagination, or an incomplete page, recompute the exact immutable run projection and endpoint-context-augmented job projection, and cross-check workflow path/digest, run ID/attempt, job database ID/name, event/head, runner labels, and authenticated actor. Review identity additionally uses the fixed GitHub compare range from `a9837f210b50bb391e6902e1e24ef84e4a8da4dc` through the subject to derive exact author/committer/implementer logins; the API-authenticated review actor must equal `reviewer.login` and be absent from all three arrays. It then requires zero recomputed P0/P1/P2 and all exact subject/ASSURE-01 bindings. Only after every input validates may it atomically write, reread, and validate the exact closure schema. No generic `--commit/--tree` mode exists. Add self-tests for schema field omission/addition/type/enumeration drift, selector/gate set drift, wrong expected filtered count, artifact substitution, wrong attestation issuer/repository/workflow/subject/digest, wrong Actions-run actor/head/event/job/digest, semantic-packet substitution, missing/oversized/tampered review-dispatch bundle, local-ledger byte substitution, hosted artifact run/attempt/name substitution, incomplete compare range, reviewer overlap, findings-count mismatch, closure input-digest substitution, mode substitution, missing/extra artifact arguments, local-as-final, premature closure output, and every positive/negative path/evidence class. The independent-review workflow is `workflow_dispatch` only with `contents: read`, `actions: read`, `id-token: write`, and `attestations: write`; it accepts exact subject plus base64 canonical `SemanticReviewPacketV1` and `ReviewDispatchBundleV1`, requires the reviewer login to equal `$GITHUB_ACTOR`, validates and hashes the embedded canonical local ledger, downloads and hashes the exact hosted evidence/attestation artifact from the bound run and attempt using `actions: read`, validates both schemas and all counts, and emits the final GitHub-attested review artifact without subject-write permission or implementation edits. Do not write final evidence or claim local closure in this task; Task07-05 performs immutable local execution and Task07-06 alone invokes final closure.</action>
  <sbom_contract>Implement the exact `CycloneDxProjectionV1` contract from `285-VALIDATION.md` in the closure/evidence and hosted paths. Both `sbom_digest` and `sbom_sha256` bind its canonical bytes, never raw CycloneDX output. The final self-test must prove equality across two absolute worktrees, normalization of only the specified serial/timestamp/worktree-prefix fields, semantic mutation sensitivity, document inventory binding, and rejection of unknown absolute paths or prefix aliases.</sbom_contract>
  <integrity_contract>The final-local and hosted matrices invoke `combined-checkpoint` only through `tools/check-phase285-witness-integrity.sh`, with the exact nonempty externally supplied launcher and committed-manifest SHA-256 pins. The pinned integrity launcher dispatches the conformance checker; direct checker invocation, an internally recomputed or ambient pin, stale bytes, or omission of the five per-case selector records fails closure.</integrity_contract>
  <gzip_contract>`ReviewDispatchBundleV1` carries no raw-ledger field: it carries one deterministic metadata-free gzip member of the exact local-ledger bytes, with separate compressed and decompressed SHA-256 bindings. Construction and review enforce compressed-input, 262,144-byte expansion, and combined workflow-dispatch bounds and reject trailing bytes, concatenated members, malformed gzip, and decompression bombs.</gzip_contract>
  <identity_contract>The evidence schema and independent-review workflow implement the exact `SubjectPublicationReceiptV1` and `CommitIdentityAssociationV1` records in `285-VALIDATION.md`. They authenticate the current exact subject ref plus the implementer's canonical publication-observation comment and carry a byte-identical receipt through local evidence and `ReviewDispatchBundleV1`; self-tests reject a missing, duplicate, edited, deleted, wrong-actor, wrong-ref, wrong-subject, or post-freeze receipt. The contract does not claim GitHub can distinguish delete-and-recreate at the same commit. For every null compare-API author or committer, they recompute the role-specific raw Git identity digest, require one dispatch-bundle association, fetch the same-repository commit comment by immutable ID using `contents: read`, and authenticate its commit, canonical body digest, actor, role, and pre-freeze time. Self-tests reject missing, duplicate, surplus, wrong-role, wrong-commit, wrong-identity, edited, deleted, post-freeze, or reviewer-authored associations; directly resolved roles reject surplus associations.</identity_contract>
  <external_test_contract>`bash tools/check-phase285-external-tests.sh --all` owns the literal set difference between compiler-discovered intentional external-environment ignores and every FQN already owned by Plan01 through Plan06C selectors. Its frozen registry includes all standalone `swarm-pheromone` library and integration JetStream cases and any other otherwise-unowned external FQN. Each row executes by exact FQN in its declared real harness and emits a distinct `SelectorRecordV1` under selector `external-test`. Its exact sorted `mutation_ids` registry has cardinality nine: `duplicate_ownership`, `extra_registry_row`, `filtered_only_execution`, `missing_registry_row`, `reason_drift`, `substituted_registry_row`, `synthetic_receipt`, `target_drift`, and `zero_execution`; `mutation_failure_count` must equal nine. Independent discovery must equal the union of prior selector-owned ignores and this literal registry; each named mutation must make the real checker fail for its intended reason.</external_test_contract>
  <workspace_test_contract>`workspace-tests` is the compound `bash tools/check-phase285-workspace-tests.sh` gate, not a raw `cargo test --workspace` transcript. It compiles every workspace test target, derives and validates the exact stable-reason `#[ignore]` FQN inventory, and runs only compiler-discovered FQNs not owned by the serial runtime/ingest gate, any Plan01-through-Plan06C selector, or the Plan07B `external-test` registry. It supplies the complete exclusion list explicitly to each test binary so accepted summaries contain `ignored: 0`, then consumes the already-produced confined receipts for every excluded selector/external FQN. The closure invokes all selector and external lanes first. It rejects an unaccounted ignored test, an ignored pure test, missing/extra/duplicate/substituted FQN, reason drift, excluded unowned test, serial/selector/external overlap, raw aggregate success containing a nonzero ignored count, receipt reuse, wrong target, wrong filter count, or selector omission. Its self-test kills each mutation. The exact union of serial, selector, external-test, and workspace receipts must equal compiler discovery with each FQN owned and executed once.</workspace_test_contract>
  <worktree_provenance_contract>The checker validates only observable authenticated state: canonical registered linked-worktree path, detached/no-branch record, shared exact git-common-dir, clean index/worktree/untracked state, and exact commit/tree. Git does not retain trustworthy worktree creation history, so a currently valid registered detached worktree is accepted regardless of whether it was initially added detached or later detached; no self-test may claim to distinguish those histories.</worktree_provenance_contract>
  <verify>
    <automated>bash tools/check-gates-wired.sh && bash tools/check-phase285-external-tests.sh --self-test && bash tools/check-phase285-workspace-tests.sh --self-test && bash tools/check-phase285-evidence.sh --self-test && bash tools/check-phase285-closure.sh --self-test</automated>
    All checker/CI wiring and path/evidence mutations must execute with positive counts; stale/dirty/zero/ignored/partial/self-attesting/slice-completion/rollback-claim/path-overlap fixtures must fail before final inventory refresh.
  </verify>
  <done>The final local/hosted checker and CI code is frozen and mutation-sensitive; no local or hosted acceptance evidence has yet been claimed.</done>
</task>

<task type="auto">
  <wave>15</wave>
  <name>Task 285-07-04: Refresh final authority and negative-registry inventories with failing controls</name>
  <files>tools/check-single-governor-key.sh, tools/check-negative-registry.sh</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md (file and ownership guidance); .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; Cargo.toml; Cargo.lock; crates/swarm-governance/Cargo.toml; crates/swarm-runtime-http/Cargo.toml; crates/swarm-governance/src/lib.rs; crates/swarm-runtime-http/src/bin/swarm_detect.rs; .github/workflows/ci.yml; .github/workflows/phase285-independent-review.yml; tools/schemas/phase285-evidence.schema.json; tools/check-phase285-witness-conformance.sh; tools/check-phase285-governance-persistence.sh; tools/check-phase285-external-tests.sh; tools/check-phase285-workspace-tests.sh; tools/check-phase285-closure.sh; tools/check-phase285-evidence.sh; tools/check-gates-wired.sh; tools/check-single-governor-key.sh; tools/check-negative-registry.sh</read_first>
  <action>Only after Task07-03 freezes every production manifest, lockfile, CI, workflow, schema, and checker byte, recalculate every exact package/manifest/normal-target/compiler-input/authority/privacy/checker digest and protected negative-registry subject consumed by `check-single-governor-key.sh` and `check-negative-registry.sh`. Include the new witness crate and all targets, governance and runtime-http manifests, `Cargo.lock`, governance exports/fixed-lane/coordinator sources, detector construction, final CI wiring, the independent-review workflow, the exact evidence schema, the external-test registry, the workspace-test orchestrator, and every Phase 285 checker that can authorize an assurance claim. Preserve declared versus resolved dependency distinctions and do not bless unrelated dirty files. Add named self-test cases `single_governor_inventory_rejects_stale_manifest_source_or_lock_digest` and `negative_registry_rejects_stale_authority_manifest_source_or_checker_digest`; each must copy the checked subject into a temporary fixture, mutate independently every newly inventoried manifest/source/lock/workflow/schema/checker class, and require the real gate to fail for the intended stale or forbidden condition. Add omission mutations that delete one inventory row and one registry digest while keeping syntax valid. A raw hash edit without these observed failures is not acceptance. Run both gates again on the unmodified combined subject and require positive case counts. Tasks07-05/06 are execution-only and cannot change repository bytes after this inventory freeze.</action>
  <verify>
    <automated>bash tools/check-single-governor-key.sh && bash tools/check-negative-registry.sh</automated>
    Both gates must report their named stale-inventory self-tests and positive production check with executed > 0 and failed = 0; every manifest/source/lock/checker mutation and omitted registry row must make the copied gate fail.
  </verify>
  <done>The final authority and negative-registry baselines cover the complete frozen Phase 285 subject and are trusted only after their own stale/omission mutations are observed red.</done>
</task>

<task type="auto">
  <wave>16</wave>
  <name>Task 285-07-05: Run final local closure and write subject-bound local evidence</name>
  <identity_freeze_contract>The subject freezes at the canonical `created_at` timestamp written to `285-LOCAL-EVIDENCE.json`, not merely when the Git commit is created. After the final subject commit exists and before any GitHub compare or identity-association API call, establish the deterministic lightweight tag `phase285-subject-$SUBJECT_COMMIT`: non-force publish it when absent, or reuse it on retry only when GitHub's authenticated ref API reports object type `commit` at exactly `SUBJECT_COMMIT`; annotated or mismatched refs fail and deletion/force-update is forbidden. Then create or reuse exactly one canonical authenticated `SubjectPublicationReceiptV1` commit comment for that ref/subject/publisher and bind it into the local ledger. The receipt proves authenticated observation of current state, not unobservable ref-event history. Only after this receipt and before invoking local closure, query the complete fixed compare range. For every null author or committer login, compute the role-specific raw Git identity digest, create exactly one canonical same-repository commit comment as the authenticated implementer, read it back, and validate commit, role, identity, body digest, actor, uniqueness, and API `created_at` strictly earlier than the eventual local-ledger timestamp. Directly resolved roles forbid association comments. The independent reviewer may not create any association. Any missing, duplicate, conflicting, edited, deleted, surplus, reviewer-authored, or not-strictly-pre-freeze receipt/association blocks ledger creation.</identity_freeze_contract>
  <files>$PHASE285_EVIDENCE_DIR/285-LOCAL-EVIDENCE.json (external only; no repository subject files)</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; .planning/phases/285-assurance-foundation-closure/285-VERIFICATION.md; tools/check-phase285-closure.sh; tools/check-phase285-evidence.sh; $PHASE285_EVIDENCE_DIR</read_first>
  <action>This task is execution-only and makes zero repository edits. Create a new detached linked worktree for the final post-Task07-04 commit, outside the external evidence directory, and provide its explicit path as `$PHASE285_SUBJECT_WORKTREE`. Derive `SUBJECT_COMMIT=$(git -C "$PHASE285_SUBJECT_WORKTREE" rev-parse HEAD)`, `SUBJECT_TREE=$(git -C "$PHASE285_SUBJECT_WORKTREE" rev-parse 'HEAD^{tree}')`, and `SUBJECT_TAG="phase285-subject-$SUBJECT_COMMIT"` before any ledger exists. Require the canonical path-confinement checker to prove the evidence directory is absolute and non-overlapping with every repository/worktree and the subject is a distinct registered detached linked worktree sharing the exact git common dir. Before any compare or identity-association API call, fetch `refs/tags/$SUBJECT_TAG` through GitHub's authenticated API. If absent, require the same local ref to be absent, create the exact lightweight tag at `SUBJECT_COMMIT`, push it with an explicit non-forced refspec, and wait until the API reports object type `commit` and object SHA exactly `SUBJECT_COMMIT`. If already present, reuse it without a push only when the API reports that same exact type/SHA; an annotated or mismatched ref fails, and no path may delete or force-update it. Then create or reuse exactly one canonical same-repository commit comment as the authenticated implementer, read it back, and construct `SubjectPublicationReceiptV1`; reject missing, duplicate, edited, deleted, wrong-actor, wrong-ref, or wrong-subject receipts. Query the complete fixed GitHub compare range at that now-reachable exact commit; before subject freeze, create and authenticate the exact `CommitIdentityAssociationV1` commit comments required for null author/committer logins and reject missing, duplicate, conflicting, surplus, directly resolved, or reviewer-authored associations. Only after the publication receipt and every required association exist and validate, invoke `(cd "$PHASE285_SUBJECT_WORKTREE" && bash tools/check-phase285-closure.sh --local --evidence-out "$PHASE285_EVIDENCE_DIR/285-LOCAL-EVIDENCE.json" --subject-commit "$SUBJECT_COMMIT" --subject-tree "$SUBJECT_TREE")`. The closure command itself writes the ledger only after all final-local lanes pass; its canonical `created_at` is the subject-freeze timestamp and must be strictly later than the publication receipt and every required association comment's API `created_at`. The JSON must say `phase_status: final-local-pending-hosted`, embed the exact publication receipt, repeat the exact top-level subject in each of the five named `assurance` components, and record all mandatory artifact digests and exact positive/mutation counts from `285-VALIDATION.md`; record truthful wired/executed/passed/protected-required fields, exact selector/target/mutation counts, inventory/SBOM/lock/deployment results, rollback non-claims, external App `deferred`, and Phases 286-289 `blocked`. Use `jq` to require ledger `subject_commit`/`subject_tree` equality with the pre-run values, re-fetch and revalidate the exact current tag, publication receipt, and every required association against that freeze timestamp, re-run `--verify-paths`, then invoke the distinct `--local-ledger` mode with the explicit local evidence path; it must accept only the pending-hosted schema and must reject any hosted/review/final claim. Require HEAD/tree plus index/worktree/untracked cleanliness before and after the run. Any repository edit invalidates this evidence and returns execution to Task07-03/04; an already-published exact tag remains reusable only for that same immutable subject, and the contract makes no claim about unobservable same-commit ref history.</action>
  <verify>
    <automated>test -n "${PHASE285_EVIDENCE_DIR:?}" && test -n "${PHASE285_SUBJECT_WORKTREE:?}" && SUBJECT_COMMIT="$(git -C "$PHASE285_SUBJECT_WORKTREE" rev-parse HEAD)" && SUBJECT_TREE="$(git -C "$PHASE285_SUBJECT_WORKTREE" rev-parse 'HEAD^{tree}')" && SUBJECT_TAG="phase285-subject-$SUBJECT_COMMIT" && test "$(gh api "repos/backbay-labs/ambush/git/ref/tags/$SUBJECT_TAG" --jq '.object.type + ":" + .object.sha')" = "commit:$SUBJECT_COMMIT" && bash "$PHASE285_SUBJECT_WORKTREE/tools/check-phase285-evidence.sh" --verify-paths --evidence-dir "$PHASE285_EVIDENCE_DIR" --subject-worktree "$PHASE285_SUBJECT_WORKTREE" --commit "$SUBJECT_COMMIT" --tree "$SUBJECT_TREE" && (cd "$PHASE285_SUBJECT_WORKTREE" && bash tools/check-phase285-closure.sh --local --evidence-out "$PHASE285_EVIDENCE_DIR/285-LOCAL-EVIDENCE.json" --subject-commit "$SUBJECT_COMMIT" --subject-tree "$SUBJECT_TREE") && test "$SUBJECT_COMMIT" = "$(jq -er '.subject_commit' "$PHASE285_EVIDENCE_DIR/285-LOCAL-EVIDENCE.json")" && test "$SUBJECT_TREE" = "$(jq -er '.subject_tree' "$PHASE285_EVIDENCE_DIR/285-LOCAL-EVIDENCE.json")" && test "$(gh api "repos/backbay-labs/ambush/git/ref/tags/$SUBJECT_TAG" --jq '.object.type + ":" + .object.sha')" = "commit:$SUBJECT_COMMIT" && bash "$PHASE285_SUBJECT_WORKTREE/tools/check-phase285-evidence.sh" --verify-paths --evidence-dir "$PHASE285_EVIDENCE_DIR" --subject-worktree "$PHASE285_SUBJECT_WORKTREE" --commit "$SUBJECT_COMMIT" --tree "$SUBJECT_TREE" && bash "$PHASE285_SUBJECT_WORKTREE/tools/check-phase285-evidence.sh" --local-ledger --evidence-dir "$PHASE285_EVIDENCE_DIR" --subject-worktree "$PHASE285_SUBJECT_WORKTREE" --local-evidence "$PHASE285_EVIDENCE_DIR/285-LOCAL-EVIDENCE.json" --commit "$SUBJECT_COMMIT" --tree "$SUBJECT_TREE"</automated>
    The local ledger must bind every non-vacuous final gate to the exact clean detached subject and remain outside all repository/worktree roots.
  </verify>
  <done>The immutable subject is remotely reachable through its exact-commit tag before GitHub identity resolution, has a ledger-bound authenticated publication-observation receipt and authenticated identity associations predating its local-ledger freeze, and has final local evidence without any repository mutation; hosted and independent closure remain pending.</done>
</task>

<task type="auto">
  <wave>17</wave>
  <name>Task 285-07-06: Validate fresh hosted execution and independent review from the ledger's detached subject</name>
  <bootstrap_contract>The bootstrap PRs contain the complete byte-identical frozen `.github/workflows/ci.yml` and complete byte-identical frozen `.github/workflows/phase285-independent-review.yml`; a selected job, dispatch stanza, reconstructed subset, or semantically equivalent rewrite is invalid. Default-branch API bytes for both complete files must hash exactly to the frozen subject workflow digests before either dispatch.</bootstrap_contract>
  <identity_dispatch_contract>Before constructing `ReviewDispatchBundleV1`, re-fetch the exact current subject ref and the local ledger's authenticated `SubjectPublicationReceiptV1`, then re-query the complete fixed compare range and reconstruct exactly the required association array from authenticated same-repository commit comments. Each receipt/comment API `created_at` must be strictly earlier than the canonical local-evidence `created_at` subject-freeze timestamp. The dispatch and final-closure checkers reject missing, duplicate, conflicting, edited, deleted, surplus, directly resolved, reviewer-authored, wrong-ref/subject/actor, or post-freeze receipts/associations; they never create either after freeze.</identity_dispatch_contract>
  <files>$PHASE285_EVIDENCE_DIR/285-LOCAL-EVIDENCE.json, $PHASE285_HOSTED_EVIDENCE, $PHASE285_HOSTED_ATTESTATION, $PHASE285_REVIEW_EVIDENCE, $PHASE285_REVIEW_ATTESTATION, $PHASE285_EVIDENCE_DIR/285-CLOSURE-EVIDENCE.json (external only; no repository subject files)</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; .planning/phases/285-assurance-foundation-closure/285-VERIFICATION.md; .planning/phases/285-assurance-foundation-closure/285-CONTEXT.md; tools/schemas/phase285-evidence.schema.json; tools/check-phase285-evidence.sh; tools/check-phase285-closure.sh; .github/workflows/ci.yml; .github/workflows/phase285-independent-review.yml; $PHASE285_EVIDENCE_DIR/285-LOCAL-EVIDENCE.json</read_first>
  <action>This task is execution-only and must make zero frozen-subject edits. Before either dispatch, create ready minimal bootstrap PRs against `main` containing byte-identical copies of the frozen subject's complete `.github/workflows/ci.yml` and complete `.github/workflows/phase285-independent-review.yml`, obtain ordinary review and CI, land those bootstrap PRs, fetch the default-branch blobs through the GitHub API, and require both SHA-256 values to equal the subject workflow digests. Inability to land bootstrap leaves this task blocked. Require the canonical local input `$PHASE285_EVIDENCE_DIR/285-LOCAL-EVIDENCE.json` to exist; it is the only external evidence artifact read before a producing dispatch. Reserve `$PHASE285_HOSTED_EVIDENCE`, `$PHASE285_HOSTED_ATTESTATION`, `$PHASE285_REVIEW_EVIDENCE`, and `$PHASE285_REVIEW_ATTESTATION` as canonical absent output destinations; all five evidence paths and the closure path must be absolute, inside the confined external evidence directory, pairwise distinct, and absent from every repository/worktree. Derive the subject values only with `SUBJECT_COMMIT=$(jq -er '.subject_commit' "$PHASE285_EVIDENCE_DIR/285-LOCAL-EVIDENCE.json")`, `SUBJECT_TREE=$(jq -er '.subject_tree' "$PHASE285_EVIDENCE_DIR/285-LOCAL-EVIDENCE.json")`, and `SUBJECT_TAG="phase285-subject-$SUBJECT_COMMIT"`; do not read ambient HEAD or undeclared `C_s`/`T_s` variables. Before any dispatch, require the GitHub tag ref to have object type `commit` and SHA exactly `SUBJECT_COMMIT`, re-fetch and validate the ledger-bound publication receipt, reject an absent, annotated, moved, or mismatched current ref or a missing/edited/duplicate/wrong-actor/wrong-subject/post-freeze receipt, and run the subject's frozen `--verify-paths` mode with explicit evidence dir, subject worktree, commit, and tree. Task07-06 must not create, delete, or update the tag or receipt. Dispatch the credential-free hosted workflow only on that exact tag, require the run's authenticated `head_sha` to equal `SUBJECT_COMMIT`, wait for successful completion, and download its evidence and attestation with no-replace writes into the reserved hosted destinations; only after the producing run and writes complete may it read, require, or validate either hosted file. Construct canonical `ReviewDispatchBundleV1` from the exact local-ledger bytes, the byte-identical publication receipt, the hosted run ID, attempt, artifact/file names, and recomputed hosted evidence/attestation digests; enforce the decoded-ledger and combined workflow-input byte bounds, then provide that bundle separately from canonical `SemanticReviewPacketV1` to the independent-review dispatch on the same immutable tag. Wait for the exact review run to complete, download its evidence and attestation with no-replace writes into the reserved review destinations, and only after the producing run and writes complete may it read, require, or validate either review file. Require the hosted JSON, review JSON, and closure JSON to match the exact recursively deny-unknown field/type/enumeration contracts in `285-VALIDATION.md` and the subject's `tools/schemas/phase285-evidence.schema.json`; prose or a workflow banner is never evidence. Require a fresh credential-free hosted Linux artifact at `$PHASE285_HOSTED_EVIDENCE` for exactly `SUBJECT_COMMIT`/`SUBJECT_TREE`, every literal selector/case and mandatory gate record, exact execution/filtered/mutation counts, the five ASSURE-01 records, six requirement states, and the fixed deferred/non-claim states. Verify `$PHASE285_HOSTED_ATTESTATION` as a Sigstore DSSE GitHub artifact attestation for the exact hosted JSON digest, repository `backbay-labs/ambush`, workflow `backbay-labs/ambush/.github/workflows/ci.yml`, GitHub OIDC issuer, run ID/attempt, and subject commit. Require the independent-review run to prove it decoded and rehashed the identical local ledger, authenticated the byte-identical publication receipt, downloaded the exact bound hosted artifact with no extra entries, and recomputed all three input digests. Require `$PHASE285_REVIEW_EVIDENCE` to contain the exact structured findings/commands/digests/identity fields and recomputed P0/P1/P2 all zero. Verify `$PHASE285_REVIEW_ATTESTATION` against the independent-review workflow, require its authenticated GitHub actor to equal `reviewer.login`, and require that login to be absent from the exact sorted subject-derived author, committer, and implementer arrays. Invoke the distinct `--final-closure` mode with explicit local, hosted, both attestation, review, and closure-output paths plus exact subject. That mode must validate every field, copy, count, and SHA-256 input binding specified in `285-VALIDATION.md`, including the five ASSURE-01 components and six per-requirement state records. It must require `wired=true`, `executed=true`, `passed=true`, and `protected_required=false` for each ASSURE ID; external App and repository protection `deferred`; trusted-volume/coordinated-anchor rollback outside guarantee; and Phases 286-289 blocked. Only after all inputs and reviewer independence pass may it atomically write and reread `$PHASE285_EVIDENCE_DIR/285-CLOSURE-EVIDENCE.json`. A later evidence commit C_e may contain a copy, but it records the subject and never requires evidence-tree == subject-tree or self-attests its own tree. Mark Phase 285 final only after this exact command passes.</action>
  <verify>
    <automated>test -n "${PHASE285_EVIDENCE_DIR:?}" && test -n "${PHASE285_SUBJECT_WORKTREE:?}" && test -n "${PHASE285_HOSTED_EVIDENCE:?}" && test -n "${PHASE285_HOSTED_ATTESTATION:?}" && test -n "${PHASE285_REVIEW_EVIDENCE:?}" && test -n "${PHASE285_REVIEW_ATTESTATION:?}" && test -f "$PHASE285_EVIDENCE_DIR/285-LOCAL-EVIDENCE.json" && test -f "$PHASE285_HOSTED_EVIDENCE" && test -f "$PHASE285_HOSTED_ATTESTATION" && test -f "$PHASE285_REVIEW_EVIDENCE" && test -f "$PHASE285_REVIEW_ATTESTATION" && SUBJECT_COMMIT="$(jq -er '.subject_commit' "$PHASE285_EVIDENCE_DIR/285-LOCAL-EVIDENCE.json")" && SUBJECT_TREE="$(jq -er '.subject_tree' "$PHASE285_EVIDENCE_DIR/285-LOCAL-EVIDENCE.json")" && bash "$PHASE285_SUBJECT_WORKTREE/tools/check-phase285-evidence.sh" --verify-paths --evidence-dir "$PHASE285_EVIDENCE_DIR" --subject-worktree "$PHASE285_SUBJECT_WORKTREE" --commit "$SUBJECT_COMMIT" --tree "$SUBJECT_TREE" && bash "$PHASE285_SUBJECT_WORKTREE/tools/check-phase285-evidence.sh" --final-closure --evidence-dir "$PHASE285_EVIDENCE_DIR" --subject-worktree "$PHASE285_SUBJECT_WORKTREE" --local-evidence "$PHASE285_EVIDENCE_DIR/285-LOCAL-EVIDENCE.json" --hosted-evidence "$PHASE285_HOSTED_EVIDENCE" --hosted-attestation "$PHASE285_HOSTED_ATTESTATION" --review-evidence "$PHASE285_REVIEW_EVIDENCE" --review-attestation "$PHASE285_REVIEW_ATTESTATION" --closure-out "$PHASE285_EVIDENCE_DIR/285-CLOSURE-EVIDENCE.json" --commit "$SUBJECT_COMMIT" --tree "$SUBJECT_TREE" && test -f "$PHASE285_EVIDENCE_DIR/285-CLOSURE-EVIDENCE.json"</automated>
    The checker must fail stale subject/tree, ambient-HEAD substitution, no-count hosted banner, stale credentials, skipped/ignored lanes, non-independent/self-authored review, any P0/P1/P2 finding, false App protection, advanced later phase, out-of-scope rollback claim, or evidence-tree equality requirement, and pass only fresh exact-subject evidence.
  </verify>
  <done>The final closure evidence is produced only after detached-subject local/hosted/review checks pass for the ledger-derived subject commit/tree; evidence remains external or later C_e, App enforcement is truthfully deferred, out-of-scope rollback claims are absent, and later phases remain blocked.</done>
</task>

</tasks>

<verification>
Run Plan07A render/live deployment, Task07-03's final checker/CI freeze, Task07-04's inventory refresh, Task07-05 local closure from its verified subject worktree, then Task07-06's ledger-derived hosted/review command. Confirm all six requirements have truthful states, all local/hosted/review counts bind to one subject commit/tree, external App enforcement remains deferred, and Phases 286-289 remain blocked until final evidence passes.
</verification>

<success_criteria>
Deployment, final local, hosted, and independent review lanes are non-vacuously green; `285-LOCAL-EVIDENCE.json` and `285-CLOSURE-EVIDENCE.json` are external and exact-subject bound; detached verification derives both subject identifiers from the local ledger; no ambient HEAD/undeclared variable or evidence-tree equality is used; ASSURE-01..06 are truthful; and no protected App or later-phase claim is made.
</success_criteria>

<output>
After exact-subject closure, create `.planning/phases/285-assurance-foundation-closure/285-07B-SUMMARY.md` only in the later evidence/state commit; never amend or rebuild the frozen subject commit/tree.
</output>
