---
phase: 285-assurance-foundation-closure
plan: "05C"
type: execute
wave: 8
depends_on:
  - "285-05B"
files_modified:
  - crates/swarm-governance/src/lib.rs
  - crates/swarm-governance/src/persistence_coordinator.rs
  - crates/swarm-governance/src/persistence_recovery.rs
  - crates/swarm-governance/tests/phase285_governance_persistence.rs
  - crates/swarm-runtime/src/dispatcher.rs
  - crates/swarm-runtime/tests/dispatch_integration.rs
  - crates/swarm-runtime/tests/governance_resilience_integration.rs
  - crates/swarm-runtime/tests/negative_runtime_fail_closed.rs
  - crates/swarm-agents/tests/governance_authorization.rs
  - crates/swarm-agents/tests/governance_single_key.rs
  - crates/swarm-ingest-runtime/src/ingest/tests.rs
  - crates/swarm-runtime-http/src/http/tests.rs
  - crates/swarm-runtime-http/src/bin/swarm_detect.rs
  - crates/swarm-cli/src/core.inc
autonomous: true
requirements:
  - ASSURE-04
  - ASSURE-06
must_haves:
  truths:
    - "Enforced `GovernancePolicy` accepts only an authenticated witness bridge and refuses before authority mint or local mutation when witness readiness, admission, fence, head, anchor, namespace, or session state is absent, stale, conflicting, or unavailable."
    - "The governance-owned bridge is opaque and bounded: `GovernanceWitnessBridgeChannel::bounded` yields a synchronous `GovernanceWitnessBridgeHandle` and a blocking `GovernanceWitnessWorkerEndpoint`, and only the authenticated wrapper can reach the coordinator."
    - "Plan05C's local mock endpoint proves timeout, disconnect, cancellation, backpressure, typed error propagation, and one shared authority identity without depending on runtime-http; Plan06C alone supplies the real async worker."
    - "Every current workspace caller of the legacy local constructors is migrated to a production witness-backed test harness or fails closed; `migrate_persistence_lock` and the CLI migration command cannot bypass the later witness-backed epoch transition."
  artifacts:
    - path: "crates/swarm-governance/src/lib.rs"
      provides: "Witness-first GovernancePolicy constructors, authenticated bridge gate, concrete typed witness error, and offline-only local constructor boundary."
      contains: "AuthenticatedGovernanceWitnessBridge"
    - path: "crates/swarm-governance/src/persistence_coordinator.rs"
      provides: "GovernanceWitnessBridgeChannel, handle, worker endpoint, readiness handshake, and GovernancePersistenceCoordinator bridge calls."
      contains: "GovernanceWitnessBridgeChannel::bounded"
    - path: "crates/swarm-governance/tests/phase285_governance_persistence.rs"
      provides: "No-witness, bridge, authority-sharing, and local-constructor mutation target."
      contains: "enforced_checkpoint_refuses_missing_witness_before_authority_mint"
  key_links:
    - from: "GovernanceWitnessBridgeHandle"
      to: "GovernanceWitnessWorkerEndpoint"
      via: "private typed request/response channel with bounded capacity and exactly-once completion"
      pattern: "GovernanceWitnessBridgeChannel::bounded.*GovernanceWitnessWorkerEndpoint"
    - from: "AuthenticatedGovernanceWitnessBridge"
      to: "GovernancePolicy::with_witness_backed_persistence"
      via: "readiness consumes the handle and proves admission/fence/head/anchor before authority mint"
      pattern: "AuthenticatedGovernanceWitnessBridge.*with_witness_backed_persistence"
    - from: "GovernancePersistenceCoordinator"
      to: "GovernanceDurabilityWitness::prepare_successor"
      via: "synchronous bridge forwards the actual witness operation without policy generics or string errors"
      pattern: "GovernancePersistenceCoordinator.*prepare_successor"
---

<objective>
Inject the external witness into enforced governance through an opaque governance-owned bridge.

Purpose: The durable protocol and local recovery are not enough if `GovernancePolicy` can still select an optional witness, mint authority before readiness, or be forced into async-runtime coupling. This plan closes the governance boundary with a local mock endpoint that later runtime-http code can consume.
Output: Bounded bridge channel/handle/endpoint, authenticated readiness wrapper, witness-first policy constructors, offline-only local constructors, and local bridge conformance tests.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/285-assurance-foundation-closure/285-CONTEXT.md
@.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md
@.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md
@.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md
@<git show 5be011a07690a63a297d5bba8fbf740bb659c19d:.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md>
@crates/swarm-governance/src/lib.rs
@crates/swarm-governance/src/persistence_protocol.rs
@crates/swarm-governance/src/persistence_coordinator.rs
@crates/swarm-governance/src/persistence_recovery.rs
@crates/swarm-governance-witness/src/runtime_client.rs
@crates/swarm-governance/tests/phase285_governance_persistence.rs
@crates/swarm-runtime/src/dispatcher.rs
@crates/swarm-runtime/tests/dispatch_integration.rs
@crates/swarm-runtime/tests/governance_resilience_integration.rs
@crates/swarm-runtime/tests/negative_runtime_fail_closed.rs
@crates/swarm-agents/tests/governance_authorization.rs
@crates/swarm-agents/tests/governance_single_key.rs
@crates/swarm-ingest-runtime/src/ingest/tests.rs
@crates/swarm-runtime-http/src/http/tests.rs
@crates/swarm-runtime-http/src/bin/swarm_detect.rs
@crates/swarm-cli/src/core.inc

The bridge is intentionally owned by `swarm-governance`: the downstream witness client cannot implement a governance trait or gain a raw authority handle. Plan06C owns the real `swarm-runtime-http` worker and its async deadlock tests; this plan uses only a governance-local mock endpoint. Because the legacy methods become governance-private in this slice, the two compiled callers outside the earlier test inventory are explicit Plan05C migrations: the dispatcher test helper moves to the bounded mock bridge, while `swarm_detect.rs` removes all three local constructor/reinitialize calls and returns a typed witness-integration-required refusal until Plan06C replaces that refusal with the real worker.
</context>

<tasks>

<task type="auto">
  <wave>8</wave>
  <name>Task 285-05-04: Wire the authenticated witness bridge into enforced GovernancePolicy construction</name>
  <files>crates/swarm-governance/src/lib.rs, crates/swarm-governance/src/persistence_coordinator.rs, crates/swarm-governance/src/persistence_recovery.rs, crates/swarm-governance/tests/phase285_governance_persistence.rs, crates/swarm-runtime/src/dispatcher.rs, crates/swarm-runtime/tests/dispatch_integration.rs, crates/swarm-runtime/tests/governance_resilience_integration.rs, crates/swarm-runtime/tests/negative_runtime_fail_closed.rs, crates/swarm-agents/tests/governance_authorization.rs, crates/swarm-agents/tests/governance_single_key.rs, crates/swarm-ingest-runtime/src/ingest/tests.rs, crates/swarm-runtime-http/src/http/tests.rs, crates/swarm-runtime-http/src/bin/swarm_detect.rs, crates/swarm-cli/src/core.inc</files>
  <read_first>git show 5be011a07690a63a297d5bba8fbf740bb659c19d:.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md; .planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance/src/lib.rs (GovernancePolicy, GovernancePersistence, initialize_persistence, with_persistence, reinitialize_persistence, migrate_persistence_lock, GovernanceAuthority); crates/swarm-governance/src/persistence_protocol.rs (GovernanceDurabilityWitness, WitnessSessionFenceRequestV1, attestation types); crates/swarm-governance/src/persistence_coordinator.rs; crates/swarm-governance/src/persistence_recovery.rs; crates/swarm-governance-witness/src/runtime_client.rs; crates/swarm-runtime/src/dispatcher.rs; crates/swarm-runtime/tests/dispatch_integration.rs; crates/swarm-runtime/tests/governance_resilience_integration.rs; crates/swarm-runtime/tests/negative_runtime_fail_closed.rs; crates/swarm-agents/tests/governance_authorization.rs; crates/swarm-agents/tests/governance_single_key.rs; crates/swarm-ingest-runtime/src/ingest/tests.rs; crates/swarm-runtime-http/src/http/tests.rs; crates/swarm-runtime-http/src/bin/swarm_detect.rs; crates/swarm-cli/src/core.inc</read_first>
  <action>Work red-first by adding exact tests `enforced_checkpoint_refuses_missing_witness_before_authority_mint`, `enforced_checkpoint_refuses_witness_outage_or_bad_admission_before_local_mutation`, `enforced_checkpoint_shares_one_authority_identity_across_coordinator_consumers`, `enforced_checkpoint_keeps_local_constructor_offline_only`, `enforced_checkpoint_mock_bridge_timeout_disconnect_and_capacity`, and `enforced_checkpoint_compile_time_rejects_detector_local_toggle` before changing constructors. Define governance-owned, closed `GovernanceWitnessBridgeRequestV1`, `GovernanceWitnessBridgeResponseV1`, and `GovernanceWitnessBridgeErrorV1`, including every witness operation needed by `GovernanceDurabilityWitness` and concrete matchable error/context fields; do not encode protocol state as `String`. Implement `GovernanceWitnessBridgeChannel::bounded(capacity) -> (GovernanceWitnessBridgeHandle, GovernanceWitnessWorkerEndpoint)` with private fields, nonzero bounded capacity, fixed request deadlines, and exactly-once typed response completion: the synchronous handle exposes only typed calls to policy, while the endpoint exposes only blocking receive and one completion capability to a runtime-owned worker. Add the governance-owned readiness handshake that consumes the handle into opaque `AuthenticatedGovernanceWitnessBridge` only after validating the admitted selector/authority/pool binding, witness identity, signed session fence, external epoch/anchor/Ready manifest, current head, and service identity; `GovernancePolicy::with_witness_backed_persistence`, load, initialize, and reinitialize must accept only that authenticated type plus held guards. Route `GovernancePersistenceCoordinator` publication through the bridge call for the actual `GovernanceDurabilityWitness::prepare_successor(session, expected_head, candidate, authorization)` and preserve signed discovery/fetch/commit/abort resolution from Plan05B. Keep `GovernancePolicy` synchronous and non-generic; no `Option<dyn witness>`, downstream-implemented sealed trait, local/memory/default witness, raw NATS capability, `Handle::block_on`, `block_in_place`, or string-erased error. Add `GovernanceWitnessClientError` as a concrete `Error` with closed code/context fields. Retain selector, authority-pair, state, pool, and binding guards through successful construction or refusal. Make legacy `initialize_persistence`, `with_persistence`, `reinitialize_persistence`, and `migrate_persistence_lock` require an unforgeable private `OfflineMaintenanceToken`; no public feature, environment/config toggle, or downstream crate can construct it. Migrate every existing caller in all fourteen task-owned files: all test callers, including the dispatcher test helper, construct an authenticated bridge through a bounded in-process mock worker and exercise the production witness-backed constructor. Runtime-http tests receive the same public test seam without an offline feature. In `swarm_detect.rs`, remove the two bootstrap calls and the reinitialize call and return a concrete `WitnessBackedDetectorConstructionRequired`/`WitnessBackedReinitializationRequired` refusal before authority mint or local mutation; Plan06C later replaces only these typed refusal sites with the real worker and epoch transition. The production CLI `MigrateGovernanceLock` path becomes a typed fail-closed `WitnessBackedMigrationRequired` result until Plan06B replaces it with the authenticated epoch-transition command. Add a workspace lexical/compile control enumerating all four legacy method names and requiring zero call sites outside the governance-private maintenance module after this task; compile every listed package/target under the same feature set used by the final matrix. Use a governance-local mock endpoint implementation only: cross-package tests may drive it through the public bounded bridge DTOs but may not gain a local witness constructor or raw store handle. Exercise timeout, endpoint disconnect, capacity/backpressure, dropped-reply cancellation, exactly-once completion, typed outage/admission/fence/head/anchor mismatch, and authority/local-byte immutability before mint. Prove the same selector/authority identity reaches coordinator, ingest, dispatcher, containment, human resume, health, and recovery through the governance test seam, while a swapped selector or authority pair refuses before mutation. Do not add the runtime-http worker, module declaration, dependency, or detector witness configuration here; Plan06C alone owns those real integration files. The only Plan05C `swarm_detect.rs` edit is the compileable typed fail-closed removal of legacy constructor/reinitialize routes.</action>
  <verify>
    <automated>bash tools/check-phase285-governance-persistence.sh enforced-checkpoint && cargo test -p swarm-runtime -p swarm-agents -p swarm-ingest-runtime -p swarm-runtime-http -p swarm-cli --all-targets --locked --offline --no-run</automated>
    The wrapper must execute all six exact bridge/enforcement cases with `passed = executed`, `failed = 0`, and `ignored = 0`; missing witness, outage, stale admission/fence/head/anchor, bridge disconnect, capacity, dropped reply, local-constructor/migration, and authority-swap fixtures must fail before authority or local bytes change. The explicit multi-package build must prove every migrated current caller compiles without an offline/runtime toggle.
  </verify>
  <done>Only an authenticated governance-owned bridge can construct enforced policy; all refusal paths precede authority mint and local mutation; the bridge is bounded and opaque with typed errors and exactly-once completion; all current workspace callers compile through production witness-backed test seams; local constructors and local lock migration are unreachable; and Plans06B/06C have clean contracts for witness-backed migration and the real runtime worker.</done>
</task>

</tasks>

<verification>
Run `bash tools/check-phase285-governance-persistence.sh enforced-checkpoint`, governance package tests, strict clippy, formatting, and `git diff --check`. Confirm the local mock never depends on runtime-http or async `Handle::block_on`; inspect compile-fail/lexical controls and authority identity assertions. Obtain an independent zero-P0/P1/P2 review before Plan06A; this is a slice checkpoint only.
</verification>

<success_criteria>
The enforced-checkpoint selector is non-vacuously green; policy refuses absent/stale/ambiguous/unavailable witness state before authority mint; the exact opaque bridge API and authenticated wrapper are exported with private fields and bounded typed calls; all existing callers are migrated and compile; actual prepare-successor semantics remain witness-backed; and no shipped local/optional/fallback constructor or local-only migration is reachable.
</success_criteria>

<output>
After completion, create `.planning/phases/285-assurance-foundation-closure/285-05C-SUMMARY.md`.
</output>
