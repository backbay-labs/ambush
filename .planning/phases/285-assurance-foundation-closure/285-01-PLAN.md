---
phase: 285-assurance-foundation-closure
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/swarm-governance/src/witness_service.rs
  - crates/swarm-governance/src/witness_service/tests.rs
  - crates/swarm-governance/src/witness_candidate_verifier.rs
  - crates/swarm-governance/tests/phase285_witness_conformance.rs
  - .github/workflows/ci.yml
  - tools/check-gates-wired.sh
  - tools/check-phase285-witness-conformance.sh
  - tools/check-phase285-deployment.sh
  - tools/check-phase285-closure.sh
  - tools/check-phase285-evidence.sh
  - tools/check-phase285-plan-schema.sh
  - tools/schemas/phase285-plan-frontmatter.schema.yaml
autonomous: true
requirements:
  - ASSURE-04
  - ASSURE-06
must_haves:
  truths:
    - "A witness service response is a closed transport enum over the accepted operation-specific signed attestations or a signed failure; no generic success signature/domain or transport text can authorize an operation."
    - "The service independently verifies every candidate authority field before a prepare transition, and every field mutation is rejected without changing the supplied store snapshot."
    - "The Phase 285 conformance runner cannot report a green lane when its target is absent, zero tests execute, tests are ignored, a named case fails, or a requested lane is omitted."
  artifacts:
    - path: "crates/swarm-governance/src/witness_service.rs"
      provides: "Closed WitnessServiceResponseV1 transport enum, signed WitnessServiceFailureV1, closed failure codes, and request/operation binding validators over accepted operation-specific attestations."
      contains: "WitnessServiceResponseV1"
    - path: "crates/swarm-governance/src/witness_candidate_verifier.rs"
      provides: "Independent WitnessCandidateVerifier and non-forgeable verified admission result."
      contains: "WitnessCandidateVerifier"
    - path: "tools/check-phase285-witness-conformance.sh"
      provides: "Exact named-case registry and non-vacuous execution parser for all witness selectors."
      contains: "response-failure-wire"
    - path: "crates/swarm-governance/tests/phase285_witness_conformance.rs"
      provides: "Explicit integration target consumed by the frozen task selectors."
      contains: "response_failure_wire_binds_operation_and_request_digest"
  key_links:
    - from: "WitnessServiceResponseV1"
      to: "WitnessServiceRequestV1"
      via: "validate_for_request matches the request operation to the exact existing signed attestation variant and validates its operation-specific fields; only Failure uses the fixed witness-service-failure domain"
      pattern: "validate_for_request.*WitnessSessionStateFenceV1.*WitnessSessionAttestationV1.*WitnessDiscoveryAttestationV1.*WitnessOutcomeAttestationV1.*WitnessReadAttestationV1.*WitnessServiceFailureAttestationV1"
    - from: "WitnessCandidateVerifier::verify_prepare"
      to: "CandidatePreimageV1, WitnessSessionV1, WitnessStoreEnvelopeV1"
      via: "independent field-by-field validation before store transition"
      pattern: "verify_prepare.*CandidatePreimageV1.*WitnessStoreEnvelopeV1"
    - from: "tools/check-phase285-witness-conformance.sh"
      to: "crates/swarm-governance/tests/phase285_witness_conformance.rs"
      via: "exact test-name selector plus executed/passed/failed/ignored count parser"
      pattern: "cargo test.*phase285_witness_conformance.*--exact"
---

<objective>
Implement Slice A: signed witness responses/failures and the independent candidate-verifier contract, while creating the complete Wave 0 non-vacuous checker surface.

Purpose: The accepted tree currently stops at a request envelope and a client-usable candidate model. The next slice must make protocol rejection matchable and prove that a client-constructed candidate cannot become a prepared operation without an independent service-side decision.
Output: Canonical response/failure DTOs, candidate admission verifier, red-first conformance cases, and fail-closed checker entry points for every later Phase 285 selector.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/285-assurance-foundation-closure/285-CONTEXT.md
@.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md
@.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md
@.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md
@<git show 5be011a07690a63a297d5bba8fbf740bb659c19d:.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md>
@crates/swarm-governance/src/persistence_protocol.rs
@crates/swarm-governance/src/witness_service.rs
@crates/swarm-governance/src/witness_service/tests.rs

The current `witness_service.rs` intentionally owns only the nine-operation request wire. Preserve the accepted `persistence_protocol.rs`, `witness_engine.rs`, session-fence types, and `GovernanceDurabilityWitness` trait. The old PROJECT sentence saying Phase 285 is passed is stale; STATE, CONTEXT, VALIDATION, and VERIFICATION define this reopened gate. The Wave 0 checker files are intentional fail-closed infrastructure: a future selector must report a missing target until its owning slice creates the target.

The frozen `transport-layering` registry is not an aggregate placeholder: Plan03A must install the six literal case IDs `transport_layering_rejects_governance_reverse_dependency`, `transport_layering_rejects_raw_kv_subject`, `transport_layering_rejects_second_governor_signer`, `transport_layering_rejects_unrelated_authority_crate`, `transport_layering_rejects_missing_library_target`, and `transport_layering_rejects_zero_or_omitted_mutation`, each mapped to an exact shell target and positive/mutation count. Until all six exist, the Wave 0 selector remains red.

The Rust selector registry is also normative. Each listed case is one literal registry row and must execute through the row's command with `<case>` substituted literally:

| Selector | Exact command per case | Literal case IDs |
|---|---|---|
| `response-failure-wire` | `cargo test -p swarm-governance --test phase285_witness_conformance --locked --offline -- <case> --exact` | `response_failure_wire_binds_operation_and_request_digest`; `failure_retryability_is_derived_from_code`; `response_decoder_rejects_unknown_fields_or_unsigned_success`; `failure_store_state_digest_binds_current_snapshot_or_proven_absent_stream` |
| `candidate-verifier` | `cargo test -p swarm-governance --test phase285_witness_conformance --locked --offline -- <case> --exact` | `candidate_verifier_accepts_exact_candidate_without_mutation`; `candidate_verifier_rejects_each_field_mutation_without_store_change`; `candidate_verifier_rejects_stale_session_authorization_and_bounds_without_store_change` |
| `protocol-checkpoint` | `cargo test -p swarm-governance --test phase285_witness_conformance --locked --offline -- <case> --exact` | `protocol_checkpoint_rejects_unverified_prepare_and_accepts_only_one_step_transition`; `response_failure_maps_each_matchable_protocol_error`; `canonical_response_round_trip_preserves_signing_preimage` |
| `atomic-store-contract` | `cargo test -p swarm-governance --test phase285_witness_conformance --locked --offline -- <case> --exact` | `atomic_store_contract_rejects_zero_revision_and_unvalidated_transition`; `atomic_store_contract_confirms_revision_and_bytes`; `atomic_store_contract_enforces_manifest_bounds` |
| `in-memory-differential` | `cargo test -p swarm-governance --test phase285_witness_conformance --locked --offline -- <case> --exact` | `in_memory_differential_matches_reference_for_every_operation`; `in_memory_store_preserves_bytes_after_refusal`; `in_memory_faults_return_ambiguous_without_guessing`; `in_memory_capacity_exhaustion_is_pre_mutation` |
| `typed-proxy` | `cargo test -p swarm-governance --test phase285_witness_conformance --locked --offline -- <case> --exact` | `typed_proxy_rejects_signature_body_header_and_revision_mutations`; `typed_proxy_delegates_only_after_canonical_validation`; `typed_proxy_preserves_reference_outcomes` |
| `jetstream-cas` | `cargo test -p swarm-governance-witness --test jetstream_cas --locked --offline -- <case> --exact` | `jetstream_cas_rejects_raw_config_unknown_field_or_persist_mode`; `jetstream_cas_rejects_each_raw_config_mutation`; `jetstream_cas_rejects_wrong_revision_header_or_ack`; `jetstream_cas_confirms_raw_sequence_and_bytes`; `jetstream_cas_rejects_del_purge_rollup_and_direct_reads` |
| `jetstream-checkpoint` | `cargo test -p swarm-governance-witness --test jetstream_checkpoint --locked --offline -- <case> --exact` | `jetstream_checkpoint_survives_restart_for_current_predecessor_prepared_abort_and_genesis`; `jetstream_checkpoint_rejects_rolled_back_anchor_or_recreated_stream`; `jetstream_checkpoint_rejects_unavailable_server_instead_of_skipping`; `jetstream_checkpoint_uses_global_revision_not_store_generation` |
| `public-dispatcher` | `cargo test -p swarm-governance-witness --test full_service_path --locked --offline -- <case> --exact` | `public_dispatcher_rejects_unknown_subject_operation_or_body`; `public_dispatcher_returns_overload_without_spawning_or_touching_store`; `public_dispatcher_signs_only_after_confirmed_transition`; `public_dispatcher_maps_typed_failure_without_string_fallback` |
| `full-service-path` | `cargo test -p swarm-governance-witness --test full_service_path --locked --offline -- <case> --exact` | `full_service_path_rejects_runtime_private_subject_and_store_raw_api`; `full_service_path_rejects_credential_account_and_mount_swaps`; `full_service_path_validates_proxy_response_before_public_attestation`; `full_service_path_fails_closed_on_store_queue_exhaustion` |
| `service-checkpoint` | `cargo test -p swarm-governance-witness --test service_checkpoint --locked --offline -- <case> --exact` | `service_checkpoint_rejects_unsigned_or_wrong_request_response`; `service_checkpoint_retries_only_exact_idempotent_request`; `service_checkpoint_rotates_session_after_lost_response`; `service_checkpoint_init_never_serves_requests`; `service_checkpoint_survives_process_restart_without_local_fallback`; `service_checkpoint_replays_fence_after_one_and_one_hundred_rotations`; `service_checkpoint_exact_rotation_retry_remains_byte_identical_after_later_prepare_commit_abort`; `service_checkpoint_rejects_expected_head_only_stale_retry`; `service_checkpoint_rejects_concurrent_conflicting_challenges`; `service_checkpoint_rejects_stale_fence_after_head_prepared_session_manifest_epoch_anchor`; `service_checkpoint_rejects_generation_exhaustion`; `service_checkpoint_reads_current_predecessor_prepared_aborted_and_evicted_txids`; `service_checkpoint_rejects_stale_replica_transaction_reads`; `service_checkpoint_enforces_every_protocol_limit_at_max_and_max_plus_one`; `service_checkpoint_retention_window_keeps_constant_keys_and_bounded_values` |

`transport-layering` is the sole non-Rust selector and consumes Plan03A's exact six-row shell registry. For every Rust row above, require exactly one invocation, `running = 1`, `passed = 1`, `failed = 0`, `ignored = 0`, and the exact full test name once. A nonzero Cargo `filtered out` count is expected when the target contains other registered tests: before executing a row, enumerate that exact target with `cargo test ... -- --list`, require its discovered test-name set to equal the complete materialized target-inventory registry for the current serial slice, derive `expected_filtered_out = discovered_target_test_count - 1`, and require the row summary's `filtered out` count to equal that value exactly. The target-inventory registry is broader than a selector's own rows and later owners must append every literal direct control sharing that target—including Plan06C combined-checkpoint cases—before creating the test; therefore legitimate later controls change the exact expected count without becoming unregistered extras. Zero execution, filtered-only output, a wrong filtered count, or a discovered missing/extra/duplicate/substring test name is invalid. The wrapper must separately require selector registry cardinality equal to the literal selector cardinality and reject duplicate or extra selector rows. Every Rust selector self-test executes exactly the eight deliberate red mutation IDs `missing_target`, `zero_execution`, `ignored_test`, `failed_test`, `duplicate_registry_row`, `extra_registry_row`, `substring_only_match`, and `partial_or_filtered_only_wrong_count`; it requires `mutation_failure_count = 8`. Later owning plans create the targets/cases serially; an unmaterialized future row remains red rather than being omitted.
</context>

<tasks>

<task type="auto">
  <wave>0</wave>
  <name>Task 285-01-01: Add signed response/failure DTOs and create the Wave 0 checker registry</name>
  <files>crates/swarm-governance/src/witness_service.rs, crates/swarm-governance/src/witness_service/tests.rs, crates/swarm-governance/tests/phase285_witness_conformance.rs, tools/check-phase285-witness-conformance.sh, tools/check-phase285-deployment.sh, tools/check-phase285-closure.sh, tools/check-phase285-evidence.sh, tools/check-phase285-plan-schema.sh, tools/schemas/phase285-plan-frontmatter.schema.yaml, .github/workflows/ci.yml, tools/check-gates-wired.sh</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; .planning/phases/285-assurance-foundation-closure/285-CONTEXT.md; git show 5be011a07690a63a297d5bba8fbf740bb659c19d:.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md; crates/swarm-governance/src/witness_service.rs; crates/swarm-governance/src/witness_service/tests.rs; crates/swarm-governance/src/persistence_protocol.rs; crates/swarm-governance/Cargo.toml; tools/with-nats-jetstream.sh; .github/workflows/ci.yml; tools/check-gates-wired.sh</read_first>
  <action>Work red-first by adding `response_failure_wire_binds_operation_and_request_digest`, `failure_retryability_is_derived_from_code`, `response_decoder_rejects_unknown_fields_or_unsigned_success`, and `failure_store_state_digest_binds_current_snapshot_or_proven_absent_stream` to the explicit integration target before the DTO implementation. In `witness_service.rs`, add the closed `WitnessServiceFailureCodeV1` enum, `WitnessServiceFailureV1`, and exact `WitnessServiceFailureAttestationV1 { schema_version, operation, request_digest, stream_id, admission_digest, witness_identity, witness_key_id, store_state_digest: Option<String>, failure_code, retryable, signature }` with `#[serde(deny_unknown_fields)]`. A decoded admission-bound failure must bind `store_state_digest = Some(current WitnessStoreEnvelopeV1 signing-preimage digest)` whenever the exact admitted stream has a current store entry. `None` is permitted only when an authenticated `InspectReady` snapshot proves the exact admitted stream entry absent; missing, `None` despite a present entry, stale, mismatched, uppercase, wrong-length, or mutated store-state digest fails verification. The failure code must derive `retryable` from the enum and reject a caller-supplied retryability mismatch. Add `WitnessServiceResponseV1` only as a closed transport enum with the exact variants `Fence(WitnessSessionStateFenceV1)`, `Establish(WitnessSessionAttestationV1)`, `Discover(WitnessDiscoveryAttestationV1)`, `Outcome(WitnessOutcomeAttestationV1)`, `Read(WitnessReadAttestationV1)`, and `Failure(WitnessServiceFailureAttestationV1)`. Do not add `WitnessServiceResponseBodyV1`, a generic success signature, or a new success-response domain. Only the failure attestation uses the accepted fixed domain `swarm.governance.witness-service-failure.v1`, whose preimage binds every exact field above including the optional store-state digest. `validate_for_request` must match Fence, Establish, Discover, Prepare/Commit/Abort, and ReadPrepared/ReadHead/FetchPayload requests to the exact response variant; invoke each existing attestation's signature/field validator; recompute or compare the request digest wherever that attestation carries it; and reject any operation, challenge/session, txid/candidate, expected-head, admission, signer, witness, store-state, or body mismatch. A success is acceptable only through the existing valid nested operation-specific signature. Durable-transition-before-signing is a dispatcher/service ordering rule owned and tested by Plan04, not a property invented in this transport DTO. Keep framing/timeout/overload failures outside signed protocol responses. Create `phase285_witness_conformance.rs` with the normative exact registry in this plan and create the five non-governance Phase 285 checker scripts owned by this plan: `check-phase285-witness-conformance.sh`, `check-phase285-deployment.sh`, `check-phase285-closure.sh`, `check-phase285-evidence.sh`, and `check-phase285-plan-schema.sh`, plus `tools/schemas/phase285-plan-frontmatter.schema.yaml`. The sixth Phase 285 checker, `tools/check-phase285-governance-persistence.sh`, is deliberately created and owned by Plan 05A when its governance test registry exists; Plan01 must not modify it. The witness wrapper must enforce each normative selector/target/case/command row and exact per-row counts, enumerate each target's exact materialized test inventory, derive and require `expected_filtered_out = discovered_target_test_count - 1`, reject unknown selectors, duplicate/extra/missing rows, wrong filtered counts, substring/partial/filtered-only output, and execute all eight exact negative self-test mutations per selector. The deployment, closure, evidence, and plan-schema wrappers must fail with explicit missing-target diagnostics until their later named target exists; they must never return 0 for an empty registry. Add self-tests in each owned wrapper for missing, zero, ignored, failed, stale-commit/tree, and omitted-lane evidence cases. Add the schema as the normative nested YAML schema for `phase`, `plan`, `type`, `wave`, `depends_on`, `files_modified`, `autonomous`, `requirements`, and `must_haves.truths/artifacts/key_links` (each key link requires `from`, `to`, `via`, and `pattern`), and make `check-phase285-plan-schema.sh` use a real YAML parser plus explicit nested-map validation rather than the flattening GSD helper. Its exact command is `bash tools/check-phase285-plan-schema.sh`; missing plans, malformed YAML, missing nested keys, or a key link without a concrete pattern fail closed. In the same bounded Wave 0 change, add initial non-vacuous checker invocations to `.github/workflows/ci.yml` and `tools/check-gates-wired.sh`, including a self-test proving all six required Phase 285 checker paths are eventually wired and the absent Plan05A governance wrapper fails closed until its owner lands. Plan 07B owns later hosted-evidence additions to `.github/workflows/ci.yml`; this task owns only initial checker registration. Do not add a local witness fallback or transport implementation.</action>
  <acceptance_criteria>All response/failure fields are canonical and unknown fields are rejected; `retryable` is reproducibly derived from `WitnessServiceFailureCodeV1`; signed application failures bind the exact current store-state digest or a proven absent stream; each success verifies only through its accepted operation-specific signed attestation and exact request fields; the response enum introduces no generic success signature/domain; the five Plan01-owned Phase 285 checker scripts, including the plan-schema checker, plus its schema file exist, are executable where applicable, and deliberately fail when their target is absent; the sixth governance wrapper is explicitly required but remains Plan05A-owned; the four response/failure tests execute by exact name.</acceptance_criteria>
  <verify>
    <automated>bash tools/check-phase285-witness-conformance.sh response-failure-wire && bash tools/check-phase285-plan-schema.sh</automated>
    The checker must print a nonzero executed count for each named case and reject an injected ignored, zero-match, failed, stale-hash, or omitted-lane fixture. The plan-schema command must parse the YAML schema and validate all 13 Phase 285 plan frontmatters, including nested `must_haves.key_links.pattern` fields.
  </verify>
  <done>Task 285-01-01 has a green response-failure-wire lane with typed signed failures and a complete Wave 0 checker registry; future missing selectors fail closed rather than passing vacuously.</done>
</task>

<task type="auto">
  <wave>1</wave>
  <name>Task 285-01-02: Implement the independent candidate verifier</name>
  <files>crates/swarm-governance/src/witness_candidate_verifier.rs, crates/swarm-governance/src/witness_service.rs, crates/swarm-governance/src/witness_service/tests.rs, crates/swarm-governance/tests/phase285_witness_conformance.rs</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; git show 5be011a07690a63a297d5bba8fbf740bb659c19d:.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md; crates/swarm-governance/src/persistence_protocol.rs (CandidatePreimageV1, CandidateV1, WitnessHeadV1, WitnessSessionV1, WitnessSessionAuthorizationV1, PublicationBindingV1, PublicationRoleIdentitiesV1); crates/swarm-governance/src/witness_engine.rs (WitnessStoreEnvelopeV1, WitnessStoreExpectationV1, validate_store_transition); crates/swarm-governance/src/witness_service.rs; crates/swarm-governance/src/witness_service/tests.rs; crates/swarm-governance/tests/phase285_witness_conformance.rs</read_first>
  <action>Keep the verifier separate from client request construction. Add the three exact registry cases `candidate_verifier_accepts_exact_candidate_without_mutation`, `candidate_verifier_rejects_each_field_mutation_without_store_change`, and `candidate_verifier_rejects_stale_session_authorization_and_bounds_without_store_change` red-first. Create `WitnessAdmissionRecordV1` with the admitted stream, signer/witness identities, binding generation/digest, authority pair, `ProtocolLimitsV1`, initial epoch/sequence/intent, and admission digest. Create `VerifiedCandidateAdmissionV1` with private fields and accessors only for the transition layer. Implement `WitnessCandidateVerifier::verify_prepare(admission, current_envelope, session, authorization, expected_head, candidate)` so it independently canonical-decodes complete state/checkpoint bytes, verifies both detached signatures against the admitted signer, recomputes byte lengths and SHA-256 digests, recomputes candidate digest and txid, checks predecessor head and data-head equality, checks epoch/sequence/intent successor arithmetic, checks binding/authority/mapping equality and pairwise-distinct roles, enforces every admitted and retained-byte bound, and invokes `WitnessSessionAuthorizationV1::verify_for_session_record`. It must reject a client boolean or precomputed admission result and must return before mutating `current_envelope`. Mutate signer, witness key, admission, binding, authority pair, role alias, head, data-head, payload bytes, length, digest, epoch, sequence, intent, candidate digest, txid, stale session, wrong authorization, and over-limit payload; each mutation must run inside the exact named case and assert byte-identical store state. Expose the verifier through a narrow service preparation function that consumes `VerifiedCandidateAdmissionV1`, not `CandidateV1` alone.</action>
  <acceptance_criteria>Every listed mutation returns a typed verification error; the unchanged envelope digest and canonical bytes are equal before and after every refusal; a valid candidate produces a non-forgeable verified admission value; no verifier path trusts a client boolean, transport string, or candidate digest without recomputation.</acceptance_criteria>
  <verify>
    <automated>bash tools/check-phase285-witness-conformance.sh candidate-verifier</automated>
    The checker must report every field-mutation case as executed and failed-closed, with no ignored or zero-test result.
  </verify>
  <done>Task 285-01-02 provides the service-side candidate admission contract required by Prepare and proves all independent fields are checked without store mutation on refusal.</done>
</task>

<task type="auto">
  <wave>1</wave>
  <name>Task 285-01-03: Close the pure protocol checkpoint</name>
  <files>crates/swarm-governance/src/witness_service.rs, crates/swarm-governance/src/witness_candidate_verifier.rs, crates/swarm-governance/src/witness_service/tests.rs, crates/swarm-governance/tests/phase285_witness_conformance.rs</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; git show 5be011a07690a63a297d5bba8fbf740bb659c19d:.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md; crates/swarm-governance/src/witness_service.rs; crates/swarm-governance/src/witness_candidate_verifier.rs; crates/swarm-governance/src/witness_engine.rs; crates/swarm-governance/src/persistence_protocol.rs; crates/swarm-governance/src/witness_service/tests.rs; crates/swarm-governance/tests/phase285_witness_conformance.rs</read_first>
  <action>Use the verifier result as the only input to the pure Prepare transition adapter. Add `protocol_checkpoint_rejects_unverified_prepare_and_accepts_only_one_step_transition`, `response_failure_maps_each_matchable_protocol_error`, and `canonical_response_round_trip_preserves_signing_preimage` to the explicit target. Ensure the nine `WitnessServiceOperationV1` variants remain exactly `Fence`, `Establish`, `Discover`, `Prepare`, `Commit`, `Abort`, `ReadPrepared`, `ReadHead`, and `FetchPayload`; ensure session-bound authorization uses `verify_for_session_record`; ensure a signed success cannot be produced from a request digest, proposed envelope, or raw acknowledgement. Map canonical, admission, signature, bounds, stale-session, stale-intent, conflict, and store-transition failures to closed failure codes without converting them to `String`. Add the selector registry entry and assert the test output contains each named case and a positive execution count. Keep this checkpoint pure: no async-nats dependency, no filesystem mutation, and no production `GovernancePolicy` construction change.</action>
  <acceptance_criteria>The protocol checkpoint accepts one valid typed transition and rejects an unverified or combined transition; every mapped failure is closed and matchable; all nine operation/body pairings and authorization rules remain exact; the package compiles with no transport dependency and no production response is signed before a confirmed transition.</acceptance_criteria>
  <verify>
    <automated>bash tools/check-phase285-witness-conformance.sh protocol-checkpoint</automated>
    The checker must prove the named checkpoint tests executed > 0, passed = executed, failed = 0, ignored = 0.
  </verify>
  <done>Slice A is independently reviewable as a pure governance/service contract. This is a slice checkpoint only and is not Phase 285 completion evidence.</done>
</task>

</tasks>

<verification>
Run the three frozen selector commands, `cargo test -p swarm-governance --lib --locked --offline`, `cargo clippy -p swarm-governance --all-targets --all-features --locked --offline -- -D warnings`, `cargo fmt --all -- --check`, and `git diff --check`. Confirm the checker self-tests observe deliberate red results for missing, zero, ignored, failed, stale, and omitted evidence. Obtain an independent zero-P0/P1/P2 review of this slice; do not treat it as the combined Phase 285 verdict.
</verification>

<success_criteria>
The response/failure and candidate-verifier selectors are non-vacuously green, all Wave 0 checker entry points exist and fail closed on absent targets, no transport or local fallback was introduced, and the accepted governance/session-fence types remain unchanged outside their explicit new validation contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/285-assurance-foundation-closure/285-01-SUMMARY.md`.
</output>
