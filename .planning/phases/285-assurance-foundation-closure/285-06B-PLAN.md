---
phase: 285-assurance-foundation-closure
plan: "06B"
type: execute
wave: 10
depends_on:
  - "285-06A"
files_modified:
  - crates/swarm-governance/src/lib.rs
  - crates/swarm-governance/src/persistence_coordinator.rs
  - crates/swarm-governance/src/persistence_coordinator/maintenance.rs
  - crates/swarm-governance/src/persistence_recovery.rs
  - crates/swarm-governance/tests/phase285_governance_persistence.rs
  - tools/check-phase285-governance-persistence.sh
autonomous: true
requirements:
  - ASSURE-04
  - ASSURE-06
must_haves:
  truths:
    - "Offline maintenance is the only path that can drain or reset a fixed pool, requires a selector-held quiescence guard and exact lock order, and never runs as ordinary startup or retention fallback."
    - "Every maintenance journal entry proves each slot's name, inode, digest, length, record head, source/destination/parent/pool/archive identities, and before/after mapping; a resumed move is accepted only when the authenticated destination matches exactly."
    - "Drain and reset are distinct preservation-only operations using no-replace descriptor moves; malformed, uncertain, foreign, or collision cases preserve bytes and return typed errors rather than unlinking or reusing them."
    - "Legacy migration authenticates the original legacy state/checkpoint bytes before normalization and establishes the first witness-backed epoch; an archived malformed/replaced inactive lane can re-enter service only through a signed witness-backed rebind."
  artifacts:
    - path: "crates/swarm-governance/src/persistence_coordinator/maintenance.rs"
      provides: "Private maintenance helpers plus GovernancePersistenceCoordinator drain/reset methods, a signed alternating maintenance journal, and descriptor-relative no-replace moves."
      contains: "drain_cleanup_pool_offline"
    - path: "crates/swarm-governance/src/persistence_recovery.rs"
      provides: "Crash/restart validation for maintenance journal and source/archive mapping."
      contains: "CleanupPoolMaintenanceJournalV1"
    - path: "crates/swarm-governance/tests/phase285_governance_persistence.rs"
      provides: "Offline maintenance distinction, lock/quiescence, crash resume, and no-startup mutation target."
      contains: "offline_maintenance_crash_resumes_from_signed_alternating_journal"
    - path: "tools/check-phase285-governance-persistence.sh"
      provides: "Serial exact-registry extension for authenticated legacy migration and inactive-lane rebind."
      contains: "offline_maintenance_legacy_migration_authenticates_before_normalization_and_establishes_witness_epoch"
  key_links:
    - from: "GovernancePersistenceCoordinator::drain_cleanup_pool_offline / reset_cleanup_pool_offline"
      to: "GovernanceOfflineMaintenanceGuard and CleanupPoolBindingV1"
      via: "selector/quiescence -> state -> authority-pair -> cleanup-pool order and full signed namespace validation"
      pattern: "GovernanceOfflineMaintenanceGuard.*CleanupPoolBindingV1.*lock_order"
    - from: "CleanupPoolMaintenanceJournalV1"
      to: "slot proof"
      via: "signed predecessor-linked record binds every slot identity/digest/length/head and before/after mapping"
      pattern: "slot_name.*inode.*content_digest.*record_chain_head.*archive_identity.*before_mapping.*after_mapping"
    - from: "drain_cleanup_pool_offline / reset_cleanup_pool_offline"
      to: "rustix::fs::renameat_with"
      via: "held descriptors and RENAME_NOREPLACE with fsync after each move"
      pattern: "renameat_with.*NOREPLACE.*fsync"
    - from: "GovernancePersistenceCoordinator::migrate_legacy_stream_offline / rebind_inactive_lanes_offline"
      to: "GovernanceDurabilityWitness"
      via: "authenticate legacy bytes before normalization, preserve hostile lanes, and establish or transition only through signed witness prepare/commit evidence"
      pattern: "migrate_legacy_stream_offline.*rebind_inactive_lanes_offline.*prepare_successor.*commit_prepared"
---

<objective>
Implement explicit offline cleanup-pool maintenance with crash-resumable proofs.

Purpose: A bounded pool must be repairable without turning cleanup into an unbounded, pathname-derived, or destructive fallback. Maintenance must prove exactly what moved and preserve anything it cannot authenticate.
Output: Offline drain/reset coordinator, complete per-slot maintenance proof, signed alternating journal, no-replace archive moves, and startup/no-fallback controls.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/285-assurance-foundation-closure/285-CONTEXT.md
@.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md
@.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md
@.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md
@<.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md>
@crates/swarm-governance/src/persistence_coordinator.rs
@crates/swarm-governance/src/persistence_coordinator/retention.rs
@crates/swarm-governance/src/persistence_recovery.rs
@crates/swarm-governance/tests/phase285_governance_persistence.rs

Consume the complete Plan05A binding and Plan06A retention proof. The accepted protocol permits operator-owned archives to have an explicit lifecycle, but runtime must create no fallback archive name after exhaustion and must never unlink foreign bytes.
</context>

<tasks>

<task type="auto">
  <wave>10</wave>
  <name>Task 285-06-02: Implement offline drain/reset, complete maintenance proof, and crash-resumable journal</name>
  <files>crates/swarm-governance/src/lib.rs, crates/swarm-governance/src/persistence_coordinator.rs, crates/swarm-governance/src/persistence_coordinator/maintenance.rs, crates/swarm-governance/src/persistence_recovery.rs, crates/swarm-governance/tests/phase285_governance_persistence.rs</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md (offline maintenance, journal, archive move, rebind); .planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance/src/persistence_coordinator.rs; crates/swarm-governance/src/persistence_coordinator/retention.rs; crates/swarm-governance/src/persistence_recovery.rs; crates/swarm-governance/src/fixed_lanes.rs</read_first>
  <action>Work red-first by adding exact tests `offline_maintenance_drain_and_reset_are_distinct_preservation_only_operations`, `offline_maintenance_crash_resumes_from_signed_alternating_journal`, `offline_maintenance_requires_quiescence_and_lock_order`, and `offline_maintenance_never_runs_as_normal_startup`. Keep `GovernancePersistenceCoordinator` as the sole coordinator type: add its `drain_cleanup_pool_offline` and `reset_cleanup_pool_offline` methods, implemented through private functions in `persistence_coordinator/maintenance.rs`, plus an opaque `GovernanceOfflineMaintenanceGuard` requiring a selector-held quiescence proof. Acquire nonblocking locks only in `selector/quiescence -> state -> authority-pair -> cleanup-pool` order, then validate the complete signed `CleanupPoolBindingV1`, state/checkpoint copies, authority hard-link pair, namespace, limits, and held descriptors before mutation. Define signed predecessor-linked `CleanupPoolMaintenanceJournalV1` with `operation_id`, explicit `mode` (`Drain` or `Reset`), complete binding, archive identity, selected slots, moved slots, and phase (`Prepared`, `InProgress`, `Completed`); journal lanes alternate between two distinct authenticated files, fsync after each journal write and each no-replace move, and refuse forks, rollback, equal generations, unknown entries, malformed records, or changed bindings. For every selected slot persist a complete proof before and after movement: exact fixed `slot_name`, held `slot_inode` (device and inode), content `digest`, byte `length`, signed `record_chain_head`, `source_identity`, `destination_identity`, `parent_identity`, `pool_identity`, `archive_identity`, and exact `before_mapping`/`after_mapping` of names to identities. `drain_cleanup_pool_offline` accepts only fully validated terminal `Retained` or `ForeignPreserved` slots and moves them to a caller-supplied validated archive component; `reset_cleanup_pool_offline` may preserve malformed/uncertain slots opaquely but must not reinterpret them as valid retention, and both use held directory descriptors plus `rustix::fs::renameat_with(..., RenameFlags::NOREPLACE)`, immediate pre/post identity checks, and explicit fsync. Neither entry point may unlink, purge, overwrite, reuse, or generate timestamp/process/transaction/random fallback archive names; archive collisions, missing/replaced parent/pool/slot/archive, symlink/hard-link aliases, stale source bytes, short/corrupt records, cleanup errors, and generation/size overflow are typed refusals with source/namespace preservation. A crash after every move or journal phase resumes only when the signed journal proves either source-present/archive-absent or source-absent/archive-present with the exact destination identity and digest; any other mapping is Uncertain. Normal startup/load/retention must reject a maintenance journal in progress and must not invoke drain/reset, create a pool, rebind a pool, or silently archive bytes. Include explicit preservation tests for foreign and uncertain entries, drain/reset semantic swaps, missing quiescence, wrong lock order, journal rollback/fork, partial move, repeated retry, and post-move seam mutation. Reinitialization remains a witness-backed epoch transition in Plan06C; this task must not add another coordinator type, a local sequence reset, or legacy pathname migration.</action>
  <verify>
    <automated>bash tools/check-phase285-governance-persistence.sh offline-maintenance</automated>
    The wrapper must execute all four exact offline-maintenance cases with `passed = executed`, `failed = 0`, and `ignored = 0`; every crash resume must prove the complete per-slot before/after mapping, and startup/foreign/archive/alias mutations must fail without deleting or overwriting bytes.
  </verify>
  <done>Offline drain/reset are separate guarded preservation-only operations with complete signed per-slot proofs, alternating crash-resumable journals, no-replace moves, exact lock order, and no ordinary-startup fallback.</done>
</task>

<task type="auto">
  <wave>10</wave>
  <name>Task 285-06-03: Add authenticated legacy migration and witness-backed inactive-lane rebind</name>
  <files>crates/swarm-governance/src/lib.rs, crates/swarm-governance/src/persistence_coordinator.rs, crates/swarm-governance/src/persistence_coordinator/maintenance.rs, crates/swarm-governance/src/persistence_recovery.rs, crates/swarm-governance/tests/phase285_governance_persistence.rs, tools/check-phase285-governance-persistence.sh</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md (Offline maintenance and reinitialization, crash rules, mandatory migration/rebind controls); .planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance/src/persistence_protocol.rs; crates/swarm-governance/src/persistence_coordinator.rs; crates/swarm-governance/src/persistence_coordinator/maintenance.rs; crates/swarm-governance/src/persistence_recovery.rs; crates/swarm-governance/tests/phase285_governance_persistence.rs; tools/check-phase285-governance-persistence.sh</read_first>
  <action>Work red-first with exact tests `offline_maintenance_legacy_migration_authenticates_before_normalization_and_establishes_witness_epoch` and `offline_maintenance_inactive_lane_rebind_requires_signed_witness_and_preserves_foreign_bytes`, then append both governance test names to the `offline-maintenance` registry and execute both individually with `--exact` on their declared target. Keep `GovernancePersistenceCoordinator` as the sole coordinator type and expose only guarded methods `migrate_legacy_stream_offline` and `rebind_inactive_lanes_offline`, implemented by private maintenance/recovery functions. Both require the opaque selector-held quiescence guard and the documented nonblocking lock order. Migration must parse and authenticate the exact original legacy state/checkpoint wire bytes, signatures, authority identity, monotonic values, and mutual consistency before applying defaults or normalization; reject unsigned, default-smuggled, rolled-back, aliased, or ambiguous legacy bytes without creating a pool or witness head. After preservation-first archival through held descriptors and no-replace moves, establish the first fixed binding and witness-backed epoch transition with the preserved legacy payload digest, complete new binding, exact admission/session authorization, external prepare, durable local journal/barriers, external commit, and signed discovery recovery. Leave Plan05C's fail-closed `MigrateGovernanceLock` CLI placeholder unchanged and unreachable in this wave; Task06C alone replaces it after the concrete authenticated transport worker and dependency exist, and `--confirm-offline` alone remains non-authoritative. Rebind is permitted only after offline maintenance has preserved a malformed/replaced inactive lane and proved the governed canonical/external head unchanged; create a fresh fixed inactive lane with a new authenticated identity only through a signed witness-backed `epoch + 1` transition that binds the old head retirement digest, preserved hostile bytes, new complete binding, and before/after mappings. Arbitrary epoch jumps, local sequence reset, ordinary-startup migration/rebind, local-only rebind, path-derived replacement, failed fsync, lost-response guess, or overwriting/deleting the foreign lane returns typed refusal/`Uncertain`. Extend registry self-tests so removing either governance case, substituting an aggregate, or returning zero cannot pass.</action>
  <verify>
    <automated>bash tools/check-phase285-governance-persistence.sh offline-maintenance</automated>
    The selector must execute the four drain/reset controls plus both exact migration/rebind controls, all with `passed = executed`, `failed = 0`, and `ignored = 0`; pre-normalization tampering, missing witness evidence, absent quiescence, arbitrary epoch, attempted local fallback, and foreign-lane overwrite mutations must fail with preserved bytes. The CLI remains the Plan05C fail-closed placeholder until Task06C materializes the real transport worker.
  </verify>
  <done>Legacy streams and damaged inactive lanes have explicit authenticated offline recovery paths, while ordinary startup and local-only maintenance remain unable to migrate, normalize, or rebind authority.</done>
</task>

</tasks>

<verification>
Run the `offline-maintenance` selector, governance package tests, strict clippy, formatting, and `git diff --check`. Inspect every journal resume decision and assert source-present/archive-absent or source-absent/archive-present proof is exact; verify no unlink or generated archive name occurs. Obtain an independent zero-P0/P1/P2 review before Plan06C.
</verification>

<success_criteria>
All offline maintenance cases execute non-vacuously; every selected slot carries the mandated identity/digest/length/head/source/destination/parent/pool/archive/mapping proof; drain/reset semantics cannot be swapped; crash recovery preserves foreign/uncertain bytes; and normal startup never performs maintenance.
</success_criteria>

<output>
After completion, create `.planning/phases/285-assurance-foundation-closure/285-06B-SUMMARY.md`.
</output>
