---
phase: 285-assurance-foundation-closure
plan: "05A"
type: execute
wave: 6
depends_on:
  - "285-04"
files_modified:
  - crates/swarm-governance/Cargo.toml
  - Cargo.lock
  - crates/swarm-governance/src/lib.rs
  - crates/swarm-governance/src/fixed_lanes.rs
  - crates/swarm-governance/src/persistence_coordinator.rs
  - crates/swarm-governance/tests/phase285_governance_persistence.rs
  - tools/check-phase285-governance-persistence.sh
autonomous: true
requirements:
  - ASSURE-04
  - ASSURE-06
must_haves:
  truths:
    - "Fixed publication lanes are authenticated by held descriptor identities and a complete signed cleanup-pool binding; pathname replacement, hard-link aliasing, same-inode content mutation, and cross-role identity swaps refuse before mutation."
    - "Current and legacy authority sidecars are exactly two hard links to one regular inode with `st_nlink == 2`; extra external links and every cross-role alias refuse, and the opaque selector/authority/state/pool guards remain live through construction, exchange, refusal, and return."
    - "The governance-persistence checker has six exact selector registries, including runtime-http detector-integration, and cannot pass absent, zero, ignored, failed, partial, or substring-matched governance evidence."
  artifacts:
    - path: "crates/swarm-governance/src/fixed_lanes.rs"
      provides: "Safe rustix descriptor-relative fixed-lane identities, hard-link authority-pair proof, namespace enumeration, within-role exchange, and fsync barriers."
      contains: "FixedLaneGuard"
    - path: "crates/swarm-governance/src/persistence_coordinator.rs"
      provides: "GovernancePersistenceCoordinator seam plus signed CleanupPoolBindingV1 and fixed guard lifetime."
      contains: "GovernancePersistenceCoordinator"
    - path: "crates/swarm-governance/tests/phase285_governance_persistence.rs"
      provides: "Non-vacuous fixed-lane, binding, inode-alias, and guard-lifetime mutation target."
      contains: "fixed_lanes_validate_complete_binding_and_namespace"
    - path: "tools/check-phase285-governance-persistence.sh"
      provides: "Exact six-selector governance registry and executed/passed/failed/ignored parser for all later governance commands."
      contains: "detector-integration"
  key_links:
    - from: "CleanupPoolBindingV1"
      to: "signed pool binding, governance state, and sequence checkpoint"
      via: "one canonical signed binding digest is compared across all three copies before allocation or exchange"
      pattern: "binding_file_identity.*slot_names.*state_pair.*external_witness_identity"
    - from: "AuthorityPairGuard"
      to: "current and legacy authority sidecars"
      via: "rustix statat proves regular-file st_dev/st_ino equality and rejects cross-role hard links"
      pattern: "current.*legacy.*st_ino.*st_dev"
    - from: "GovernancePersistenceCoordinator"
      to: "FixedLaneGuard"
      via: "opaque selector, authority-pair, state, pool, binding, canonical, staging, and journal descriptors survive the complete operation"
      pattern: "GovernancePersistenceCoordinator.*FixedLaneGuard"
    - from: "tools/check-phase285-governance-persistence.sh"
      to: "phase285_governance_persistence.rs and swarm-detect"
      via: "six explicit selectors invoke exact test names with --exact and parse every Rust count"
      pattern: "detector-integration.*swarm-runtime-http.*--exact"
---

<objective>
Implement the fixed authenticated filesystem-lane foundation and the sole governance-persistence selector registry.

Purpose: Local persistence must never turn a pathname, replaceable inode, or ambiguous checker result into governance authority. This plan freezes the exact binding and identity contract that transaction recovery, retention, maintenance, and detector integration consume.
Output: Safe descriptor-relative fixed lanes, complete signed cleanup-pool binding, hard-link authority proof, typed `GovernancePersistenceCoordinator` seam, and the six-selector non-vacuous governance wrapper.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/285-assurance-foundation-closure/285-CONTEXT.md
@.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md
@.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md
@.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md
@<.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md>
@crates/swarm-governance/src/lib.rs
@crates/swarm-governance/src/persistence_protocol.rs
@crates/swarm-governance/src/witness_engine.rs
@crates/swarm-governance/Cargo.toml

The accepted protocol names `Intent`, `WitnessPrepared`, `PayloadsStaged`, `StateExchanged`, `CheckpointExchanged`, `ReadyForWitnessCommit`, `AbortPending`, `Committed`, and `Aborted`; later recovery must preserve this exact phase vocabulary. This plan does not implement recovery, retention, maintenance, or detector construction. `GovernancePersistenceCoordinator` is the only accepted coordinator symbol; all later plans use that same symbol.
</context>

<tasks>

<task type="auto">
  <wave>6</wave>
  <name>Task 285-05-01: Implement fixed lanes, complete cleanup binding, and the six-selector governance registry</name>
  <files>crates/swarm-governance/Cargo.toml, Cargo.lock, crates/swarm-governance/src/lib.rs, crates/swarm-governance/src/fixed_lanes.rs, crates/swarm-governance/src/persistence_coordinator.rs, crates/swarm-governance/tests/phase285_governance_persistence.rs, tools/check-phase285-governance-persistence.sh</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md; .planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md; .planning/phases/285-assurance-foundation-closure/285-RESEARCH.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance/Cargo.toml; Cargo.lock; crates/swarm-governance/src/lib.rs; crates/swarm-governance/src/persistence_protocol.rs; crates/swarm-governance/src/witness_engine.rs</read_first>
<action>Work red-first by adding exact cases `fixed_lanes_validate_complete_binding_and_namespace`, `fixed_lanes_rejects_authority_sidecar_nonregular_or_not_hard_linked`, `fixed_lanes_rejects_cross_role_alias_and_same_inode_mutation`, `fixed_lanes_exchanges_only_within_authenticated_pairs`, `fixed_lanes_holds_guards_through_failure_and_fsync`, and `fixed_lanes_rejects_derived_names_and_seam_replacement` to `crates/swarm-governance/tests/phase285_governance_persistence.rs`. Add the direct `rustix` dependency with safe `fs` features. Use the safe descriptor-backed `rustix::fs::Dir::read_from` iterator to enumerate every entry from the already-held directory descriptor, and use only `rustix::fs::{openat,statat,fsync,renameat_with}` plus `AtFlags`, `OFlags`, `Mode`, and `RenameFlags::{NOREPLACE,EXCHANGE}` for descriptor-relative inspection and mutation; no `std::fs::read_dir`, `libc`, `unsafe`, reopened path authority, or hand-rolled syscall wrapper. Implement `FixedLaneGuard`, `GovernanceSelectorGuard`, `AuthorityPairGuard`, `StateLaneGuard`, `CleanupPoolGuard`, `FixedLaneIdentityV1`, and `GovernancePersistenceCoordinator` in focused modules, exporting only the narrow coordinator seam from `lib.rs` and preserving accepted protocol types. Define `CleanupPoolBindingV1` as a closed, canonical, signed structure containing every field required by the reviewed protocol: `schema_version`, `domain_version`, `binding_file_identity`, `parent_identity`, `pool_identity`, `lock_identity`, `namespace_generation` (exact 32-byte generation), `pool_dir_name`, `slot_names` (the exact fixed names), `slot_count`, explicit `state_staging_name`, `checkpoint_staging_name`, `journal_primary_name`, and `journal_secondary_name`, immutable within-role inode pairs for state canonical/staging, checkpoint canonical/staging, and journal primary/secondary, limits for state/checkpoint/binding/record/journal/retained/pool bytes and slot count, `external_witness_identity`, `external_stream_id`, `witness_binding_generation`, and `witness_binding_digest`. The binding-file identity is distinct from parent, pool, lock, canonical, staging, journal, and slot identities; all roles are regular files/directories of the expected type and pairwise inode-distinct except the authority pair. Sign the pool-local binding envelope under a fixed domain and embed the identical canonical binding plus digest in signed governance state and signed sequence checkpoint; startup/load/save/recovery refuse if any copy, signature, field, fixed name, slot list, staging lane, immutable pair, limit, witness identity, or stream binding differs. Generate the namespace generation only during explicit first initialization; never rebind a missing/legacy/replaced pool or lock during ordinary startup. Acquire selector -> state -> authority-pair -> cleanup-pool locks, retain parent/pool/lock/binding/canonical/staging/journal descriptors as opaque guard fields, enumerate the complete namespace, verify current/legacy authority sidecars, and perform precheck -> exchange/no-replace -> postcheck -> directory/pool/parent fsync. Ordinary names are only the fixed canonical, staging, and two alternating journal names; reject counter/process/timestamp/transaction/random/pathname-derived names. For authority proof, require current and legacy sidecars to be regular files with identical `st_dev` and `st_ino` and exactly `st_nlink == 2`, retain both held descriptors, and reject a missing link, extra external hard link, replacement, content mutation, or a cross-role hard link that aliases either sidecar to state, checkpoint, pool, lock, binding, staging, journal, archive, or slot. Mutations must attempt third-link creation, same-inode content replacement, and cross-role aliases after each held identity seam and assert typed refusal before a durable exchange. In the same task create `tools/check-phase285-governance-persistence.sh` as the sole owner of the six exact selectors used by this and all later plans: `fixed-lanes` maps to the six exact `phase285_governance_persistence` names above; `transaction-recovery` maps to the nine exact crash tests `transaction_recovery_crash_restart_at_intent`, `transaction_recovery_crash_restart_at_witness_prepared`, `transaction_recovery_crash_restart_at_payloads_staged`, `transaction_recovery_crash_restart_at_state_exchanged`, `transaction_recovery_crash_restart_at_checkpoint_exchanged`, `transaction_recovery_crash_restart_at_ready_for_witness_commit`, `transaction_recovery_crash_restart_at_abort_pending`, `transaction_recovery_crash_restart_at_committed`, and `transaction_recovery_crash_restart_at_aborted`, plus `transaction_recovery_uncertain_state_survives_restart`, `transaction_recovery_abort_pending_requires_exact_signed_outcome`, `transaction_recovery_lost_prepare_commit_abort_uses_discovery`, `transaction_recovery_fetches_payload_and_reconstructs_verified_lanes`, `transaction_recovery_missing_replaced_or_corrupt_local_bytes_is_uncertain`, and `transaction_recovery_never_selects_highest_sequence`; `enforced-checkpoint` maps to `enforced_checkpoint_refuses_missing_witness_before_authority_mint`, `enforced_checkpoint_refuses_witness_outage_or_bad_admission_before_local_mutation`, `enforced_checkpoint_shares_one_authority_identity_across_coordinator_consumers`, `enforced_checkpoint_keeps_local_constructor_offline_only`, `enforced_checkpoint_mock_bridge_timeout_disconnect_and_capacity`, and `enforced_checkpoint_compile_time_rejects_detector_local_toggle`; `retention` maps to `retention_uses_exact_state_machine_and_post_commit_barrier`, `retention_uncertain_overrides_retained_after_final_seam_change`, `retention_pool_exhaustion_refuses_before_source_move`, `cleanup_pool_binding_survives_restart_and_rejects_namespace_replacement`, and `retention_foreign_preserved_requires_verified_preservation_and_never_masks_uncertain`; `offline-maintenance` maps to `offline_maintenance_drain_and_reset_are_distinct_preservation_only_operations`, `offline_maintenance_crash_resumes_from_signed_alternating_journal`, `offline_maintenance_requires_quiescence_and_lock_order`, and `offline_maintenance_never_runs_as_normal_startup`; `detector-integration` maps to these seven literal `swarm-detect` binary tests: `detector_integration_refuses_without_authenticated_witness_before_authority_mint`, `detector_integration_shares_selector_and_authority_across_all_trust_consumers`, `detector_integration_reinitialize_requires_epoch_plus_one_and_retired_head`, `detector_integration_removing_witness_cannot_reach_local_constructor`, `detector_integration_rejects_unknown_or_cross_role_config`, `detector_integration_worker_deadlock_outage_cancellation_and_backpressure`, and `detector_integration_compile_time_rejects_local_constructor_toggle`; invoke each with `cargo test -p swarm-runtime-http --bin swarm_detect --locked --offline -- <name> --exact`. The other five selectors invoke `cargo test -p swarm-governance --test phase285_governance_persistence --locked --offline -- <name> --exact`. The wrapper must reject unknown selectors, run every mapped name individually with `--exact`, parse `running`, `passed`, `failed`, `ignored`, and `filtered out` counts from every Rust summary, require every requested name to appear exactly once with executed > 0, passed = executed, failed = 0, ignored = 0, and reject absent targets, substring-only matches, partial selector output, `#[ignore]`, zero tests, or a command that reports only filtered tests. Add wrapper self-tests using temporary fixture targets for missing, zero, ignored, failed, partial, and substring-only cases; do not weaken the real registry to make the self-tests green. Plan01 owns the other five `check-phase285-*` scripts; this task alone owns this governance wrapper after its exact registry exists.</action>
  <verify>
    <automated>bash tools/check-phase285-governance-persistence.sh fixed-lanes && cargo test -p swarm-governance --test phase285_governance_persistence --locked --offline -- fixed_lanes_validate_complete_binding_and_namespace --exact</automated>
    The wrapper must print the selector-to-target mapping and positive per-test counts, reject all temporary missing/zero/ignored/failed/partial/substring-only fixtures, and show the six binding/identity cases passed with `failed = 0` and `ignored = 0`.
  </verify>
  <done>The fixed-lane target proves the complete signed `CleanupPoolBindingV1`, same-inode authority sidecars, cross-role alias refusal, held guard lifetime, within-role exchange, fsync barriers, and fixed-name policy; the sole governance wrapper has all six exact selector registries and non-vacuous count parsing without modifying Plan01-owned checkers.</done>
</task>

</tasks>

<verification>
Run `bash tools/check-phase285-governance-persistence.sh fixed-lanes`, `cargo test -p swarm-governance --lib --locked --offline`, strict governance clippy, `cargo fmt --all -- --check`, and `git diff --check`. Inspect the wrapper's self-test report for positive and deliberate red controls. Obtain an independent zero-P0/P1/P2 review of this fixed-lane slice; it is not Phase 285 completion evidence.
</verification>

<success_criteria>
The fixed-lanes selector is non-vacuously green; all CleanupPoolBindingV1 fields and its three signed copies are mutation-tested; authority sidecars prove one regular inode with same-device/inode identity and reject cross-role aliases; rustix-only fixed-lane operations preserve opaque guards and fail closed; and the governance wrapper owns exact `fixed-lanes`, `transaction-recovery`, `enforced-checkpoint`, `retention`, `offline-maintenance`, and `detector-integration` mappings with executed/passed/failed/ignored enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/285-assurance-foundation-closure/285-05A-SUMMARY.md`.
</output>
