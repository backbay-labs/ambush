---
phase: 285-assurance-foundation-closure
plan: "03B"
type: execute
wave: 4
depends_on:
  - "285-03A"
files_modified:
  - docker-compose.yml
  - crates/swarm-governance/src/witness_engine/store.rs
  - crates/swarm-governance-witness/src/lib.rs
  - crates/swarm-governance-witness/src/nats_config.rs
  - crates/swarm-governance-witness/src/raw_config.rs
  - crates/swarm-governance-witness/src/jetstream_store.rs
  - crates/swarm-governance-witness/tests/jetstream_cas.rs
  - crates/swarm-governance-witness/tests/jetstream_checkpoint.rs
  - tools/check-negative-registry.sh
  - tools/check-phase285-witness-conformance.sh
  - tools/with-nats-jetstream.sh
autonomous: true
requirements:
  - ASSURE-04
  - ASSURE-06
must_haves:
  truths:
    - "NATS 2.11.17 raw create/info config is decoded by one closed version-specific DTO before async-nats normalization can discard unknown, present-false, or absent-field evidence."
    - "The downstream repository reuses the accepted Plan 02 read/CAS validators, proves the exact Ready snapshot, and reports Applied only after an exact non-duplicate acknowledgement and leader-mediated raw confirmation."
    - "The same accepted nineteen-scenario semantic corpus reaches direct, reference, typed-proxy, and JetStream stores; fixed raw/header/iterator/ack/crash matrices have independent expected and actual ledgers."
    - "Five post-ack/pre-confirmation SIGKILL checkpoints return Ambiguous without retry or upgrade, then survive restart on the same pinned image and volume with exact bytes, sequence, headers, epoch, anchor, and initialization binding."
  artifacts:
    - path: "crates/swarm-governance-witness/src/raw_config.rs"
      provides: "Closed Nats21117RawStreamInfoV1/Nats21117RawConfigV1 projection and byte-exact raw-config digest."
      contains: "deny_unknown_fields"
    - path: "crates/swarm-governance/src/witness_engine/store.rs"
      provides: "Public typed read/CAS validation seam shared by the in-memory, reference, proxy, and downstream JetStream implementations."
      contains: "validate_cas_transition"
    - path: "crates/swarm-governance-witness/src/jetstream_store.rs"
      provides: "Fixed-subject leader-mediated NatsWitnessStore with exact acknowledgement and raw-read confirmation."
      contains: "get_last_raw_message_by_subject"
    - path: "tools/check-phase285-witness-conformance.sh"
      provides: "Target-specific materialization plus exact inner-ledger validation for the frozen five CAS and four checkpoint rows."
      contains: "jetstream-checkpoint"
    - path: "tools/with-nats-jetstream.sh"
      provides: "Pinned two-account, non-skipping, same-volume SIGKILL/restart harness and hostile scratch self-test."
      contains: "e4bf19f15fd3218814a4e3c9e0064e1334bd8aa20d5984b9f1a0afd084f8cc00"
  key_links:
    - from: "Nats21117RawConfigV1::project"
      to: "WitnessBucketEpochV1.bucket_configuration_digest"
      via: "the projected WitnessBucketConfigurationV1 canonical digest must equal the epoch-bound semantic digest"
      pattern: "bucket_configuration.*digest.*bucket_configuration_digest"
    - from: "Nats21117RawConfigV1::raw_digest"
      to: "WitnessBucketAnchorV1.raw_stream_configuration_digest"
      via: "the distinct domain-separated digest of the exact closed raw config bytes must equal the sealed anchor"
      pattern: "raw_stream_configuration_digest"
    - from: "NatsWitnessStore::inspect_ready"
      to: "closed raw snapshots plus Stream::info_with_subjects"
      via: "bounded initial raw snapshot, exact iterator ledger, fresh final raw snapshot, then leader-mediated validation of every entry"
      pattern: "subjects_count.*messages.*first_sequence.*last_sequence.*created"
    - from: "NatsWitnessStore::compare_and_swap"
      to: "validate_cas_transition and Stream::get_last_raw_message_by_subject"
      via: "shared semantic admission before publish; exact ack predicates; exact post-ack raw sequence, bytes, digest, and headers before Applied"
      pattern: "validate_cas_transition.*publish_put_cas.*get_last_raw_message_by_subject"
---

<objective>
Implement the pinned NATS 2.11.17 JetStream repository and a non-skipping, same-volume restart harness without duplicating Plan 02 semantics or exposing a generic raw-management surface.

Purpose: make the accepted revision-CAS model durable. Raw server configuration remains observable before permissive client normalization, and a publish acknowledgement is never authority without exact leader confirmation.
Output: closed raw-config projection, shared typed validation seam, fixed-subject NATS repository, exact inner-case ledgers, pinned two-account Compose harness, and deterministic post-ack restart evidence.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/285-assurance-foundation-closure/285-CONTEXT.md
@.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md
@.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md
@.planning/phases/285-assurance-foundation-closure/285-VERIFICATION.md
@.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md
@<git show 5be011a07690a63a297d5bba8fbf740bb659c19d:.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md>
@crates/swarm-governance/src/witness_engine/store.rs
@crates/swarm-governance-witness/Cargo.toml
@crates/swarm-governance-witness/src/lib.rs
@docker-compose.yml
@tools/check-negative-registry.sh
@tools/check-phase285-witness-conformance.sh
@tools/with-nats-jetstream.sh

The exact image is `docker.io/library/nats:2.11.17-alpine@sha256:e4bf19f15fd3218814a4e3c9e0064e1334bd8aa20d5984b9f1a0afd084f8cc00`; the index digest was independently resolved before this plan freeze. One private, fixed-subject, read-only `Context::request` to `STREAM.INFO.<validated exact stream>` and one init-only closed create-response decoder are required because async-nats 0.47's `Stream::get_info` normalizes into permissive `Info/Config`. No caller-supplied management subject, update/delete/purge API, generic normalizer, public raw-subject/header constructor, floating image, or async persistence projection is permitted.

Plan 03B has three mandatory internal freeze/review checkpoints: A (raw/config/harness), B (repository/differential), and C (restart). Each checkpoint has a fresh two-hour convergence window. The executor stops at the boundary; the hostile reviewer must report P0/P1/P2 = 0/0/0 on the immutable staged tree before the next checkpoint begins. The final accepted Plan 03B object remains one atomic commit from accepted Plan 03A.
</context>

<tasks>

<task type="auto">
  <wave>4</wave>
  <name>Task 285-03-02A: Freeze closed 2.11.17 raw projection and the pinned harness</name>
  <files>docker-compose.yml, crates/swarm-governance-witness/src/lib.rs, crates/swarm-governance-witness/src/nats_config.rs, crates/swarm-governance-witness/src/raw_config.rs, crates/swarm-governance-witness/tests/jetstream_cas.rs, tools/check-negative-registry.sh, tools/with-nats-jetstream.sh</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md lines 784-920 and 1286-1341; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance-witness/Cargo.toml; crates/swarm-governance-witness/src/lib.rs; tools/check-negative-registry.sh; tools/with-nats-jetstream.sh; docker-compose.yml; async-nats 0.47 stream.rs Config/Info and context.rs Context::request</read_first>
  <action>Red-first add the five frozen `jetstream-cas` test names, but implement only the two raw/config cases in this checkpoint. Define `Nats21117RawStreamInfoV1` and `Nats21117RawConfigV1`; the config uses `#[serde(deny_unknown_fields)]`, has no serde defaults that erase presence, and accepts exactly `name`, `description`, `subjects`, `retention`, `max_consumers`, `max_msgs`, `max_bytes`, `max_age`, `max_msgs_per_subject`, `max_msg_size`, `discard`, `storage`, `num_replicas`, `duplicate_window`, `compression`, `allow_direct`, `mirror_direct`, `sealed`, `deny_delete`, `deny_purge`, `allow_rollup_hdrs`, `consumer_limits`, `allow_msg_ttl`, and `metadata`. Reject every other key, every missing allowed key, `persist_mode` including `async`, changed false/zero/empty/absent semantics, changed reserved metadata, wrong server/image/version, and noncanonical DTO encoding. Project the complete contract values, including the ten required-absent keys (`no_ack`, `discard_new_per_subject`, `template_owner`, `placement`, `mirror`, `sources`, `first_seq`, `subject_transform`, `republish`, `subject_delete_marker_ttl`), the three 2.12-only absent keys, empty consumer limits, and exactly `_nats.level=1`, `_nats.req.level=0`, `_nats.ver=2.11.17`. Keep a literal golden config value in Rust and mutate its JSON value in memory. The anchor raw digest covers canonical reserialization of the complete deny-unknown raw DTO after exact key-presence/value validation; it does not claim original response whitespace, key order, or numeric lexical bytes. Separately project/digest `WitnessBucketConfigurationV1` and compare it to the epoch semantic digest.

Pin Compose to the exact image reference. Extend `with-nats-jetstream.sh` to create a canonical, pre-write-confined scratch directory, generated NATS config, and Compose override with exact expected/foreign accounts, distinct fixed harness credentials, account-local JetStream, a read-only config mount, and the same named data volume across stop/start. Plaintext is loopback-only in this harness. Inspect the resolved repo digest, `/varz` reported version, health, expected account, and foreign-account refusal. Add `--self-test` fixtures for wrong/floating image, wrong version/account, absent/unhealthy service, hostile root/Git-dir/Git-common-dir/symlink `TMPDIR`, cleanup failure, zero/skipped/ignored/partial output, and stale coordination transcript. Do not use `|| true` on an acceptance path.

Replace the obsolete whole-crate `$KV.` lexical ban with a structural boundary that remains red until Stage B adds the one private fixed derivation: no public subject/header builder, raw string input, wildcard writer, generic request method, or management/update/delete/purge operation. Its self-test must inject public/generic/raw-authority mutants and kill every one. Freeze and stage only the seven owned paths; the root records the exact tree and an independent reviewer must return 0/0/0 before Task 285-03-02B starts.</action>
  <acceptance_criteria>The exact image/version/two-account harness and hostile self-tests pass; closed raw config rejects every key/presence/value mutation; canonical DTO and semantic digests bind the correct anchor/epoch fields; no raw authority is exported; only the two declared raw/config tests are green.</acceptance_criteria>
  <verify>
    <automated>test "$(git rev-parse HEAD)" = cf0ad8b287a23fd1a4b57c922c8318b77c2cea81 && bash tools/with-nats-jetstream.sh --self-test && bash tools/check-negative-registry.sh --self-test phase285-raw-kv-subject && bash tools/with-nats-jetstream.sh cargo test -p swarm-governance-witness --test jetstream_cas --locked --offline -- jetstream_cas_rejects_raw_config_unknown_field_or_persist_mode --exact && bash tools/with-nats-jetstream.sh cargo test -p swarm-governance-witness --test jetstream_cas --locked --offline -- jetstream_cas_rejects_each_raw_config_mutation --exact && cargo test -p swarm-governance-witness --lib --locked --offline && cargo clippy -p swarm-governance-witness --lib --test jetstream_cas --all-features --locked --offline -- -D warnings && cargo fmt --all -- --check && shellcheck tools/check-negative-registry.sh tools/with-nats-jetstream.sh && actionlint .github/workflows/ci.yml && git diff --check</automated>
    Root stages exactly the seven declared paths after this command, freezes `git write-tree`, verifies zero unstaged paths and accepted-parent identity, and sends only that immutable tree to the hostile reviewer.
  </verify>
  <done>Checkpoint A is an immutable zero-finding raw/config/harness tree; Task 285-03-02B is now authorized.</done>
</task>

<task type="auto">
  <wave>4</wave>
  <name>Task 285-03-02B: Freeze shared validation, stable InspectReady, confirmed CAS, and the nineteen-row differential</name>
  <files>crates/swarm-governance/src/witness_engine/store.rs, crates/swarm-governance-witness/src/lib.rs, crates/swarm-governance-witness/src/raw_config.rs, crates/swarm-governance-witness/src/jetstream_store.rs, crates/swarm-governance-witness/tests/jetstream_cas.rs, tools/check-negative-registry.sh, tools/check-phase285-witness-conformance.sh</files>
  <read_first>the immutable accepted Checkpoint A tree; .planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md lines 975-1017, 1030-1134, 1218-1229, and 1286-1375; crates/swarm-governance/src/witness_engine/store.rs; crates/swarm-governance-witness/src/raw_config.rs; crates/swarm-governance-witness/tests/jetstream_cas.rs; tools/check-negative-registry.sh; tools/check-phase285-witness-conformance.sh; async-nats 0.47 InfoWithSubjects/get_last_raw_message_by_subject, Context::request, and PublishAck</read_first>
  <action>Red-first implement the remaining three CAS tests and their exact ledgers. Make only `validate_read_entry` and `validate_cas_transition` public typed validator seams in governance; keep their helpers private. `NatsWitnessStore` must call them rather than reproduce Plan 02 semantics. `Nats21117RawStreamInfoV1` must preserve the original server-reported `created` spelling in the complete canonical response bytes/digest while projecting pinned NATS 2.11.17 Go `RFC3339Nano` creation times into the protocol's exactly-nine-fractional-digit UTC form: accept only `YYYY-MM-DDTHH:MM:SSZ` or the same form with one through nine fractional digits, right-pad the fraction with zeroes, and reject offsets, lowercase/non-UTC suffixes, malformed dates/times, empty fractions, and more than nine digits. Anchor comparison and stable creation-time equality use only that canonical projection; neither the original `created` nor top-level `ts` may be normalized or omitted from full-response evidence. Controls must cover zero/one/eight/nine fractional digits, equivalent-spelling projection equality, changed-instant inequality, every rejected form, a raw-spelling-as-identity mutant, and a full-evidence normalization mutant. The one internal raw-info reader derives the exact `STREAM.INFO.<validated stream>` request itself and decodes `Response<Nats21117RawStreamInfoV1>`; it exposes no generic request subject. `InspectReady` snapshots the initial closed raw info, rejects `subjects_count` above one manifest plus the admitted maximum, bounds and counts every `info_with_subjects` yield, rejects count other than one, duplicate/short/extra/wildcard/cross-page/cumulative-overflow cases, requires exact manifest-plus-stream-key equality, then takes a fresh closed raw info snapshot and requires unchanged subject/message counts, first/last sequence, canonical stream creation time, canonical raw-configuration bytes/digest, and projected configuration before leader-reading every entry. Each complete closed Stream.Info response, including its original `created` spelling and required top-level response `ts`, remains canonically decoded and retains its own full-response bytes/digest as per-read evidence; those full-response values are never used as an equality oracle because pinned NATS 2.11.17 refreshes `ts` on every request. A mutant that compares the complete responses, omits either full-response evidence value, ignores one enumerated stable field, normalizes raw `created`, or normalizes/excludes `ts` from the per-read full-response digest must fail.

`CompareAndSwap` derives exactly `$KV.<bucket>.s.<admitted-digest>`, sets only one each of `KV-Operation: PUT`, `Nats-Expected-Stream: KV_<bucket>`, and `Nats-Expected-Last-Subject-Sequence: <observed_revision>`, and refuses zero revision or caller-controlled subject/header input. Before publish it leader-reads current bytes and calls the shared CAS validator. After publish it requires `ack.stream == configured stream`, `ack.duplicate == false`, `ack.sequence > observed_revision`, then leader-reads and requires `raw.sequence == ack.sequence`, byte-identical value, expected value digest, exact fixed subject, exactly the three permitted headers, and expected-subject-sequence equal to the observed revision. Missing/DEL/PURGE/unknown/duplicate/conflicting operation, rollup, TTL, message-ID, wrong-stream/revision, direct read, plain create/put/delete/purge, wildcard, last-write-wins, and unconfirmed acknowledgement refuse; failure after acknowledgement is typed Ambiguous and never Applied.

Drive the accepted nineteen scenario IDs exactly (`genesis`, `rotation`, `sealed_prepare`, `commit`, `abort`, `read`, `conflict`, `exact_idempotent_observation`, `resigned_content`, `resigned_stale_session`, `resigned_admission`, `component_limit`, `capacity`, `crash_before_cas`, `lost_after_cas`, `wrong_revision_ack`, `duplicate_ack`, `corrupt_read`, `injected_capacity`) through the direct/reference/typed-proxy/JetStream projection. The exact normalized record retains outcome/error kind, stream ID, previous-revision relation, strictly-increasing-new-revision predicate, acknowledged digest, duplicate flag, complete conflict or ambiguity metadata, read envelope bytes, final canonical envelope bytes/digest, and pre/post bytes for every refusal. It may abstract only the physical revision number into those explicit relations. Require exact record equality and exact terminal bytes; field-removal, relation-to-constant, metadata substitution, and refusal-byte elision mutants must fail. Real NATS supplies positive CAS/read/config evidence; explicit broken ack/read/commit controls prove adapter classifications. Each top-level CAS test writes an actual inner ledger only to the checker-provided confined path. The shell owns an independent expected ID projection derived from fixed raw keys, absent keys, semantic fields, iterator predicates, acknowledgement/header predicates, and nineteen scenario IDs; require exact set/cardinality/status equality and kill omission/addition/duplication/zero/renaming/wrong-status/digest mutants.

Own `tools/check-phase285-witness-conformance.sh`: preserve the exact 58-row registry, digest, tuple commands, all accepted Plan 01/02 behavior, and six transport rows; replace the global materialized inventory with target-specific inventories, materialize only the five CAS and four checkpoint names, and enable only those selectors. Complete the structural raw-authority guard so the fixed prefix/subject derivation exists only in private `jetstream_store`; compile/source mutants exporting a builder, raw input, wildcard, generic request, or management operation must fail. Freeze and stage the cumulative A+B tree; root records its exact tree and an independent reviewer must return 0/0/0 before Task 285-03-03 starts.</action>
  <acceptance_criteria>The private raw boundary kills every public/generic mutant; InspectReady proves a stable complete set; CAS reuses Plan 02 validation and returns Applied only after every exact ack/read predicate; all nineteen non-lossy normalized records, terminal bytes, and fixed inner mutation IDs execute exactly once.</acceptance_criteria>
  <verify>
    <automated>test "$(git rev-parse HEAD)" = cf0ad8b287a23fd1a4b57c922c8318b77c2cea81 && bash tools/with-nats-jetstream.sh bash tools/check-phase285-witness-conformance.sh jetstream-cas && bash tools/check-phase285-witness-conformance.sh transport-layering && bash tools/check-phase285-witness-conformance.sh in-memory-differential && bash tools/check-phase285-witness-conformance.sh typed-proxy && bash tools/check-witness-dependency-closure.sh --library-only && cargo test -p swarm-governance --locked --offline && bash tools/with-nats-jetstream.sh cargo test -p swarm-governance-witness --locked --offline && cargo clippy -p swarm-governance -p swarm-governance-witness --all-targets --all-features --locked --offline -- -D warnings && cargo fmt --all -- --check && shellcheck tools/check-negative-registry.sh tools/check-phase285-witness-conformance.sh tools/with-nats-jetstream.sh && actionlint .github/workflows/ci.yml && git diff --check</automated>
    Root stages exactly the cumulative declared paths after this command, freezes `git write-tree`, verifies zero unstaged paths, accepted parent, and byte identity for every untouched Plan 03A path, and sends only that immutable tree to the hostile reviewer.
  </verify>
  <done>Checkpoint B is an immutable zero-finding repository/differential tree; Task 285-03-03 is now authorized.</done>
</task>

<task type="auto">
  <wave>4</wave>
  <name>Task 285-03-03: Prove post-ack ambiguity and same-volume restart without skipping</name>
  <files>crates/swarm-governance-witness/src/jetstream_store.rs, crates/swarm-governance-witness/tests/jetstream_checkpoint.rs, tools/check-phase285-witness-conformance.sh, tools/with-nats-jetstream.sh, docker-compose.yml</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md lines 975-1017, 1100-1134, 1218-1229, and 1345-1363; crates/swarm-governance-witness/src/jetstream_store.rs; crates/swarm-governance-witness/tests/jetstream_cas.rs; tools/check-phase285-witness-conformance.sh; tools/with-nats-jetstream.sh; docker-compose.yml</read_first>
  <action>Red-first add the four frozen `jetstream-checkpoint` tests. Under `cfg(debug_assertions)` only, expose a doc-hidden test constructor that installs an exact one-shot post-ack/pre-confirming-read barrier. The acknowledgement event is an immutable value captured from the real publish acknowledgement and contains exactly `{stream, sequence, duplicate, proposed_digest, token}`; it is emitted only after the configured-stream, non-duplicate, and increasing-sequence predicates pass, and before any confirming read or result classification. The release event must carry the same token and occur exactly once. The test uses the harness's token-bound control subcommand to `docker compose kill -s KILL nats`, observes the original CAS return `Ambiguous` with no internal retry and no later upgrade, restarts the same service/project/volume and exact pinned image, waits for the same expected-account leader, then independently reads the persisted raw entry and compares its sequence, stream, bytes, and digest to the pre-kill acknowledgement event rather than deriving both sides after restart.

Run the exact five-state matrix: current, predecessor, prepared, abort, and genesis abort. For each state require byte-identical signed envelope, raw sequence equal to the acknowledged sequence, exact fixed subject and headers, matching external epoch/anchor/configuration/creation time/Ready-manifest initialization digest, and monotonic envelope `store_generation`; global raw sequence remains distinct from `store_generation`. Add exact recreated-stream/stale-anchor, wrong expected-subject-sequence, understated/short/cross-page/overflow/final-snapshot, wrong-account, unavailable-server, and global-sequence controls. The cross-key control performs at least ten A updates, creates B, and successfully updates B with B's first global sequence as the expected-last-subject-sequence; a global-tail comparison mutant must fail.

Each top-level checkpoint test writes an actual ledger to the confined checker path. The independent shell expectation contains the five state IDs, complete acknowledgement tuple fields, barrier reach/release phases and token equality, restart identity predicates, anchor/recreation predicates, iterator/page predicates, unavailable/account predicates, global/store-generation cases, and `release_hook_absent`. Require exact set/cardinality/status equality and reject omission/addition/duplication/zero/renaming/wrong-status/stale-image/stale-volume/stale-tree/ack-substitution mutants.

The normal `jetstream-checkpoint` selector itself, not only a separate self-test, creates one confined downstream probe and binds a fresh invocation token. It runs `cargo generate-lockfile --offline` exactly once, then validates the generated probe lock before any compile: it contains exactly one new probe root package; that root's sole local dependency is the exact accepted `swarm-governance-witness` path and version; every reachable non-root package name/version/source/checksum equals an entry in the accepted root lock; and no Git, network, or unapproved package appears. Freeze and hash that probe lock. With the same frozen probe lock, first run `cargo check --release --locked --offline --message-format=json` on a positive source that constructs only the normal `NatsWitnessStore`; it must exit zero. Change only the pinned constructor-call line to name the debug hook, rerun the identical locked command, and require nonzero exit plus the exact missing-item diagnostic code, hook symbol, source path, and pinned span. The selector writes a fresh `release_hook_absent` actual-ledger row containing the invocation token, positive/negative source SHA-256 values, generated-lock SHA-256, closure-validation result, `release` profile, both exit statuses, diagnostic code/symbol/span, and normal-constructor/hook-absence result; the row is consumed in the same selector invocation and cannot reuse a prior file. The checker self-test `jetstream-release-hook` mutates the source pair, lock omission/substitution/closure, unlocked resolution, token, profile, status, diagnostic, span, and ledger freshness; both-probes-pass and both-probes-fail controls must be killed. Structural mutations removing `cfg(debug_assertions)` from any hook type, field, constructor, control parser, or call site must also fail. Prove the normal constructor has no hook field or control behavior. The wrapper must prove `running=1`, `passed=1`, `failed=0`, `ignored=0` for every frozen test and reject NATS absence, timeout, `#[ignore]`, skip, zero tests, partial selector, cleanup error, or incomplete transcript. Freeze and stage the cumulative A+B+C tree; obtain independent P0/P1/P2 = 0/0/0 review before the final commit.</action>
  <acceptance_criteria>All five post-ack crash states reach the barrier once, return Ambiguous without retry/upgrade, and survive SIGKILL/restart on the identical pinned image and volume. Every restart, anchor, iterator, account, unavailable, and revision distinction ID executes exactly once; skipped, ignored, zero, partial, stale-image, stale-volume, stale-tree, or incomplete evidence fails.</acceptance_criteria>
  <verify>
    <automated>test "$(git rev-parse HEAD)" = cf0ad8b287a23fd1a4b57c922c8318b77c2cea81 && bash tools/check-phase285-witness-conformance.sh --self-test jetstream-release-hook && bash tools/with-nats-jetstream.sh --self-test && bash tools/with-nats-jetstream.sh bash tools/check-phase285-witness-conformance.sh jetstream-checkpoint && bash tools/with-nats-jetstream.sh bash tools/check-phase285-witness-conformance.sh jetstream-cas && bash tools/check-phase285-witness-conformance.sh transport-layering && bash tools/check-phase285-witness-conformance.sh in-memory-differential && bash tools/check-phase285-witness-conformance.sh typed-proxy && bash tools/check-witness-dependency-closure.sh --library-only && cargo test -p swarm-governance --locked --offline && bash tools/with-nats-jetstream.sh cargo test -p swarm-governance-witness --locked --offline && cargo clippy -p swarm-governance -p swarm-governance-witness --all-targets --all-features --locked --offline -- -D warnings && cargo fmt --all -- --check && shellcheck tools/check-negative-registry.sh tools/check-phase285-witness-conformance.sh tools/with-nats-jetstream.sh && actionlint .github/workflows/ci.yml && git diff --check</automated>
    Root stages exactly the cumulative eleven declared paths after this command, freezes `git write-tree`, verifies zero unstaged paths, accepted parent, and no added dependency or target, and sends only that immutable tree to the hostile reviewer. Only after a zero-finding verdict may root create the one final commit.
  </verify>
  <done>Task 285-03-03 closes the exact JetStream restart/non-skipping checkpoint. Plan 04 may add service binaries and runtime client only after the single final Plan 03B commit and both remote refs exist.</done>
</task>

</tasks>

<verification>
Require the three immutable internal stage reviews, then run the complete final command chain on one frozen tree. The final Plan 03B diff must contain only the eleven declared paths, use no new dependency or target, remain one atomic child of accepted Plan 03A, and receive independent P0/P1/P2 = 0/0/0 review before two refs are pushed. This is a local slice checkpoint; it makes no hosted, protected-check, external-App, combined-tree, or phase-advancement claim.
</verification>

<success_criteria>
Raw NATS 2.11.17 evidence is closed before normalization; semantic and raw digests bind the correct epoch/anchor fields; exact private CAS matches the accepted model and is leader-confirmed; five post-ack crash states return Ambiguous then survive same-volume restart; every selector and inner case is non-vacuous; and no generic/raw authority is exported.
</success_criteria>

<output>
After completion, create `.planning/phases/285-assurance-foundation-closure/285-03B-SUMMARY.md` in the planning-only ledger commit, not the production slice.
</output>
