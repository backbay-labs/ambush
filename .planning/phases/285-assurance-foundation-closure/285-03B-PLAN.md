---
phase: 285-assurance-foundation-closure
plan: "03B"
type: execute
wave: 4
depends_on:
  - "285-03A"
files_modified:
  - docker-compose.yml
  - crates/swarm-governance-witness/src/lib.rs
  - crates/swarm-governance-witness/src/nats_config.rs
  - crates/swarm-governance-witness/src/raw_config.rs
  - crates/swarm-governance-witness/src/jetstream_store.rs
  - crates/swarm-governance-witness/tests/jetstream_cas.rs
  - crates/swarm-governance-witness/tests/jetstream_checkpoint.rs
  - crates/swarm-governance/tests/phase285_witness_conformance.rs
  - tools/with-nats-jetstream.sh
autonomous: true
requirements:
  - ASSURE-04
  - ASSURE-06
must_haves:
  truths:
    - "The 2.11.17 raw stream create/info projection is closed, canonical, deny-unknown-fields, and anchored by an exact raw-config digest; every false/zero/empty/absent field and reserved metadata entry is checked."
    - "InspectReady proves the complete leader snapshot and fixed subject set, while JetStream CAS confirms exact raw sequence, bytes, and headers after acknowledgement."
    - "Restart, lost-response, anchor, and non-skipping harness mutations fail closed; NATS unavailability, skipped/ignored tests, zero execution, and partial selectors cannot pass."
  artifacts:
    - path: "crates/swarm-governance-witness/src/raw_config.rs"
      provides: "Nats21117RawConfigV1 deny-unknown-fields DTO and exact semantic projection/golden fixture validator."
      contains: "deny_unknown_fields"
    - path: "crates/swarm-governance-witness/src/jetstream_store.rs"
      provides: "Leader-mediated NatsWitnessStore CAS and complete InspectReady validation."
      contains: "get_last_raw_message_by_subject"
    - path: "tools/with-nats-jetstream.sh"
      provides: "Pinned 2.11.17 non-skipping restart harness."
      contains: "e4bf19f15fd3218814a4e3c9e0064e1334bd8aa20d5984b9f1a0afd084f8cc00"
  key_links:
    - from: "Nats21117RawConfigV1"
      to: "WitnessBucketConfigurationV1.bucket_configuration_digest"
      via: "canonical raw projection and domain-separated digest are compared to epoch/anchor before service"
      pattern: "raw_stream_configuration_digest.*bucket_configuration_digest"
    - from: "NatsWitnessStore::inspect_ready"
      to: "Stream::info_with_subjects and leader raw snapshot"
      via: "counts, per-item cardinality, duplicate/short iterator, overflow, and final snapshot equality"
      pattern: "subjects_count.*messages.*first_sequence.*last_sequence.*created"
    - from: "NatsWitnessStore::compare_and_swap"
      to: "Stream::get_last_raw_message_by_subject"
      via: "acknowledged publish is read-confirmed with exact subject sequence, value bytes, digest, and headers"
      pattern: "publish_put_cas.*get_last_raw_message_by_subject"
---

<objective>
Implement the pinned NATS 2.11.17 JetStream repository and non-skipping restart harness.

Purpose: This is the durable implementation of the Plan 02 revision-CAS contract. Raw server configuration must be projected exactly, and an acknowledgement is not authority until the leader snapshot and raw message are independently confirmed.
Output: Closed raw-config DTO/projection, exact-header CAS/InspectReady repository, pinned Compose harness, and restart/header/acknowledgement mutation lanes.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/285-assurance-foundation-closure/285-CONTEXT.md
@.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md
@.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md
@.planning/phases/285-assurance-foundation-closure/285-VERIFICATION.md
@.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md
@<git show 5be011a07690a63a297d5bba8fbf740bb659c19d:.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md>
@crates/swarm-governance-witness/Cargo.toml
@crates/swarm-governance-witness/src/lib.rs
@crates/swarm-governance/src/witness_engine/store.rs
@crates/swarm-governance/src/witness_engine.rs
@docker-compose.yml
@tools/with-nats-jetstream.sh

The exact image is `docker.io/library/nats:2.11.17-alpine@sha256:e4bf19f15fd3218814a4e3c9e0064e1334bd8aa20d5984b9f1a0afd084f8cc00`. No generic normalizer, direct KV authority, raw management API, floating tag, or async persistence projection is acceptable.
</context>

<tasks>

<task type="auto">
  <wave>4</wave>
  <name>Task 285-03-02: Implement exact 2.11.17 raw projection and leader-mediated CAS</name>
  <files>docker-compose.yml, crates/swarm-governance-witness/src/lib.rs, crates/swarm-governance-witness/src/nats_config.rs, crates/swarm-governance-witness/src/raw_config.rs, crates/swarm-governance-witness/src/jetstream_store.rs, crates/swarm-governance-witness/tests/jetstream_cas.rs</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md (exact raw projection, InspectReady, headers, bucket settings); .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance-witness/Cargo.toml; crates/swarm-governance-witness/src/lib.rs; crates/swarm-governance/src/witness_engine/store.rs; crates/swarm-governance/src/witness_engine.rs; docker-compose.yml</read_first>
  <action>Red-first: add `jetstream_cas_rejects_raw_config_unknown_field_or_persist_mode`, `jetstream_cas_rejects_each_raw_config_mutation`, `jetstream_cas_rejects_wrong_revision_header_or_ack`, `jetstream_cas_confirms_raw_sequence_and_bytes`, and `jetstream_cas_rejects_del_purge_rollup_and_direct_reads` before the repository. Implement `Nats21117RawConfigV1` with `#[serde(deny_unknown_fields)]` and allow exactly `name`, `description`, `subjects`, `retention`, `max_consumers`, `max_msgs`, `max_bytes`, `max_age`, `max_msgs_per_subject`, `max_msg_size`, `discard`, `storage`, `num_replicas`, `duplicate_window`, `compression`, `allow_direct`, `mirror_direct`, `sealed`, `deny_delete`, `deny_purge`, `allow_rollup_hdrs`, `consumer_limits`, `allow_msg_ttl`, and `metadata`; reject every other key. Project exactly the contract's false/zero/empty/absent values: `Limits`, `New`, `false` discard-new-per-subject, `File`, `max_messages=-1`, `max_age_nanos=0`, `max_consumers=-1`, `no_ack=false`, duplicate window 120_000_000_000, `compression: none -> Disabled`, all mirror/TTL/atomic/schedule/counter/placement/republish/source/transform/consumer-limit flags false/empty/None, `persist_mode_wire_key_present=false`, and exactly three `_nats.level=1`, `_nats.req.level=0`, `_nats.ver=2.11.17` server metadata entries. Reject `persist_mode` including `async`, any changed false/zero/empty/absent projection, unknown field, changed reserved metadata, wrong image/version, or noncanonical raw-config digest. Store the golden 2.11.17 create/info fixture and generate per-field mutations in a temporary directory; no checked-in fixture tree is required. Anchor the canonical raw-config bytes and domain-separated digest in `WitnessBucketAnchorV1`. Implement `NatsWitnessStore` using only leader-mediated `Stream::get_last_raw_message_by_subject`; `InspectReady` must bound `subjects_count` by manifest plus maximum admitted streams, count every yielded item exactly once, reject duplicates, short iterators, cumulative overflow, extra/wildcard subjects, pagination/cross-page mutations, and initial/final mismatch, then require final leader snapshot equality for subject count, message count, first/last sequences, creation time, and raw-config digest. `CompareAndSwap` must derive the exact `$KV.<bucket>.s.<admitted-digest>` subject, set only `KV-Operation: PUT`, `Nats-Expected-Stream`, and `Nats-Expected-Last-Subject-Sequence`, require nonzero expected revision, and read-confirm exact raw sequence, value bytes, digest, expected header, and no DEL/PURGE/rollup/TTL/unknown/duplicate header before `CasApplied`. Pin Compose to the exact image/version and reject direct reads, Store::create/put/delete/purge, wildcard/management subjects, and last-write-wins.</action>
  <acceptance_criteria>The raw DTO is closed and golden-projection mutations all fail; exactly three reserved metadata entries and every absent/false/zero value are represented; raw-config digest is anchor-bound; InspectReady validates all snapshot/cardinality/iterator/page/final-equality rules; CAS succeeds only after exact raw confirmation; exact NATS image/version/configuration is enforced.</acceptance_criteria>
  <verify>
    <automated>bash tools/with-nats-jetstream.sh bash tools/check-phase285-witness-conformance.sh jetstream-cas</automated>
    The harness must execute every raw-config, InspectReady, header, acknowledgement, and direct-read mutation > 0 with passed = executed, failed = 0, ignored = 0; unavailable NATS, wrong image, skipped/ignored tests, zero execution, or omitted mutation must fail.
  </verify>
  <done>Task 285-03-02 implements exact pinned raw projection, InspectReady, and durable CAS without exposing raw storage authority to public code.</done>
</task>

<task type="auto">
  <wave>4</wave>
  <name>Task 285-03-03: Add restart, anchor, and non-skipping JetStream controls</name>
  <files>crates/swarm-governance-witness/tests/jetstream_checkpoint.rs, crates/swarm-governance/tests/phase285_witness_conformance.rs, tools/with-nats-jetstream.sh, docker-compose.yml</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md (restart, anchor, CAS, and non-skipping requirements); .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance-witness/src/raw_config.rs; crates/swarm-governance-witness/src/jetstream_store.rs; crates/swarm-governance-witness/tests/jetstream_cas.rs; crates/swarm-governance/tests/phase285_witness_conformance.rs; tools/with-nats-jetstream.sh; docker-compose.yml</read_first>
  <action>Red-first: add `jetstream_checkpoint_survives_restart_for_current_predecessor_prepared_abort_and_genesis`, `jetstream_checkpoint_rejects_rolled_back_anchor_or_recreated_stream`, `jetstream_checkpoint_rejects_unavailable_server_instead_of_skipping`, and `jetstream_checkpoint_uses_global_revision_not_store_generation` before orchestration. Extend `with-nats-jetstream.sh` to resolve the exact image index digest and reported 2.11.17, wait for a healthy leader, preserve its data volume across controlled stop/start, and fail on unavailable service, wrong account, unhealthy readiness, timeout, skip, `#[ignore]`, zero tests, nonzero ignored count, or partial selector; witness selectors may not use the ordinary pheromone skip-on-unavailable path. Kill NATS immediately after acknowledged CAS and before response delivery for current, predecessor, prepared, abort, and genesis states; restart and require byte-identical envelopes, exact raw sequence/headers, external epoch/anchor, manifest initialization digest, and monotonic store_generation. Add recreated-stream/stale-anchor, wrong expected-subject-sequence, subject-count/cross-page, and global-sequence-versus-store-generation mutations. Register exact test names for `jetstream-checkpoint`; do not use an undefined aggregate selector.</action>
  <acceptance_criteria>Every declared restart, lost-response, anchor, InspectReady, and revision distinction case executes without skip behavior; restart preserves only the exact pinned anchored state; stale/recreated/header/cross-page mutations fail; global revision remains distinct from store_generation.</acceptance_criteria>
  <verify>
    <automated>bash tools/with-nats-jetstream.sh bash tools/check-phase285-witness-conformance.sh jetstream-checkpoint</automated>
    The checker must show positive execution for every restart and mutation case with passed = executed, failed = 0, ignored = 0, and reject unavailable, skipped, ignored, zero, omitted, stale-image, or stale-tree fixtures.
  </verify>
  <done>Task 285-03-03 closes the JetStream restart/non-skipping checkpoint; Plan 04 may now add service binaries and the runtime client.</done>
</task>

</tasks>

<verification>
Run both frozen Slice C selectors under the pinned harness and confirm all raw projection, InspectReady, CAS, restart, anchor, and non-skipping mutations have positive execution counts. This is a slice checkpoint only; no hosted or final closure claim is permitted.
</verification>

<success_criteria>
JetStream-CAS and JetStream-checkpoint are non-vacuously green; raw 2.11.17 projection and digest anchoring are exact; InspectReady and CAS are leader-confirmed; and unavailable/ignored/zero/partial service evidence fails closed.
</success_criteria>

<output>
After completion, create `.planning/phases/285-assurance-foundation-closure/285-03B-SUMMARY.md`.
</output>
