---
phase: 285-assurance-foundation-closure
plan: "05B"
type: execute
wave: 7
depends_on:
  - "285-05A"
files_modified:
  - crates/swarm-governance/src/lib.rs
  - crates/swarm-governance/src/persistence_coordinator.rs
  - crates/swarm-governance/src/persistence_recovery.rs
  - crates/swarm-governance/tests/phase285_governance_persistence.rs
  - tools/check-phase285-governance-persistence.sh
autonomous: true
requirements:
  - ASSURE-04
  - ASSURE-06
must_haves:
  truths:
    - "Recovery accepts only the reviewed journal phase vocabulary and has an explicit crash/restart test at Intent, WitnessPrepared, PayloadsStaged, StateExchanged, CheckpointExchanged, ReadyForWitnessCommit, AbortPending, Committed, and Aborted; Uncertain is a typed refusal/outcome, never an inferred phase."
    - "Every lost prepare/commit/abort response is resolved by authenticated discovery and exact transaction/outcome fields; recovery never chooses the highest local sequence or a bare status."
    - "External escrow uses the accepted session-bound four-argument `fetch_payload` API to reconstruct verified state/checkpoint lanes; missing, replaced, or corrupt local bytes keep ordinary startup Uncertain until an explicit verified repair completes, and foreign bytes are preserved."
    - "Bootstrap recovery is separately exercised before external prepare, before the first local journal, after every local bootstrap durability barrier, after external witness commit, and after local Committed; incomplete bootstrap can never be mistaken for an ordinary initialized stream."
  artifacts:
    - path: "crates/swarm-governance/src/persistence_recovery.rs"
      provides: "Private recovery state-machine helpers owned and invoked only by GovernancePersistenceCoordinator, with exact journal phases, alternating signed lanes, discovery resolution, and escrow reconstruction."
      contains: "fetch_payload"
    - path: "crates/swarm-governance/src/persistence_coordinator.rs"
      provides: "GovernancePersistenceCoordinator integration that prepares externally before local exchange and commits only after final revalidation."
      contains: "GovernancePersistenceCoordinator"
    - path: "crates/swarm-governance/tests/phase285_governance_persistence.rs"
      provides: "Crash/restart, abort outcome, discovery, highest-sequence, and escrow mutation target."
      contains: "transaction_recovery_crash_restart_at_intent"
    - path: "tools/check-phase285-governance-persistence.sh"
      provides: "Serial extension of the exact transaction-recovery registry with the eight bootstrap crash controls."
      contains: "transaction_recovery_bootstrap_crash_before_local_journal_discovers_unique_prepared"
  key_links:
    - from: "GovernancePersistenceCoordinator::recover"
      to: "TransactionPhaseV1"
      via: "exact accepted phases and checked transitions from persistence_protocol.rs"
      pattern: "Intent.*WitnessPrepared.*PayloadsStaged.*StateExchanged.*CheckpointExchanged.*ReadyForWitnessCommit.*AbortPending.*Committed.*Aborted"
    - from: "GovernancePersistenceCoordinator::recover"
      to: "GovernanceDurabilityWitness::prepare_successor"
      via: "external prepare precedes local journal/staging/exchange and commit follows final seam validation"
      pattern: "prepare_successor.*StateExchanged.*CheckpointExchanged.*commit_prepared"
    - from: "GovernancePersistenceCoordinator::recover"
      to: "GovernanceDurabilityWitness::fetch_payload"
      via: "verified escrow bytes are canonical-decoded, signature/digest checked, and reconstructed only into authenticated within-role lanes"
      pattern: "fetch_payload.*Uncertain.*digest"
    - from: "AbortPending"
      to: "WitnessIntentOutcomeV1"
      via: "only exact signed commit/abort discovery outcome can produce a terminal local record"
      pattern: "AbortPending.*last_intent_outcome.*Committed.*Aborted"
    - from: "GovernancePersistenceCoordinator::bootstrap"
      to: "tools/check-phase285-governance-persistence.sh transaction-recovery"
      via: "eight exact crash controls cover absent head, incomplete namespace, unique prepared discovery, every local barrier, witness commit, escrow reconstruction, and final revalidation"
      pattern: "bootstrap_crash_before_external_prepare.*bootstrap_crash_after_local_commit"
---

<objective>
Implement witness-backed transaction recovery and crash semantics on the fixed-lane foundation.

Purpose: Local journal state is not authority after a crash. The accepted protocol requires external prepare/commit/abort linearization, exact recovery phases, and immutable escrow reconstruction with fail-closed uncertainty.
Output: Private recovery functions owned by `GovernancePersistenceCoordinator`, an exact phase/crash/restart matrix, signed alternating journal resolution, and verified session-bound `fetch_payload` repair/refusal behavior.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/285-assurance-foundation-closure/285-CONTEXT.md
@.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md
@.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md
@.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md
@<.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md>
@crates/swarm-governance/src/persistence_protocol.rs
@crates/swarm-governance/src/fixed_lanes.rs
@crates/swarm-governance/src/persistence_coordinator.rs
@crates/swarm-governance/src/lib.rs
@crates/swarm-governance/tests/phase285_governance_persistence.rs

Use the exact `TransactionPhaseV1` enum and `allows_transition_to` implementation from `persistence_protocol.rs` around the accepted phase definition; do not invent a `CheckpointWritten` or `LocalCommitted` substitute. `Uncertain` is the typed recovery outcome described by the protocol, not an additional accepted `TransactionPhaseV1` variant. `GovernancePersistenceCoordinator` remains the only coordinator symbol.
</context>

<tasks>

<task type="auto">
  <wave>7</wave>
  <name>Task 285-05-02: Implement exact transaction recovery, crash matrix, and escrow reconstruction</name>
  <files>crates/swarm-governance/src/lib.rs, crates/swarm-governance/src/persistence_coordinator.rs, crates/swarm-governance/src/persistence_recovery.rs, crates/swarm-governance/tests/phase285_governance_persistence.rs</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md; .planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance/src/persistence_protocol.rs (GovernanceDurabilityWitness, TransactionPhaseV1, TransactionRecordV1, validate_recovery_pair, select_recovery_record); crates/swarm-governance/src/fixed_lanes.rs; crates/swarm-governance/src/persistence_coordinator.rs; crates/swarm-governance/src/witness_engine.rs</read_first>
<action>Work red-first by adding exact tests `transaction_recovery_crash_restart_at_intent`, `transaction_recovery_crash_restart_at_witness_prepared`, `transaction_recovery_crash_restart_at_payloads_staged`, `transaction_recovery_crash_restart_at_state_exchanged`, `transaction_recovery_crash_restart_at_checkpoint_exchanged`, `transaction_recovery_crash_restart_at_ready_for_witness_commit`, `transaction_recovery_crash_restart_at_abort_pending`, `transaction_recovery_crash_restart_at_committed`, `transaction_recovery_crash_restart_at_aborted`, `transaction_recovery_uncertain_state_survives_restart`, `transaction_recovery_abort_pending_requires_exact_signed_outcome`, `transaction_recovery_lost_prepare_commit_abort_uses_discovery`, `transaction_recovery_fetches_payload_and_reconstructs_verified_lanes`, `transaction_recovery_missing_replaced_or_corrupt_local_bytes_is_uncertain`, and `transaction_recovery_never_selects_highest_sequence` before implementation. Keep `GovernancePersistenceCoordinator` as the sole coordinator type. Implement recovery as private functions in `persistence_recovery.rs` invoked exclusively by its `recover`/publication methods over the exact accepted phase sequence `Intent -> WitnessPrepared -> PayloadsStaged -> StateExchanged -> CheckpointExchanged -> ReadyForWitnessCommit -> Committed`, with the alternate terminal route `Intent|WitnessPrepared|PayloadsStaged|StateExchanged|CheckpointExchanged|ReadyForWitnessCommit -> AbortPending -> Aborted`. Use `TransactionRecordV1::resolve_verified_prepare` with an exact verified witness outcome for `Intent -> WitnessPrepared`; the generic `TransactionRecordV1::transition` is used only for transitions it permits. Use the existing checked `begin_abort`, `validate_recovery_pair`, and `select_recovery_record`, and represent any ambiguity as typed `Uncertain` without widening the phase enum. Journal records are self-contained, signed, predecessor-linked, and alternate between the two authenticated journal lanes; every test simulates a crash and restart at each listed phase, validates the prior/next mapping and journal generation, and asserts no torn or foreign bytes are overwritten. In `GovernancePersistenceCoordinator`, construct a canonical `WitnessServiceRequestV1` through `WitnessServiceRequestDraftV1::new` with a fresh request nonce, the exact admission digest, and `WitnessServiceRequestBodyV1::Prepare { session, expected_head, candidate }`, finalize it with the opaque verified session capability, and call the accepted one-argument `GovernanceDurabilityWitness::prepare_successor(request)` before any local staging/exchange, validate its signed outcome, and pass that outcome to `resolve_verified_prepare` before persisting and fsyncing `WitnessPrepared`; stage and verify both payloads, exchange state then checkpoint only within their authenticated inode pairs with immediate pre/post checks, persist and fsync `ReadyForWitnessCommit`, revalidate complete namespace/authority-pair/binding/journal/bytes, then construct and finalize the corresponding immutable `WitnessServiceRequestV1` Commit envelope and call `commit_prepared(request)`. Every witness mutation/read uses the accepted one-argument request-envelope API; an exact transport retry reuses the already-finalized canonical request bytes and never regenerates its nonce, digest, authorization, session, or body. A lost response is never success and is resolved only by signed `discover_stream` plus a newly verified session fence. Before any external abort, persist/fsync `AbortPending` containing txid, candidate digest, predecessor head digest, reserved intent, unchanged data-head digest, complete local mappings, and journal provenance. Write `Aborted` only when direct `abort_prepared` or session-independent discovery supplies a signed `last_intent_outcome` whose outcome, txid, candidate digest, predecessor, reserved counter, and resulting unchanged data-head identity match every `AbortPending` field. If commit wins, abort resolves to authenticated `Committed`; if abort wins, commit resolves to authenticated `Aborted`; stale outcomes become `Uncertain`. Never use highest local sequence, transaction-ID knowledge, a bare status, or a local session as authority. For escrow repair, on a witness-committed/local-missing, replaced, truncated, unsigned, or digest-mismatched lane construct a canonical `WitnessServiceRequestV1` through `WitnessServiceRequestDraftV1::new` with a fresh nonce, the exact admission digest, and `WitnessServiceRequestBodyV1::FetchPayload { session, txid }`, finalize it with the same opaque verified session capability, and call the accepted one-argument `GovernanceDurabilityWitness::fetch_payload(request)` API. Verify the signed `WitnessReadAttestationV1`, canonical bytes, signer, stream/binding/authority namespace, payload lengths, SHA-256 digests, candidate/txid/head derivations, and predecessor relation, then reconstruct only through held authenticated fixed lanes after no-replace archival of any conflicting local entry. Detection of missing/replaced/corrupt local bytes keeps ordinary startup in typed `Uncertain`; only the explicit repair flow may clear it, and only after escrow verification, lane reconstruction, file/directory fsync, exact reread, complete namespace/mapping revalidation, and signed external-head equality all succeed. Failed archive, uncertain mapping, failed fsync, or mismatched escrow response remains `Uncertain` and leaves foreign bytes untouched; recovery never silently uses a local copy or selects a newer-looking sequence. Test crashes before/after prepare, after each exchange, before/after commit/abort calls, after lost responses, and during escrow reconstruction, including session rotation and witness restart. Do not introduce another coordinator type, a new filesystem name, local fallback witness, or error-string protocol state.</action>
  <verify>
    <automated>bash tools/check-phase285-governance-persistence.sh transaction-recovery</automated>
    The wrapper must execute every exact phase/restart, abort-binding, discovery, escrow, uncertainty, and highest-sequence case with `passed = executed`, `failed = 0`, and `ignored = 0`; a deliberately replaced/missing/corrupt local byte must produce `Uncertain` and preserve the original foreign bytes.
  </verify>
  <done>Recovery uses only the accepted phase vocabulary, exercises crash/restart at every phase, prepares and commits through the actual witness trait, resolves terminal outcomes by exact signed evidence, reconstructs only verified external escrow, and fails closed on missing/replaced/corrupt local bytes or ambiguous mappings.</done>
</task>

<task type="auto">
  <wave>7</wave>
  <name>Task 285-05-03: Implement and register the bootstrap crash/recovery matrix</name>
  <files>crates/swarm-governance/src/lib.rs, crates/swarm-governance/src/persistence_coordinator.rs, crates/swarm-governance/src/persistence_recovery.rs, crates/swarm-governance/tests/phase285_governance_persistence.rs, tools/check-phase285-governance-persistence.sh</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md (Bootstrap state machine, crash and recovery rules, mandatory adversarial verification); .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance/src/persistence_protocol.rs; crates/swarm-governance/src/persistence_coordinator.rs; crates/swarm-governance/src/persistence_recovery.rs; crates/swarm-governance/tests/phase285_governance_persistence.rs; tools/check-phase285-governance-persistence.sh</read_first>
  <action>Work red-first with the eight exact tests `transaction_recovery_bootstrap_crash_before_namespace_creation_preserves_absence`, `transaction_recovery_bootstrap_crash_after_namespace_before_external_prepare_preserves_incomplete_bootstrap`, `transaction_recovery_bootstrap_crash_before_local_journal_discovers_unique_prepared`, `transaction_recovery_bootstrap_crash_after_local_witness_prepared_resumes`, `transaction_recovery_bootstrap_crash_after_payload_fsync_resumes`, `transaction_recovery_bootstrap_crash_after_ready_before_witness_commit_resumes`, `transaction_recovery_bootstrap_crash_after_witness_commit_reconstructs_from_escrow`, and `transaction_recovery_bootstrap_crash_after_local_commit_revalidates`. Extend the existing `transaction-recovery` selector with those eight literal names and execute each individually with `--exact`; missing, zero, ignored, substring-only, or omitted bootstrap rows fail the registry self-test. Implement bootstrap only through private functions owned by `GovernancePersistenceCoordinator`: require the external head and fixed namespace absent or a recognized incomplete-bootstrap state; create the entire fixed namespace with descriptor-relative no-replace operations; sign the complete epoch-0/sequence-0 binding, state, and checkpoint; prepare the absent-head candidate externally; fsync `WitnessPrepared`; populate/reread/fsync every fixed canonical and staging entry and its directories; fsync `ReadyForWitnessCommit`; revalidate the full namespace; commit externally; resolve a lost response only by signed discovery; then fsync local `Committed` and revalidate before return. A crash before namespace creation preserves complete absence. After the first no-replace namespace entry is created, every crash before external prepare preserves and recognizes the exact incomplete-bootstrap namespace; it never deletes those entries or reports complete absence, ordinary initialization, or a usable stream. A crash before the first local journal after external prepare must discover the witness's unique prepared record and deterministic txid. A crash after external commit must reconstruct only from immutable verified escrow using the exact session-bound fetch API. Every non-absence crash state resumes only from exact signed phase, mapping, binding, and witness evidence. An incomplete bootstrap never enters ordinary publication or returns initialized, and foreign/torn/replaced bytes are preserved with typed `Uncertain`.</action>
  <verify>
    <automated>bash tools/check-phase285-governance-persistence.sh transaction-recovery</automated>
    The selector must execute all eight bootstrap names in addition to the ordinary transaction matrix with `passed = executed`, `failed = 0`, and `ignored = 0`; controls must distinguish pre-namespace absence, post-namespace/pre-prepare incomplete bootstrap, prepared-without-local-journal discovery, post-witness-commit escrow reconstruction, and final local revalidation.
  </verify>
  <done>Bootstrap has a non-vacuous crash/restart proof at every authority and durability boundary, and its registry cannot omit those controls while reporting transaction recovery green.</done>
</task>

</tasks>

<verification>
Run the full `transaction-recovery` selector, `cargo test -p swarm-governance --lib --locked --offline`, strict governance clippy, formatting, and `git diff --check`. Review the per-phase crash output and prove no test or recovery branch selects a record by highest sequence. Obtain an independent zero-P0/P1/P2 review before Plan05C; this is not Phase 285 completion evidence.
</verification>

<success_criteria>
All nine accepted phases plus `AbortPending` terminal behavior and typed `Uncertain` outcomes have non-vacuous crash/restart evidence; external `fetch_payload` reconstruction verifies canonical bytes/digests/signatures and preserves foreign local bytes on uncertainty; commit/abort races and lost responses resolve through signed discovery; and no local sequence or fallback path can authorize recovery.
</success_criteria>

<output>
After completion, create `.planning/phases/285-assurance-foundation-closure/285-05B-SUMMARY.md`.
</output>
