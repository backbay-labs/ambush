---
phase: 285-assurance-foundation-closure
plan: "02"
type: execute
wave: 2
depends_on:
  - "285-01"
files_modified:
  - crates/swarm-governance/src/witness_engine.rs
  - crates/swarm-governance/src/witness_engine/store.rs
  - crates/swarm-governance/src/witness_engine/store/in_memory.rs
  - crates/swarm-governance/src/witness_engine/store/proxy.rs
  - crates/swarm-governance/src/witness_engine/store/tests.rs
  - crates/swarm-governance/tests/phase285_witness_conformance.rs
autonomous: true
requirements:
  - ASSURE-04
  - ASSURE-06
must_haves:
  truths:
    - "A witness store mutation is revision-CAS and cannot succeed from a zero revision, an arbitrary subject, an unvalidated envelope, or a last-write-wins retry."
    - "The deterministic in-memory implementation and its reference transition model accept and reject the same operations with byte-identical state after every refusal, fault, and ambiguity."
    - "The typed proxy exposes only InspectReady, ReadEntry, and CompareAndSwap DTOs; signature, body, stream, epoch, anchor, header, and revision mutations fail before a store call."
  artifacts:
    - path: "crates/swarm-governance/src/witness_engine/store.rs"
      provides: "WitnessAtomicStore, WitnessStoreProxyRequestV1/ResponseV1, closed store failures, admission and manifest contracts, and revision-CAS transition boundary."
      contains: "WitnessAtomicStore"
    - path: "crates/swarm-governance/src/witness_engine/store/in_memory.rs"
      provides: "Deterministic InMemoryWitnessStore, ReferenceWitnessStoreModel, and explicit fault injection."
      contains: "InMemoryWitnessStore"
    - path: "crates/swarm-governance/src/witness_engine/store/proxy.rs"
      provides: "Typed WitnessStoreProxy validating canonical request/response and delegating only admitted CAS operations."
      contains: "WitnessStoreProxy"
    - path: "crates/swarm-governance/src/witness_engine/store/tests.rs"
      provides: "Differential, contention, ambiguity, capacity, and mutation coverage consumed by exact selectors."
      contains: "atomic_store_contract"
  key_links:
    - from: "WitnessAtomicStore::compare_and_swap"
      to: "WitnessStoreEnvelopeV1 and WitnessStoreExpectationV1"
      via: "validate_store_transition runs before any state replacement and advances store_generation exactly once"
      pattern: "compare_and_swap.*validate_store_transition.*store_generation"
    - from: "InMemoryWitnessStore"
      to: "ReferenceWitnessStoreModel"
      via: "each operation compares result variant, revision, digest, and canonical bytes"
      pattern: "ReferenceWitnessStoreModel.*canonical.*bytes"
    - from: "WitnessStoreProxy"
      to: "WitnessStoreProxyRequestV1/ResponseV1"
      via: "typed body and pinned domain signature validation excludes raw subjects and headers"
      pattern: "WitnessStoreProxy(Request|Response)V1.*signature.*raw"
---

<objective>
Implement Slice B: the revision-CAS store contract, deterministic in-memory/reference conformance, and typed proxy boundary.

Purpose: A valid protocol candidate is not durable until one atomic store transition has been independently validated and confirmed. This slice makes that transition testable without JetStream and freezes the exact DTO boundary consumed by the downstream transport package.
Output: Public store contracts under the existing witness-engine module, in-memory and reference implementations with fault injection, typed proxy DTO validation, and non-vacuous conformance lanes.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/285-assurance-foundation-closure/285-CONTEXT.md
@.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md
@.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md
@.planning/phases/285-assurance-foundation-closure/285-VERIFICATION.md
@.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md
@<git show 5be011a07690a63a297d5bba8fbf740bb659c19d:.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md>
@crates/swarm-governance/src/witness_engine.rs
@crates/swarm-governance/src/persistence_protocol.rs
@crates/swarm-governance/src/witness_service.rs
@crates/swarm-governance/src/witness_candidate_verifier.rs
@crates/swarm-governance/tests/phase285_witness_conformance.rs

`witness_engine.rs` remains the owner of the accepted envelope and one-step transition validator. Add a child `store` module there so this plan does not take ownership of `crates/swarm-governance/src/lib.rs`; Plan 05 is the only plan allowed to change that file. The proxy is a governance DTO boundary, not a JetStream client. Transport, raw credentials, wildcard subjects, and NATS headers belong to later plans.
</context>

<tasks>

<task type="auto">
  <wave>2</wave>
  <name>Task 285-02-01: Define the revision-CAS store and proxy contracts</name>
  <files>crates/swarm-governance/src/witness_engine.rs, crates/swarm-governance/src/witness_engine/store.rs, crates/swarm-governance/src/witness_engine/store/tests.rs, crates/swarm-governance/tests/phase285_witness_conformance.rs</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; .planning/phases/285-assurance-foundation-closure/285-RESEARCH.md; git show 5be011a07690a63a297d5bba8fbf740bb659c19d:.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md; crates/swarm-governance/src/witness_engine.rs; crates/swarm-governance/src/persistence_protocol.rs; crates/swarm-governance/src/witness_service.rs; crates/swarm-governance/src/witness_candidate_verifier.rs; crates/swarm-governance/tests/phase285_witness_conformance.rs</read_first>
  <action>Red-first: add `atomic_store_contract_rejects_zero_revision_and_unvalidated_transition`, `atomic_store_contract_confirms_revision_and_bytes`, and `atomic_store_contract_enforces_manifest_bounds` to the conformance target before implementing the contract. Declare `pub mod store;` in `witness_engine.rs` and create `store.rs` with `WitnessAtomicStore`, `WitnessStoreErrorV1`, `WitnessStoreEntryV1`, `WitnessStoreReadResultV1`, `WitnessStoreCasResultV1`, `WitnessBucketManifestV1`, `WitnessStreamInitializationV1`, `WitnessAdmissionSetV1`, and the exact `WitnessStoreProxyOperationV1::{InspectReady, ReadEntry, CompareAndSwap}`, `WitnessStoreProxyRequestV1`, `WitnessStoreProxyRequestBodyV1`, `WitnessStoreProxyResponseV1`, and `WitnessStoreProxyResponseBodyV1` variants from the witness adapter contract. Require canonical `#[serde(deny_unknown_fields)]` decoding, nonzero `CompareAndSwap.expected_revision`, exact admitted stream-to-key mapping, external bucket epoch/anchor and initialization digest equality, bounded byte counts, and one `store_generation` increment per accepted transition. Make the trait expose only typed inspect/read/CAS methods and return typed failures for missing, corrupt, wrong epoch/anchor, admission, signature, bounds, conflict, unavailable, and ambiguous results. Call `validate_store_transition` and the Plan 01 `WitnessCandidateVerifier` before replacing bytes; never accept caller-provided subjects, headers, `KV-Operation`, raw keys, or a revision-zero initializer in this online API. Keep all declarations under `witness_engine::store` so the accepted `lib.rs` is untouched.</action>
  <acceptance_criteria>The exact three store operations and all response body variants are closed enums; unknown fields, operation/body mismatches, zero expected revisions, stale store generations, wrong stream keys, wrong epoch/anchor, and over-limit values return typed failures before state replacement; a successful CAS reports the previous and new revision plus acknowledged value digest and increments store_generation exactly once.</acceptance_criteria>
  <verify>
    <automated>bash tools/check-phase285-witness-conformance.sh atomic-store-contract</automated>
    The checker must report every named atomic-store contract case executed > 0 with passed = executed, failed = 0, ignored = 0, and must reject a missing target, zero-match selector, omitted lane, or ignored test fixture.
  </verify>
  <done>Task 285-02-01 freezes the typed revision-CAS interface that later in-memory and JetStream implementations must implement; no raw storage capability or transport dependency exists.</done>
</task>

<task type="auto">
  <wave>2</wave>
  <name>Task 285-02-02: Implement deterministic in-memory and reference-model conformance</name>
  <files>crates/swarm-governance/src/witness_engine/store/in_memory.rs, crates/swarm-governance/src/witness_engine/store/tests.rs, crates/swarm-governance/tests/phase285_witness_conformance.rs</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance/src/witness_engine/store.rs; crates/swarm-governance/src/witness_engine/store/tests.rs; crates/swarm-governance/src/witness_engine.rs (WitnessStoreEnvelopeV1, WitnessStoreExpectationV1, validate_store_transition); crates/swarm-governance/src/persistence_protocol.rs (canonical bytes, bounds, digest, and transaction types); crates/swarm-governance/src/witness_candidate_verifier.rs; crates/swarm-governance/tests/phase285_witness_conformance.rs</read_first>
  <action>Red-first: add `in_memory_differential_matches_reference_for_every_operation`, `in_memory_store_preserves_bytes_after_refusal`, `in_memory_faults_return_ambiguous_without_guessing`, and `in_memory_capacity_exhaustion_is_pre_mutation` before adding storage behavior. Implement `InMemoryWitnessStore` with a deterministic `BTreeMap<StreamId, (revision, WitnessStoreEnvelopeV1)>`, immutable initialization manifest, explicit `store_generation`, and a `WitnessStoreFault` enum covering crash-before-CAS, lost-after-CAS, wrong-revision, duplicate-ack, corrupt-read, and capacity exhaustion. Implement `ReferenceWitnessStoreModel` using the same canonical DTOs but separate transition code; compare result discriminant, typed error code, revision, store_generation, digest, and canonical envelope bytes after every operation. Test genesis, prepare, commit, abort, session rotation, read, conflict, idempotent retry, content mutation, stale session, admission mismatch, payload/collection limits, and exhaustion. For every rejected or ambiguous operation assert the full preimage bytes and signature-bearing envelope digest are unchanged, and assert that an ambiguous CAS is resolved only by read-confirming revision/value rather than by assuming success.</action>
  <acceptance_criteria>The direct store and reference model produce byte-identical outcomes for all accepted and rejected cases; all fault cases are deterministic and typed; no refusal mutates revision, store_generation, manifest, or envelope bytes; CAS retries cannot turn a conflict or ambiguity into last-write-wins success.</acceptance_criteria>
  <verify>
    <automated>bash tools/check-phase285-witness-conformance.sh in-memory-differential</automated>
    The checker must prove the differential, refusal-immutability, ambiguity, and capacity cases executed with no ignored or skipped cases and fail if the target is absent or the selected lane runs zero tests.
  </verify>
  <done>Task 285-02-02 supplies a deterministic in-memory oracle and fault matrix that makes store semantics independently reviewable before JetStream is introduced.</done>
</task>

<task type="auto">
  <wave>2</wave>
  <name>Task 285-02-03: Add the typed proxy conformance boundary</name>
  <files>crates/swarm-governance/src/witness_engine/store/proxy.rs, crates/swarm-governance/src/witness_engine/store/tests.rs, crates/swarm-governance/tests/phase285_witness_conformance.rs</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance/src/witness_engine/store.rs; crates/swarm-governance/src/witness_engine/store/in_memory.rs; crates/swarm-governance/src/witness_engine/store/tests.rs; crates/swarm-governance/src/witness_service.rs; crates/swarm-governance/src/witness_candidate_verifier.rs; crates/swarm-governance/tests/phase285_witness_conformance.rs</read_first>
  <action>Red-first: add `typed_proxy_rejects_signature_body_header_and_revision_mutations`, `typed_proxy_delegates_only_after_canonical_validation`, and `typed_proxy_preserves_reference_outcomes` before implementing `WitnessStoreProxy`. Implement `WitnessStoreProxy<S: WitnessAtomicStore>` as a typed adapter that validates request schema, operation/body pairing, request digest, pinned witness key, bucket epoch/anchor, admission digest, fixed stream mapping, complete proposed envelope, and all limits before invoking `S`. Its `compare_and_swap` method must derive the admitted stream key and revision header internally, must never accept a caller subject/header/raw KV marker, and must confirm a successful backend result with an exact read of revision, canonical value bytes, value digest, and expected-subject-sequence metadata before returning `CasApplied`. Map backend conflict, absent, corrupt, wrong-header, timeout, and ambiguous outcomes to the closed `WitnessStoreProxyFailureCodeV1` body without converting errors to `String`. Run the same generated operation corpus through `InMemoryWitnessStore` and `WitnessStoreProxy`, mutating each request field, body variant, signature byte, epoch/anchor digest, stream ID, expected revision, envelope byte, and response header; assert no backend call occurs on precondition failure and direct/proxy result bytes are equal for accepted operations.</action>
  <acceptance_criteria>The proxy exposes only InspectReady, ReadEntry, and CompareAndSwap; every listed mutation is rejected before backend access; successful CAS requires exact post-write read confirmation; direct and proxy canonical responses and typed failures are byte-identical; no raw subject, header, wildcard, or revision-zero field crosses the public DTO boundary.</acceptance_criteria>
  <verify>
    <automated>bash tools/check-phase285-witness-conformance.sh typed-proxy</automated>
    The checker must report all typed-proxy mutation and differential cases executed > 0, passed = executed, failed = 0, ignored = 0, and reject partial selector output or any ignored/zero execution.
  </verify>
  <done>Task 285-02-03 closes the in-memory/proxy checkpoint. The downstream transport package can implement this exact interface without adding a dependency back into governance.</done>
</task>

</tasks>

<verification>
Run the three frozen Slice B selectors and inspect their execution-count reports. Confirm direct and proxy stores produce the same canonical outcomes, deliberate faults are not silently retried, and no files outside the plan-owned store module and conformance target were changed. Independent review must identify zero P0/P1/P2 findings for this slice before Slice C consumes the interface; this is not Phase 285 completion evidence.
</verification>

<success_criteria>
The atomic-store, in-memory-differential, and typed-proxy lanes are non-vacuously green; all refusal and ambiguity paths preserve store bytes; the only exported storage capability is the typed revision-CAS contract under `swarm_governance::witness_engine::store`; and no async-nats, JetStream, local persistence fallback, or transport cycle has been introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/285-assurance-foundation-closure/285-02-SUMMARY.md`.
</output>
