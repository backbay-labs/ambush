---
phase: 285-assurance-foundation-closure
plan: "02"
type: execute
wave: 2
depends_on:
  - "285-01"
files_modified:
  - crates/swarm-governance/src/witness_engine.rs
  - crates/swarm-governance/src/witness_engine/store.rs
  - crates/swarm-governance/src/witness_engine/store/in_memory.rs
  - crates/swarm-governance/src/witness_engine/store/proxy.rs
  - crates/swarm-governance/src/witness_engine/store/tests.rs
  - crates/swarm-governance/tests/phase285_witness_conformance.rs
  - tools/check-phase285-witness-conformance.sh
autonomous: true
requirements:
  - ASSURE-04
  - ASSURE-06
must_haves:
  truths:
    - "A witness store mutation is revision-CAS and cannot succeed from a zero revision, an arbitrary subject, an unvalidated envelope, or a last-write-wins retry."
    - "The deterministic in-memory implementation and an implementation-independent reference model agree on typed outcomes and resulting canonical state; refusals and pre-CAS faults are immutable, while a lost post-CAS response may advance exactly once and must be resolved by an exact read."
    - "The typed proxy exposes only InspectReady, ReadEntry, and CompareAndSwap DTOs; request-only mutations fail before a read, state-dependent mutations fail before CAS, and no raw subject, NATS header, operation marker, wildcard, or revision-zero initializer crosses the governance boundary."
    - "A Ready manifest contains every admitted stream key with a signed empty or later envelope; InspectReady rejects a missing admitted key and can never mint a VerifiedWitnessStoreStateV1 carrying an unproved None digest."
  artifacts:
    - path: "crates/swarm-governance/src/witness_engine/store.rs"
      provides: "WitnessAtomicStore, WitnessStoreProxyRequestV1/ResponseV1, closed store failures, admission and manifest contracts, and revision-CAS transition boundary."
      contains: "WitnessAtomicStore"
    - path: "crates/swarm-governance/src/witness_engine/store/in_memory.rs"
      provides: "Deterministic InMemoryWitnessStore, ReferenceWitnessStoreModel, and explicit fault injection."
      contains: "InMemoryWitnessStore"
    - path: "crates/swarm-governance/src/witness_engine/store/proxy.rs"
      provides: "Typed WitnessStoreProxy validating canonical request/response and delegating only admitted CAS operations."
      contains: "WitnessStoreProxy"
    - path: "crates/swarm-governance/src/witness_engine/store/tests.rs"
      provides: "Differential, contention, ambiguity, capacity, and mutation coverage consumed by exact selectors."
      contains: "atomic_store_contract"
    - path: "tools/check-phase285-witness-conformance.sh"
      provides: "Serial materialized-target inventory for the 13-, 17-, and 20-case Plan 02 checkpoints while preserving the frozen 58-row tuple registry."
      contains: "atomic-store-contract"
  key_links:
    - from: "WitnessAtomicStore::compare_and_swap"
      to: "WitnessStoreEnvelopeV1 and WitnessStoreExpectationV1"
      via: "validate_store_transition runs before any state replacement and advances store_generation exactly once"
      pattern: "compare_and_swap.*validate_store_transition.*store_generation"
    - from: "InMemoryWitnessStore"
      to: "ReferenceWitnessStoreModel"
      via: "each operation compares result variant, revision, digest, and canonical bytes"
      pattern: "ReferenceWitnessStoreModel.*canonical.*bytes"
    - from: "WitnessStoreProxy"
      to: "WitnessStoreProxyRequestV1/ResponseV1"
      via: "canonical request and pinned witness signature validation precede reads; authenticated current-state and transition validation precede CAS; response bodies remain unsigned private-transport DTOs"
      pattern: "validate_request.*read_entry.*validate_store_transition.*compare_and_swap"
---

<objective>
Implement Slice B: the revision-CAS store contract, deterministic in-memory/reference conformance, and typed proxy boundary.

Purpose: A valid protocol candidate is not durable until one atomic store transition has been independently validated and confirmed. This slice makes that transition testable without JetStream and freezes the exact DTO boundary consumed by the downstream transport package.
Output: Public store contracts under the existing witness-engine module, in-memory and reference implementations with fault injection, typed proxy DTO validation, and non-vacuous conformance lanes.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/285-assurance-foundation-closure/285-CONTEXT.md
@.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md
@.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md
@.planning/phases/285-assurance-foundation-closure/285-VERIFICATION.md
@.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md
@<git show 5be011a07690a63a297d5bba8fbf740bb659c19d:.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md>
@crates/swarm-governance/src/witness_engine.rs
@crates/swarm-governance/src/persistence_protocol.rs
@crates/swarm-governance/src/witness_service.rs
@crates/swarm-governance/src/witness_candidate_verifier.rs
@crates/swarm-governance/tests/phase285_witness_conformance.rs

`witness_engine.rs` remains the owner of the accepted envelope and one-step transition validator. Add a child `store` module there so this plan does not take ownership of `crates/swarm-governance/src/lib.rs`; Plan 05 is the only plan allowed to change that file. The proxy is a governance DTO boundary, not a JetStream client. Transport, raw credentials, wildcard subjects, and NATS headers belong to later plans.

The accepted Plan 01 checker freezes 58 selector/package/target/command/case tuples but materializes only ten Plan 01 tests. Plan 02 owns the same checker serially: Task 01 extends the materialized target inventory to 13 tests and enables only `atomic-store-contract`; Task 02 extends it to 17 and enables `in-memory-differential`; Task 03 extends it to 20 and enables `typed-proxy`. The expected filtered count is therefore 12, 16, and 19 respectively. The tuple digest remains unchanged because the Plan 02 tuples are already frozen; each task must keep unmaterialized later selectors explicitly red.

The generic store cannot call `WitnessCandidateVerifier::verify_prepare`: the exact CAS DTO deliberately has no candidate request, session authorization, request digest, or verified abort outcome. Candidate verification remains the public-service responsibility in Plan 04. Plan 02 proves the seam with a signed envelope produced from `prepare_verified_candidate`, then independently calls `validate_store_transition` against the authenticated current entry before mutation.

Ready initialization creates every admitted key with a signed empty envelope, and `InspectReady` requires exact equality with the admission-derived key set. A missing admitted key is corruption and an unconditional readiness refusal, never an authenticated absent-stream application outcome. Plan 02 must not add a `VerifiedWitnessStoreStateV1(None)` constructor; `from_present` remains the only admitted-request construction path.

Store-proxy requests are signed by the witness key. Store-proxy responses have no governance signature and rely on the authenticated private service import; they bind the exact request digest and are independently validated by the public witness. Governance code carries logical revision and confirmation metadata only. Actual `KV-Operation`, expected-sequence, stream, and acknowledgement header construction and mutation belong exclusively to Plan 03B.
</context>

<tasks>

<task type="auto">
  <wave>2</wave>
  <name>Task 285-02-01: Define the revision-CAS store and proxy contracts</name>
  <files>crates/swarm-governance/src/witness_engine.rs, crates/swarm-governance/src/witness_engine/store.rs, crates/swarm-governance/src/witness_engine/store/tests.rs, crates/swarm-governance/tests/phase285_witness_conformance.rs, tools/check-phase285-witness-conformance.sh</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; .planning/phases/285-assurance-foundation-closure/285-RESEARCH.md; git show 5be011a07690a63a297d5bba8fbf740bb659c19d:.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md; crates/swarm-governance/src/witness_engine.rs; crates/swarm-governance/src/persistence_protocol.rs; crates/swarm-governance/src/witness_service.rs; crates/swarm-governance/src/witness_candidate_verifier.rs; crates/swarm-governance/tests/phase285_witness_conformance.rs; tools/check-phase285-witness-conformance.sh</read_first>
  <action>Red-first: add `atomic_store_contract_rejects_zero_revision_and_unvalidated_transition`, `atomic_store_contract_confirms_revision_and_bytes`, and `atomic_store_contract_enforces_manifest_bounds` to the conformance target, extend the checker's exact materialized inventory from 10 to 13, require `filtered_out = 12`, and enable only `atomic-store-contract` before implementing the contract. Preserve the already frozen 58-tuple registry and its SHA-256. Declare `pub mod store;` in `witness_engine.rs` and create `store.rs` with the complete adapter-contract surface: `WitnessAdmissionSetV1`; `WitnessBucketManifestV1` and its phase enum; `WitnessStreamInitializationRecordV1`; `WitnessStreamInitializationV1`; `WitnessBucketConfigurationV1`; `WitnessRetentionPolicyV1`; `WitnessDiscardPolicyV1`; `WitnessStorageTypeV1`; `WitnessPersistenceSemanticsV1`; `WitnessCompressionV1`; `WitnessBucketEpochV1`; `WitnessBucketAnchorV1`; `WitnessStoreProxyValidatedEntryV1`; `WitnessStoreProxyFailureCodeV1`; the exact `WitnessStoreProxyOperationV1::{InspectReady, ReadEntry, CompareAndSwap}`; the exact request, request-body, response, and response-body DTOs; and the lower-level `WitnessStoreErrorV1`, `WitnessStoreReadyResultV1`, `WitnessStoreReadResultV1`, and `WitnessStoreCasResultV1`. Every wire type is versioned, canonical, bounded, and `#[serde(deny_unknown_fields)]`; exact declaration fields and closed variants come from the witness adapter contract rather than an abbreviated substitute. Define `#[async_trait] pub trait WitnessAtomicStore: Send + Sync` with exactly three logical methods: `inspect_ready() -> Result<WitnessStoreReadyResultV1, WitnessStoreErrorV1>`, `read_entry(stream_id: &str) -> Result<WitnessStoreReadResultV1, WitnessStoreErrorV1>`, and `compare_and_swap(stream_id: &str, expected_revision: u64, expected_store_state_digest: &str, proposed_envelope: &WitnessStoreEnvelopeV1) -> Result<WitnessStoreCasResultV1, WitnessStoreErrorV1>`. No method accepts a subject, raw key, header, KV operation marker, wildcard, or revision-zero initializer. The immutable Ready snapshot is supplied only through a fully validating constructor outside the online trait. Require exact admitted stream-to-key mapping, external epoch/anchor/manifest equality, complete admitted-key equality, signed empty-envelope initialization digests, bounded counts/bytes, and missing admitted-key refusal. Before replacement, reread the authenticated current entry, recompute its store-state digest, require exact revision/digest equality, validate both signed envelopes and `validate_store_transition`, and require one store-generation increment. Do not call `WitnessCandidateVerifier` in this layer; instead use a sealed output from `prepare_verified_candidate` in the positive Prepare fixture.</action>
  <acceptance_criteria>The exact three logical operations, all supporting wire types, and all response variants are closed and compile against an async downstream implementation; unknown fields, operation/body mismatches, zero expected revisions, missing admitted keys, stale state digests/generations, wrong mapping/epoch/anchor/manifest, and over-limit values fail before mutation; a successful CAS reports prior/new revision and acknowledged value digest and applies one already sealed one-step transition.</acceptance_criteria>
  <verify>
    <automated>bash tools/check-phase285-witness-conformance.sh atomic-store-contract</automated>
    The checker must report every named atomic-store contract case executed > 0 with passed = executed, failed = 0, ignored = 0, and must reject a missing target, zero-match selector, omitted lane, or ignored test fixture.
  </verify>
  <done>Task 285-02-01 freezes the typed revision-CAS interface that later in-memory and JetStream implementations must implement; no raw storage capability or transport dependency exists.</done>
</task>

<task type="auto">
  <wave>2</wave>
  <name>Task 285-02-02: Implement deterministic in-memory and reference-model conformance</name>
  <files>crates/swarm-governance/src/witness_engine/store/in_memory.rs, crates/swarm-governance/src/witness_engine/store/tests.rs, crates/swarm-governance/tests/phase285_witness_conformance.rs, tools/check-phase285-witness-conformance.sh</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance/src/witness_engine/store.rs; crates/swarm-governance/src/witness_engine/store/tests.rs; crates/swarm-governance/src/witness_engine.rs (WitnessStoreEnvelopeV1, WitnessStoreExpectationV1, validate_store_transition); crates/swarm-governance/src/persistence_protocol.rs (canonical bytes, bounds, digest, and transaction types); crates/swarm-governance/src/witness_candidate_verifier.rs; crates/swarm-governance/tests/phase285_witness_conformance.rs; tools/check-phase285-witness-conformance.sh</read_first>
  <action>Red-first: add `in_memory_differential_matches_reference_for_every_operation`, `in_memory_store_preserves_bytes_after_refusal`, `in_memory_faults_return_ambiguous_without_guessing`, and `in_memory_capacity_exhaustion_is_pre_mutation`, extend the materialized inventory to 17, require `filtered_out = 16`, and enable `in-memory-differential` before adding storage behavior. Implement `InMemoryWitnessStore` from a fully validated immutable Ready snapshot with a deterministic `BTreeMap<String, (revision, WitnessStoreEnvelopeV1)>`, bounded capacity, and `WitnessStoreFault::{CrashBeforeCas, LostAfterCas, WrongRevision, DuplicateAck, CorruptRead, CapacityExhaustion}`. Refusals, capacity exhaustion, and crash-before-CAS preserve revision, store generation, manifest, and canonical envelope bytes. Lost-after-CAS may apply exactly one valid transition and return `Ambiguous`; it must never claim immutability or trigger a blind retry. Implement `ReferenceWitnessStoreModel` over the same public DTO values but prohibit it from calling `WitnessAtomicStore`, `InMemoryWitnessStore`, `validate_store_transition`, or any shared mutation/admission helper. Its transition table independently checks namespace immutability, one store-generation step, and the four legal transition shapes. Run genesis, rotation, Prepare from a sealed verifier output, commit, abort, read, conflict, exact idempotent observation, content/signature mutation, stale session, admission mismatch, limits, capacity, pre-CAS crash, and post-CAS lost-response cases through both models. Compare normalized result variant, typed error, revision, generation, digests, and resulting canonical store bytes. Add a deliberate production-transition mutant in the differential case and require the independent model to disagree, proving the oracle is not circular. Resolve ambiguous outcomes only by an exact subsequent read of revision, canonical envelope bytes, digest, and expected prior revision metadata.</action>
  <acceptance_criteria>The direct store and independent model agree on normalized outcomes and resulting bytes; the oracle cannot call production transition helpers and kills a deliberate direct-engine mutant; refusals and pre-CAS faults are immutable; a post-CAS lost response may advance exactly once and remains ambiguous until exact read confirmation; no retry becomes last-write-wins.</acceptance_criteria>
  <verify>
    <automated>bash tools/check-phase285-witness-conformance.sh in-memory-differential</automated>
    The checker must prove the differential, refusal-immutability, ambiguity, and capacity cases executed with no ignored or skipped cases and fail if the target is absent or the selected lane runs zero tests.
  </verify>
  <done>Task 285-02-02 supplies a deterministic in-memory oracle and fault matrix that makes store semantics independently reviewable before JetStream is introduced.</done>
</task>

<task type="auto">
  <wave>2</wave>
  <name>Task 285-02-03: Add the typed proxy conformance boundary</name>
  <files>crates/swarm-governance/src/witness_engine/store/proxy.rs, crates/swarm-governance/src/witness_engine/store/tests.rs, crates/swarm-governance/tests/phase285_witness_conformance.rs, tools/check-phase285-witness-conformance.sh</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance/src/witness_engine/store.rs; crates/swarm-governance/src/witness_engine/store/in_memory.rs; crates/swarm-governance/src/witness_engine/store/tests.rs; crates/swarm-governance/src/witness_service.rs; crates/swarm-governance/src/witness_candidate_verifier.rs; crates/swarm-governance/tests/phase285_witness_conformance.rs; tools/check-phase285-witness-conformance.sh</read_first>
  <action>Red-first: add `typed_proxy_rejects_signature_body_header_and_revision_mutations`, `typed_proxy_delegates_only_after_canonical_validation`, and `typed_proxy_preserves_reference_outcomes`, extend the exact materialized inventory to 20, require `filtered_out = 19`, and enable `typed-proxy`. Implement `WitnessStoreProxy<S: WitnessAtomicStore>` with this fixed order: bound raw payload length; canonical decode; operation/body pairing; request-digest recomputation; pinned witness-key signature; request-only epoch, anchor, admission, fixed mapping, and limit checks; authenticated backend read; exact expected revision and recomputed current store-state digest; complete signed proposed-envelope and one-step transition validation; CAS; then exact confirmation read. Track read and CAS calls separately. Canonical, re-digested, validly re-signed semantic mutants must reach and fail their specific epoch/admission/mapping/revision/transition checks; only the signature mutant remains invalidly signed. Unknown injected subject, header, KV marker, wildcard, and raw-key fields fail canonical decode—Plan 02 does not create or interpret actual NATS headers. Map backend missing, corrupt, configuration, admission, signature, bounds, conflict, unavailable, and unresolved ambiguity to the closed failure body without strings. Return `CasApplied` only from an `Applied` backend result carrying complete validated logical acknowledgement evidence for the exact configured stream, a non-duplicate outcome, and a new revision greater than the observed revision, and only after a subsequent read proves that exact revision, canonical bytes, value digest, and expected prior revision metadata. A post-CAS `Ambiguous` result may perform a diagnostic authenticated read and expose the observed state to the later fenced resolver, but it remains typed `Ambiguous` even when the bytes match; it is never upgraded or retried. Compare direct/reference/proxy normalized body semantics, failure code, revisions/digests, and resulting store bytes; do not require byte equality between structurally different direct and proxy envelopes. `InspectReady` must reject a missing admitted key, and the test must prove this condition cannot create `VerifiedWitnessStoreStateV1(None)` or a signed admitted application failure.</action>
  <acceptance_criteria>The proxy exposes only InspectReady, ReadEntry, and CompareAndSwap; request-only invalidity causes zero reads and zero CAS calls, state-dependent invalidity may read but causes zero CAS calls, and accepted CAS requires validated non-duplicate acknowledgement evidence plus exact confirmation. Lost-response ambiguity remains unknown despite a matching diagnostic read and is left for fenced discovery. Direct/reference/proxy semantics match without falsely equating different wire encodings; no raw storage capability or admitted-stream absence constructor exists.</acceptance_criteria>
  <verify>
    <automated>bash tools/check-phase285-witness-conformance.sh typed-proxy</automated>
    The checker must report all typed-proxy mutation and differential cases executed > 0, passed = executed, failed = 0, ignored = 0, and reject partial selector output or any ignored/zero execution.
  </verify>
  <done>Task 285-02-03 closes the in-memory/proxy checkpoint. The downstream transport package can implement this exact interface without adding a dependency back into governance.</done>
</task>

</tasks>

<verification>
Run the three frozen Slice B selectors plus the three Plan 01 selectors after the checker extension. Run `cargo test -p swarm-governance --lib --locked --offline`, `cargo test -p swarm-governance --test phase285_witness_conformance --locked --offline`, `cargo clippy -p swarm-governance --all-targets --all-features --locked --offline -- -D warnings`, `cargo fmt --all -- --check`, `shellcheck tools/check-phase285-witness-conformance.sh`, and `git diff --check`. Confirm the direct, reference, and proxy layers agree on normalized outcomes, all 20 materialized exact tests are enumerated, later selectors remain red, and deliberate faults are never silently retried. Independent review of one frozen tree must identify zero P0/P1/P2 findings before Slice C consumes the interface; this is not Phase 285 completion evidence.
</verification>

<success_criteria>
The atomic-store, in-memory-differential, and typed-proxy lanes are non-vacuously green with an exact 20-test materialized inventory; refusals and pre-CAS faults preserve bytes, post-CAS ambiguity advances at most once and requires exact read confirmation, missing admitted keys fail readiness without minting absence, and the only exported storage capability is the typed revision-CAS contract under `swarm_governance::witness_engine::store`. No async-nats, JetStream, raw header logic, local persistence fallback, or transport cycle is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/285-assurance-foundation-closure/285-02-SUMMARY.md`.
</output>
