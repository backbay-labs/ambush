---
phase: 285-assurance-foundation-closure
plan: "04"
task: "285-04-01"
type: immutable-candidate-freeze
status: review-required
base_commit: 8abe28dbc42c444643ea473614bee7a8cf912b8b
base_tree: 7ce5ed8e7ae305153170ff92b71f79dc218ae1cf
candidate_tree: c8b49c5785335d1d8dbd27e64e71b06525c59c06
accepted_plan_commit: 952cc28df79b0e473a923d00825f00b773aaec47
launcher_sha256: e59ba9f62bf126bccdf8c0d3331b54adae9e74f8fe1ee6e31d43e3dec9ca66b1
manifest_sha256: 3b8a842fe7f8ba58a8f0c9fc5def13180270f578338a0d30fe8df03ed50e5729
checker_sha256: 72d8a63b22764f29ee41b2402c790bbc1f175acffdb07a04266d33a88e811475
registry_sha256: a3a3ec459600ac3163a9b66aa40aa39e9387c50cc75b1e765d9f0693ddb8983b
---

# Task 285-04-01 immutable candidate freeze

This object pins the redesigned cumulative Plan 04 Task 04-01 candidate before authorization-critical selector execution and supersedes rejected trees `d64c7835acbb9aeb7e69759eb094e40c858f11e1`, `096eb974fd0181ba94c6605f27dc92dbd3a0f2d7`, `d9e15889cf5d0535559a6a3a57a6aeed25c69314`, `75531427850c3e1cbedf508903b9bfa8622020f1`, `6978c0cdcecb7ed0921a4e6a0b5f75e7ed9448c3`, `ccd7d16cc5f48f837e7079758aff10a7c38226c6`, and `25e8f1b26aa541bfee3f4655c37aff092f188da4`. Production code cannot author or refresh these values. The root integrator obtained the tree from the exact staged index on base `8abe28d`; the working tree had no unstaged or untracked paths, `git diff --cached --check` passed, and the index contained exactly the twelve paths below.

## Exact candidate scope and file identities

| Path | SHA-256 |
|---|---|
| `crates/swarm-governance-witness/Cargo.toml` | `a95c796de22d7ae0e13af615fc8785f2ea1885d616e52a730cf8aa3ec7ba188c` |
| `crates/swarm-governance-witness/src/lib.rs` | `d27ccccd2b9184c8538939f9b4552f86ac84d7015eb49d1e0f532cda75cd69a7` |
| `crates/swarm-governance-witness/src/public_dispatcher.rs` | `6eb6f8b1d79f310ab3c9dda7855a9ac60338539387df1c02261ffaaa6aa913a5` |
| `crates/swarm-governance-witness/src/service_config.rs` | `2d2f3a95946e3700c093c19a3251feac928ae2dbe44981a75e73381d8426a90c` |
| `crates/swarm-governance-witness/tests/full_service_path.rs` | `fc49d5a10518c3dd46e1d1e6ff5e102e4bac53b03618070c81cdc0db0e638778` |
| `crates/swarm-governance/src/persistence_protocol.rs` | `1fc3837ca0fdd6739352053266017162051925feb655a3b4f21fde77b5b8cfe0` |
| `crates/swarm-governance/src/witness_candidate_verifier.rs` | `09cf8f652b685e831b8a537eefc743f5ce69cad4e7db6b043d60b8e9de1e532e` |
| `crates/swarm-governance/src/witness_service.rs` | `556b12886b9bb1379dfa8206aa59bd9008db201b8a57cd7a33756452f1e56ecf` |
| `crates/swarm-governance/tests/phase285_witness_conformance.rs` | `9800ded6dbfc5ca9036564d87af21449f66d991ffd4f9479155a812496056cf4` |
| `tools/check-phase285-witness-conformance.sh` | `72d8a63b22764f29ee41b2402c790bbc1f175acffdb07a04266d33a88e811475` |
| `tools/check-witness-dependency-closure.sh` | `692d1db25d9bb0d239420ebb5fefdea164914fa783f6fa6af68014ffa037191c` |
| `tools/fixtures/phase285-witness-integrity.json` | `3b8a842fe7f8ba58a8f0c9fc5def13180270f578338a0d30fe8df03ed50e5729` |

The integrity launcher remains byte-identical to accepted Plan 03B and is outside the candidate scope.

## Executor evidence submitted for independent verification

- Genuine RED execution proved that a tight secondary-stream proposed state max-plus-one violation returned unsigned `CanonicalEncoding("public witness request is invalid")` instead of a signed `BoundsExceeded` response bound to the authenticated initial current digest with zero attempted CAS.
- Governance library `133/133`; governance conformance `22/22`; witness library `15/15`; full-service path `4/4`; public-dispatcher selector `4/4`; dispatcher mapping `9/9`.
- Frozen top-level registry `58` rows with unchanged registry digest; source/integration/config/service/verifier/protocol mutants `262/262` unique; integrity mutations `14/14`; inner ledger `9/9`; mapping ledger `9/9`.
- `Prepare` performs only outer identity and routing checks before one governance-owned `verify_public_prepare` seam. That verifier exclusively owns nested canonical validation, session authorization, signature/admission/transition/bounds checks, genesis-abort proof, and sole-defect `StaleIntent` classification for current-head and genesis old/skipped intent.
- Behavioral max/max-plus-one controls cover request, response, state, checkpoint, binding, and retained limits through startup, authenticated initial read, proposed mutation, conflict-winner observation, and post-Applied confirmation. They assert exact response classification, binding digest, attempted/applied CAS counts, retry count, and call sequence. Startup refuses before serving; request/response wire overflow is unsigned; initial/proposed failure is signed with zero attempted CAS; a bounded conflict winner is signed against the winner after exactly one attempted and zero applied CAS with no retry; confirmation uncertainty returns `OutcomeUnknown` without deterministic signed upgrade or retry.
- Eleven coherent post-CAS acknowledgement controls cover transport loss, malformed acknowledgement, duplicate/lower revision, wrong response kind, wrong stream/value/revision/digest, and confirmation mismatch. Only a validated nonduplicate `CasApplied` acknowledgement plus an exact authenticated confirming read can produce signed success. Diagnostic reads after uncertain acknowledgements are evidence only: they never upgrade to success, downgrade to deterministic `Invalid`, or trigger retry.
- The authoritative verifier returns a closed non-forgeable `NewTransition`/`AlreadyPrepared`/`Conflict` result only after complete authentication and stronger-defect validation. An exact authenticated Prepare replay returns signed `AlreadyPrepared` with one read and zero additional CAS; a different valid live successor returns signed `Conflict` with zero CAS. `ApplyThenUnavailable` remains `OutcomeUnknown`, and replaying the identical finalized request recovers through `AlreadyPrepared` without another CAS. Same/different authenticated conflict winners classify identically after one attempted and zero applied CAS. Invalid authorization and candidate defects outrank both idempotency outcomes.
- Coherent redigested and reauthorized current-head old/skipped candidates reach the typed verifier; mixed signature, admission, predecessor, transition, retained, and selected-entry bound defects outrank `StaleIntent`. Mutations that restore early complete validation, omit a verifier branch, or collapse taxonomy branches are killed.
- Dependency closure reports normal `12`, dev `0`, build `0`, forbidden `0`; dependency self-test kills `17/17` mutations.
- Integrity self-test kills `14/14` boundary mutations with positive checker execution.
- Strict scoped clippy, formatting, shellcheck, and diff checks passed.

These are executor claims, not acceptance. The independent reviewer must reconstruct the exact candidate from Git tree `c8b49c5785335d1d8dbd27e64e71b06525c59c06`, verify the file hashes and semantic contract, and return P0/P1/P2 = 0/0/0 before the root supplies this object's manifest and launcher hashes to an authorization-critical selector.

## Explicit non-claims and review focus

- The unwrapped live JetStream package run is not evidence; it correctly failed without the pinned NATS harness.
- This candidate claims authenticated Fence/read behavior and read-CAS-confirm-sign ordering for Prepare, Establish, Discover, Commit, and Abort. It also claims exact retry, authenticated cross-operation winners, seam-specific bound outcomes, bounded public ingress, exact bounded multi-stream startup/Ready validation and routing, pre-store admission refusal, current stored-session enforcement, applied-but-response-lost uncertainty, precise mixed Prepare failure classification, and a restart-safe successor after genesis abort. These claims remain review-required until an independent whole-task audit returns zero findings.
- No Task 04-02 three-account NATS service, store-proxy service, harness topology, or capability-isolation claim is made.
- No hosted, protected-check, combined Phase 285, external App enforcement, phase-completion, or later-phase claim is made.
