---
phase: 285-assurance-foundation-closure
plan: "03A"
type: execute
wave: 3
depends_on:
  - "285-02"
files_modified:
  - Cargo.toml
  - Cargo.lock
  - .github/workflows/ci.yml
  - crates/swarm-governance-witness/Cargo.toml
  - crates/swarm-governance-witness/src/lib.rs
  - tools/check-workspace-layering.sh
  - tools/check-single-governor-key.sh
  - tools/check-negative-registry.sh
  - tools/check-phase285-witness-conformance.sh
  - tools/check-witness-dependency-closure.sh
  - tools/check-gates-wired.sh
  - tools/fixtures/phase285-layering/README.md
autonomous: true
requirements:
  - ASSURE-02
  - ASSURE-06
must_haves:
  truths:
    - "The witness transport package is a library that depends downstream on swarm-governance and owns async-nats, with no reverse edge into governance and no forbidden direct or resolved workspace package."
    - "Workspace layering, single-governor-key, negative-registry, and resolved dependency-closure mutations fail before transport targets can be accepted."
    - "The initial CI registration invokes exactly the seven materialized witness selectors plus library-only dependency closure, and the same workflow parser rejects each missing, duplicated, or substituted invocation; later hosted-evidence additions are owned by Plan 07B."
  artifacts:
    - path: "crates/swarm-governance-witness/Cargo.toml"
      provides: "Downstream transport library manifest with an exact dependency set sufficient through Plan 03B and no premature binary declarations."
      contains: "async-nats"
    - path: "tools/check-witness-dependency-closure.sh"
      provides: "Exact declared and resolved normal/dev/build closure gate with frozen direct, internal-allow, forbidden-package, and lib/bin target sets."
      contains: "cargo tree"
    - path: "tools/check-workspace-layering.sh"
      provides: "Negative dependency-direction gate for transport and governance."
      contains: "swarm-governance-witness"
  key_links:
    - from: "crates/swarm-governance-witness/Cargo.toml"
      to: "crates/swarm-governance/src/witness_engine/store.rs"
      via: "manifest depends on governance's typed store contract without governance importing transport"
      pattern: "swarm-governance-witness.*swarm-governance"
    - from: "tools/check-witness-dependency-closure.sh"
      to: "Cargo.lock"
      via: "locked metadata plus normal/dev/build cargo trees resolve exact target closure and reject every frozen forbidden package"
      pattern: "cargo tree.*swarm-governance-witness.*swarm-runtime-http"
    - from: ".github/workflows/ci.yml"
      to: "tools/check-gates-wired.sh"
      via: "initial checker registration is present and missing-target self-tests are required"
      pattern: "check-phase285-witness-conformance.*check-gates-wired"
---

<objective>
Create the downstream witness transport package and workspace/gate boundary.

Purpose: Transport owns async-nats and service-side storage capabilities; governance remains the authority/protocol layer. This plan freezes the dependency direction before any JetStream code exists.
Output: Library-only transport manifest, resolved closure checker, negative workspace gates, and initial CI/check-gates wiring.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/285-assurance-foundation-closure/285-CONTEXT.md
@.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md
@.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md
@.planning/phases/285-assurance-foundation-closure/285-VERIFICATION.md
@.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md
@<.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md>
@Cargo.toml
@Cargo.lock
@.github/workflows/ci.yml
@crates/swarm-governance/Cargo.toml
@crates/swarm-governance/src/witness_engine/store.rs
@tools/check-workspace-layering.sh
@tools/check-single-governor-key.sh
@tools/check-negative-registry.sh
@tools/check-phase285-witness-conformance.sh
@tools/check-gates-wired.sh

This plan owns initial workspace/package registration only. It must not declare or auto-discover binary targets before their source files exist; Plan 04 owns the three exact explicit binary declarations after creating their source files. Plan 05 may add the safe rustix workspace dependency and lockfile entries for fixed-lane primitives; that additive handoff is explicit and serial.

The `transport-layering` wrapper registry is normative and one-to-one:

| Exact case ID | Exact command | Positive count | Mutation-failure count | Registry owner |
|---|---|---:|---:|---|
| `transport_layering_rejects_governance_reverse_dependency` | `bash tools/check-workspace-layering.sh --self-test phase285-witness-reverse-dependency` | 1 | 1 | `tools/check-phase285-witness-conformance.sh` |
| `transport_layering_rejects_raw_kv_subject` | `bash tools/check-negative-registry.sh --self-test phase285-raw-kv-subject` | 1 | 1 | `tools/check-phase285-witness-conformance.sh` |
| `transport_layering_rejects_second_governor_signer` | `bash tools/check-single-governor-key.sh --self-test phase285-second-governor-signer` | 1 | 1 | `tools/check-phase285-witness-conformance.sh` |
| `transport_layering_rejects_unrelated_authority_crate` | `bash tools/check-negative-registry.sh --self-test phase285-unrelated-authority-crate` | 1 | 1 | `tools/check-phase285-witness-conformance.sh` |
| `transport_layering_rejects_missing_library_target` | `bash tools/check-witness-dependency-closure.sh --self-test missing-library-target` | 1 | 1 | `tools/check-phase285-witness-conformance.sh` |
| `transport_layering_rejects_zero_or_omitted_mutation` | `bash tools/check-phase285-witness-conformance.sh --self-test transport-layering-zero-or-omitted` | 1 | 1 | `tools/check-phase285-witness-conformance.sh` |

The wrapper must execute the literal command in each row, observe exactly one positive control and one intended mutation failure, and reject duplicate IDs, extra IDs, command substitution, or count drift.

The package boundary is closed, not descriptive. Through Plan 03B the exact direct normal dependency set is `async-nats`, `async-trait`, `futures-util`, `hex`, `serde`, `serde_json`, `sha2`, `swarm-governance`, `thiserror`, `tokio`, and `tracing`; the exact direct dev dependency set is `swarm-crypto`; the direct build dependency set is empty. `async-trait` is required to implement the accepted `WitnessAtomicStore` trait, and the dev-only crypto edge constructs independently signed JetStream fixtures without granting production signing authority. The only permitted internal packages in any resolved normal/dev/build closure are `swarm-governance-witness`, `swarm-governance`, `swarm-consensus`, `swarm-core`, `swarm-crypto`, and `swarm-policy`. The exact forbidden workspace package IDs are `swarm-whisker`, `swarm-ingest-tetragon`, `swarm-ingest-json`, `swarm-ingest-taxii`, `swarm-ingest-sentinel`, `swarm-pheromone`, `swarm-response`, `swarm-runtime`, `swarm-agents`, `swarm-runtime-http`, `swarm-ingest-runtime`, `swarm-runtime-workbench`, `swarm-evolution`, `swarm-cli`, `swarm-spine`, and `swarm-guard`. Declared edges are inspected separately for normal, dev, and build kinds; resolved normal, dev, and build closures are each inspected. Plan 03A requires exactly one library target named `swarm_governance_witness` and no binary target. After Plan 04, `--all-targets` requires that library plus exactly the three binary targets `swarm-governance-witness`, `swarm-governance-witness-store`, and `swarm-governance-witness-init`; extra-target rejection applies only to library and binary kinds, so declared integration-test targets remain permitted.

Execution has two serial internal checkpoints but still produces one final atomic Plan 03A production commit. First freeze only the workspace, lockfile, new crate, closure checker, and fixture documentation; record its Git tree, run the library compile and closure controls, and obtain a read-only zero-P0/P1/P2 review before touching the three integrity-pinned gates, witness registry, CI, or gate-wiring checker. Then unstage without changing bytes, complete the registry/CI integration, run the final combined gate, and freeze the complete plan tree for the final independent review.

The accepted base has three known global-inventory conditions that this slice must not conceal or repair out of sequence. `tools/check-single-governor-key.sh` and `tools/check-negative-registry.sh` reject stale accepted-base/ongoing-Phase-285 inventory digests; Plan 07B Task 285-07-04 explicitly owns their single final refresh only after every production manifest, source, workflow, schema, lockfile, and checker byte is frozen. Plan 03A therefore runs only their exact Phase-285 transport self-test modes through the six-row registry and must not refresh their global pins or claim their normal modes green. The normal `tools/check-gates-wired.sh` on base `ff762236a216f44d26da90d7b3fe7eeecc3d178d` reports exactly one unwired accepted file, `tools/check-collective-hypothesis-graph.sh`, from the parked Phase 286 lineage. This slice must not wire, execute, delete, exempt, or otherwise alter that future-phase gate. Instead `check-gates-wired.sh --phase285-differential ff762236a216f44d26da90d7b3fe7eeecc3d178d` must parse the immutable base objects and candidate filesystem with the same normal wiring parser, require the candidate global unwired set to equal that exact singleton, prove the new witness dependency checker is wired, and reject any added/removed/substituted unwired path or Phase-285 invocation. Full global inventory/wiring success remains a Plan 07B gate, not a Plan 03A claim.
</context>

<tasks>

<task type="auto">
  <wave>3</wave>
  <name>Task 285-03-01: Create the library-only downstream transport package and closure gates</name>
  <files>Cargo.toml, Cargo.lock, .github/workflows/ci.yml, crates/swarm-governance-witness/Cargo.toml, crates/swarm-governance-witness/src/lib.rs, tools/check-workspace-layering.sh, tools/check-single-governor-key.sh, tools/check-negative-registry.sh, tools/check-phase285-witness-conformance.sh, tools/check-witness-dependency-closure.sh, tools/check-gates-wired.sh, tools/fixtures/phase285-layering/README.md</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md; .planning/phases/285-assurance-foundation-closure/285-RESEARCH.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; Cargo.toml; Cargo.lock; crates/swarm-governance/Cargo.toml; crates/swarm-governance/src/lib.rs; crates/swarm-governance/src/witness_engine/store.rs; .github/workflows/ci.yml; tools/check-workspace-layering.sh; tools/check-single-governor-key.sh; tools/check-negative-registry.sh; tools/check-phase285-witness-conformance.sh; tools/check-gates-wired.sh</read_first>
  <action>Red-first stage one: create `tools/check-witness-dependency-closure.sh` and its real-tree/scratch-copy controls before the package exists, prove missing library target, forbidden direct dependency kind, forbidden resolved normal/dev/build package, reverse governance edge, wrong library name, and premature binary target all fail, then add `crates/swarm-governance-witness` as a workspace member. Its manifest must contain exactly the direct normal and dev dependencies plus the empty build set frozen in this plan, exactly one library named `swarm_governance_witness`, and no binary declarations. Its library root exports no JetStream implementation or service target yet. Update `Cargo.lock`, run `cargo check -p swarm-governance-witness --lib --tests --locked --offline`, run `--library-only`, stage only the six stage-one paths, record the Git tree, require zero unstaged stage-one changes and a read-only zero-P0/P1/P2 intermediate review, then unstage without changing bytes. Red-first stage two: preserve the already-frozen six transport registry rows in `tools/check-phase285-witness-conformance.sh` and materialize their executor instead of redefining the registry. Each exact row first runs its normal gate against the actual subject tree and requires one positive control, then copies the minimum subject into a confined scratch tree, applies its named reverse/raw/signer/direct-authority/missing-library/omitted-row mutation, and runs the same evaluator to require exactly one failure. Extend `check-workspace-layering.sh`, `check-single-governor-key.sh`, and `check-negative-registry.sh` with only the literal scoped self-test modes in the registry; they must reject substring-only output, independent-clean-fixture substitution, duplicate/extra/missing IDs, altered commands, and count drift. Do not refresh either global inventory: Plan 07B owns that operation. The unrelated-authority mutation adds a forbidden direct `swarm-consensus` edge even though governance may carry it transitively. `--library-only` parses locked metadata plus declared manifests, checks all direct normal/dev/build sets, resolves normal, dev, and build closures separately, requires the exact library name, ignores test targets, and enforces the exact allowed/forbidden package sets above. `--all-targets` is deliberately red until Plan 04 creates the three binaries; afterward it requires the exact library and three exact binary names, ignores only integration-test targets, performs target-aware locked checks, and applies the same edge policy. Update CI to invoke exactly once each of `response-failure-wire`, `candidate-verifier`, `protocol-checkpoint`, `atomic-store-contract`, `in-memory-differential`, `typed-proxy`, `transport-layering`, and `check-witness-dependency-closure.sh --library-only`. Extend `tools/check-gates-wired.sh` so its Phase-285 parser validates those exact eight invocations in the actual workflow and its `--phase285-self-test` makes scratch workflow copies, removes or duplicates each invocation and substitutes each selector/mode/command in turn, reruns the same parser, and requires every mutation to fail. Add `--phase285-differential ff762236a216f44d26da90d7b3fe7eeecc3d178d`: use immutable Git objects for the base and the candidate filesystem for the subject, run the same global parser over both, require the base and candidate unwired sets to equal exactly `tools/check-collective-hypothesis-graph.sh`, and require the new dependency checker to be executable and wired exactly once. Its controls must add a second unwired checker, remove/rename the baseline checker, delete or substitute a required witness invocation, and alter the base commit; every mutation fails without writing either subject. Plan 07B owns later hosted-evidence CI edits, the final global wiring pass, and final authority/negative inventory refresh. Do not wire or execute the Phase 286 checker, add JetStream code, create integration/binary targets, refresh global inventory pins, or make hosted/final assurance claims here.</action>
  <acceptance_criteria>The exact library-only package compiles and resolves with the frozen direct and transitive boundary; declared normal/dev/build, resolved normal/dev/build, reverse/raw/signer/direct-authority, target-name, and premature-binary mutations fail through the same engines used on the subject tree; CI contains exactly the seven materialized selectors plus library-only closure and same-parser workflow mutations fail; the base/candidate global-wiring differential proves this slice adds no unwired gate while preserving the exact parked Phase 286 singleton; scoped authority/negative self-tests pass while their global inventory modes remain explicitly pending Plan 07B; the intermediate package/closure tree and final combined tree are independently reviewable; no JetStream implementation or binary target exists.</acceptance_criteria>
  <verify>
    <automated>cargo check -p swarm-governance-witness --lib --tests --locked --offline && bash tools/check-phase285-witness-conformance.sh transport-layering && bash tools/check-witness-dependency-closure.sh --library-only && bash tools/check-gates-wired.sh --phase285-self-test && bash tools/check-gates-wired.sh --phase285-differential ff762236a216f44d26da90d7b3fe7eeecc3d178d && bash tools/check-workspace-layering.sh</automated>
    The selector must execute all six literal case IDs and their exact workspace-layering, authority, negative-registry, and dependency-closure commands with one real-tree positive and one same-engine mutated-copy failure per row. The closure gate must report one checked library, zero forbidden declared or resolved edges, and zero binaries. The wiring self-test must parse the actual workflow and reject deletion, duplication, selector/mode substitution, or command substitution for each of the seven selectors and library-only closure. Every command must report executed > 0, passed = executed, failed = 0, ignored = 0; a missing target, zero match, forbidden package, reverse edge, substring-only match, independent-fixture substitution, or omitted registry/CI mutation fails.
  </verify>
  <done>Task 285-03-01 establishes the downstream package and non-vacuous dependency gates without premature binaries or a transport cycle.</done>
</task>

</tasks>

<verification>
Run the full scoped automated command, confirm the exact declared and resolved closure sets, and retain counts for all six real-tree-positive/same-engine-mutation registry rows. Confirm the actual CI workflow contains the exact seven selectors plus library-only closure and that the same parser kills every deletion, duplication, selector/mode substitution, and command substitution. Confirm the immutable-base/candidate differential preserves exactly the one parked Phase 286 unwired checker and adds no new global wiring defect. Record, without masking, that normal authority/negative inventory gates and normal global wiring remain pending their Plan 07B owners. Then hand source ownership to Plan 03B for JetStream.
</verification>

<success_criteria>
Transport-layering is non-vacuously green; the exact library and test configuration compile from the locked graph; direct and resolved normal/dev/build boundaries are exact; no forbidden package, reverse edge, premature binary, Phase-285 CI omission, or newly introduced global unwired gate passes; scoped authority/negative checks are green without premature global re-pinning; and both the intermediate package tree and final combined tree receive zero-finding independent review before one atomic production commit.
</success_criteria>

<output>
After completion, create `.planning/phases/285-assurance-foundation-closure/285-03A-SUMMARY.md`.
</output>
