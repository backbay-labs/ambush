---
phase: 285-assurance-foundation-closure
plan: "03A"
type: execute
wave: 3
depends_on:
  - "285-02"
files_modified:
  - Cargo.toml
  - Cargo.lock
  - .github/workflows/ci.yml
  - crates/swarm-governance-witness/Cargo.toml
  - crates/swarm-governance-witness/src/lib.rs
  - tools/check-workspace-layering.sh
  - tools/check-single-governor-key.sh
  - tools/check-negative-registry.sh
  - tools/check-phase285-witness-conformance.sh
  - tools/check-witness-dependency-closure.sh
  - tools/fixtures/phase285-layering/README.md
autonomous: true
requirements:
  - ASSURE-02
  - ASSURE-06
must_haves:
  truths:
    - "The witness transport package is a downstream-only library dependency of swarm-governance and async-nats, with no reverse edge into governance or unrelated runtime/authority packages."
    - "Workspace layering, single-governor-key, negative-registry, and resolved dependency-closure mutations fail before transport targets can be accepted."
    - "The initial CI registration invokes the non-vacuous checker surface and gates missing targets; later hosted-evidence additions are owned by Plan 07B."
  artifacts:
    - path: "crates/swarm-governance-witness/Cargo.toml"
      provides: "Downstream transport library manifest with async-nats ownership and no premature binary declarations."
      contains: "async-nats"
    - path: "tools/check-witness-dependency-closure.sh"
      provides: "Resolved cargo-tree closure gate excluding full runtime/http/ingest/agents/workbench and unrelated authority crates."
      contains: "cargo tree"
    - path: "tools/check-workspace-layering.sh"
      provides: "Negative dependency-direction gate for transport and governance."
      contains: "swarm-governance-witness"
  key_links:
    - from: "crates/swarm-governance-witness/Cargo.toml"
      to: "crates/swarm-governance/src/witness_engine/store.rs"
      via: "manifest depends on governance's typed store contract without governance importing transport"
      pattern: "swarm-governance-witness.*swarm-governance"
    - from: "tools/check-witness-dependency-closure.sh"
      to: "Cargo.lock"
      via: "cargo tree -e normal resolves exact target closure and rejects forbidden packages"
      pattern: "cargo tree.*swarm-governance-witness.*swarm-runtime-http"
    - from: ".github/workflows/ci.yml"
      to: "tools/check-gates-wired.sh"
      via: "initial checker registration is present and missing-target self-tests are required"
      pattern: "check-phase285-witness-conformance.*check-gates-wired"
---

<objective>
Create the downstream witness transport package and workspace/gate boundary.

Purpose: Transport owns async-nats and service-side storage capabilities; governance remains the authority/protocol layer. This plan freezes the dependency direction before any JetStream code exists.
Output: Library-only transport manifest, resolved closure checker, negative workspace gates, and initial CI/check-gates wiring.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/285-assurance-foundation-closure/285-CONTEXT.md
@.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md
@.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md
@.planning/phases/285-assurance-foundation-closure/285-VERIFICATION.md
@.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md
@<git show 5be011a07690a63a297d5bba8fbf740bb659c19d:.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md>
@Cargo.toml
@Cargo.lock
@.github/workflows/ci.yml
@crates/swarm-governance/Cargo.toml
@crates/swarm-governance/src/witness_engine/store.rs
@tools/check-workspace-layering.sh
@tools/check-single-governor-key.sh
@tools/check-negative-registry.sh
@tools/check-phase285-witness-conformance.sh
@tools/check-gates-wired.sh

This plan owns initial workspace/package registration only. It must not declare explicit binary targets before their source files exist; Plan 04 owns target declarations after creating the binaries, or Cargo's `src/bin` auto-discovery may be used. Plan 05 may add the safe rustix workspace dependency and lockfile entries for fixed-lane primitives; that additive handoff is explicit and serial.

The `transport-layering` wrapper registry is normative and one-to-one:

| Exact case ID | Exact command | Positive count | Mutation-failure count | Registry owner |
|---|---|---:|---:|---|
| `transport_layering_rejects_governance_reverse_dependency` | `bash tools/check-workspace-layering.sh --self-test phase285-witness-reverse-dependency` | 1 | 1 | `tools/check-phase285-witness-conformance.sh` |
| `transport_layering_rejects_raw_kv_subject` | `bash tools/check-negative-registry.sh --self-test phase285-raw-kv-subject` | 1 | 1 | `tools/check-phase285-witness-conformance.sh` |
| `transport_layering_rejects_second_governor_signer` | `bash tools/check-single-governor-key.sh --self-test phase285-second-governor-signer` | 1 | 1 | `tools/check-phase285-witness-conformance.sh` |
| `transport_layering_rejects_unrelated_authority_crate` | `bash tools/check-negative-registry.sh --self-test phase285-unrelated-authority-crate` | 1 | 1 | `tools/check-phase285-witness-conformance.sh` |
| `transport_layering_rejects_missing_library_target` | `bash tools/check-witness-dependency-closure.sh --self-test missing-library-target` | 1 | 1 | `tools/check-phase285-witness-conformance.sh` |
| `transport_layering_rejects_zero_or_omitted_mutation` | `bash tools/check-phase285-witness-conformance.sh --self-test transport-layering-zero-or-omitted` | 1 | 1 | `tools/check-phase285-witness-conformance.sh` |

The wrapper must execute the literal command in each row, observe exactly one positive control and one intended mutation failure, and reject duplicate IDs, extra IDs, command substitution, or count drift.
</context>

<tasks>

<task type="auto">
  <wave>3</wave>
  <name>Task 285-03-01: Create the library-only downstream transport package and closure gates</name>
  <files>Cargo.toml, Cargo.lock, .github/workflows/ci.yml, crates/swarm-governance-witness/Cargo.toml, crates/swarm-governance-witness/src/lib.rs, tools/check-workspace-layering.sh, tools/check-single-governor-key.sh, tools/check-negative-registry.sh, tools/check-phase285-witness-conformance.sh, tools/check-witness-dependency-closure.sh, tools/fixtures/phase285-layering/README.md</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md; .planning/phases/285-assurance-foundation-closure/285-RESEARCH.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; Cargo.toml; Cargo.lock; crates/swarm-governance/Cargo.toml; crates/swarm-governance/src/lib.rs; crates/swarm-governance/src/witness_engine/store.rs; .github/workflows/ci.yml; tools/check-workspace-layering.sh; tools/check-single-governor-key.sh; tools/check-negative-registry.sh; tools/check-phase285-witness-conformance.sh; tools/check-gates-wired.sh</read_first>
  <action>Red-first: install the normative six-row one-to-one `transport-layering` registry declared in this plan into `tools/check-phase285-witness-conformance.sh`. Preserve every literal case ID, exact command, positive count of 1, mutation-failure count of 1, and wrapper ownership; execute each row individually. Substring matches, aggregate-only output, command substitution, duplicate/extra/missing IDs, or any count drift fail. Add temporary fixtures that add `swarm-governance-witness` as a dependency of `swarm-governance`, import a raw `$KV.>` subject, define a second governor signer, register an unrelated authority crate, remove the library target, and omit one registry row; each mutation must fail before adding the valid package. Add `crates/swarm-governance-witness` as a workspace member with a library target only, depend on `swarm-governance` and existing shared protocol/crypto crates, and own `async-nats`; do not add `[[bin]]` declarations because the source files are not present until Plan 04. Update the lockfile for this manifest. Extend `check-workspace-layering.sh`, `check-single-governor-key.sh`, and `check-negative-registry.sh` with the exact self-test modes named in the registry for reverse imports, raw storage capability, signer minting, visibility bypass, and duplicate authority. Create `tools/check-witness-dependency-closure.sh` with the exact registry self-test plus two fail-closed modes. `--library-only` parses locked `cargo metadata`, requires exactly the present library target, resolves its normal dependency closure, and rejects runtime-http, ingest, agents, workbench, full runtime/http, unrelated authority packages, and any reverse edge from governance. `--all-targets` is deliberately red until Plan04 creates the three binaries; afterward it must enumerate the library and exact public/store/init binary targets, run locked target-aware check/tree inspection for each, require a positive count for every target, and apply the same forbidden-edge set. Missing, extra, zero-count, or unbuildable targets fail. Use `--library-only` for this plan checkpoint and wire both modes into gate self-tests so an early `--all-targets` invocation cannot pass vacuously. Add initial checker invocations and exact target existence/count assertions to `.github/workflows/ci.yml` and `tools/check-gates-wired.sh`. Plan07B owns later hosted-evidence CI edits and must preserve this registration. Do not add JetStream implementation or service binaries here.</action>
  <acceptance_criteria>The library-only package resolves with async-nats downstream of governance; reverse/raw/signer/duplicate-authority mutations fail; the resolved closure command proves excluded runtime/http/ingest/agents/workbench/unrelated authority targets; initial CI and check-gates self-tests fail closed on missing selectors; no binary target is declared before its source exists.</acceptance_criteria>
  <verify>
    <automated>bash tools/check-phase285-witness-conformance.sh transport-layering && bash tools/check-witness-dependency-closure.sh --library-only</automated>
    The selector must execute all six literal case IDs and their exact workspace-layering, authority, negative-registry, and resolved dependency-closure targets with executed > 0, passed = executed, failed = 0, ignored = 0; a missing target, zero match, forbidden package, reverse edge, substring-only match, or omitted registry row/mutation must fail.
  </verify>
  <done>Task 285-03-01 establishes the downstream package and non-vacuous dependency gates without premature binaries or a transport cycle.</done>
</task>

</tasks>

<verification>
Run the frozen transport-layering selector and confirm the exact resolved closure excludes the forbidden runtime/authority packages. Confirm CI and check-gates wiring has a deliberate missing-target red control, then hand workspace ownership to Plan 03B for JetStream and Plan 07B for final hosted additions.
</verification>

<success_criteria>
Transport-layering is non-vacuously green, package closure is resolved from Cargo.lock, no forbidden runtime/authority edge exists, and the manifest contains no binary target whose source is absent.
</success_criteria>

<output>
After completion, create `.planning/phases/285-assurance-foundation-closure/285-03A-SUMMARY.md`.
</output>
