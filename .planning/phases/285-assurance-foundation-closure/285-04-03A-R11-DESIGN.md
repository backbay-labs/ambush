---
phase: 285-assurance-foundation-closure
plan: "04"
task: "285-04-03A"
type: rejected-candidate-redesign
status: pending-independent-review
supersedes_candidate_tree: 80e073974373cac2f4737ae22bf957ae16e80524
production_base_commit: 5ade79141a2f8249ef084d24fcfd5ef12251e48e
owned_path_count: 16
owned_paths_sha256: 1decd3b39a6433d9358a10289d6886f67211b2560563b417381273dd1662d614
---

# Task 285-04-03A r11 state-machine redesign

This addendum is normative and supersedes only conflicting r10 clauses in `285-04-PLAN.md`. All other r10 recovery, abort, topology, integrity, sequencing, and non-claim requirements remain mandatory. Production repair must start again from accepted Task 04-02 commit `5ade791`; rejected r10 worktree/tree `80e0739` is read-only diagnostic evidence.

## 1. Harness-owned bounded response loss

Remove `PublicWitnessPublishSuppressionV1`, `PublicWitnessServiceExecutionReceiptV1`, `start_with_publish_suppression`, deadline-control exports, and every related re-export from the library. No debug, release, default-feature, or all-feature library artifact may expose reply-suppression authority or an unbounded channel.

Implement lost response entirely in `service_checkpoint.rs` plus the owned NATS harness. A capacity-one one-shot reply bridge forwards the real request to the unmodified `PublicWitnessServiceRunner` with the bridge sink as the service reply subject. The sink receives the actual canonical post-handler reply, binds it to scenario ID, operation, request nonce/digest, response digest, exact recording-store backend sequence, and runner/connection identity, then deliberately does not forward it to the real requester's inbox. The independent requester must time out; mutation operations whose CAS may have applied must surface `OutcomeUnknown`; retries remain zero. Reserve the single evidence slot before forwarding the request. If the slot is full, closed, missing, or cannot accept the receipt, do not arm loss and forward the reply normally. Reject request-drop, pre-handler/pre-response receipt, fabricated receipt, delivered reply, capacity greater than one, unbounded channel, repeated arm, omitted receive, and any production/debug/all-feature suppression symbol.

## 2. Exact private-hop deadline without legacy weakening

`NatsPublicWitnessStoreProxyClient::new` remains source-compatible but must require `request_deadline_millis == STORE_RESPONSE_GRANT_MILLIS`; reject 0, 2,999, 3,001, `u64::MAX`, predicate deletion, and constant substitution. Production callers continue to pass the constant. Update the single inherited direct-client fixture in `full_service_path.rs` from 1,000 to exactly 3,000 and add/retain a coherent lower/upper constructor refusal oracle. This is the sole newly writable sixteenth path; no other Task 04-02 behavior or fixture is changed.

The legacy `request_deadline_millis` fields inside bound service configuration remain bounded identity inputs for Task 04-02 compatibility but cannot determine r11 receipt, handler, proxy-exchange, or response-publication deadlines. The conformance checker must reject any production use of those fields in the deadline state machine. Receipt deadlines remain the exact closed constants 2,000/3,000/10,000/12,000 and every asynchronous operation is bounded by the original absolute deadline or the exact private-exchange subdeadline, never a stale relative duration.

## 3. Nine observations of the production state machine

Delete `private_deadline_control`, `public_deadline_control`, and helper-created `DeadlineControlEvidenceV1` counters. The exact nine ledger rows must be derived from recording implementations at the same seams used by production:

1. `private_queue_expired`: actual bounded private ingress dequeues after its receipt deadline; zero preflight/store/publish.
2. `private_preflight_expired`: actual decode/verify/admission/preflight gate advances the deterministic clock to expiry; zero store/publish.
3. `private_store_crosses_deadline`: actual recording store call begins before the original deadline, crosses it under `timeout_at`, records one store call, and publishes zero replies.
4. `private_response_enqueue_expired`: actual store response completes before expiry, deterministic clock expires before the real enqueue/publish seam, records one store call and zero publication.
5. `public_queue_expired`: actual bounded public ingress dequeues expired work; zero private/store/publish.
6. `public_private_exchange_crosses_deadline`: actual dispatcher invokes the recording private proxy once and its absolute exchange timeout fires; zero public publication/retry.
7. `public_response_enqueue_expired`: actual dispatcher completes and signs before expiry, deterministic clock expires at the real response enqueue/publish seam, zero publication/retry.
8. `post_cas_timeout_outcome_unknown`: actual recording CAS applies once, the confirming/private response crosses the absolute deadline, requester observes `OutcomeUnknown`, and CAS/retry counts are exactly 1/0.
9. `deadline_topology_exact`: the production constants and actual runner/grant construction prove 2,000/3,000/10,000/12,000 with 1,000/1,000/2,000 reserves and max-one response grants.

Use a deterministic clock/gate injected into the real runner/handler state machine, not a parallel helper implementation. Counters come only from recording queue, preflight, proxy/store, CAS, signer, enqueue, and publisher seams. The ledger writer may serialize those observations but may not assign or repair them. In isolated source copies, run the focused behavioral test against receipt-reset, queue-check deletion, preflight-check deletion, pre-store-check deletion, relative-timeout substitution, late-publication, retry, CAS-ambiguity downgrade, fabricated-counter, helper-bypass, and topology substitution mutants. Every mutant must compile, execute the exact case, and fail for the intended behavioral reason.

## Exact r11 scope and gate

The r11 candidate owns the original fifteen paths plus `crates/swarm-governance-witness/tests/full_service_path.rs`: exactly 16 sorted paths, digest `1decd3b39a6433d9358a10289d6886f67211b2560563b417381273dd1662d614`. The integrity launcher remains byte-identical; the 58-row registry remains unchanged; exactly two bins exist; init and three-bin dependency closure remain 04-03B scope.

Root must freeze an exact staged tree with zero unstaged/untracked paths. Before commit, independent review must return P0/P1/P2 = `0/0/0` and the pinned gate must pass: integrity self-test, public-dispatcher, harness self-test, governance library serial, witness all-target offline compile, focused recovery/deadline checker, strict combined clippy, formatting, shellcheck, diff checks, exact 16-path identity, and zero Desktop Phase 285 residue. Only the exact reviewed tree may be committed and pushed to work plus checkpoint refs.
