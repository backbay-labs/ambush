---
phase: 285-assurance-foundation-closure
plan: "04"
task: "285-04-03A"
type: bankable-execution-decomposition
status: awaiting-independent-review
revision: r38
supersedes_planning_commit: 6ec4a17d6a2735613741e6d069265ef9755323c6
supersedes_candidate_tree: 02d8fc929c8661d410a31c8b13aea68658b33eb4
supersedes_incomplete_tree: ec278370395cda1b70589b9e7e59e8c2a8d93c67
production_base_commit: 5ade79141a2f8249ef084d24fcfd5ef12251e48e
owned_path_count: 16
owned_paths_sha256: 1decd3b39a6433d9358a10289d6886f67211b2560563b417381273dd1662d614
---

# Task 285-04-03A r38 immutable-commit execution decomposition

This stable-filename addendum is revision r38. Revision r35 `9262cf53b668ee3d829230ef40b4c5847eb87b08` passed independent P0/P1/P2=`0/0/0`, but two A2b1 implementation candidates were rejected. `5ad200f` was rejected `0/3/0` for unenforced capacity-one behavior, fabricable evidence, and lexical mutants. Fresh compliance candidate `8a7df5d` closed those findings but was rejected `0/2/0`: it copied a helper-owned evidence object instead of consuming and independently reconciling the accepted A2a ledger, and it introduced an unsafe in-Rust NATS environment handoff. Revision r36 candidate `8615228962cafbd8d5f2efd81d45ee198875b8be` closed both production defects but was rejected P0/P1/P2=`0/2/0` for executable command defects. Revision r37 `6ec4a17d6a2735613741e6d069265ef9755323c6` closed those findings and passed independent P0/P1/P2=`0/0/0`, but its fresh execution proved the standalone external integration command impossible: Cargo builds the library without `cfg(test)` for integration tests, so the external FQN cannot name the private receipt predicate, and the command supplied no canonical artifacts. Revision r38 makes the checker-owned focused invocation the only external proof path. The shell checker creates two explicit bounded artifacts, runs the internal predicate/producer, then runs the external independent decoder against the same read-only bytes and owns cleanup. No public/test-support API or Rust-owned NATS lifecycle is introduced. A2b2 and later work remain blocked.

## r26 bankable execution decomposition

1. **A1 — subscriber-to-worker deadline seam.** Own exactly eight paths with sorted digest `3b9e0dcdef3b37beb4dd22cfb576cb0f3d72d45e2c87c54042c548dbc4deacd7`: witness `Cargo.toml`; `src/lib.rs`; `src/public_dispatcher.rs`; `src/service_config.rs`; `src/store_proxy_service.rs`; `tests/full_service_path.rs`; the witness conformance checker; and integrity manifest. It starts from `5ade791`. For each public/private path, one crate-private production function must own raw subscription-message validation, immediate receipt-time capture, absolute-deadline construction, typed ingress-message construction, and bounded enqueue; the real NATS subscriber and exact library test must call that same function. A1 proves eight distinct event-derived worker rows plus one checker-joined budget/real-constructor row, receives its own immutable review/commit/pushed refs, and makes no live NATS grant, relay, or recovery claim.
2. **A2a — observed worker/proxy/store spine.** Start only from accepted A1. Own exactly nine paths with slice digest `919bf3addbcdbf8625f055f9c8737f22aa5b9d3dd99130fe8e62fc981b65d4c9`: witness `src/lib.rs`, dispatcher, runtime client, service config, store proxy, `tests/service_checkpoint.rs`, checker, manifest, and NATS harness. The cumulative A1+A2a union is exactly eleven paths with digest `aa6b8eae0de4d644ebb18c08a36264d09b6adc81cddc50288d3067ed3715030f`. Its exact library-internal normal-NATS test alone can use the crate-private cfg(test) builders to inject the accepted A1 `WorkerTransitionObserverV1` recorder while still invoking both shipping `start` methods; no external integration test may name that seam. The harness consumes explicit prefix `--expect-exact-test <full-fqn> <expected-filtered-out> --`, binds the forwarded exact FQN, and requires the pinned A2a transcript `1/1/0/0/17`; malformed, mismatched, duplicate, filtered-only, zero, ignored, or failed executions are explicit self-test mutations. It banks only ordered worker, proxy, store, publisher, and server-issued connection observations. Every count and digest is derived from serialized evidence. It makes no relay, response-grant, suppression, replay, recovery, or abort claim.
3. **A2b1 — accepted-ledger-bound suppression predicate.** Start only from accepted A2a. Own exactly four paths with digest `ccd00d773edc3826aecd28bf6d451d0d2a5323ee766224e01b5633cb3815070a`: witness `src/lib.rs`, `tests/service_checkpoint.rs`, checker, and manifest. The checker owns one `mktemp -d` directory and two absent sibling paths exposed only as `PHASE285_COMPLETE_RECEIPT_LEDGER_PATH` and `PHASE285_COMPLETE_RECEIPT_PATH`. The accepted A2a internal test writes and reopens canonical `ObservationLedgerV1` bytes, independently recomputes every overlapping request, signed response, exact worker operation/CAS sequence, proxy/store input and result digest, authenticated store-envelope/public-head relation, publisher reply subject/bytes, and three server-bound identities, then validates the receipt before exact capacity-one reservation. A same-helper typed copy or byte-equality oracle is forbidden. A2b1-specific private exchange and causal fields are separately decoded and reconciled. Invalid, partial, cross-copied, full, closed, missing, failed, capacity-zero, or capacity-greater-than-one cases forward. The external integration FQN cannot name or duplicate the cfg(test)-private predicate; after the internal target finishes, it independently decodes and recomputes the same read-only artifacts. Producer-plus-receipt coherent mutants must compile, execute the real internal target and external decoder, and fail the independently recomputed relation. Rust may only create-new/write/fsync/reopen those exact checker-selected files; it may not spawn NATS, choose/remove/replace a directory or file, or read any other artifact selector. The shell checker enforces `0600`, distinct regular no-symlink inodes, unchanged parent identity, newline framing, canonical reopen, maxima of 1,048,576 ledger bytes and 2,097,152 receipt bytes, digest/identity stability across the handoff, and cleanup on `EXIT HUP INT TERM`. At A2b1 it requires exact internal `1/1/0/0/18` and external `1/1/0/0/1`; the manifest binds inherited A2b2/A2b3 external filtered count `2` to their exact tree pins. Either FQN invoked outside that checker-owned sequence is invalid evidence. A2b1 makes no topology, live relay, grant, timing, recovery, or A2c claim.
4. **A2b2 — account/principal-scoped topology validator.** Start only from accepted A2b1. Own exactly five paths with digest `964b80e1d926ccc78f1e2f487edb84169a01f1cb6a1eadcf3b294c3234ce98ff`: the A2b1 four plus the NATS harness. It parses and compares each account/principal block as a closed tuple set and kills cardinality-preserving block moves, import/export-account substitutions, and cross-principal permission swaps. Global counts are forbidden. It makes no serving-binary, live relay, grant, timing, recovery, or A2c claim.
5. **A2b3 — live four-account relay, grants, and causal timing.** Start only from accepted A2b2. Own the exact eleven-path set/digest `4d9d95a9253af2cede7f42175322169a4deb3fafebc8fb0d58bcb845cd992adf`: witness `Cargo.toml`; two serving bins; `src/lib.rs`; raw config, runtime client, service config; `tests/service_checkpoint.rs`; checker; manifest; and NATS harness. It integrates on the accepted A2b1/A2b2 seams, invokes both shipping starts through accepted A1/A2a observers, proves fresh public/private relay identities, exact owner-block authority, and server-bound 3,000/12,000-ms max-one refusal. It measures the true origin-relative `second_publish_at - first_publish_at` and kills a delayed-first-response mutant. The Task02 cumulative union is exactly fourteen paths/digest `4e6629733654f7b4b6f1233bfaaef96a0fede0368620380c8b97f9d75bb18664`. It makes no final oracle, recovery, or abort claim.
6. **A2c — positive-first independent oracle and non-bypassable corpus.** Start only from accepted A2b3. Own exactly four paths with slice digest `8b37d06097626b9d3f302ea1053ced7852e5553f095c8ead99964c8bca66b6c5`: `tests/service_checkpoint.rs`, checker, manifest, and NATS harness. It adds no production behavior. It independently decodes every canonical request/private/public response, recomputes all typed and top-level relations, validates real causal order, and requires the positive immutable ledger to pass the exact ordinary predicate before any mutant is run. The top-level accepted selector rejects ambient child/family controls and unconditionally executes one static named real-target corpus; internal children use a distinct private selector and cannot suppress or narrow the parent inventory. Coherent mutations replace the former direct-error synthetic identity/count cases. The cumulative union remains exactly fourteen paths/digest `4e6629733654f7b4b6f1233bfaaef96a0fede0368620380c8b97f9d75bb18664`. It receives the final A2 checkpoint and makes no recovery or abort claim.
7. **A3 — physical recovery/abort matrix.** Start only from accepted A2c. Own exactly six paths with slice digest `769825832abfff2e80561b7c4692a0aec750699096f094e3ab5c54765726d4ae`: service checkpoint integration test; persistence protocol and its source-local tests; conformance checker; integrity manifest; and NATS harness. The final cumulative union remains the reviewed sixteen paths with digest `1decd3b39a6433d9358a10289d6886f67211b2560563b417381273dd1662d614`. It proves the 18 physical restart/lost-response rows and late abort predicates while separately rerunning A1 and the final A2c selector.

Each slice requires its own exact red control and staged candidate validation, then exactly one local immutable production commit with zero dirty state. Root binds that commit, its tree, launcher hash, manifest hash, and focused-checker tree token; authorization-critical commands explicitly consume those values. Independent P0/P1/P2=`0/0/0` review audits the immutable commit. Only then are its work and checkpoint refs pushed. A later slice is unauthorized until both origin remote-tracking refs for its predecessor resolve to the reviewed commit. The existing implementation worktree is reused for A2a, A2b, A2c, and A3; no per-revision or per-slice worktree may be created. Each production window is two hours from explicit authorization. A timeout or rejected immutable review stops only that slice; it cannot authorize repair of the rejected object or parallel successor work.

## r26 inherited private-service source-guard preflight

Before another focused NATS run, add exact local-only mode `bash tools/check-phase285-witness-conformance.sh --self-test store-proxy-source`. The first and only pre-RED change may route that mode to the existing positive and mutation guards without altering either guard or its anchors; running it must then fail specifically at the stale connection-cardinality invariant, not at option parsing, missing execution, Docker, NATS, or another source fragment. After that RED, repair the guards and mutations. The mode must invoke the inherited private-service source guard once in positive mode and once in mutation mode without Docker or NATS, require both invocations to execute nonzero, and print `store_proxy_source_guard passed=1` plus `store_proxy_source_guard_self_test mutations=44 unique=44 passed=1`. This preflight is mandatory after every post-RED checker or manifest edit and before any expensive selector; it exists to expose the entire lexical/source-guard integration surface in seconds rather than one anchor per expensive combined run.

The positive guard must extract the `StoreProxyServiceRunner<S>` implementation and prove all four conditions independently: exactly one public `pub async fn start` signature whose first parameter is `connection: StoreRoleConnectionV1`; exactly one private `async fn start_inner` signature with the same typed first parameter; exactly two occurrences of that typed parameter inside the implementation; and exactly one ordered `Self::start_inner(connection, service).await` delegation between the two signatures. It must also retain the existing requirement that the Ready-binding comparison precedes stream lookup and queue subscription. A generic whole-file `count == 1`, an unscoped substring, or treating either signature as redundant is forbidden.

Replace the old single `arbitrary_store_client` source-guard mutation with three separately named mutations: `arbitrary_public_store_client` changes only the public signature; `arbitrary_private_store_client` changes only the private `start_inner` signature; and `omit_public_start_delegation` changes only the exact delegation. The validator must emit distinct stable reason codes `public_start_signature`, `private_start_inner_signature`, and `public_start_delegation`, and each self-test mutation must compare the observed reason to its exact expected reason rather than accepting an arbitrary `ValueError`. Each mutated combined source/config/integration/harness/compose digest must be unique and the positive validator must run. The inherited source-guard inventory therefore changes from 42 to exactly 44; no existing mutation may be dropped. These source-validator controls complement rather than replace the seven compiled A1 callsite mutants, including both executable public/private start-delegation bypasses.

The stopped diagnostic tree is read-only evidence until r26 is accepted and pushed. A later authorized attempt may resume it in place only after proving HEAD is accepted base `5ade79141a2f8249ef084d24fcfd5ef12251e48e`, `git write-tree` is exactly `39b515f29ca134d48571765211ac3be43356fb3c`, the index contains exactly the eight A1 paths with the reviewed digest, and there are no unstaged or untracked files. It must first record the exact pre-repair RED from `--self-test store-proxy-source`, then edit only the checker and manifest for this repair. Any mismatch requires reconstruction from the accepted base. The final candidate remains one direct-child commit of `5ade791`; the diagnostic tree itself is never committed or pushed.

## 1. Harness-owned bounded response loss

Remove `PublicWitnessPublishSuppressionV1`, `PublicWitnessServiceExecutionReceiptV1`, `start_with_publish_suppression`, deadline-control exports, and every related re-export from the library. No debug, release, default-feature, or all-feature library artifact may expose reply-suppression authority or an unbounded channel.

Implement lost response entirely in `service_checkpoint.rs` plus a conditional relay mode in the owned NATS harness, never through a production suppression seam, in-process dispatcher call, or manual request serialization. Normal harness mode remains the accepted three-account/four-principal topology. Relay mode materializes an isolated fourth `relay` account and fifth relay principal with exact fixed transforms for nine public and three private subjects and no wildcard, raw KV, JetStream, signing-key, or management authority.

For each public operation, `RuntimeWitnessClient` publishes unchanged canonical bytes on the ordinary fixed public subject with original requester reply route `R`; the runtime account has only a transformed service import to `swarm.governance.witness.relay.v1.<operation>` in the relay account and no direct witness route. The capacity-one relay reserves its evidence slot, consumes exactly one request, records exact subject/body/nonce/digest, and republishes those unchanged bytes over its sole transformed import to the ordinary fixed subject in the witness account with fresh sink `B`, where `B != R`. The unchanged `PublicWitnessServiceRunner` handles that request. For each resulting private call, the witness account has only a transformed service import to `swarm.governance.witness.relay.store.v1.<operation>` in the relay account and no direct store route. The same relay forwards the unchanged private request exactly once over its sole transformed import to the ordinary fixed private subject in the store account with fresh sink `C`, authenticates the typed private response, records the ordered private subject/request/response bytes and digests, and forwards it exactly once to the witness caller's original private reply route. The unchanged `StoreProxyServiceRunner` and real recording atomic store perform the backend work.

After the public runner completes, the relay receives and authenticates the canonical signed public response on `B`. Its harness-only receipt binds scenario ID, operation, public request nonce/digest, public response bytes/digest, the complete ordered private relay transcript, exact recording-store ReadEntry/CompareAndSwap/ReadEntry sequence, CAS counts, store generation/state digest, public/private runner and connection identities, and `R`/`B`/`C` inequality. On the first lost-response execution it performs zero publishes to `R`; the independent requester times out, possibly applied mutation maps to `OutcomeUnknown`, and retries remain zero. Normal replay uses a fresh relay instance, repeats the route, and forwards the authenticated `B` response to `R` exactly once.

The harness must parse the literal `--relay-service-checkpoint` mode before any credential, account, import/export, or Compose configuration generation, remove that one flag, generate relay topology, and export a fresh root-bound mode/topology token to the child checker. The focused Task 03A gate must invoke that flag; ordinary selectors and harness self-tests use normal mode. Relay mode must prove exact `accounts=4 principals=5 public=9 private=3 relay_public=9 relay_private=3`, and the child must reject a missing/stale/wrong-tree token. Normal mode must reject relay state/token and remain exact `accounts=3 principals=4`. Kill omitted, duplicated, or substituted flags; ambient-only enablement; command-string sniffing; relay topology on a normal selector; normal topology on the focused selector; and child-created or cross-invocation mode tokens.

If the capacity-one slot is full, closed, missing, or cannot accept the complete receipt, the relay refuses to arm loss and forwards the public response normally. The focused checker must kill manual-requester serialization, public or private relay bypass, `R == B`, `B == C`, duplicate/direct witness or store delivery, wildcard permission, unrelated-inbox timeout, missing/reordered/substituted private trace, request drop before either handler, pre-handler/pre-response receipt, fabricated receipt, delivered first public reply, capacity greater than one, unbounded channel, repeated arm, omitted sink receive, and any production/debug/all-feature suppression symbol.

## 2. Real-constructor deadline evidence without legacy weakening

`NatsPublicWitnessStoreProxyClient::new` remains source-compatible and must itself require `request_deadline_millis == STORE_RESPONSE_GRANT_MILLIS`; reject 0, 2,999, 3,001, `u64::MAX`, predicate deletion, comparison substitution, constant substitution, validator-delegation deletion, and delegation to an alternate permissive predicate. Production callers continue to pass the constant. Update the single inherited direct-client fixture in `full_service_path.rs` from 1,000 to exactly 3,000 and add the exact integration target `full_service_path_constructor_deadline_is_exact_and_receipt_bound`. Under the normal three-account/four-principal NATS harness, that target invokes the real public constructor with exactly 0, 2,999, 3,000, 3,001, and `u64::MAX`, requires only 3,000 to succeed before any store operation, and writes one fresh tree/token/case-bound receipt containing the ordered five inputs, observed result classes, and canonical digest. Calling a validator directly, scanning source literals, or running an unjoined full-service test is not evidence.

The focused A1 checker runs through the normal NATS harness and owns three exact positive targets. Both production runners factor their complete real subscriber loops through one crate-private `start_inner`, and each shipping public `start` delegates to that corresponding function. Public constructors and ordinary production construction always place `NoopSubscriberAdmissionObserverV1` in private runner state. Library-internal `#[cfg(test)]` runner builders may instead place a capacity-one post-enqueue recorder in that same private state, but they return the ordinary runner and expose no alternate serving entrypoint. Exact internal target `deadline_state_machine_tests::subscriber_callsite_is_receipt_anchored_and_mutation_sensitive` must invoke `PublicWitnessServiceRunner::start` and `StoreProxyServiceRunner::start`; calling `start_inner` directly is forbidden. Through those public entrypoints it blocks the first recording backend/proxy operation, publishes a second request over NATS, consumes the bounded post-enqueue receipt proving acceptance, holds the request queued beyond its fixed receipt deadline, releases the first request, and proves the second expires with zero second backend call and zero second publication. The observer is post-fact, infallible, synchronous, bounded, and cannot affect enqueue, deadlines, branching, replies, or authority. Its fresh tree/token/case-bound private/public callsite receipt digests are mandatory fields in `private_queue_expired` and `public_queue_expired`; helper-only rows cannot validate without them. The checker separately joins the first exact library test's worker-budget receipt with `full_service_path_constructor_deadline_is_exact_and_receipt_bound`'s real-constructor receipt to mint `deadline_budget_constructor_exact`. Every receipt binds the same frozen tree, fresh harness token, case ID, launcher, and manifest. Omission, reuse, cross-tree/case/token substitution, result fabrication, digest refresh, or a live target that performs unauthorized store work fails before the affected row exists. In normal/debug/release/all-feature libraries, `start_inner`, the observer trait, and the Noop implementation remain private and downstream-unnameable; only the `#[cfg(test)]` builder, recorder, test events, receipt controls, and setters must be absent from non-test artifacts and symbol inventories.

The legacy `request_deadline_millis` fields inside bound service configuration remain bounded identity inputs for Task 04-02 compatibility but cannot determine r14 receipt, handler, proxy-exchange, or response-publication deadlines. The conformance checker must reject any production use of those fields in the deadline state machine. Receipt deadlines remain the exact closed constants 2,000/3,000/10,000/12,000 and every asynchronous operation is bounded by the original absolute deadline or the exact private-exchange subdeadline, never a stale relative duration.

## 3. Nine observations of the production state machine

Delete `private_deadline_control`, `public_deadline_control`, and helper-created `DeadlineControlEvidenceV1` counters. The first eight ledger rows must be derived from recording implementations at the same seams used by production; the ninth is a separate closed budget/constructor oracle:

1. `private_queue_expired`: actual bounded private ingress dequeues after its receipt deadline; zero preflight/store/publish.
2. `private_preflight_expired`: actual decode/verify/admission/preflight gate advances the deterministic clock to expiry; zero store/publish.
3. `private_store_crosses_deadline`: actual recording store call begins before the original deadline, crosses it under `timeout_at`, records one store call, and publishes zero replies.
4. `private_response_enqueue_expired`: actual store response completes before expiry, deterministic clock expires before the real enqueue/publish seam, records one store call and zero publication.
5. `public_queue_expired`: actual bounded public ingress dequeues expired work; zero private/store/publish.
6. `public_private_exchange_crosses_deadline`: actual dispatcher invokes the recording private proxy once and its absolute exchange timeout fires; zero public publication/retry.
7. `public_response_enqueue_expired`: actual dispatcher completes and signs before expiry, deterministic clock expires at the real response enqueue/publish seam, zero publication/retry.
8. `post_cas_timeout_outcome_unknown`: actual recording CAS applies once, the confirming/private response crosses the absolute deadline, requester observes `OutcomeUnknown`, and CAS/retry counts are exactly 1/0.
9. `deadline_budget_constructor_exact`: the checker joins two fresh receipts for the same frozen tree/token/case. The exact library FQN contributes only the production worker-budget constants 2,000/3,000/10,000/12,000 and 1,000/1,000/2,000 reserve arithmetic. The exact normal-NATS integration target contributes actual calls to `NatsPublicWitnessStoreProxyClient::new` at 0, 2,999, 3,000, 3,001, and `u64::MAX`, with only 3,000 accepted and zero store operations. The canonical row contains both ordered observation sets and their digests, states `live_nats_grants_proved=false`, and makes no claim about server-side `allow_responses` duration or cardinality; A2 owns and live-proves those grants.

For each public/private path, factor one crate-private subscriber-admission function called immediately after `subscription.next()` and before any other per-message statement. That function alone validates the raw subscription message, captures the receipt instant from the injected monotonic clock, derives the absolute deadline from the fixed production budget, constructs the typed `PublicIngressMessage` or `PrivateIngressMessage`, and performs the bounded enqueue. The NATS subscriber loops may only delegate to it; they cannot capture, replace, extend, or recompute a deadline or construct an ingress value themselves. The exact library FQN must submit the same raw message type to those functions with the paused test clock and real bounded senders. Then factor one crate-private worker body per path for the single dequeue-to-publish transition. Each body accepts a private, infallible, synchronous `WorkerTransitionObserverV1` whose production instantiation is `NoopWorkerTransitionObserverV1`. The observer has no result, async work, mutation handle, reply authority, configuration input, or control over branching; it can observe only post-fact transition events. Emit exact typed events from the real code at dequeue, post-preflight, proxy/store begin/end, CAS attempted/applied observation, response-enqueue attempt, and publish attempt. Normal/debug/release/all-feature external APIs expose neither subscriber seam, observer, nor events.

Put `DeadlineTestClockV1`, `DeadlineTestGateV1`, `RecordingWorkerTransitionObserverV1`, the event-to-row reducer, authenticated Ready/in-memory-store/public-dispatcher test fixture, worker-budget receipt serialization, and the exact test in a library-internal `#[cfg(test)] mod deadline_state_machine_tests` in `src/lib.rs`; its fully qualified test name is `deadline_state_machine_tests::deadline_state_machine_is_receipt_anchored_and_mutation_sensitive`. The test drives raw received messages through both exact production subscriber-admission functions, their real bounded mpsc senders/receivers, the exact production worker functions, real preflight/dispatcher/proxy/store/CAS logic, real enqueue/publisher abstraction, and paused Tokio clock. It emits exactly eight transition rows plus a separate budget-only receipt; it cannot mint row 9. CAS facts come from the recording `WitnessAtomicStore`; publication facts come from the recording publisher; all other facts come from the production-emitted event sequence. The reducer may count and validate events but may not invent, default, repair, or directly assign a row counter. The ledger writer only serializes reducer output.

The focused checker must run through the normal NATS harness and execute exactly three positive targets: (1) `cargo test -p swarm-governance-witness --lib --locked --offline deadline_state_machine_tests::deadline_state_machine_is_receipt_anchored_and_mutation_sensitive -- --exact --test-threads=1`; (2) `cargo test -p swarm-governance-witness --lib --locked --offline deadline_state_machine_tests::subscriber_callsite_is_receipt_anchored_and_mutation_sensitive -- --exact --test-threads=1`; and (3) `cargo test -p swarm-governance-witness --test full_service_path --locked --offline full_service_path_constructor_deadline_is_exact_and_receipt_bound -- --exact --test-threads=1`. Each target must run once with zero failed/ignored and the pinned inventories must contain no second match. The checker consumes eight transition rows plus the budget receipt from target 1, binds target 2's private/public post-enqueue callsite receipt digests into rows 1 and 5, and joins target 3's constructor receipt into row 9 after validating identical tree/token/case/launcher/manifest bindings. It must report 9/9, kill exactly 18 canonical-ledger mutations, compile default/all-feature debug/release non-test libraries, pass a positive downstream client build, and kill exact private-API probes plus artifact-symbol checks for exactly 10 boundary receipts.

In confined exact-tree copies, target 1 must kill exactly 19 coherent compiled mutants: the original receipt reset, queue-check deletion, preflight-check deletion, pre-store-check deletion, relative-timeout substitution, late publication, retry, CAS-ambiguity downgrade, fabricated counter, helper bypass, worker-budget substitution, private/public reset after dequeue, and private/public subscriber capture reset, capture-after-queue, and overlong-deadline substitutions. Target 2 must kill exactly seven coherent compiled real-loop mutants: private and public callsites inline cardinality-preserving typed ingress construction with a fresh/overlong deadline while retaining an unreachable helper reference; drop request two before bounded enqueue; fabricate a receipt without successful enqueue; capture/emit the receipt only after acceptance with a refreshed deadline; replace `PublicWitnessServiceRunner::start` delegation with a separate fresh/overlong legacy subscriber loop while leaving the correct private `start_inner` unreachable; and apply the same delegation bypass independently to `StoreProxyServiceRunner::start`. Target 3 must kill exactly five coherent compiled mutants: constructor predicate substitution, constructor-receipt omission, constructor-result fabrication, real-constructor delegation deletion, and delegation to an alternate permissive predicate. Every mutant must compile, execute exactly one copy of its declared exact target, and fail for the intended behavioral reason; compiler failure, zero execution, generic failure, source scanning, or same-engine lexical presence is not mutation evidence. All 31 full mutated-source digests must be unique.

## Exact r35 cumulative scope and serial gates

Across A1, A2a, A2b1, A2b2, A2b3, A2c, and A3, the cumulative candidate owns the original fifteen paths plus `crates/swarm-governance-witness/tests/full_service_path.rs`: exactly 16 sorted paths, digest `1decd3b39a6433d9358a10289d6886f67211b2560563b417381273dd1662d614`. The per-slice path sets and digests above are independent acceptance boundaries, not advisory partitions. A2a is nine paths/digest `919bf3addbcdbf8625f055f9c8737f22aa5b9d3dd99130fe8e62fc981b65d4c9`; A2b1 is four/digest `ccd00d773edc3826aecd28bf6d451d0d2a5323ee766224e01b5633cb3815070a`; A2b2 is five/digest `964b80e1d926ccc78f1e2f487edb84169a01f1cb6a1eadcf3b294c3234ce98ff`; A2b3 is eleven/digest `4d9d95a9253af2cede7f42175322169a4deb3fafebc8fb0d58bcb845cd992adf`; A2c is four/digest `8b37d06097626b9d3f302ea1053ced7852e5553f095c8ead99964c8bca66b6c5`. A2b3 and A2c preserve the exact fourteen-path Task02 cumulative digest `4e6629733654f7b4b6f1233bfaaef96a0fede0368620380c8b97f9d75bb18664`. The 58-row registry remains unchanged; exactly two bins exist; init and three-bin dependency closure remain 04-03B scope.

Root freezes and independently reviews A2a, A2b1, A2b2, A2b3, A2c, and A3 separately as local production commits, never staged indexes. A2b1 reconstructs from accepted A2a; rejected `ef4c043` is diagnostic-only. A2b2 and A2b3 each start only after both exact predecessor refs are fetched. A2b3 executes normal-grant and relay-positive gates but cannot claim the final independent oracle. A2c adds no production behavior: it requires the positive immutable ledger to pass the same independently decoded/recomputed predicate first, rejects ambient corpus controls, freezes the exact 38-name inventory, and runs every coherent mutant through a distinct real-target selector with stable reason and vacuous=0. A3 reruns all accepted seams before the eighteen recovery rows and abort matrix. Every task mechanically verifies both predecessor origin refs, exactly one parent, and exact predecessor parentage. Task 04-03B is wave 12. Only an independently accepted commit may be pushed to its named work plus checkpoint refs. No combined monolithic A2 or A1-A3 candidate is authorized.
