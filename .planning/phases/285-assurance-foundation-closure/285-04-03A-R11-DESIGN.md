---
phase: 285-assurance-foundation-closure
plan: "04"
task: "285-04-03A"
type: bankable-execution-decomposition
status: ready-for-immutable-review
revision: r72
supersedes_planning_commit: 493e8a2451107ef9142f7e17ecdd77f5a6427384
production_base_commit: ed77183d3d13d046e446d2837e8c5d21d1d1765d
production_base_tree: bdfadfa1e6872596e2f37da4fe6cbebf66ee66d4
frozen_incomplete_tree: ec278370395cda1b70589b9e7e59e8c2a8d93c67
owned_path_count: 7
owned_paths_sha256: 272eb9961675aa24a93164825a64c2db7930d1cfa544a520fddd7d085a10e9ab
---

# Task 285-04-03A r72 cumulative blocker decomposition

This stable-filename addendum is revision r72. Immutable r71 planning commit
`493e8a2451107ef9142f7e17ecdd77f5a6427384` / tree
`6ab1db79175444d67e6d539d8995d4db87763aa9` was rejected
P0/P1/P2=`0/1/0` solely because A2b3's live validation status still said
`r69 required`. R72 changes that cell to r72 and updates only the review
ledger. No plan command, architecture, task scope, digest, or production byte
changes.

This stable-filename addendum is revision r71. Immutable r70 planning commit
`f7dc0c5a228bed248326101bdfea9db1071417e0` / tree
`aba63457aa36375dd27e3ed29f966997937d102d` was rejected
P0/P1/P2=`0/2/0`. Its four substantive execution fixes passed. R71 changes
only two literals: `InvalidSubject` remains `Configuration` and cannot become
`Unavailable`; current authorization routing names r71 rather than rejected
r69 or r70. Architecture, commands, task scopes, digests, waves, and successor
blocks remain byte-for-byte unchanged except those literal corrections.

This stable-filename addendum is revision r70. Immutable r69 planning commit
`2bcd25b1616425c9c681ab62255b7fe356d8ad9c` / tree
`dae3cd1d99e66da3332abb71480f1811026fc4c7` was reviewed read-only and
rejected P0/P1/P2=`0/5/0`, confidence high. Its architecture, four serial
candidate scopes, path digests, waves, deadline model, A2c freeze, and
candidate-only sequencing were valid. R70 corrects only the five executable
contract defects: narrow `Unavailable` to server-proven zero interest while
keeping invalid configuration typed `Configuration`; converge SIGINT/SIGTERM on one idempotent bounded
drain; supervise the exact test inventory; brace all Git object expressions;
and reject ambient indexes while pinning the real and clone indexes. No new
task, path owner, worktree, production edit, or planning redesign is allowed.

This stable-filename addendum is revision r69. Uncommitted r68 is abandoned and
authorizes no implementation. A full cumulative review of banked A2b3 found
five independent P1 defects: blanket `Other -> Unavailable`, an unmodelled
NATS-grant/responder-clock gap, abort-only relay replacement, red canonical CI,
and serving processes that can remain alive after capability loss. It also
found three P2 defects: count fields bounded by a byte constant, local enqueue
mislabelled as server publication, and ordinary secret/config path reads. A2b3
remains banked but red; A2c remains frozen at exactly three dirty paths.

R69 resolves these in four serial direct-child candidates from A2b3: R1a owns
transport ambiguity, truthful enqueue evidence, and real expired-grant replay;
R1b owns service/task/connection supervision, operational counts and checked
memory budget, and descriptor-bound zeroized secret/config reads; R1c owns
self-contained workspace tests, exact ignored-test harness registration,
current two-bin dependency closure, and witness-integrity gate wiring; R1d owns
only quiescent relay teardown and exact server-observed replacement readiness.
R1a-R1c may be independently reviewed and remotely banked only as `-candidate`
objects. The accepted frontier stays A2b2 until exact cumulative R1d passes
ordinary workspace tests, explicit live tests, dependency and wiring gates,
hosted CI, and immutable hostile P0/P1/P2=`0/0/0`.

The response-grant decision is explicit. NATS starts `allow_responses.expires`
in the server before async-nats yields a message; no responder API exposes the
remaining duration. A responder-local timestamp cannot prove grant-fit.
`ReceiptDeadlineV1` remains a first-observable-dequeue processing budget. If
the server grant expires first, local `publish().await == Ok` means only
`enqueued`; the requester must return `OutcomeUnknown` and exact replay must
recover the one applied or unapplied decision without another CAS. R1a proves
both exact 3,000-ms private and 12,000-ms public pre-poll stalls through real
NATS. `NoResponders` remains `Unavailable`; `TimedOut|Other` are
`OutcomeUnknown`; `InvalidSubject` is configuration failure.

R69 is the final architecture reset for this blocker. Each candidate has one
exact ownership digest, one red-first implementation window, one immutable
review, and a two-hour stop. No new registered worktree, parallel successor,
production-labelled intermediate ref, A2c byte import, or planning micro-patch
is allowed. If R1a cannot satisfy its real expired-grant control, stop and
redesign only the response transport rather than revising the whole plan set.

This stable-filename addendum is revision r68. Immutable r67 `ce206ae400c23d7f47480c89999894b59ac934ab` / tree `bfbfdf453fc034e05c85181b626282dcdf34fd61` was rejected P0/P1/P2=`0/2/0`. Its successful replay did not execute the `NoResponders` or `Other` branches, so error-collapse/acceptance mutants were vacuous; fixed-sleep substitution had no deterministic delayed-readiness case; and explicit pre-drain flush could be observationally redundant because async-nats drain already drains subscriptions and flushes. Its sole active command also omitted the literal detached candidate, absent target, compiler JSON, exact executable/hash/argv runner, same-binary proof, and both immutable A2b3 predecessor refs. R68 changes only those two findings; r66's consumed RED, exact four-file ownership, frozen A2c bytes, no-shipping-change rule, and serial successor block remain.

R68 removes pre-drain flush as a claimed independent requirement. The actual live FQN now executes four reason-bound cases through the same real request seam: imported subject with no relay responder must produce typed `no_responders` and public `Unavailable`; a separately authenticated runtime client drained before request must produce typed `other` and public `Unavailable`; readiness held behind a test-only oneshot must remain pending while the exact private subscription set is absent and complete only after release plus server observation; final replay must produce typed `response`. A closed `require_replay_response` accepts only `response`, so both actual error observations are explicitly rejected rather than lying dormant. The twelve compiled controls are: abort-only stop; omit one task await; omit public drain; omit private drain; accept one old ID; delete public set equality; delete private set equality; exact zero-duration fixed-sleep readiness; collapse no-responders to other; collapse other to no-responders; accept no-responders as replay success; accept other as replay success. Every control executes the exact four-case live FQN from its own absent target and must die only at its named relation. The mutation parent retains each mutated source, executed binary, canonical runner receipt, and bounded transcript; an independent parser in the active command verifies their hashes, exact argv/counts/reason, and unique source/executable/target identities before accepting the 12/12 summary.

The then-active r68 command literally checked both A2b3 refs at `ed77183...`, the A2b3R1 candidate's direct parent/tree/four-path digest, and fail-closed Docker residue; created one validated unregistered shared clone detached at the candidate; required one initially absent target; bounded locked/offline compiler JSON; selected exactly one confined executable; hashed it; wrote a canonical mode-0600 exact-argv runner receipt before exec; and required the same executable/hash plus exact FQN `1/0/0/18`, teardown/readiness/outcome/grant/relay receipts, and exactly two topology rows. That command is historical and superseded by r69. R69's R1d instead inventories the exact compiled test executable and derives the one-test filtered count from that immutable inventory, because R1a and R1b legitimately add tests before R1d. Only after that positive passes does a second NATS lifecycle execute the twelve-mutant selector, whose children each own separate absent-target/source/executable/argv receipts. Terminal clone/candidate/ref/residue checks are mandatory. R69 is command authority, not prose inheritance from r66 or r68.

This stable-filename addendum is revision r67. Immutable r66 `070baabfda3493839ccf63bc6a8400016f47433b` passed independent review and its one fresh-target authority was consumed. The detached exact-A2b3 build selected one test executable SHA-256 `3d9d359fd54c579575148d438f2070e77c7d11538292aaca88443f2d3cfc4aff`; every forbidden diagnostic token was independently absent and every positive detector control passed. The exact relay FQN then failed `0/1/0/18`, exit `101`, in `38.26s`: the first request completed the real worker/store/receipt/suppression path and returned requester-observed `OutcomeUnknown`, but the request immediately after abort-only relay recreation returned `Unavailable`. This is accepted-source RED, not a stale binary result. Async-nats exposes `TimedOut`, `NoResponders`, and `Other`; the runtime maps the latter two to `Unavailable`, so r67 does not invent the missing subtype.

### r67 A2b3R1 precedence rule

R67 consumes and supersedes r66's no-edit replay. It authorizes one bounded test-assurance repair child of exact banked A2b3, never another whole A2b3 implementation. Ownership is exactly `crates/swarm-governance-witness/src/lib.rs`, `crates/swarm-governance-witness/src/runtime_client.rs`, `tools/check-phase285-witness-conformance.sh`, and `tools/fixtures/phase285-witness-integrity.json`, path digest `142ddd609045739262806bb564cfa6ec21184175225f4f139f3ea495ab3dde07`. All Rust behavior is crate-private and `cfg(test)`; shipping request errors and public APIs do not change. Because the existing implementation worktree contains the frozen partial A2c candidate, implement in one ephemeral unregistered shared clone rooted at exact A2b3, with an initially absent target; do not create another registered worktree and do not import any A2c byte.

`LiveRelayLegsV1` must retain both relay clients. `stop_and_confirm` aborts every one of the nine public plus three private task handles, awaits each handle and accepts only cancellation, flushes each retained client after subscriber drop, drains both clients, and then uses a bounded monotonic predicate loop over fail-closed `/connz?auth=1&subs=1` observations until both old client IDs are absent. It may yield between observations but may not use a fixed sleep as readiness authority. Replacement startup flushes both new clients and requires two fresh, distinct, nonzero identities whose server records bind the relay account/user and whose exact subscription sets are respectively the nine `swarm.governance.witness.relay.v1.*` subjects and three `swarm.governance.witness.relay.store.v1.*` subjects, with no omission, addition, wildcard, duplicate, or cross-leg subject. Only then may replay publish.

The runtime test seam records the exact async-nats request outcome as a closed `response|timed_out|no_responders|other` value while retaining the existing public mapping (`TimedOut` to `OutcomeUnknown`; `NoResponders|Other` to `Unavailable`). The replay must record `response`; accepting either erased error is forbidden. On any red result it must persist the exact subtype before the test fails, preventing another erased-error loop. The checker executes coherent compiled controls for synchronous abort-only teardown, omitted task await, omitted post-drop flush, omitted drain, accepting an old client ID, deleting public subscription equality, deleting private subscription equality, replacing predicate readiness with a fixed sleep, collapsing `NoResponders` with `Other`, and accepting either error as replay success. Every mutant must compile, execute the same real seam from its own absent target, and die at its named relation; compile failure, timeout, zero-test, direct expected-error, and lexical-only results do not count.

Acceptance reruns one live positive from an exact fresh target and executable receipt inherited from r66. It requires the teardown line `tasks_joined=12 old_absent=2 drained=2`, readiness line `new_present=2 public_subscriptions=9 private_subscriptions=3 wildcard=0`, request outcome `response`, exact FQN `1/0/0/18`, grant and relay receipts, and exactly two identical topology summary rows—one harness projection and one live-test projection. R66's erroneous one-row topology predicate is historical and cannot authorize success. Freeze one clean direct-child A2b3R1 commit, run focused/static/package/clippy/format/diff gates, obtain immutable hostile P0/P1/P2=`0/0/0`, then push/fetch `checkpoint/v179-phase285-plan04-task03a2b3r1-production`; only that object becomes A2c's parent. Failure or the two-hour stop ends the slice without retry or expansion.

This stable-filename addendum is revision r66. R63 appeared green and banked A2b3 `ed77183...`, but later A2c execution proved the shared cached lib-test binary SHA-256 `ab563048...` contains diagnostic-only `DeliveryTimeout` absent from accepted final11 source SHA-256 `3dac41d...`. Cargo had not rebuilt that unit-test target, and the A2c invocation also passed the commit hash where the tree pin was required. Neither the later green nor failure is evidence over accepted bytes; r63's live success lacks source-to-binary provenance. Immutable r64 was rejected 0/2/0 for prose-only authority and a concatenated fingerprint. Immutable r65 was rejected 0/2/0 because its plan identity was substitutable and its Docker residue queries failed open. R66 authorizes no source edit. Its literal replay binds exact reviewed r65 commit/tree, makes every Docker query failure fatal, retains compiler-to-runner identity, and checks all five tokens separately with one positive detector control per token. Only reviewed success reaccepts banked A2b3. A later window may then resume A2c with four real compiled reservation-seam mutants and 34 real integration-oracle artifact mutants.

### r66 precedence rule

R66 supersedes the expired r63/A2c windows and rejected r64/r65 plans while preserving immutable A2b3 commit/tree/scope and both remote refs as banked, not accepted. Before any replay require independent r66 P0/P1/P2=`0/0/0`, exact r65 checkpoint `0b5040e4e76ed9dd2a53dc5c631b52f65de6be34` / tree `93d9e57d50ac3856918d1d4443ca917849ffca94`, r66 as its direct-child work-ref authorization ledger, and a fresh window. The task's literal `r66_a2b3_provenance_replay` command is the sole reacceptance path. It first equates supplied/checkpoint/authorization-parent plan identity to that literal object. Every Docker container/volume/network query must return zero before its captured output may be tested empty; this is required at preflight, terminal, and EXIT. The command then creates one validated unregistered shared clone, detaches exact A2b3, requires clean exact tree/parent/path digest, and starts with an absent clone-local target. Bounded locked/offline Cargo JSON selects exactly one confined test-profile library executable; five independent forbidden-token predicates and five independent synthetic controls run. A host-target runner creates exactly one mode-0600 canonical executable/hash/exact-argv receipt before exec. Relay-positive runs once with correct tree/launcher/manifest, same target, and diagnostic variables absent. Require unchanged executable/runner hashes, FQN `1/0/0/18`, exact topology/grant/relay summaries, and successful empty residue queries. Any failure stops without retry/edit/acceptance; EXIT moves only the validated root to Trash. Independent evidence review `0/0/0` alone restores A2b3 acceptance; no source commit or ref changes.

The stopped A2c candidate is partial evidence only: three uncommitted owned paths, exact hashes recorded in freeze, genuine RED retained, no mutation execution, manifest unchanged. It may be resumed only in a separate later window after A2b3 reacceptance. Keep the four-path A2c scope/digest. Replace the four serialized-artifact reservation substitutions with four compiled source mutants of the actual receipt reservation/disposition sequence. Each uses its own initially absent target and recorded source/executable hash, executes the same internal real seam and external validator, and must be killed only at the named unit-token, post-suppression-construction, non-capacity-one/unbounded, or reservation-failure suppression relation. The other 34 rows use the one immutable correctly produced live ledger but each invokes the real integration oracle. Exact arithmetic `4+34=38`, unique/executed/killed `38`, vacuous `0`; positive runs first. Shared compiled targets, mtime sleep as freshness authority, commit-as-tree pins, ambient selection, direct expected-error shortcuts, and helper-authored counters are forbidden. A2c still requires one direct-child commit, full gates, hostile 0/0/0, and exact refs before A3.

This stable-filename addendum is revision r63. The accepted r62 diagnostic validly returned `not_reproduced`: public solo, private solo, and concurrent request deliveries all passed under one fresh four-account relay topology. It did not execute full accepted/delayed/expired grants or later receipt/worker/store/replay semantics. R63 therefore authorizes no repair. It is a no-production-byte validation decision: replay the original r56 relay-positive selector once against unchanged exact final11; only success permits the remaining r56 freeze gates.

### r63 precedence rule

R63 supersedes every expired r56-r62 execution window but preserves exact final11, accepted A2b2, r56 carried receipts through normal grants, r62 non-acceptance, and every no-new-worktree/successor block. Before and after every command, run the accepted `verify_final11` identity gate against base `30eb13c57a3dfdd54ae1eb9ee8fdd75a88067ef1`, alternate index `/private/tmp/phase285-a2b3-r55-prospective.LrPTQI/index`, tree `bdfadfa1e6872596e2f37da4fe6cbebf66ee66d4`, exact eleven-path digest `4d9d95a9253af2cede7f42175322169a4deb3fafebc8fb0d58bcb845cd992adf`, checker/manifest/launcher/lib identities, empty real index, no candidate untracked paths, exact A2b2 refs, and fail-closed zero Docker residue. Require r62 checkpoint `1fcd49930a61320bf8e15857cfcc523c186b4af4`; require r62 result ledger `4c1445380e981888d9d3cd436858eb4268275133` as the direct parent of immutable r63; and at execution require the work ref equal the separate root-authored r63 authorization commit recorded in STATE. Explicitly remove ambient `PHASE285_GRANT_DIAGNOSTIC`, `PHASE285_GRANT_DIAGNOSTIC_PATH`, `PHASE285_GRANT_ONLY`, and `PHASE285_RELAY_TOPOLOGY_TOKEN` before the original replay command.

Run exactly once the original r56 relay-positive command: relay harness; exact launcher/manifest/tree pins; selector `--focused-service-checkpoint-relay-positive`. This is a replay, not a carry from r62. Preserve its complete output and require exact FQN materialization, `1 passed; 0 failed; 0 ignored; 18 filtered out`, all relay ledger/receipt/grant/timing counts owned by that selector, harness exit `0`, clean lifecycle, and no harness terminal. Any nonzero, timeout, vacuity, residue, or identity drift ends r63 immediately without retry, edit, staging, commit, push, extension, or causal claim.

Only after that green replay, run each still-incomplete r56 gate once in order, bracketed by `verify_final11`: witness all-target locked/offline no-run; governance serial library suite; strict governance+witness all-target/all-feature locked/offline clippy; full format check; bash syntax; shellcheck; diff check; terminal identity/scope/hash/index/untracked/ref/residue gate. Any failure stops under the same prohibitions. A completely green chain may run `env -u GIT_INDEX_FILE git read-tree bdfadfa1e6872596e2f37da4fe6cbebf66ee66d4` without `-u`, changing only the real index. Require its `write-tree` equal final11, its A2b2-relative cached set equal exactly eleven paths/digest `4d9d95a9253af2cede7f42175322169a4deb3fafebc8fb0d58bcb845cd992adf`, cached diff-check clean, and working tree equal the index; then create one clean direct child of A2b2 with tree final11 and make no post-commit edit. The immutable production commit then requires independent P0/P1/P2=`0/0/0`; only that verdict authorizes the two A2b3 refs to be pushed/fetched. A2c remains blocked until both refs resolve exactly. R63 remains planning-only before its own immutable zero-finding review, checkpoint push/fetch, and a fresh two-hour root window.

This stable-filename addendum is revision r62. Immutable r61 review returned P0/P1/P2=`0/1/0`. R61 closed r60's same-inode content-race defect, but its static-symlink control failed at the initial regular-file lstat even when `O_NOFOLLOW` was deleted. It therefore did not prove that the open flag was necessary. R62 changes only that no-follow control; every other r61/r60 rule remains exact.

### r62 precedence rule

The parser self-test supplies a separate pre-open hook used only by `drop-O_NOFOLLOW/symlink`. Begin with the canonical sidecar at its exact path and an absent fixed sibling backup path. Initial lstat must pass on the regular inode. The hook then uses Darwin `renamex_np(..., RENAME_EXCL)` for no-replace moves: move that inode to the backup path and install at the original path a create-new relative symlink whose exact `readlink` bytes resolve to the backup. Canonical validation next attempts the one open with `O_RDONLY|O_NOFOLLOW|O_CLOEXEC` and must refuse specifically at the no-follow open with `ELOOP`, before reading bytes. A finally path lstat/readlink-verifies and removes only that symlink, uses the same no-replace primitive to restore the original inode/path, then revalidates its exact initial identity and bytes.

The corresponding coherent validator mutant deletes only `O_NOFOLLOW`. Against the identical synchronized specimen it must follow the symlink to the original inode; the hook then removes the verified symlink and restores the path after open, allowing every descriptor double-read, metadata, final-lstat, canonical-record, and process predicate to pass. The outer mutation self-test counts the mutant killed only when canonical validation produces the exact no-follow refusal and the single-flag-deletion mutant reaches otherwise-valid classification. Failure at initial lstat, hook setup, cleanup, or any unrelated predicate is invalid and does not count. The link-count-two and mode-widening controls likewise require their canonical exact refusal and corresponding single-predicate-deletion mutant to reach otherwise-valid classification. Inventory remains twenty-five parser/artifact controls plus one hostile-index control.

R62 is planning-only pending immutable P0/P1/P2=`0/0/0` and exact push/fetch. No live diagnostic, production edit, relay-positive run, retry, A2c, or new registered worktree is authorized before a fresh root window.

This stable-filename addendum is revision r61. Immutable r60 review returned P0/P1/P2=`0/1/0`. Its single descriptor read/hash followed by metadata-only revalidation could miss a same-length, same-inode content rewrite after the read; the advertised `sidecar-after-read` control could therefore pass only with a weaker append or replacement. R61 changes only that artifact-stability proof. R60's record transport, conditional stdout, wrapper argv, fresh relay-token binding, exact process/libtest/panic conditions, clone isolation, and all prohibitions passed review and remain unchanged.

### r61 precedence rule

Open the completed sidecar exactly once with `O_RDONLY|O_NOFOLLOW|O_CLOEXEC`. Before reading, require `lstat(path)` and `fstat(fd)` to identify the same coordinator-owned regular inode with link count `1`, mode exactly `0600`, and length `1..4096`. Snapshot device/inode/uid/gid/mode/link-count/length. Read to exact EOF, hash, and canonical-parse bytes A. Execute no real mutation hook. Re-fstat and require the snapshot exact; seek the same descriptor to byte zero; independently read to exact EOF, hash, and canonical-parse bytes B; fstat again; then final-lstat the path. Require A and B byte-for-byte equal, both hashes equal, both canonical projections equal, every fstat field exact at all three snapshots, and final lstat equal to the descriptor identity and initial path snapshot. Only those twice-proved bytes enter process classification.

The parser self-test supplies a test-only between-read hook. Its `sidecar-after-read` control performs a same-length, same-inode, mode-preserving rewrite from one canonical record to a different canonical account/token record and must fail on A/B bytes and hash before classification. Add independent controls that remove `O_NOFOLLOW` while substituting a symlink, present an otherwise valid inode with link count `2`, and widen mode from `0600` to `0640`. The inventory is exactly twenty-five parser/artifact controls with unique labels and zero survivors plus the separately executed hostile ambient-index control. The live parser has no mutation hook, and the claim is bounded to the terminated diagnostic child and these descriptor/path checks; it does not claim protection from an arbitrary same-UID writer after the final check.

R61 remains planning-only until one immutable P0/P1/P2=`0/0/0` review and exact push/fetch. Only then may root record a fresh two-hour window for one corrected clone run. All r60/r59 no-production, no-retry, cleanup, non-reproduction-not-acceptance, no-relay-positive, no-A2c, and no-registered-worktree rules remain exact.

This stable-filename addendum is revision r60. The sole r59 diagnostic compiled and ran the exact FQN once. Its clone-only code emitted one canonical final11/token-bound `not_reproduced` record, and libtest reported `1 passed; 0 failed; 0 ignored; 18 filtered out`. It is nevertheless invalid evidence. With `--nocapture`, the record appeared between libtest's `test <FQN> ...` prefix and trailing `ok`, so the existing harness's exact materialization validator returned `PHASE285-HARNESS[non-materialized-test:...]`, exit `1`. The outer parser also required `phase285_harness_nonce` from captured stdout even though the harness writes that line only to its deleted private transcript. R59's one-run authority is consumed. Its transcript was 11,696 bytes, SHA-256 `828db205897a7534d5910d2894f8121b45245fea61dae37045fdeb7d1d1c3174`; the result is a clue only and cannot authorize a fix, acceptance, or successor work.

### r60 precedence rule

R60 changes only diagnostic record transport and fresh-token observation. Every r59 clone-index isolation, five-stage public-solo/private-solo/concurrent ordering, 300-second/4,194,304-byte process-group bound, exact status/libtest/panic classification, cleanup, no-retry, no-production, and successor prohibition remains mandatory. The production harness is not patched. The ephemeral clone patch still changes only test-only `src/lib.rs`; it writes exactly one newline-terminated canonical record with create-new semantics to an absent, coordinator-owned, regular-file path under the validated diagnostic root, mode `0600`, maximum 4,096 bytes, then syncs the file before success or the deliberate classified panic. On `not_reproduced`, it emits no diagnostic stdout. On `failed`, only after the sidecar sync it emits exactly the same canonical bytes once to stdout and then executes the deliberate panic, preserving observable record-before-panic ordering on the path where the harness returns Cargo's `101` before materialization validation. Missing, pre-existing, symlinked, multiply-linked, oversized, partial, duplicate-line, stdout/sidecar-divergent, or post-read-changing sidecars are invalid. The supervisor opens the completed sidecar with `O_NOFOLLOW`, requires one-link regular mode/owner/size identity, reads and hashes through that descriptor, then re-fstats and re-lstats the path for unchanged device/inode/mode/length before classification.

Create one ephemeral executable wrapper under that same root with fixed reviewed bytes and mode `0500`. The harness exact-test prefix invokes this wrapper with the original Cargo argv. The wrapper requires relay mode, prints exactly one distinct `phase285_diagnostic_topology_token=<PHASE285_RELAY_TOPOLOGY_TOKEN>` line, and then `exec`s the Cargo argv without mutation. This line may appear in the harness transcript without disrupting `test <FQN> ... ok`; the test itself emits no diagnostic stdout on non-reproduction. The outer validator requires one wrapper token, closed syntax `relay-phase285-phase285-<ppid>-<pid>-<epoch>`, epoch within the supervised invocation interval, and byte equality with the sidecar's `topology_token`. It does not require the private `phase285_harness_nonce` line. The harness overwrites the relay token immediately before invoking the wrapper, so an ambient token cannot satisfy this binding.

The validator self-test retains r59's eleven record/process controls other than its separately executed hostile ambient-index control and adds missing-sidecar, pre-existing-sidecar, success-stdout-record, missing-wrapper-token, duplicate-wrapper-token, stale-wrapper-token, wrapper/record mismatch, out-of-window epoch, noncanonical sidecar, sidecar overflow, and sidecar-after-read mutation: exactly twenty-two killed parser/artifact controls plus one hostile-index control, all with unique labels and zero survivors. The inherited missing/extra/swapped/stale record controls cover both failed stdout and sidecar projections and require their byte equality. A valid non-reproduction requires normal unsignaled harness exit `0`, exact materialized FQN line, counts `1/0/0/18`, no panic or diagnostic stdout, one valid sidecar record with `status=not_reproduced`, and exact fresh wrapper-token binding. A valid classified failure requires normal unsignaled harness exit `101`, counts `0/1/0/18`, one valid `failed` sidecar, its byte-identical stdout record after sidecar sync and before the exact deliberate inner panic, followed only by the exact outer join panic. Any harness terminal, exit `1`, signal, absent/mismatched artifact, or other output is invalid.

R60 is planning-only until frozen, independently reviewed at P0/P1/P2=`0/0/0`, and pushed/fetched exactly. Only then may root open one fresh two-hour window and run the corrected clone diagnostic once. In every outcome, preserve the bounded transcript and sidecar hashes in the ledger, remove only the validated diagnostic root, reverify production final11 and both indexes/refs plus zero residue, and stop. Do not convert a repeated `not_reproduced` result into relay acceptance; it authorizes at most a later planning-only no-edit replay decision. A classified result authorizes at most a later planning-only causal repair and executable late RED. No production edit, diagnostic commit, retry, relay-positive, A2c, new registered worktree, or later task is authorized.

This stable-filename addendum is revision r59. Immutable r58 review returned P0/P1/P2=`0/2/0`: clone operations were not protected from an ambient production `GIT_INDEX_FILE`, and a classified failure accepted arbitrary nonzero termination. R59 closes both mechanically. The coordinator unsets `GIT_INDEX_FILE` before clone creation and pins every clone Git, Cargo, harness, supervisor, and parser subprocess to the canonical clone index. Production alternate-index checks always name their immutable path explicitly. A wrong ambient-index control must prove the clone tree remains final11 and both the production and hostile index hashes remain unchanged.

### r59 precedence rule

R59 supersedes r58 index and process-status acceptance. Every other r58 isolation, five-stage, solo/solo/concurrent, canonical-record, bound, cleanup, no-retry, and successor prohibition remains mandatory. The validator now has exactly twelve negative controls: missing record, extra record, swapped account, stale tree, stale token, generic stage, invalid status, ambient wrong index, `failed+0`, `not_reproduced+101`, wrong libtest summary, and signal-after-record.

A classified failure is valid only when the supervised harness process completes normally and unsignaled with exact exit `101`; libtest reports exactly one running test, the exact FQN failed exactly once, and `0 passed; 1 failed; 0 ignored; 18 filtered out`; the terminal record precedes the deliberate exact inner panic `relay grant diagnostic classified failure`; and the only following panic is the outer `complete receipt thread panicked: Any { .. }`. Missing, additional, reordered, or substituted panic records are invalid. A non-reproduction is valid only on normal unsignaled exit `0`, exact FQN `ok` once, and `1 passed; 0 failed; 0 ignored; 18 filtered out`. The correct record with any other exit, signal, summary, FQN, panic sequence, or harness terminal is invalid and carries no diagnosis.

This stable-filename addendum is revision r58. Immutable r57 review returned P0/P1/P2=`0/3/0`: its command omitted the harness's exact FQN/`18` filtered-count prefix; its inverse working-tree patch was not fail-safe under interruption; and its output had no bounded canonical parser or negative controls. R58 closes those findings without granting a production mutation. It uses an ephemeral shared clone whose index is reset directly to frozen final11 tree `bdfadfa1e6872596e2f37da4fe6cbebf66ee66d4`; the production worktree is never patched. The clone is not a registered worktree, cannot become a production ref, and is disposable even after interruption.

### r58 precedence rule

R58 supersedes all r57 diagnostic execution details and every expired r56 production-continuation sentence. Preserve r57's causal classification: the original failure is pre-worker and ambiguous, so no receipt, `ReadHead`, store/CAS, replay, state/generation, or production fix claim is authorized. Accepted A2b2 and exact final11 identities remain frozen.

After immutable r58 planning receives independent P0/P1/P2=`0/0/0` and is pushed/fetched exactly, root may open one two-hour diagnostic window. Create one `mktemp -d` root outside every repository boundary; `git clone --shared --no-checkout` the repository into it; checkout accepted A2b2; then `git read-tree --reset -u bdfadfa1e6872596e2f37da4fe6cbebf66ee66d4`. Require clone index/worktree tree equality to final11 and no untracked path. Using `apply_patch`, alter only the clone's `crates/swarm-governance-witness/src/lib.rs`. The production worktree must remain exact before, during, and after the run.

The temporary test-only patch replaces the five erased `next_grant_request` failures with a closed enum: `reply_subscription`, `request_publish`, `request_flush`, `delivery_timeout`, and `delivery_closed`. It adds a delivery-only diagnostic driver, leaving the accepted/expired/delayed grant semantics untouched. The driver runs public solo; only on success private solo; only when both pass, public/private concurrently. It emits exactly one prefixed canonical JSON terminal record. A failure record binds schema, frozen tree, fresh harness topology token, `status=failed`, `probe=solo|concurrent`, `case=public|private`, `phase=accepted_request`, exact requester/responder account and subject, and one closed stage. If every delivery probe passes, the sole record instead binds `status=not_reproduced`, `probe=all`, `case=none`, `phase=accepted_request`, `stage=none`, and omits singular account/subject fields. It then returns success only for non-reproduction; a classified failure deliberately fails the test after fsync-free terminal emission. Generic panics and any second record are invalid.

Run the exact FQN once under relay topology, `PHASE285_GRANT_ONLY=1`, `PHASE285_GRANT_DIAGNOSTIC=1`, candidate-tree binding, and the harness prefix `--expect-exact-test service_checkpoint_relay_tests::complete_receipt_authority_and_grants_are_observed 18 --`. Include `--nocapture`. A process-group supervisor enforces 300 seconds and 4,194,304 combined bytes, terminates then kills on bound failure, and persists the bounded transcript outside the clone before parsing. Before the live run, the isolated canonical-record parser must kill missing, extra, swapped-account, stale-tree, stale-token, generic-stage, and invalid-status synthetic controls. Afterward it requires exactly one terminal record, canonical bytes, exact fresh identity, the closed conditional schema, and process status consistent with `failed` versus `not_reproduced`. Timeout, overflow, compile failure, generic failure, partial output, duplicate output, harness-count rejection, invalid all-pass, or parser failure yields no diagnosis.

After the one result, remove only the validated `mktemp` root, reverify production final11 SHA/blob/tree/path/index/ref/untracked identities and zero Docker residue, record the result, and stop. No diagnostic clone commit, retry, production edit, stage, commit, push, new worktree, relay-positive, fix, or A2c work is authorized. R59 may name exactly one causal fix and executable late RED only after a valid r58 record.

This stable-filename addendum is revision r57. R56 proved every final11 gate through normal grants, then the first relay-topology command failed in `2.03s` at `grant accepted request: WitnessOutcomeMismatch`. The failure precedes complete-receipt construction, worker startup, public `ReadHead`, private exchange, store/CAS, replay, state/generation comparison, and relay-ledger validation. The retained transcript contains no publish-permission or disconnect error and cannot distinguish the concurrent public and private grant cases. Static audit found that `next_grant_request` collapses reply-subscription, request-publish, flush, delivery-timeout, and closed-subscription failures into the same `ProtocolError::WitnessOutcomeMismatch`, while `tokio::join!` gives both cases the same outer label. Claiming a witness-state mismatch would therefore be false. R57 authorizes one diagnostic projection, not another production implementation or acceptance run.

### r57 precedence rule

R57 supersedes r56 production continuation and acceptance authority only. Accepted A2b2 remains the durable frontier. Exact final11 tree `bdfadfa1e6872596e2f37da4fe6cbebf66ee66d4`, eleven-path digest `4d9d95a9253af2cede7f42175322169a4deb3fafebc8fb0d58bcb845cd992adf`, checker/manifest/launcher identities, alternate index, empty real index, and all r56 completed receipts remain frozen. No final11 byte, index entry, production commit, production ref, or worktree may be created or changed durably.

After immutable r57 planning receives independent P0/P1/P2=`0/0/0` and is pushed/fetched exactly, root may open one two-hour A2b3 causal-diagnostic window in the existing production worktree. First reverify exact final11 and zero Docker residue. Using `apply_patch`, temporarily alter only `crates/swarm-governance-witness/src/lib.rs`: introduce a test-only closed diagnostic error enum with distinct `reply_subscription`, `request_publish`, `request_flush`, `delivery_timeout`, and `delivery_closed` variants; bind each error to the exact grant case label, requester/responder account, requester/responder subject, and phase; and replace the joined diagnostic path with ordered public-only, private-only, then original-concurrent probes. Each solo probe uses fresh connections and must complete its entire accepted/expired/delayed sequence before the next probe. The concurrent probe runs only if both solo probes pass. No production API, runner, topology, timeout, permission, request, response, receipt, or assertion semantics may change.

Run exactly one relay-harnessed direct diagnostic invocation with `PHASE285_GRANT_ONLY=1` and the exact library FQN. It is diagnostic, not acceptance evidence: do not invoke the integrity checker, relay-positive selector, or any later gate. The first nonzero result must identify one exact case and phase; if both solo cases pass but the concurrent probe fails, it must identify concurrency as the first differing relation. If every probe passes, record that the r56 failure was not reproduced and stop rather than laundering the result into acceptance. In every outcome, restore `src/lib.rs` with `apply_patch`, require its SHA-256/blob and exact final11 tree to return to the frozen identities, require the real index empty and zero Docker residue, then close the window. Do not retry, implement a fix, stage, commit, push, start A2c, or create a new worktree. R58 may be authored only from that exact diagnostic result and must own one causal fix plus an executable late RED.

This stable-filename addendum is revision r56. Accepted/pushed r55 planning produced exact final11 tree `bdfadfa1e6872596e2f37da4fe6cbebf66ee66d4`, then its single production chain stopped at the first nonzero command. Final11 identity, integrity `14/14`, harness self-test, and complete-receipt suppression passed. R55 next invoked `topology_validator_binds_every_tuple_to_owner_block` directly; the integration target requires harness-generated `PHASE285_TOPOLOGY_*` artifacts, so it failed `topology input path absent: NotPresent` before owner-block behavior. Read-only audit found the accepted A2b2 producer→consumer contract already used the correct atomic command: `with-nats-jetstream.sh ... check-phase285-witness-integrity.sh --self-test topology-owner-blocks`. The wrapper creates and binds canonical config, credentials, projections, invocation token, and scratch identity before the exact external test and all 33 controls. R56 changes planning command authority only. Final11 production bytes remain unchanged; no failed command is retried before r56 review/push and a new window.

### r56 precedence rule

R56 supersedes r55's invalid direct topology invocation, unwrapped topology selector, expired production authorization, and status sentences only. Every accepted r55 checker/manifest byte, exact message, 31-control oracle, final11 tree/hash/blob/path identity, no-retry, two-hour hard-stop, immutable-review, push/fetch, and successor block remains mandatory.

The only admissible production subject remains final11 tree `bdfadfa1e6872596e2f37da4fe6cbebf66ee66d4` from index `/private/tmp/phase285-a2b3-r55-prospective.LrPTQI/index`, direct parent accepted A2b2 `30eb13c57a3dfdd54ae1eb9ee8fdd75a88067ef1`. Checker SHA-256/blob remain `1300a65a963e0831a5815c237ffdf0eba96db4e53b96caf2d85d0acb88fb5cd0` / `4a507d8b4424a126d7fc754da339489f2ff4c6bb`; manifest SHA-256/blob remain `7ede69e3f0818508bfd0f4df21e0349a02b8a82739aa1845e76f2bd80f340b4e` / `422d42bf5ad9f8eae29b8e35a358ba49bbfc0b95`; launcher SHA-256 remains `e59ba9f62bf126bccdf8c0d3331b54adae9e74f8fe1ee6e31d43e3dec9ca66b1`. A2b2-to-final11 remains exactly eleven paths/digest `4d9d95a9253af2cede7f42175322169a4deb3fafebc8fb0d58bcb845cd992adf`; final9-to-final11 remains exactly checker/manifest with digest `3ed67f9590ac9240b2318c84c3d1aa34e801331766aed59714828e60dcdd0595`. No production edit, index change, or new worktree is authorized.

The r55 identity, integrity, harness-self-test, and complete-receipt receipts carry because the exact final11 tree and bytes are unchanged; the failed direct topology invocation is diagnostic and carries no semantic evidence. R56 begins with exact final11/ref/index/hash/residue preflight, then invokes exactly one harness-wrapped topology-owner-block selector. That selector itself creates/binds all topology artifacts, runs the exact positive external target and `23+3+7=33` unique/vacuous-zero controls, and includes its required internal complete-receipt producer. A missing, malformed, stale, aliased, replaced, or unbound artifact remains fatal.

After immutable r56 planning receives independent P0/P1/P2=`0/0/0` and is pushed/fetched exactly, root may start one fresh two-hour continuation in the existing production worktree. Freshly run wrapped topology-owner-blocks; production-bound observations; complete deadline selector including all compiled mutants; grants; relay-positive; witness all-target locked/offline no-run; governance serial library suite; strict all-target/all-feature clippy; formatting, syntax, shellcheck, diff, exact identity/scope/hash/index/untracked/ref, and fail-closed Docker-residue gates. Bracket every gate with the exact final11 verifier. Any nonzero result, vacuity, timeout, identity drift, or hard stop ends r56 without retry, repair, extension, commit, push, new worktree, or successor work.

Only a completely green chain permits root to stage exactly the eleven final11 paths in the real index, require real-index tree `bdfadfa1e6872596e2f37da4fe6cbebf66ee66d4`, create one direct child of accepted A2b2, clear the worktree/index, and obtain immutable hostile P0/P1/P2=`0/0/0`. Only that verdict authorizes pushing `work/v179-phase285-plan04-task03a2b3` and `checkpoint/v179-phase285-plan04-task03a2b3-production`. A2c remains blocked until both fetched refs equal the reviewed commit.

This stable-filename addendum is revision r51. Accepted/pushed r41 produced accepted A2b1 commit `a4b01038e5e33e5b3901dcaca1c82f7ca1db65c1` / tree `0da809e552441608f41f652ee21602fac3c3d35c`. Accepted/pushed r50 planning `2a35bb99113568bc8878def6a8447de9383a81d7` split cold compilation from steady-state release evidence. Its sole continuation produced unfrozen final-byte tree `4f04081a619bf87352d11af648208235afbebd01`: all inherited gates through deadline mutations and both debug receipts passed; `cold_default_release_seed` passed with Cargo `fresh=false`, `elapsed_ms=39730`, and SHA-256 `a0e99d78eec913d5497fb43d91de4df54fdaed33bd267d5fed2bc3e4c69e947b`. The immediate default-release command succeeded with Cargo `fresh=true` at the same declared path, then the checker rejected a generic seed identity mismatch. A bounded diagnostic against the same candidate bytes reproduced exact same path, length `4440592`, device `16777235`, mode, and SHA-256 before/after a `fresh=true` build while only the top-level publication inode changed from `599885074` to `599885142`. Cargo may republish that root artifact without rerunning rustc. All later gates were unrun; no production commit or push occurred. Revision r51 corrects only cross-invocation publication identity and diagnostic granularity. Production is frozen with no active hard-stop clock until immutable r51 passes independent review and is pushed/fetched exactly; A2b3 and all later work remain blocked.

### r51 precedence rule

R51 supersedes r50 planning-status/production-authorization and cross-invocation inode-equality sentences only. Every r50 cold-seed/fresh/command/timeout rule, r49 bounded-runner/output/JSON/per-invocation artifact rule, r48 projection progress/stop rule, r47 panic parser, r46 observation bound, and r45 topology semantic/count/ownership/digest requirement remains mandatory.

The cold seed still requires Cargo `fresh=false` and the complete r49 declared-path lstat, canonical shared-target confinement, `O_NOFOLLOW` open, same-file-descriptor hash, and post-lstat/re-resolve stability proof. Keep that seed descriptor open until the final symbol scan completes. Retain its canonical declared/resolved paths plus length, SHA-256, device, inode, and mode. After each final release command and once before symbol scan, rewind and rehash the still-open seed descriptor and require its fstat length/device/inode/mode and SHA-256 to remain exact. Close it through one finally path on every success/failure before scratch cleanup.

Each final release receipt still requires Cargo `fresh=true`, the same declared and resolved path strings, length, SHA-256, device, and mode as the seed, plus its own complete per-invocation r49 lstat/confinement/`O_NOFOLLOW` fd-hash/post-lstat stability proof. Cross-invocation publication inode may equal or differ because Cargo may replace the top-level artifact link/copy without compiling; record both exact decimal inodes and `inode_relation=<stable|replaced>` after the existing receipt fields and `seed_artifact_sha256`. Neither relation alone grants success: path/content/length/device/mode/fresh/per-invocation stability and retained-seed-fd revalidation all remain mandatory. A replacement inode is therefore observed, not trusted.

Replace the generic mismatch with exact fatal reasons for declared path, resolved path, length, SHA-256, device, mode, per-invocation identity, retained-seed-fd fstat, and retained-seed-fd hash. The two fixed release stages remain unconditional and single-execution; no diagnostic retry or new run is authorized. The generic 120-second helper, downstream public-API positive, four privacy compile-fail probes, artifact scan, all 31 compiled mutants, complete final-byte chain, exact five-path scope/digest, and successor blocks remain unchanged.

The stopped tree `4f04081a619bf87352d11af648208235afbebd01` is diagnostic-only and remains unstaged, uncommitted, and unpushed. After independently accepted/pushed r51, one fresh bounded continuation may alter only checker publication-identity/diagnostic code and the bound integrity manifest while preserving every other final byte. It must run the complete final-byte chain once from the beginning. No new worktree is authorized.

### r50 precedence rule

R50 supersedes r49 planning-status/production-authorization sentences only. Every r49 bounded-runner/output/JSON/artifact/receipt rule, r48 projection progress/stop rule, r47 panic parser, r46 observation bound, and r45 topology semantic/count/ownership/digest requirement remains mandatory except for the explicit cold-release preparation below.

After both unchanged 300-second debug boundary receipts pass and before either final witness release receipt starts, run exactly `cargo build -p swarm-governance-witness --lib --release --locked --offline --message-format=json` in the same exact-tree CWD and same target directory that was fresh at the chain start. Use the r49 process-group/output runner with a hard 900-second timeout, the same exact 16,777,216-byte combined-output cap, at-most-65,536-byte failure tail, and no external target/cache/fallback. Emit flushed `deadline_boundary_prepare receipt=cold_default_release_seed phase=<compile|artifact> state=<start|passed|failed> timeout_seconds=900 reason=<...>` progress; passed/failed rows also publish measured `elapsed_ms`, and the passed row binds `artifact_sha256`. This preparation row is a prerequisite, not one of the four `non_test_*` final boundary receipts. It is the validation contract's explicitly longer final-local lane and remains subordinate to the slice's two-hour hard stop. This cold seed and the later default-release receipt are two unconditional, single-execution stages; neither may be conditionally retried after any failure.

Require exit zero, exactly one successful Cargo `build-finished`, and exactly one `compiler-artifact` whose target name is `swarm_governance_witness`, target kind is exactly `lib`, profile has `test=false`, Cargo `fresh=false`, and filenames contain exactly one `.rlib`. Apply the complete r49 declared-path pre-resolve lstat, canonical shared-target confinement, `O_NOFOLLOW` same-file-descriptor hash, length/SHA-256/device/inode, post-lstat/re-resolve stability checks. Retain that canonical path and identity through both final release witness builds. A missing/ambiguous/malformed record, timeout, overflow, nonzero exit, artifact failure, or any later identity/hash/path change is fatal and vacuous.

The two existing final witness release commands remain byte-for-byte unchanged except for their inherited `--message-format=json` and continue to use the r49 300-second runner. Require each matching witness `compiler-artifact` record to have Cargo `fresh=true` and require its path, length, SHA-256, device, and inode to equal the cold seed exactly. Preserve all r49 final receipt fields/order and `artifact_sha256`, then append `seed_artifact_sha256=<seed-lowercase-64-hex>` to each of the two release receipt rows only. Revalidate the retained seed artifact after each release build and once more before the unchanged non-test symbol scan. The two debug rows, generic 120-second helper, downstream public-API positive, four privacy compile-fail probes, artifact scan, all 31 compiled mutants, complete final-byte chain, exact five-path scope/digest, and successor blocks remain unchanged.

The stopped tree `852a61559f1c1af380231bd8f33123aa5c847825` is diagnostic-only and remains unstaged, uncommitted, and unpushed. After independently accepted/pushed r50, one fresh bounded continuation may alter only the checker cold-release-seed/fresh-receipt code and bound integrity manifest while preserving every other final byte. It must run the complete final-byte chain once from the beginning. No new worktree is authorized.

### r49 precedence rule

R49 supersedes r48 planning-status/production-authorization sentences only. Every r48 projection progress/stop rule, r47 panic parser, r46 observation bound, and r45 topology semantic/count/ownership/digest requirement remains mandatory and unchanged.

The four `non_test_*` boundary builds alone must leave the generic 120-second `cargo()` helper. Run each exact existing build command with added `--message-format=json` through a fresh function that uses the same confined exact-tree CWD and same fresh shared `deadline-shared-target`, a hard 300-second timeout, a new process group killed on timeout/overflow, and streamed combined output capped at exactly 16,777,216 bytes. On failure, emit the receipt name, phase, reason, and final at-most-65,536 captured bytes before cleanup. No retry, shared external target, cache fallback, or timeout extension is evidence. Immediately before/after each build, emit flushed progress-only records with receipt, state start/passed, and `timeout_seconds=300`; progress records have no acceptance authority.

For each build require exit zero, exactly one successful Cargo `build-finished` record, and exactly one `compiler-artifact` with target name `swarm_governance_witness`, target kind `lib`, profile `test=false`, and at least one `.rlib` filename. Require exactly one `.rlib` for that record, lstat its declared path before resolution as a nonempty regular non-symlink, resolve it beneath the canonical shared target, snapshot length/SHA-256/device/inode, and revalidate the snapshot after parsing. Preserve every existing field and its order in the final `deadline_boundary receipt=<name> ... status=passed` row, then append `artifact_sha256=<lowercase-64-hex>`. Missing/ambiguous/malformed JSON, unsuccessful build, artifact cardinality/type/confinement/identity failure, output overflow, or timeout is fatal and vacuous. The generic 120-second helper, downstream public-API positive, four privacy compile-fail probes, later non-test symbol scan, and all 31 compiled mutants remain unchanged.

The stopped tree `7b565585fb7d167d68ceb52a6f61a3348dcf2c03` is diagnostic-only and remains unstaged, uncommitted, and unpushed. After independently accepted/pushed r49, one fresh bounded continuation may alter only the checker boundary-build runner/receipts and bound integrity manifest while preserving the exact five-path slice/digest and every other final byte. It must run the complete final-byte chain once from the beginning. No new worktree is authorized.

### r48 precedence rule

R48 supersedes r47 planning-status/production-authorization sentences only. Every r47 panic parser, r46 observation bound, and r45 topology semantic/count/ownership/digest requirement remains mandatory and unchanged.

Invoke the existing `topology_projection_controls` Python body unbuffered with `python3 -I -u -`; do not change any mutation, timeout, subprocess command, intended relation, or final evidence row. Immediately before and after each bounded subprocess, emit and flush a progress-only record containing mutation label, phase (`internal` or `external`), state (`start` or `passed`), and its existing timeout seconds. The two Rust controls each emit internal `300` start/passed and external `300` start/passed. `shell_owner_swap` and `stale_projection_token` each emit external `120` start/passed. Progress records contribute no digest, mutation count, success count, or acceptance authority; only the unchanged final mutation row and final `33` summary count. A subprocess timeout remains fatal through the existing Python timeout. Coordinator silence is never timeout evidence and cannot authorize termination; only a checker-emitted timeout/failure, the explicit two-hour slice hard stop, or user interruption may terminate the run. Because the theoretical sequential subprocess maximum is 1,440 seconds, no shorter inferred group bound exists.

The stopped tree `b3af2d0f12d5ec4db39cee10d24692bcd71c1fff` is diagnostic-only and remains unstaged, uncommitted, and unpushed. After independently accepted/pushed r48, one fresh bounded continuation may alter only the checker projection invocation/progress records and bound integrity manifest while preserving the exact five-path slice/digest and every other final byte. It must run the complete final-byte chain once from the beginning. No new worktree is authorized.

### r47 precedence rule

R47 supersedes r46's planning-status, production-authorization, and exactly-one-raw-panic implementation only. Every r46 compile/execute bound and every r45 topology semantic, count, ownership, digest, and acceptance requirement remains mandatory and unchanged.

Keep direct executable exit exactly `101`, exact test cardinality `running 1 test` and `0 passed; 1 failed; 0 ignored`, and exactly one intended phrase. Replace the raw `text.count("panicked at")==1` predicate with a complete ordered panic-record parser. Match every header only with anchored grammar `^thread '([^']+)'(?: \([0-9]+\))? panicked at .+:$`; its message is the first following nonempty line before the next panic header, and a missing message is fatal. This ignores only the optional volatile numeric thread ID and source path/line/column. The complete ordered `(thread_name, message)` list must equal exactly: (1) `phase285-a2a-worker-observations` / `observation public ReadHead is absent`; then (2) `service_checkpoint_observation_tests::worker_observations_are_real_and_reconciled` / `observation thread panicked: Any { .. }`. Missing, additional, reordered, renamed, or substituted panic records/messages; the intended phrase outside the first record or occurring other than once; a different exit; or different test counts is fatal and vacuous. Stack/backtrace/note text cannot create, replace, or excuse a record. Output/process bounds, binary identity, cleanup, and no-cache/no-retry semantics remain exactly r46.

The stopped tree `ea6e8316e3f0805fa0f6cc0ee8a83dc186ff8995` is diagnostic-only and remains unstaged, uncommitted, and unpushed. After independently accepted/pushed r47, one fresh bounded continuation may alter only the checker transcript predicate and bound integrity manifest while preserving the exact five-path slice/digest and every other final byte. It must run the complete final-byte chain once from the beginning, including topology, observation, deadline, and strict gates, before any freeze. No new worktree is authorized.

### r46 precedence rule

R46 supersedes r45's planning-status and production-authorization sentences only. Every r45 topology semantic, count, ownership, digest, and acceptance requirement remains mandatory and unchanged.

The confined observation `public_head_absent` mutation must no longer combine a cold build and test execution under one timeout. Extract the exact candidate tree into a fresh confined directory, apply the already authorized single anchored response mutation, and use a fresh confined `CARGO_TARGET_DIR`. First run `cargo test -p swarm-governance-witness --lib --locked --offline --no-run --message-format=json` with a hard 300-second compilation timeout. Stream combined output into a phase-specific capture capped at exactly 16,777,216 bytes; exceeding the cap terminates the child and is vacuous. Require exit zero, exactly one successful Cargo `build-finished` record, and exactly one `compiler-artifact` whose target name is `swarm_governance_witness`, target kind is `lib`, profile has `test=true`, and executable is non-null. Require that executable to be a regular non-symlink beneath the canonical fresh target directory, then snapshot its length, SHA-256, device, and inode. Execute that exact binary directly, once, with `--test-threads=1 service_checkpoint_observation_tests::worker_observations_are_real_and_reconciled --exact` under a separate hard 60-second execution timeout and the same exact capture cap. Require exactly `running 1 test`, `0 passed; 1 failed; 0 ignored`, and the intended late message `observation public ReadHead is absent`; revalidate the binary snapshot afterward. Compilation failure or timeout, output-cap overflow, missing/ambiguous/out-of-target executable identity, execution timeout, zero/multiple tests, an earlier failure, or a missing intended message is vacuous and fatal. Before cleanup, every failure reports its phase and the final at-most-65,536 captured bytes so a future failure is classifiable. No prebuilt external target, shared cache, direct source oracle, timeout extension beyond these two bounds, or retry is evidence.

The stopped tree `718f766f91aca30836826ee885f5f087bf2f3534` is diagnostic-only and remains unstaged, uncommitted, and unpushed. After independently accepted/pushed r46, one fresh bounded continuation may alter only the checker implementation and integrity manifest as required for the split compile/execute receipt while preserving the exact five-path slice/digest and all other final bytes unless a new RED forces another stop. It must run the complete final-byte chain once, including observation and deadline selectors, before any freeze. No new worktree is authorized.

### r45 precedence rule

R45 also supersedes r44's planning-status and production-authorization sentences: r44 is rejected, and only independently accepted/pushed r45 may authorize a fresh window.

For A2b2 topology authority and mutation evidence, this r45 section supersedes r44 topology-control counts and inventory only. Every non-count r44 and r43 requirement remains mandatory. The topology inventory is exactly 33 controls with 33 unique full mutated-source or artifact-control digests and vacuous=`0`: all twenty-one r44 config controls unchanged and in their prior order; then `malformed_store_import_row` and `malformed_runtime_export_row`; then the unchanged three comparator-deletion controls; then the four original r43 projection controls followed by `projection_mode_widening`, `projection_inode_alias`, and `projection_post_snapshot_replacement`. Arithmetic is exactly `23 + 3 + 7 = 33`.

`malformed_store_import_row` adds one raw-config-grammar-valid store import row with an otherwise valid service object plus one unexpected field. It must reach the current validator and fail only at `topology[imports-malformed]`; deleting that row-shape predicate must make the store's projected empty imports set survive. `malformed_runtime_export_row` does the symmetric operation in a runtime export row and fails only at `topology[exports-malformed]`; deleting that predicate must make the runtime's projected empty exports set survive. NATS startup or a generic parse failure cannot count.

The three artifact controls execute the real checker-owned projection handoff. `projection_mode_widening` changes exactly one completed projection from `0600` to `0644` before its next read and fails only at `topology_snapshot[mode]`. `projection_inode_alias` replaces the shell canonical path with a hard link to the byte-identical Rust canonical artifact before the distinct-identity gate and fails only at `topology_snapshot[alias]`. `projection_post_snapshot_replacement` replaces exactly one recorded projection with a new mode-`0600` inode containing identical bytes before the next handoff and fails only at `topology_snapshot[changed]`. Each control must prove the ordinary four-artifact flow first, execute nonzero, preserve all unrelated bytes/bindings, and reject compile failure, setup failure, a missing target, or an earlier generic relation as vacuous.

### r44 precedence rule

For A2b2 topology authority and mutation evidence, this r44 section supersedes the r43 topology-count and config-mutation-inventory sentences only. All other r43 requirements remain mandatory. The canonical validator must discover the complete authenticated-principal inventory inside each exact account block, require exactly the four account-bound principals, distinguish a truly absent optional authority array from any malformed or duplicate declaration, and reject malformed rows rather than project them as an empty set. Four projection artifacts must be mode `0600` and have four distinct device/inode identities at every handoff.

The topology inventory is exactly 28 controls with 28 unique full mutated-source digests and vacuous=`0`: all eighteen r43 config controls in their exact order, followed by `extra_store_principal_raw_authority`, `duplicate_store_imports_declaration`, and `duplicate_runtime_exports_declaration`; then the unchanged three executable comparator-deletion controls and unchanged four cross-language projection controls. The extra-principal control adds one grammar-valid authenticated principal under `PHASE285_WITNESS_STORE` with `$JS.API.>` publish authority and must fail only at exact principal inventory. The imports control adds two grammar-valid empty `imports` declarations to `PHASE285_WITNESS_STORE` and must fail only at declaration uniqueness. The exports control adds two grammar-valid empty `exports` declarations to `PHASE285_RUNTIME` and must fail only at declaration uniqueness. Each hostile config must reach the current validator, execute nonzero, emit its exact stable intended reason, and contribute one unique full mutated-config digest. Failure in NATS startup, text anchoring, an earlier unrelated parser relation, or a helper-authored counter is vacuous. Arithmetic is exactly `21 + 3 + 4 = 28`.

### r43 precedence rule

For A2b2 topology authority and mutation evidence, this r43 section supersedes every r42/r41/r35 topology projection, init-authority, artifact-bound, topology-count, topology-selector, and mutation-inventory sentence elsewhere in this document. A2b1 remains accepted byte-for-byte and its complete receipt, artifact, signal/release lifecycle, compiled mutation, observation, and deadline contracts remain mandatory predecessor evidence.

The canonical normal harness has exactly three accounts and four authenticated principals. The init principal `phase285_expected` belongs only to `PHASE285_WITNESS_STORE`. Its publish tuple is the exact closed six-subject sequence `$JS.API.STREAM.CREATE.KV_phase285_service`, `$JS.API.STREAM.UPDATE.KV_phase285_service`, `$JS.API.STREAM.INFO.KV_phase285_service`, `$JS.API.STREAM.MSG.GET.KV_phase285_service`, `$KV.phase285_service.__witness_bucket_manifest`, and `$KV.phase285_service.s.0fc95119eb171c924f962c3af0a1f03c70078a8fd8a590189d7358e3c62ba1ef`. It subscribes only `_INBOX.>`, has no `allow_responses`, cannot serve public or private requests, and has neither `$JS.API.>` nor `$KV.>` authority. Every other principal retains its exact accepted fixed subject set. Ordering, duplication, omission, addition, wildcard substitution, and cross-principal movement are all semantic differences.

One checker-owned topology run occurs inside the accepted NATS harness. The harness exports the exact generated `nats.conf`, four canonical credential-role files, candidate tree, and fresh invocation token. Before every read, both Rust and shell require a regular non-symlink input with metadata length in `1..=262144` bytes for each canonical/probe config and `1..=4096` bytes for each credential-role file. The checker creates four absent projection paths inside one canonical `0700` scratch directory: Rust and shell projections for both canonical and probe inputs. Each projection must be a regular non-symlink create-new canonical newline-framed mode-`0600` file with metadata length in `1..=16384` bytes before any read. The existing exact internal Rust target `service_checkpoint_relay_tests::complete_receipt_authority_and_grants_are_observed` first preserves its accepted A2b1 work, then independently parses the actual config and credential-role bytes at runtime and create-news the canonical Rust projection. The projection binds schema, candidate tree, invocation token, exact case `service_checkpoint_topology_owner_blocks`, both canonical and probe config SHA-256 values, and the ordered account/principal pairs derived from the parsed blocks. It cannot use a literal topology constant, assert a value against itself, scan its own source, or emit from dead code.

The checker also constructs one projection-only probe config before either parser runs. It preserves the exact canonical block grammar and tuple cardinalities but replaces every account with a fresh independent 128-bit uppercase-hex-suffixed grammar-valid identifier and every principal with a distinct fresh 128-bit lowercase-hex-suffixed grammar-valid username. The probe bytes and digest are fixed before parser launch, never supplied as expected output, and cannot equal a canonical identifier. Both Rust and shell parsers must derive and emit the same fresh probe projection. Parser acceptance occurs before and separately from policy validation: the projection oracle requires the two fresh derived outputs to match, then the canonical policy oracle must reject the probe only for noncanonical account/principal inventory. A parser that emits the correct fixed canonical literal therefore fails the fresh probe projection equality before policy rejection can count.

After the internal target returns, the shell parser independently parses both snapshotted config inputs and create-news its canonical and probe mode-`0600` projections with identical bindings. Exact external FQN `topology_validator_binds_every_tuple_to_owner_block` independently reopens all four artifacts, rejects symlinks/aliases/replacements/extra fields/noncanonical bytes, recomputes their digests and bindings, and requires Rust/shell equality separately for canonical and fresh probe projections before applying the canonical-policy oracle. It does not spawn shell, regex-scan Rust, call the private Rust parser, or accept a helper counter. The checker snapshots length, device, inode, mode, and SHA-256 for both config inputs, all four credential files, and all four projection artifacts; every value must remain identical after each handoff and at final acceptance. No file is read before its metadata length bound passes. The checker removes its scratch on every exit.

The topology inventory remains exactly 25 controls with 25 unique full mutated-source digests and vacuous=`0`. The eighteen config controls are exactly `runtime_witness_import_block_move`, `missing_runtime_account`, `missing_init_principal`, `store_init_credential_swap`, `public_subject_omission`, `private_subject_omission`, `public_wildcard_import`, `runtime_private_import`, `witness_raw_js`, `store_public_service`, `init_serving`, `init_raw_authority_widening`, `init_manifest_admitted_subject_swap`, `import_to_substitution`, `import_source_account_substitution`, `export_allowed_account_substitution`, `response_grant_move`, and `cross_principal_permission_swap`. `init_manifest_admitted_subject_swap`, the three account/grant substitutions, and both block/permission moves preserve cardinality. The three executable validator controls are exactly `delete_response_grant_comparator`, `delete_import_account_comparator`, and `delete_export_account_comparator`. For each, the checker extracts the current full Python validator from the manifest-bound harness into a confined file, proves the deletion anchor occurs once, applies only that deletion, executes the mutated validator under `python3 -I` first on the canonical config and then on the matching isolated hostile config, and rejects the mutant only because the hostile config survives. The four projection controls are exactly `rust_literal_disconnect`, `rust_owner_swap`, `shell_owner_swap`, and `stale_projection_token`. The first compiles a confined exact-tree Rust mutant that returns the correct canonical literal while ignoring input; the canonical projection passes, the same exact target then consumes the fresh probe, and the mutant fails only because its output does not contain the unpredictable identifiers. `rust_owner_swap` compiles and executes a distinct wrong-owner mutant; `shell_owner_swap` executes a confined shell-parser mutant; `stale_projection_token` replays canonical prior bytes under a fresh token. Compilation failure, source fragments, global counts, an earlier parse failure, or a target that does not execute once cannot count. The manifest freezes every label, exact intended relation, and count.

The immutable A2b2 chain invokes `with-nats-jetstream.sh --self-test`, then the root-pinned integrity launcher under normal NATS with selector `--self-test topology-owner-blocks`. That selector owns the complete A2b1 two-stage proof plus the Rust/shell topology handoff and all 25 controls. It requires the internal A2b1 target transcript `running/passed/failed/ignored/filtered=1/1/0/0/18`, the A2b1 external target `1/1/0/0/2`, and the topology external target `1/1/0/0/2`; every exact target runs once. Separate standalone topology Rust or shell invocations are not evidence. The chain then reruns accepted observations and deadline evidence, strict clippy, formatting, both shell checks, exact identity/scope, and clean-tree checks. No relay mode, live grant timing, serving binary, recovery, A2c, or successor behavior is authorized.

### r41 precedence rule

For A2b1 lifecycle coordination, this r41 section supersedes every r40 readiness, EXIT, snapshot, mutation-count, and lifecycle-count sentence elsewhere in this document. All non-lifecycle A2b1 semantics remain unchanged. The positional mode remains `--self-test complete-receipt-real-signal <EXIT|HUP|INT|TERM> <absolute-parent-confined-control-root>` and must call the same `run_complete_receipt_focus` function. Before launch, the parent creates and snapshots one canonical `0700` control-root directory and requires both readiness and release paths absent. After the real focus function creates its canonical `0700` scratch and artifact directories and arms the real EXIT plus HUP/INT/TERM cleanup-and-re-raise traps, its single blocking checkpoint create-news a canonical newline-framed `0600` readiness record. That record binds candidate tree, fresh invocation token, requested case, child PID, a fresh 64-hex release nonce, and canonical root/scratch/artifact paths with directory type, mode `0700`, device, and inode. The child then blocks without a fixed sleep or time-derived permission to exit.

The parent requires record PID equal the spawned PID, then fully revalidates canonical root, scratch, and artifact paths as directories with mode `0700` and the exact recorded device/inode under the unchanged root snapshot. HUP/INT/TERM receive no release: the parent sends the exact signal and requires cleanup, default-handler restoration, re-raise, signal-specific subprocess status, and absence of the bound scratch inode within two seconds. EXIT alone uses a release handshake. Only after all post-readiness validation succeeds, the parent create-news a canonical newline-framed `0600` release record, writes canonical bytes, fsyncs the file, canonically reopens and verifies its identity/content, then fsyncs the parent directory; the record binds the same tree, token, case, PID, release nonce, and root/scratch/artifact identities. The child accepts only that exact record, revalidates it, exits zero, and proves cleanup through the real EXIT trap. Missing, malformed, aliased, replaced, duplicate, stale, cross-case, cross-PID, cross-token, cross-tree, cross-nonce, early, or mode-widened coordination fails closed with a stable relation-specific reason.

The exact lifecycle inventory is 25 unique controls: nine inherited artifact attacks; four real-flow EXIT/HUP/INT/TERM cases; the six inherited coordination/trap controls (checkpoint-before-trap, fabricated readiness, ignored signal, normal-exit substitution, trap removal, trap substitution); and six release/metadata controls (EXIT release omission, stale/cross-case release, early EXIT before release, fixed-sleep substitution for the release wait, post-readiness control-root mode widening, post-readiness artifact mode widening). Every control must execute nonzero and fail only its intended stable relation: `release-timeout` or `exit-before-release`, `release-identity`, `control-root-snapshot`, `artifact-snapshot`, signal-specific termination, or residue. Helper-authored counters, failure before the intended relation, and a timeout used as generic success are invalid evidence.

The release-omission control is bounded and non-vacuous: after a valid readiness checkpoint the parent withholds release, proves the child remains alive and the bound scratch inode remains present through the release deadline, then sends TERM and requires the real cleanup-and-re-raise path, signal-specific status, and residue-free teardown. Only that ordered observation emits `release-timeout`; an arbitrary process timeout, pre-checkpoint stall, early exit, or failed cleanup cannot count. Release durability order is create-new, canonical write, file fsync, canonical reopen and identity/content verification, then parent-directory fsync before the child may accept it.

## r26 bankable execution decomposition

1. **A1 — subscriber-to-worker deadline seam.** Own exactly eight paths with sorted digest `3b9e0dcdef3b37beb4dd22cfb576cb0f3d72d45e2c87c54042c548dbc4deacd7`: witness `Cargo.toml`; `src/lib.rs`; `src/public_dispatcher.rs`; `src/service_config.rs`; `src/store_proxy_service.rs`; `tests/full_service_path.rs`; the witness conformance checker; and integrity manifest. It starts from `5ade791`. For each public/private path, one crate-private production function must own raw subscription-message validation, immediate receipt-time capture, absolute-deadline construction, typed ingress-message construction, and bounded enqueue; the real NATS subscriber and exact library test must call that same function. A1 proves eight distinct event-derived worker rows plus one checker-joined budget/real-constructor row, receives its own immutable review/commit/pushed refs, and makes no live NATS grant, relay, or recovery claim.
2. **A2a — observed worker/proxy/store spine.** Start only from accepted A1. Own exactly nine paths with slice digest `919bf3addbcdbf8625f055f9c8737f22aa5b9d3dd99130fe8e62fc981b65d4c9`: witness `src/lib.rs`, dispatcher, runtime client, service config, store proxy, `tests/service_checkpoint.rs`, checker, manifest, and NATS harness. The cumulative A1+A2a union is exactly eleven paths with digest `aa6b8eae0de4d644ebb18c08a36264d09b6adc81cddc50288d3067ed3715030f`. Its exact library-internal normal-NATS test alone can use the crate-private cfg(test) builders to inject the accepted A1 `WorkerTransitionObserverV1` recorder while still invoking both shipping `start` methods; no external integration test may name that seam. The harness consumes explicit prefix `--expect-exact-test <full-fqn> <expected-filtered-out> --`, binds the forwarded exact FQN, and requires the pinned A2a transcript `1/1/0/0/17`; malformed, mismatched, duplicate, filtered-only, zero, ignored, or failed executions are explicit self-test mutations. It banks only ordered worker, proxy, store, publisher, and server-issued connection observations. Every count and digest is derived from serialized evidence. It makes no relay, response-grant, suppression, replay, recovery, or abort claim.
3. **A2b1 — accepted-ledger-bound suppression predicate.** Start only from accepted A2a. Own exactly four paths with digest `ccd00d773edc3826aecd28bf6d451d0d2a5323ee766224e01b5633cb3815070a`: witness `src/lib.rs`, `tests/service_checkpoint.rs`, checker, and manifest. The checker owns one `mktemp -d` directory and two absent sibling paths exposed only as `PHASE285_COMPLETE_RECEIPT_LEDGER_PATH` and `PHASE285_COMPLETE_RECEIPT_PATH`. The accepted A2a internal test writes and reopens canonical `ObservationLedgerV1` bytes. Before any reservation, the private validator takes the current expected candidate tree, the fresh checker-generated invocation token, and exact case `service_checkpoint_complete_receipt`; it requires all three ledger fields to match those expected values. A valid receipt copied from invocation A into invocation B must return `Forward` with the exact capacity-one receiver still empty. The validator independently recomputes every overlapping request, signed response, exact worker operation/CAS sequence, store input/result, authenticated store-envelope/public-head relation, publisher reply subject/bytes, and three server-bound identities. It additionally requires exactly one public `proxy_exchanges` row whose canonical bytes equal `private_exchanges[2]`, and exactly two decoded `publisher_attempts` rows bound to the validated worker trace: ordinal 8/private/`published=true` and ordinal 11/public/`published=true`. A same-helper typed copy, array-count/digest-only check, or byte-equality oracle against the producer object is forbidden. The external integration FQN cannot name or duplicate the cfg(test)-private predicate; after the internal target finishes, it independently decodes and recomputes the same invocation, proxy, publisher, and read-only artifact relations. Internal exact controls coherently refresh nested, array, ledger, and outer-receipt digests and require private rejection for cross-invocation, proxy cross-copy, and fabricated publisher rows. The compiled inventory is exactly ten unique controls: the existing seven two-stage relation mutants plus proxy cross-copy and publisher fabrication must make the real internal positive target pass and the external decoder fail its exact relation; the private current-invocation-bypass mutant must execute the internal cross-copy control and fail because it suppresses instead of forwarding. Compilation failure, timeout, generic failure, zero tests, or failure before the intended assertion cannot count. Rust may only create-new/write/fsync/reopen the exact checker-selected files; it may not spawn NATS, choose/remove/replace a directory or file, or read any other artifact selector. The shell checker enforces `0600`, distinct regular no-symlink inodes, unchanged parent identity, newline framing, canonical reopen, maxima of 1,048,576 ledger bytes and 2,097,152 receipt bytes, and stable identity/digests across handoff. The only signal-control seam is positional checker mode `--self-test complete-receipt-real-signal <EXIT|HUP|INT|TERM> <absolute-parent-confined-control-root>`; it must call the same `run_complete_receipt_focus` function with positional arguments, never a parallel cleanup function or ambient selector. The parent creates and snapshots a canonical `0700` control root and requires readiness and release paths absent before launch. In signal-control mode the real focus function creates canonical `0700` scratch and artifact directories, arms the exact normal-flow `EXIT` cleanup plus `HUP/INT/TERM` cleanup-and-re-raise handlers, then enters one blocking checkpoint. That checkpoint alone create-news a newline-framed canonical JSON readiness record at mode `0600`, fsyncs/reopens it, and binds candidate tree, fresh token, requested case, child PID, fresh 64-hex release nonce, and canonical root/scratch/artifact paths with directory type, mode `0700`, device, and inode. The child then blocks without a fixed sleep or time-derived exit permission. The parent requires the record PID equal the spawned PID and revalidates every canonical root/scratch/artifact path as a directory with exact mode, device, and inode. HUP/INT/TERM receive no release: the parent sends the exact signal and requires cleanup, default-handler restoration, re-raise, signal-specific subprocess status, and bound-scratch absence within two seconds. EXIT alone requires the parent, after successful validation, to create-new a canonical newline-framed mode-`0600` release record, fsync it and its parent, reopen it, and bind the same tree, token, case, PID, release nonce, and root/scratch/artifact identities. The child validates only that exact release, exits zero, and cleans through the real EXIT trap. Missing, stale, cross-case, cross-PID, cross-token, cross-tree, cross-nonce, malformed, aliased, replaced, duplicate, early, or mode-widened coordination fails closed. The exact lifecycle inventory is twenty-five unique controls: nine artifact attacks, four real-flow cases, six inherited coordination/trap mutations, and six release/metadata mutations covering EXIT release omission, stale/cross-case release, early EXIT, fixed-sleep substitution, post-readiness control-root mode widening, and post-readiness artifact mode widening. Every control executes nonzero and fails only its stable intended relation: `release-timeout` or `exit-before-release`, `release-identity`, `control-root-snapshot`, `artifact-snapshot`, signal-specific termination, or residue. At A2b1 the checker requires exact internal `1/1/0/0/18` and external `1/1/0/0/1`; the manifest binds exact inventory/counts and inherited A2b2/A2b3 external filtered count `2` to their tree pins. Either FQN invoked outside that checker-owned sequence is invalid evidence. A2b1 makes no topology, live relay, grant, timing, recovery, or A2c claim.
4. **A2b2 — account/principal-scoped topology validator.** Start only from accepted A2b1. Own exactly five paths with digest `964b80e1d926ccc78f1e2f487edb84169a01f1cb6a1eadcf3b294c3234ce98ff`: the A2b1 four plus the NATS harness. It parses and compares each account/principal block as a closed tuple set and kills cardinality-preserving block moves, import/export-account substitutions, and cross-principal permission swaps. Global counts are forbidden. It makes no serving-binary, live relay, grant, timing, recovery, or A2c claim.
5. **A2b3 — live four-account relay, grants, and causal timing.** Start only from accepted A2b2. Own the exact eleven-path set/digest `4d9d95a9253af2cede7f42175322169a4deb3fafebc8fb0d58bcb845cd992adf`: witness `Cargo.toml`; two serving bins; `src/lib.rs`; raw config, runtime client, service config; `tests/service_checkpoint.rs`; checker; manifest; and NATS harness. It integrates on the accepted A2b1/A2b2 seams, invokes both shipping starts through accepted A1/A2a observers, proves fresh public/private relay identities, exact owner-block authority, and server-bound 3,000/12,000-ms max-one refusal. It measures the true origin-relative `second_publish_at - first_publish_at` and kills a delayed-first-response mutant. The Task02 cumulative union is exactly fourteen paths/digest `4e6629733654f7b4b6f1233bfaaef96a0fede0368620380c8b97f9d75bb18664`. It makes no final oracle, recovery, or abort claim.
6. **A2c — positive-first independent oracle and non-bypassable corpus.** Start only from accepted A2b3. Own exactly four paths with slice digest `8b37d06097626b9d3f302ea1053ced7852e5553f095c8ead99964c8bca66b6c5`: `tests/service_checkpoint.rs`, checker, manifest, and NATS harness. It adds no production behavior. It independently decodes every canonical request/private/public response, recomputes all typed and top-level relations, validates real causal order, and requires the positive immutable ledger to pass the exact ordinary predicate before any mutant is run. The top-level accepted selector rejects ambient child/family controls and unconditionally executes one static named real-target corpus; internal children use a distinct private selector and cannot suppress or narrow the parent inventory. Coherent mutations replace the former direct-error synthetic identity/count cases. The cumulative union remains exactly fourteen paths/digest `4e6629733654f7b4b6f1233bfaaef96a0fede0368620380c8b97f9d75bb18664`. It receives the final A2 checkpoint and makes no recovery or abort claim.
7. **A3 — physical recovery/abort matrix.** Start only from accepted A2c. Own exactly six paths with slice digest `769825832abfff2e80561b7c4692a0aec750699096f094e3ab5c54765726d4ae`: service checkpoint integration test; persistence protocol and its source-local tests; conformance checker; integrity manifest; and NATS harness. The final cumulative union remains the reviewed sixteen paths with digest `1decd3b39a6433d9358a10289d6886f67211b2560563b417381273dd1662d614`. It proves the 18 physical restart/lost-response rows and late abort predicates while separately rerunning A1 and the final A2c selector.

Each slice requires its own exact red control and staged candidate validation, then exactly one local immutable production commit with zero dirty state. Root binds that commit, its tree, launcher hash, manifest hash, and focused-checker tree token; authorization-critical commands explicitly consume those values. Independent P0/P1/P2=`0/0/0` review audits the immutable commit. Only then are its work and checkpoint refs pushed. A later slice is unauthorized until both origin remote-tracking refs for its predecessor resolve to the reviewed commit. The existing implementation worktree is reused for A2a, A2b, A2c, and A3; no per-revision or per-slice worktree may be created. Each production window is two hours from explicit authorization. A timeout or rejected immutable review stops only that slice; it cannot authorize repair of the rejected object or parallel successor work.

## r26 inherited private-service source-guard preflight

Before another focused NATS run, add exact local-only mode `bash tools/check-phase285-witness-conformance.sh --self-test store-proxy-source`. The first and only pre-RED change may route that mode to the existing positive and mutation guards without altering either guard or its anchors; running it must then fail specifically at the stale connection-cardinality invariant, not at option parsing, missing execution, Docker, NATS, or another source fragment. After that RED, repair the guards and mutations. The mode must invoke the inherited private-service source guard once in positive mode and once in mutation mode without Docker or NATS, require both invocations to execute nonzero, and print `store_proxy_source_guard passed=1` plus `store_proxy_source_guard_self_test mutations=44 unique=44 passed=1`. This preflight is mandatory after every post-RED checker or manifest edit and before any expensive selector; it exists to expose the entire lexical/source-guard integration surface in seconds rather than one anchor per expensive combined run.

The positive guard must extract the `StoreProxyServiceRunner<S>` implementation and prove all four conditions independently: exactly one public `pub async fn start` signature whose first parameter is `connection: StoreRoleConnectionV1`; exactly one private `async fn start_inner` signature with the same typed first parameter; exactly two occurrences of that typed parameter inside the implementation; and exactly one ordered `Self::start_inner(connection, service).await` delegation between the two signatures. It must also retain the existing requirement that the Ready-binding comparison precedes stream lookup and queue subscription. A generic whole-file `count == 1`, an unscoped substring, or treating either signature as redundant is forbidden.

Replace the old single `arbitrary_store_client` source-guard mutation with three separately named mutations: `arbitrary_public_store_client` changes only the public signature; `arbitrary_private_store_client` changes only the private `start_inner` signature; and `omit_public_start_delegation` changes only the exact delegation. The validator must emit distinct stable reason codes `public_start_signature`, `private_start_inner_signature`, and `public_start_delegation`, and each self-test mutation must compare the observed reason to its exact expected reason rather than accepting an arbitrary `ValueError`. Each mutated combined source/config/integration/harness/compose digest must be unique and the positive validator must run. The inherited source-guard inventory therefore changes from 42 to exactly 44; no existing mutation may be dropped. These source-validator controls complement rather than replace the seven compiled A1 callsite mutants, including both executable public/private start-delegation bypasses.

The stopped diagnostic tree is read-only evidence until r26 is accepted and pushed. A later authorized attempt may resume it in place only after proving HEAD is accepted base `5ade79141a2f8249ef084d24fcfd5ef12251e48e`, `git write-tree` is exactly `39b515f29ca134d48571765211ac3be43356fb3c`, the index contains exactly the eight A1 paths with the reviewed digest, and there are no unstaged or untracked files. It must first record the exact pre-repair RED from `--self-test store-proxy-source`, then edit only the checker and manifest for this repair. Any mismatch requires reconstruction from the accepted base. The final candidate remains one direct-child commit of `5ade791`; the diagnostic tree itself is never committed or pushed.

## 1. Harness-owned bounded response loss

Remove `PublicWitnessPublishSuppressionV1`, `PublicWitnessServiceExecutionReceiptV1`, `start_with_publish_suppression`, deadline-control exports, and every related re-export from the library. No debug, release, default-feature, or all-feature library artifact may expose reply-suppression authority or an unbounded channel.

Implement lost response entirely in `service_checkpoint.rs` plus a conditional relay mode in the owned NATS harness, never through a production suppression seam, in-process dispatcher call, or manual request serialization. Normal harness mode remains the accepted three-account/four-principal topology. Relay mode materializes an isolated fourth `relay` account and fifth relay principal with exact fixed transforms for nine public and three private subjects and no wildcard, raw KV, JetStream, signing-key, or management authority.

For each public operation, `RuntimeWitnessClient` publishes unchanged canonical bytes on the ordinary fixed public subject with original requester reply route `R`; the runtime account has only a transformed service import to `swarm.governance.witness.relay.v1.<operation>` in the relay account and no direct witness route. The capacity-one relay reserves its evidence slot, consumes exactly one request, records exact subject/body/nonce/digest, and republishes those unchanged bytes over its sole transformed import to the ordinary fixed subject in the witness account with fresh sink `B`, where `B != R`. The unchanged `PublicWitnessServiceRunner` handles that request. For each resulting private call, the witness account has only a transformed service import to `swarm.governance.witness.relay.store.v1.<operation>` in the relay account and no direct store route. The same relay forwards the unchanged private request exactly once over its sole transformed import to the ordinary fixed private subject in the store account with fresh sink `C`, authenticates the typed private response, records the ordered private subject/request/response bytes and digests, and forwards it exactly once to the witness caller's original private reply route. The unchanged `StoreProxyServiceRunner` and real recording atomic store perform the backend work.

After the public runner completes, the relay receives and authenticates the canonical signed public response on `B`. Its harness-only receipt binds scenario ID, operation, public request nonce/digest, public response bytes/digest, the complete ordered private relay transcript, exact recording-store ReadEntry/CompareAndSwap/ReadEntry sequence, CAS counts, store generation/state digest, public/private runner and connection identities, and `R`/`B`/`C` inequality. On the first lost-response execution it performs zero publishes to `R`; the independent requester times out, possibly applied mutation maps to `OutcomeUnknown`, and retries remain zero. Normal replay uses a fresh relay instance, repeats the route, and forwards the authenticated `B` response to `R` exactly once.

The harness must parse the literal `--relay-service-checkpoint` mode before any credential, account, import/export, or Compose configuration generation, remove that one flag, generate relay topology, and export a fresh root-bound mode/topology token to the child checker. The focused Task 03A gate must invoke that flag; ordinary selectors and harness self-tests use normal mode. Relay mode must prove exact `accounts=4 principals=5 public=9 private=3 relay_public=9 relay_private=3`, and the child must reject a missing/stale/wrong-tree token. Normal mode must reject relay state/token and remain exact `accounts=3 principals=4`. Kill omitted, duplicated, or substituted flags; ambient-only enablement; command-string sniffing; relay topology on a normal selector; normal topology on the focused selector; and child-created or cross-invocation mode tokens.

If the capacity-one slot is full, closed, missing, or cannot accept the complete receipt, the relay refuses to arm loss and forwards the public response normally. The focused checker must kill manual-requester serialization, public or private relay bypass, `R == B`, `B == C`, duplicate/direct witness or store delivery, wildcard permission, unrelated-inbox timeout, missing/reordered/substituted private trace, request drop before either handler, pre-handler/pre-response receipt, fabricated receipt, delivered first public reply, capacity greater than one, unbounded channel, repeated arm, omitted sink receive, and any production/debug/all-feature suppression symbol.

## 2. Real-constructor deadline evidence without legacy weakening

`NatsPublicWitnessStoreProxyClient::new` remains source-compatible and must itself require `request_deadline_millis == STORE_RESPONSE_GRANT_MILLIS`; reject 0, 2,999, 3,001, `u64::MAX`, predicate deletion, comparison substitution, constant substitution, validator-delegation deletion, and delegation to an alternate permissive predicate. Production callers continue to pass the constant. Update the single inherited direct-client fixture in `full_service_path.rs` from 1,000 to exactly 3,000 and add the exact integration target `full_service_path_constructor_deadline_is_exact_and_receipt_bound`. Under the normal three-account/four-principal NATS harness, that target invokes the real public constructor with exactly 0, 2,999, 3,000, 3,001, and `u64::MAX`, requires only 3,000 to succeed before any store operation, and writes one fresh tree/token/case-bound receipt containing the ordered five inputs, observed result classes, and canonical digest. Calling a validator directly, scanning source literals, or running an unjoined full-service test is not evidence.

The focused A1 checker runs through the normal NATS harness and owns three exact positive targets. Both production runners factor their complete real subscriber loops through one crate-private `start_inner`, and each shipping public `start` delegates to that corresponding function. Public constructors and ordinary production construction always place `NoopSubscriberAdmissionObserverV1` in private runner state. Library-internal `#[cfg(test)]` runner builders may instead place a capacity-one post-enqueue recorder in that same private state, but they return the ordinary runner and expose no alternate serving entrypoint. Exact internal target `deadline_state_machine_tests::subscriber_callsite_is_receipt_anchored_and_mutation_sensitive` must invoke `PublicWitnessServiceRunner::start` and `StoreProxyServiceRunner::start`; calling `start_inner` directly is forbidden. Through those public entrypoints it blocks the first recording backend/proxy operation, publishes a second request over NATS, consumes the bounded post-enqueue receipt proving acceptance, holds the request queued beyond its fixed receipt deadline, releases the first request, and proves the second expires with zero second backend call and zero second publication. The observer is post-fact, infallible, synchronous, bounded, and cannot affect enqueue, deadlines, branching, replies, or authority. Its fresh tree/token/case-bound private/public callsite receipt digests are mandatory fields in `private_queue_expired` and `public_queue_expired`; helper-only rows cannot validate without them. The checker separately joins the first exact library test's worker-budget receipt with `full_service_path_constructor_deadline_is_exact_and_receipt_bound`'s real-constructor receipt to mint `deadline_budget_constructor_exact`. Every receipt binds the same frozen tree, fresh harness token, case ID, launcher, and manifest. Omission, reuse, cross-tree/case/token substitution, result fabrication, digest refresh, or a live target that performs unauthorized store work fails before the affected row exists. In normal/debug/release/all-feature libraries, `start_inner`, the observer trait, and the Noop implementation remain private and downstream-unnameable; only the `#[cfg(test)]` builder, recorder, test events, receipt controls, and setters must be absent from non-test artifacts and symbol inventories.

The legacy `request_deadline_millis` fields inside bound service configuration remain bounded identity inputs for Task 04-02 compatibility but cannot determine r14 receipt, handler, proxy-exchange, or response-publication deadlines. The conformance checker must reject any production use of those fields in the deadline state machine. Receipt deadlines remain the exact closed constants 2,000/3,000/10,000/12,000 and every asynchronous operation is bounded by the original absolute deadline or the exact private-exchange subdeadline, never a stale relative duration.

## 3. Nine observations of the production state machine

Delete `private_deadline_control`, `public_deadline_control`, and helper-created `DeadlineControlEvidenceV1` counters. The first eight ledger rows must be derived from recording implementations at the same seams used by production; the ninth is a separate closed budget/constructor oracle:

1. `private_queue_expired`: actual bounded private ingress dequeues after its receipt deadline; zero preflight/store/publish.
2. `private_preflight_expired`: actual decode/verify/admission/preflight gate advances the deterministic clock to expiry; zero store/publish.
3. `private_store_crosses_deadline`: actual recording store call begins before the original deadline, crosses it under `timeout_at`, records one store call, and publishes zero replies.
4. `private_response_enqueue_expired`: actual store response completes before expiry, deterministic clock expires before the real enqueue/publish seam, records one store call and zero publication.
5. `public_queue_expired`: actual bounded public ingress dequeues expired work; zero private/store/publish.
6. `public_private_exchange_crosses_deadline`: actual dispatcher invokes the recording private proxy once and its absolute exchange timeout fires; zero public publication/retry.
7. `public_response_enqueue_expired`: actual dispatcher completes and signs before expiry, deterministic clock expires at the real response enqueue/publish seam, zero publication/retry.
8. `post_cas_timeout_outcome_unknown`: actual recording CAS applies once, the confirming/private response crosses the absolute deadline, requester observes `OutcomeUnknown`, and CAS/retry counts are exactly 1/0.
9. `deadline_budget_constructor_exact`: the checker joins two fresh receipts for the same frozen tree/token/case. The exact library FQN contributes only the production worker-budget constants 2,000/3,000/10,000/12,000 and 1,000/1,000/2,000 reserve arithmetic. The exact normal-NATS integration target contributes actual calls to `NatsPublicWitnessStoreProxyClient::new` at 0, 2,999, 3,000, 3,001, and `u64::MAX`, with only 3,000 accepted and zero store operations. The canonical row contains both ordered observation sets and their digests, states `live_nats_grants_proved=false`, and makes no claim about server-side `allow_responses` duration or cardinality; A2 owns and live-proves those grants.

For each public/private path, factor one crate-private subscriber-admission function called immediately after `subscription.next()` and before any other per-message statement. That function alone validates the raw subscription message, captures the receipt instant from the injected monotonic clock, derives the absolute deadline from the fixed production budget, constructs the typed `PublicIngressMessage` or `PrivateIngressMessage`, and performs the bounded enqueue. The NATS subscriber loops may only delegate to it; they cannot capture, replace, extend, or recompute a deadline or construct an ingress value themselves. The exact library FQN must submit the same raw message type to those functions with the paused test clock and real bounded senders. Then factor one crate-private worker body per path for the single dequeue-to-publish transition. Each body accepts a private, infallible, synchronous `WorkerTransitionObserverV1` whose production instantiation is `NoopWorkerTransitionObserverV1`. The observer has no result, async work, mutation handle, reply authority, configuration input, or control over branching; it can observe only post-fact transition events. Emit exact typed events from the real code at dequeue, post-preflight, proxy/store begin/end, CAS attempted/applied observation, response-enqueue attempt, and publish attempt. Normal/debug/release/all-feature external APIs expose neither subscriber seam, observer, nor events.

Put `DeadlineTestClockV1`, `DeadlineTestGateV1`, `RecordingWorkerTransitionObserverV1`, the event-to-row reducer, authenticated Ready/in-memory-store/public-dispatcher test fixture, worker-budget receipt serialization, and the exact test in a library-internal `#[cfg(test)] mod deadline_state_machine_tests` in `src/lib.rs`; its fully qualified test name is `deadline_state_machine_tests::deadline_state_machine_is_receipt_anchored_and_mutation_sensitive`. The test drives raw received messages through both exact production subscriber-admission functions, their real bounded mpsc senders/receivers, the exact production worker functions, real preflight/dispatcher/proxy/store/CAS logic, real enqueue/publisher abstraction, and paused Tokio clock. It emits exactly eight transition rows plus a separate budget-only receipt; it cannot mint row 9. CAS facts come from the recording `WitnessAtomicStore`; publication facts come from the recording publisher; all other facts come from the production-emitted event sequence. The reducer may count and validate events but may not invent, default, repair, or directly assign a row counter. The ledger writer only serializes reducer output.

The focused checker must run through the normal NATS harness and execute exactly three positive targets: (1) `cargo test -p swarm-governance-witness --lib --locked --offline deadline_state_machine_tests::deadline_state_machine_is_receipt_anchored_and_mutation_sensitive -- --exact --test-threads=1`; (2) `cargo test -p swarm-governance-witness --lib --locked --offline deadline_state_machine_tests::subscriber_callsite_is_receipt_anchored_and_mutation_sensitive -- --exact --test-threads=1`; and (3) `cargo test -p swarm-governance-witness --test full_service_path --locked --offline full_service_path_constructor_deadline_is_exact_and_receipt_bound -- --exact --test-threads=1`. Each target must run once with zero failed/ignored and the pinned inventories must contain no second match. The checker consumes eight transition rows plus the budget receipt from target 1, binds target 2's private/public post-enqueue callsite receipt digests into rows 1 and 5, and joins target 3's constructor receipt into row 9 after validating identical tree/token/case/launcher/manifest bindings. It must report 9/9, kill exactly 18 canonical-ledger mutations, compile default/all-feature debug/release non-test libraries, pass a positive downstream client build, and kill exact private-API probes plus artifact-symbol checks for exactly 10 boundary receipts.

In confined exact-tree copies, target 1 must kill exactly 19 coherent compiled mutants: the original receipt reset, queue-check deletion, preflight-check deletion, pre-store-check deletion, relative-timeout substitution, late publication, retry, CAS-ambiguity downgrade, fabricated counter, helper bypass, worker-budget substitution, private/public reset after dequeue, and private/public subscriber capture reset, capture-after-queue, and overlong-deadline substitutions. Target 2 must kill exactly seven coherent compiled real-loop mutants: private and public callsites inline cardinality-preserving typed ingress construction with a fresh/overlong deadline while retaining an unreachable helper reference; drop request two before bounded enqueue; fabricate a receipt without successful enqueue; capture/emit the receipt only after acceptance with a refreshed deadline; replace `PublicWitnessServiceRunner::start` delegation with a separate fresh/overlong legacy subscriber loop while leaving the correct private `start_inner` unreachable; and apply the same delegation bypass independently to `StoreProxyServiceRunner::start`. Target 3 must kill exactly five coherent compiled mutants: constructor predicate substitution, constructor-receipt omission, constructor-result fabrication, real-constructor delegation deletion, and delegation to an alternate permissive predicate. Every mutant must compile, execute exactly one copy of its declared exact target, and fail for the intended behavioral reason; compiler failure, zero execution, generic failure, source scanning, or same-engine lexical presence is not mutation evidence. All 31 full mutated-source digests must be unique.

## Exact r35 cumulative scope and serial gates

Across A1, A2a, A2b1, A2b2, A2b3, A2c, and A3, the cumulative candidate owns the original fifteen paths plus `crates/swarm-governance-witness/tests/full_service_path.rs`: exactly 16 sorted paths, digest `1decd3b39a6433d9358a10289d6886f67211b2560563b417381273dd1662d614`. The per-slice path sets and digests above are independent acceptance boundaries, not advisory partitions. A2a is nine paths/digest `919bf3addbcdbf8625f055f9c8737f22aa5b9d3dd99130fe8e62fc981b65d4c9`; A2b1 is four/digest `ccd00d773edc3826aecd28bf6d451d0d2a5323ee766224e01b5633cb3815070a`; A2b2 is five/digest `964b80e1d926ccc78f1e2f487edb84169a01f1cb6a1eadcf3b294c3234ce98ff`; A2b3 is eleven/digest `4d9d95a9253af2cede7f42175322169a4deb3fafebc8fb0d58bcb845cd992adf`; A2c is four/digest `8b37d06097626b9d3f302ea1053ced7852e5553f095c8ead99964c8bca66b6c5`. A2b3 and A2c preserve the exact fourteen-path Task02 cumulative digest `4e6629733654f7b4b6f1233bfaaef96a0fede0368620380c8b97f9d75bb18664`. The 58-row registry remains unchanged; exactly two bins exist; init and three-bin dependency closure remain 04-03B scope.

Root freezes and independently reviews A2a, A2b1, A2b2, A2b3, A2c, and A3 separately as local production commits, never staged indexes. A2b1 reconstructs from accepted A2a; rejected `ef4c043` is diagnostic-only. A2b2 and A2b3 each start only after both exact predecessor refs are fetched. A2b3 executes normal-grant and relay-positive gates but cannot claim the final independent oracle. A2c adds no production behavior: it requires the positive immutable ledger to pass the same independently decoded/recomputed predicate first, rejects ambient corpus controls, freezes the exact 38-name inventory, and runs every coherent mutant through a distinct real-target selector with stable reason and vacuous=0. A3 reruns all accepted seams before the eighteen recovery rows and abort matrix. Every task mechanically verifies both predecessor origin refs, exactly one parent, and exact predecessor parentage. Task 04-03B is wave 12. Only an independently accepted commit may be pushed to its named work plus checkpoint refs. No combined monolithic A2 or A1-A3 candidate is authorized.
