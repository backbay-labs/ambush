---
phase: 285-assurance-foundation-closure
plan: "04"
task: "285-04-03A"
type: rejected-candidate-redesign
status: pending-independent-review
revision: r12
supersedes_planning_commit: 1938c8a229a5b917ce636604cfc377d2c932aa00
supersedes_candidate_tree: 80e073974373cac2f4737ae22bf957ae16e80524
production_base_commit: 5ade79141a2f8249ef084d24fcfd5ef12251e48e
owned_path_count: 16
owned_paths_sha256: 1decd3b39a6433d9358a10289d6886f67211b2560563b417381273dd1662d614
---

# Task 285-04-03A r12 state-machine redesign

This stable-filename addendum is revision r12. It supersedes planning commit `1938c8a`, the rejected r10 candidate, and only conflicting r10 clauses in `285-04-PLAN.md`. All other r10 recovery, abort, topology, integrity, sequencing, and non-claim requirements remain mandatory. Production repair must start again from accepted Task 04-02 commit `5ade791`; every rejected worktree/tree is read-only diagnostic evidence.

## 1. Harness-owned bounded response loss

Remove `PublicWitnessPublishSuppressionV1`, `PublicWitnessServiceExecutionReceiptV1`, `start_with_publish_suppression`, deadline-control exports, and every related re-export from the library. No debug, release, default-feature, or all-feature library artifact may expose reply-suppression authority or an unbounded channel.

Implement lost response entirely in `service_checkpoint.rs` plus the owned NATS harness as an explicit confined relay route, not an in-process bridge or direct service call. For each of the nine literal public subjects, the requester credential publishes the unchanged canonical `RuntimeWitnessClient` bytes to a relay-only fixed subject with its original reply route `R`. The requester has no direct import to the witness service subject. Before receiving, the relay reserves its sole capacity-one evidence slot; it consumes exactly one request, authenticates and records the exact fixed subject, body bytes, nonce, and digest, and republishes those bytes exactly once over the sole relay-to-witness fixed-subject import with a freshly allocated relay sink `B`, where `B != R`. The unchanged `PublicWitnessServiceRunner` processes that request through its real handler and backend. The relay receives the actual canonical post-handler response on `B`, authenticates it, binds scenario ID, operation, request nonce/digest, response digest, exact recording-store backend sequence, and runner/connection identity, then deliberately performs zero publishes to `R`. The independent requester must time out; mutation operations whose CAS may have applied must surface `OutcomeUnknown`; retries remain zero. Normal replay uses a fresh relay instance that performs the same one-request route and forwards the authenticated `B` response to `R` exactly once.

The harness must materialize exact requester, relay, witness, and store principals with fixed non-wildcard imports/exports for this graph; no requester-to-witness or requester-to-store route, relay wildcard, raw store authority, or second delivery route may exist. If the capacity-one slot is full, closed, missing, or cannot accept the receipt, the relay refuses to arm loss and forwards the response normally. The focused checker must kill manual-requester serialization, relay bypass, `R == B`, duplicate/direct witness delivery, wildcard permission, unrelated-inbox timeout, request drop before the handler, pre-handler/pre-response receipt, fabricated receipt, delivered first reply, capacity greater than one, unbounded channel, repeated arm, omitted sink receive, and any production/debug/all-feature suppression symbol.

## 2. Exact private-hop deadline without legacy weakening

`NatsPublicWitnessStoreProxyClient::new` remains source-compatible but must require `request_deadline_millis == STORE_RESPONSE_GRANT_MILLIS`; reject 0, 2,999, 3,001, `u64::MAX`, predicate deletion, comparison substitution, and constant substitution. Production callers continue to pass the constant. Update the single inherited direct-client fixture in `full_service_path.rs` from 1,000 to exactly 3,000 and add one literal runtime oracle that invokes the public constructor with exactly 0, 2,999, 3,000, 3,001, and `u64::MAX`, requiring only 3,000 to succeed before any NATS/store operation. The pinned `full-service-path` selector must execute this oracle; compilation alone is not evidence. This is the sole newly writable sixteenth path; no other Task 04-02 behavior or fixture is changed.

The legacy `request_deadline_millis` fields inside bound service configuration remain bounded identity inputs for Task 04-02 compatibility but cannot determine r12 receipt, handler, proxy-exchange, or response-publication deadlines. The conformance checker must reject any production use of those fields in the deadline state machine. Receipt deadlines remain the exact closed constants 2,000/3,000/10,000/12,000 and every asynchronous operation is bounded by the original absolute deadline or the exact private-exchange subdeadline, never a stale relative duration.

## 3. Nine observations of the production state machine

Delete `private_deadline_control`, `public_deadline_control`, and helper-created `DeadlineControlEvidenceV1` counters. The exact nine ledger rows must be derived from recording implementations at the same seams used by production:

1. `private_queue_expired`: actual bounded private ingress dequeues after its receipt deadline; zero preflight/store/publish.
2. `private_preflight_expired`: actual decode/verify/admission/preflight gate advances the deterministic clock to expiry; zero store/publish.
3. `private_store_crosses_deadline`: actual recording store call begins before the original deadline, crosses it under `timeout_at`, records one store call, and publishes zero replies.
4. `private_response_enqueue_expired`: actual store response completes before expiry, deterministic clock expires before the real enqueue/publish seam, records one store call and zero publication.
5. `public_queue_expired`: actual bounded public ingress dequeues expired work; zero private/store/publish.
6. `public_private_exchange_crosses_deadline`: actual dispatcher invokes the recording private proxy once and its absolute exchange timeout fires; zero public publication/retry.
7. `public_response_enqueue_expired`: actual dispatcher completes and signs before expiry, deterministic clock expires at the real response enqueue/publish seam, zero publication/retry.
8. `post_cas_timeout_outcome_unknown`: actual recording CAS applies once, the confirming/private response crosses the absolute deadline, requester observes `OutcomeUnknown`, and CAS/retry counts are exactly 1/0.
9. `deadline_topology_exact`: the production constants and actual runner/grant construction prove 2,000/3,000/10,000/12,000 with 1,000/1,000/2,000 reserves and max-one response grants.

Use the production state machine with private clock/gate injection, not a parallel helper implementation. Put `DeadlineTestClockV1`, `DeadlineTestGateV1`, recording seam adapters, ledger construction, and the exact test in a library-internal `#[cfg(test)] mod deadline_state_machine_tests` in `src/lib.rs`; its fully qualified test name is `deadline_state_machine_tests::deadline_state_machine_is_receipt_anchored_and_mutation_sensitive`. None of those test items is `pub`, `pub(crate)` outside that module, re-exported, feature-gated, or reachable from an integration target. The production queue/runner/handler implementation accepts only its existing private clock/gate seam, whose normal instantiation remains the monotonic production clock. The private unit test drives the actual queue, preflight, proxy/store, CAS, signer, response-enqueue, and publisher state transitions and observes counters only through recording implementations of those same production seams. The ledger writer may serialize observations but may not assign or repair counters.

The focused checker must run exactly `cargo test -p swarm-governance-witness --lib --locked --offline deadline_state_machine_tests::deadline_state_machine_is_receipt_anchored_and_mutation_sensitive -- --exact --test-threads=1`, prove one exact fully qualified test passes with zero failed/ignored and the pinned library inventory has no second match, and consume the fresh tree/token/case-bound nine-row ledger from that execution. It must also compile default, all-feature, debug, and release non-test library artifacts and prove the private test symbols/API are absent. In isolated source copies, run that exact private unit test against receipt-reset, queue-check deletion, preflight-check deletion, pre-store-check deletion, relative-timeout substitution, late-publication, retry, CAS-ambiguity downgrade, fabricated-counter, helper-bypass, and topology substitution mutants. Every mutant must compile, execute the exact case, and fail for the intended behavioral reason.

## Exact r12 scope and gate

The r12 candidate owns the original fifteen paths plus `crates/swarm-governance-witness/tests/full_service_path.rs`: exactly 16 sorted paths, digest `1decd3b39a6433d9358a10289d6886f67211b2560563b417381273dd1662d614`. The integrity launcher remains byte-identical; the 58-row registry remains unchanged; exactly two bins exist; init and three-bin dependency closure remain 04-03B scope.

Root must freeze an exact staged tree with zero unstaged/untracked paths. Before commit, independent review must return P0/P1/P2 = `0/0/0` and the pinned gate must pass: integrity self-test, public-dispatcher, harness self-test, the harnessed pinned `full-service-path` selector that executes the five-value constructor oracle, governance library serial, witness all-target offline compile, focused recovery plus private deadline-unit checker, strict combined clippy, formatting, shellcheck, diff checks, exact 16-path identity, and zero Desktop Phase 285 residue. Only the exact reviewed tree may be committed and pushed to work plus checkpoint refs.
