---
phase: 285-assurance-foundation-closure
plan: "04"
task: "285-04-02"
type: immutable-candidate-freeze
status: review-required
base_commit: f52a25bc798dbdd0bd0bcff036b165ce8dc82119
base_tree: c8b49c5785335d1d8dbd27e64e71b06525c59c06
candidate_tree: 3e52c75335c693cc46e10a5e23248cb5d9b7e694
accepted_plan_commit: 952cc28df79b0e473a923d00825f00b773aaec47
accepted_task01_ledger_commit: 2613c5b4d62213fa29af6a749e4f0263db5cf61b
launcher_sha256: e59ba9f62bf126bccdf8c0d3331b54adae9e74f8fe1ee6e31d43e3dec9ca66b1
manifest_sha256: d2c6c0f313a939b03e9c33a20dc0802418bbc054adab630d07c009ab2f615caa
checker_sha256: 2386a4abb2fc70fd87db27dc8e745b3916739e7e51defca7985a56c10cad0bf0
registry_sha256: a3a3ec459600ac3163a9b66aa40aa39e9387c50cc75b1e765d9f0693ddb8983b
task_delta_sha256: ea17ec31b1a30a4518eea705cb8bf95fcb48cfb2d8d3bb4274d14949a1206c2e
task_paths_sha256: c05e77f6c18eb1b75f067afc2ad0c8de68c9f44afa92f92a45f61777e7cb8c0c
---

# Task 285-04-02 immutable candidate freeze

This object pins the repaired cumulative Plan 04 candidate after Task 04-02 and supersedes rejected trees `1636b69f7056446b161832cf77280c05c0ee58c8`, `183a6e1b4b62f88846c504113cde3068fd650897`, and `795b824ca4409ada27530919fc31eef40cc986a5`. Production code cannot author or refresh these values. The root integrator obtained the candidate from the exact staged index on accepted Task 04-01 commit `f52a25b`; the working tree had no unstaged or untracked paths, `git diff --cached --check` passed, and the Task 04-02 delta contained exactly the eight authorized paths below.

## Exact Task 04-02 delta and file identities

| Path | SHA-256 |
|---|---|
| `crates/swarm-governance-witness/src/lib.rs` | `9be0447153c829b8f83bd982ca9996e33c1e82c778903ece9ef9db9ba22139ea` |
| `crates/swarm-governance-witness/src/service_config.rs` | `8b00cd446769a4c9f7dc632a1b643e2e131bbc4902899691e0bf8166fd75df75` |
| `crates/swarm-governance-witness/src/store_proxy_service.rs` | `6af18f54c8751729bfa2b0577643e4412438f5f20b2ed772eaeced349271ec4d` |
| `crates/swarm-governance-witness/tests/full_service_path.rs` | `a60803a87c59efdfccee865bff740c742f5ff3c1bdf73a0f1e0a5e1131cfe950` |
| `docker-compose.yml` | `d4a2dace2dcf5632e70f18758a9bfa9c420c9b38702c79864059ee06fb0fd8ca` |
| `tools/check-phase285-witness-conformance.sh` | `2386a4abb2fc70fd87db27dc8e745b3916739e7e51defca7985a56c10cad0bf0` |
| `tools/fixtures/phase285-witness-integrity.json` | `d2c6c0f313a939b03e9c33a20dc0802418bbc054adab630d07c009ab2f615caa` |
| `tools/with-nats-jetstream.sh` | `0c7c58a727bc8e226a66488b694b1f93cbf21775d20f103d229a6c3982b2a392` |

The accepted Plan 03B integrity launcher remains byte-identical and outside the Task 04-02 delta.

## Executor evidence submitted for independent verification

- Genuine RED evidence includes both implementation seams: on exact base `f52a25b`, the new literal full-service test executed zero tests with four filtered and the registered selector failed closed with `missing target`; on rejected tree `183a6e1b`, the focused TLS test exited 101 because a connection validated from configuration A successfully started a service validated from configuration B with a distinct deadline.
- The private typed service subscribes to the three fixed proxy subjects and accepts only an opaque store-role connection constructed from validated configuration. Both the service and connection retain a private SHA-256 binding over canonical full configuration plus exact Ready evidence, computed only after `validate_for_ready`; the runner compares the typed bindings in constant time before JetStream, subscriptions, task spawning, or raw-store access. The connector consumes the exact fresh credential path, requires TLS, pins CA/server name, authenticates the exact store account/role token, and applies bounded client capacities and deadlines; the runner cannot accept an arbitrary `async_nats::Client`.
- Fifteen coherent A/B substitution controls reject differences in credential path/token, TLS URL/CA/server name, subscription/client/read/worker capacities, deadline, stream, epoch, anchor, admission set/digest, and selected-entry limits. Every mismatch returns `Configuration` with zero raw-store calls. The source guard kills 42 unique mutants that delete, weaken, self-bind, or cross-copy the comparison.
- The inherited public-dispatcher guard parses the exact ordered seven-element pre-store `FIELDS` array rather than globally counting field-name literals across the integration source. Negative controls reject omission, substitution, duplication, and ordering drift; a positive control proves an unrelated `"stream"` literal is tolerated.
- Preflight returns the opaque selected admission entry and enforces both selected request and response ceilings. Normal and overload canonical responses are checked against the authenticated selected response ceiling; two-stream exact-max/max-plus-one controls kill wrong-entry and global-ceiling substitutions and assert exact store-call behavior.
- The harness materializes exactly three accounts and four fresh confined principals, distinct per-run credentials, fixed one-way imports/exports, bounded responses, no wildcard, no cross-role credential mount, TLS listener, pinned CA/server name, and role validation. Harness self-test covers topology, environment, credential binding, matcher, and source mutations and leaves zero Phase 285 projects on the evidence daemon.
- Wrapped cross-account proxy execution passed `1/1`. The wrapped witness package passed library `15`, full-service `8`, JetStream CAS `5`, and checkpoint `4`. Governance conformance passed `22/22`; governance library passed `133/133` serially.
- Cumulative `full-service-path` reported top-level `4/4`, capability matrix `20/20`, capability-ledger mutants `14/14`, source mutants `42/42`, and top-level registry mutants `8/8`. Each capability row is bound to its exact credential/account/subject, unique invocation token, expected failure, and store-call count; live unauthorized NATS attempts or isolated mutated TLS/topology instances cover credential, account, subject, reply, mount, and wildcard seams. The real runner covers reply-payload refusal and queue exhaustion. The top-level registry remains exactly 58 rows at the frozen digest. Public-dispatcher regression passed `4/4`, mapping `9/9`, inner ledger `9/9`, and source mutants `266/266`.
- Dependency closure reported no forbidden edge. Strict combined all-target/all-feature clippy, formatting, shellcheck, actionlint, and diff checks passed. Integrity self-test killed `14/14` mutations with locally computed development pins.
- Both default-parallel and serial governance library runs passed `133/133`.

These are executor claims, not acceptance. The reviewer must reconstruct Git tree `3e52c75335c693cc46e10a5e23248cb5d9b7e694`, verify all eight hashes plus the exact task path and binary-diff digests, audit semantics and evidence, and return P0/P1/P2 = `0/0/0` before the root supplies the pinned hashes to the authorization-critical wrapped selector.

## Explicit host residue and non-claims

- Acceptance evidence ran on the clean `desktop-linux` Docker context and verified zero Phase 285 projects there after teardown. Two stopped Phase 285 containers remain inside the separate broken Colima VM because containerd's metadata database returns I/O errors. They are excluded from candidate evidence; no unrelated Colima resource was touched.
- No Task 04-03 binary, runtime adapter, restart/checkpoint, hosted, protected-check, full Plan 04, full Phase 285, external App enforcement, or later-phase claim is made.
