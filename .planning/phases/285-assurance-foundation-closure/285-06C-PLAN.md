---
phase: 285-assurance-foundation-closure
plan: "06C"
type: execute
wave: 11
depends_on:
  - "285-06B"
files_modified:
  - crates/swarm-governance/src/persistence_coordinator.rs
  - crates/swarm-governance/src/persistence_coordinator/epoch_transition.rs
  - crates/swarm-governance/tests/phase285_governance_persistence.rs
  - crates/swarm-core/src/config/runtime.rs
  - crates/swarm-core/src/config/root.rs
  - crates/swarm-core/src/config/validation.rs
  - crates/swarm-core/src/config/tests.rs
  - crates/swarm-runtime-http/Cargo.toml
  - Cargo.lock
  - crates/swarm-runtime-http/src/lib.rs
  - crates/swarm-runtime-http/src/witness_blocking_worker.rs
  - crates/swarm-runtime-http/src/bin/swarm_detect.rs
  - crates/swarm-governance/tests/phase285_witness_conformance.rs
  - crates/swarm-cli/src/core.inc
  - tools/check-phase285-governance-persistence.sh
  - tools/check-phase285-witness-conformance.sh
  - tools/fixtures/phase285-witness-integrity.json
  - tools/check-phase285-closure.sh
autonomous: true
requirements:
  - ASSURE-04
  - ASSURE-06
must_haves:
  truths:
    - "The shipped detector constructs enforced governance only after the real authenticated witness bridge proves configuration, admission, fence, head, anchor, namespace, and session readiness; removing or swapping the witness refuses before authority mint or local mutation."
    - "The runtime bridge worker is real and bounded: `swarm-runtime-http/src/lib.rs` declares it, a dedicated OS thread owns a current-thread Tokio runtime, and no async runtime thread calls `Handle::block_on` or `block_in_place`."
    - "The through-06C checkpoint runs every earlier exact selector plus retention, offline-maintenance, detector-integration, dependency closure, and strict gates on one clean exact tree, records C_06/T_06 externally as checkpoint-only, and leaves App enforcement deferred and later phases blocked."
  artifacts:
    - path: "crates/swarm-runtime-http/src/witness_blocking_worker.rs"
      provides: "Runtime-owned bridge worker mapping opaque governance requests to RuntimeWitnessClient with outage/cancellation/backpressure handling."
      contains: "WitnessBlockingWorker"
    - path: "crates/swarm-runtime-http/src/lib.rs"
      provides: "Explicit witness_blocking_worker module declaration owned by this plan."
      contains: "mod witness_blocking_worker"
    - path: "crates/swarm-runtime-http/src/bin/swarm_detect.rs"
      provides: "Witness-first production construction, all-consumer authority handoff, closed detector config, and witness-backed epoch reinitialization."
      contains: "ShippedGovernanceWiring"
    - path: "crates/swarm-governance/tests/phase285_witness_conformance.rs"
      provides: "Combined checkpoint exact-selector and stale/dirty/slice-claim mutations."
      contains: "combined_checkpoint"
    - path: "crates/swarm-cli/src/core.inc"
      provides: "Offline governance migration command routed through the now-materialized authenticated witness worker and epoch transition."
      contains: "MigrateGovernanceLock"
    - path: "tools/check-phase285-witness-conformance.sh"
      provides: "Materialized target inventory registration for every combined checkpoint case before through-06C closure executes."
      contains: "combined_checkpoint_rejects_omitted_selector"
    - path: "tools/fixtures/phase285-witness-integrity.json"
      provides: "Refreshed integrity pin for the modified witness-conformance checker."
      contains: "tools/check-phase285-witness-conformance.sh"
    - path: "tools/check-phase285-closure.sh"
      provides: "Through-06C dependency-ordered checkpoint orchestrator and external C_06/T_06 evidence binding."
      contains: "through-plan 06C"
  key_links:
    - from: "GovernanceWitnessWorkerEndpoint"
      to: "WitnessBlockingWorker"
      via: "one dedicated current-thread runtime/OS thread consumes bounded typed requests and completes each response once"
      pattern: "GovernanceWitnessWorkerEndpoint.*current_thread.*RuntimeWitnessClient"
    - from: "swarm_detect::governance_policy_for_bootstrap"
      to: "GovernancePersistenceCoordinator / AuthenticatedGovernanceWitnessBridge"
      via: "witness readiness and guard transfer precede one shared ShippedGovernanceWiring authority"
      pattern: "with_witness_backed_persistence.*ShippedGovernanceWiring"
    - from: "tools/check-phase285-closure.sh --through-plan 06C"
      to: "C_06/T_06 external checkpoint ledger"
      via: "clean exact subject tree, explicit selector list/counts, and checkpoint_only status"
      pattern: "checkpoint_commit.*checkpoint_tree.*checkpoint_only"
---

<objective>
Complete the real detector integration and freeze the combined-through-06C checkpoint.

Purpose: The governance bridge is only useful when the shipped runtime consumes it without deadlock or a local bypass, and the implementation must be tested as one combined tree before deployment/final closure work.
Output: Runtime-owned blocking worker, witness-first `swarm-detect` construction and epoch transition, exact detector configuration, and external C_06/T_06 checkpoint evidence.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/285-assurance-foundation-closure/285-CONTEXT.md
@.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md
@.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md
@.planning/phases/285-assurance-foundation-closure/285-VERIFICATION.md
@.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md
@<.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md>
@crates/swarm-governance/src/lib.rs
@crates/swarm-governance/src/persistence_coordinator.rs
@crates/swarm-governance-witness/src/runtime_client.rs
@crates/swarm-runtime-http/src/lib.rs
@crates/swarm-runtime-http/src/bin/swarm_detect.rs
@crates/swarm-cli/src/core.inc
@crates/swarm-core/src/config/runtime.rs
@tools/check-phase285-closure.sh

Plan05C's `GovernanceWitnessBridgeChannel::bounded -> GovernanceWitnessBridgeHandle + GovernanceWitnessWorkerEndpoint -> AuthenticatedGovernanceWitnessBridge` is the interface. This plan owns the real runtime worker and the `swarm-runtime-http/src/lib.rs` module declaration; the worker file is new and must not appear in this task's `read_first` list. The external GitHub App/protected-required boundary remains deferred.
</context>

<tasks>

<task type="auto">
  <wave>11</wave>
  <name>Task 285-06-04: Integrate the real witness worker and witness-first detector construction</name>
  <files>crates/swarm-governance/src/persistence_coordinator.rs, crates/swarm-governance/src/persistence_coordinator/epoch_transition.rs, crates/swarm-governance/tests/phase285_governance_persistence.rs, crates/swarm-core/src/config/runtime.rs, crates/swarm-core/src/config/root.rs, crates/swarm-core/src/config/validation.rs, crates/swarm-core/src/config/tests.rs, crates/swarm-runtime-http/Cargo.toml, Cargo.lock, crates/swarm-runtime-http/src/lib.rs, crates/swarm-runtime-http/src/witness_blocking_worker.rs, crates/swarm-runtime-http/src/bin/swarm_detect.rs, crates/swarm-cli/src/core.inc, tools/check-phase285-governance-persistence.sh</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md; .planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance/src/lib.rs (opaque bridge and witness-backed constructors from Plan05C); crates/swarm-governance/src/persistence_coordinator.rs; crates/swarm-governance/src/persistence_coordinator/maintenance.rs; crates/swarm-governance/src/persistence_recovery.rs; crates/swarm-governance-witness/src/runtime_client.rs; crates/swarm-runtime-http/Cargo.toml; Cargo.lock; crates/swarm-runtime-http/src/lib.rs; crates/swarm-runtime-http/src/bin/swarm_detect.rs (governance_policy_for_bootstrap, ShippedGovernanceWiring, reinitialize branch); crates/swarm-cli/src/core.inc (MigrateGovernanceLock command, identity/key/admission loading, fail-closed placeholder, former local migrate_persistence_lock route); crates/swarm-core/src/config/runtime.rs; crates/swarm-core/src/config/root.rs; crates/swarm-core/src/config/validation.rs; tools/check-phase285-governance-persistence.sh</read_first>
  <action>Work red-first by adding the seven exact runtime-http tests `detector_integration_refuses_without_authenticated_witness_before_authority_mint`, `detector_integration_shares_selector_and_authority_across_all_trust_consumers`, `detector_integration_reinitialize_requires_epoch_plus_one_and_retired_head`, `detector_integration_removing_witness_cannot_reach_local_constructor`, `detector_integration_rejects_unknown_or_cross_role_config`, `detector_integration_worker_deadlock_outage_cancellation_and_backpressure`, and `detector_integration_compile_time_rejects_local_constructor_toggle` before changing construction. Add the direct `swarm-governance-witness` dependency to `crates/swarm-runtime-http/Cargo.toml`, update `Cargo.lock` in the same task, and keep its resolved closure downstream-only. In the existing runtime-http `src/lib.rs`, declare/export `witness_blocking_worker` in this task; create `WitnessBlockingWorker` in the new file. It consumes Plan05C's `GovernanceWitnessWorkerEndpoint`, owns a dedicated OS thread with a dedicated current-thread Tokio runtime, constructs `RuntimeWitnessClient`, maps every closed bridge request exactly once to the corresponding async witness operation (including `issue_session_fence`, establish/discover, actual `prepare_successor`, reads, the exact session-bound `fetch_payload`, commit, and abort), propagates typed outage/cancellation/timeout/backpressure, and never calls `Handle::block_on` or `block_in_place` from an async runtime thread. Runtime-http owns the real worker deadlock/outage/cancellation/backpressure tests; Plan05C's local mock remains independent. Only after that real worker and the `swarm-governance-witness` runtime-http dependency compile, replace the Plan05C fail-closed `MigrateGovernanceLock` CLI placeholder in `swarm-cli` with the already-declared `swarm-runtime-http` dependency's exported `WitnessBlockingWorker`: add exact test `governance_lock_migration_cli_requires_authenticated_witness_epoch_transition`, load the admitted Tom identity plus closed witness service/admission/binding configuration, obtain the selector-held quiescence guard, establish the authenticated bridge/session fence, and invoke `GovernancePersistenceCoordinator::migrate_legacy_stream_offline`. Remove the old direct `GovernancePolicy::migrate_persistence_lock` route; make missing service, wrong admission, absent quiescence, unsigned legacy bytes, epoch jump, lost response, worker outage, and attempted local fallback fail before mutation, and treat `--confirm-offline` alone as non-authoritative. In `swarm_detect.rs`, replace every enforced `GovernancePolicy::initialize_persistence`/`with_persistence` route in `governance_policy_for_bootstrap` and reinitialize handling with the worker, bridge readiness handshake, `GovernancePolicy::with_witness_backed_persistence`, authenticated admission/fence/head/anchor/namespace/session checks, and one shared `ShippedGovernanceWiring` selector/authority identity reaching ingest, dispatcher, containment, human resume, health, and recovery. Refuse before authority mint or local mutation when witness removal, service outage, wrong key/account, stale fence/head, anchor/manifest drift, binding/pool mismatch, selector swap, or session failure occurs. Add `WitnessDetectorConfigV1` with `#[serde(deny_unknown_fields)]` role-specific identity/key/epoch/anchor/admission/TLS/server-name/limit/NATS-version/image fields and no production defaults; reject cross-role paths, plaintext/insecure TLS, tag-only versions, and combined superset configs. Keep `GovernancePersistenceCoordinator` as the sole coordinator type: implement private epoch-transition helpers in `persistence_coordinator/epoch_transition.rs` and expose them only through `GovernancePersistenceCoordinator::transition_epoch` for `--reinitialize-governance-state`. Require authenticated `epoch + 1`, retired old witness-head digest, quiescence and authority-pair proof, new complete binding, and preserved legacy payload digest; reject arbitrary jumps and local archive/reset. Add compile/lexical controls that no shipped runtime feature or detector option can select old local constructors. Preserve App enforcement as explicitly deferred; do not add another coordinator type or later-phase behavior.</action>
  <selector_contract>Extend the governance wrapper with a seventh exact selector `cli-migration`, mapped only to package `swarm-cli`, library target `swarm_cli`, and exact test `governance_lock_migration_cli_requires_authenticated_witness_epoch_transition`. Invoke it individually with `--exact`, parse the same executed/passed/failed/ignored/filtered counts as every baseline selector, and give it exactly the eight mutation IDs frozen by Plan05A. Missing, duplicate, extra, renamed, substring-only, filtered-only, ignored, zero, or wrong-target CLI evidence must fail before the selector reports green.</selector_contract>
  <verify>
    <automated>bash tools/check-phase285-governance-persistence.sh detector-integration && bash tools/check-phase285-governance-persistence.sh cli-migration</automated>
    The wrapper must run all seven exact runtime-http detector tests and the exact CLI migration test must execute once, all with `passed = executed`, `failed = 0`, and `ignored = 0`; witness removal, config drift, authority/selector swap, epoch jump, worker deadlock, cancellation, outage, absent migration quiescence, and local-constructor/fallback mutations must fail before authority or local state changes.
  </verify>
  <done>The real runtime worker consumes the opaque bridge without async deadlock, detector startup/load/reinitialize is witness-first with closed configuration and epoch+1 semantics, the CLI migration command uses that materialized worker and authenticated coordinator path, every trust consumer shares one authority identity, and no enforced local constructor or CLI fallback remains reachable.</done>
</task>

<task type="auto">
  <wave>11</wave>
  <name>Task 285-06-05: Freeze the combined-through-06C checkpoint</name>
  <integrity_contract>The through-06C selector set contains all six baseline governance selectors plus `cli-migration`. Invoke `combined-checkpoint` only through `tools/check-phase285-witness-integrity.sh` with nonempty externally supplied `PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256` and the refreshed committed-manifest `PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256`; the integrity launcher then dispatches the pinned conformance checker. Its exact sorted six-ID mutation registry is `duplicate_registry_row`, `extra_registry_row`, `missing_registry_row`, `registered_but_uninvoked_selector`, `substituted_registry_row`, and `unregistered_selector`, with `mutation_failure_count: 6`. Direct final-matrix invocation of `tools/check-phase285-witness-conformance.sh combined-checkpoint`, an ambient or internally recomputed pin, stale checker/manifest bytes, or mutation-registry drift must fail.</integrity_contract>
  <files>crates/swarm-governance/tests/phase285_witness_conformance.rs, tools/check-phase285-witness-conformance.sh, tools/fixtures/phase285-witness-integrity.json, tools/check-phase285-closure.sh</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; .planning/phases/285-assurance-foundation-closure/285-VERIFICATION.md; .planning/phases/285-assurance-foundation-closure/285-RESEARCH.md; tools/check-phase285-closure.sh; tools/check-phase285-witness-conformance.sh; tools/check-phase285-witness-integrity.sh; tools/fixtures/phase285-witness-integrity.json; tools/check-phase285-governance-persistence.sh; crates/swarm-governance/tests/phase285_governance_persistence.rs; crates/swarm-governance/tests/phase285_witness_conformance.rs; crates/swarm-runtime-http/src/bin/swarm_detect.rs; crates/swarm-runtime-http/src/witness_blocking_worker.rs; crates/swarm-governance-witness/tests/full_service_path.rs; crates/swarm-governance-witness/tests/service_checkpoint.rs</read_first>
  <action>Work red-first by adding combined checkpoint cases `combined_checkpoint_rejects_omitted_selector`, `combined_checkpoint_rejects_swapped_detector_witness`, `combined_checkpoint_rejects_dirty_tree_or_stale_commit_tree`, `combined_checkpoint_rejects_local_constructor_toggle`, and `combined_checkpoint_rejects_slice_only_completion` to the explicit witness conformance target. In the same red-first change, extend `tools/check-phase285-witness-conformance.sh`'s materialized `phase285_witness_conformance` target inventory with all five exact names before any through-06C selector runs. Register the exact selector ID `combined-checkpoint` with exactly five `(selector, target, case_id)` mappings, one for each named case and all targeting `phase285_witness_conformance`; require its direct invocation to emit exactly five distinct `SelectorRecordV1` values with `executed: 1`, `passed: 1`, `failed: 0`, `ignored: 0`, and the inventory-derived exact `filtered_out` count for each case. Emit one `SelectorMutationRecordV1` for `combined-checkpoint` whose sorted `mutation_ids` are exactly `duplicate_registry_row`, `extra_registry_row`, `missing_registry_row`, `registered_but_uninvoked_selector`, `substituted_registry_row`, and `unregistered_selector`, and whose `mutation_failure_count` is exactly six; each named mutant must prove that an absent, extra, substituted, unregistered, multiply registered, or registered-but-nonexecuted case fails for the intended inventory/selector reason. Extend `tools/check-phase285-closure.sh --through-plan 06C` with explicit individual invocations for every exact selector in Plans01-04 (`response-failure-wire`, `candidate-verifier`, `protocol-checkpoint`, `atomic-store-contract`, `in-memory-differential`, `typed-proxy`, `transport-layering`, `jetstream-cas`, `jetstream-checkpoint`, `public-dispatcher`, `full-service-path`, `service-checkpoint`), every governance selector (`fixed-lanes`, `transaction-recovery`, `enforced-checkpoint`, `retention`, `offline-maintenance`, `detector-integration`, `cli-migration`), and externally pinned `bash tools/check-phase285-witness-integrity.sh combined-checkpoint`; never invoke an undefined aggregate or an obsolete selector spelling. Preserve this integrity-launched `combined-checkpoint` invocation, its five per-case records, and its exact six-ID mutation record in Plan07B's final `--local` closure mode and hosted selector set. Because this task modifies the launcher-pinned witness-conformance checker, recompute the final committed checker's SHA-256 into `tools/fixtures/phase285-witness-integrity.json`, recompute that manifest's SHA-256 as the new externally supplied `PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256`, and require every through-06C integrity invocation/evidence record to use that exact refreshed digest; stale checker or manifest pins must fail. Also run `bash tools/check-witness-dependency-closure.sh --all-targets`, locked workspace/layering/authority/negative-registry/supply-chain gates, mapping/fixture/gates-wired checks, strict package/workspace tests, clippy/fmt, immutable subject diff checks, and clean-tree checks, and require every selected target to report executed > 0, passed = executed, failed = 0, ignored = 0. After all implementation, checker, and manifest changes are committed, capture the actual clean combined checkpoint commit and tree as `C_06` and `T_06`; rerun the through-06C command from a clean checkout of that exact subject with the refreshed external manifest digest. Emit only an external `$PHASE285_EVIDENCE_DIR/285-THROUGH-06C.json` with `phase_status: checkpoint_only`, `checkpoint_commit: C_06`, `checkpoint_tree: T_06`, exact selector/target counts, refreshed integrity-manifest digest, independent-review status, `protected_required: deferred`, external GitHub App `deferred`, Phases 286-289 `blocked`, and the explicit TCB truth that trusted-volume rollback and coordinated external-anchor rollback are outside the runtime guarantee. Mutations asserting runtime detection/protection of those out-of-scope rollbacks must fail. Do not write checkpoint JSON or `.planning` evidence into `T_06`, do not use final `C_s/T_s` names, and do not claim Phase 285 completion.</action>
  <verify>
    <automated>bash tools/check-phase285-closure.sh --through-plan 06C</automated>
    The checker must execute every Plan01-06C selector and strict gate positively, reject missing/zero/ignored/partial/dirty/stale-tree/slice-completion/non-independent-review/out-of-scope-rollback fixtures, and validate external `C_06/T_06` binding without placing evidence in the subject tree.
  </verify>
  <done>The combined-through-06C checkpoint is exact-head bound, non-vacuous, checkpoint-only, externally recorded as C_06/T_06, and explicitly leaves external App enforcement deferred and Phases 286-289 blocked; Plan07A remains required for deployment and final closure.</done>
</task>

</tasks>

<verification>
Run `bash tools/check-phase285-governance-persistence.sh detector-integration`, `bash tools/check-phase285-governance-persistence.sh cli-migration`, then `bash tools/check-phase285-closure.sh --through-plan 06C` from one clean combined tree. Confirm the runtime worker has no async blocking misuse, the CLI has no local migration fallback, all selectors/counts are positive, C_06/T_06 evidence is external and checkpoint-only, and no later-phase advancement or App protection claim is emitted. Obtain independent zero-P0/P1/P2 review of the combined checkpoint.
</verification>

<success_criteria>
Detector integration and through-06C selectors are non-vacuously green; runtime construction is witness-first with one shared authority and a real bounded worker; epoch transitions are authenticated; the closure matrix proves every earlier lane on one clean tree; and C_06/T_06 evidence is truthful, external, checkpoint-only, App-deferred, and later-phase-blocked.
</success_criteria>

<output>
After completion, create `.planning/phases/285-assurance-foundation-closure/285-06C-SUMMARY.md`.
</output>
