---
phase: 285-assurance-foundation-closure
plan: "04"
task: "285-04-03A"
type: immutable-candidate-freeze
status: rejected-at-time-boundary
base_commit: 9ff689ce71a9a05892a9c0d7eda21210e467183e
base_tree: 96310ff48db788d1cfe6a735b1e9a78d586d911b
candidate_tree: fc930f36e80a7ace9f955078ee8574dc0c2b9357
accepted_plan_commit: 1f88de7fcb19d27c8183dce7fc3db476dd42a1c5
launcher_sha256: e59ba9f62bf126bccdf8c0d3331b54adae9e74f8fe1ee6e31d43e3dec9ca66b1
manifest_sha256: 8ba4ebbb8c6caaef521632ed817628c78ad069eb2a58dfa732c964e2c04806de
checker_sha256: fba3dd4160c329c2e18d1e809bce06b90935980852fcd4fb5ff93c7cc2256abc
harness_sha256: 472ccc8561536f7256d4beabbd16d906c08300bdc1cb2cbc4a954e9fb1433cc8
service_checkpoint_sha256: 9f7b1d9810cbc0723d2734390b5a64979a498944dab9d59280fc3bd0220290e6
task_delta_sha256: 19ed58a1b211a4d7671910e2893e17eed606e37386991047b7d7199d2dc076c4
task_paths_sha256: 63891e64631df71736d4a21c852c43cec261b257b2508c10db72d2fc4c04c164
supersedes_rejected_tree: ec278370395cda1b70589b9e7e59e8c2a8d93c67
review_verdict: P0=0/P1=6/P2=0
---

# Task 285-04-03A immutable candidate freeze ledger

This root-authored object pins the r10 staged index on accepted Task 04-02 commit `5ade791` before authorization-critical execution. It has zero unstaged/untracked paths, passes cached diff checks, and contains exactly the fifteen r10-owned paths at the declared digest. The prior `7542165` candidate remains immutably rejected in history with P0/P1/P2 = `0/3/0`; no prior evidence is promoted here.

The candidate changes only the witness package manifest; witness lib, dispatcher, store proxy, raw config, runtime client, service config, and two serving bins; `service_checkpoint.rs`; governance persistence protocol and its source-local tests; the conformance checker, integrity manifest, and NATS harness. The launcher and `full_service_path.rs` are byte-identical. Exactly two bins exist; init and three-bin dependency closure remain deferred to 04-03B.

## Executor evidence awaiting hostile review

- Tree-bound focused Desktop execution: `18/18` distinct recovery rows, `9/9` behavioral deadline rows, transcript `1 passed/0 failed/0 ignored/14 filtered`, `35/35` ledger mutants and `11/11` unique source mutants killed.
- Lost-response rows bind post-handler suppression receipts to request/response/backend evidence and an independent requester timeout. Restart mutation rows use fresh fixture/service lifecycles and closed/fresh runner/connection identities.
- Receipt-anchored absolute deadlines are rechecked after preflight and immediately before store work; expiry yields zero work/publication, and possibly applied CAS yields `OutcomeUnknown` without retry.
- Public dispatcher passed `4/4`, mapping `9/9`, and `266/266` unique mutants; the registry remains exactly 58 rows at the accepted digest.
- Governance library passed `134/134` serially. Witness all-target offline compilation, strict combined clippy, formatting, shellcheck, harness self-test, diff checks, and final zero Desktop residue passed. A default-parallel governance run hit the accepted-base state-lock drop timing race and is not acceptance evidence.

These were executor claims. The two-hour boundary expired at 20:24:04 ET without an independent zero-finding verdict, so root stopped acceptance without running the pinned authorization-critical gate or creating a production commit/ref. The bounded findings-only review then confirmed P0/P1/P2 = `0/3/0`: suppression authority was public and unbounded in debug builds; the public store-proxy constructor accepted non-exact private-hop deadlines; and all nine deadline rows were generated by helper functions that assigned counters rather than traversing the production queue/runner/proxy/CAS/publisher seams.

Tree `80e073974373cac2f4737ae22bf957ae16e80524` remains an immutable rejected candidate. The r12 attempt from accepted base `5ade791` stopped uncommitted at incomplete 14-path tree `6df92b96ba1840f2d087ddc0e49c425069738ded`: its recovery case was genuine RED (one run, zero pass, one fail, fourteen filtered), its nine deadline rows were invalidated because helper code directly assigned counters, and it had no authenticated dual-relay trace. Planning commit `c3864f1` (r13) was independently rejected P0/P1/P2=`0/1/0`; r14 planning commit `46b2c93` passed but monolithic implementation was stopped before production edits; r15 planning commit `332e83a` was rejected P0/P1/P2=`0/3/1`; r16 planning commit `b31a8b2` passed but had zsh-unsafe object expressions. Revision r17 planning commit `40b52404bcbe97de8700161ce1fcbcfb03329309` corrected those expressions, independently passed P0/P1/P2=`0/0/0`, and was pushed. Its first immutable A1 production commit `ba0929d` was then independently rejected P0/P1/P2=`0/4/0` for lexical mutants, repaired/indistinguishable event evidence, missing non-test release/API probes, and a ninth row that claimed live NATS grants absent from the A1-owned tree. Revision r18 planning commit `da9ce517a6bdd94a963ce6f757b64780cf0617b6` was rejected P0/P1/P2=`0/2/1`; revision r19 planning commit `6badc93e6f2d10ac1757091205e9430eda03e538` closed those direct defects but was rejected P0/P1/P2=`0/1/1` because A3 omitted the new normal grant projection and STATE retained stale timestamps. Revision r20 under the stable-filename `285-04-03A-R11-DESIGN.md` corrects only that projection and those timestamps.

Rejected A1 production commit `ba0929d83743e0b46a4dc6cd452c5db19466e677` / tree `ab0fd9199e34a0fe40775bd9e23054a9107e5e9a` remains immutable, clean, and unpushed diagnostic evidence. Its former root pins are not acceptance authority. Revision r20 subsequently passed and was pushed, but the two replacement candidates recorded below also failed immutable review. Revision r21 therefore requires a new A1 only: exactly eight changed paths from base `5ade791`, digest `3b9e0dcdef3b37beb4dd22cfb576cb0f3d72d45e2c87c54042c548dbc4deacd7`, with the complete subscriber-capture and real-constructor receipt contracts closed and no live NATS grant claim. Root must bind new commit/tree/launcher/manifest/checker identities and obtain another immutable zero-finding review before either A1 ref may be pushed. A2 then owns separate normal/relay live exact 3,000/12,000-ms max-one NATS grant gates, and A3 must rerun both; A3 remains blocked. A monolithic sixteen-path 04-03A freeze is forbidden.

No Task 04-03A1/A2/A3 acceptance, 04-03B work, full Plan 04, combined Phase 285, hosted/protected evidence, external App enforcement, or Phase 286-289 advancement is claimed.

## r20 A1 immutable rejection ledger and r21 stop

Audited r20 planning commit `e298a74848669aa8d712204ff794b8491cbc6ce3` was pushed, but it authorized no production acceptance by itself. Three direct-child A1 commits on accepted base `5ade79141a2f8249ef084d24fcfd5ef12251e48e` remain immutable, clean, unpushed rejection evidence:

- `ba0929d83743e0b46a4dc6cd452c5db19466e677` / tree `ab0fd9199e34a0fe40775bd9e23054a9107e5e9a`: rejected P0/P1/P2=`0/4/0` for lexical rather than executable mutants, overwritten/indistinguishable event evidence, missing non-test API-boundary proof, and an invalid live-grant claim.
- `15be9c9770eda4340b951d4caad1599a765e6ba5` / tree `91913d7115a5d888f7c65439566029f666b8cd02`: rejected `0/2/0` because queue-expiry evidence bypassed the production bounded receiver ingress and row 9 did not bind the five-value constructor oracle.
- `2640f55a7cb0a6da31443410dd934e2776470c7d` / tree `7dc7fd060bc264c58347a19a007fd55f75ce762a`: rejected `0/2/0` because subscriber-side validation/deadline capture/message construction remained outside the tested dequeue helper and row 9 proved direct validator behavior rather than mandatory use by `NatsPublicWitnessStoreProxyClient::new`.

The latter two candidates passed their declared green gates, which is evidence that the gate model—not merely implementation syntax—was incomplete. Neither candidate may be repaired, rebased, or pushed under r20. The two-hour implementation window closed without an accepted checkpoint, so production work stopped. Revision r21 must first receive an independent planning verdict and a pushed planning checkpoint. It replaces the bypassable seams with one complete subscriber validation/capture/message/enqueue function per path and a normal-harness checker join between budget evidence and fresh calls to the real public constructor. The next permitted production candidate remains exactly one new direct child of `5ade791`, in the same implementation worktree, with the same eight-path digest. A2 remains unauthorized until that candidate has immutable P0/P1/P2=`0/0/0` review and both origin refs resolve to it.

Revision r21 planning commit `cb9d10b506cec8c00bd7c4b5c9ccdadd1910b0fe` / tree `6734b5377f80438a66b88667b2a59d33d10984b5` was independently rejected P0/P1/P2=`0/3/0` and remains unpushed. Its helper-level capture mutants did not execute a real subscriber-loop callsite, its A2/A3 commands reran the joined A1 selector without the required normal NATS harness, and STATE still instructed obsolete r20 work. Revision r22 replaces that object in the same single direct-child planning slot. It adds a normal-harness actual-runner target whose private/public receipt digests bind the two queue rows, two compiled inline-callsite bypass mutants, exact normal-harness A1 reruns in both successor tasks and validation rows, and a current executable handoff. Production remains frozen until r22 itself passes and is pushed.

Revision r22 planning commit `6ceca62f537c597b2f9f8f78d1fdc9f19d6f3916` / tree `067ae8406a466a00d9a87915059374826f1447bc` was independently rejected P0/P1/P2=`0/1/0` and remains unpushed. Its external `full_service_path` callsite test could not consume the required crate-private post-enqueue receipt without a forbidden public side channel, so zero backend/publication was not a positive acceptance oracle. Revision r23 moved the actual-runner proof to a second exact library-internal target under normal NATS, factored production/test runner startup through private `start_inner`, and added drop-before-enqueue plus fabricated/post-accept receipt mutants. The external integration target remained only for the real public constructor; the next paragraph records why r23 was still rejected.

Revision r23 planning commit `692cecbea660c506b1c127312e49beb0b29d6206` / tree `d7136e7f43c11889854a0a385b2b58c2ecf5d88c` was independently rejected P0/P1/P2=`0/1/1` and remains unpushed. Its internal live target invoked private `start_inner` directly rather than the shipping public `start`, leaving both production entrypoints free to diverge; it also required production-private symbols to be absent from artifacts rather than merely private and downstream-unnameable. Revision r24 requires the exact live target to invoke both public `start` methods, confines recorder injection to crate-private cfg(test) runner builders, adds two independent compiled public-start delegation-bypass mutants, raises the live-callsite inventory to 7 and the unique full-source inventory to 31, and limits non-test artifact absence to test-only builders, recorders, events, receipt controls, and setters. Production remains frozen until r24 independently passes and is pushed.

Revision r24 planning commit `77e637b3686e6e0311a6db372c2b2f9e74e7e6c9` / tree `a58be6e9e26e158f9ece0a5280471b3c090aa065` passed independent P0/P1/P2=`0/0/0` review and was atomically pushed to `origin/work/v179-phase285-planning` and `origin/checkpoint/v179-phase285-plan04-task03a-plan-r24`. Its A1 window ran from 06:39:46Z and stopped at 08:34Z without a commit. Final staged diagnostic tree `39b515f29ca134d48571765211ac3be43356fb3c` on accepted HEAD `5ade79141a2f8249ef084d24fcfd5ef12251e48e` contains exactly the eight A1 paths, has no unstaged/untracked files, preserves launcher hash `e59ba9f62bf126bccdf8c0d3331b54adae9e74f8fe1ee6e31d43e3dec9ca66b1`, checker hash `b680ae6428c7e38304db4df6e4be6eea89173bfc5ce11d38b4bbe68d0c7d8a23`, and manifest hash `efee551ca10f0f6cafd8246ce99679fcf7c2445c525c36eddf5662cdeb37f736`. Its final focused selector passed 9 rows, 18 ledger mutants, 19 first-FQN mutants, 7 callsite mutants, 5 constructor mutants, 31 unique source digests, and 10 boundary receipts with token `e98a6023c7925e64fdbd77a6b28465c7ad2f43ed437284f0b362e4533b7d3556`.

That evidence is diagnostic only. The inherited full-service path stopped before test execution because its private-service source guard asserted that `connection: StoreRoleConnectionV1` occurred exactly once; r24 legitimately places it once in public `start` and once in private `start_inner`. The stopped attempt therefore has P0/P1/P2=`0/1/0`, no production commit/ref, and no A1 acceptance. Revision r25 proposed an exact cheap `--self-test store-proxy-source` preflight, two scoped signatures, exactly two typed parameters, one ordered delegation, and 44 unique source-guard mutations with separate public-signature, private-signature, and delegation controls before any expensive rerun. Production remained frozen pending its independent review.

Revision r25 planning commit `0a26475c9578ecb34be9cf5d55320d719a4bfd37` / tree `abac945cce96c5c685cb507a248e2db3732be39b` was independently rejected P0/P1/P2=`0/1/0` and remains unpushed. Its A1 preflight contract was coherent, but A2 and A3 each own the same conformance checker and their automated successor gates omitted `--self-test store-proxy-source`, allowing a later slice to regress the guard while preserving its other projections. Revision r26 adds the exact one-positive/44-unique-mutation preflight to both successor final-tree chains after predecessor/candidate identity and hash checks and before any Cargo or NATS command. No production edit is authorized until r26 independently passes and is pushed.

Revision r26 planning commit `1f88de7fcb19d27c8183dce7fc3db476dd42a1c5` / tree `3480b9fb860843a33908e35f8c26fed23a4307db` passed independent P0/P1/P2=`0/0/0` review and was atomically pushed to `origin/work/v179-phase285-planning` and `origin/checkpoint/v179-phase285-plan04-task03a-plan-r26`. One A1 production repair in the existing worktree is authorized until 2026-08-27T10:56:22Z; no A2 edit is authorized.

That repair produced immutable production commit `0acee095d2bc8d454f3f5f95068f2e7930b508a9` / tree `e645cff528fa3baded37bbb76cec6ec17c2d15b9`, a clean eight-path direct child of `5ade791`. Its declared final-tree gates were green, but independent review rejected it P0/P1/P2=`0/1/0`: the exact first FQN constructed `PrivateIngressMessage` and `PublicIngressMessage` directly instead of feeding raw NATS messages through the production admission functions; six required private/public capture-reset, capture-after-queue, and overlong-deadline compiled mutants were absent; and the two purported inline-helper controls dropped requests rather than preserving cardinality with coherent inline ingress. The source guard's 44/44 result does not cover that behavioral omission. Commit `0acee09` remains immutable, unpushed diagnostic evidence. Because accepted r26 already specifies the missing boundary and mutations, no planning redesign is required; one fresh direct-child compliance attempt may reconstruct from `5ade791` in the same worktree before the existing hard stop. A2 remains unauthorized.

The fresh direct-child A1 candidate is commit `9ff689ce71a9a05892a9c0d7eda21210e467183e` / tree `96310ff48db788d1cfe6a735b1e9a78d586d911b`. It preserves the exact eight-path digest `3b9e0dcdef3b37beb4dd22cfb576cb0f3d72d45e2c87c54042c548dbc4deacd7`, unchanged launcher `e59ba9f62bf126bccdf8c0d3331b54adae9e74f8fe1ee6e31d43e3dec9ca66b1`, checker `1b65200c0d1548364723dcd6b86ab976d836a5b7116deeef368f5c863ca827dd`, and manifest `9393f2d7f1bf6f63e6fefdb9b4d2e65950ff04a6ab098e9eb96a5c8876073730`. Independent review confirmed both raw-message admission paths, all six receipt-capture mutants, both coherent inline-bypass mutants, the complete 9/18/19/7/5/31/10 focused evidence, and returned P0/P1/P2=`0/0/0`. The alternate checker hash in the executor's prose was a typo; the immutable object, manifest, and reviewer agree on the hash above. The commit was atomically pushed to `origin/work/v179-phase285-plan04-task03a1` and `origin/checkpoint/v179-phase285-plan04-task03a1-production`, fetched back, and both refs resolve exactly to it. Task 04-03A1 is accepted. A2 alone is now authorized in the same worktree; A3 remains blocked.

The first A2 candidate is commit `fa4ad9c2578491cc4ac180da06dc0ce3cc280c54` / tree `3ca0b7ea3c3363fa7159ef62947500c2e430974d`, a clean direct child of accepted A1 with the exact 13-path slice and 14-path cumulative digests. Its scripts were green, but executor self-audit and independent review both rejected it P0/P1/P2=`0/2/0`. The relay used bespoke `RelayPayloadV1`, raw NATS echo tasks, fixed ledger fields, and checker-synthesized worker/proxy/store counts instead of a signed `ReadHead` through the real runtime client, public/private shipping runners, proxy, and recording store. The grant test skipped relay mode, accepted generic permission errors without exact account/subject/request binding, checked textual minimums rather than exact authority tuples, and lacked coherent executable grant/topology/relay mutations. No planning amendment is required because r26 already demands both missing contracts. Commit `fa4ad9c` remains immutable and unpushed. A fresh A2 attempt is authorized from accepted A1 using two internal RED-first steps; A3 remains blocked.

The second A2 candidate is commit `bf26069c8894e4d32608350d11803ff0af9853bf` / tree `ec278370395cda1b70589b9e7e59e8c2a8d93c67`, also a clean direct child of accepted A1 with exact scope/digests. It replaced the echo path with the real typed client/runners/store and added 15/15 real-target mutations with zero vacuous runs, but independent review rejected it P0/P1/P2=`0/4/0`. Suppressed/private responses were hashed without typed authentication, publication/identity/count evidence remained partly inferred or synthesized, and replay reused one relay instance. The runtime credential retained raw JetStream/KV authority while topology checks were global minima rather than exact account/principal tuples. Max-one timing was measured after the first response rather than from the original request and permission evidence was not account/principal bound. The executable corpus was exposed only through a separate mode outside the accepted focused relay gate and omitted controls for those residual seams. r26 already specifies all repairs, so no planning amendment is authorized. Commit `bf26069` remains immutable and unpushed. A fresh A2 attempt uses four ordered micro-gates above; A3 remains blocked.

The third A2 candidate is commit `958fe44ef8072a8aef9c1f346eceac57446a5132` / tree `fc930f36e80a7ace9f955078ee8574dc0c2b9357`, a clean, unpushed direct child of accepted A1 with exact 13-path slice digest `63891e64631df71736d4a21c852c43cec261b257b2508c10db72d2fc4c04c164` and 14-path cumulative digest `4e6629733654f7b4b6f1233bfaaef96a0fede0368620380c8b97f9d75bb18664`. Its immutable chain reported source 1+44, integrity 14, registry 58, public 4/4+266, full-service 4/4+20, A1 9/18/19/7/5/31/10, normal/relay grant rows 2 with 10 mutations each, relay observations 6/store operations 5/ledger mutations 13, 22/22 real-target mutants with vacuous=0, governance 133, all-target compile, strict clippy, formatting, shell, diff, and zero residue. Those executor results are diagnostic only.

Independent hostile review rejected `958fe44` P0/P1/P2=`0/6/0`. The ledger substitutes relay transcript hashes and literal checker counts for actual A1 worker/proxy observer events. Lost-response suppression consumes a unit token rather than reserving the complete canonical receipt, and replay recreates only the public relay leg. The independent oracle does not first require the positive row to pass its own predicate and does not decode/cross-bind all canonical typed relations. Relay-mode authority validation is not a complete per-account/principal/import/export/permission graph and does not exclude all relay-principal management authority. Grant rejection account/user/reason values are derived from labels rather than bounded server evidence. Finally, ambient mutation variables can bypass or narrow the supposedly mandatory corpus, while synthetic-identity/count cases return direct expected errors rather than coherent mutations through the ordinary oracle.

The A2 production window is stopped before its nominal boundary because six semantic repairs, a full immutable chain, and independent re-audit cannot credibly complete by `2026-08-27T15:05:16Z`. No repair or fourth monolithic A2 candidate is authorized. The next planning object may change only Plan 04, validation, state, freeze, and the stable design file; it must split A2 into independently bankable A2a observation, A2b relay/authority, and A2c immutable-oracle slices, each with an exact ownership list, executable red control, immutable commit, independent review, pushed checkpoint, and two-hour stop. A3 remains blocked.

Revision r27 is the one bounded redesign after that stop. It changes exactly five planning paths and no production file. A2a owns eight paths/digest `1da59d9b30b0cc6c588d3a8f1a1626dcd4430691851423e1b4d876dded09c512` and a ten-path cumulative digest `3e3093857a828297ff1ab86685da7f94aee3941ad3b84109eace0499834dc27d`; it can bank only real A1 worker, proxy, store, publisher, and server-issued connection observations through a library-internal test that alone can inject the crate-private recorder. A2b owns eleven paths/digest `4d9d95a9253af2cede7f42175322169a4deb3fafebc8fb0d58bcb845cd992adf`; its library-internal relay test consumes the same observer seam and can bank only complete-receipt fail-open suppression, fresh public/private relay legs, exact closed authority, and server-bound pre-expiry grants. A2c owns four paths/digest `8b37d06097626b9d3f302ea1053ced7852e5553f095c8ead99964c8bca66b6c5`; it adds no production behavior and can bank only the positive-first independent oracle plus exact 34-name unconditional real-target corpus. The final A2 union remains fourteen paths/digest `4e6629733654f7b4b6f1233bfaaef96a0fede0368620380c8b97f9d75bb18664`.

Every r27 production slice begins from accepted predecessor origin refs, records its exact executable RED, freezes one direct-child commit, receives independent immutable P0/P1/P2=`0/0/0` review, pushes separate work/checkpoint refs, and stops after two hours if not accepted. Rejected objects cannot be amended. A later slice cannot start from local-only evidence. Until r27 itself passes immutable planning review and is pushed, A2a is unauthorized and all production remains frozen.

Frozen r27 planning candidate `242ff4a6d8019b59fcea52554b25810ce7e05902` / tree `3f0acfd85846f9e4a95a2accf1caf5cd6b70e48e` is a five-path direct child of `8c003c8` and remains unpushed. Its independent audit was interrupted before verdict. Reinspection of the immutable object proves A2a already owns exact library-internal FQN `service_checkpoint_observation_tests::worker_observations_are_real_and_reconciled`, A2b already owns exact library-internal FQN `service_checkpoint_relay_tests::complete_receipt_authority_and_grants_are_observed`, and both invoke shipping starts through crate-private cfg(test) builders. The earlier root `0/2/0` statement described a pre-freeze draft and is withdrawn for this immutable object.

Revision r28 preserves all r27 three-slice boundaries and executable semantics. It corrects the one frozen inconsistency: A2b's `read_first` said ten owned paths even though its exact `<files>` list, action, and automated gate all said eleven with digest `4d9d95a9253af2cede7f42175322169a4deb3fafebc8fb0d58bcb845cd992adf`. A2b's cumulative union remains fourteen paths/digest `4e6629733654f7b4b6f1233bfaaef96a0fede0368620380c8b97f9d75bb18664`. Production remains frozen until r28 independently passes and is pushed.

Frozen r28 planning commit `5ad4a4234c5b01d351c0b6f782c41e0d3964c30f` / tree `48e404bc14029980a24976c35c2ebb5235c03726` is a clean five-path direct child of r27 and remains unpushed. Independent hostile review rejected it P0/P1/P2=`0/3/1`: A2a/A2b/A2c/A3 asserted but did not mechanically prove direct single-parent commits; 03B remained wave 5 without accepted A3 origin/parent gates; the exact 34-name corpus omitted unit-token, post-suppression-construction, unbounded-capacity, and suppress-on-reservation-failure mutations at the complete-receipt boundary; and STATE Session Continuity still authorized the obsolete second-candidate attempt.

Revision r29 changes only those four defects in the same five planning paths. Each of A2a, A2b, A2c, A3, and 03B verifies both predecessor origin refs, exactly one commit parent, and exact direct parentage. Task 03B is wave 10, freezes an immutable direct child of accepted A3 before its final gates, and cannot authorize Plan 05A until its own reviewed work/checkpoint refs are pushed. A2c's static non-bypassable inventory is exactly 38 coherent real-target controls, adding the four complete-receipt reservation-boundary mutations and retaining vacuous=0. Session Continuity states that production is frozen and no production hard-stop clock is active. No production edit is authorized until r29 independently passes and is pushed.

Frozen r29 planning commit `1d86c8fa96d04156fc34aafe02e0a7ea82c827f6` / tree `c2bb4aff24896cf0b84b45957896adfd593f8474` is a clean five-path direct child of rejected r28 and remains unpushed. Independent review rejected it P0/P1/P2=`0/1/0` only because the outer Plan 04 gate asserted both pushed 03B refs while its prose also required the same outer review before those refs could be pushed. All five direct-parent gates, wave-10 dependency, 38-name corpus, path digests, internal observer seams, and current Session Continuity passed review.

Revision r30 removes that circularity without changing production scope or semantics. The immutable 03B commit first receives its task-level zero-finding review; that review alone authorizes pushing the two 03B refs. The outer cumulative Plan 04 gate then runs post-push, verifies those refs, and must receive its own P0/P1/P2=`0/0/0` before Plan 04 completion or Plan 05A. A failed outer review leaves the accepted 03B slice banked while advancement remains blocked. Production stays frozen until r30 itself passes and is pushed.

Frozen r30 planning commit `aae0e88513808dce91fd75544e8bbbdb07cd4b86` / tree `a50d5504b83d52bb48aab2963f94205906b3a1f5` is a five-path direct child of rejected r29. Independent hostile review returned P0/P1/P2=`0/0/0`, confidence high. It was atomically pushed to both `origin/work/v179-phase285-planning` and `origin/checkpoint/v179-phase285-plan04-task03a-plan-r30`; both fetched remote-tracking refs resolve exactly to `aae0e88513808dce91fd75544e8bbbdb07cd4b86`.

A2a alone is authorized from `2026-08-27T15:15:51Z` through hard stop `2026-08-27T17:15:51Z`. The existing implementation worktree is clean at rejected diagnostic `958fe44`; the executor must detach/reconstruct from accepted A1 `9ff689ce71a9a05892a9c0d7eda21210e467183e`, whose work and checkpoint origin refs both resolve exactly. The diagnostic cannot be amended, repaired, cherry-picked, or pushed. A2a owns exactly eight paths/digest `1da59d9b30b0cc6c588d3a8f1a1626dcd4430691851423e1b4d876dded09c512`, must record executable RED, freeze one direct-child commit, pass the exact chain and independent immutable review, and push its own two refs before A2b. No new worktree or successor edit is authorized.

Execution against r30 recorded a genuine normal-NATS RED and then produced a green observation spine on temporary tree `2072a3785d92be2b37b1ebe0725d295b3a3b6b32`: source preflights `44/44+8/8`, integrity self-test `14/14`, focused observation `1/1`, real derived counts worker `12`, proxy `1`, store `1`, publisher `2`, server-issued connections `3`, CAS `0`, and `6/6` intended evidence mutations. This remains uncommitted diagnostic evidence. The exact r30 final command is impossible under the accepted A1 harness: its scan binds the token immediately before `--exact`, which is literal `--` in the planned order, while the equivalent valid Cargo order produces one passing test plus seventeen filtered tests and is rejected against the harness's hard-coded four. No production commit, ref, or acceptance exists.

Revision r31 is the only authorized planning correction. It changes exactly the same five planning paths and no production file. A2a adds `tools/with-nats-jetstream.sh` as its ninth path, giving exact slice digest `919bf3addbcdbf8625f055f9c8737f22aa5b9d3dd99130fe8e62fc981b65d4c9` and eleven-path cumulative digest `aa6b8eae0de4d644ebb18c08a36264d09b6adc81cddc50288d3067ed3715030f`; the final A2 union is unchanged because A2b already owned the harness. The new non-ambient prefix `--expect-exact-test <full-fqn> <expected-filtered-out> --` is parsed before Docker/NATS startup, stripped before child execution, cross-checked against exactly one forwarded `--exact`, and transcript-validated against the explicit pinned count. A2a pins seventeen filtered tests; A2b pins eighteen and requires exactly nineteen library tests. Prefix/name/count/delimiter/marker and zero/ignored/failed/filtered-only transcript mutations must fail. Existing unprefixed selector behavior remains unchanged.

Until immutable r31 review returns P0/P1/P2=`0/0/0` and the planning checkpoint is pushed, the executor may preserve and continue only the previously authorized eight-path observation implementation; the harness path and candidate freeze are forbidden. The existing two-hour A2a stop remains `2026-08-27T17:15:51Z`. A failed r31 audit or missed A2a deadline stops this slice without authorizing A2b or a new worktree.

Frozen r31 planning commit `3c7d5ec2701d2bd78d856c2865a202c47a1121d7` / tree `a7189bb58d7d1093e8e0e64dd40f158fce19399d` is a clean five-path direct child of the r30 authorization ledger. Independent review confirmed the exact 9/11/14 path projections, implementable pre-NATS stripped prefix, valid A2a `18 total / 17 filtered` and A2b `19 total / 18 filtered` commands, non-circular composition, 8/8 task-validation bijection, nine syntax-clean commands, and all execution/sequencing blocks. It rejected r31 P0/P1/P2=`0/0/1` solely because the design frontmatter still superseded r29, STATE's timestamp remained r30's authorization time, and STATE Progress still described r26 as current. r31 remains immutable and unpushed.

Revision r32 changes only those stale ledger identities plus its own revision markers in exactly four ledger paths: STATE, this freeze, the stable design, and validation status. Plan 04 itself is byte-identical to r31. The design frontmatter supersedes immutable r31 `3c7d5ec`; STATE records the current timestamp and r30-accepted/r31-rejected/r32-pending sequence. No harness semantic, path digest, command, production scope, hard stop, or successor gate changes. Production remains frozen pending r32 `0/0/0` and push.

Frozen r32 planning commit `28287e0177f2de8e8ac8d2c1987c765b4f76531b` / tree `740164109300ed4b0ff71a546f9f9cd5db131cc7` is a clean four-path direct child of r31 and remains unpushed. Root pre-audit invalidated it before independent review because STATE Session Continuity still said the harness/freeze was pending r31 and the stable design's current scope heading still said r31. No r32 semantic or production finding exists.

Revision r33 changes only those two stale current labels, the four ledger paths' current revision references, timestamp, and the direct supersession pin to r32. Plan 04 remains byte-identical to r31/r32; all harness semantics, exact commands, path digests, production scope, hard stop, and successor gates remain unchanged. Production remains frozen pending r33 `0/0/0` and push.

Frozen r33 planning commit `2cb699cf244d4c527ca39696ccfc28bd739531ea` / tree `f345afa815853c295dd29624a36d7e1cb444f9ae` passed independent P0/P1/P2=`0/0/0`, confidence high. It was atomically pushed to `origin/work/v179-phase285-planning` and `origin/checkpoint/v179-phase285-plan04-task03a-plan-r33`; both fetched refs resolve exactly. A2a alone is authorized through the unchanged hard stop `2026-08-27T17:15:51Z`, including the ninth harness path and one immutable production freeze. A2b and every successor remain blocked.

The first r33 A2a production candidate is immutable commit `16de82aadf04282eabf732090c5a51aaa7f94906` / tree `98cf97fd6288930cb91da9aeaf20026f9da5cc72`, a clean nine-path direct child of accepted A1 with exact slice digest `919bf3addbcdbf8625f055f9c8737f22aa5b9d3dd99130fe8e62fc981b65d4c9` and eleven-path cumulative digest `aa6b8eae0de4d644ebb18c08a36264d09b6adc81cddc50288d3067ed3715030f`. Its declared chain was green, including exact prefix `1/1/0/0/17`, prefix mutations `13+1`, observation `12/1/1/2/3` with CAS `0/0` and six mutations, inherited A1 `9/18/19/7/5/31/10`, integrity `14`, strict compile/clippy/format/shell/diff, and zero runtime residue.

Independent review rejected `16de82a` P0/P1/P2=`0/2/1`. The signed public Head was not independently equated to the authenticated store envelope's selected current head/txid; store-derived values were mislabeled as response fields and no coherent wrong-valid-head/absence control existed. Server-issued client IDs were real, but account/user/runner labels were literal rather than derived from bounded server connection evidence, with no cardinality-preserving substitution control. Three canonical store-observation serializers silently defaulted errors to empty bytes. r33 already requires exact response/store/connection reconciliation and forbids defaults, so no planning revision is needed. `16de82a` remains unpushed/read-only; one fresh direct-child compliance attempt from accepted A1 is authorized in the same worktree through the existing hard stop. A2b remains blocked.

Fresh A2a compliance commit `bbfa4b150b486953e43bc4d22d373b36a0c1a76e` / tree `30190f10b0d36ceaa112874608ddb4ea42e74cc8` is a clean nine-path direct child of accepted A1 with unchanged slice/cumulative digests. Its final chain reported exact prefix `1/1/0/0/17`, observation source `12`, validator `10`, compiled public-head `1`, real counts `12/1/1/2/3` with CAS `0/0`, inherited A1 `9/18/19/7/5/31/10`, integrity `14`, public `4/4+266`, full-service `4/4+20/20`, governance `133`, strict compile/clippy/format/shell/diff, and zero runtime residue. Independent review returned P0/P1/P2=`0/0/0`, confidence high. It was atomically pushed to both A2a work/checkpoint refs and fetched back exactly. A2a is accepted.

A2b alone is authorized in the same worktree from `2026-08-27T16:53:44Z` through `2026-08-27T18:53:44Z`. It must verify both A2a origin refs, start from exact `bbfa4b1`, record the r33 library-internal relay RED, preserve accepted A2a, own exactly eleven paths/digest `4d9d95a9253af2cede7f42175322169a4deb3fafebc8fb0d58bcb845cd992adf`, freeze one direct-child commit, pass immutable `0/0/0`, and push both refs before A2c. No new worktree or successor edit is authorized.

The r33 A2b attempt froze clean immutable commit `ef4c043e0039d77282683fab5b99fbe9f3ed34c2` / tree `02d8fc929c8661d410a31c8b13aea68658b33eb4`, direct parent accepted A2a `bbfa4b150b486953e43bc4d22d373b36a0c1a76e`. Its eleven-path digest is `4d9d95a9253af2cede7f42175322169a4deb3fafebc8fb0d58bcb845cd992adf`; its fourteen-path Task02 cumulative digest is `4e6629733654f7b4b6f1233bfaaef96a0fede0368620380c8b97f9d75bb18664`. Final-tree relay, grant, observation, deadline, compile, clippy, format, shell, diff, and cleanup gates were green. Independent immutable review rejected it P0/P1/P2=`0/3/0`: partial receipts could reserve suppression before completeness validation; account/principal topology tuples were compared globally instead of within owner blocks; and the `<50 ms` field measured first-response-to-second-publish rather than first-publish-to-second-publish. `ef4c043` is not accepted or pushed. Production stopped before the two-hour boundary because repair, the full live chain, and re-review could not credibly complete.

Revision r34 is the one bounded redesign after that stop. It changes planning/ledger files only and splits A2b into three serial independently bankable checkpoints. A2b1 owns exactly four paths/digest `ccd00d773edc3826aecd28bf6d451d0d2a5323ee766224e01b5633cb3815070a` and banks only the closed pre-suppression complete-receipt predicate plus forwarding behavior. A2b2 owns exactly five paths/digest `964b80e1d926ccc78f1e2f487edb84169a01f1cb6a1eadcf3b294c3234ce98ff` and banks only account/principal-scoped topology comparison with cardinality-preserving block-move controls. A2b3 owns the exact eleven-path digest `4d9d95a9253af2cede7f42175322169a4deb3fafebc8fb0d58bcb845cd992adf` and integrates the four-account relay, server-bound grants, fresh identities, and the true origin-relative publish delta with a delayed-first-response control. Each slice starts only from the accepted predecessor's two exact origin refs, records its own executable RED, freezes one clean direct-child commit, receives immutable P0/P1/P2=`0/0/0`, and pushes its own two refs before its successor. A2b1 must reconstruct from accepted A2a in the existing worktree; rejected `ef4c043` is diagnostic-only and cannot be amended or cherry-picked. A2c and every later slice remain blocked. No new worktree is authorized.

Frozen r34 planning commit `87d3d535e150fb2dc50c7de85bdc6017ebe9bdad` / tree `1d825f164eabcb9b6d3035adee6414bf26f6895a` is a clean five-path direct child of the A2a acceptance ledger and remains unpushed. Independent hostile review verified that the three slice contracts close the three production findings, all path digests reproduce, the library count is 19/18 filtered, waves and 10/10 task-validation bijection are correct, and no later work is authorized. It rejected r34 P0/P1/P2=`0/3/0` because the commands omitted full candidate commit/tree/one-parent pins, root-pinned launcher/manifest execution, and required reruns of accepted overlapping predecessor gates.

Revision r35 changes only those mechanical gates and current revision ledgers. A2b1/A2b2/A2b3 semantics, file ownership, path digests, waves, REDs, two-hour stops, and successor blocks are unchanged. Every final chain now requires explicit candidate commit/tree variables; both predecessor origin refs; exactly one parent and exact direct parent; exact candidate tree, scope count/digest, `diff-tree --check`, and clean identity before and after validation. Every chain verifies root-supplied launcher/manifest hashes from the immutable object and runs the integrity self-test before checker selectors. A2b1 reruns accepted A2a observations and A1 deadline; A2b2 reruns all A2b1 controls plus those projections; A2b3 reruns all A2b1/A2b2 controls plus observations, grants, relay-positive, and deadline. Production remains frozen until r35 receives independent `0/0/0` and is pushed.

The first r35 planning candidate `27132827e402f667d50541d75250be98078ce771` / tree `f5e802caec13568665322e0eb746a0eb0c741181` closed all three r34 findings but was rejected P0/P1/P2=`0/1/0` for one stale STATE handoff token naming rejected r34. Compliance commit `9262cf53b668ee3d829230ef40b4c5847eb87b08` / tree `4a34aca15873b207c6077ff7c15a17929caadaa8` changes exactly that one token. Independent review returned P0/P1/P2=`0/0/0`, confidence high; every substantive/mechanical r35 blob remained unchanged. The commit was atomically pushed to `origin/work/v179-phase285-planning` and `origin/checkpoint/v179-phase285-plan04-task03a-plan-r35`, fetched back, and both refs resolve exactly.

A2b1 alone is authorized in the existing implementation worktree from `2026-08-27T18:37:06Z` through hard stop `2026-08-27T20:37:06Z`. It reconstructs from accepted A2a `bbfa4b150b486953e43bc4d22d373b36a0c1a76e`, owns exactly four paths/digest `ccd00d773edc3826aecd28bf6d451d0d2a5323ee766224e01b5633cb3815070a`, records its exact RED, freezes one immutable direct child, passes the complete r35 final-tree chain and independent P0/P1/P2=`0/0/0`, and pushes its work/checkpoint refs before A2b2. Rejected `ef4c043` remains diagnostic-only; no new worktree or successor edit is authorized.

The first r35 A2b1 candidate froze as commit `5ad200f558d691ea96a5fdab9903536f2efa04ca` / tree `1879e3506a6e674b0d885d4d19554886ff867bef`, a clean four-path direct child of accepted A2a with the required digest. Its reported focused, mutation, inherited, compile, format, and diff gates were green, but independent immutable review rejected it P0/P1/P2=`0/3/0`. The predicate suppressed after any successful send without behaviorally enforcing capacity one; its connection, worker, store, and publisher receipt fields could be coherently fabricated and re-digested rather than independently reconciled to real A2a observations; and its 37 mutants plus external test scanned source strings instead of compiling and executing the predicate. The object remains immutable and unpushed. Those findings were direct implementation violations of accepted r35, so only one fresh r35 compliance attempt was authorized at that point.

One fresh A2b1 compliance attempt is authorized in the same worktree from `2026-08-27T19:26:16Z` through hard stop `2026-08-27T21:26:16Z`. It must reconstruct from accepted A2a, rerun the exact RED, own the same four paths/digest, enforce exact capacity one before reservation with executed capacity zero/one/two and partially occupied capacity-two controls, derive the receipt exclusively from real accepted A2a observations, independently reconcile the three real server-issued connection identities plus exact worker/private Entry/store/signed-response/publisher-reply relations, and replace lexical acceptance with confined coherent mutants that compile and execute the exact Rust targets once. Zero-test, compilation, generic, or lexical-only failure cannot count. It must freeze one new direct child, rerun the complete r35 final-tree chain, receive independent P0/P1/P2=`0/0/0`, and push both refs before A2b2. Rejected `ef4c043` and `5ad200f` may not be amended, cherry-picked, or pushed; no new worktree or successor edit is authorized.

Fresh compliance candidate `8a7df5d30fa3cf02c9aa127e4ac7d08f07e36d6c` / tree `9e8d758e8d036803c13c4235f4bde61bd16ddd97` closed the capacity-one and compiled-mutation findings but was independently rejected P0/P1/P2=`0/2/0`. Its helper created an evidence object, copied that object into the receipt, and primarily validated equality with the same helper-owned object; coherent producer-plus-receipt changes could survive because public/store Head, exact worker operation/CAS fields, store input/result digests, publisher reply subject, and redundant store fields were not all independently reconciled. It also spawned the NATS shell harness from Rust through predictable ambient-environment temp files with unbounded reads and failure-leaky cleanup. The commit remains immutable and unpushed. Production stopped; no third r35 attempt or successor work is authorized.

Revision r36 is a bounded A2b1-only redesign. It consumes the already accepted fresh A2a `ObservationLedgerV1` and its independent validator, requires explicit producer-plus-receipt coherent mutations, and removes the Rust-owned NATS child/environment-file seam completely. The accepted external harness owns every NATS-dependent exact test, mutation, and cleanup. A2b1 production remains frozen until one r36 planning commit receives independent P0/P1/P2=`0/0/0`, is pushed/fetched exactly, and root records a new two-hour authorization window. Rejected `ef4c043`, `5ad200f`, and `8a7df5d` remain read-only evidence.

Immutable r36 planning candidate `8615228962cafbd8d5f2efd81d45ee198875b8be` / tree `598b9f1d40786d39fcc51b4020569593455edf18` closed both `8a7df5d` production findings, and its five-path scope/digest plus structural checks were clean. Independent review rejected it P0/P1/P2=`0/2/0` only for executable command defects: the A2b1 external exact target omitted `--expect-exact-test`, and A2b2/A2b3 retained direct inherited A2b1 invocations that contradicted the harness-only lifecycle. Revision r37 changes only those command rows plus revision/current ledgers. It pins filtered count 1 for A2b1's two-test integration inventory, count 2 after A2b2 adds its topology test, keeps the library target at 18 filtered, and passes the applicable launcher/manifest/tree pins into every inherited mutation selector. Production remains frozen until immutable r37 receives independent zero-finding review and is pushed/fetched exactly.

R37 planning commit `08646c68e69b5438afdb56b03528b381fa19687a` closed both r36 command findings and was rejected only P2=`1` for one stale `r36` acceptance token. Compliance commit `6ec4a17d6a2735613741e6d069265ef9755323c6` / tree `13fbf28834826d7c4931b35be9bc6438b61e842b` changes exactly that token to `r37`; independent review returned P0/P1/P2=`0/0/0`, confidence high. It was atomically pushed/fetched at both `origin/work/v179-phase285-planning` and `origin/checkpoint/v179-phase285-plan04-task03a-plan-r37`. One fresh A2b1 attempt is authorized in the existing implementation worktree from `2026-08-27T20:51:00Z` through hard stop `2026-08-27T22:51:00Z`; all successors and new worktrees remain forbidden.

The fresh r37 attempt recorded the required real NATS RED and then stopped without freeze, commit, or push when implementation proved the accepted external command unexecutable. Cargo compiles the library dependency without `cfg(test)` for `--test service_checkpoint`; therefore the external integration FQN cannot name the private validator/disposition, and the standalone command supplied no canonical ledger or receipt artifact. The partial tree remains uncommitted diagnostic evidence. Revision r38 is one bounded command-contract correction: the accepted shell checker owns a fresh confined directory and two explicit bounded artifact paths, runs the exact internal producer/predicate, then runs the external FQN solely as an independent decoder of those same read-only bytes, executes coherent two-stage mutants, and removes the directory on every exit. Direct standalone external invocation, a public/debug/test-support API, Rust child/NATS lifecycle, and Rust-selected temp/readiness/cleanup remain forbidden. Production is frozen until immutable r38 planning receives independent P0/P1/P2=`0/0/0`, is committed and pushed/fetched exactly, and root records a fresh two-hour window. A2b2 and all successors remain blocked; no new worktree is authorized.

Immutable r38 candidate `83b616363cdef2719658f5fb344d8264228ae7d1` / tree `21b5c33f5fb7f82467ef8fccaa08a00237abc019` was rejected P0/P1/P2=`0/1/0` only because design frontmatter prematurely said accepted and superseded r36 rather than accepted r37. Compliance commit `1809c903de9a6f09b56ca4fba2e8671827795076` / tree `bf629301619e4ce9aca715d0c09f96f00f62c232` changes exactly those two metadata values; independent review returned P0/P1/P2=`0/0/0`, confidence high. The reviewed object was atomically pushed/fetched at `origin/work/v179-phase285-planning` and `origin/checkpoint/v179-phase285-plan04-task03a-plan-r38`. One fresh A2b1 attempt is authorized in the existing implementation worktree from `2026-08-27T21:24:00Z` through hard stop `2026-08-27T23:24:00Z`; all successors and new worktrees remain forbidden.

The r38 attempt froze immutable production candidate `096e57eb3e8b59d711c0446905d58868f32805b0` / tree `7ad4ccb2ed1d47dba4a2e81a783f4fbac74793e7`, a clean four-path direct child of accepted A2a with exact scope digest `ccd00d773edc3826aecd28bf6d451d0d2a5323ee766224e01b5633cb3815070a`. Its local selector, A2a, A1, compile, strict clippy, format, shell, and identity gates were green. Independent immutable review rejected it P0/P1/P2=`0/3/0`: the private validator did not compare ledger tree/token/case to the current invocation before reserving suppression authority; proxy and publisher arrays were only count-and-digest checked rather than reconciled to the final private exchange and the two worker publish events; and signal cleanup controls exercised a separate helper trap, so deleting the real focused-flow trap survived. The candidate remains immutable, clean, unpushed, and read-only. Its interrupted post-freeze A1 replay is non-acceptance evidence.

Revision r39 is the bounded A2b1-only redesign after that rejection. It retains the exact four paths/digest and checker-owned two-stage artifact architecture. It adds pre-reservation current tree/token/case binding and a capacity-one cross-invocation Forward/empty-receiver control; exact one-row proxy equality to the final private exchange; exact two-row publisher reconciliation to worker ordinals 8/private and 11/public with `published=true`; coherent refreshed-digest private and external controls; exactly ten compiled intended-relation mutants; and exactly fifteen lifecycle controls whose four signal cases plus two trap mutants execute the real focused path. A separate cleanup-child trap is forbidden. Production is frozen with no active hard-stop clock until one immutable r39 planning commit receives independent P0/P1/P2=`0/0/0`, is pushed/fetched exactly, and root records a fresh two-hour A2b1 authorization window. A2b2 and all successors remain blocked; no new worktree is authorized.

Immutable r39 planning commit `056e3ce9d880904c951d5a9adcff0f1b44cc31ad` / tree `7d3a188c93ebb42e027b78ea3aba273caaaf4af8` changed exactly five planning/ledger paths with digest `65cd1bd16b725227d25f6445521c4389dcc7314a76f4decfcc9499793a2e78c1`. Independent review closed current-invocation binding, both proxy/publisher validators, the coherent private controls, and the internally consistent ten-mutant split, but rejected r39 P0/P1/P2=`0/1/0`: the real-flow signal requirement had no closed positional readiness channel binding the actual child PID and scratch/artifact inode identities, no signal-specific bounded termination, and no controls for readiness reordering/fabrication, ignored signals, or normal-exit substitution. The planning object remains immutable and unpushed. Production remains frozen.

Revision r40 changes only that signal-control contract and current ledgers. Exact positional mode `--self-test complete-receipt-real-signal <EXIT|HUP|INT|TERM> <absolute-parent-confined-control-root>` must call the same `run_complete_receipt_focus` function. A blocking checkpoint after the actual trap arm and artifact creation emits one create-new `0600` canonical readiness record binding tree, token, requested case, child PID, and scratch/artifact paths plus device/inode. The parent validates the live identities, sends the exact signal, requires cleanup-and-re-raise with signal-specific termination within two seconds, and proves the bound scratch inode absent. The exact lifecycle inventory becomes nineteen: nine artifact attacks, four real-flow cases, and six coordination/trap mutations covering checkpoint-before-trap, fabricated readiness, ignored signal, normal-exit substitution, trap removal, and trap substitution. Production has no active hard-stop clock until one immutable r40 planning commit receives independent P0/P1/P2=`0/0/0`, is pushed/fetched exactly, and root records a fresh two-hour A2b1 authorization window. All successors remain blocked; no new worktree is authorized.

Immutable r40 planning commit `754880012eb239c1e8747645900516e1c71cf660` / tree `7b39bfa27753e6f89b55cd8fd88c7da27bd82a6a` passed independent P0/P1/P2=`0/0/0`, confidence high. The exact reviewed object was atomically pushed/fetched at both `origin/work/v179-phase285-planning` and `origin/checkpoint/v179-phase285-plan04-task03a-plan-r40`. One fresh A2b1 attempt is authorized in the existing implementation worktree from `2026-08-27T22:28:00Z` through hard stop `2026-08-28T00:28:00Z`. It must reconstruct as one clean direct child of accepted A2a; rejected `096e57e` is read-only diagnostic evidence and cannot be amended or cherry-picked. A2b2 and all successors remain blocked; no new worktree is authorized.

The r40 attempt froze immutable production candidate `fc735b48e94ccb9dab2d41d01dad2668a7ad84df` / tree `000c6a1ae37aaabe0f2f204e698413cf7ae61ae1`, a clean four-path direct child of accepted A2a with exact scope digest `ccd00d773edc3826aecd28bf6d451d0d2a5323ee766224e01b5633cb3815070a`. Its declared local chain passed: exact internal `1/1/0/0/18`, external `1/1/0/0/1`, lifecycle `19/19`, compiled controls `10/10` with vacuous `0`, inherited A2a/A1 projections, all-target no-run, strict clippy, formatting, shell, diff, identity, and clean-tree checks. Independent immutable review rejected it P0/P1/P2=`0/2/0`, confidence high. EXIT used readiness followed by a fixed 250 ms delay rather than an identity-bound parent release, so cleanup could occur before the parent finished validating the checkpoint. The parent also rechecked only control-root device/inode, not directory type/mode, and did not pin promised scratch/artifact modes after readiness. `fc735b4` remains immutable, clean, unpushed, and read-only. R40 production authorization is consumed; A2b2 remains blocked.

Revision r41 is the bounded A2b1-only planning correction. It preserves accepted A2a, the exact four production paths/digest, the two-stage artifact proof, and all ten compiled controls. It replaces time-based EXIT progress with a create-new, fsync/reopen, identity-bound parent release record and requires post-readiness canonical root/scratch/artifact directory type, mode `0700`, device, and inode revalidation. The lifecycle inventory becomes exactly 25: nine artifact attacks, four real-flow signal cases, six inherited coordination/trap controls, and six release/metadata controls for omission, stale/cross-case release, early EXIT, fixed-sleep substitution, root-mode widening, and artifact-mode widening. Production is frozen with no active hard-stop clock until one immutable r41 planning commit receives independent P0/P1/P2=`0/0/0`, is pushed/fetched exactly, and root records a fresh two-hour A2b1 authorization window. No new worktree or successor work is authorized.

R41 compliance planning commit `d2d9fb573449cea248ccdb18f9df33d8d441ec91` / tree `7b079a6b5d0dfe18568eace5d75000259f1b7588` passed independent P0/P1/P2=`0/0/0`, confidence high, and was pushed/fetched exactly at both r41 planning refs. It authorized one fresh A2b1 attempt from accepted A2a in the existing worktree.

Fresh A2b1 production commit `a4b01038e5e33e5b3901dcaca1c82f7ca1db65c1` / tree `0da809e552441608f41f652ee21602fac3c3d35c` is a clean four-path direct child of accepted A2a with exact digest `ccd00d773edc3826aecd28bf6d451d0d2a5323ee766224e01b5633cb3815070a`. Its immutable chain passed exact internal `1/1/0/0/18`, external `1/1/0/0/1`, artifact controls `9/9`, real signals `4/4`, inherited lifecycle controls `6/6`, release/metadata controls `6/6`, lifecycle `25/25`, compiled controls `10/10` unique with vacuous `0`, A2a observations, A1 deadline projections, all-target compile-only, strict clippy, formatting, shellcheck, identity, clean-tree, and zero-residue gates. Independent review returned P0/P1/P2=`0/0/0`, confidence high. The exact commit was atomically pushed/fetched at `origin/work/v179-phase285-plan04-task03a2b1` and `origin/checkpoint/v179-phase285-plan04-task03a2b1-production`. A2b1 is accepted.

A2b2 alone is authorized in the same implementation worktree from `2026-08-28T00:38:01Z` through hard stop `2026-08-28T02:38:01Z`. It starts from exact accepted A2b1, owns exactly five paths/digest `964b80e1d926ccc78f1e2f487edb84169a01f1cb6a1eadcf3b294c3234ce98ff`, records the cardinality-preserving owner-block RED, freezes one direct-child commit, reruns the full A2b2 chain, receives independent P0/P1/P2=`0/0/0`, and pushes both A2b2 refs before A2b3. No new worktree or successor edit is authorized.

The first A2b2 attempt froze immutable production candidate `a7c4b138aba99764ec7cd352290050358adcbb21` / tree `816d0098b46309147499ec8d5e030a86adb47436`, a clean five-path direct child of accepted A2b1 with exact scope digest `964b80e1d926ccc78f1e2f487edb84169a01f1cb6a1eadcf3b294c3234ce98ff`. Its complete immutable chain exited zero: topology `15/15` unique with vacuous `0`, inherited A2b1 internal/external plus compiled `10/10` and lifecycle `25/25`, observation and deadline projections, strict clippy, formatting, shell, identity, and residue gates. Independent review rejected it P0/P1/P2=`0/3/0`, confidence high. The canonical init principal retained forbidden `$JS.API.>` and `$KV.>` wildcards; the Rust projection was a literal self-equality assertion observed only through shell regex; and response-grant, import source-account, and export allowed-account comparisons lacked isolated mutations and executable comparator-deletion controls. The candidate remains immutable, clean, unpushed, and read-only. Its green counters are diagnostic, not acceptance evidence. The prior A2b2 authorization is consumed.

Revision r42 is the bounded A2b2-only redesign. It preserves accepted A2b1 and the same exact five production paths/digest. It closes init authority to the six literal create/update/info/raw-get/manifest/admitted-key subjects; requires an executed Rust projection parsed from the actual generated config and credential-role bytes; independently derives the shell projection; and compares both through canonical tree/token/case/config-digest-bound artifacts in one checker-owned NATS run. Its exact topology inventory is 25 non-vacuous controls: 18 config mutations, three executable comparator deletions, and four cross-language projection/provenance mutations. Production is frozen with no active implementation hard-stop clock until one immutable r42 planning commit receives independent P0/P1/P2=`0/0/0`, is pushed/fetched exactly, and root records a fresh two-hour A2b2 authorization window. Rejected `a7c4b13` cannot be amended, cherry-picked, or pushed. A2b3 and all successors remain blocked; no new worktree is authorized.

Immutable r42 planning commit `19c0172aebc7c667249543fc9c72f1db70765630` / tree `2ac085f93814a048ddc1dd315ca1e0c8ddd0733d` changed exactly five planning paths with digest `65cd1bd16b725227d25f6445521c4389dcc7314a76f4decfcc9499793a2e78c1`. Independent review verified the exact init authority, `18+3+4=25` arithmetic, current target counts, task-validation bijection, scope/digest, and sequencing, but rejected r42 P0/P1/P2=`0/1/1`. A Rust parser disconnected to the correct canonical literal remained indistinguishable on the sole canonical input, and config/credential/projection artifacts had no finite pre-read size bounds. The planning object remains immutable and unpushed. Production remains frozen.

Revision r43 changes only those two planning defects. The checker creates a fresh unpredictable projection-only probe with canonical grammar/cardinality and independent 128-bit-suffixed account/principal identifiers. Both parsers must derive identical canonical and probe projections before the policy oracle separately accepts canonical and rejects the probe as noncanonical; `rust_literal_disconnect` hard-codes the correct canonical literal and must fail only on the fresh probe. Exact pre-read maxima are 262,144 bytes per canonical/probe config, 4,096 per credential-role file, and 16,384 per projection artifact, with length/digest/device/inode/mode stable across every handoff. The five production paths/digest, 25-control count, A2b1 preservation, and successor blocks are unchanged. Production has no active clock until immutable r43 receives independent P0/P1/P2=`0/0/0`, is pushed/fetched exactly, and a fresh two-hour A2b2 window is recorded. No new worktree is authorized.

Immutable r43 planning commit `dee50c127fe4dfba6401d392483eb767b725b767` / tree `941e51b975a107bf3d9a5bfef916597b91b706d9` is a clean five-path direct child of rejected r42. Independent hostile review returned P0/P1/P2=`0/0/0`, confidence high, and verified the unpredictable probe, finite pre-read bounds, exact init authority, 25-control arithmetic, target counts, task-validation bijection, scope/digest, and sequencing. The exact object was pushed/fetched at `origin/work/v179-phase285-planning` and `origin/checkpoint/v179-phase285-plan04-task03a-plan-r43`.

One fresh A2b2 attempt is authorized in the existing implementation worktree from `2026-08-28T01:41:13Z` through hard stop `2026-08-28T03:41:13Z`. It must detach/reconstruct from accepted A2b1 `a4b01038e5e33e5b3901dcaca1c82f7ca1db65c1`, own exactly five paths/digest `964b80e1d926ccc78f1e2f487edb84169a01f1cb6a1eadcf3b294c3234ce98ff`, record both predecessor REDs, freeze one immutable direct child, pass the complete r43 chain and independent P0/P1/P2=`0/0/0`, and push both A2b2 refs before A2b3. Rejected `a7c4b13` cannot be amended, cherry-picked, continued, or pushed. No new worktree or successor edit is authorized.

The r43 implementation attempt stopped unfrozen after root pre-freeze review found three untested authority-closure attacks: an extra authenticated store principal with raw authority, duplicate/malformed store imports, and duplicate/malformed runtime exports. It also found missing exact-`0600` and distinct-inode checks across the four projection artifacts. The exact r43 18-name config registry could not absorb three new controls without becoming false evidence, so the production clock is cancelled and the candidate remains unstaged, uncommitted, unpushed, and unaccepted. Revision r44 preserves every r43 control and adds exactly those three config controls, changing topology arithmetic only from `18+3+4=25` to `21+3+4=28`; the useful fail-closed parser, mode, and identity repairs remain frozen in place but have no valid evidence until r44 is independently accepted. No production selector, freeze, review, push, or successor work is authorized before an immutable r44 planning object passes P0/P1/P2=`0/0/0` and both planning refs resolve exactly.

Immutable r44 planning commit `09466109404e8af541b871370f8462d6b5e38c01` / tree `c9e4d69e46026acf5b7810f7d50e118459441522` changed the exact five planning paths/digest but was independently rejected P0/P1/P2=`0/2/0`, confidence high. Its 28 controls did not isolate singular malformed import/export rows, and exact projection mode, distinct inode, and post-snapshot identity predicates remained positive-only. Revision r45 preserves all r44 controls and adds exactly two malformed-row config controls plus three real artifact-handoff controls, yielding `23+3+7=33`. R44 remains immutable and unpushed. Production remains frozen with no active clock until immutable r45 receives P0/P1/P2=`0/0/0` and is pushed/fetched exactly at both planning refs.

Immutable r45 planning commit `0beddbfdc4635445d5b6143429718daa659b2541` / tree `7c138e21fac8b53056c76a5b6a9772d4b9d91195` is a clean five-path direct child of rejected r44 with exact planning-path digest `65cd1bd16b725227d25f6445521c4389dcc7314a76f4decfcc9499793a2e78c1`. Independent hostile review returned P0/P1/P2=`0/0/0`, confidence high, and verified both isolated malformed-row controls, all three real artifact controls, exact `23+3+7=33` arithmetic, precedence, scope, and sequencing. The exact object was pushed/fetched at `origin/checkpoint/v179-phase285-plan04-task03a-plan-r45` and is contained by `origin/work/v179-phase285-planning`. One A2b2 continuation in the existing worktree is authorized from `2026-08-28T03:03:00Z` through hard stop `2026-08-28T05:03:00Z`. No new worktree or successor edit is authorized.

The r45 continuation produced unfrozen temporary-index tree `718f766f91aca30836826ee885f5f087bf2f3534` on accepted A2b1 with the exact five production paths/digest. Final-byte topology evidence passed all `33/33` controls with vacuous `0`; the NATS harness and integrity self-tests plus strict build/lint/format/shell/scope/residue gates were green. Acceptance stopped before freeze when `--focused-service-checkpoint-observations` timed out in the compiled `public_head_absent` mutation. Its real exact observation test had passed `1/1/0/0/18`, but the checker combined a fresh-target cold compile and execution in one 120-second subprocess and discarded timeout output, so it could not prove whether compilation or execution consumed the bound. Timeout is uncredited; deadline was not started; the candidate remains unstaged, uncommitted, unpushed, and unaccepted.

Revision r46 changes only that observation-mutant execution boundary. It requires a fresh exact-tree/fresh-target compile-only Cargo JSON receipt within 300 seconds, strict selection and snapshot of exactly one confined library test executable, then one direct exact-FQN execution within 60 seconds requiring the intended late `observation public ReadHead is absent` failure and post-run executable identity stability. Each phase has an exact 16,777,216-byte combined-output cap; overflow is vacuous, and failure reports the phase plus the final at-most-65,536 captured bytes before cleanup. Every timeout, compile/identity/execution ambiguity, zero run, or earlier failure remains vacuous and fatal. Production has no active clock until immutable r46 receives independent P0/P1/P2=`0/0/0`, is pushed/fetched exactly, and root records one fresh two-hour continuation in the same worktree. No new worktree or successor work is authorized.

Immutable r46 planning commit `8e1570390ccfe3c3b65a95b87b7b72377ff874ac` / tree `3f11641cf2f08561c5ecf97946a834f0d5d07bfa` changed exactly the five planning paths with digest `65cd1bd16b725227d25f6445521c4389dcc7314a76f4decfcc9499793a2e78c1`. Independent hostile review returned P0/P1/P2=`0/0/0`, confidence high, and verified the split compile/execution receipt, exact phase/output bounds, identity confinement, r45 `23+3+7=33` preservation, complete observation/deadline order, scope, and sequencing. The exact object was pushed/fetched at `origin/checkpoint/v179-phase285-plan04-task03a-plan-r46` and is contained by `origin/work/v179-phase285-planning`. One A2b2 continuation in the existing worktree is authorized from `2026-08-28T03:47:19Z` through hard stop `2026-08-28T05:47:19Z`. No new worktree or successor edit is authorized.
