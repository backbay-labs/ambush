---
phase: 285-assurance-foundation-closure
plan: "04"
type: execute
wave: 5
depends_on:
  - "285-03B"
files_modified:
  - crates/swarm-governance-witness/Cargo.toml
  - crates/swarm-governance-witness/src/lib.rs
  - crates/swarm-governance-witness/src/raw_config.rs
  - crates/swarm-governance-witness/src/public_dispatcher.rs
  - crates/swarm-governance-witness/src/store_proxy_service.rs
  - crates/swarm-governance-witness/src/runtime_client.rs
  - crates/swarm-governance-witness/src/service_config.rs
  - crates/swarm-governance-witness/src/bin/swarm-governance-witness.rs
  - crates/swarm-governance-witness/src/bin/swarm-governance-witness-store.rs
  - crates/swarm-governance-witness/src/bin/swarm-governance-witness-init.rs
  - crates/swarm-governance-witness/tests/full_service_path.rs
  - crates/swarm-governance-witness/tests/service_checkpoint.rs
  - crates/swarm-governance/src/persistence_protocol.rs
  - crates/swarm-governance/src/persistence_protocol/tests.rs
  - crates/swarm-governance/src/witness_candidate_verifier.rs
  - crates/swarm-governance/src/witness_service.rs
  - crates/swarm-governance/tests/phase285_witness_conformance.rs
  - tools/check-phase285-witness-conformance.sh
  - tools/check-witness-dependency-closure.sh
  - tools/fixtures/phase285-witness-integrity.json
  - tools/with-nats-jetstream.sh
  - docker-compose.yml
autonomous: true
requirements:
  - ASSURE-04
  - ASSURE-06
must_haves:
  truths:
    - "A runtime request reaches exactly one typed public-witness operation, and a success response is signed only after the corresponding durable fence, session, read, or CAS transition is confirmed."
    - "The runtime, public witness, witness-store proxy, and one-shot init identities have disjoint credentials, subjects, mounts, and capabilities; no public process can access raw KV or JetStream management subjects."
    - "The full request/reply path survives retries, contention, process restart, lost responses, and session rotation without local, unsigned, or optional-witness fallback."
    - "End-to-end controls return signed presence for current, predecessor, and live-prepared reads, request-bound signed absence for aborted and evicted transaction IDs, and an unsigned typed refusal for stale-replica reads; they exercise every configured limit at max and max-plus-one and prove bounded retention."
  artifacts:
    - path: "crates/swarm-governance-witness/src/public_dispatcher.rs"
      provides: "Bounded nine-operation dispatcher with exact request/body matching and durable-response sequencing."
      contains: "WitnessServiceOperationV1"
    - path: "crates/swarm-governance/src/witness_candidate_verifier.rs"
      provides: "Single authoritative typed Prepare verifier that emits StaleIntent only when intent is the sole defect and reports admission, transition, signature, and bound failures distinctly."
      contains: "WitnessPrepareVerification"
    - path: "crates/swarm-governance-witness/src/store_proxy_service.rs"
      provides: "Private typed proxy service that alone opens NatsWitnessStore and accepts only three private subjects."
      contains: "witness.governance.witness.store.v1"
    - path: "crates/swarm-governance-witness/src/runtime_client.rs"
      provides: "Runtime client implementing GovernanceDurabilityWitness with nested signature/failure verification and fixed retry policy."
      contains: "GovernanceDurabilityWitness"
    - path: "crates/swarm-governance-witness/tests/full_service_path.rs"
      provides: "Credential/account/subject/replay/timeout integration coverage for runtime to witness to store to JetStream."
      contains: "full_service_path"
    - path: "crates/swarm-governance-witness/tests/service_checkpoint.rs"
      provides: "Exact transaction-read, max/max-plus-one, retention-window, restart, and session-rotation controls."
      contains: "service_checkpoint_enforces_every_protocol_limit_at_max_and_max_plus_one"
  key_links:
    - from: "PublicWitnessDispatcher"
      to: "WitnessServiceOperationV1 and WitnessCandidateVerifier"
      via: "exact nine operation/body dispatch plus one typed authoritative Prepare verification result before a store transition"
      pattern: "WitnessServiceOperationV1.*WitnessPrepareVerification"
    - from: "PublicWitnessDispatcher"
      to: "StoreProxyService"
      via: "bounded private request/reply import; public process holds no KV handle"
      pattern: "PublicWitnessDispatcher.*StoreProxyService.*bounded"
    - from: "RuntimeWitnessClient"
      to: "GovernanceDurabilityWitness"
      via: "typed async methods verify witness response/failure attestation before returning"
      pattern: "impl.*GovernanceDurabilityWitness.*RuntimeWitnessClient"
---

<objective>
Implement Slice D: the public nine-operation witness service, private store proxy, one-shot initializer target, and runtime `GovernanceDurabilityWitness` client.

Purpose: The durable store is not a governance authority until the service boundary enforces operation semantics, account isolation, signed response ordering, retry limits, and client-side attestation checks.
Output: Bounded public/private service modules and binaries, a concrete runtime adapter, and full-service/restart conformance through the three-account NATS path.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/285-assurance-foundation-closure/285-CONTEXT.md
@.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md
@.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md
@.planning/phases/285-assurance-foundation-closure/285-VERIFICATION.md
@.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md
@<git show 5be011a07690a63a297d5bba8fbf740bb659c19d:.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md>
@crates/swarm-governance-witness/Cargo.toml
@crates/swarm-governance-witness/src/lib.rs
@crates/swarm-governance-witness/src/nats_config.rs
@crates/swarm-governance-witness/src/jetstream_store.rs
@crates/swarm-governance/src/witness_service.rs
@crates/swarm-governance/src/witness_candidate_verifier.rs
@crates/swarm-governance/src/witness_engine/store.rs
@crates/swarm-governance/src/persistence_protocol.rs
@crates/swarm-governance/tests/phase285_witness_conformance.rs
@tools/check-witness-dependency-closure.sh
@tools/check-phase285-witness-integrity.sh
@tools/fixtures/phase285-witness-integrity.json
@tools/with-nats-jetstream.sh
@docker-compose.yml

Plan 03 owns the package manifest and JetStream implementation; consume their frozen DTO/config types. The exact public subjects are `swarm.governance.witness.v1.{fence,establish,discover,prepare,commit,abort,read_prepared,read_head,fetch_payload}` and the exact private subjects are `swarm.governance.witness.store.v1.{inspect_ready,read_entry,compare_and_swap}`. External GitHub App enforcement remains deferred by the phase context and must not be added to this slice.

The accepted Plan 03B launcher remains byte-identical. Any checker or integrity-manifest change invalidates the prior freeze. After each task, the root integrator must freeze the cumulative candidate tree and publish a separately reviewed planning object containing that tree plus the exact launcher and refreshed manifest SHA-256 values. Only those planning-object-pinned values may be supplied to `tools/check-phase285-witness-integrity.sh`; a digest computed or refreshed by production code is not authorization evidence. The frozen 58 selector/package/target/command/case tuples and registry digest `a3a3ec459600ac3163a9b66aa40aa39e9387c50cc75b1e765d9f0693ddb8983b` are immutable throughout Plan 04.

Tasks are strictly serial. Tasks 04-01, 04-02, and 04-03A1 retain their accepted wave-5 checkpoints; A2a, A2b, A2c, A3, and 04-03B execute only in waves 6, 7, 8, 9, and 10 respectively. Every successor requires both accepted predecessor origin refs, an exact single-parent commit relationship, and independent P0/P1/P2 = 0/0/0 review before its own work/checkpoint refs may be pushed. A task-level planning pin is evidence metadata for the frozen candidate tree, not authorization to advance another phase or to bypass final Plan 04 review.

The exact trait-to-wire mapping is: `issue_session_fence` -> `Fence`/`Fence`/no authorization; `establish_session` -> `Establish`/`Establish`/no authorization; `discover_stream` -> `Discover`/`Discover`/no authorization; `prepare_successor` -> `Prepare`/`Outcome`/session authorization; `commit_prepared` -> `Commit`/`Outcome`/session authorization; `abort_prepared` -> `Abort`/`Outcome`/session authorization; `read_prepared_for_stream` -> `ReadPrepared`/`Read`/session authorization; `read_head` -> `ReadHead`/`Read`/session authorization; and `fetch_payload` -> `FetchPayload`/`Read`/session authorization. No method may infer or substitute another operation, body, target transaction, nonce, or response variant.

The exact planned Rust seam is:

```rust
pub fn witness_service_request_nonce(entropy: [u8; 32]) -> String;

pub struct WitnessServiceRequestDraftV1 { /* private fields */ }

impl WitnessServiceRequestDraftV1 {
    pub fn new(
        request_nonce: String,
        admission_digest: String,
        body: WitnessServiceRequestBodyV1,
    ) -> ProtocolResult<Self>;
    pub fn request_digest(&self) -> &str;
    pub fn finalize_without_authorization(self) -> ProtocolResult<WitnessServiceRequestV1>;
    pub fn finalize_with_session(
        self,
        session: &GovernanceWitnessSession,
    ) -> ProtocolResult<WitnessServiceRequestV1>;
}

impl WitnessServiceResponseV1 {
    pub fn decode_for_client_request(
        bytes: &[u8],
        request: &WitnessServiceRequestV1,
    ) -> ProtocolResult<Self>;
}

#[async_trait]
pub trait GovernanceDurabilityWitness: Send + Sync {
    type Error: std::error::Error + Send + Sync + 'static;
    async fn issue_session_fence(&self, request: WitnessServiceRequestV1) -> Result<WitnessSessionStateFenceV1, Self::Error>;
    async fn establish_session(&self, request: WitnessServiceRequestV1) -> Result<WitnessSessionAttestationV1, Self::Error>;
    async fn discover_stream(&self, request: WitnessServiceRequestV1) -> Result<WitnessDiscoveryAttestationV1, Self::Error>;
    async fn prepare_successor(&self, request: WitnessServiceRequestV1) -> Result<WitnessOutcomeAttestationV1, Self::Error>;
    async fn commit_prepared(&self, request: WitnessServiceRequestV1) -> Result<WitnessOutcomeAttestationV1, Self::Error>;
    async fn abort_prepared(&self, request: WitnessServiceRequestV1) -> Result<WitnessOutcomeAttestationV1, Self::Error>;
    async fn read_prepared_for_stream(&self, request: WitnessServiceRequestV1) -> Result<WitnessReadAttestationV1, Self::Error>;
    async fn read_head(&self, request: WitnessServiceRequestV1) -> Result<WitnessReadAttestationV1, Self::Error>;
    async fn fetch_payload(&self, request: WitnessServiceRequestV1) -> Result<WitnessReadAttestationV1, Self::Error>;
}
```

The caller creates one 32-byte entropy value per new logical request and derives the nonce exactly once. An exact retry reuses the complete finalized request bytes and nonce; a new entropy value means a new request and can never reuse an old authorization. `new` derives `operation` from `body.operation()` and computes the digest with authorization absent. `finalize_without_authorization` accepts only Fence, Establish, or Discover. `finalize_with_session` derives both operation and target txid only from the body (`candidate.txid` for Prepare; `txid` for Commit, Abort, and FetchPayload; `target_txid` for ReadPrepared and ReadHead), calls `GovernanceWitnessSession::authorize`, inserts that proof, and revalidates the complete request. No trait method retains duplicate typed arguments. `decode_for_client_request` verifies a Failure's canonical bytes, signature/key, operation, request digest, stream, admission, optional store-state-digest syntax, and closed failure code against the exact request; it cannot construct a failure or access `VerifiedWitnessStoreStateV1`, which remains mandatory on the service-side signing path.

The checker owns independent canonical inner ledgers in addition to the frozen 4/4/15 top-level tuple registry. Their exact row sets are:

- `dispatcher_mapping` (9): `issue_session_fence`, `establish_session`, `discover_stream`, `prepare_successor`, `commit_prepared`, `abort_prepared`, `read_prepared_for_stream`, `read_head`, `fetch_payload`.
- `capability_matrix` (20): `runtime_private_subject`, `runtime_raw_kv`, `runtime_js_api`, `witness_raw_kv`, `witness_js_api`, `init_serving_subject`, `runtime_credential_swap`, `witness_credential_swap`, `store_credential_swap`, `init_credential_swap`, `account_swap`, `mount_swap`, `reply_subject_injection`, `wildcard_import`, `tls_ca_swap`, `tls_server_name_swap`, `store_queue_exhaustion`, `public_store_bypass`, `private_public_signing`, `hostile_cleanup_preservation`.
- `read_classification` (6): `current_some`, `predecessor_some`, `prepared_some`, `aborted_none`, `evicted_none`, `stale_replica_unsigned_refusal`.
- `restart_lost_response` (18): `restart_fence`, `lost_response_fence`, `restart_establish`, `lost_response_establish`, `restart_discover`, `lost_response_discover`, `restart_prepare`, `lost_response_prepare`, `restart_commit`, `lost_response_commit`, `restart_abort`, `lost_response_abort`, `restart_read_prepared`, `lost_response_read_prepared`, `restart_read_head`, `lost_response_read_head`, `restart_fetch_payload`, `lost_response_fetch_payload`.
- `bounds` (33): `state_bytes`, `checkpoint_bytes`, `binding_bytes`, `public_request_bytes`, `public_response_bytes`, `per_stream_retained_bytes`, `kv_value_bytes`, `store_proxy_request_bytes`, `store_proxy_response_bytes`, `bucket_bytes`, `admitted_stream_count`, `initial_epoch`, `sequence_counter`, `intent_counter`, `session_generation_counter`, `cas_retry_count`, `request_deadline_millis`, `kv_history`, `replica_count`, `subscription_capacity`, `client_command_capacity`, `initial_read_buffer_capacity`, `public_ingress_queue_capacity`, `private_ingress_queue_capacity`, `public_max_in_flight`, `private_max_in_flight`, `nats_max_payload`, `nats_header_overhead_budget`, `jetstream_entry_overhead_budget`, `async_nats_i32_max_value_size`, `async_nats_i64_max_bytes`, `usize_capacity_conversion`, `u16_read_buffer_conversion`; every row has independent `max` and `max_plus_one` results, for exactly 66 boundary cells.
- `rotation_retention` (9): `rotation_one`, `rotation_hundred`, `rotation_retry_after_prepare`, `rotation_retry_after_commit`, `rotation_retry_after_abort`, `retention_beyond_window`, `retention_constant_keys`, `retention_bounded_values`, `evicted_not_live`.

For every inner ledger, the checker owns an independent literal expected projection and requires exact set equality, exact cardinality, unique row IDs, executed > 0, passed = executed, failed = 0, ignored = 0, the frozen candidate tree, and a digest over canonical rows. Same-engine mutations must kill omission, addition, duplication, row-ID substitution, wrong status/count, stale/cross-case ledger, and coherently refreshed-digest attacks.
</context>

<tasks>

<task type="auto">
  <wave>5</wave>
  <name>Task 285-04-01: Implement the bounded nine-operation public dispatcher</name>
  <files>crates/swarm-governance-witness/Cargo.toml, crates/swarm-governance-witness/src/lib.rs, crates/swarm-governance-witness/src/public_dispatcher.rs, crates/swarm-governance-witness/src/service_config.rs, crates/swarm-governance-witness/tests/full_service_path.rs, crates/swarm-governance/src/persistence_protocol.rs, crates/swarm-governance/src/witness_candidate_verifier.rs, crates/swarm-governance/src/witness_service.rs, crates/swarm-governance/tests/phase285_witness_conformance.rs, tools/check-phase285-witness-conformance.sh, tools/check-witness-dependency-closure.sh, tools/fixtures/phase285-witness-integrity.json</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md (service operations, fixed subjects, queue limits, response signing order); .planning/phases/285-assurance-foundation-closure/285-03B-PLAN.md (integrity invalidation and launcher rules); .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance-witness/Cargo.toml; crates/swarm-governance-witness/src/lib.rs; crates/swarm-governance-witness/src/nats_config.rs; crates/swarm-governance/src/witness_service.rs; crates/swarm-governance/src/witness_candidate_verifier.rs; crates/swarm-governance/src/witness_engine/store.rs; crates/swarm-governance/src/persistence_protocol.rs; crates/swarm-governance/tests/phase285_witness_conformance.rs; tools/check-phase285-witness-conformance.sh; tools/check-witness-dependency-closure.sh; tools/check-phase285-witness-integrity.sh; tools/fixtures/phase285-witness-integrity.json</read_first>
  <action>
Red-first: add `public_dispatcher_rejects_unknown_subject_operation_or_body`, `public_dispatcher_returns_overload_without_spawning_or_touching_store`, `public_dispatcher_signs_only_after_confirmed_transition`, and `public_dispatcher_maps_typed_failure_without_string_fallback` before implementation. Implement the exact Rust draft, finalizers, decoder, trait signatures, nonce lifecycle, target derivation, and literal nine-row mapping frozen in context; no duplicate trait arguments or alternative constructor is allowed. Mutation controls reject swapped nonce, body, target, authorization, response variant, failure signature, or optional store-state digest. Implement `PublicWitnessDispatcher` with an explicit match over exactly the nine operations; subscribe only to the nine fixed public subjects; use queue subscriptions, a bounded ingress queue and semaphore; and reject wildcard or client-supplied reply subjects.

Before handler selection, validate only payload/global framing, the outer canonical wire/schema, operation/body pairing, nonce/request/admission digests, subject, and admission routing through the outer-dispatch identity seam. For `Prepare`, the dispatcher must not call complete `WitnessServiceRequestV1` validation, complete body validation, `CandidateV1::validate`, `CandidateV1::canonical_bytes`, or session authorization before the authoritative verifier. After the authenticated current envelope is read, pass the raw decoded Prepare body, outer request identity, authorization material, authenticated current envelope, and selected `WitnessAdmissionEntryV1` to one authoritative verifier owned by `witness_candidate_verifier.rs`. A non-forgeable typed protocol seam in `persistence_protocol.rs` may authenticate a current-head stale candidate without accepting its transition. The verifier alone performs nested candidate canonicalization, request/session signatures, current or canonical-genesis predecessor, epoch, sequence, abort receipt, admission, payload signatures, retained bytes, transition semantics, and selected-entry state/checkpoint/binding limits. It returns only an opaque admitted value or a closed failure enum. It may return `StaleIntent` only after every stronger defect is absent; current-head and genesis old/skipped intent use this same path, and no catch-all `WitnessOutcomeMismatch` may map to stale. Add coherent outer-valid, redigested, reauthorized controls for current-head old/skipped intent and source mutants that reintroduce any forbidden early complete validation.

Apply this exact seam-specific bound taxonomy: (1) a startup envelope violation refuses construction before serving and emits no response; (2) selected request/response maximum violations are unsigned adapter refusals at the wire boundary; (3) an initial authenticated current-entry state/checkpoint/binding/retained violation returns signed `BoundsExceeded` bound to that current digest with zero attempted CAS; (4) an authoritative-verifier or proposed-envelope violation returns signed `BoundsExceeded` bound to the initial current digest with zero attempted CAS; (5) a conflict-winner envelope violation, once the authenticated winner is available, returns signed `BoundsExceeded` bound to the winner digest after exactly one attempted CAS, zero applied CAS, and no retry; and (6) a post-Applied confirmation mismatch or bound violation returns typed `Ambiguous`/`OutcomeUnknown`, never a signed deterministic rejection, and is never upgraded or retried. Framing, transport, timeout, pre-admission overload, and wire-bound request/response size failures remain unsigned adapter errors. Add behavioral max/max-plus-one controls for each applicable selected-entry request, response, state, checkpoint, binding, and retained limit at startup, initial read, proposed mutation, conflict observation, and confirmation read, including a tighter secondary stream; assert exact response kind, failure signature when permitted, store digest, attempted/applied CAS count, retry count, and proxy-call sequence. Add source mutants that collapse any two taxonomy branches or restore a universal signed-bound rule.

Add mixed-defect controls for current/genesis old-or-skipped intent combined individually with every stronger signature, admission, predecessor, transition, retained, and selected-entry bound defect; only pure intent may produce `StaleIntent`. Mutations perform one durable transition, read-confirm the exact revision/value, then sign; the confirmation ambiguity rule above is authoritative when confirmation is not exact. Queue overflow returns typed overload before a store call. Add closed config parsing. Promote `swarm-crypto` from dev-only to the exact approved normal dependency, update declared/resolved dependency-closure expectations and mutants, and prove locked library compilation. Materialize exactly the already-registered four `public-dispatcher` rows plus all 9 `dispatcher_mapping` inner-ledger rows without changing any of the 58 tuples or their digest; make every inner-ledger and top-level mutation frozen in context red. The source guard must kill omission/substitution of each typed verifier branch, signed-bound conversion, selected-entry ceiling, and startup/initial/proposed/conflict/confirmation seam, but lexical source presence cannot substitute for the executable controls. Refresh only the canonical integrity manifest's checker hash; leave the launcher byte-identical. Freeze the cumulative candidate tree, obtain the separate planning-object pin, then run the selector only through that pin.
  </action>
  <acceptance_criteria>Each of the nine subjects has exactly one typed handler; unknown/wildcard/body-mismatch/oversize/unauthorized requests are rejected; queue saturation creates no store task. Prepare performs only outer identity/routing checks before one authoritative typed verifier, never complete request/body/candidate validation or session authorization; the verifier emits `StaleIntent` only when intent is the sole defect for both current-head and genesis successors. Bound outcomes follow the six-part taxonomy exactly: startup refusal without serving; unsigned request/response wire refusal; signed initial/proposed `BoundsExceeded` with zero attempted CAS; signed conflict-winner `BoundsExceeded` after exactly one attempted and zero applied CAS with no retry; and typed ambiguous/unknown confirmation after an applied CAS without deterministic signed rejection, upgrade, or retry. Every mutation success is signed only after exact durable read confirmation; signed failure codes remain closed and matchable; no public module opens a KV handle or calls JetStream management API.</acceptance_criteria>
  <verify>
    <automated>test -n "${PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256:-}" && test -n "${PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256:-}" && bash tools/check-phase285-witness-integrity.sh --integrity-self-test && bash tools/check-phase285-witness-integrity.sh public-dispatcher && bash tools/check-witness-dependency-closure.sh --library-only</automated>
    The two integrity values must equal the separately reviewed planning-object pin for the frozen cumulative Task 04-01 tree; runtime digest discovery is forbidden. The checker must report exactly 4/4 top-level dispatcher cases and 9/9 mapping rows with failed = 0 and ignored = 0, preserve all 58 registry tuples and their digest, and kill every top-level and inner-ledger mutation.
  </verify>
  <done>Task 285-04-01 establishes the public witness service boundary and its durable-response ordering without exposing private storage authority.</done>
</task>

<task type="auto">
  <wave>5</wave>
  <name>Task 285-04-02: Implement the private typed proxy service and account isolation path</name>
  <files>crates/swarm-governance-witness/src/lib.rs, crates/swarm-governance-witness/src/store_proxy_service.rs, crates/swarm-governance-witness/src/service_config.rs, crates/swarm-governance-witness/tests/full_service_path.rs, crates/swarm-governance/tests/phase285_witness_conformance.rs, tools/check-phase285-witness-conformance.sh, tools/fixtures/phase285-witness-integrity.json, tools/with-nats-jetstream.sh, docker-compose.yml</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md (three accounts, imports/exports, private subjects, raw capability boundary, proxy request/response); .planning/phases/285-assurance-foundation-closure/285-03B-PLAN.md (integrity invalidation and launcher rules); .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance-witness/src/lib.rs; crates/swarm-governance-witness/src/jetstream_store.rs; crates/swarm-governance-witness/src/nats_config.rs; crates/swarm-governance-witness/src/public_dispatcher.rs; crates/swarm-governance-witness/src/service_config.rs; crates/swarm-governance/src/witness_engine/store.rs; crates/swarm-governance/tests/phase285_witness_conformance.rs; tools/check-phase285-witness-conformance.sh; tools/check-phase285-witness-integrity.sh; tools/fixtures/phase285-witness-integrity.json; tools/with-nats-jetstream.sh; docker-compose.yml</read_first>
  <action>Red-first: add the four literal `full_service_path_*` tests before implementing `StoreProxyService`. The private service subscribes only to the three fixed private subjects, owns the sole raw-store handle/credential, and accepts only the typed proxy request. Runtime imports only nine public subjects; witness imports only three private subjects; init serves none. Require TLS URL, pinned CA/server name, exact credential role, bounded queues, and deadlines. Validate request digest, witness signature, epoch/anchor, admission, stream mapping, and bounds before JetStream; return a closed typed response. Extend the owned harness/Compose fixture to exactly three accounts and four principals with distinct generated credentials, fixed one-way imports/exports, bounded responses, no wildcard, and no cross-role mount. Execute all 20 `capability_matrix` rows frozen in context, including hostile cleanup preservation. Materialize exactly 4/4 top-level `full-service-path` rows plus 20/20 capability rows cumulatively, preserve the 58 tuples/digest, kill every inner/top-level mutant, refresh only the manifest checker hash, leave the launcher unchanged, freeze the cumulative tree, and obtain the next separate planning-object pin before execution.</action>
  <acceptance_criteria>Runtime/public/private/init identities have disjoint credentials and exact subject permissions; every isolation mutation fails before a JetStream call; the private service alone reaches raw CAS; proxy response body/request digest is checked before public signing; queue exhaustion is a typed refusal; no wildcard/raw management capability passes the fixture.</acceptance_criteria>
  <verify>
    <automated>test -n "${PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256:-}" && test -n "${PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256:-}" && bash tools/check-phase285-witness-integrity.sh --integrity-self-test && bash tools/with-nats-jetstream.sh --self-test && bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" bash tools/check-phase285-witness-integrity.sh full-service-path</automated>
    The checker must report 4/4 top-level cases and 20/20 capability rows with passed = executed, failed = 0, ignored = 0; unavailable NATS, a missing/extra/duplicate/substituted capability cell, partial fixture, or raw-subject access must fail.
  </verify>
  <done>Task 285-04-02 proves the complete typed private path and account isolation. Public service code cannot acquire raw storage authority.</done>
</task>

<task type="auto">
  <wave>5</wave>
  <name>Task 285-04-03A1: Extract observable public/private worker state machines</name>
  <files>crates/swarm-governance-witness/Cargo.toml, crates/swarm-governance-witness/src/lib.rs, crates/swarm-governance-witness/src/public_dispatcher.rs, crates/swarm-governance-witness/src/service_config.rs, crates/swarm-governance-witness/src/store_proxy_service.rs, crates/swarm-governance-witness/tests/full_service_path.rs, tools/check-phase285-witness-conformance.sh, tools/fixtures/phase285-witness-integrity.json</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-04-03A-R11-DESIGN.md; .planning/phases/285-assurance-foundation-closure/285-04-03A-FREEZE.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance-witness/src/lib.rs; crates/swarm-governance-witness/src/public_dispatcher.rs; crates/swarm-governance-witness/src/service_config.rs; crates/swarm-governance-witness/src/store_proxy_service.rs; crates/swarm-governance-witness/tests/full_service_path.rs; tools/check-phase285-witness-conformance.sh; tools/check-phase285-witness-integrity.sh; tools/fixtures/phase285-witness-integrity.json</read_first>
  <action>Reuse the existing Task 04-03A implementation worktree; creating another implementation worktree is forbidden. Before editing, prove HEAD equals accepted Task 04-02 base 5ade79141a2f8249ef084d24fcfd5ef12251e48e, the staged index equals stopped diagnostic tree 39b515f29ca134d48571765211ac3be43356fb3c, exactly the eight A1 paths/digest are staged, and no unstaged/untracked path exists. If any identity differs, reconstruct from the accepted base; never repair or rewrite a rejected commit. Make only the minimal mode-routing change needed to expose existing guards through new local-only `--self-test store-proxy-source`, without changing guard logic or anchors; then record executable RED that fails specifically on stale one-occurrence connection cardinality, not option parsing, missing execution, Docker/NATS, or another fragment. After RED, change only the checker and manifest to make the private-service source guard extract the StoreProxyServiceRunner implementation and prove exactly one typed public start signature, exactly one typed private start_inner signature, exactly two `connection: StoreRoleConnectionV1` parameters, and exactly one ordered `Self::start_inner(connection, service).await` delegation. Replace old `arbitrary_store_client` with separate `arbitrary_public_store_client`, `arbitrary_private_store_client`, and `omit_public_start_delegation` controls; retain every other mutation for exactly 44 unique source-guard mutations. Emit stable reasons public_start_signature, private_start_inner_signature, and public_start_delegation, and require each new mutation to observe its own reason rather than any generic rejection. The positive/mutation preflight must pass after every post-RED checker/manifest edit before any expensive selector runs. Preserve the r24 production contract: each public/private runner factors its complete real NATS subscriber loop through one crate-private start_inner, and its shipping public start delegates to that function. Public constructors and ordinary builds always select a private no-op subscriber-admission observer. Crate-private cfg(test) runner builders may inject a capacity-one post-enqueue recorder into private runner state but return the ordinary runner; the exact internal target must invoke PublicWitnessServiceRunner::start and StoreProxyServiceRunner::start, never start_inner directly. One crate-private admission function per path, called immediately after receipt, alone validates the raw message, captures monotonic receipt time, derives the fixed absolute deadline, constructs typed ingress, performs bounded enqueue, and emits an infallible post-enqueue receipt. Under normal NATS, deadline_state_machine_tests::subscriber_callsite_is_receipt_anchored_and_mutation_sensitive calls both public starts, blocks request-one backend/proxy work, publishes request two, consumes its positive post-enqueue receipt, holds it queued past deadline, releases request one, and proves zero second backend/publication. Those receipt digests bind queue rows 1/5. The first exact library FQN retains eight ordered worker rows plus budget receipt. Exact integration target full_service_path_constructor_deadline_is_exact_and_receipt_bound calls real NatsPublicWitnessStoreProxyClient::new at 0, 2,999, 3,000, 3,001, u64::MAX, accepts only 3,000, performs zero store work, and emits the constructor receipt joined into row 9 with live_nats_grants_proved=false. Kill exactly 18 ledger mutants, 19 compiled first-FQN mutants, seven compiled live internal-callsite mutants (private/public inline helper bypass, drop-before-enqueue, fabricated receipt, post-accept refreshed capture, and independent private/public public-start delegation bypasses), and five compiled real-constructor mutants. Every mutant compiles, executes exactly one declared target, and fails its intended assertion; require 31 unique full-source digests. Require 10 boundary receipts proving production-private start_inner/observer/Noop seams are downstream-unnameable and only cfg(test) builders, recorders, events, receipt controls, setters, and other test seams are absent from normal artifacts. Preserve the unchanged launcher, 58-row registry, manifest-only refresh, and exact eight-path digest 3b9e0dcdef3b37beb4dd22cfb576cb0f3d72d45e2c87c54042c548dbc4deacd7. A1 makes no relay, live-grant, recovery, or abort claim. Freeze exactly one new direct-child commit on base 5ade791 with no post-freeze edit.</action>
  <acceptance_criteria>Pre-repair `--self-test store-proxy-source` is genuine RED after only mode routing and binds failure to stale connection cardinality on diagnostic tree 39b515f; after repair it reports one positive execution and exactly 44/44 unique source-guard mutation failures. Separate public-signature, private-signature, and public-delegation mutations each observe its exact stable reason code and fail its intended scoped invariant, while the seven compiled A1 callsite mutants retain behavioral coverage. Executable RED also proves helper bypass, drop-before-enqueue/fabricated/post-accept receipt, capture reset/extension, both public-start delegation bypasses, and real-constructor delegation defects survive before repair. The normal-harness focused checker runs exactly two internal library targets and one constructor integration target once each; reports 9/9 rows, 18/18 ledger mutants, 19/19 first-FQN mutants, 7/7 internal actual-runner mutants, 5/5 constructor mutants, 31 unique source digests, and 10/10 boundary receipts. The live target enters through both shipping public start methods; public construction always uses Noop; only the cfg(test) builders inject a recorder; both public starts and internal tests converge on the same private start_inner and admission functions. Only a successful bounded enqueue emits the positive receipt; private/public receipt digests bind rows 1/5; an unreachable helper reference, dropped request, fabricated/post-accept receipt, or public-start delegation bypass fails. Row 9 requires fresh same-tree/token/case calls to the real public constructor at all five values and records live_nats_grants_proved=false. CAS/store facts, pairwise traces, observed retries, production-private downstream unnameability, test-only artifact absence, exact scope/digest, launcher/registry stability, strict package/live/clippy/format/shell/diff, and independent immutable P0/P1/P2=0/0/0 review all pass.</acceptance_criteria>
  <verify>
    <automated>set -e; test -n "$PHASE285_PLAN04_TASK03A1_COMMIT"; test -n "$PHASE285_PLAN04_TASK03A1_TREE"; test -n "$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256"; test -n "$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256"; test "$(git rev-parse HEAD)" = "$PHASE285_PLAN04_TASK03A1_COMMIT"; test "$(git rev-parse "$PHASE285_PLAN04_TASK03A1_COMMIT^{tree}")" = "$PHASE285_PLAN04_TASK03A1_TREE"; test -z "$(git status --short)"; git diff-tree --check 5ade79141a2f8249ef084d24fcfd5ef12251e48e "$PHASE285_PLAN04_TASK03A1_COMMIT"; test "$(git diff-tree --no-commit-id --name-only -r 5ade79141a2f8249ef084d24fcfd5ef12251e48e "$PHASE285_PLAN04_TASK03A1_COMMIT" | LC_ALL=C sort | shasum -a 256 | awk '{print $1}')" = 3b9e0dcdef3b37beb4dd22cfb576cb0f3d72d45e2c87c54042c548dbc4deacd7; test "$(git diff-tree --no-commit-id --name-only -r 5ade79141a2f8249ef084d24fcfd5ef12251e48e "$PHASE285_PLAN04_TASK03A1_COMMIT" | wc -l | tr -d ' ')" = 8; test "$(git show "${PHASE285_PLAN04_TASK03A1_COMMIT}:tools/check-phase285-witness-integrity.sh" | shasum -a 256 | awk '{print $1}')" = "$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256"; test "$(git show "${PHASE285_PLAN04_TASK03A1_COMMIT}:tools/fixtures/phase285-witness-integrity.json" | shasum -a 256 | awk '{print $1}')" = "$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256"; bash tools/check-phase285-witness-conformance.sh --self-test store-proxy-source; cargo test -p swarm-governance-witness --lib --locked --offline deadline_state_machine_tests::deadline_state_machine_is_receipt_anchored_and_mutation_sensitive -- --exact --test-threads=1; env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A1_TREE" bash tools/check-phase285-witness-integrity.sh --integrity-self-test; env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A1_TREE" bash tools/check-phase285-witness-integrity.sh public-dispatcher; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A1_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-deadline; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A1_TREE" bash tools/check-phase285-witness-integrity.sh full-service-path; cargo test -p swarm-governance --lib --locked --offline -- --test-threads=1; cargo test -p swarm-governance-witness --all-targets --locked --offline --no-run; cargo clippy -p swarm-governance -p swarm-governance-witness --all-targets --all-features --locked --offline -- -D warnings; cargo fmt --all -- --check; shellcheck tools/check-phase285-witness-conformance.sh tools/with-nats-jetstream.sh; git show --check --oneline "$PHASE285_PLAN04_TASK03A1_COMMIT"; test -z "$(git status --short)"</automated>
    Root must freeze the exact local A1 production commit, commit tree, launcher hash, and manifest hash before review. Independent review audits that immutable commit and its tree-bound evidence. Only after P0/P1/P2=0/0/0 may root push work/v179-phase285-plan04-task03a1 and checkpoint/v179-phase285-plan04-task03a1-production. No A2 edit is authorized before both origin remote-tracking refs resolve to the reviewed commit.
  </verify>
  <done>Task 285-04-03A1 banks only the real observable worker/deadline seam. A2, A3, 03B, and final Plan 04 acceptance remain blocked.</done>
</task>

<task type="auto">
  <wave>6</wave>
  <name>Task 285-04-03A2a: Bank the observed worker/proxy/store spine</name>
  <files>crates/swarm-governance-witness/src/lib.rs, crates/swarm-governance-witness/src/public_dispatcher.rs, crates/swarm-governance-witness/src/runtime_client.rs, crates/swarm-governance-witness/src/service_config.rs, crates/swarm-governance-witness/src/store_proxy_service.rs, crates/swarm-governance-witness/tests/service_checkpoint.rs, tools/check-phase285-witness-conformance.sh, tools/fixtures/phase285-witness-integrity.json, tools/with-nats-jetstream.sh</files>
  <read_first>The accepted immutable Task 04-03A1 production commit and freeze; the rejected `958fe44` immutable object and its `0/6/0` review; .planning/phases/285-assurance-foundation-closure/285-04-03A-R11-DESIGN.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; all nine owned paths; tools/check-phase285-witness-integrity.sh</read_first>
  <action>Reuse the existing implementation worktree and reconstruct only from accepted A1 after both A1 origin refs resolve exactly to `PHASE285_PLAN04_TASK03A1_COMMIT`; never amend or cherry-pick a rejected A2 object. Before implementation, add exact library-internal red test `service_checkpoint_observation_tests::worker_observations_are_real_and_reconciled` in `src/lib.rs` and prove it fails because the relay-derived/literal worker and proxy facts from `958fe44` cannot satisfy an A1 `WorkerTransitionEventV1` oracle. The test must remain library-internal because only crate-private cfg(test) builders may inject the accepted recorder; an external integration test cannot name or bypass that seam. Implement only the observation spine. Under normal NATS, the exact internal test drives one nonmutating `ReadHead` through `RuntimeWitnessClient`, both shipping public `start` methods using the crate-private cfg(test) builders that return the ordinary runners, the real public worker, private worker/proxy, recording store, and publisher. Serialize the ordered A1 worker events, proxy requests/responses, store calls/results, publish attempts/results, and server-issued connection client IDs as complete canonical records. Independently reconcile request nonce/digest, operation, subject, response digest, store generation/state digest, CAS facts, runner role, authenticated account/user, and connection identity across those records. Every emitted count must be computed from the validated serialized arrays; literal counts, transcript hashes relabeled as worker execution, defaults, repair, inferred identities, and checker-authored evidence are forbidden. Add focused mode `--focused-service-checkpoint-observations` that invokes that exact internal FQN through the normal NATS harness, first validates the positive ledger, and then kills coherent source/evidence substitutions for missing, reordered, duplicated, synthetic, relabeled, or cross-request worker/proxy/store/connection facts. Extend the harness with one explicit, non-ambient prefix `--expect-exact-test <full-fqn> <expected-filtered-out> -- <command...>`. Parse and validate the prefix before any Docker/NATS startup, strip it rather than forwarding it, require a nonempty Rust FQN, a decimal filtered count, the delimiter, exactly one forwarded `--exact`, and the same FQN immediately before that marker. The A2a command pins `expected-filtered-out=17`, matching the exact current eighteen-test library inventory. Missing/duplicate markers, wrong delimiter/FQN/count, zero execution, ignored/failed execution, filtered-only output, or a transcript with any count other than one running/one passed/zero failed/zero ignored/seventeen filtered must fail. Add self-test mutations for every prefix and transcript refusal; no ambient environment variable may select or weaken the expected test/count. Preserve existing unprefixed harness behavior for accepted selectors. Preserve the accepted A1 9/18/19/7/5/31/10 projection. Make no relay account, response-grant, suppression, replay, recovery, or abort change or claim. The exact nine-path slice digest is `919bf3addbcdbf8625f055f9c8737f22aa5b9d3dd99130fe8e62fc981b65d4c9`; the cumulative A1+A2a union is exactly eleven paths with digest `aa6b8eae0de4d644ebb18c08a36264d09b6adc81cddc50288d3067ed3715030f`. Freeze exactly one direct-child A2a commit and make no post-freeze edit.</action>
  <acceptance_criteria>The exact red test fails for absence of real A1 event reconciliation before implementation and passes afterward. The immutable ledger contains actual ordered `WorkerTransitionEventV1`, proxy, store, publisher, and server-issued connection records; the checker derives every count/digest from those arrays and independently cross-binds the real `ReadHead`. Coherent omission/reorder/duplication/synthetic/relabeled/cross-request controls each reach the ordinary oracle and fail for their intended relation. A1 remains byte-identical and independently green. Exact path counts/digests, strict package validation, immutable review P0/P1/P2=0/0/0, and pushed A2a work/checkpoint refs are required before A2b.</acceptance_criteria>
  <verify>
    <automated>set -e; test -n "$PHASE285_PLAN04_TASK03A1_COMMIT"; test -n "$PHASE285_PLAN04_TASK03A2A_COMMIT"; test -n "$PHASE285_PLAN04_TASK03A2A_TREE"; test -n "$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256"; test -n "$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256"; test "$(git rev-parse refs/remotes/origin/work/v179-phase285-plan04-task03a1)" = "$PHASE285_PLAN04_TASK03A1_COMMIT"; test "$(git rev-parse refs/remotes/origin/checkpoint/v179-phase285-plan04-task03a1-production)" = "$PHASE285_PLAN04_TASK03A1_COMMIT"; test "$(git rev-parse HEAD)" = "$PHASE285_PLAN04_TASK03A2A_COMMIT"; test "$(git rev-list --parents -n 1 "$PHASE285_PLAN04_TASK03A2A_COMMIT" | wc -w | tr -d ' ')" = 2; test "$(git rev-parse "${PHASE285_PLAN04_TASK03A2A_COMMIT}^")" = "$PHASE285_PLAN04_TASK03A1_COMMIT"; test "$(git rev-parse "$PHASE285_PLAN04_TASK03A2A_COMMIT^{tree}")" = "$PHASE285_PLAN04_TASK03A2A_TREE"; test -z "$(git status --short)"; git diff-tree --check "$PHASE285_PLAN04_TASK03A1_COMMIT" "$PHASE285_PLAN04_TASK03A2A_COMMIT"; test "$(git diff-tree --no-commit-id --name-only -r "$PHASE285_PLAN04_TASK03A1_COMMIT" "$PHASE285_PLAN04_TASK03A2A_COMMIT" | LC_ALL=C sort | shasum -a 256 | awk '{print $1}')" = 919bf3addbcdbf8625f055f9c8737f22aa5b9d3dd99130fe8e62fc981b65d4c9; test "$(git diff-tree --no-commit-id --name-only -r "$PHASE285_PLAN04_TASK03A1_COMMIT" "$PHASE285_PLAN04_TASK03A2A_COMMIT" | wc -l | tr -d ' ')" = 9; test "$(git diff-tree --no-commit-id --name-only -r 5ade79141a2f8249ef084d24fcfd5ef12251e48e "$PHASE285_PLAN04_TASK03A2A_COMMIT" | LC_ALL=C sort | shasum -a 256 | awk '{print $1}')" = aa6b8eae0de4d644ebb18c08a36264d09b6adc81cddc50288d3067ed3715030f; test "$(git diff-tree --no-commit-id --name-only -r 5ade79141a2f8249ef084d24fcfd5ef12251e48e "$PHASE285_PLAN04_TASK03A2A_COMMIT" | wc -l | tr -d ' ')" = 11; test "$(git show "${PHASE285_PLAN04_TASK03A2A_COMMIT}:tools/check-phase285-witness-integrity.sh" | shasum -a 256 | awk '{print $1}')" = "$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256"; test "$(git show "${PHASE285_PLAN04_TASK03A2A_COMMIT}:tools/fixtures/phase285-witness-integrity.json" | shasum -a 256 | awk '{print $1}')" = "$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256"; bash tools/check-phase285-witness-conformance.sh --self-test store-proxy-source; bash tools/with-nats-jetstream.sh --self-test; bash tools/with-nats-jetstream.sh --expect-exact-test service_checkpoint_observation_tests::worker_observations_are_real_and_reconciled 17 -- cargo test -p swarm-governance-witness --lib --locked --offline -- --test-threads=1 service_checkpoint_observation_tests::worker_observations_are_real_and_reconciled --exact; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A2A_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-observations; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A2A_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-deadline; cargo test -p swarm-governance-witness --all-targets --locked --offline --no-run; cargo clippy -p swarm-governance -p swarm-governance-witness --all-targets --all-features --locked --offline -- -D warnings; cargo fmt --all -- --check; shellcheck tools/check-phase285-witness-conformance.sh tools/with-nats-jetstream.sh; git show --check --oneline "$PHASE285_PLAN04_TASK03A2A_COMMIT"; test -z "$(git status --short)"</automated>
    Root freezes the exact A2a commit/tree/launcher/manifest before immutable review. Only P0/P1/P2=0/0/0 authorizes pushing `work/v179-phase285-plan04-task03a2a` and `checkpoint/v179-phase285-plan04-task03a2a-production`. A2b remains blocked until both origin refs resolve to that commit.
  </verify>
  <done>Task 285-04-03A2a banks only real observed worker/proxy/store/connection evidence and preserves A1. A2b, A2c, A3, 03B, and final Plan 04 acceptance remain blocked.</done>
</task>

<task type="auto">
  <wave>7</wave>
  <name>Task 285-04-03A2b1: Bank the accepted-ledger-bound suppression predicate</name>
  <files>crates/swarm-governance-witness/src/lib.rs, crates/swarm-governance-witness/tests/service_checkpoint.rs, tools/check-phase285-witness-conformance.sh, tools/fixtures/phase285-witness-integrity.json</files>
  <read_first>The accepted immutable A2a commit and freeze; rejected immutable candidates `5ad200f558d691ea96a5fdab9903536f2efa04ca`, `8a7df5d30fa3cf02c9aa127e4ac7d08f07e36d6c`, and `096e57eb3e8b59d711c0446905d58868f32805b0`; all three hostile reviews; accepted A2a `ObservationLedgerV1` producer and independent Python validator; all four owned paths</read_first>
  <action>Reconstruct only from accepted A2a in the existing worktree; never amend, cherry-pick, or continue any rejected candidate. Record exact RED `service_checkpoint_relay_tests::complete_receipt_authority_and_grants_are_observed` under the accepted external NATS harness. Reuse the accepted A2a execution to emit one canonical fresh `ObservationLedgerV1`; reopen the exact bytes and independently validate them before constructing the receipt. Do not create a parallel `AcceptedA2aReceiptEvidenceV1`, copy a helper-owned object, or use equality with the producer object as an oracle. Before any reservation, pass the current expected candidate tree, fresh checker-generated invocation token, and exact case `service_checkpoint_complete_receipt` into the closed private validator and require all three ledger fields to match. A valid receipt copied from invocation A into invocation B must return `Forward` with the exact capacity-one receiver empty. The private validator must independently decode and recompute canonical public request and signed response; exact twelve worker events including every operation and CAS field; store input/result digests and redundant stream/revision/generation/state/envelope fields; private Entry equality to the store result; public signed Head equality to authenticated store current Head/txid; exact publisher reply subject and response bytes; exactly three server/credential-bound identities; all three A2b1 private request/response exchanges; and causal timestamps. It must also require exactly one public `proxy_exchanges` row whose canonical bytes equal `private_exchanges[2]`, plus exactly two decoded `publisher_attempts` rows derived from the validated worker trace: ordinal 8/private/`published=true` and ordinal 11/public/`published=true`. Count-and-digest equality alone is forbidden. Only after every relation succeeds may an exact capacity-one sender reserve the receipt. Missing, invalid, partial, cross-copied, full, closed, failed, zero-capacity, capacity-two, and partially occupied capacity-two cases forward normally. Internal controls must coherently refresh nested, array, ledger, and outer-receipt digests and still reject cross-invocation identity, proxy cross-copy, and fabricated publisher rows. The focused shell checker, invoked once inside the accepted NATS harness, owns a `mktemp -d` confined directory and two absent sibling paths exported only to its two child commands as `PHASE285_COMPLETE_RECEIPT_LEDGER_PATH` and `PHASE285_COMPLETE_RECEIPT_PATH`. It passes those exact paths first to the internal FQN, requires both fresh files to be mode `0600`, distinct regular no-symlink inodes under the unchanged directory, newline framed, canonical, fsync-visible after reopen, and no larger than 1,048,576 ledger bytes and 2,097,152 receipt bytes, then passes the same read-only paths to external FQN `complete_receipt_validation_precedes_suppression_and_failures_forward`. That external FQN does not call or duplicate the cfg(test)-private predicate; it independently decodes and recomputes the current-invocation, proxy, publisher, and remaining explicit ledger/receipt relations. The checker snapshots parent/file identity and digest across the handoff and rejects absent, precreated, aliased, replaced, partial, oversized, extra, stale, or post-internal-mutated artifacts before any acceptance count. The only signal-control seam is positional checker mode `--self-test complete-receipt-real-signal <EXIT|HUP|INT|TERM> <absolute-parent-confined-control-root>`; it calls the same `run_complete_receipt_focus` function with positional arguments, never a parallel cleanup function or ambient selector. The parent creates and snapshots a canonical `0700` control root and requires readiness and release paths absent before launch. In signal mode the real focus function creates canonical `0700` scratch and artifact directories, arms the exact normal-flow `EXIT` cleanup plus `HUP/INT/TERM` cleanup-and-re-raise handlers, then enters one blocking checkpoint. That checkpoint alone create-news a canonical newline-framed mode-`0600` readiness record, fsyncs/reopens it, and binds candidate tree, fresh token, requested case, child PID, fresh 64-hex release nonce, and canonical root/scratch/artifact paths with directory type, mode `0700`, device, and inode. The child blocks without a fixed sleep or time-derived permission to exit. The parent requires record PID equal spawned PID, then fully revalidates canonical root, scratch, and artifact paths as directories with exact mode `0700`, device, and inode. HUP/INT/TERM receive no release; the parent sends the exact signal and requires cleanup, default-handler restoration, re-raise, signal-specific subprocess status, and absence of the bound scratch inode within two seconds. EXIT alone requires an exact parent release after successful validation: create-new a canonical newline-framed mode-`0600` release record, write canonical bytes, fsync the file, canonically reopen and verify its identity/content, then fsync the parent directory; the record binds the same tree, token, case, PID, release nonce, and root/scratch/artifact identities. The child validates only that exact release, exits zero, and cleans through the real EXIT trap. For release omission only, after valid readiness the parent withholds release, proves the child and bound scratch inode remain present through the bounded release deadline, then sends TERM and requires the real cleanup-and-re-raise path, signal-specific status, and residue-free teardown; only that ordered path emits `release-timeout`. Missing, malformed, aliased, replaced, duplicate, stale, cross-case, cross-PID, cross-token, cross-tree, cross-nonce, early, or mode-widened coordination fails closed. Freeze exactly twenty-five lifecycle controls: nine inherited artifact attacks; four real-flow EXIT/HUP/INT/TERM cases; six inherited coordination/trap mutations for checkpoint-before-trap, fabricated readiness, ignored-signal continuation, normal-exit substitution, trap removal, and trap substitution; and six release/metadata mutations for EXIT release omission, stale/cross-case release, early EXIT before release, fixed-sleep substitution for release wait, post-readiness control-root mode widening, and post-readiness artifact mode widening. Every control must execute nonzero and fail only its intended stable relation: `release-timeout` or `exit-before-release`, `release-identity`, `control-root-snapshot`, `artifact-snapshot`, signal-specific termination, or residue. Helper-authored counters, generic timeout success, and failure before the intended relation do not count. Freeze exactly ten compiled coherent controls: the existing seven producer-plus-receipt relations plus proxy cross-copy and publisher fabrication must compile, make the real internal positive target pass, then make the external decoder fail its intended stable relation; the current-invocation-bypass mutant must compile, execute the internal cross-copy control, and fail because the private predicate suppresses instead of forwarding. Zero tests, compile failure, timeout, generic failure, helper-authored counters, lexical rejection, failure before the named assertion, or either exact target invoked outside this checker-owned sequence do not count. Rust code may not spawn `bash`, `with-nats-jetstream.sh`, or another child; may not create an NATS/readiness/temp directory; and may not read any path selector except the two exact checker-scoped variables plus the already required current tree and invocation token. Rust may only create-new/write/fsync/reopen the two checker-selected artifact files; it may not remove or replace them. All NATS setup, path selection, boundedness checks, mutation orchestration, and cleanup stay in the shell harness/checker. Do not add topology, grants, live loss/replay, recovery, or A2c behavior. Preserve the exact four-path digest `ccd00d773edc3826aecd28bf6d451d0d2a5323ee766224e01b5633cb3815070a` and freeze one clean direct child of A2a.</action>
  <acceptance_criteria>The NATS-wrapped RED runs exactly once and fails only because accepted A2a lacks the ledger-bound pre-suppression predicate. The immutable positive path consumes a fresh accepted A2a ledger; the internal target proves current-invocation binding, complete proxy/publisher reconciliation, actual validation-before-reservation, and fail-open behavior, while the external integration target independently proves the same artifact relations without naming the private predicate. Both run only as one checker-owned two-stage invocation. At A2b1 the checker requires internal running/passed/failed/ignored/filtered=`1/1/0/0/18`, external=`1/1/0/0/1`, lifecycle=`25/25`, and compiled controls=`10/10` with vacuous=`0`; inherited A2b2/A2b3 gates require the external filtered count `2`, and every count is pinned by the candidate tree plus manifest rather than ambient selection. Every invalid/partial/coherent mutation reaches and fails its named relation; the invocation-A-to-B capacity-one case forwards with an empty receiver; every reservation failure forwards; only a current, fully reconciled receipt followed by successful exact capacity-one reservation suppresses. The four signal cases, six inherited coordination/trap mutants, and six release/metadata mutants execute the real focused function through the exact positional readiness/release contract, with PID/inode/type/mode binding and signal-specific termination. EXIT cannot pass before the parent validates and releases it; no fixed-sleep, helper-trap, ordinary-exit, stale-release, or mode-widening substitute can pass. No public/debug/test-support production API and no Rust-owned NATS child, readiness, directory, or cleanup handoff exists. No topology, live-grant, relay freshness, recovery, or A2c claim is made. Exact scope, full r41 final-tree chain, immutable P0/P1/P2=`0/0/0` review, and two exact pushed refs are required.</acceptance_criteria>
  <verify>
    <automated>set -e; test -n "$PHASE285_PLAN04_TASK03A2A_COMMIT"; test -n "$PHASE285_PLAN04_TASK03A2B1_COMMIT"; test -n "$PHASE285_PLAN04_TASK03A2B1_TREE"; test -n "$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256"; test -n "$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256"; test "$(git rev-parse refs/remotes/origin/work/v179-phase285-plan04-task03a2a)" = "$PHASE285_PLAN04_TASK03A2A_COMMIT"; test "$(git rev-parse refs/remotes/origin/checkpoint/v179-phase285-plan04-task03a2a-production)" = "$PHASE285_PLAN04_TASK03A2A_COMMIT"; test "$(git rev-parse HEAD)" = "$PHASE285_PLAN04_TASK03A2B1_COMMIT"; test "$(git rev-list --parents -n 1 "$PHASE285_PLAN04_TASK03A2B1_COMMIT" | wc -w | tr -d ' ')" = 2; test "$(git rev-parse "${PHASE285_PLAN04_TASK03A2B1_COMMIT}^")" = "$PHASE285_PLAN04_TASK03A2A_COMMIT"; test "$(git rev-parse "$PHASE285_PLAN04_TASK03A2B1_COMMIT^{tree}")" = "$PHASE285_PLAN04_TASK03A2B1_TREE"; test -z "$(git status --short)"; git diff-tree --check "$PHASE285_PLAN04_TASK03A2A_COMMIT" "$PHASE285_PLAN04_TASK03A2B1_COMMIT"; test "$(git diff-tree --no-commit-id --name-only -r "$PHASE285_PLAN04_TASK03A2A_COMMIT" "$PHASE285_PLAN04_TASK03A2B1_COMMIT" | LC_ALL=C sort | shasum -a 256 | awk '{print $1}')" = ccd00d773edc3826aecd28bf6d451d0d2a5323ee766224e01b5633cb3815070a; test "$(git diff-tree --no-commit-id --name-only -r "$PHASE285_PLAN04_TASK03A2A_COMMIT" "$PHASE285_PLAN04_TASK03A2B1_COMMIT" | wc -l | tr -d ' ')" = 4; test "$(git show "${PHASE285_PLAN04_TASK03A2B1_COMMIT}:tools/check-phase285-witness-integrity.sh" | shasum -a 256 | awk '{print $1}')" = "$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256"; test "$(git show "${PHASE285_PLAN04_TASK03A2B1_COMMIT}:tools/fixtures/phase285-witness-integrity.json" | shasum -a 256 | awk '{print $1}')" = "$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256"; bash tools/check-phase285-witness-integrity.sh --integrity-self-test; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A2B1_TREE" bash tools/check-phase285-witness-integrity.sh --self-test complete-receipt-suppression; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A2B1_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-observations; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A2B1_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-deadline; cargo test -p swarm-governance-witness --all-targets --locked --offline --no-run; cargo clippy -p swarm-governance-witness --all-targets --all-features --locked --offline -- -D warnings; cargo fmt --all -- --check; shellcheck tools/check-phase285-witness-conformance.sh; git show --check --oneline "$PHASE285_PLAN04_TASK03A2B1_COMMIT"; test "$(git rev-parse HEAD)" = "$PHASE285_PLAN04_TASK03A2B1_COMMIT"; test "$(git rev-parse HEAD^{tree})" = "$PHASE285_PLAN04_TASK03A2B1_TREE"; test -z "$(git status --short)"</automated>
    Only immutable zero-finding review authorizes `work/v179-phase285-plan04-task03a2b1` and `checkpoint/v179-phase285-plan04-task03a2b1-production`.
  </verify>
  <done>A2b1 banks only the pre-suppression complete-receipt predicate and fail-open behavior.</done>
</task>

<task type="auto">
  <wave>8</wave>
  <name>Task 285-04-03A2b2: Bank the account/principal-scoped topology validator</name>
  <r51_precedence>R51 supersedes only r50 planning-status/production-authorization and cross-invocation inode equality. Stopped tree `4f04081a619bf87352d11af648208235afbebd01` passed through the cold seed (`fresh=false`, 39730 ms) and then rejected the immediate `fresh=true` default release because Cargo republished the top-level rlib inode. A bounded same-candidate diagnostic proved path, length 4440592, device 16777235, mode, and SHA stayed exact while inode changed 599885074->599885142. Preserve every r50-r45 rule. Keep the seed O_NOFOLLOW fd open through symbol scan; after each release and before scan, rewind/rehash it and require exact fstat length/dev/inode/mode/hash. Each fresh receipt requires exact seed declared/resolved path, length/hash/dev/mode and full per-invocation r49 inode stability; allow only the cross-invocation publication inode to be stable or replaced, record both inodes/relation, and never trust that relation as success. Use exact component-specific mismatch reasons. Close fd on every exit. No retry; generic120/downstream/API/artifact scan/31 mutants/full chain unchanged. Production remains frozen until immutable r51 passes independent 0/0/0, is pushed/fetched exactly, and root records a fresh two-hour continuation.</r51_precedence>
  <r50_precedence>R50 supersedes only r49 planning-status/production-authorization. Stopped unfrozen tree `852a61559f1c1af380231bd8f33123aa5c847825` passed integrity/harness, topology `33/33`, observations, deadline positives/mutations, and both debug boundary receipts. Its exact r49 default-release receipt timed out at 300 seconds while compiling release dependencies, before any witness artifact. Preserve all r49-r45 contracts. After the two debug receipts, run the exact default witness release command once as `cold_default_release_seed` in the same exact tree/shared target via the same process-group/output/JSON/artifact runner with timeout 900 and no external cache/fallback. Require sole successful build-finished, sole fresh=false non-test `swarm_governance_witness` lib artifact, exactly one rlib, the complete r49 confinement/O_NOFOLLOW/fd-hash/stable identity proof, measured elapsed_ms, and seed artifact SHA. This is the declared longer final-local lane under the same two-hour slice stop. Then unconditionally run both unchanged final witness release receipts once with their r49 300-second bounds; require Cargo fresh=true and exact seed path/length/SHA/dev/inode, append seed SHA to their existing rows, and revalidate it after each plus before symbol scan. Neither stage may retry after failure. Generic120/downstream/API negatives/artifact scan/31 mutants and full chain remain unchanged. Production remains frozen until immutable r50 passes independent 0/0/0, is pushed/fetched exactly, and root records a fresh two-hour continuation.</r50_precedence>
  <r49_precedence>R49 supersedes only r48 planning-status/production-authorization. Stopped unfrozen tree `7b565585fb7d167d68ceb52a6f61a3348dcf2c03` passed topology `33/33`, observations, deadline positives/mutations, and debug boundary builds, then cold default-release build exceeded the generic 120-second timeout with discarded output. Preserve all r48/r47/r46/r45 contracts. Move only four `non_test_*` builds to a same-exact-tree/same-fresh-shared-target bounded runner: exact commands plus `--message-format=json`, 300 seconds, new process group killed on timeout/16,777,216-byte combined-output overflow, failure receipt plus final at-most-65,536 bytes, no retry/external cache/fallback. Emit non-evidence receipt/state/timeout progress. Require exit0, sole successful build-finished, sole non-test `swarm_governance_witness` lib compiler artifact, exactly one declared `.rlib`, pre-resolve lstat regular/non-symlink/nonempty, canonical shared-target confinement, and stable length/SHA256/dev/inode. Preserve the existing final receipt fields/order and append its artifact SHA256. Generic120/downstream/API negatives/artifact scan/31 mutants stay unchanged. Production remains frozen until immutable r49 passes independent P0/P1/P2=`0/0/0`, is pushed/fetched exactly, and root records a fresh two-hour continuation.</r49_precedence>
  <r48_precedence>R48 supersedes only r47 planning-status/production-authorization. Stopped unfrozen tree `b3af2d0f12d5ec4db39cee10d24692bcd71c1fff` was interrupted by root after roughly 300 seconds of buffered silence inside topology projections; this was not a checker timeout because sequential existing bounds total up to `2*(300+300)+2*120=1440` seconds. Termination flushed four completed rows and no assertion failure. Preserve all r47/r46/r45 semantics and timeouts. Invoke only `topology_projection_controls` as `python3 -I -u -` and emit flushed progress-only mutation/phase/state/timeout records immediately before and after each existing subprocess: Rust literal and owner-swap internal/external `300`; shell owner-swap and stale-token external `120`. Progress rows never count as mutation/evidence/digest success; unchanged final rows and final `33` summary remain authoritative. Silence cannot be inferred as timeout; only checker-emitted failure/timeout, the two-hour slice stop, or user interruption can terminate. Production remains frozen until immutable r48 passes independent P0/P1/P2=`0/0/0`, is pushed/fetched exactly, and root records a fresh two-hour continuation.</r48_precedence>
  <r47_precedence>R47 supersedes only r46 planning-status/production-authorization and the stopped implementation's unplanned exactly-one-raw-`panicked at` predicate. Stopped unfrozen tree `ea6e8316e3f0805fa0f6cc0ee8a83dc186ff8995` passed topology `33/33`, the observation baseline `1/1/0/0/18`, fresh-target compile, confined executable identity, direct exact execution, exit `101`, and the intended late message, but was rejected because expected worker-to-join panic propagation produced two panic headers. Preserve every r46 bound and r45 topology requirement. Parse every anchored `^thread '([^']+)'(?: \([0-9]+\))? panicked at .+:$` header and its first following nonempty line before the next header; missing messages are fatal. Ignoring only that optional numeric ID and source coordinates, require the complete ordered list to equal exactly `phase285-a2a-worker-observations` / `observation public ReadHead is absent`, then `service_checkpoint_observation_tests::worker_observations_are_real_and_reconciled` / `observation thread panicked: Any { .. }`. Retain exact exit `101`, one running/zero passed/one failed/zero ignored, and exactly one intended phrase. Any missing/additional/reordered/substituted record, intended phrase elsewhere or not exactly once, different status/count, timeout, overflow, ambiguity, or earlier failure is vacuous and fatal. Stack/note text cannot count as a record. Production remains frozen until immutable r47 passes independent P0/P1/P2=`0/0/0`, is pushed/fetched exactly, and root records a fresh two-hour continuation.</r47_precedence>
  <r46_precedence>R46 supersedes r45 planning-status and production-authorization sentences only. The stopped unfrozen tree `718f766f91aca30836826ee885f5f087bf2f3534` passed topology `33/33`, both self-tests, and static gates but failed the final-byte observation selector when its combined cold compile-and-run `public_head_absent` mutation hit 120 seconds; the mutation received no credit, deadline was not started, and no commit/push occurred. Preserve every r45 semantic/count/scope requirement. Replace only that combined subprocess with a fresh-target, exact-tree two-phase receipt: `cargo test -p swarm-governance-witness --lib --locked --offline --no-run --message-format=json` within 300 seconds and a 16,777,216-byte combined-output cap; require exit zero, exactly one successful `build-finished`, and exactly one non-null executable from a test-profile `swarm_governance_witness` `lib` compiler artifact; require that executable to be a regular non-symlink beneath the canonical confined target and snapshot length/SHA-256/device/inode; then execute that exact binary once within 60 seconds and the same output cap as `--test-threads=1 service_checkpoint_observation_tests::worker_observations_are_real_and_reconciled --exact`, requiring one running, zero passed, one failed, zero ignored, and only the late `observation public ReadHead is absent` relation, followed by executable snapshot revalidation. Either timeout, output-cap overflow, compile failure, ambiguous/missing/out-of-target executable, zero/multiple execution, earlier failure, or missing late relation is vacuous and fatal. Before cleanup, emit the phase plus the final at-most-65,536 captured bytes on failure; clean the confined tree/target on every exit; never use a shared/prebuilt target and never retry. Production remains frozen until immutable r46 passes independent P0/P1/P2=`0/0/0`, is pushed/fetched exactly, and root records a fresh two-hour continuation.</r46_precedence>
  <r45_precedence>R45 also supersedes r44's planning-status and production-authorization sentences: r44 is rejected, and only independently accepted/pushed r45 may authorize a fresh window.</r45_precedence>
  <r45_amendment>This r45 amendment supersedes r44 topology-control counts and inventory only; all non-count r44 and r43 requirements remain mandatory. Run exactly thirty-three non-vacuous controls with thirty-three unique full mutated-source or artifact-control digests: the twenty-one r44 config controls unchanged and in their prior order; then `malformed_store_import_row` and `malformed_runtime_export_row`; then the unchanged three comparator-deletion controls; then the four original projection controls followed by `projection_mode_widening`, `projection_inode_alias`, and `projection_post_snapshot_replacement`. Arithmetic is exactly `23+3+7=33`. Each malformed-row control must be raw-config-grammar-valid, reach the current validator, and fail only at `topology[imports-malformed]` or `topology[exports-malformed]`; removing its row-shape predicate must make that otherwise empty owner projection survive. The three artifact controls must execute the ordinary real four-projection flow before changing exactly one relation: widen one completed artifact from `0600` to `0644` and fail only at `topology_snapshot[mode]`; hard-link the byte-identical shell canonical path to the Rust canonical inode before the alias gate and fail only at `topology_snapshot[alias]`; replace one snapshotted projection with a new mode-`0600` inode containing identical bytes before the next handoff and fail only at `topology_snapshot[changed]`. NATS startup failure, generic parsing/setup failure, compile failure, zero targets, or an earlier unrelated relation is vacuous. The stopped r43 implementation remains unaccepted and production stays frozen until immutable r45 receives independent P0/P1/P2=`0/0/0`, is pushed/fetched exactly, and root records a fresh two-hour window.</r45_amendment>
  <r44_amendment>This r44 amendment supersedes every `twenty-five`, `25`, `eighteen`, `18`, and `r43 named registry` topology-control reference in this task's action and acceptance criteria only; all non-count r43 requirements remain mandatory. Run exactly twenty-eight non-vacuous topology controls with twenty-eight unique full mutated-source digests: the original eighteen r43 config controls unchanged and in their original order, followed by `extra_store_principal_raw_authority`, `duplicate_store_imports_declaration`, and `duplicate_runtime_exports_declaration`; then the unchanged three executable comparator-deletion controls and unchanged four projection controls. Arithmetic is exactly `21+3+4=28`. The canonical validator must enumerate the complete authenticated-principal inventory inside each exact account, reject the extra store principal carrying `$JS.API.>` at `topology[principal-inventory:PHASE285_WITNESS_STORE]`, distinguish true absence of optional imports/exports from duplicate or malformed declarations, and reject the duplicate store imports and runtime exports controls only at their declaration-uniqueness relations. All three controls must reach the current validator, execute nonzero, and be non-vacuous. Before every projection read, require exact mode `0600`; require all four Rust/shell canonical/probe projections to have distinct device/inode identities and preserve those identities across handoffs. The stopped r43 implementation has no valid evidence. Production remains frozen until immutable r44 planning receives independent P0/P1/P2=`0/0/0`, is pushed/fetched exactly, and root records a fresh two-hour window.</r44_amendment>
  <files>crates/swarm-governance-witness/src/lib.rs, crates/swarm-governance-witness/tests/service_checkpoint.rs, tools/check-phase285-witness-conformance.sh, tools/fixtures/phase285-witness-integrity.json, tools/with-nats-jetstream.sh</files>
  <read_first>The accepted immutable A2b1 commit and freeze; rejected immutable candidates `ef4c043e0039d77282683fab5b99fbe9f3ed34c2` and `a7c4b138aba99764ec7cd352290050358adcbb21`; rejected r42 planning commit `19c0172aebc7c667249543fc9c72f1db70765630`; all three topology reviews; the r43 precedence rule in `285-04-03A-R11-DESIGN.md`; the exact init authority contract at `285-WITNESS-ADAPTER-CONTRACT.md:158-169`; all five owned paths</read_first>
  <action>Start only after both A2b1 origin refs resolve exactly and never amend, cherry-pick, or continue a rejected candidate. Parse the actual generated `nats.conf` account and authenticated-principal blocks into closed tuple sets. Compare publish, subscribe, export subject/type/allowed-account, import subject/to/account, response-grant, and prohibited raw authority inside the exact owner block. The init principal's publish set is exactly `$JS.API.STREAM.CREATE.KV_phase285_service`, `$JS.API.STREAM.UPDATE.KV_phase285_service`, `$JS.API.STREAM.INFO.KV_phase285_service`, `$JS.API.STREAM.MSG.GET.KV_phase285_service`, `$KV.phase285_service.__witness_bucket_manifest`, and `$KV.phase285_service.s.0fc95119eb171c924f962c3af0a1f03c70078a8fd8a590189d7358e3c62ba1ef`; it subscribes only `_INBOX.>`, has no response grant, and has no `$JS.API.>` or `$KV.>` wildcard. The checker owns confined create-new `0600` Rust/shell projection artifacts bound to the exact candidate tree, fresh invocation token, case, and both canonical/probe config digests. Before any read, require canonical and probe configs to be regular non-symlink files of `1..=262144` bytes, each of four credential-role files `1..=4096` bytes, and each projection `1..=16384` bytes; snapshot and revalidate length/digest/device/inode/mode across every handoff. The checker creates one projection-only probe with canonical grammar/cardinality, fresh independent 128-bit uppercase-hex-suffixed account identifiers, and distinct lowercase-hex-suffixed principal usernames fixed before parser launch. The existing exact internal Rust target must parse both actual canonical config/credential inputs and the fresh probe at runtime; the shell parser independently parses the same bytes. First require Rust/shell equality for canonical and unpredictable probe projections, then separately require canonical policy acceptance and probe rejection for noncanonical inventory. A literal constant, self-equality assertion, dead projection, regex scan, or use of expected-output bytes is forbidden. The external exact topology FQN independently decodes all four canonical artifacts and their bindings; it may not spawn shell or name a cfg(test)-private predicate. Run exactly twenty-five non-vacuous topology controls per the r43 named registry: eighteen config, three executable current-validator comparator deletions, and four cross-language projection controls. `rust_literal_disconnect` must compile the correct canonical literal, pass canonical equality, then fail only on the fresh unpredictable probe. Each comparator-deletion control must make canonical pass, make only its isolated hostile config survive the altered validator, and then fail the outer checker at that named component; source fragments or global counts do not count. Preserve the complete A2b1 internal/external/artifact/lifecycle/compiled chain in the same checker-owned invocation. Make no serving-binary, live relay, grant-timing, recovery, or A2c claim. The exact five-path direct-child digest remains `964b80e1d926ccc78f1e2f487edb84169a01f1cb6a1eadcf3b294c3234ce98ff`. Freeze one fresh clean direct child of accepted A2b1.</action>
  <acceptance_criteria>The predecessor RED proves both the cardinality-preserving runtime/witness import move and canonical init `$JS.API.>`/`$KV.>` wildcards survive. The immutable candidate accepts only the six-subject init inventory, reports executed Rust-derived and independently shell-derived canonical plus fresh-probe projections with exact bounded artifacts and stable identity/digest snapshots, and rejects all twenty-five topology controls at their intended late relations with twenty-five unique mutated-source digests and vacuous=`0`. The correct-literal disconnected Rust mutant fails on the unpredictable probe; deleting any response-grant, import-account, or export-account comparator makes its isolated hostile graph survive and therefore fails the outer checker. A2b1 stays green inside the same bounded run. Exact scope, strict focused tests, immutable P0/P1/P2=`0/0/0` review, and two exact pushed refs are required.</acceptance_criteria>
  <verify>
    <automated>set -e; test -n "$PHASE285_PLAN04_TASK03A2B1_COMMIT"; test -n "$PHASE285_PLAN04_TASK03A2B2_COMMIT"; test -n "$PHASE285_PLAN04_TASK03A2B2_TREE"; test -n "$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256"; test -n "$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256"; test "$(git rev-parse refs/remotes/origin/work/v179-phase285-plan04-task03a2b1)" = "$PHASE285_PLAN04_TASK03A2B1_COMMIT"; test "$(git rev-parse refs/remotes/origin/checkpoint/v179-phase285-plan04-task03a2b1-production)" = "$PHASE285_PLAN04_TASK03A2B1_COMMIT"; test "$(git rev-parse HEAD)" = "$PHASE285_PLAN04_TASK03A2B2_COMMIT"; test "$(git rev-list --parents -n 1 "$PHASE285_PLAN04_TASK03A2B2_COMMIT" | wc -w | tr -d ' ')" = 2; test "$(git rev-parse "${PHASE285_PLAN04_TASK03A2B2_COMMIT}^")" = "$PHASE285_PLAN04_TASK03A2B1_COMMIT"; test "$(git rev-parse "$PHASE285_PLAN04_TASK03A2B2_COMMIT^{tree}")" = "$PHASE285_PLAN04_TASK03A2B2_TREE"; test -z "$(git status --short)"; git diff-tree --check "$PHASE285_PLAN04_TASK03A2B1_COMMIT" "$PHASE285_PLAN04_TASK03A2B2_COMMIT"; test "$(git diff-tree --no-commit-id --name-only -r "$PHASE285_PLAN04_TASK03A2B1_COMMIT" "$PHASE285_PLAN04_TASK03A2B2_COMMIT" | LC_ALL=C sort | shasum -a 256 | awk '{print $1}')" = 964b80e1d926ccc78f1e2f487edb84169a01f1cb6a1eadcf3b294c3234ce98ff; test "$(git diff-tree --no-commit-id --name-only -r "$PHASE285_PLAN04_TASK03A2B1_COMMIT" "$PHASE285_PLAN04_TASK03A2B2_COMMIT" | wc -l | tr -d ' ')" = 5; test "$(git show "${PHASE285_PLAN04_TASK03A2B2_COMMIT}:tools/check-phase285-witness-integrity.sh" | shasum -a 256 | awk '{print $1}')" = "$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256"; test "$(git show "${PHASE285_PLAN04_TASK03A2B2_COMMIT}:tools/fixtures/phase285-witness-integrity.json" | shasum -a 256 | awk '{print $1}')" = "$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256"; bash tools/check-phase285-witness-integrity.sh --integrity-self-test; bash tools/with-nats-jetstream.sh --self-test; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A2B2_TREE" bash tools/check-phase285-witness-integrity.sh --self-test topology-owner-blocks; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A2B2_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-observations; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A2B2_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-deadline; cargo clippy -p swarm-governance-witness --all-targets --all-features --locked --offline -- -D warnings; cargo fmt --all -- --check; shellcheck tools/check-phase285-witness-conformance.sh tools/with-nats-jetstream.sh; git show --check --oneline "$PHASE285_PLAN04_TASK03A2B2_COMMIT"; test "$(git rev-parse HEAD)" = "$PHASE285_PLAN04_TASK03A2B2_COMMIT"; test "$(git rev-parse HEAD^{tree})" = "$PHASE285_PLAN04_TASK03A2B2_TREE"; test -z "$(git status --short)"</automated>
    Only immutable zero-finding review authorizes `work/v179-phase285-plan04-task03a2b2` and `checkpoint/v179-phase285-plan04-task03a2b2-production`.
  </verify>
  <done>A2b2 banks only the exact owner-block topology validator while preserving A2b1.</done>
</task>

<task type="auto">
  <wave>9</wave>
  <name>Task 285-04-03A2b3: Integrate the four-account relay, live grants, and causal timing</name>
  <files>crates/swarm-governance-witness/Cargo.toml, crates/swarm-governance-witness/src/bin/swarm-governance-witness-store.rs, crates/swarm-governance-witness/src/bin/swarm-governance-witness.rs, crates/swarm-governance-witness/src/lib.rs, crates/swarm-governance-witness/src/raw_config.rs, crates/swarm-governance-witness/src/runtime_client.rs, crates/swarm-governance-witness/src/service_config.rs, crates/swarm-governance-witness/tests/service_checkpoint.rs, tools/check-phase285-witness-conformance.sh, tools/fixtures/phase285-witness-integrity.json, tools/with-nats-jetstream.sh</files>
  <read_first>The accepted immutable A2b2 commit and freeze; rejected immutable candidate `ef4c043e0039d77282683fab5b99fbe9f3ed34c2`; its P1-3 review finding and verified-clean areas; all eleven owned paths</read_first>
  <r64_precedence>R63's live success lacks source-to-binary provenance: cached lib-test binary SHA-256 `ab563048ba9f3410e6bf2f77524621cdae81ef61478ec45332a425f72c4d5c52` contains diagnostic-only `DeliveryTimeout`, while accepted final11 lib SHA-256 `3dac41d81b30b5b30b10327c410b015843685baf5dbf0c62ff01b9cb1584a139` does not. A2b3 remains banked at both exact refs but acceptance is suspended. R64 authorizes no source edit. After immutable r64 review/push and a fresh window, create one validated ephemeral unregistered shared clone at exact `ed77183...` / `bdfadfa...`, use one initially absent clone-local target with locked/offline Cargo and `CARGO_INCREMENTAL=0`, parse bounded no-run JSON to identify exactly one lib-test executable under that target, hash it, and require the exact diagnostic-enum fingerprint `ReplySubscriptionRequestPublishRequestFlushDeliveryTimeoutDeliveryClosed` absent. Run relay-positive exactly once with correct tree pin `bdfadfa...` and the same target; require Cargo to execute that exact binary, hash unchanged, FQN 1/0/0/18, full relay/grant evidence, and cleanup. Any failure stops without retry. Independent evidence review 0/0/0 alone reaccepts the existing object; no new production commit/ref.</r64_precedence>
  <r63_precedence>Accepted r62 validly did not reproduce the pre-worker delivery failure but made no relay-positive or acceptance claim. R63 authorizes no production edit: require r62 checkpoint exact, r62 result ledger as r63's direct parent, and the work ref equal the later root authorization ledger; after exact final11/A2b2/index/hash/residue preflight, replay the original r56 relay-positive selector once with all diagnostic/grant-only ambient variables explicitly removed. Nonzero/vacuous/timeout/residue/drift stops without retry. Only green permits the remaining r56 no-run/governance/clippy/fmt/shell/diff/terminal gates once, each identity-bracketed. Only a fully green chain permits exact final11 real-index staging, one direct-child commit, immutable production 0/0/0 review, then both A2b3 refs. No edit, new worktree, extension, or A2c authority exists. R63 itself is planning-only pending immutable 0/0/0 and push/fetch.</r63_precedence>
  <r62_precedence>Immutable r61 review returned P0/P1/P2=`0/1/0`: a static symlink failed initial lstat, so deleting `O_NOFOLLOW` did not change the result. R62's test-only pre-open hook runs after successful regular-file lstat, uses Darwin `renamex_np(RENAME_EXCL)` to move the exact inode to an absent sibling, installs a create-new relative symlink with exact readlink bytes at the original path, and then permits open. Canonical `O_NOFOLLOW` must refuse exactly with ELOOP; a finally path verifies/removes only that symlink and no-replace restores/revalidates the original inode and bytes. The coherent mutant deletes only `O_NOFOLLOW`, follows the symlink, restores the path after open, and must pass every later double-read/metadata/record/process predicate; only that canonical-refusal/mutant-acceptance pair counts. Link-count and mode controls receive the same single-predicate-deletion late-pass requirement. Count remains 25+1. All other r61/r60 rules remain exact; r62 is planning-only pending immutable 0/0/0 and push/fetch.</r62_precedence>
  <r61_precedence>Immutable r60 review returned P0/P1/P2=`0/1/0`: one sidecar read/hash plus metadata-only revalidation cannot detect a same-length same-inode content rewrite. R61 opens once with `O_NOFOLLOW`, binds exact regular-inode owner/link-count/mode/length identity, independently reads/hashes/canonical-parses twice from the same descriptor with fstat before/between/after and final same-inode lstat, and requires exact A/B bytes, hashes, projections, and metadata. Its test-only between-read hook performs an equal-length canonical rewrite. Independent symlink-without-no-follow, link-count-two, and mode-widening controls bring the inventory to exactly twenty-five parser/artifact controls plus the hostile-index control. Every other reviewed r60 rule remains unchanged. R61 is planning-only pending immutable 0/0/0 and exact push/fetch.</r61_precedence>
  <r60_precedence>The sole r59 diagnostic is consumed and invalid. Its child emitted one final11/token-bound `not_reproduced` record and passed exact `1/0/0/18`, but `--nocapture` split the harness's required literal test-materialization line, causing harness exit `1`; the outer parser also sought the private nonce on stdout. R60 leaves production and the harness unchanged. Clone-only test code writes one create-new, synced, mode-0600, maximum-4096-byte canonical sidecar under the validated diagnostic root. Non-reproduction emits no diagnostic stdout, preserving literal materialization. Classified failure emits byte-identical stdout only after sidecar sync and before the deliberate panic, preserving observable panic order. A fixed mode-0500 ephemeral wrapper prints exactly one current harness-generated relay topology token before execing the unchanged Cargo argv. The outer validator binds that token to the descriptor-validated sidecar and supervised epoch, retains r59 process/libtest/panic rules, and kills exactly twenty-two parser/artifact controls plus the separate hostile-index control. R60 is planning-only pending immutable P0/P1/P2=`0/0/0` and push/fetch; its later one-run window still forbids production edits, retries, relay-positive, A2c, and registered worktrees.</r60_precedence>
  <r59_precedence>Immutable r58 review rejected ambient-index sensitivity and arbitrary-nonzero acceptance. R59 begins by unsetting `GIT_INDEX_FILE`; pins every clone Git/Cargo/harness/supervisor/parser operation to the canonical clone index; names the production alternate index only in explicit immutable checks; and kills an ambient-wrong-index control without changing either index hash. Failure requires normal unsignaled exit 101, exact one-test FQN failure summary `0/1/0/18`, one classified record before exact inner deliberate panic and exact outer join panic, with no extra panic. Non-reproduction requires normal exit 0 and exact FQN success summary `1/0/0/18`. The validator kills exactly twelve controls including failed+0, not-reproduced+101, wrong summary, and signal-after-record. All other r58 bounds/isolation/no-retry/no-production/no-successor rules remain exact.</r59_precedence>
  <r58_precedence>Immutable r57 review rejected its wrong default filtered count, non-fail-safe in-place restore, and prose-only unbounded output. R58 permits one diagnostic clone only: shared no-checkout clone in a validated `mktemp` root, accepted A2b2 checkout, index/worktree reset to exact final11, temporary test-only `src/lib.rs` patch, production worktree untouched. The patch maps exactly five request-delivery stages, runs public solo then private solo then concurrent delivery, and emits exactly one canonical tree/token-bound terminal record. A 300-second/4,194,304-byte process-group supervisor runs exact FQN once with `PHASE285_GRANT_ONLY=1`, `PHASE285_GRANT_DIAGNOSTIC=1`, `--expect-exact-test <FQN> 18 --`, and `--nocapture`. A parser first kills seven synthetic controls, then rejects missing/extra/stale/generic/partial/status-inconsistent results. Delete only the validated temporary root, reverify untouched final11 and zero residue, record one diagnosis or invalid diagnostic, and stop. No retry, diagnostic commit, production fix/edit/commit/push, registered worktree, relay-positive, or A2c is authorized.</r58_precedence>
  <r57_precedence>R56 stopped after every final11 gate through normal grants passed. The relay-topology invocation failed in `2.03s` before receipt/worker/replay semantics at the shared `grant accepted request: WitnessOutcomeMismatch`. Source audit proves the helper erases five transport stages and both concurrent case identities. No causal production change is authorized. After immutable r57 review/push, run one existing-worktree diagnostic projection only: temporarily instrument test-only `src/lib.rs` with a closed five-stage error enum and exact case/account/subject binding, execute ordered public-only, private-only, then original-concurrent relay grant probes under one relay harness and `PHASE285_GRANT_ONLY=1`, stop at the first classified relation, restore exact final11 bytes/tree/index, and close. The run is not acceptance evidence. No retry, production fix, commit, push, new worktree, relay-positive, or A2c work is authorized. R58 must name the observed causal relation and one executable late RED before any fix.</r57_precedence>
  <r56_precedence>R56 changes command authority only after r55's direct topology integration test failed `topology input path absent: NotPresent` before semantics. Exact final11 `bdfadfa1e6872596e2f37da4fe6cbebf66ee66d4` is unchanged. Carry only r55's completed exact-tree identity, integrity `14/14`, harness self-test, and complete-receipt receipts; the failed direct topology command has no evidence authority. Replace both the direct artifact-consuming test and unwrapped selector with the single accepted A2b2 producer→consumer command: `bash tools/with-nats-jetstream.sh env <exact integrity/tree pins> bash tools/check-phase285-witness-integrity.sh --self-test topology-owner-blocks`. It must create/bind the canonical config, four credentials, scratch, probe, four projections, token, and snapshots before its internal producer, exact positive external test, and `23+3+7=33` unique/vacuous-zero controls. Then run every other incomplete final11 gate once. Before/after each gate require exact tree, eleven-path digest, final9 repair digest, checker/manifest/launcher hashes/blobs, working bytes, empty real index, zero candidate untracked paths, exact A2b2 refs, and fail-closed Docker residue. Any nonzero, vacuity, timeout, identity drift, or hard stop ends r56 without retry, edit, extension, commit, push, new worktree, or successor work. Green alone permits exact eleven-path real-index final11, one direct child of A2b2, immutable P0/P1/P2=`0/0/0`, and both A2b3 refs pushed/fetched. Production remains frozen until immutable r56 planning is independently accepted and pushed/fetched; A2c remains blocked.</r56_precedence>
  <action>Start only after both A2b2 origin refs resolve exactly. Reimplement the two serving binaries and live four-account/five-principal public/private relay on the accepted A2b1 predicate and A2b2 owner-block validator; rejected `ef4c043` is diagnostic evidence only and must not be amended or cherry-picked. Invoke both shipping starts through the accepted crate-private A1/A2a observer seam. Bind the complete canonical receipt before capacity-one reservation, tear down and recreate both relay legs and all identities for replay, and require exact typed `ReadHead` equality. Bind max-one refusal to bounded server connz/permission evidence for the authenticated account/user/client. Preserve exact private/public grant durations of 3,000/12,000 ms. Record both origin-relative publish timestamps and require `second_publish_at_millis - first_publish_at_millis < 50`; a control delays the first response beyond 50 ms and must fail even when the second publish immediately follows it. Run exact normal-grant and relay-positive modes plus accepted A2a observations and A1 deadline. A2c retains the final 38-mutant oracle. The direct-child changed set is exactly the eleven owned paths/digest `4d9d95a9253af2cede7f42175322169a4deb3fafebc8fb0d58bcb845cd992adf`; the cumulative A2a-to-A2b3 union is the same eleven paths/digest, and the Task02-to-A2b3 union is exactly fourteen paths/digest `4e6629733654f7b4b6f1233bfaaef96a0fede0368620380c8b97f9d75bb18664`. Freeze one clean direct child of accepted A2b2.</action>
  <acceptance_criteria>The exact live RED runs once and fails before integration. The immutable candidate proves one complete pre-suppression receipt, fail-open reservation, fresh public/private identities, owner-block-closed authority, server-bound max-one refusal before exact expiry, and the true first-to-second publish interval. The delayed-first-response control kills the former timing oracle. A2b1/A2b2, A2a observations, and A1 deadline remain independently green. Exact scope, strict final-tree gates, immutable P0/P1/P2=`0/0/0` review, and two exact pushed refs are required before A2c.</acceptance_criteria>
  <verify>
    <r63_no_edit_replay>Do not execute before immutable r63 receives P0/P1/P2=`0/0/0`, is pushed/fetched exactly, and root records a fresh two-hour window. Require r62 checkpoint `1fcd49930a61320bf8e15857cfcc523c186b4af4`; require r62 result ledger `4c1445380e981888d9d3cd436858eb4268275133` as r63's direct parent; require the work ref equal the exact later r63 authorization commit recorded by root. Use r56's exact `verify_final11` before/after every command. Run once: `env -u PHASE285_GRANT_DIAGNOSTIC -u PHASE285_GRANT_DIAGNOSTIC_PATH -u PHASE285_GRANT_ONLY -u PHASE285_RELAY_TOPOLOGY_TOKEN bash tools/with-nats-jetstream.sh --relay-service-checkpoint env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256=e59ba9f62bf126bccdf8c0d3331b54adae9e74f8fe1ee6e31d43e3dec9ca66b1 PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256=7ede69e3f0818508bfd0f4df21e0349a02b8a82739aa1845e76f2bd80f340b4e PHASE285_SERVICE_CHECKPOINT_TREE=bdfadfa1e6872596e2f37da4fe6cbebf66ee66d4 bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-relay-positive`; require exact selector-owned relay evidence, FQN 1/0/0/18, exit 0, cleanup, and identity. On green only, run once in order: `cargo test -p swarm-governance-witness --all-targets --locked --offline --no-run`; `cargo test -p swarm-governance --lib --locked --offline -- --test-threads=1`; `cargo clippy -p swarm-governance -p swarm-governance-witness --all-targets --all-features --locked --offline -- -D warnings`; `cargo fmt --all -- --check`; `bash -n tools/check-phase285-witness-conformance.sh tools/check-phase285-witness-integrity.sh tools/with-nats-jetstream.sh`; `shellcheck` those same three scripts; `git diff --cached --check "$BASE"`; terminal `verify_final11`. Any failure stops. Only all-green may run `env -u GIT_INDEX_FILE git read-tree bdfadfa1e6872596e2f37da4fe6cbebf66ee66d4` without `-u`, require the real index `write-tree` equal final11, exact eleven A2b2-relative cached paths/digest, clean cached diff check, and worktree/index equality; then create one direct-child commit with tree final11, clear status, and request immutable hostile production review. No production byte edit, replay retry, diagnostic rerun, new worktree, push before review, or A2c.</r63_no_edit_replay>
    <r62_causal_diagnostic>Do not execute before immutable r62 receives P0/P1/P2=`0/0/0`, is pushed/fetched exactly, and root records a fresh two-hour window. Run the exact r61 diagnostic once. Before it, the parser self-test must prove the no-follow predicate non-vacuously: after canonical regular-file lstat, a test-only pre-open hook rename-no-replace moves the original inode to an absent sibling and installs a relative symlink at the original path; canonical open with `O_NOFOLLOW` refuses exactly ELOOP; finally cleanup verifies/removes only that symlink, restores the original inode/path no-replace, and revalidates exact bytes/identity. The single-flag-deletion mutant follows the symlink, restoration occurs after its open, and every later r61 descriptor/record/process predicate must pass. Initial-lstat or unrelated failure does not count. Require equivalent canonical-refusal/single-predicate-deletion-late-pass proof for nlink-two and mode-widening. Preserve exact 25 parser/artifact kills plus hostile-index, all r61/r60 invocation/classification/cleanup/identity rules, and every no-production/no-retry/no-successor prohibition.</r62_causal_diagnostic>
    <r61_causal_diagnostic>Do not execute before immutable r61 receives P0/P1/P2=`0/0/0`, is pushed/fetched exactly, and root records a fresh two-hour window. Run the exact r60 clone/wrapper/sidecar invocation once, but validate the completed sidecar by one `O_RDONLY|O_NOFOLLOW|O_CLOEXEC` descriptor: matching initial lstat/fstat, coordinator owner, regular file, nlink 1, mode 0600, length 1..4096; read/hash/canonical-parse A; exact middle fstat; rewind; independently read/hash/canonical-parse B; exact final fstat and same-identity final lstat; require identical A/B bytes, hashes, projections, and all metadata snapshots before process classification. The self-test's only between-read hook performs a same-length same-inode mode-preserving rewrite between two distinct canonical account/token records. Require exactly twenty-five uniquely labelled parser/artifact controls killed: the r60 twenty-two with that strengthened rewrite plus drop-O_NOFOLLOW/symlink, nlink-two, and mode-widening; run the hostile ambient-index control separately. Preserve every r60 success/failure wrapper-token/sidecar/stdout/status/libtest/panic condition, transcript/sidecar hashing, validated cleanup, production recheck, one-run, no-fix, no-retry, no-relay-positive, no-A2c, and no-registered-worktree rule.</r61_causal_diagnostic>
    <r60_causal_diagnostic>Do not execute before immutable r60 receives P0/P1/P2=`0/0/0`, is pushed/fetched exactly, and root records a fresh two-hour window. Then perform r59's exact final11/A2b2/index/hash/ref/untracked/residue preflight and clone-index hostile control. Create one absent diagnostic sidecar path and one fixed-byte mode-0500 wrapper beneath the validated external root. Set the sidecar path explicitly, invoke the relay harness exact prefix with the wrapper followed by the unchanged Cargo/FQN/`--exact --nocapture` argv, and run once under the unchanged process-group bounds. The wrapper requires a relay token, emits exactly one `phase285_diagnostic_topology_token=` row, and execs Cargo. Clone-only test code create-new writes, syncs, and closes exactly one canonical mode-0600 sidecar before returning success or deliberately panicking; success emits no diagnostic stdout, while failure emits byte-identical stdout after sync and before panic. The supervisor validates the sidecar through one `O_NOFOLLOW` descriptor with before/after fstat and final same-device/inode lstat. Before the live run, require exactly twenty-two uniquely labelled parser/artifact mutants killed with zero survivors: r59's eleven record/process controls other than ambient wrong index plus missing-sidecar, pre-existing-sidecar, success-stdout-record, missing-wrapper-token, duplicate-wrapper-token, stale-wrapper-token, wrapper/record mismatch, out-of-window epoch, noncanonical sidecar, sidecar overflow, and sidecar-after-read mutation; run the hostile ambient-index control separately. For `not_reproduced`, require normal exit 0, literal exact FQN materialization, counts 1/0/0/18, no panic or diagnostic stdout, one sidecar, and fresh wrapper-token equality. For `failed`, require normal exit 101, counts 0/1/0/18, one sidecar, its identical stdout record before the exact deliberate inner panic, and only the exact outer panic afterward. Any other result is invalid. Hash the bounded transcript and sidecar into the planning ledger, clean only the validated root, reverify untouched production/index/ref/residue identities, and stop without retry, fix, relay-positive, production mutation, registered worktree, or A2c.</r60_causal_diagnostic>
    <r59_causal_diagnostic>Start in a coordinator shell with `unset GIT_INDEX_FILE`. Preflight accepted A2b2 refs/HEAD, exact final11 production alternate-index tree/path digest/checker/manifest/launcher/lib identities, empty real index, no untracked paths, and zero Docker residue by naming `/private/tmp/phase285-a2b3-r55-prospective.LrPTQI/index` explicitly where required. Create one validated external `mktemp -d`; hash a hostile ambient index; run `env -u GIT_INDEX_FILE git clone --shared --no-checkout`; define the canonical clone index as `$CLONE/.git/index`; run every clone Git command with `env GIT_INDEX_FILE=$CLONE_INDEX`; checkout A2b2; reset index/worktree to final11; require clone `write-tree`, zero working diff, and no untracked path. Under an ambient hostile index, invoke the exact wrapper that first unsets then pins clone index; require final11 clone tree and unchanged hostile/production index hashes. Use `apply_patch` on only clone `src/lib.rs`; require clone index final11 and exactly one unstaged path. Run the canonical-record/process validator self-test with exactly twelve killed controls: missing, extra, swapped account, stale tree, stale token, generic stage, invalid status, ambient wrong index, failed+0, not-reproduced+101, wrong-summary, signal-after-record. Run the r58 exact prefixed/`--nocapture` command once under its 300-second/4194304-byte process-group supervisor, with every subprocess pinned to `$CLONE_INDEX`; persist transcript outside clone. Accept `failed` only for normal unsignaled exit 101, exact FQN failed once, counts 0/1/0/18, one exact conditional record followed by exact deliberate inner and outer panics only. Accept `not_reproduced` only for normal exit 0, exact FQN ok once, counts 1/0/0/18, and its exact conditional record. Otherwise diagnosis is invalid. Remove only validated temporary root; reverify explicit production indexes/identities/refs/untracked and zero residue. Record one result and stop; no second run or mutation.</r59_causal_diagnostic>
    <r56_continuation_automated>set -e; IDX=/private/tmp/phase285-a2b3-r55-prospective.LrPTQI/index; FINAL9_IDX=/private/tmp/phase285-a2b3-final9.cPYgIr/index; BASE=30eb13c57a3dfdd54ae1eb9ee8fdd75a88067ef1; FINAL9=9657a2799126ce79d69185ad0139eac3c27cfb62; TREE=bdfadfa1e6872596e2f37da4fe6cbebf66ee66d4; PATH_DIGEST=4d9d95a9253af2cede7f42175322169a4deb3fafebc8fb0d58bcb845cd992adf; REPAIR_DIGEST=3ed67f9590ac9240b2318c84c3d1aa34e801331766aed59714828e60dcdd0595; CHECKER=1300a65a963e0831a5815c237ffdf0eba96db4e53b96caf2d85d0acb88fb5cd0; CHECKER_BLOB=4a507d8b4424a126d7fc754da339489f2ff4c6bb; MANIFEST=7ede69e3f0818508bfd0f4df21e0349a02b8a82739aa1845e76f2bd80f340b4e; MANIFEST_BLOB=422d42bf5ad9f8eae29b8e35a358ba49bbfc0b95; LAUNCHER=e59ba9f62bf126bccdf8c0d3331b54adae9e74f8fe1ee6e31d43e3dec9ca66b1; test -f "$IDX"; test -f "$FINAL9_IDX"; env -u GIT_INDEX_FILE git diff --cached --quiet; export GIT_INDEX_FILE="$IDX" PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$LAUNCHER" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$MANIFEST" PHASE285_SERVICE_CHECKPOINT_TREE="$TREE"; verify_final11() { local phase285_residue; test "$(git rev-parse HEAD)" = "$BASE"; test "$(git rev-parse refs/remotes/origin/work/v179-phase285-plan04-task03a2b2)" = "$BASE"; test "$(git rev-parse refs/remotes/origin/checkpoint/v179-phase285-plan04-task03a2b2-production)" = "$BASE"; test "$(git write-tree)" = "$TREE"; test "$(env GIT_INDEX_FILE="$FINAL9_IDX" git write-tree)" = "$FINAL9"; test "$(git diff --cached --name-only "$BASE" | wc -l | tr -d ' ')" = 11; test "$(git diff --cached --name-only "$BASE" | LC_ALL=C sort | shasum -a 256 | awk '{print $1}')" = "$PATH_DIGEST"; test "$(git diff-tree --no-commit-id --name-only -r "$FINAL9" "$TREE" | wc -l | tr -d ' ')" = 2; test "$(git diff-tree --no-commit-id --name-only -r "$FINAL9" "$TREE" | LC_ALL=C sort | shasum -a 256 | awk '{print $1}')" = "$REPAIR_DIGEST"; git diff --cached --check "$BASE"; git diff --exit-code; test -z "$(git ls-files --others --exclude-standard)"; test "$(shasum -a 256 tools/check-phase285-witness-conformance.sh | awk '{print $1}')" = "$CHECKER"; test "$(git hash-object tools/check-phase285-witness-conformance.sh)" = "$CHECKER_BLOB"; test "$(shasum -a 256 tools/fixtures/phase285-witness-integrity.json | awk '{print $1}')" = "$MANIFEST"; test "$(git hash-object tools/fixtures/phase285-witness-integrity.json)" = "$MANIFEST_BLOB"; test "$(shasum -a 256 tools/check-phase285-witness-integrity.sh | awk '{print $1}')" = "$LAUNCHER"; env -u GIT_INDEX_FILE git diff --cached --quiet; phase285_residue="$(DOCKER_CONTEXT=desktop-linux docker ps -aq --filter name=phase285-nats-)"; test -z "$phase285_residue"; }; verify_final11; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$LAUNCHER" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$MANIFEST" PHASE285_SERVICE_CHECKPOINT_TREE="$TREE" bash tools/check-phase285-witness-integrity.sh --self-test topology-owner-blocks; verify_final11; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$LAUNCHER" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$MANIFEST" PHASE285_SERVICE_CHECKPOINT_TREE="$TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-observations; verify_final11; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$LAUNCHER" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$MANIFEST" PHASE285_SERVICE_CHECKPOINT_TREE="$TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-deadline; verify_final11; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$LAUNCHER" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$MANIFEST" PHASE285_SERVICE_CHECKPOINT_TREE="$TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-grants; verify_final11; bash tools/with-nats-jetstream.sh --relay-service-checkpoint env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$LAUNCHER" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$MANIFEST" PHASE285_SERVICE_CHECKPOINT_TREE="$TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-relay-positive; verify_final11; cargo test -p swarm-governance-witness --all-targets --locked --offline --no-run; verify_final11; cargo test -p swarm-governance --lib --locked --offline -- --test-threads=1; verify_final11; cargo clippy -p swarm-governance -p swarm-governance-witness --all-targets --all-features --locked --offline -- -D warnings; verify_final11; cargo fmt --all -- --check; verify_final11; bash -n tools/check-phase285-witness-conformance.sh tools/check-phase285-witness-integrity.sh tools/with-nats-jetstream.sh; verify_final11; shellcheck tools/check-phase285-witness-conformance.sh tools/check-phase285-witness-integrity.sh tools/with-nats-jetstream.sh; verify_final11; git diff --cached --check "$BASE"; verify_final11</r56_continuation_automated>
    <automated>set -e; test -n "$PHASE285_PLAN04_TASK03A2B2_COMMIT"; test -n "$PHASE285_PLAN04_TASK03A2B3_COMMIT"; test -n "$PHASE285_PLAN04_TASK03A2B3_TREE"; test -n "$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256"; test -n "$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256"; test "$(git rev-parse refs/remotes/origin/work/v179-phase285-plan04-task03a2b2)" = "$PHASE285_PLAN04_TASK03A2B2_COMMIT"; test "$(git rev-parse refs/remotes/origin/checkpoint/v179-phase285-plan04-task03a2b2-production)" = "$PHASE285_PLAN04_TASK03A2B2_COMMIT"; test "$(git rev-parse HEAD)" = "$PHASE285_PLAN04_TASK03A2B3_COMMIT"; test "$(git rev-list --parents -n 1 "$PHASE285_PLAN04_TASK03A2B3_COMMIT" | wc -w | tr -d ' ')" = 2; test "$(git rev-parse "${PHASE285_PLAN04_TASK03A2B3_COMMIT}^")" = "$PHASE285_PLAN04_TASK03A2B2_COMMIT"; test "$(git rev-parse "$PHASE285_PLAN04_TASK03A2B3_COMMIT^{tree}")" = "$PHASE285_PLAN04_TASK03A2B3_TREE"; test -z "$(git status --short)"; git diff-tree --check "$PHASE285_PLAN04_TASK03A2B2_COMMIT" "$PHASE285_PLAN04_TASK03A2B3_COMMIT"; test "$(git diff-tree --no-commit-id --name-only -r "$PHASE285_PLAN04_TASK03A2B2_COMMIT" "$PHASE285_PLAN04_TASK03A2B3_COMMIT" | LC_ALL=C sort | shasum -a 256 | awk '{print $1}')" = 4d9d95a9253af2cede7f42175322169a4deb3fafebc8fb0d58bcb845cd992adf; test "$(git diff-tree --no-commit-id --name-only -r "$PHASE285_PLAN04_TASK03A2B2_COMMIT" "$PHASE285_PLAN04_TASK03A2B3_COMMIT" | wc -l | tr -d ' ')" = 11; test "$(git diff-tree --no-commit-id --name-only -r 5ade79141a2f8249ef084d24fcfd5ef12251e48e "$PHASE285_PLAN04_TASK03A2B3_COMMIT" | LC_ALL=C sort | shasum -a 256 | awk '{print $1}')" = 4e6629733654f7b4b6f1233bfaaef96a0fede0368620380c8b97f9d75bb18664; test "$(git diff-tree --no-commit-id --name-only -r 5ade79141a2f8249ef084d24fcfd5ef12251e48e "$PHASE285_PLAN04_TASK03A2B3_COMMIT" | wc -l | tr -d ' ')" = 14; test "$(git show "${PHASE285_PLAN04_TASK03A2B3_COMMIT}:tools/check-phase285-witness-integrity.sh" | shasum -a 256 | awk '{print $1}')" = "$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256"; test "$(git show "${PHASE285_PLAN04_TASK03A2B3_COMMIT}:tools/fixtures/phase285-witness-integrity.json" | shasum -a 256 | awk '{print $1}')" = "$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256"; bash tools/check-phase285-witness-integrity.sh --integrity-self-test; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A2B3_TREE" bash tools/check-phase285-witness-integrity.sh --self-test complete-receipt-suppression; cargo test -p swarm-governance-witness --test service_checkpoint --locked --offline topology_validator_binds_every_tuple_to_owner_block -- --exact --test-threads=1; bash tools/check-phase285-witness-integrity.sh --self-test topology-owner-blocks; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A2B3_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-observations; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A2B3_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-grants; bash tools/with-nats-jetstream.sh --relay-service-checkpoint env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A2B3_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-relay-positive; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A2B3_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-deadline; cargo test -p swarm-governance-witness --all-targets --locked --offline --no-run; cargo clippy -p swarm-governance -p swarm-governance-witness --all-targets --all-features --locked --offline -- -D warnings; cargo fmt --all -- --check; shellcheck tools/check-phase285-witness-conformance.sh tools/with-nats-jetstream.sh; git show --check --oneline "$PHASE285_PLAN04_TASK03A2B3_COMMIT"; test "$(git rev-parse HEAD)" = "$PHASE285_PLAN04_TASK03A2B3_COMMIT"; test "$(git rev-parse HEAD^{tree})" = "$PHASE285_PLAN04_TASK03A2B3_TREE"; test -z "$(git status --short)"</automated>
    Only immutable zero-finding review authorizes `work/v179-phase285-plan04-task03a2b3` and `checkpoint/v179-phase285-plan04-task03a2b3-production`. A2c remains blocked until both refs resolve exactly.
  </verify>
  <done>A2b3 banks the live relay/grant/timing integration while preserving accepted A2b1 and A2b2.</done>
</task>

<task type="auto">
  <wave>10</wave>
  <name>Task 285-04-03A2c: Bank the positive-first independent oracle and non-bypassable corpus</name>
  <files>crates/swarm-governance-witness/tests/service_checkpoint.rs, tools/check-phase285-witness-conformance.sh, tools/fixtures/phase285-witness-integrity.json, tools/with-nats-jetstream.sh</files>
  <read_first>The accepted immutable A2b3 commit and freeze; the rejected `958fe44` object and review findings 3 and 6; .planning/phases/285-assurance-foundation-closure/285-04-03A-R11-DESIGN.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; all four owned paths</read_first>
  <r64_precedence>A2c may resume only after fresh-target A2b3 reacceptance and a separate root window. Preserve the genuine RED and exact three-path partial candidate bytes solely as implementation input, not evidence. Positive live production and every Cargo child must use correctly pinned candidate tree and initially absent target provenance. Replace the four artifact-only reservation rows with four compiled coherent source mutants of the actual complete-receipt capacity-one/disposition sequence, each in its own initially absent target with source/executable hashes and the same internal real seam plus external validator: unit-token reservation without receipt transfer; receipt construction/enqueue only after the suppress decision; capacity greater than one or unbounded reservation accepted; and suppression after missing/full/closed/failed reservation. The remaining 34 rows may mutate the one immutable live ledger but each executes the real integration oracle. Require positive first, exact inventory 4 compiled + 34 artifact = 38, unique/executed/killed 38, vacuous 0. Shared target reuse, mtime sleeps, commit-as-tree pins, direct-error/compile-failure/timeout/zero-test/lexical/helper evidence remain forbidden. Scope stays four paths/digest `8b37d06097626b9d3f302ea1053ced7852e5553f095c8ead99964c8bca66b6c5`; one direct-child commit and immutable review/push still gate A3.</r64_precedence>
  <action>Start only after both A2b3 origin refs resolve exactly to `PHASE285_PLAN04_TASK03A2B3_COMMIT`; reconstruct from accepted A2b3 in the same worktree. Add exact red test `service_checkpoint_oracle_positive_and_mutation_inventory_are_non_bypassable`. It must prove the rejected oracle's positive row fails its own predicate and that ambient child/family controls can bypass or narrow its corpus. Implement no production behavior. The checker first independently decodes complete canonical public requests, every private request/response, and both public responses; recomputes request nonce/digest, typed operation, all top-level canonical hashes, signatures, store state/generation, A2a event/store/publisher/connection cross-bindings, exact first/replay `ReadHead` equality, disjoint relay identity sets, and real causal order. The immutable positive ledger must pass the same ordinary predicate before any mutant executes; a mutant result is invalid if positive validation did not execute and pass.

The accepted top-level `--focused-service-checkpoint-relay` must reject ambient `PHASE285_A2_MUTATION_CHILD`, `PHASE285_A2_MUTATION_FAMILY`, or any other corpus-selection variable and unconditionally execute the complete static inventory. Internal real-target children use a distinct checker selector created only by the parent and cannot dispatch or reduce the parent inventory. Freeze exactly these 38 named coherent controls: malformed suppressed response; unit-token receipt reservation; post-suppression receipt construction; unbounded receipt reservation; suppress-on-reservation-failure; same public relay instance; same private relay instance; synthetic worker event; synthetic proxy event; synthetic store event; synthetic connection ID; literal count; raw runtime JS; raw runtime KV; raw relay JS; raw relay KV; raw relay signing; cross-account import; import-to swap; export-account swap; principal-publish swap; principal-subscribe swap; post-expiry second publish; grant-role swap; grant-account swap; grant-user swap; grant-event swap; canonical-request-digest swap; canonical-private-response swap; canonical-public-response swap; typed-operation swap; first/replay-response swap; causal-order swap; positive-oracle bypass; corpus skip; corpus family narrowing; coherent synthetic-identity substitution; and coherent synthetic-count substitution. Each child must execute a real positive Cargo/NATS target, reach the ordinary oracle, fail only its named relation with a stable reason, and report real_target=1, executed=1, killed=1, vacuous=0. The four reservation controls must mutate the real capacity-one complete-receipt boundary: substitute a unit token, construct/enqueue only after suppression, use capacity greater than one or an unbounded channel, and suppress after a full/closed/missing/partial/failed reservation. Direct expected-error branches, compile failure, timeout, zero tests, generic failure, helper-authored ledgers, and lexical-only mutants are forbidden. The checker freezes inventory name/order/digest, proves omission/addition/duplication/substitution mutations against that registry, and reports exactly 38/38 killed with vacuous=0. The exact four-path slice digest is `8b37d06097626b9d3f302ea1053ced7852e5553f095c8ead99964c8bca66b6c5`; cumulative A1+A2a+A2b+A2c union remains exactly fourteen paths/digest `4e6629733654f7b4b6f1233bfaaef96a0fede0368620380c8b97f9d75bb18664`. Freeze exactly one direct-child A2c commit with no post-freeze edit.</action>
  <acceptance_criteria>The red test proves both positive-oracle invalidity and ambient corpus bypass before implementation. The immutable positive ledger passes the exact independent predicate first. Every canonical/typed/event/identity/authority/grant/causal relation is independently recomputed. The top-level gate rejects ambient selection controls and always runs the frozen 38-name inventory; all 38 coherent real-target mutants, including the four complete-receipt reservation-boundary controls, are killed at their intended stable reason with vacuous=0. A2a observations, A2b grants/positive relay, and A1 deadline remain independent green invocations. Exact scope, strict validation, immutable 0/0/0 review, and pushed A2c refs are required before A3.</acceptance_criteria>
  <verify>
    <automated>set -e; test -n "$PHASE285_PLAN04_TASK03A2B3_COMMIT"; test -n "$PHASE285_PLAN04_TASK03A2C_COMMIT"; test -n "$PHASE285_PLAN04_TASK03A2C_TREE"; test -n "$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256"; test -n "$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256"; test "$(git rev-parse refs/remotes/origin/work/v179-phase285-plan04-task03a2b3)" = "$PHASE285_PLAN04_TASK03A2B3_COMMIT"; test "$(git rev-parse refs/remotes/origin/checkpoint/v179-phase285-plan04-task03a2b3-production)" = "$PHASE285_PLAN04_TASK03A2B3_COMMIT"; test "$(git rev-parse HEAD)" = "$PHASE285_PLAN04_TASK03A2C_COMMIT"; test "$(git rev-list --parents -n 1 "$PHASE285_PLAN04_TASK03A2C_COMMIT" | wc -w | tr -d ' ')" = 2; test "$(git rev-parse "${PHASE285_PLAN04_TASK03A2C_COMMIT}^")" = "$PHASE285_PLAN04_TASK03A2B3_COMMIT"; test "$(git rev-parse "$PHASE285_PLAN04_TASK03A2C_COMMIT^{tree}")" = "$PHASE285_PLAN04_TASK03A2C_TREE"; test -z "$(git status --short)"; git diff-tree --check "$PHASE285_PLAN04_TASK03A2B3_COMMIT" "$PHASE285_PLAN04_TASK03A2C_COMMIT"; test "$(git diff-tree --no-commit-id --name-only -r "$PHASE285_PLAN04_TASK03A2B3_COMMIT" "$PHASE285_PLAN04_TASK03A2C_COMMIT" | LC_ALL=C sort | shasum -a 256 | awk '{print $1}')" = 8b37d06097626b9d3f302ea1053ced7852e5553f095c8ead99964c8bca66b6c5; test "$(git diff-tree --no-commit-id --name-only -r "$PHASE285_PLAN04_TASK03A2B3_COMMIT" "$PHASE285_PLAN04_TASK03A2C_COMMIT" | wc -l | tr -d ' ')" = 4; test "$(git diff-tree --no-commit-id --name-only -r 5ade79141a2f8249ef084d24fcfd5ef12251e48e "$PHASE285_PLAN04_TASK03A2C_COMMIT" | LC_ALL=C sort | shasum -a 256 | awk '{print $1}')" = 4e6629733654f7b4b6f1233bfaaef96a0fede0368620380c8b97f9d75bb18664; test "$(git diff-tree --no-commit-id --name-only -r 5ade79141a2f8249ef084d24fcfd5ef12251e48e "$PHASE285_PLAN04_TASK03A2C_COMMIT" | wc -l | tr -d ' ')" = 14; test "$(git show "${PHASE285_PLAN04_TASK03A2C_COMMIT}:tools/check-phase285-witness-integrity.sh" | shasum -a 256 | awk '{print $1}')" = "$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256"; test "$(git show "${PHASE285_PLAN04_TASK03A2C_COMMIT}:tools/fixtures/phase285-witness-integrity.json" | shasum -a 256 | awk '{print $1}')" = "$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256"; bash tools/check-phase285-witness-conformance.sh --self-test store-proxy-source; cargo test -p swarm-governance-witness --test service_checkpoint --locked --offline service_checkpoint_oracle_positive_and_mutation_inventory_are_non_bypassable -- --exact --test-threads=1; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A2C_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-observations; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A2C_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-grants; bash tools/with-nats-jetstream.sh --relay-service-checkpoint env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A2C_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-relay; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A2C_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-deadline; cargo test -p swarm-governance --lib --locked --offline -- --test-threads=1; cargo test -p swarm-governance-witness --all-targets --locked --offline --no-run; cargo clippy -p swarm-governance -p swarm-governance-witness --all-targets --all-features --locked --offline -- -D warnings; cargo fmt --all -- --check; shellcheck tools/check-phase285-witness-conformance.sh tools/with-nats-jetstream.sh; git show --check --oneline "$PHASE285_PLAN04_TASK03A2C_COMMIT"; test -z "$(git status --short)"</automated>
    Root freezes exact A2c identities before immutable review. Only P0/P1/P2=0/0/0 authorizes pushing `work/v179-phase285-plan04-task03a2c` and `checkpoint/v179-phase285-plan04-task03a2c-production`. A3 remains blocked until both origin refs resolve to that commit.
  </verify>
  <done>Task 285-04-03A2c banks the final authenticated relay assurance oracle while preserving A1+A2a+A2b1+A2b2+A2b3. It makes no recovery or abort claim.</done>
</task>

<task type="auto">
  <wave>11</wave>
  <name>Task 285-04-03A3: Execute the physical recovery and abort matrix</name>
  <files>crates/swarm-governance-witness/tests/service_checkpoint.rs, crates/swarm-governance/src/persistence_protocol.rs, crates/swarm-governance/src/persistence_protocol/tests.rs, tools/check-phase285-witness-conformance.sh, tools/fixtures/phase285-witness-integrity.json, tools/with-nats-jetstream.sh</files>
  <read_first>The accepted immutable Task 04-03A2c production commit and freeze; .planning/phases/285-assurance-foundation-closure/285-04-03A-R11-DESIGN.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; all six owned paths; the accepted A1 deadline, A2a observation, A2b grant/positive-relay, and A2c independent-oracle evidence</read_first>
  <action>Continue in the same implementation worktree only after PHASE285_PLAN04_TASK03A2C_COMMIT identifies the accepted final A2 checkpoint and both A2c origin work/checkpoint remote-tracking refs resolve to it. Start with the exact red test service_checkpoint_recovers_every_operation_after_restart_or_lost_response. Execute eighteen physically distinct rows: restart and lost-response variants for each of the nine typed operations. Every restart row must observe the first reply, close the original witness and store runners/connections, start fresh identities, and replay through the accepted authenticated public/private relay. Every lost-response row must prove service execution from the accepted A2a worker/proxy/store/publisher/connection events, suppress only the accepted A2b3 complete canonical receipt, observe requester timeout with zero retry, and then replay through both fresh relay legs. Bind exact request, response, backend revision, terminal store state, runner/connection identity, and first/replay CAS counts in one canonical ledger; reject copied, synthesized, cross-row, pre-response, fabricated, or delivered-reply evidence through the accepted A2c positive-first oracle. Add coherent abort controls that rebuild and re-sign current, overshoot, wrong-sequence, correct-sequence/wrong-txid, max-sequence overflow, and exact-replay candidates. Overshoot and wrong-sequence must reach and fail only the successor predicate; correct-sequence/wrong-txid must pass intent-abort validation and fail prepared/request binding; deleting either relation must fail. Re-run A1, A2a, A2b1, A2b2, A2b3, and A2c as separate gates. The exact six-path slice digest is 769825832abfff2e80561b7c4692a0aec750699096f094e3ab5c54765726d4ae; the cumulative final union is the existing exact sixteen paths with digest 1decd3b39a6433d9358a10289d6886f67211b2560563b417381273dd1662d614. Freeze exactly one local A3 direct-child commit and make no post-freeze edit.</action>
  <acceptance_criteria>The recovery red control fails before implementation and passes afterward. On the immutable A3 final tree, after all predecessor/candidate pin and launcher/manifest hash checks and before Cargo or NATS, the source preflight reports one positive and 44 unique mutations. The focused recovery checker reports exactly 18/18 physically distinct rows with failed=0 and ignored=0. All nine operations have one real restart and one real lost-response execution with exact identities and terminal equality; first/replay proxy traces prove one then zero CAS. Every copied/synthetic/receipt/publication/identity/CAS/abort mutant fails at its intended late predicate. Accepted A1, A2a, A2b, and A2c evidence remains green as independent invocations. The slice/cumulative digests, strict validation, and immutable review must be P0/P1/P2=0/0/0.</acceptance_criteria>
  <verify>
    <automated>set -e; test -n "$PHASE285_PLAN04_TASK03A2C_COMMIT"; test -n "$PHASE285_PLAN04_TASK03A3_COMMIT"; test -n "$PHASE285_PLAN04_TASK03A3_TREE"; test -n "$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256"; test -n "$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256"; test "$(git rev-parse refs/remotes/origin/work/v179-phase285-plan04-task03a2c)" = "$PHASE285_PLAN04_TASK03A2C_COMMIT"; test "$(git rev-parse refs/remotes/origin/checkpoint/v179-phase285-plan04-task03a2c-production)" = "$PHASE285_PLAN04_TASK03A2C_COMMIT"; test "$(git rev-parse HEAD)" = "$PHASE285_PLAN04_TASK03A3_COMMIT"; test "$(git rev-list --parents -n 1 "$PHASE285_PLAN04_TASK03A3_COMMIT" | wc -w | tr -d ' ')" = 2; test "$(git rev-parse "${PHASE285_PLAN04_TASK03A3_COMMIT}^")" = "$PHASE285_PLAN04_TASK03A2C_COMMIT"; test "$(git rev-parse "$PHASE285_PLAN04_TASK03A3_COMMIT^{tree}")" = "$PHASE285_PLAN04_TASK03A3_TREE"; test -z "$(git status --short)"; git diff-tree --check "$PHASE285_PLAN04_TASK03A2C_COMMIT" "$PHASE285_PLAN04_TASK03A3_COMMIT"; test "$(git diff-tree --no-commit-id --name-only -r "$PHASE285_PLAN04_TASK03A2C_COMMIT" "$PHASE285_PLAN04_TASK03A3_COMMIT" | LC_ALL=C sort | shasum -a 256 | awk '{print $1}')" = 769825832abfff2e80561b7c4692a0aec750699096f094e3ab5c54765726d4ae; test "$(git diff-tree --no-commit-id --name-only -r "$PHASE285_PLAN04_TASK03A2C_COMMIT" "$PHASE285_PLAN04_TASK03A3_COMMIT" | wc -l | tr -d ' ')" = 6; test "$(git diff-tree --no-commit-id --name-only -r 5ade79141a2f8249ef084d24fcfd5ef12251e48e "$PHASE285_PLAN04_TASK03A3_COMMIT" | LC_ALL=C sort | shasum -a 256 | awk '{print $1}')" = 1decd3b39a6433d9358a10289d6886f67211b2560563b417381273dd1662d614; test "$(git diff-tree --no-commit-id --name-only -r 5ade79141a2f8249ef084d24fcfd5ef12251e48e "$PHASE285_PLAN04_TASK03A3_COMMIT" | wc -l | tr -d ' ')" = 16; test "$(git show "${PHASE285_PLAN04_TASK03A3_COMMIT}:tools/check-phase285-witness-integrity.sh" | shasum -a 256 | awk '{print $1}')" = "$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256"; test "$(git show "${PHASE285_PLAN04_TASK03A3_COMMIT}:tools/fixtures/phase285-witness-integrity.json" | shasum -a 256 | awk '{print $1}')" = "$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256"; bash tools/check-phase285-witness-conformance.sh --self-test store-proxy-source; cargo test -p swarm-governance-witness --test service_checkpoint --locked --offline service_checkpoint_recovers_every_operation_after_restart_or_lost_response -- --exact --test-threads=1; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A3_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-observations; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A3_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-grants; bash tools/with-nats-jetstream.sh --relay-service-checkpoint env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A3_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-recovery; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A3_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-deadline; bash tools/with-nats-jetstream.sh --relay-service-checkpoint env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03A3_TREE" bash tools/check-phase285-witness-integrity.sh --focused-service-checkpoint-relay; cargo test -p swarm-governance --lib --locked --offline -- --test-threads=1; cargo test -p swarm-governance-witness --all-targets --locked --offline --no-run; cargo clippy -p swarm-governance -p swarm-governance-witness --all-targets --all-features --locked --offline -- -D warnings; cargo fmt --all -- --check; shellcheck tools/check-phase285-witness-conformance.sh tools/with-nats-jetstream.sh; git show --check --oneline "$PHASE285_PLAN04_TASK03A3_COMMIT"; test -z "$(git status --short)"</automated>
    Root must freeze the exact local A3 production commit, commit tree, launcher hash, and manifest hash before review. Independent review audits that immutable commit and its tree-bound evidence. Only after P0/P1/P2=0/0/0 may root push work/v179-phase285-plan04-task03a3 and checkpoint/v179-phase285-plan04-task03a3-production. Task 04-03B becomes eligible only after both origin remote-tracking refs resolve to the reviewed commit.
  </verify>
  <done>Task 285-04-03A3 banks the eighteen-row recovery/abort matrix while preserving A1+A2a+A2b1+A2b2+A2b3+A2c. It does not claim 03B bounds, retention, initialization, or final Plan 04 acceptance.</done>
</task>

<task type="auto">
  <wave>12</wave>
  <name>Task 285-04-03B: Complete service checkpoint bounds, retention, and final pin</name>
  <files>crates/swarm-governance-witness/Cargo.toml, crates/swarm-governance-witness/src/lib.rs, crates/swarm-governance-witness/src/raw_config.rs, crates/swarm-governance-witness/src/runtime_client.rs, crates/swarm-governance-witness/src/service_config.rs, crates/swarm-governance-witness/tests/full_service_path.rs, crates/swarm-governance-witness/src/bin/swarm-governance-witness.rs, crates/swarm-governance-witness/src/bin/swarm-governance-witness-store.rs, crates/swarm-governance-witness/src/bin/swarm-governance-witness-init.rs, crates/swarm-governance-witness/tests/service_checkpoint.rs, tools/check-phase285-witness-conformance.sh, tools/check-witness-dependency-closure.sh, tools/fixtures/phase285-witness-integrity.json</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-04-03A-FREEZE.md (root-authored accepted immutable production commit/tree and exact live evidence); .planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md; .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance-witness/Cargo.toml; crates/swarm-governance-witness/src/lib.rs; crates/swarm-governance-witness/src/raw_config.rs; crates/swarm-governance-witness/src/runtime_client.rs; crates/swarm-governance-witness/src/service_config.rs; crates/swarm-governance-witness/tests/full_service_path.rs; crates/swarm-governance-witness/tests/service_checkpoint.rs; tools/check-phase285-witness-conformance.sh; tools/check-witness-dependency-closure.sh; tools/check-phase285-witness-integrity.sh; tools/fixtures/phase285-witness-integrity.json</read_first>
  <action>Build only on the accepted A3 checkpoint after both `origin/work/v179-phase285-plan04-task03a3` and `origin/checkpoint/v179-phase285-plan04-task03a3-production` resolve exactly to `PHASE285_PLAN04_TASK03A3_COMMIT`. Finish exactly the fifteen literal service_checkpoint_* cases already frozen in the unchanged 58-row registry; do not register, rename, omit, or replace them. Execute all 6 read_classification rows, retain the accepted 18 restart_lost_response rows, execute max and max-plus-one for all 33 bounds as exactly 66 cells, and execute all 9 rotation_retention rows. Every inner set and mutation follows the frozen ledger contract and must be materialized rather than inferred. Implement signed presence for current, predecessor, and live-prepared reads; request-bound signed absence for aborted and evicted txids; and unsigned typed stale-replica refusal. Complete RuntimeWitnessClient nested attestation/failure verification for every trait method and keep the accepted deadline state machine byte-for-byte unless a new RED forces another reviewed amendment. Complete one-shot initialization in the exact order: create/open fixed stream, authenticate complete fixed-key/progress reread, CAS Ready or authenticate an already-Ready resume, make one fresh leader-mediated Stream.Info request, invoke exactly one crate-private inspection seam on that fresh response, derive/retain its complete canonical bytes and digest, then construct/sign the epoch-bound anchor and exit without serving. Reject absent/revision-zero initialization, tombstone, interrupted resume, changed key, Initializing, Ready-CAS crash, pre-Ready/cached/duplicate/missing seam calls, public seam visibility, serving-path use, duplicate JSON parsing, expected-configuration bypass, precomputed anchor inputs, and mutated creation/configuration/messages/sequence/subject-count evidence; prove byte-identical anchor re-emission. Positively compile the library and three exact binaries and reject forbidden normal/reverse dependency edges. Preserve the launcher byte-for-byte, refresh only the manifest checker hash, retain the exact 58 tuples/digest, and kill every inner/top-level mutant. Freeze exactly one single-parent 04-03B commit whose direct parent is accepted A3, make no post-freeze edit, and obtain the separate planning-object pin for that immutable commit/tree before final selector execution.</action>
  <acceptance_criteria>The checker reports exactly 15/15 top-level, 6/6 reads, the already accepted 18/18 restart/lost-response rows, 66/66 boundary cells, and 9/9 rotation/retention rows with failed=0 and ignored=0; all read, bound, retention, initializer, binary, runtime-adapter, and dependency mutations fail non-vacuously; the 04-03A deadline/recovery guarantees remain green; one library plus three exact bins compile under dependency closure; the launcher and 58-row registry digest are unchanged; only the reviewed manifest hash changes; the immutable direct-child 03B commit receives independent P0/P1/P2=0/0/0 before either remote ref is pushed.</acceptance_criteria>
  <verify>
    <automated>set -e; test -n "$PHASE285_PLAN04_TASK03A3_COMMIT"; test -n "$PHASE285_PLAN04_TASK03B_COMMIT"; test -n "$PHASE285_PLAN04_TASK03B_TREE"; test -n "$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256"; test -n "$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256"; test "$(git rev-parse refs/remotes/origin/work/v179-phase285-plan04-task03a3)" = "$PHASE285_PLAN04_TASK03A3_COMMIT"; test "$(git rev-parse refs/remotes/origin/checkpoint/v179-phase285-plan04-task03a3-production)" = "$PHASE285_PLAN04_TASK03A3_COMMIT"; test "$(git rev-parse HEAD)" = "$PHASE285_PLAN04_TASK03B_COMMIT"; test "$(git rev-list --parents -n 1 "$PHASE285_PLAN04_TASK03B_COMMIT" | wc -w | tr -d ' ')" = 2; test "$(git rev-parse "${PHASE285_PLAN04_TASK03B_COMMIT}^")" = "$PHASE285_PLAN04_TASK03A3_COMMIT"; test "$(git rev-parse "$PHASE285_PLAN04_TASK03B_COMMIT^{tree}")" = "$PHASE285_PLAN04_TASK03B_TREE"; test -z "$(git status --short)"; git diff-tree --check "$PHASE285_PLAN04_TASK03A3_COMMIT" "$PHASE285_PLAN04_TASK03B_COMMIT"; test "$(git diff-tree --no-commit-id --name-only -r 8abe28dbc42c444643ea473614bee7a8cf912b8b "$PHASE285_PLAN04_TASK03B_COMMIT" | LC_ALL=C sort | shasum -a 256 | awk '{print $1}')" = 0140b2810cb3fea86b44d003d3593d7164ad33c4d33f9f1e8f9e82b0c15315b7; test "$(git diff-tree --no-commit-id --name-only -r 8abe28dbc42c444643ea473614bee7a8cf912b8b "$PHASE285_PLAN04_TASK03B_COMMIT" | wc -l | tr -d ' ')" = 22; test "$(git show "${PHASE285_PLAN04_TASK03B_COMMIT}:tools/check-phase285-witness-integrity.sh" | shasum -a 256 | awk '{print $1}')" = "$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256"; test "$(git show "${PHASE285_PLAN04_TASK03B_COMMIT}:tools/fixtures/phase285-witness-integrity.json" | shasum -a 256 | awk '{print $1}')" = "$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256"; bash tools/check-phase285-witness-integrity.sh --integrity-self-test; bash tools/check-phase285-witness-integrity.sh --self-test jetstream-release-hook; bash tools/check-phase285-witness-integrity.sh response-failure-wire; bash tools/check-phase285-witness-integrity.sh candidate-verifier; bash tools/check-phase285-witness-integrity.sh protocol-checkpoint; bash tools/check-phase285-witness-integrity.sh atomic-store-contract; bash tools/check-phase285-witness-integrity.sh in-memory-differential; bash tools/check-phase285-witness-integrity.sh typed-proxy; bash tools/check-phase285-witness-integrity.sh transport-layering; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" bash tools/check-phase285-witness-integrity.sh jetstream-cas; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" bash tools/check-phase285-witness-integrity.sh jetstream-checkpoint; bash tools/check-phase285-witness-integrity.sh public-dispatcher; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" bash tools/check-phase285-witness-integrity.sh full-service-path; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03B_TREE" bash tools/check-phase285-witness-integrity.sh service-checkpoint; bash tools/with-nats-jetstream.sh --self-test; cargo test -p swarm-governance --locked --offline; bash tools/with-nats-jetstream.sh cargo test -p swarm-governance-witness --locked --offline; bash tools/check-witness-dependency-closure.sh --all-targets; cargo clippy -p swarm-governance -p swarm-governance-witness --all-targets --all-features --locked --offline -- -D warnings; cargo fmt --all -- --check; shellcheck tools/check-phase285-witness-conformance.sh tools/check-phase285-witness-integrity.sh tools/check-witness-dependency-closure.sh tools/with-nats-jetstream.sh; actionlint .github/workflows/ci.yml; git show --check --oneline "$PHASE285_PLAN04_TASK03B_COMMIT"; test -z "$(git status --short)"</automated>
    The supplied hashes must equal the separately reviewed full-tree pin. The checker must show the exact cumulative row counts above and dependency closure must positively compile one library plus three exact bins. Independent immutable task-level P0/P1/P2=`0/0/0` review alone authorizes pushing `work/v179-phase285-plan04-task03b` and `checkpoint/v179-phase285-plan04-task03b-production`. Those refs bank 03B but do not complete Plan 04 or authorize Plan 05A; both remain blocked until the separate post-push outer cumulative review returns `0/0/0`.
  </verify>
  <done>Task 285-04-03B closes the service-layer checkpoint and provides the only runtime witness implementation consumed by Plan 05. External GitHub App protection remains explicitly deferred.</done>
</task>

</tasks>

<verification>
After the immutable 04-03B commit passes its task-level P0/P1/P2 = `0/0/0` review and both 03B remote refs are pushed, publish the final Plan 04 planning object containing the exact candidate commit/tree, accepted A3 parent, unchanged launcher SHA-256, and refreshed manifest SHA-256. Supply only those pinned values to the launcher and run the outer cumulative review against the now-remotely-banked object. Run `--integrity-self-test`, `--self-test jetstream-release-hook`, then the literal inherited selectors `response-failure-wire`, `candidate-verifier`, `protocol-checkpoint`, `atomic-store-contract`, `in-memory-differential`, `typed-proxy`, `transport-layering`, `jetstream-cas`, and `jetstream-checkpoint`, followed by `public-dispatcher`, `full-service-path`, and `service-checkpoint`; every authorization-critical checker call goes through the launcher and NATS selectors go through the owned harness. Require the exact unchanged 58-row registry digest. Then run `cargo test -p swarm-governance --locked --offline`, harnessed `cargo test -p swarm-governance-witness --locked --offline`, `bash tools/check-witness-dependency-closure.sh --all-targets`, strict clippy, formatting, shellcheck, actionlint, immutable commit/diff checks, exact twenty-two-path cumulative scope verification, temp/container cleanup, and final zero-dirty identity. This separate post-push outer review must return P0/P1/P2 = `0/0/0` before Plan 04 is marked complete or Plan 05A is authorized. A failed outer review leaves the reviewed 03B checkpoint banked but blocks advancement. Treat this as a local slice checkpoint only; keep Phases 286-289 blocked.
  <automated>set -e; test -n "$PHASE285_PLAN04_TASK03A3_COMMIT"; test -n "$PHASE285_PLAN04_TASK03B_COMMIT"; test -n "$PHASE285_PLAN04_TASK03B_TREE"; test -n "$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256"; test -n "$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256"; test "$(git rev-parse refs/remotes/origin/work/v179-phase285-plan04-task03a3)" = "$PHASE285_PLAN04_TASK03A3_COMMIT"; test "$(git rev-parse refs/remotes/origin/checkpoint/v179-phase285-plan04-task03a3-production)" = "$PHASE285_PLAN04_TASK03A3_COMMIT"; test "$(git rev-parse refs/remotes/origin/work/v179-phase285-plan04-task03b)" = "$PHASE285_PLAN04_TASK03B_COMMIT"; test "$(git rev-parse refs/remotes/origin/checkpoint/v179-phase285-plan04-task03b-production)" = "$PHASE285_PLAN04_TASK03B_COMMIT"; test "$(git rev-parse HEAD)" = "$PHASE285_PLAN04_TASK03B_COMMIT"; test "$(git rev-list --parents -n 1 "$PHASE285_PLAN04_TASK03B_COMMIT" | wc -w | tr -d ' ')" = 2; test "$(git rev-parse "${PHASE285_PLAN04_TASK03B_COMMIT}^")" = "$PHASE285_PLAN04_TASK03A3_COMMIT"; test "$(git rev-parse "$PHASE285_PLAN04_TASK03B_COMMIT^{tree}")" = "$PHASE285_PLAN04_TASK03B_TREE"; test -z "$(git status --short)"; git diff-tree --check "$PHASE285_PLAN04_TASK03A3_COMMIT" "$PHASE285_PLAN04_TASK03B_COMMIT"; test "$(git diff-tree --no-commit-id --name-only -r 8abe28dbc42c444643ea473614bee7a8cf912b8b "$PHASE285_PLAN04_TASK03B_COMMIT" | LC_ALL=C sort | shasum -a 256 | awk '{print $1}')" = 0140b2810cb3fea86b44d003d3593d7164ad33c4d33f9f1e8f9e82b0c15315b7; test "$(git diff-tree --no-commit-id --name-only -r 8abe28dbc42c444643ea473614bee7a8cf912b8b "$PHASE285_PLAN04_TASK03B_COMMIT" | wc -l | tr -d ' ')" = 22; test "$(git show "${PHASE285_PLAN04_TASK03B_COMMIT}:tools/check-phase285-witness-integrity.sh" | shasum -a 256 | awk '{print $1}')" = "$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256"; test "$(git show "${PHASE285_PLAN04_TASK03B_COMMIT}:tools/fixtures/phase285-witness-integrity.json" | shasum -a 256 | awk '{print $1}')" = "$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256"; bash tools/check-phase285-witness-integrity.sh --integrity-self-test; bash tools/check-phase285-witness-integrity.sh --self-test jetstream-release-hook; bash tools/check-phase285-witness-integrity.sh response-failure-wire; bash tools/check-phase285-witness-integrity.sh candidate-verifier; bash tools/check-phase285-witness-integrity.sh protocol-checkpoint; bash tools/check-phase285-witness-integrity.sh atomic-store-contract; bash tools/check-phase285-witness-integrity.sh in-memory-differential; bash tools/check-phase285-witness-integrity.sh typed-proxy; bash tools/check-phase285-witness-integrity.sh transport-layering; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" bash tools/check-phase285-witness-integrity.sh jetstream-cas; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" bash tools/check-phase285-witness-integrity.sh jetstream-checkpoint; bash tools/check-phase285-witness-integrity.sh public-dispatcher; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" bash tools/check-phase285-witness-integrity.sh full-service-path; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" PHASE285_SERVICE_CHECKPOINT_TREE="$PHASE285_PLAN04_TASK03B_TREE" bash tools/check-phase285-witness-integrity.sh service-checkpoint; bash tools/with-nats-jetstream.sh --self-test; cargo test -p swarm-governance --locked --offline; bash tools/with-nats-jetstream.sh cargo test -p swarm-governance-witness --locked --offline; bash tools/check-witness-dependency-closure.sh --all-targets; cargo clippy -p swarm-governance -p swarm-governance-witness --all-targets --all-features --locked --offline -- -D warnings; cargo fmt --all -- --check; shellcheck tools/check-phase285-witness-conformance.sh tools/check-phase285-witness-integrity.sh tools/check-witness-dependency-closure.sh tools/with-nats-jetstream.sh; actionlint .github/workflows/ci.yml; git show --check --oneline "$PHASE285_PLAN04_TASK03B_COMMIT"; test -z "$(git status --short)"</automated>
</verification>

<success_criteria>
Public-dispatcher, full-service-path, and service-checkpoint lanes are non-vacuously green; only the private store process can access raw JetStream; runtime receives typed attested outcomes via `GovernanceDurabilityWitness`; no local/optional/unsigned fallback exists in enforced service mode; and no external GitHub App check is claimed.
</success_criteria>

<output>
After completion, create `.planning/phases/285-assurance-foundation-closure/285-04-SUMMARY.md`.
</output>
