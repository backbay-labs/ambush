---
phase: 285-assurance-foundation-closure
plan: "04"
type: execute
wave: 5
depends_on:
  - "285-03B"
files_modified:
  - crates/swarm-governance-witness/Cargo.toml
  - crates/swarm-governance-witness/src/lib.rs
  - crates/swarm-governance-witness/src/raw_config.rs
  - crates/swarm-governance-witness/src/public_dispatcher.rs
  - crates/swarm-governance-witness/src/store_proxy_service.rs
  - crates/swarm-governance-witness/src/runtime_client.rs
  - crates/swarm-governance-witness/src/service_config.rs
  - crates/swarm-governance-witness/src/bin/swarm-governance-witness.rs
  - crates/swarm-governance-witness/src/bin/swarm-governance-witness-store.rs
  - crates/swarm-governance-witness/src/bin/swarm-governance-witness-init.rs
  - crates/swarm-governance-witness/tests/full_service_path.rs
  - crates/swarm-governance-witness/tests/service_checkpoint.rs
  - crates/swarm-governance/src/persistence_protocol.rs
  - crates/swarm-governance/src/witness_candidate_verifier.rs
  - crates/swarm-governance/src/witness_service.rs
  - crates/swarm-governance/tests/phase285_witness_conformance.rs
  - tools/check-phase285-witness-conformance.sh
  - tools/check-witness-dependency-closure.sh
  - tools/fixtures/phase285-witness-integrity.json
  - tools/with-nats-jetstream.sh
  - docker-compose.yml
autonomous: true
requirements:
  - ASSURE-04
  - ASSURE-06
must_haves:
  truths:
    - "A runtime request reaches exactly one typed public-witness operation, and a success response is signed only after the corresponding durable fence, session, read, or CAS transition is confirmed."
    - "The runtime, public witness, witness-store proxy, and one-shot init identities have disjoint credentials, subjects, mounts, and capabilities; no public process can access raw KV or JetStream management subjects."
    - "The full request/reply path survives retries, contention, process restart, lost responses, and session rotation without local, unsigned, or optional-witness fallback."
    - "End-to-end controls return signed presence for current, predecessor, and live-prepared reads, request-bound signed absence for aborted and evicted transaction IDs, and an unsigned typed refusal for stale-replica reads; they exercise every configured limit at max and max-plus-one and prove bounded retention."
  artifacts:
    - path: "crates/swarm-governance-witness/src/public_dispatcher.rs"
      provides: "Bounded nine-operation dispatcher with exact request/body matching and durable-response sequencing."
      contains: "WitnessServiceOperationV1"
    - path: "crates/swarm-governance/src/witness_candidate_verifier.rs"
      provides: "Single authoritative typed Prepare verifier that emits StaleIntent only when intent is the sole defect and reports admission, transition, signature, and bound failures distinctly."
      contains: "WitnessPrepareVerification"
    - path: "crates/swarm-governance-witness/src/store_proxy_service.rs"
      provides: "Private typed proxy service that alone opens NatsWitnessStore and accepts only three private subjects."
      contains: "witness.governance.witness.store.v1"
    - path: "crates/swarm-governance-witness/src/runtime_client.rs"
      provides: "Runtime client implementing GovernanceDurabilityWitness with nested signature/failure verification and fixed retry policy."
      contains: "GovernanceDurabilityWitness"
    - path: "crates/swarm-governance-witness/tests/full_service_path.rs"
      provides: "Credential/account/subject/replay/timeout integration coverage for runtime to witness to store to JetStream."
      contains: "full_service_path"
    - path: "crates/swarm-governance-witness/tests/service_checkpoint.rs"
      provides: "Exact transaction-read, max/max-plus-one, retention-window, restart, and session-rotation controls."
      contains: "service_checkpoint_enforces_every_protocol_limit_at_max_and_max_plus_one"
  key_links:
    - from: "PublicWitnessDispatcher"
      to: "WitnessServiceOperationV1 and WitnessCandidateVerifier"
      via: "exact nine operation/body dispatch plus one typed authoritative Prepare verification result before a store transition"
      pattern: "WitnessServiceOperationV1.*WitnessPrepareVerification"
    - from: "PublicWitnessDispatcher"
      to: "StoreProxyService"
      via: "bounded private request/reply import; public process holds no KV handle"
      pattern: "PublicWitnessDispatcher.*StoreProxyService.*bounded"
    - from: "RuntimeWitnessClient"
      to: "GovernanceDurabilityWitness"
      via: "typed async methods verify witness response/failure attestation before returning"
      pattern: "impl.*GovernanceDurabilityWitness.*RuntimeWitnessClient"
---

<objective>
Implement Slice D: the public nine-operation witness service, private store proxy, one-shot initializer target, and runtime `GovernanceDurabilityWitness` client.

Purpose: The durable store is not a governance authority until the service boundary enforces operation semantics, account isolation, signed response ordering, retry limits, and client-side attestation checks.
Output: Bounded public/private service modules and binaries, a concrete runtime adapter, and full-service/restart conformance through the three-account NATS path.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/285-assurance-foundation-closure/285-CONTEXT.md
@.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md
@.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md
@.planning/phases/285-assurance-foundation-closure/285-VERIFICATION.md
@.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md
@<git show 5be011a07690a63a297d5bba8fbf740bb659c19d:.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md>
@crates/swarm-governance-witness/Cargo.toml
@crates/swarm-governance-witness/src/lib.rs
@crates/swarm-governance-witness/src/nats_config.rs
@crates/swarm-governance-witness/src/jetstream_store.rs
@crates/swarm-governance/src/witness_service.rs
@crates/swarm-governance/src/witness_candidate_verifier.rs
@crates/swarm-governance/src/witness_engine/store.rs
@crates/swarm-governance/src/persistence_protocol.rs
@crates/swarm-governance/tests/phase285_witness_conformance.rs
@tools/check-witness-dependency-closure.sh
@tools/check-phase285-witness-integrity.sh
@tools/fixtures/phase285-witness-integrity.json
@tools/with-nats-jetstream.sh
@docker-compose.yml

Plan 03 owns the package manifest and JetStream implementation; consume their frozen DTO/config types. The exact public subjects are `swarm.governance.witness.v1.{fence,establish,discover,prepare,commit,abort,read_prepared,read_head,fetch_payload}` and the exact private subjects are `swarm.governance.witness.store.v1.{inspect_ready,read_entry,compare_and_swap}`. External GitHub App enforcement remains deferred by the phase context and must not be added to this slice.

The accepted Plan 03B launcher remains byte-identical. Any checker or integrity-manifest change invalidates the prior freeze. After each task, the root integrator must freeze the cumulative candidate tree and publish a separately reviewed planning object containing that tree plus the exact launcher and refreshed manifest SHA-256 values. Only those planning-object-pinned values may be supplied to `tools/check-phase285-witness-integrity.sh`; a digest computed or refreshed by production code is not authorization evidence. The frozen 58 selector/package/target/command/case tuples and registry digest `a3a3ec459600ac3163a9b66aa40aa39e9387c50cc75b1e765d9f0693ddb8983b` are immutable throughout Plan 04.

Tasks are strictly serial despite sharing plan wave 5: Task 04-02 cannot start until the cumulative Task 04-01 tree and its planning-object pin have independent P0/P1/P2 = 0/0/0 review, and Task 04-03 has the identical dependency on accepted Task 04-02. A task-level planning pin is evidence metadata for the frozen candidate tree, not authorization to advance another phase or to bypass final Plan 04 review.

The exact trait-to-wire mapping is: `issue_session_fence` -> `Fence`/`Fence`/no authorization; `establish_session` -> `Establish`/`Establish`/no authorization; `discover_stream` -> `Discover`/`Discover`/no authorization; `prepare_successor` -> `Prepare`/`Outcome`/session authorization; `commit_prepared` -> `Commit`/`Outcome`/session authorization; `abort_prepared` -> `Abort`/`Outcome`/session authorization; `read_prepared_for_stream` -> `ReadPrepared`/`Read`/session authorization; `read_head` -> `ReadHead`/`Read`/session authorization; and `fetch_payload` -> `FetchPayload`/`Read`/session authorization. No method may infer or substitute another operation, body, target transaction, nonce, or response variant.

The exact planned Rust seam is:

```rust
pub fn witness_service_request_nonce(entropy: [u8; 32]) -> String;

pub struct WitnessServiceRequestDraftV1 { /* private fields */ }

impl WitnessServiceRequestDraftV1 {
    pub fn new(
        request_nonce: String,
        admission_digest: String,
        body: WitnessServiceRequestBodyV1,
    ) -> ProtocolResult<Self>;
    pub fn request_digest(&self) -> &str;
    pub fn finalize_without_authorization(self) -> ProtocolResult<WitnessServiceRequestV1>;
    pub fn finalize_with_session(
        self,
        session: &GovernanceWitnessSession,
    ) -> ProtocolResult<WitnessServiceRequestV1>;
}

impl WitnessServiceResponseV1 {
    pub fn decode_for_client_request(
        bytes: &[u8],
        request: &WitnessServiceRequestV1,
    ) -> ProtocolResult<Self>;
}

#[async_trait]
pub trait GovernanceDurabilityWitness: Send + Sync {
    type Error: std::error::Error + Send + Sync + 'static;
    async fn issue_session_fence(&self, request: WitnessServiceRequestV1) -> Result<WitnessSessionStateFenceV1, Self::Error>;
    async fn establish_session(&self, request: WitnessServiceRequestV1) -> Result<WitnessSessionAttestationV1, Self::Error>;
    async fn discover_stream(&self, request: WitnessServiceRequestV1) -> Result<WitnessDiscoveryAttestationV1, Self::Error>;
    async fn prepare_successor(&self, request: WitnessServiceRequestV1) -> Result<WitnessOutcomeAttestationV1, Self::Error>;
    async fn commit_prepared(&self, request: WitnessServiceRequestV1) -> Result<WitnessOutcomeAttestationV1, Self::Error>;
    async fn abort_prepared(&self, request: WitnessServiceRequestV1) -> Result<WitnessOutcomeAttestationV1, Self::Error>;
    async fn read_prepared_for_stream(&self, request: WitnessServiceRequestV1) -> Result<WitnessReadAttestationV1, Self::Error>;
    async fn read_head(&self, request: WitnessServiceRequestV1) -> Result<WitnessReadAttestationV1, Self::Error>;
    async fn fetch_payload(&self, request: WitnessServiceRequestV1) -> Result<WitnessReadAttestationV1, Self::Error>;
}
```

The caller creates one 32-byte entropy value per new logical request and derives the nonce exactly once. An exact retry reuses the complete finalized request bytes and nonce; a new entropy value means a new request and can never reuse an old authorization. `new` derives `operation` from `body.operation()` and computes the digest with authorization absent. `finalize_without_authorization` accepts only Fence, Establish, or Discover. `finalize_with_session` derives both operation and target txid only from the body (`candidate.txid` for Prepare; `txid` for Commit, Abort, and FetchPayload; `target_txid` for ReadPrepared and ReadHead), calls `GovernanceWitnessSession::authorize`, inserts that proof, and revalidates the complete request. No trait method retains duplicate typed arguments. `decode_for_client_request` verifies a Failure's canonical bytes, signature/key, operation, request digest, stream, admission, optional store-state-digest syntax, and closed failure code against the exact request; it cannot construct a failure or access `VerifiedWitnessStoreStateV1`, which remains mandatory on the service-side signing path.

The checker owns independent canonical inner ledgers in addition to the frozen 4/4/15 top-level tuple registry. Their exact row sets are:

- `dispatcher_mapping` (9): `issue_session_fence`, `establish_session`, `discover_stream`, `prepare_successor`, `commit_prepared`, `abort_prepared`, `read_prepared_for_stream`, `read_head`, `fetch_payload`.
- `capability_matrix` (20): `runtime_private_subject`, `runtime_raw_kv`, `runtime_js_api`, `witness_raw_kv`, `witness_js_api`, `init_serving_subject`, `runtime_credential_swap`, `witness_credential_swap`, `store_credential_swap`, `init_credential_swap`, `account_swap`, `mount_swap`, `reply_subject_injection`, `wildcard_import`, `tls_ca_swap`, `tls_server_name_swap`, `store_queue_exhaustion`, `public_store_bypass`, `private_public_signing`, `hostile_cleanup_preservation`.
- `read_classification` (6): `current_some`, `predecessor_some`, `prepared_some`, `aborted_none`, `evicted_none`, `stale_replica_unsigned_refusal`.
- `restart_lost_response` (18): `restart_fence`, `lost_response_fence`, `restart_establish`, `lost_response_establish`, `restart_discover`, `lost_response_discover`, `restart_prepare`, `lost_response_prepare`, `restart_commit`, `lost_response_commit`, `restart_abort`, `lost_response_abort`, `restart_read_prepared`, `lost_response_read_prepared`, `restart_read_head`, `lost_response_read_head`, `restart_fetch_payload`, `lost_response_fetch_payload`.
- `bounds` (33): `state_bytes`, `checkpoint_bytes`, `binding_bytes`, `public_request_bytes`, `public_response_bytes`, `per_stream_retained_bytes`, `kv_value_bytes`, `store_proxy_request_bytes`, `store_proxy_response_bytes`, `bucket_bytes`, `admitted_stream_count`, `initial_epoch`, `sequence_counter`, `intent_counter`, `session_generation_counter`, `cas_retry_count`, `request_deadline_millis`, `kv_history`, `replica_count`, `subscription_capacity`, `client_command_capacity`, `initial_read_buffer_capacity`, `public_ingress_queue_capacity`, `private_ingress_queue_capacity`, `public_max_in_flight`, `private_max_in_flight`, `nats_max_payload`, `nats_header_overhead_budget`, `jetstream_entry_overhead_budget`, `async_nats_i32_max_value_size`, `async_nats_i64_max_bytes`, `usize_capacity_conversion`, `u16_read_buffer_conversion`; every row has independent `max` and `max_plus_one` results, for exactly 66 boundary cells.
- `rotation_retention` (9): `rotation_one`, `rotation_hundred`, `rotation_retry_after_prepare`, `rotation_retry_after_commit`, `rotation_retry_after_abort`, `retention_beyond_window`, `retention_constant_keys`, `retention_bounded_values`, `evicted_not_live`.

For every inner ledger, the checker owns an independent literal expected projection and requires exact set equality, exact cardinality, unique row IDs, executed > 0, passed = executed, failed = 0, ignored = 0, the frozen candidate tree, and a digest over canonical rows. Same-engine mutations must kill omission, addition, duplication, row-ID substitution, wrong status/count, stale/cross-case ledger, and coherently refreshed-digest attacks.
</context>

<tasks>

<task type="auto">
  <wave>5</wave>
  <name>Task 285-04-01: Implement the bounded nine-operation public dispatcher</name>
  <files>crates/swarm-governance-witness/Cargo.toml, crates/swarm-governance-witness/src/lib.rs, crates/swarm-governance-witness/src/public_dispatcher.rs, crates/swarm-governance-witness/src/service_config.rs, crates/swarm-governance-witness/tests/full_service_path.rs, crates/swarm-governance/src/persistence_protocol.rs, crates/swarm-governance/src/witness_candidate_verifier.rs, crates/swarm-governance/src/witness_service.rs, crates/swarm-governance/tests/phase285_witness_conformance.rs, tools/check-phase285-witness-conformance.sh, tools/check-witness-dependency-closure.sh, tools/fixtures/phase285-witness-integrity.json</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md (service operations, fixed subjects, queue limits, response signing order); .planning/phases/285-assurance-foundation-closure/285-03B-PLAN.md (integrity invalidation and launcher rules); .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance-witness/Cargo.toml; crates/swarm-governance-witness/src/lib.rs; crates/swarm-governance-witness/src/nats_config.rs; crates/swarm-governance/src/witness_service.rs; crates/swarm-governance/src/witness_candidate_verifier.rs; crates/swarm-governance/src/witness_engine/store.rs; crates/swarm-governance/src/persistence_protocol.rs; crates/swarm-governance/tests/phase285_witness_conformance.rs; tools/check-phase285-witness-conformance.sh; tools/check-witness-dependency-closure.sh; tools/check-phase285-witness-integrity.sh; tools/fixtures/phase285-witness-integrity.json</read_first>
  <action>
Red-first: add `public_dispatcher_rejects_unknown_subject_operation_or_body`, `public_dispatcher_returns_overload_without_spawning_or_touching_store`, `public_dispatcher_signs_only_after_confirmed_transition`, and `public_dispatcher_maps_typed_failure_without_string_fallback` before implementation. Implement the exact Rust draft, finalizers, decoder, trait signatures, nonce lifecycle, target derivation, and literal nine-row mapping frozen in context; no duplicate trait arguments or alternative constructor is allowed. Mutation controls reject swapped nonce, body, target, authorization, response variant, failure signature, or optional store-state digest. Implement `PublicWitnessDispatcher` with an explicit match over exactly the nine operations; subscribe only to the nine fixed public subjects; use queue subscriptions, a bounded ingress queue and semaphore; and reject wildcard or client-supplied reply subjects.

Before handler selection, validate only payload/global framing, the outer canonical wire/schema, operation/body pairing, nonce/request/admission digests, subject, and admission routing through the outer-dispatch identity seam. For `Prepare`, the dispatcher must not call complete `WitnessServiceRequestV1` validation, complete body validation, `CandidateV1::validate`, `CandidateV1::canonical_bytes`, or session authorization before the authoritative verifier. After the authenticated current envelope is read, pass the raw decoded Prepare body, outer request identity, authorization material, authenticated current envelope, and selected `WitnessAdmissionEntryV1` to one authoritative verifier owned by `witness_candidate_verifier.rs`. A non-forgeable typed protocol seam in `persistence_protocol.rs` may authenticate a current-head stale candidate without accepting its transition. The verifier alone performs nested candidate canonicalization, request/session signatures, current or canonical-genesis predecessor, epoch, sequence, abort receipt, admission, payload signatures, retained bytes, transition semantics, and selected-entry state/checkpoint/binding limits. It returns only an opaque admitted value or a closed failure enum. It may return `StaleIntent` only after every stronger defect is absent; current-head and genesis old/skipped intent use this same path, and no catch-all `WitnessOutcomeMismatch` may map to stale. Add coherent outer-valid, redigested, reauthorized controls for current-head old/skipped intent and source mutants that reintroduce any forbidden early complete validation.

Apply this exact seam-specific bound taxonomy: (1) a startup envelope violation refuses construction before serving and emits no response; (2) selected request/response maximum violations are unsigned adapter refusals at the wire boundary; (3) an initial authenticated current-entry state/checkpoint/binding/retained violation returns signed `BoundsExceeded` bound to that current digest with zero attempted CAS; (4) an authoritative-verifier or proposed-envelope violation returns signed `BoundsExceeded` bound to the initial current digest with zero attempted CAS; (5) a conflict-winner envelope violation, once the authenticated winner is available, returns signed `BoundsExceeded` bound to the winner digest after exactly one attempted CAS, zero applied CAS, and no retry; and (6) a post-Applied confirmation mismatch or bound violation returns typed `Ambiguous`/`OutcomeUnknown`, never a signed deterministic rejection, and is never upgraded or retried. Framing, transport, timeout, pre-admission overload, and wire-bound request/response size failures remain unsigned adapter errors. Add behavioral max/max-plus-one controls for each applicable selected-entry request, response, state, checkpoint, binding, and retained limit at startup, initial read, proposed mutation, conflict observation, and confirmation read, including a tighter secondary stream; assert exact response kind, failure signature when permitted, store digest, attempted/applied CAS count, retry count, and proxy-call sequence. Add source mutants that collapse any two taxonomy branches or restore a universal signed-bound rule.

Add mixed-defect controls for current/genesis old-or-skipped intent combined individually with every stronger signature, admission, predecessor, transition, retained, and selected-entry bound defect; only pure intent may produce `StaleIntent`. Mutations perform one durable transition, read-confirm the exact revision/value, then sign; the confirmation ambiguity rule above is authoritative when confirmation is not exact. Queue overflow returns typed overload before a store call. Add closed config parsing. Promote `swarm-crypto` from dev-only to the exact approved normal dependency, update declared/resolved dependency-closure expectations and mutants, and prove locked library compilation. Materialize exactly the already-registered four `public-dispatcher` rows plus all 9 `dispatcher_mapping` inner-ledger rows without changing any of the 58 tuples or their digest; make every inner-ledger and top-level mutation frozen in context red. The source guard must kill omission/substitution of each typed verifier branch, signed-bound conversion, selected-entry ceiling, and startup/initial/proposed/conflict/confirmation seam, but lexical source presence cannot substitute for the executable controls. Refresh only the canonical integrity manifest's checker hash; leave the launcher byte-identical. Freeze the cumulative candidate tree, obtain the separate planning-object pin, then run the selector only through that pin.
  </action>
  <acceptance_criteria>Each of the nine subjects has exactly one typed handler; unknown/wildcard/body-mismatch/oversize/unauthorized requests are rejected; queue saturation creates no store task. Prepare performs only outer identity/routing checks before one authoritative typed verifier, never complete request/body/candidate validation or session authorization; the verifier emits `StaleIntent` only when intent is the sole defect for both current-head and genesis successors. Bound outcomes follow the six-part taxonomy exactly: startup refusal without serving; unsigned request/response wire refusal; signed initial/proposed `BoundsExceeded` with zero attempted CAS; signed conflict-winner `BoundsExceeded` after exactly one attempted and zero applied CAS with no retry; and typed ambiguous/unknown confirmation after an applied CAS without deterministic signed rejection, upgrade, or retry. Every mutation success is signed only after exact durable read confirmation; signed failure codes remain closed and matchable; no public module opens a KV handle or calls JetStream management API.</acceptance_criteria>
  <verify>
    <automated>test -n "${PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256:-}" && test -n "${PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256:-}" && bash tools/check-phase285-witness-integrity.sh --integrity-self-test && bash tools/check-phase285-witness-integrity.sh public-dispatcher && bash tools/check-witness-dependency-closure.sh --library-only</automated>
    The two integrity values must equal the separately reviewed planning-object pin for the frozen cumulative Task 04-01 tree; runtime digest discovery is forbidden. The checker must report exactly 4/4 top-level dispatcher cases and 9/9 mapping rows with failed = 0 and ignored = 0, preserve all 58 registry tuples and their digest, and kill every top-level and inner-ledger mutation.
  </verify>
  <done>Task 285-04-01 establishes the public witness service boundary and its durable-response ordering without exposing private storage authority.</done>
</task>

<task type="auto">
  <wave>5</wave>
  <name>Task 285-04-02: Implement the private typed proxy service and account isolation path</name>
  <files>crates/swarm-governance-witness/src/lib.rs, crates/swarm-governance-witness/src/store_proxy_service.rs, crates/swarm-governance-witness/src/service_config.rs, crates/swarm-governance-witness/tests/full_service_path.rs, crates/swarm-governance/tests/phase285_witness_conformance.rs, tools/check-phase285-witness-conformance.sh, tools/fixtures/phase285-witness-integrity.json, tools/with-nats-jetstream.sh, docker-compose.yml</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md (three accounts, imports/exports, private subjects, raw capability boundary, proxy request/response); .planning/phases/285-assurance-foundation-closure/285-03B-PLAN.md (integrity invalidation and launcher rules); .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance-witness/src/lib.rs; crates/swarm-governance-witness/src/jetstream_store.rs; crates/swarm-governance-witness/src/nats_config.rs; crates/swarm-governance-witness/src/public_dispatcher.rs; crates/swarm-governance-witness/src/service_config.rs; crates/swarm-governance/src/witness_engine/store.rs; crates/swarm-governance/tests/phase285_witness_conformance.rs; tools/check-phase285-witness-conformance.sh; tools/check-phase285-witness-integrity.sh; tools/fixtures/phase285-witness-integrity.json; tools/with-nats-jetstream.sh; docker-compose.yml</read_first>
  <action>Red-first: add the four literal `full_service_path_*` tests before implementing `StoreProxyService`. The private service subscribes only to the three fixed private subjects, owns the sole raw-store handle/credential, and accepts only the typed proxy request. Runtime imports only nine public subjects; witness imports only three private subjects; init serves none. Require TLS URL, pinned CA/server name, exact credential role, bounded queues, and deadlines. Validate request digest, witness signature, epoch/anchor, admission, stream mapping, and bounds before JetStream; return a closed typed response. Extend the owned harness/Compose fixture to exactly three accounts and four principals with distinct generated credentials, fixed one-way imports/exports, bounded responses, no wildcard, and no cross-role mount. Execute all 20 `capability_matrix` rows frozen in context, including hostile cleanup preservation. Materialize exactly 4/4 top-level `full-service-path` rows plus 20/20 capability rows cumulatively, preserve the 58 tuples/digest, kill every inner/top-level mutant, refresh only the manifest checker hash, leave the launcher unchanged, freeze the cumulative tree, and obtain the next separate planning-object pin before execution.</action>
  <acceptance_criteria>Runtime/public/private/init identities have disjoint credentials and exact subject permissions; every isolation mutation fails before a JetStream call; the private service alone reaches raw CAS; proxy response body/request digest is checked before public signing; queue exhaustion is a typed refusal; no wildcard/raw management capability passes the fixture.</acceptance_criteria>
  <verify>
    <automated>test -n "${PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256:-}" && test -n "${PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256:-}" && bash tools/check-phase285-witness-integrity.sh --integrity-self-test && bash tools/with-nats-jetstream.sh --self-test && bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" bash tools/check-phase285-witness-integrity.sh full-service-path</automated>
    The checker must report 4/4 top-level cases and 20/20 capability rows with passed = executed, failed = 0, ignored = 0; unavailable NATS, a missing/extra/duplicate/substituted capability cell, partial fixture, or raw-subject access must fail.
  </verify>
  <done>Task 285-04-02 proves the complete typed private path and account isolation. Public service code cannot acquire raw storage authority.</done>
</task>

<task type="auto">
  <wave>5</wave>
  <name>Task 285-04-03: Add service binaries, runtime witness adapter, and restart checkpoint</name>
  <files>crates/swarm-governance-witness/Cargo.toml, crates/swarm-governance-witness/src/lib.rs, crates/swarm-governance-witness/src/raw_config.rs, crates/swarm-governance-witness/src/runtime_client.rs, crates/swarm-governance-witness/src/service_config.rs, crates/swarm-governance-witness/src/bin/swarm-governance-witness.rs, crates/swarm-governance-witness/src/bin/swarm-governance-witness-store.rs, crates/swarm-governance-witness/src/bin/swarm-governance-witness-init.rs, crates/swarm-governance-witness/tests/service_checkpoint.rs, crates/swarm-governance/tests/phase285_witness_conformance.rs, tools/check-phase285-witness-conformance.sh, tools/check-witness-dependency-closure.sh, tools/fixtures/phase285-witness-integrity.json</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md (runtime adapter, retries, session rotation, init lifecycle, response attestation); .planning/phases/285-assurance-foundation-closure/285-03B-PLAN.md (integrity invalidation and launcher rules); .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance-witness/Cargo.toml; crates/swarm-governance-witness/src/lib.rs; crates/swarm-governance-witness/src/raw_config.rs (sole closed NATS 2.11.17 parser, bound serving inspection, typed initialization snapshot); crates/swarm-governance-witness/src/public_dispatcher.rs; crates/swarm-governance-witness/src/jetstream_store.rs; crates/swarm-governance-witness/src/service_config.rs; crates/swarm-governance/src/persistence_protocol.rs (GovernanceDurabilityWitness, session fence, authorization); crates/swarm-governance/src/witness_service.rs; crates/swarm-governance/tests/phase285_witness_conformance.rs; tools/check-phase285-witness-conformance.sh; tools/check-witness-dependency-closure.sh; tools/check-phase285-witness-integrity.sh; tools/fixtures/phase285-witness-integrity.json</read_first>
  <action>Red-first: add exactly the fifteen literal `service_checkpoint_*` cases already frozen in the 58-row registry; do not register, rename, or replace them. Implement the exact read semantics and execute all 6 `read_classification` rows; execute all 18 `restart_lost_response` rows across the nine operations; execute both max and max-plus-one for all 33 `bounds` rows (66 cells); and execute all 9 `rotation_retention` rows. Every inner set and mutation follows the frozen ledger rules in context. Implement `RuntimeWitnessClient` against the exact request-only trait and client-safe failure seam with one bounded retry/deadline policy and complete nested validation. Keep the public witness as sole governance signer, store binary as sole raw CAS process, and init as a one-shot initializer that exits before serving. Add the three exact bin targets only after sources exist. In `raw_config.rs`, add exactly one non-public `pub(crate)` initialization seam, `inspect_raw_stream_info_for_initialization(bytes, expected) -> Result&lt;Nats21117TypedSnapshotV1, RawConfigurationError&gt;`, implemented only by `inspect_raw_stream_info_unbound(bytes, expected)?.typed_snapshot()`. Implement the public closed `run_witness_init_process(config)` in the owned library `runtime_client.rs`; that library runner alone may invoke the crate-private initialization seam, while the separate init binary may call only the high-level public runner and can never name the seam or typed snapshot. The runner's exact order is: create or open the fixed stream; complete and authenticate the full fixed-key/progress reread; successfully CAS the manifest to Ready or authenticate an already-Ready resume; perform one fresh leader-mediated authoritative Stream.Info request; call the initialization seam exactly once on that fresh response; derive the anchor fields and retain the complete canonical response bytes/digest from that returned snapshot; then construct and sign the epoch-bound anchor. No pre-Ready or cached Stream.Info may authorize the anchor. No public/store/runtime serving path may call the seam; it must not accept an epoch or final anchor, become `pub`, expose raw DTO fields, duplicate JSON parsing/canonicalization/digest logic, or accept caller-precomputed creation time or raw-configuration digest. Add a positive locked all-target compilation control for the init binary and a negative visibility control proving the binary cannot directly name the crate-private seam. Add source and behavior mutations that reject seam deletion, public visibility, serving-path use, direct `serde_json` parsing outside `raw_config.rs`, bypassed expected configuration, substitution with the final-anchor-bound serving decoder, precomputed final-anchor inputs, an early/pre-Ready call, stale snapshot reuse, duplicate call, missing call, and changed final creation/configuration/messages/first-sequence/last-sequence/subject-count evidence. Cover absent/revision-zero initialization, tombstone protection, interrupted resume, changed-key refusal, `Initializing` refusal, Ready-CAS crash, and byte-identical anchor re-emission. Cumulatively materialize 15/15 top-level rows, 6/6 reads, 18/18 restart/lost-response rows, 66/66 boundary cells, and 9/9 rotation/retention rows; preserve the 58 top-level tuples/digest, kill every inner/top-level mutant, refresh only the manifest checker hash, keep the launcher byte-identical, freeze the full tree, and publish/review its pin. Run dependency closure over the library plus three exact bins and reject forbidden normal/reverse edges.</action>
  <acceptance_criteria>All three binaries enforce their role-specific closed configuration; `RuntimeWitnessClient` implements every required trait method and validates nested attestations; the separate init binary reaches initialization only through the public closed library runner and cannot name the crate-private inspection seam; init derives authoritative creation/configuration/state evidence from exactly one fresh post-Ready Stream.Info through the sole parser, retains its complete canonical bytes/digest, signs only afterward, and cannot serve requests after readiness; restart/lost-response paths use signed discovery/session rotation and bounded retries; missing or unauthorized service fails closed with no local/optional fallback.</acceptance_criteria>
  <verify>
    <automated>test -n "${PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256:-}" && test -n "${PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256:-}" && bash tools/check-phase285-witness-integrity.sh --integrity-self-test && bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" bash tools/check-phase285-witness-integrity.sh service-checkpoint && bash tools/check-witness-dependency-closure.sh --all-targets</automated>
    The values must equal the separately reviewed full-tree pin. The checker must show 15/15 top-level, 6/6 read, 18/18 restart/lost-response, 66/66 boundary, and 9/9 rotation/retention rows with failed = 0 and ignored = 0. Dependency closure must positively compile one library plus three exact bins.
  </verify>
  <done>Task 285-04-03 closes the service-layer checkpoint and provides the only runtime witness implementation consumed by Plan 05. External GitHub App protection remains explicitly deferred.</done>
</task>

</tasks>

<verification>
On one immutable staged tree, publish and independently review the final Plan 04 planning object containing the exact candidate tree, unchanged launcher SHA-256, and refreshed manifest SHA-256. Supply only those pinned values to the launcher. Run `--integrity-self-test`, `--self-test jetstream-release-hook`, then the literal inherited selectors `response-failure-wire`, `candidate-verifier`, `protocol-checkpoint`, `atomic-store-contract`, `in-memory-differential`, `typed-proxy`, `transport-layering`, `jetstream-cas`, and `jetstream-checkpoint`, followed by `public-dispatcher`, `full-service-path`, and `service-checkpoint`; every authorization-critical checker call goes through the launcher and NATS selectors go through the owned harness. Require the exact unchanged 58-row registry digest. Then run `cargo test -p swarm-governance --locked --offline`, harnessed `cargo test -p swarm-governance-witness --locked --offline`, `bash tools/check-witness-dependency-closure.sh --all-targets`, `cargo clippy -p swarm-governance -p swarm-governance-witness --all-targets --all-features --locked --offline -- -D warnings`, `cargo fmt --all -- --check`, shellcheck over every changed shell script plus the launcher/checker, actionlint, `git diff --check`, exact twenty-one-path staged-scope verification, temp/container cleanup, and final tree/index identity. Obtain independent P0/P1/P2 = 0/0/0 review before one atomic Plan 04 commit and two remote refs. Treat this as a local slice checkpoint only; keep Phases 286-289 blocked.
  <automated>set -e; test "$(git rev-parse HEAD)" = 8abe28dbc42c444643ea473614bee7a8cf912b8b; test -n "${PHASE285_PLAN04_TREE:-}"; test -n "${PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256:-}"; test -n "${PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256:-}"; test "$(git write-tree)" = "$PHASE285_PLAN04_TREE"; test -z "$(git diff --name-only)"; test -z "$(git ls-files --others --exclude-standard)"; git diff --cached --check 8abe28dbc42c444643ea473614bee7a8cf912b8b; test "$(git diff --cached --name-only 8abe28dbc42c444643ea473614bee7a8cf912b8b | LC_ALL=C sort | shasum -a 256 | awk '{print $1}')" = 4230077ae91bc7e38b3542eab16579bf3232e37c171d89bc606ddcd4d3ae503d; test "$(git diff --cached --name-only 8abe28dbc42c444643ea473614bee7a8cf912b8b | wc -l | tr -d ' ')" = 21; bash tools/check-phase285-witness-integrity.sh --integrity-self-test; bash tools/check-phase285-witness-integrity.sh --self-test jetstream-release-hook; bash tools/check-phase285-witness-integrity.sh response-failure-wire; bash tools/check-phase285-witness-integrity.sh candidate-verifier; bash tools/check-phase285-witness-integrity.sh protocol-checkpoint; bash tools/check-phase285-witness-integrity.sh atomic-store-contract; bash tools/check-phase285-witness-integrity.sh in-memory-differential; bash tools/check-phase285-witness-integrity.sh typed-proxy; bash tools/check-phase285-witness-integrity.sh transport-layering; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" bash tools/check-phase285-witness-integrity.sh jetstream-cas; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" bash tools/check-phase285-witness-integrity.sh jetstream-checkpoint; bash tools/check-phase285-witness-integrity.sh public-dispatcher; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" bash tools/check-phase285-witness-integrity.sh full-service-path; bash tools/with-nats-jetstream.sh env PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256="$PHASE285_WITNESS_INTEGRITY_LAUNCHER_SHA256" PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256="$PHASE285_WITNESS_INTEGRITY_MANIFEST_SHA256" bash tools/check-phase285-witness-integrity.sh service-checkpoint; bash tools/with-nats-jetstream.sh --self-test; cargo test -p swarm-governance --locked --offline; bash tools/with-nats-jetstream.sh cargo test -p swarm-governance-witness --locked --offline; bash tools/check-witness-dependency-closure.sh --all-targets; cargo clippy -p swarm-governance -p swarm-governance-witness --all-targets --all-features --locked --offline -- -D warnings; cargo fmt --all -- --check; shellcheck tools/check-phase285-witness-conformance.sh tools/check-phase285-witness-integrity.sh tools/check-witness-dependency-closure.sh tools/with-nats-jetstream.sh; actionlint .github/workflows/ci.yml; git diff --cached --check 8abe28dbc42c444643ea473614bee7a8cf912b8b; test "$(git write-tree)" = "$PHASE285_PLAN04_TREE"; test -z "$(git diff --name-only)"; test -z "$(git ls-files --others --exclude-standard)"</automated>
</verification>

<success_criteria>
Public-dispatcher, full-service-path, and service-checkpoint lanes are non-vacuously green; only the private store process can access raw JetStream; runtime receives typed attested outcomes via `GovernanceDurabilityWitness`; no local/optional/unsigned fallback exists in enforced service mode; and no external GitHub App check is claimed.
</success_criteria>

<output>
After completion, create `.planning/phases/285-assurance-foundation-closure/285-04-SUMMARY.md`.
</output>
