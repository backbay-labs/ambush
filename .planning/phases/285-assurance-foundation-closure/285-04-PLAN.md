---
phase: 285-assurance-foundation-closure
plan: "04"
type: execute
wave: 5
depends_on:
  - "285-03B"
files_modified:
  - crates/swarm-governance-witness/Cargo.toml
  - crates/swarm-governance-witness/src/lib.rs
  - crates/swarm-governance-witness/src/public_dispatcher.rs
  - crates/swarm-governance-witness/src/store_proxy_service.rs
  - crates/swarm-governance-witness/src/runtime_client.rs
  - crates/swarm-governance-witness/src/service_config.rs
  - crates/swarm-governance-witness/src/bin/swarm-governance-witness.rs
  - crates/swarm-governance-witness/src/bin/swarm-governance-witness-store.rs
  - crates/swarm-governance-witness/src/bin/swarm-governance-witness-init.rs
  - crates/swarm-governance-witness/tests/full_service_path.rs
  - crates/swarm-governance-witness/tests/service_checkpoint.rs
  - crates/swarm-governance/tests/phase285_witness_conformance.rs
  - tools/check-phase285-witness-conformance.sh
autonomous: true
requirements:
  - ASSURE-04
  - ASSURE-06
must_haves:
  truths:
    - "A runtime request reaches exactly one typed public-witness operation, and a success response is signed only after the corresponding durable fence, session, read, or CAS transition is confirmed."
    - "The runtime, public witness, witness-store proxy, and one-shot init identities have disjoint credentials, subjects, mounts, and capabilities; no public process can access raw KV or JetStream management subjects."
    - "The full request/reply path survives retries, contention, process restart, lost responses, and session rotation without local, unsigned, or optional-witness fallback."
    - "End-to-end service controls read current, predecessor, prepared, aborted, evicted, and stale-replica transaction states; exercise every configured limit at max and max-plus-one; and run beyond the retention window while proving constant key cardinality and bounded values."
  artifacts:
    - path: "crates/swarm-governance-witness/src/public_dispatcher.rs"
      provides: "Bounded nine-operation dispatcher with exact request/body matching and durable-response sequencing."
      contains: "WitnessServiceOperationV1"
    - path: "crates/swarm-governance-witness/src/store_proxy_service.rs"
      provides: "Private typed proxy service that alone opens NatsWitnessStore and accepts only three private subjects."
      contains: "witness.governance.witness.store.v1"
    - path: "crates/swarm-governance-witness/src/runtime_client.rs"
      provides: "Runtime client implementing GovernanceDurabilityWitness with nested signature/failure verification and fixed retry policy."
      contains: "GovernanceDurabilityWitness"
    - path: "crates/swarm-governance-witness/tests/full_service_path.rs"
      provides: "Credential/account/subject/replay/timeout integration coverage for runtime to witness to store to JetStream."
      contains: "full_service_path"
    - path: "crates/swarm-governance-witness/tests/service_checkpoint.rs"
      provides: "Exact transaction-read, max/max-plus-one, retention-window, restart, and session-rotation controls."
      contains: "service_checkpoint_enforces_every_protocol_limit_at_max_and_max_plus_one"
  key_links:
    - from: "PublicWitnessDispatcher"
      to: "WitnessServiceOperationV1 and WitnessCandidateVerifier"
      via: "exact nine operation/body dispatch and independent admission before a store transition"
      pattern: "WitnessServiceOperationV1.*WitnessCandidateVerifier"
    - from: "PublicWitnessDispatcher"
      to: "StoreProxyService"
      via: "bounded private request/reply import; public process holds no KV handle"
      pattern: "PublicWitnessDispatcher.*StoreProxyService.*bounded"
    - from: "RuntimeWitnessClient"
      to: "GovernanceDurabilityWitness"
      via: "typed async methods verify witness response/failure attestation before returning"
      pattern: "impl.*GovernanceDurabilityWitness.*RuntimeWitnessClient"
---

<objective>
Implement Slice D: the public nine-operation witness service, private store proxy, one-shot initializer target, and runtime `GovernanceDurabilityWitness` client.

Purpose: The durable store is not a governance authority until the service boundary enforces operation semantics, account isolation, signed response ordering, retry limits, and client-side attestation checks.
Output: Bounded public/private service modules and binaries, a concrete runtime adapter, and full-service/restart conformance through the three-account NATS path.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/285-assurance-foundation-closure/285-CONTEXT.md
@.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md
@.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md
@.planning/phases/285-assurance-foundation-closure/285-VERIFICATION.md
@.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md
@<git show 5be011a07690a63a297d5bba8fbf740bb659c19d:.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md>
@crates/swarm-governance-witness/Cargo.toml
@crates/swarm-governance-witness/src/lib.rs
@crates/swarm-governance-witness/src/nats_config.rs
@crates/swarm-governance-witness/src/jetstream_store.rs
@crates/swarm-governance/src/witness_service.rs
@crates/swarm-governance/src/witness_candidate_verifier.rs
@crates/swarm-governance/src/witness_engine/store.rs
@crates/swarm-governance/src/persistence_protocol.rs
@crates/swarm-governance/tests/phase285_witness_conformance.rs
@tools/check-witness-dependency-closure.sh

Plan 03 owns the package manifest and JetStream implementation; consume their frozen DTO/config types. The exact public subjects are `swarm.governance.witness.v1.{fence,establish,discover,prepare,commit,abort,read_prepared,read_head,fetch_payload}` and the exact private subjects are `swarm.governance.witness.store.v1.{inspect_ready,read_entry,compare_and_swap}`. External GitHub App enforcement remains deferred by the phase context and must not be added to this slice.
</context>

<tasks>

<task type="auto">
  <wave>5</wave>
  <name>Task 285-04-01: Implement the bounded nine-operation public dispatcher</name>
  <files>crates/swarm-governance-witness/src/lib.rs, crates/swarm-governance-witness/src/public_dispatcher.rs, crates/swarm-governance-witness/src/service_config.rs, crates/swarm-governance-witness/tests/full_service_path.rs, crates/swarm-governance/tests/phase285_witness_conformance.rs</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md (service operations, fixed subjects, queue limits, response signing order); .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance-witness/Cargo.toml; crates/swarm-governance-witness/src/lib.rs; crates/swarm-governance-witness/src/nats_config.rs; crates/swarm-governance/src/witness_service.rs; crates/swarm-governance/src/witness_candidate_verifier.rs; crates/swarm-governance/src/witness_engine/store.rs; crates/swarm-governance/src/persistence_protocol.rs; crates/swarm-governance/tests/phase285_witness_conformance.rs</read_first>
  <action>Red-first: add `public_dispatcher_rejects_unknown_subject_operation_or_body`, `public_dispatcher_returns_overload_without_spawning_or_touching_store`, `public_dispatcher_signs_only_after_confirmed_transition`, and `public_dispatcher_maps_typed_failure_without_string_fallback` before the dispatcher implementation. Implement `PublicWitnessDispatcher` with an explicit match over exactly `Fence`, `Establish`, `Discover`, `Prepare`, `Commit`, `Abort`, `ReadPrepared`, `ReadHead`, and `FetchPayload`; subscribe only to the nine fixed public subjects, use queue subscriptions, a bounded ingress queue and semaphore, and reject wildcard or client-supplied reply subjects. Validate payload length, canonical request, operation/body pairing, nonce/admission/session authorization, signature, and admission before selecting a handler. `Prepare` must call the Plan 01 independent verifier and the Plan 02 typed proxy; mutations must perform one durable transition, then read-confirm the exact revision/value, then construct/sign `WitnessServiceResponseV1`. Reads must return signed presence/absence snapshots bound to request digest and stream state. Exact retries may return the original idempotent result or a closed contention/stale/conflict failure; they may not report proposed state from an unconfirmed acknowledgement. Queue overflow returns typed overload before a store call, and framing/timeout/overload remains outside signed protocol failures. Add config parsing with `#[serde(deny_unknown_fields)]` and no production defaults for identity, key, credential, bucket, admission, limit, or TLS paths.</action>
  <acceptance_criteria>Each of the nine subjects has exactly one typed handler; unknown/wildcard/body-mismatch/oversize/unauthorized requests are rejected; queue saturation creates no store task; every mutation response is signed only after exact durable read confirmation; signed failure codes remain closed and matchable; no public module opens a KV handle or calls JetStream management API.</acceptance_criteria>
  <verify>
    <automated>bash tools/check-phase285-witness-conformance.sh public-dispatcher</automated>
    The checker must report positive execution for every public dispatcher case with passed = executed, failed = 0, ignored = 0, and reject missing handler, zero target, omitted operation, wildcard, or ignored fixtures.
  </verify>
  <done>Task 285-04-01 establishes the public witness service boundary and its durable-response ordering without exposing private storage authority.</done>
</task>

<task type="auto">
  <wave>5</wave>
  <name>Task 285-04-02: Implement the private typed proxy service and account isolation path</name>
  <files>crates/swarm-governance-witness/src/lib.rs, crates/swarm-governance-witness/src/store_proxy_service.rs, crates/swarm-governance-witness/src/service_config.rs, crates/swarm-governance-witness/tests/full_service_path.rs, crates/swarm-governance/tests/phase285_witness_conformance.rs</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md (three accounts, imports/exports, private subjects, raw capability boundary, proxy request/response); .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance-witness/src/lib.rs; crates/swarm-governance-witness/src/jetstream_store.rs; crates/swarm-governance-witness/src/nats_config.rs; crates/swarm-governance-witness/src/public_dispatcher.rs; crates/swarm-governance-witness/src/service_config.rs; crates/swarm-governance/src/witness_engine/store.rs; docker-compose.yml; deploy/helm/swarm-team-six/charts/nats/values.yaml; deploy/helm/swarm-team-six/charts/nats/templates/statefulset.yaml; crates/swarm-governance/tests/phase285_witness_conformance.rs</read_first>
  <action>Red-first: add `full_service_path_rejects_runtime_private_subject_and_store_raw_api`, `full_service_path_rejects_credential_account_and_mount_swaps`, `full_service_path_validates_proxy_response_before_public_attestation`, and `full_service_path_fails_closed_on_store_queue_exhaustion` before implementing `StoreProxyService`. The private service subscribes only to `swarm.governance.witness.store.v1.inspect_ready`, `.read_entry`, and `.compare_and_swap`, owns the sole `NatsWitnessStore` handle and raw-store credential, and accepts only `WitnessStoreProxyRequestV1`. The public witness imports only these typed private subjects; runtime imports only the nine public subjects; runtime has no witness-store import; witness has no raw `$KV` or `$JS.API` capability; init has no serving request subject. Require TLS URL, pinned CA/server name, exact account/credential role, bounded queues and request deadlines before subscription. Validate proxy request digest, witness signature, external epoch/anchor, admission, fixed stream mapping, and byte bounds before touching JetStream; return `WitnessStoreProxyResponseV1` with request digest and closed failure body, never a string. Build a full NATS fixture with accounts `runtime`, `witness`, and `witness-store`, exact one-way imports, bounded `allow_responses`, no wildcard imports, and negative mutations for wrong credentials, account swaps, reply-subject injection, raw KV/management subjects, wrong operation marker, and store queue exhaustion. Assert the public dispatcher cannot bypass the private service and the private service cannot sign public governance responses.</action>
  <acceptance_criteria>Runtime/public/private/init identities have disjoint credentials and exact subject permissions; every isolation mutation fails before a JetStream call; the private service alone reaches raw CAS; proxy response body/request digest is checked before public signing; queue exhaustion is a typed refusal; no wildcard/raw management capability passes the fixture.</acceptance_criteria>
  <verify>
    <automated>bash tools/with-nats-jetstream.sh bash tools/check-phase285-witness-conformance.sh full-service-path</automated>
    The checker must execute every account, credential, subject, queue, and response-validation mutation > 0 with passed = executed and ignored = 0; unavailable NATS, zero execution, skipped/ignored tests, partial account fixture, or raw-subject access must fail.
  </verify>
  <done>Task 285-04-02 proves the complete typed private path and account isolation. Public service code cannot acquire raw storage authority.</done>
</task>

<task type="auto">
  <wave>5</wave>
  <name>Task 285-04-03: Add service binaries, runtime witness adapter, and restart checkpoint</name>
  <files>crates/swarm-governance-witness/Cargo.toml, crates/swarm-governance-witness/src/lib.rs, crates/swarm-governance-witness/src/runtime_client.rs, crates/swarm-governance-witness/src/service_config.rs, crates/swarm-governance-witness/src/bin/swarm-governance-witness.rs, crates/swarm-governance-witness/src/bin/swarm-governance-witness-store.rs, crates/swarm-governance-witness/src/bin/swarm-governance-witness-init.rs, crates/swarm-governance-witness/tests/service_checkpoint.rs, crates/swarm-governance/tests/phase285_witness_conformance.rs, tools/check-phase285-witness-conformance.sh</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md (runtime adapter, retries, session rotation, init lifecycle, response attestation); .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance-witness/Cargo.toml; crates/swarm-governance-witness/src/lib.rs; crates/swarm-governance-witness/src/public_dispatcher.rs; crates/swarm-governance-witness/src/jetstream_store.rs; crates/swarm-governance-witness/src/service_config.rs; crates/swarm-governance/src/persistence_protocol.rs (GovernanceDurabilityWitness, session fence, authorization); crates/swarm-governance/src/witness_service.rs; crates/swarm-governance/tests/phase285_witness_conformance.rs; tools/check-phase285-witness-conformance.sh; tools/check-witness-dependency-closure.sh</read_first>
  <action>Red-first: add `service_checkpoint_rejects_unsigned_or_wrong_request_response`, `service_checkpoint_retries_only_exact_idempotent_request`, `service_checkpoint_rotates_session_after_lost_response`, `service_checkpoint_init_never_serves_requests`, `service_checkpoint_survives_process_restart_without_local_fallback`, `service_checkpoint_replays_fence_after_one_and_one_hundred_rotations`, `service_checkpoint_exact_rotation_retry_remains_byte_identical_after_later_prepare_commit_abort`, `service_checkpoint_rejects_expected_head_only_stale_retry`, `service_checkpoint_rejects_concurrent_conflicting_challenges`, `service_checkpoint_rejects_stale_fence_after_head_prepared_session_manifest_epoch_anchor`, `service_checkpoint_rejects_generation_exhaustion`, `service_checkpoint_reads_current_predecessor_prepared_aborted_and_evicted_txids`, `service_checkpoint_rejects_stale_replica_transaction_reads`, `service_checkpoint_enforces_every_protocol_limit_at_max_and_max_plus_one`, and `service_checkpoint_retention_window_keeps_constant_keys_and_bounded_values` before writing binaries/client. Register every literal name under the `service-checkpoint` selector and execute each by exact target/name. The read controls must distinguish signed current, predecessor, prepared, aborted, evicted, and stale-replica results rather than collapsing them to absence. The boundary control must independently exercise maximum and maximum-plus-one for every configured byte, collection, counter, stream-count, KV-value, bucket, raw NATS overhead, async-nats integer conversion, ingress/private queue capacity, request/reply payload, retained-value, and transaction-record bound; each max succeeds only when otherwise valid, each max-plus-one refuses before a store mutation, and checked integer overflow is permanent refusal. The retention control must run more committed/aborted transactions than the configured retention window, prove fixed key cardinality and bounded value sizes after restart, and prove evicted transaction IDs cannot be replayed as live. The exact-rotation retry case must prove that the immediately committed rotation returns the same retained response snapshot, body, signing preimage, and signature before and after each later Prepare, Commit, and Abort; separate negative controls reject a changed nonce, signature, `expected_head`, or any challenge from an older generation. Implement `RuntimeWitnessClient` as the concrete `GovernanceDurabilityWitness` adapter, including `issue_session_fence` and `prepare_successor(session, expected_head, candidate, authorization)`, with one fixed retry/deadline policy; verify response signature, operation, request digest, admission digest, stream, witness identity/key ID, body variant, and nested failure attestation before returning each typed result. Keep `swarm-governance-witness` as the only signer, `witness-store` as the only raw CAS process, and `witness-init` as a one-shot manifest/epoch/anchor initializer that exits before serving subjects. Add explicit `[[bin]]` target declarations to `crates/swarm-governance-witness/Cargo.toml` only after creating the three source files: `swarm-governance-witness`, `swarm-governance-witness-store`, and `swarm-governance-witness-init`; the runtime client remains a library target. The init binary checks exact `WitnessBucketConfigurationV1`, manifest, absent/revision-zero keys, revision-zero tombstone protection, interrupted resume, missing/changed key refusal, `Initializing` startup refusal, Ready-CAS crash before anchor, and byte-identical anchor re-emission before seal. The witness binary runs `PublicWitnessDispatcher`; the store binary runs `StoreProxyService`. Restart/lost-response cases cover fence, establish, prepare, commit, abort, reads, and fetch; session rotation uses signed `RecoveryChallengeV1`/`WitnessSessionRotationReceiptV1` and never highest-sequence or nonce-memory inference. Verify missing service, wrong account/credential, invalid nested signature, replay, timeout, store conflict, or init interruption returns typed failure and cannot activate an in-process/memory/unsigned witness. After all source and manifest targets exist, run `bash tools/check-witness-dependency-closure.sh --all-targets`; it must parse `cargo metadata` to enumerate the library plus the three exact binaries, run target-aware locked checks, report positive counts for each target, and reject any normal edge to runtime/http/ingest/agents/workbench or unrelated authority crates. Register exact service checkpoint names; do not use an undefined aggregate selector.</action>
  <acceptance_criteria>All three binaries enforce their role-specific closed configuration; `RuntimeWitnessClient` implements every required trait method and validates nested attestations; init cannot serve requests after readiness; restart/lost-response paths use signed discovery/session rotation and bounded retries; missing or unauthorized service fails closed with no local/optional fallback.</acceptance_criteria>
  <verify>
    <automated>bash tools/with-nats-jetstream.sh bash tools/check-phase285-witness-conformance.sh service-checkpoint && bash tools/check-witness-dependency-closure.sh --all-targets</automated>
    The checker must show every service checkpoint case executed > 0 with passed = executed, failed = 0, ignored = 0, including current/predecessor/prepared/aborted/evicted/stale-replica reads, every max/max-plus-one family, and beyond-retention execution; reject unavailable process, zero target, ignored/skipped test, missing rotation, omitted boundary, credential swap, or stale response fixture. Then run `bash tools/check-witness-dependency-closure.sh --all-targets` and require one positively checked library target plus the three exact binary targets.
  </verify>
  <done>Task 285-04-03 closes the service-layer checkpoint and provides the only runtime witness implementation consumed by Plan 05. External GitHub App protection remains explicitly deferred.</done>
</task>

</tasks>

<verification>
Run the three frozen Slice D selectors through the pinned NATS harness. Confirm all nine operations, private three-operation proxy, three accounts, bounded queues, retry/session rotation, process restart, and nested response/failure signatures are exercised with positive counts. Obtain an independent zero-P0/P1/P2 review; treat this as a slice checkpoint only and keep Phases 286-289 blocked.
</verification>

<success_criteria>
Public-dispatcher, full-service-path, and service-checkpoint lanes are non-vacuously green; only the private store process can access raw JetStream; runtime receives typed attested outcomes via `GovernanceDurabilityWitness`; no local/optional/unsigned fallback exists in enforced service mode; and no external GitHub App check is claimed.
</success_criteria>

<output>
After completion, create `.planning/phases/285-assurance-foundation-closure/285-04-SUMMARY.md`.
</output>
