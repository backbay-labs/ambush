---
phase: 285-assurance-foundation-closure
plan: "06A"
type: execute
wave: 9
depends_on:
  - "285-05C"
files_modified:
  - crates/swarm-governance/src/lib.rs
  - crates/swarm-governance/src/persistence_coordinator.rs
  - crates/swarm-governance/src/persistence_coordinator/retention.rs
  - crates/swarm-governance/src/persistence_recovery.rs
  - crates/swarm-governance/tests/phase285_governance_persistence.rs
autonomous: true
requirements:
  - ASSURE-04
  - ASSURE-06
must_haves:
  truths:
    - "The successful automatic retention path advances only through Reserved -> SourceBound -> CandidateCopied -> QuarantineMoved -> ReadyToRetain -> Retained, and the post-commit barrier appends Uncertain when a final seam changes; ForeignPreserved is a separate proof-bearing preservation terminal and can never mask or replace Uncertain."
    - "A full or uncertain cleanup pool refuses before source movement, with exact signed binding/state/checkpoint identity and limit checks; ordinary startup never drains, resets, rebinds, or evicts slots."
    - "Every retirement retains a complete slot proof including slot name/inode/digest/length, record head, source/destination/parent/pool/archive identities, and before/after mapping."
  artifacts:
    - path: "crates/swarm-governance/src/persistence_coordinator/retention.rs"
      provides: "Bounded retention state machine, slot proof, post-commit barrier, and typed exhaustion."
      contains: "ReadyToRetain"
    - path: "crates/swarm-governance/src/persistence_coordinator.rs"
      provides: "Authenticated cleanup-pool binding validation and retention guard orchestration."
      contains: "CleanupPoolBindingV1"
    - path: "crates/swarm-governance/tests/phase285_governance_persistence.rs"
      provides: "Retention/barrier/pool exhaustion/binding restart mutation target."
      contains: "retention_pool_exhaustion_refuses_before_source_move"
  key_links:
    - from: "RetentionStateV1::ReadyToRetain"
      to: "RetentionStateV1::Retained / Uncertain"
      via: "final slot/source/canonical/parent/pool validation then post-commit identity barrier"
      pattern: "ReadyToRetain.*Retained.*Uncertain"
    - from: "AuthorityCleanupRetirementV1"
      to: "CleanupPoolBindingV1"
      via: "complete per-slot identity/digest/length/head/mapping proof is checked before every append, move, recovery decision, and return"
      pattern: "slot_name.*slot_inode.*record_chain_head.*before_mapping.*after_mapping"
    - from: "CleanupPoolCapacityV1"
      to: "GovernancePersistenceCoordinator"
      via: "capacity and uncertainty are checked before source move under selector/state/authority/pool lock order"
      pattern: "pool_exhausted.*before.*source"
---

<objective>
Implement bounded cleanup retention and its authenticated pool handoff.

Purpose: Retention is a second publication protocol, not best-effort file cleanup. It must preserve evidence, refuse exhaustion before moving a source, and make post-commit seam races explicitly uncertain.
Output: Exact retention state machine, complete per-slot proof, signed binding/state/checkpoint validation, typed pool exhaustion, and no-startup-maintenance behavior.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/285-assurance-foundation-closure/285-CONTEXT.md
@.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md
@.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md
@.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md
@<.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md>
@crates/swarm-governance/src/persistence_coordinator.rs
@crates/swarm-governance/src/fixed_lanes.rs
@crates/swarm-governance/src/persistence_recovery.rs
@crates/swarm-governance/tests/phase285_governance_persistence.rs

Consume the exact `CleanupPoolBindingV1` created by Plan05A; do not add a smaller binding or pathname-derived slot fallback. Offline drain/reset and their signed maintenance journal belong to Plan06B.
</context>

<tasks>

<task type="auto">
  <wave>9</wave>
  <name>Task 285-06-01: Implement bounded retention, complete slot proofs, and post-commit uncertainty</name>
  <files>crates/swarm-governance/src/lib.rs, crates/swarm-governance/src/persistence_coordinator.rs, crates/swarm-governance/src/persistence_coordinator/retention.rs, crates/swarm-governance/src/persistence_recovery.rs, crates/swarm-governance/tests/phase285_governance_persistence.rs</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-GOVERNANCE-PERSISTENCE-PROTOCOL.md (retention phases, post-commit barrier, pool exhaustion); .planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md (retained-byte bounds); .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; crates/swarm-governance/src/persistence_coordinator.rs; crates/swarm-governance/src/persistence_recovery.rs; crates/swarm-governance/src/fixed_lanes.rs; crates/swarm-governance/src/lib.rs</read_first>
  <action>Work red-first by adding exact cases `retention_uses_exact_state_machine_and_post_commit_barrier`, `retention_uncertain_overrides_retained_after_final_seam_change`, `retention_pool_exhaustion_refuses_before_source_move`, `cleanup_pool_binding_survives_restart_and_rejects_namespace_replacement`, and `retention_foreign_preserved_requires_verified_preservation_and_never_masks_uncertain`. Implement `RetentionStateV1::{Reserved, SourceBound, CandidateCopied, QuarantineMoved, ReadyToRetain, Retained, ForeignPreserved, Uncertain}` and `AuthorityCleanupRetirementV1` with immutable `slot_name`, held `slot_inode`, slot content digest and length, signed record-chain head, `source_identity`, `destination_identity`, `parent_identity`, `pool_identity`, `archive_identity`, and complete `before_mapping`/`after_mapping`. `ForeignPreserved` is permitted only when an exact signed preservation record proves the foreign source bytes, no-replace destination, complete identity/digest/length/head/mapping fields, and unchanged governed canonical; it is not a success transition, cannot be synthesized from malformed evidence, and must never convert, mask, or outrank `Uncertain`. Consume Plan05A's full `CleanupPoolBindingV1` and compare the signed pool-local binding with the identical signed state and checkpoint binding before allocation, on restart, and before every record append, content move, recovery decision, and API return. The comparison covers schema/domain, binding-file identity, parent/pool/lock identities, namespace generation, fixed pool directory name, exact slot names/count, state/checkpoint staging lanes, immutable state/checkpoint/journal inode pairs, all configured byte/record limits, external witness identity/stream binding, and signatures/digests. On every retention operation acquire selector/quiescence -> state -> authority-pair -> cleanup-pool guards, enumerate the complete namespace, verify expected regular-file types and held identities, and refuse unknown/missing/renamed/replaced entries. Advance the successful path exactly `Reserved -> SourceBound -> CandidateCopied -> QuarantineMoved -> ReadyToRetain -> Retained`; `ReadyToRetain` is not terminal. Before writing the `Retained` commit record, revalidate slot name/inode/digest/length, record head, source/destination/parent/pool/archive identities, canonical absence, binding/state/checkpoint equality, limits, and before/after mapping. Immediately after the commit record, perform a post-commit barrier and append durable `Uncertain` if the canonical name, slot binding, parent, pool, or any held identity changed; never rewrite it as Retained or ForeignPreserved. `CleanupPoolCapacityV1` must reject full, uncertain, malformed, or generation/size-overflow pools before any source move, and every cleanup/fsync error remains typed and observable. Normal startup/load/recovery must not drain, reset, rebind, evict, or create a missing pool. Mutations cover every binding field/copy, slot name/inode/digest/length/head, source/destination/parent/pool/archive identity, mapping, hard-link/symlink alias, unknown namespace entry, capacity boundary, lock order, final-seam replacement, post-commit race, invalid ForeignPreserved conversion, fsync failure, and repeated retry; all refusals assert source and namespace bytes remain unchanged.</action>
  <verify>
    <automated>bash tools/check-phase285-governance-persistence.sh retention</automated>
    The wrapper must execute all five exact retention/binding/preservation cases with `passed = executed`, `failed = 0`, and `ignored = 0`; pool exhaustion and every final-seam mutation must fail before source movement or produce durable `Uncertain` with preserved evidence, and ForeignPreserved can never mask uncertainty.
  </verify>
  <done>Retention is a bounded authenticated state machine with complete per-slot proof, signed binding copies, typed pre-mutation exhaustion, exact lock/identity checks, and post-commit Uncertain precedence; ordinary startup has no maintenance fallback.</done>
</task>

</tasks>

<verification>
Run the `retention` selector, governance package tests, strict clippy, formatting, and `git diff --check`. Inspect journal/state outputs for the six-state successful path plus `ForeignPreserved` and `Uncertain`, and verify a full pool never moves a source. Obtain an independent zero-P0/P1/P2 review before Plan06B.
</verification>

<success_criteria>
All retention and binding mutation cases execute positively; exact slot and identity proof is persisted; `Retained` follows final validation and can be superseded only by durable `Uncertain`; full/uncertain pools refuse before movement; and no normal startup drains, resets, rebinds, or discards cleanup errors.
</success_criteria>

<output>
After completion, create `.planning/phases/285-assurance-foundation-closure/285-06A-SUMMARY.md`.
</output>
