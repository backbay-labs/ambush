---
phase: 285-assurance-foundation-closure
plan: "07A"
type: execute
wave: 12
depends_on:
  - "285-06C"
files_modified:
  - Dockerfile
  - deploy/helm/swarm-team-six/Chart.yaml
  - deploy/helm/swarm-team-six/values.yaml
  - deploy/helm/swarm-team-six/values-production.yaml
  - deploy/helm/swarm-team-six/templates/_helpers.tpl
  - deploy/helm/swarm-team-six/templates/configmap.yaml
  - deploy/helm/swarm-team-six/templates/secret.yaml
  - deploy/helm/swarm-team-six/templates/deployment.yaml
  - deploy/helm/swarm-team-six/templates/service.yaml
  - deploy/helm/swarm-team-six/templates/pvc.yaml
  - deploy/helm/swarm-team-six/charts/nats/values.yaml
  - deploy/helm/swarm-team-six/charts/nats/templates/_helpers.tpl
  - deploy/helm/swarm-team-six/charts/nats/templates/statefulset.yaml
  - deploy/helm/swarm-team-six/charts/nats/templates/service.yaml
  - tools/check-phase285-deployment.sh
autonomous: true
requirements:
  - ASSURE-03
  - ASSURE-05
  - ASSURE-06
must_haves:
  truths:
    - "Bootstrap and serving Helm renders isolate runtime, public witness, store proxy, and one-shot init identities with disjoint credentials/mounts, exact NATS accounts/imports, pinned image/configuration, and external epoch/anchor."
    - "The serving render has no init Job, init Secret, init user, or creation authority, and no public process can reach raw KV or JetStream management subjects."
    - "Both render validation and an executable live kind/NATS harness reject account, mount, image, bucket, init, anchor, and network mutations; render-only output is never labeled live."
  artifacts:
    - path: "Dockerfile"
      provides: "Role-specific runtime, public witness, store proxy, and init image targets containing only their intended executable."
      contains: "swarm-governance-witness-init"
    - path: "deploy/helm/swarm-team-six/templates/deployment.yaml"
      provides: "Runtime/public witness/store deployment identity and lifecycle isolation."
      contains: "swarm-governance-witness"
    - path: "deploy/helm/swarm-team-six/charts/nats/templates/statefulset.yaml"
      provides: "Pinned NATS server/accounts/imports and exact witness bucket configuration."
      contains: "2.11.17"
    - path: "tools/check-phase285-deployment.sh"
      provides: "Separate Helm-render and executable kind/NATS live isolation checks with temporary bad manifests."
      contains: "kind create cluster"
  key_links:
    - from: "deploy/helm/swarm-team-six/templates/deployment.yaml"
      to: "deploy/helm/swarm-team-six/charts/nats/templates/statefulset.yaml"
      via: "four workload identities receive only their role-specific account, import, mount, and anchor capabilities"
      pattern: "swarm-governance-witness.*witness-store.*runtime.*init"
    - from: "tools/check-phase285-deployment.sh"
      to: "helm template and kind/kubectl live harness"
      via: "rendered and live results are separately labeled and each mutation has an expected refusal"
      pattern: "helm template.*kind create cluster.*kubectl rollout status"
    - from: "sealed external epoch/anchor"
      to: "NATS stream raw-config digest"
      via: "serving pods mount read-only anchor and startup compares exact pinned stream projection"
      pattern: "bucket_epoch.*bucket_anchor.*raw_stream_configuration_digest"
---

<objective>
Implement Slice G deployment isolation and distinguish Helm render evidence from executable live validation.

Purpose: Deployment configuration is part of the witness TCB. A valid YAML render does not prove account/network/mount behavior, so this plan requires both static render controls and a real kind/NATS execution lane.
Output: Isolated Helm bootstrap/serving configuration and a checker with temporary mutation fixtures plus live kind/NATS probes.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/285-assurance-foundation-closure/285-CONTEXT.md
@.planning/phases/285-assurance-foundation-closure/285-RESEARCH.md
@.planning/phases/285-assurance-foundation-closure/285-VALIDATION.md
@.planning/phases/285-assurance-foundation-closure/285-VERIFICATION.md
@.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md
@Dockerfile
@deploy/helm/swarm-team-six/Chart.yaml
@deploy/helm/swarm-team-six/values.yaml
@deploy/helm/swarm-team-six/values-production.yaml
@deploy/helm/swarm-team-six/templates/deployment.yaml
@deploy/helm/swarm-team-six/templates/configmap.yaml
@deploy/helm/swarm-team-six/templates/secret.yaml
@deploy/helm/swarm-team-six/charts/nats/values.yaml
@deploy/helm/swarm-team-six/charts/nats/templates/statefulset.yaml
@tools/check-phase285-deployment.sh

Bad fixtures and Helm dependency builds are generated in temporary directories and deleted after the checker; do not add a checked-in fixture tree, `Chart.lock`, or packaged subchart. The exact NATS image is `docker.io/library/nats:2.11.17-alpine@sha256:e4bf19f15fd3218814a4e3c9e0064e1334bd8aa20d5984b9f1a0afd084f8cc00`. The live CLI probe image is independently pinned as `docker.io/natsio/nats-box:0.18.0@sha256:abdc9f9f0120bb8adfbf674eb037d1551db55356eb198b7bd4ffed377f6950a6`. Render output and live kind/NATS output must be separate evidence classes.
</context>

<tasks>

<task type="auto">
  <wave>12</wave>
  <name>Task 285-07-01: Render isolated bootstrap/serving configuration and mutations</name>
  <files>deploy/helm/swarm-team-six/Chart.yaml, deploy/helm/swarm-team-six/values.yaml, deploy/helm/swarm-team-six/values-production.yaml, deploy/helm/swarm-team-six/templates/_helpers.tpl, deploy/helm/swarm-team-six/templates/configmap.yaml, deploy/helm/swarm-team-six/templates/secret.yaml, deploy/helm/swarm-team-six/templates/deployment.yaml, deploy/helm/swarm-team-six/templates/service.yaml, deploy/helm/swarm-team-six/templates/pvc.yaml, deploy/helm/swarm-team-six/charts/nats/values.yaml, deploy/helm/swarm-team-six/charts/nats/templates/_helpers.tpl, deploy/helm/swarm-team-six/charts/nats/templates/statefulset.yaml, deploy/helm/swarm-team-six/charts/nats/templates/service.yaml, tools/check-phase285-deployment.sh</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md (Helm identities, accounts, imports/exports, mounts, lifecycle, bucket/image/anchor requirements); .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; Dockerfile; deploy/helm/swarm-team-six/Chart.yaml; deploy/helm/swarm-team-six/values.yaml; deploy/helm/swarm-team-six/values-production.yaml; deploy/helm/swarm-team-six/templates/_helpers.tpl; deploy/helm/swarm-team-six/templates/configmap.yaml; deploy/helm/swarm-team-six/templates/secret.yaml; deploy/helm/swarm-team-six/templates/deployment.yaml; deploy/helm/swarm-team-six/templates/service.yaml; deploy/helm/swarm-team-six/templates/pvc.yaml; deploy/helm/swarm-team-six/charts/nats/Chart.yaml; deploy/helm/swarm-team-six/charts/nats/values.yaml; deploy/helm/swarm-team-six/charts/nats/templates/statefulset.yaml; deploy/helm/swarm-team-six/charts/nats/templates/service.yaml; tools/check-phase285-deployment.sh</read_first>
  <action>Red-first: generate temporary invalid manifests for a shared secret mount, account/credential swap, floating or wrong NATS/probe/application image, wrong workload command, wrong bucket history/storage/discard/direct/delete/purge/rollup setting, recreated init Job/user/Secret in serving mode, missing external epoch/anchor, wildcard/raw JetStream import, and missing network isolation; require each render mutation to fail for the intended reason before accepting valid output. Add four explicit workloads—runtime `swarm-detect`, public `swarm-governance-witness`, private `swarm-governance-witness-store`, and one-shot `swarm-governance-witness-init`—each with distinct ServiceAccount, Secret/mount set, TLS CA/server name, exact image/command, and `automountServiceAccountToken: false`. Render NATS accounts `runtime`, `witness`, and `witness-store` with only nine public imports and three private imports, bounded `allow_responses`, and no raw `$KV.>`/`$JS.API.>` wildcard export. Mount admitted config/public key/external epoch/sealed anchor read-only in witness; raw JetStream credential only in store; temporary creation/signing authority only in init. Pin every NATS server image to `docker.io/library/nats:2.11.17-alpine@sha256:e4bf19f15fd3218814a4e3c9e0064e1334bd8aa20d5984b9f1a0afd084f8cc00`, require server 2.11.17, and project history=1, file, Limits, discard New, allow_direct=false, deny_delete=true, deny_purge=true, allow_rollup=false, fixed size/replica/ack settings, and absent raw persist_mode. Serving render must contain the sealed anchor and no init Job, init Secret, init user, or creation authority. Implement only the `render` mode of `check-phase285-deployment.sh` in this task: copy the complete chart to `mktemp -d`, run `helm dependency build` and `helm template` only in that copy, generate each named mutation there, require exact positive/mutation counts, and prove the source tree gained no `Chart.lock`, packaged subchart, or other byte. The `live` mode remains a deliberate missing-target failure until Task07-02; render output must never claim executable isolation.</action>
  <acceptance_criteria>The static render has four isolated workload identities and three exact accounts; serving mode has no init authority; exact pinned image references, configuration, epoch, anchor, and bucket settings pass; every temporary manifest mutation fails; the source chart remains byte-clean; and render output explicitly carries no live-execution claim.</acceptance_criteria>
  <verify>
    <automated>bash tools/check-phase285-deployment.sh render</automated>
    The checker must execute every static render mutation with executed > 0, passed = executed, failed = 0, ignored = 0; tag-only image, wrong bucket, shared credential, init recreation, missing anchor, wildcard import, source-chart mutation, or render output relabeled as live must fail.
  </verify>
  <done>Task 285-07-01 supplies non-vacuous static render evidence and preserves the source chart; it makes no live-execution claim.</done>
</task>

<task type="auto">
  <wave>13</wave>
  <name>Task 285-07-02: Build role-specific images and execute live kind/NATS isolation</name>
  <files>Dockerfile, tools/check-phase285-deployment.sh</files>
  <read_first>.planning/phases/285-assurance-foundation-closure/285-WITNESS-ADAPTER-CONTRACT.md (role images, executable account isolation, live NATS requirements); .planning/phases/285-assurance-foundation-closure/285-VALIDATION.md; Dockerfile; deploy/helm/swarm-team-six/Chart.yaml; deploy/helm/swarm-team-six/values.yaml; deploy/helm/swarm-team-six/values-production.yaml; deploy/helm/swarm-team-six/templates/deployment.yaml; deploy/helm/swarm-team-six/templates/secret.yaml; deploy/helm/swarm-team-six/charts/nats/values.yaml; deploy/helm/swarm-team-six/charts/nats/templates/statefulset.yaml; tools/check-phase285-deployment.sh</read_first>
  <action>Red-first: add exact live cases for four image/command identities, runtime-to-public success, public-to-private success, runtime-to-private/raw refusal, init authority revocation, store-only raw CAS, sealed anchor/stream readiness, wrong account/credential/mount, missing network isolation, floating/wrong image or command, render-only substitution, and unavailable Docker/kind/kubectl/Helm/NATS/probe. Extend `Dockerfile` with four role-specific final targets containing exactly one intended executable apiece: runtime `swarm-detect`, public `swarm-governance-witness`, private `swarm-governance-witness-store`, and one-shot `swarm-governance-witness-init`; the three witness targets must not contain full runtime binaries or runtime/http/ingest/agents/workbench dependency closure. Implement the separate `live` mode in `check-phase285-deployment.sh`: build the four exact targets with deterministic local tags, create a fresh kind cluster, load every image, install the already-frozen chart with `imagePullPolicy: Never`, and assert every Pod image, command, mount, ServiceAccount, and account subject permission. Create a separate probe Pod/Job using exact `docker.io/natsio/nats-box:0.18.0@sha256:abdc9f9f0120bb8adfbf674eb037d1551db55356eb198b7bd4ffed377f6950a6`, mount explicit least-privilege probe credentials, and execute `kubectl exec <probe> -- nats server info` plus exact positive and negative account/subject probes; never invoke the absent CLI in the NATS server container. Check rollout, one-shot init exit and credential revocation, anchor/stream creation, NATS 2.11.17 image/config digest, image/command identity, restart persistence, and deterministic cleanup. If any live dependency is unavailable or any case is zero/skipped/ignored, fail rather than reuse render evidence. The no-argument deployment command must run `render` then `live`, preserve separate result classes, and require both.</action>
  <verify>
    <automated>bash tools/check-phase285-deployment.sh live</automated>
    The live lane must execute every image/account/subject/mount/init/anchor/restart mutation with executed > 0, passed = executed, failed = 0, ignored = 0; unavailable infrastructure, wrong binary, raw-access leak, shared credential, skipped probe, or render-result substitution must fail.
  </verify>
  <done>Task 285-07-02 supplies real kind/NATS isolation evidence for the four frozen role images and keeps it distinct from Helm render evidence.</done>
</task>

</tasks>

<verification>
Run the deployment checker and retain separate render/live outputs. Confirm live kind/NATS probes execute, not merely Helm rendering, and that all bad manifests are temporary, expected-failure controls.
</verification>

<success_criteria>
Deployment render and live isolation lanes are both non-vacuously green; exact pinned NATS and four-role lifecycle constraints hold; and no evidence ledger or checkpoint file is added to the future subject tree.
</success_criteria>

<output>
After completion, create `.planning/phases/285-assurance-foundation-closure/285-07A-SUMMARY.md`.
</output>
