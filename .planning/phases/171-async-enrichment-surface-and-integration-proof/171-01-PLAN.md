---
phase: 171-async-enrichment-surface-and-integration-proof
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 170
files_modified:
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/runtime_events.rs
  - crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs
  - crates/swarm-runtime/tests/critical_path_integration.rs
autonomous: true
requirements:
  - ASYNC-03
  - ASYNC-04
must_haves:
  truths:
    - "Async backlog, pressure, confidence-vote state, and correlation outcomes must be operator-visible."
    - "The milestone must end with one bounded detect-to-review integration proof across the async lane."
    - "Failure modes in the async lane must stay visible and bounded instead of silently degrading."
  artifacts:
    - path: "crates/swarm-runtime/src/service.rs"
      provides: "Runtime-visible async queue and correlation status shaping."
    - path: "crates/swarm-runtime/src/ingest.rs"
      provides: "Serve-surface exposure of async status where appropriate."
    - path: "crates/swarm-runtime/tests/critical_path_integration.rs"
      provides: "Milestone-level bounded async-lane integration proof."
---

<objective>
Complete Phase 171 by exposing async-lane status as a first-class operator surface and proving the bounded detect → investigate → correlate → operator-review path end to end.

Purpose: finish `v1.50` with a visible and testable async product surface instead of a hidden background subsystem.
Output: async status surfaces, visible failure/backpressure semantics, and milestone closeout integration proof.
</objective>

<context>
@.planning/phases/171-async-enrichment-surface-and-integration-proof/171-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/service.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/control.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Surface async-lane queue and correlation status</name>
  <files>
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/control.rs
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/runtime_events.rs
  </files>
  <action>Add first-class runtime status for async backlog, queue pressure, confidence-vote state, recent investigation outcomes, and correlation results through the shipped operator surfaces.</action>
  <done>Operators can inspect the async lane without reading implicit logs or internal store state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make async failure modes visible and bounded</name>
  <files>
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/ingest.rs
  </files>
  <action>Ensure queue failure, backpressure, timeout, or degraded-correlation conditions surface clearly through existing runtime status and health semantics instead of failing silently.</action>
  <done>The async lane degrades visibly and remains bounded under failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the full bounded async path</name>
  <files>
    crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs
    crates/swarm-runtime/tests/critical_path_integration.rs
  </files>
  <action>Add end-to-end proof that a detection can drive investigation, richer correlation, and operator-visible incident context through the shipped async lane without widening the hot path or action authority.</action>
  <done>The milestone closes with one durable integration proof for the async enrichment lifecycle.</done>
</task>

</tasks>

<verification>
Keep the phase surface-and-proof focused. Assurance gating, Providence reconciliation, and production packaging belong to later milestones.
</verification>
