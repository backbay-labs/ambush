---
phase: 122-cross-strategy-pheromone-signals-and-rollout-scoping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/swarm-whisker/src/stream.rs
  - crates/swarm-runtime/src/detection/pipeline.rs
  - crates/swarm-runtime/src/correlation.rs
  - crates/swarm-pheromone/src/substrate.rs
  - crates/swarm-runtime/tests/escalation_integration.rs
  - crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs
autonomous: true
requirements:
  - COMPOSE-03
  - COMPOSE-04

must_haves:
  truths:
    - "Detection deposits use a strategy-scoped source identity derived from the base agent_id plus finding.strategy_id before signing or persistence"
    - "Cross-strategy deposits from one base whisker agent increment distinct_sources independently, while repeated same-strategy deposits still collapse to one source"
    - "Canary and promotion preview deposits use the same strategy-scoped identity format as live runtime deposits"
    - "Correlation scoring ignores raw strategy:* key inflation, requires at least one real non-strategy overlap, and only then applies a bounded cross-strategy bonus so same-strategy duplication no longer dominates inclusion"
  artifacts:
    - path: "crates/swarm-whisker/src/stream.rs"
      provides: "Shared strategy-scoped deposit identity helper reused by runtime and rollout preview deposits"
      contains: "strategy_scoped_agent_id"
    - path: "crates/swarm-runtime/src/correlation.rs"
      provides: "Explicit pair-scoring helper for weighted cross-strategy correlation with a real-overlap gate"
      contains: "weighted_score"
    - path: "crates/swarm-runtime/tests/escalation_integration.rs"
      provides: "Integration proof that multi-strategy deposits escalate while same-strategy duplicates do not"
      contains: "cross_strategy"
    - path: "crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs"
      provides: "Regression coverage updated for strategy-scoped whisker deposit identities"
      contains: "whisker-primary"
  key_links:
    - from: "crates/swarm-runtime/src/detection/pipeline.rs"
      to: "crates/swarm-pheromone/src/substrate.rs"
      via: "strategy-scoped deposit.agent_id feeds distinct_sources concentration math without any substrate schema change"
      pattern: "agent_id"
    - from: "crates/swarm-whisker/src/stream.rs"
      to: "crates/swarm-runtime/src/canary.rs"
      via: "findings_to_deposits keeps rollout preview deposits aligned with runtime deposit identity"
      pattern: "findings_to_deposits"
    - from: "crates/swarm-runtime/src/correlation.rs"
      to: "crates/swarm-runtime/src/investigation.rs"
      via: "pair scoring uses bundle.strategy_id and ignores strategy:* from the raw shared-key count"
      pattern: "strategy:"
---

<objective>
Implement the runtime signal changes for COMPOSE-03 and COMPOSE-04: make strategy-specific deposits count as independent pheromone sources, and make incident assembly prefer corroboration across strategies over same-strategy duplication.

Purpose: Make the swarm metaphor materially true now that CompositeDetector exists.
Output: Strategy-scoped deposit identity, weighted cross-strategy correlation decisions, and tests proving the new escalation and correlation behavior.
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/122-cross-strategy-pheromone-signals-and-rollout-scoping/122-CONTEXT.md
@.planning/phases/122-cross-strategy-pheromone-signals-and-rollout-scoping/122-RESEARCH.md

<interfaces>
<!-- Runtime contracts this plan changes without widening storage schemas -->

From `crates/swarm-whisker/src/detector.rs`:
```rust
pub struct DetectionFinding {
    pub finding_id: String,
    pub event_id: String,
    pub threat_class: ThreatClass,
    pub severity: Severity,
    pub confidence: f64,
    pub evidence: serde_json::Value,
    pub strategy_id: String,
}
```

From `crates/swarm-runtime/src/detection/pipeline.rs` (current):
```rust
let mut deposits = resolve_deposits(substrate, &findings, event, agent_id, pheromone).await?;
for deposit in &mut deposits {
    sign_deposit(deposit, signing_key)?;
    substrate.deposit(deposit.clone()).await?;
}
```

From `crates/swarm-whisker/src/stream.rs` (current):
```rust
pub fn findings_to_deposits(
    findings: &[DetectionFinding],
    event: &TelemetryEvent,
    agent_id: &AgentId,
    pheromone: &PheromoneConfig,
) -> Vec<PheromoneDeposit>;
```

From `crates/swarm-pheromone/src/substrate.rs`:
```rust
for deposit in deposits.iter().filter(|deposit| &deposit.threat_class == threat_class) {
    if deposit.is_evaporated(now, policy.evaporation_threshold) {
        continue;
    }
    sources.insert(deposit.agent_id.0.clone());
}
```

From `crates/swarm-runtime/src/correlation.rs` (current):
```rust
let shared_keys = shared_keys(seed, candidate);
if candidate.status != InvestigationStatus::Completed {
    Err("investigation not completed".to_string())
} else if shared_keys.len() < self.config.min_shared_keys {
    Err("insufficient shared correlation keys".to_string())
} else if time_delta_ms > self.config.time_window_ms {
    Err("outside correlation time window".to_string())
} else {
    Ok(format!("shared {} within correlation window", shared_keys.join(", ")))
}
```

From `crates/swarm-runtime/src/investigation.rs`:
```rust
correlation_keys.push(format!("strategy:{}", detection.strategy_id));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stamp strategy-scoped deposit identities before signing</name>
  <files>
    crates/swarm-whisker/src/stream.rs
    crates/swarm-runtime/src/detection/pipeline.rs
  </files>
  <read_first>
    crates/swarm-whisker/src/stream.rs
    crates/swarm-runtime/src/detection/pipeline.rs
    crates/swarm-core/src/pheromone.rs
  </read_first>
  <behavior>
    - Deposits for the same base agent and same strategy reuse the same `agent_id`
    - Deposits for the same base agent and different strategies use distinct `agent_id` values
    - Runtime deposits compute the final strategy-scoped `agent_id` before `sign_deposit()`
    - Canary and promotion preview deposits reuse the same identity rule instead of a second format
  </behavior>
  <action>
    1. Update `crates/swarm-whisker/src/stream.rs`:
       - Add a small shared helper such as `pub fn strategy_scoped_agent_id(base: &AgentId, strategy_id: &str) -> AgentId`.
       - Format the source identity as `{base_agent_id}:{strategy_id}`.
       - Reuse this helper inside `findings_to_deposits()` so every preview deposit already carries the strategy suffix.
       - Expand the local tests to assert:
         - one finding becomes one deposit with `agent_id == "whisker-a:suspicious_process_tree"`,
         - repeated findings from the same strategy keep the same scoped ID,
         - two findings from different strategies produce different scoped IDs.

    2. Update `crates/swarm-runtime/src/detection/pipeline.rs`:
       - Import the shared helper from `swarm_whisker::stream`.
       - In `resolve_deposits()`, derive `agent_id` from `finding.strategy_id` instead of cloning the raw runtime agent ID.
       - Keep the current threat-class policy resolution unchanged.
       - Do not mutate deposits after signing; the scoped ID must already be final when `sign_deposit()` runs.
       - Add a unit test that feeds two findings with the same base agent and different `strategy_id` values through `detect_and_deposit()` and asserts:
         - two deposits are written,
         - the resulting `agent_id`s differ,
         - both signatures validate because the scoped ID was present at signing time.
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/swarm-team-six && cargo test -p swarm-whisker --lib && cargo test -p swarm-runtime --lib detection::pipeline</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "strategy_scoped_agent_id" crates/swarm-whisker/src/stream.rs
    - grep -q "finding.strategy_id" crates/swarm-runtime/src/detection/pipeline.rs
    - grep -q "sign_deposit" crates/swarm-runtime/src/detection/pipeline.rs
    - cargo test -p swarm-whisker --lib exits 0
    - cargo test -p swarm-runtime --lib detection::pipeline exits 0
  </acceptance_criteria>
  <done>Runtime and rollout-preview deposits both use the same strategy-scoped source identity, and deposit signing still succeeds because the final ID is set before persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Weight cross-strategy correlation and prove distinct-source escalation</name>
  <files>
    crates/swarm-runtime/src/correlation.rs
    crates/swarm-pheromone/src/substrate.rs
    crates/swarm-runtime/tests/escalation_integration.rs
  </files>
  <read_first>
    crates/swarm-runtime/src/correlation.rs
    crates/swarm-runtime/src/investigation.rs
    crates/swarm-pheromone/src/substrate.rs
    crates/swarm-runtime/tests/escalation_integration.rs
  </read_first>
  <behavior>
    - `concentration_for()` counts `whisker-primary:suspicious_process_tree` and `whisker-primary:dns_exfiltration` as two distinct sources
    - Repeated deposits from `whisker-primary:suspicious_process_tree` still count as one distinct source
    - Correlation inclusion requires at least one shared non-strategy key and then uses an explicit `+1` cross-strategy bonus
    - Same-strategy pairs cannot satisfy the threshold purely because both bundles share `strategy:{strategy_id}`
    - Incident member reasons stay human-readable and explain the weighted decision
  </behavior>
  <action>
    1. Update `crates/swarm-runtime/src/correlation.rs`:
       - Add a small helper (for example `score_candidate_pair`) that returns:
         - the full shared-key list to preserve in `IncidentMemberDecision`,
         - the non-strategy shared-key count used for scoring,
         - whether the pair is cross-strategy,
         - the final weighted score.
       - Exclude keys beginning with `strategy:` from the scoreable shared-key count.
       - Compute `weighted_score = shared_non_strategy_keys + usize::from(seed.strategy_id != candidate.strategy_id)`.
       - Require `shared_non_strategy_keys >= 1` before the pair can pass; the bonus must never be sufficient on its own.
       - Compare `weighted_score` against `self.config.min_shared_keys` only after that real-overlap gate.
       - Update the accept/reject reasons so operators can see why a pair passed or failed, including when a cross-strategy bonus was applied.
       - Add correlation unit tests that prove:
         - a cross-strategy candidate can satisfy `min_shared_keys` with one real shared key plus the bonus,
         - a cross-strategy candidate with zero real shared keys is still rejected,
         - a same-strategy candidate with only the `strategy:*` overlap is rejected,
         - time-window and completion-status gates still behave exactly as before.

    2. Update `crates/swarm-pheromone/src/substrate.rs`:
       - Add a concentration regression test that proves:
         - `whisker-primary:suspicious_process_tree` plus `whisker-primary:dns_exfiltration` yields `distinct_sources == 2`,
         - two deposits from `whisker-primary:suspicious_process_tree` still yield `distinct_sources == 1`.
       - Do not change the production concentration math beyond the test coverage; the runtime deposit identity change is the feature.

    3. Update `crates/swarm-runtime/tests/escalation_integration.rs`:
       - Add an integration test that uses `detect_and_deposit()` with a lightweight test detector returning multiple findings for one event.
       - Prove that two findings with different `strategy_id` values from one base whisker agent satisfy `min_sources_for_escalation`.
       - Add the control case where the same number of repeated same-strategy findings does not escalate because `distinct_sources` remains `1`.
       - Keep the existing monitor-based assertions on alert/incident transitions and persisted escalation records.

    4. Update `crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs`:
       - replace exact string assertions that expect `deposit.agent_id == "whisker-primary"` with prefix- or strategy-aware assertions
       - prove the live whisker deposit identity still carries the base whisker role while allowing the strategy suffix required by COMPOSE-03
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/swarm-team-six && cargo test -p swarm-pheromone --lib substrate && cargo test -p swarm-runtime --lib correlation && cargo test -p swarm-runtime --test escalation_integration && cargo test -p swarm-runtime --test multi_agent_pipeline_integration && cargo clippy -p swarm-runtime -p swarm-whisker -p swarm-pheromone -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "weighted_score" crates/swarm-runtime/src/correlation.rs
    - grep -q "strategy:" crates/swarm-runtime/src/correlation.rs
    - grep -q "distinct_sources == 2" crates/swarm-pheromone/src/substrate.rs
    - grep -q "cross_strategy" crates/swarm-runtime/tests/escalation_integration.rs
    - grep -q "whisker-primary" crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs
    - cargo test -p swarm-runtime --test escalation_integration exits 0
    - cargo test -p swarm-runtime --test multi_agent_pipeline_integration exits 0
    - cargo clippy -p swarm-runtime -p swarm-whisker -p swarm-pheromone -- -D warnings exits 0
  </acceptance_criteria>
  <done>Distinct-source escalation now respects independent strategies, correlation cannot pass on a bonus alone, and existing live-pipeline tests are updated for the strategy-scoped deposit identity.</done>
</task>

</tasks>

<verification>
1. `cargo test -p swarm-whisker --lib` passes with strategy-scoped deposit helper coverage
2. `cargo test -p swarm-pheromone --lib substrate` proves strategy-scoped IDs drive `distinct_sources`
3. `cargo test -p swarm-runtime --lib correlation` proves weighted cross-strategy inclusion and same-strategy rejection cases
4. `cargo test -p swarm-runtime --test escalation_integration` proves one base agent can escalate through two different strategies but not through same-strategy duplication
5. `cargo clippy -p swarm-runtime -p swarm-whisker -p swarm-pheromone -- -D warnings` is clean
6. If the unrelated workspace baseline failure in `evolution::tests::evolution_handoff_persists_pending_launch_packet` is still present, note it in the summary instead of treating it as a regression from this plan
</verification>

<success_criteria>
- `PheromoneDeposit.agent_id` is strategy-scoped for detection and rollout-preview deposits before signing
- Cross-strategy deposits from one base whisker agent count as independent sources for escalation
- Same-strategy duplication no longer receives an accidental correlation advantage from `strategy:*` keys
- Operators can explain correlation decisions from the stored `IncidentMemberDecision.reason` text without any new incident schema fields
</success_criteria>
