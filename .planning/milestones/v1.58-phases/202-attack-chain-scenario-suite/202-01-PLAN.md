---
phase: 202-attack-chain-scenario-suite
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 201 complete
files_modified:
  - scenarios/outlook-mshta-transfer-chain.yaml
  - scenarios/remote-service-stager-chain.yaml
  - scenarios/msbuild-installutil-transfer-chain.yaml
  - scenario-suites/kill-chain-sequences-v1.yaml
  - crates/swarm-runtime/tests/sequence_detection_integration.rs
autonomous: true
requirements:
  - SEQDET-03
must_haves:
  truths:
    - "The new scenario suite must prove chain-only coverage, not repackage already-covered single-event heuristics."
    - "At least three ATT&CK chains must be expressed through the repo-owned replay manifests."
    - "Ground truth should stay deterministic through replay bundle, investigation, and incident counts."
  artifacts:
    - path: "scenario-suites/kill-chain-sequences-v1.yaml"
      provides: "Named replay suite for the shipped chain-only corpus."
    - path: "crates/swarm-runtime/tests/sequence_detection_integration.rs"
      provides: "Focused proof that the shipped single-event detectors stay quiet while the sequence detector passes."
---

<objective>
Complete Phase 202 by adding a repo-owned scenario suite with three chain-only
ATT&CK sequences and focused tests proving those scenarios remain invisible to
the shipped deterministic single-event detectors.
</objective>

<context>
@.planning/phases/202-attack-chain-scenario-suite/202-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/scenario-suites/kill-chain-sequences-v1.yaml
@/Users/connor/Medica/backbay/standalone/swarm-team-six/scenarios/outlook-mshta-transfer-chain.yaml
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/tests/sequence_detection_integration.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add three chain-only replay scenarios</name>
  <files>
    scenarios/outlook-mshta-transfer-chain.yaml
    scenarios/remote-service-stager-chain.yaml
    scenarios/msbuild-installutil-transfer-chain.yaml
    scenario-suites/kill-chain-sequences-v1.yaml
  </files>
  <action>Add three ATT&CK chains whose individual events stay below the shipped single-event heuristics but whose ordered chain should be detected by the sequence rule pack.</action>
  <done>The repo owns one named kill-chain suite and three scenarios with deterministic ground truth for replay bundles, investigations, and incidents.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Prove chain-only behavior against the baseline detectors</name>
  <files>
    crates/swarm-runtime/tests/sequence_detection_integration.rs
  </files>
  <action>Add focused integration proof that the shipped deterministic single-event detector set emits zero bundles on the new scenarios while the sequence detector passes the suite.</action>
  <done>The focused runtime integration suite proves the new scenarios require multi-event sequence matching rather than an already-shipped single-event detector.</done>
</task>

</tasks>

<verification>
Verify that all three replay scenarios are deterministic, that the named suite
passes with the new sequence detector enabled, and that the shipped
single-event detector set stays quiet on the same chains.
</verification>
