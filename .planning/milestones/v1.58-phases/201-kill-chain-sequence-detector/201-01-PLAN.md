---
phase: 201-kill-chain-sequence-detector
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 200 complete
files_modified:
  - crates/swarm-runtime/src/sequence_detector.rs
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-runtime/src/detector_factory.rs
  - sequences/kill-chain-v1.yaml
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - SEQDET-02
must_haves:
  truths:
    - "Sequence rules must stay repo-owned and YAML-authored with ATT&CK technique-chain metadata."
    - "The detector must evaluate against the shared runtime-owned temporal window from Phase 200."
    - "Single-event detector composition must continue to load configs that mention kill_chain_sequence without creating a duplicate detection path."
  artifacts:
    - path: "crates/swarm-runtime/src/sequence_detector.rs"
      provides: "Rule loading, validation, and sequence-evaluation logic."
    - path: "sequences/kill-chain-v1.yaml"
      provides: "Repo-owned ATT&CK chain rules used by the detector profile."
---

<objective>
Complete Phase 201 by introducing a repo-owned kill-chain sequence detector
that loads ATT&CK chain metadata from YAML and evaluates those rules against
the shared temporal event window.
</objective>

<context>
@.planning/phases/201-kill-chain-sequence-detector/201-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/sequence_detector.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/service.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/sequences/kill-chain-v1.yaml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the YAML-backed sequence detector</name>
  <files>
    crates/swarm-runtime/src/sequence_detector.rs
    sequences/kill-chain-v1.yaml
  </files>
  <action>Add the kill-chain rule schema, loader, validation, and ordered-step evaluation logic, plus one shipped ATT&CK rule pack with at least three chains.</action>
  <done>The runtime can load repo-owned kill-chain sequence rules and emit partial or full findings from the shared event window.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire the detector into config and service construction</name>
  <files>
    crates/swarm-runtime/src/config.rs
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/detector_factory.rs
    docs/CONFIGURATION.md
  </files>
  <action>Expose the detector profile through runtime config, attach the detector during service construction, and keep composite-detector config loading compatible in multi-strategy mode.</action>
  <done>Configs that activate `kill_chain_sequence` can build successfully, and the service wrapper owns real sequence evaluation against the shared temporal window.</done>
</task>

</tasks>

<verification>
Verify that the rule pack loads from repo-owned YAML, that partial and full
matches are emitted from the shared temporal window, and that runtime config
construction accepts the new detector profile cleanly.
</verification>
