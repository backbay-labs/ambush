---
phase: 200-temporal-event-window-infrastructure
plan: 01
type: execute
wave: 1
depends_on:
  - v1.57 complete
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/src/runtime_events.rs
  - crates/swarm-whisker/src/detector.rs
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SEQDET-01
must_haves:
  truths:
    - "Phase 200 is infrastructure only; it does not claim real sequence detections yet."
    - "The event window must stay explicitly bounded by config and safe under sustained ingest."
    - "Later sequence rules should consume a shared reusable substrate instead of a benchmark-only or detector-specific side path."
  artifacts:
    - path: "crates/swarm-runtime/src/lib.rs"
      provides: "Runtime-owned bounded event-window substrate for later sequence detectors."
    - path: "crates/swarm-core/src/config.rs"
      provides: "Configurable retention and matching bounds for sequence-window state."
---

<objective>
Complete Phase 200 by adding the bounded sliding-window event substrate required
for later multi-event sequence detection. The output is a reusable runtime
facility for retaining recent events and matching ordered predicates within a
bounded temporal window, without yet publishing sequence findings.
</objective>

<context>
@.planning/phases/200-temporal-event-window-infrastructure/200-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-whisker/src/detector.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add bounded event-window state and config</name>
  <files>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/src/runtime_events.rs
  </files>
  <action>Add one repo-owned bounded sliding-window event substrate with explicit retention controls, lifecycle hooks, and runtime-safe defaults.</action>
  <done>The runtime can retain recent events inside a configured bounded temporal window without unbounded growth or a new ingestion contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Prove ordered predicate matching over the window</name>
  <files>
    crates/swarm-runtime/src/lib.rs
    crates/swarm-whisker/src/detector.rs
  </files>
  <action>Add the internal ordered-predicate matching seam and focused tests so later sequence rules can query the window without reimplementing retention or traversal logic.</action>
  <done>A focused test set proves ordered predicate matching over the bounded event window, while Phase 200 still stops short of emitting sequence findings.</done>
</task>

</tasks>

<verification>
Verify that the runtime retains recent telemetry in a bounded sliding window,
that ordered predicate matching works across multiple events inside the window,
and that the new infrastructure remains configuration-bounded and sequence-rule
agnostic.
</verification>
