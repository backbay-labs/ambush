---
phase: 203-sequence-detection-integration
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 201 complete
  - Phase 202 complete
files_modified:
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime/src/replay/core.inc
  - crates/swarm-runtime/src/detection/pipeline.rs
  - crates/swarm-runtime/tests/sequence_detection_integration.rs
autonomous: true
requirements:
  - SEQDET-04
must_haves:
  truths:
    - "Sequence findings must reuse the existing pheromone, replay, investigation, and incident lanes instead of inventing a sidecar persistence path."
    - "Partial chain matches must emit lower-confidence intermediate pheromones."
    - "Offline replay must construct the same configured sequence detector as the live runtime service."
  artifacts:
    - path: "crates/swarm-runtime/src/service.rs"
      provides: "Service-owned sequence evaluation and shared detector construction."
    - path: "crates/swarm-runtime/src/replay/core.inc"
      provides: "Replay harness alignment with the configured sequence detector."
---

<objective>
Complete Phase 203 by integrating sequence findings into the existing runtime
deposit and replay lanes so partial and full chain matches behave like normal
detector output throughout persistence, replay, investigation, and incident
correlation.
</objective>

<context>
@.planning/phases/203-sequence-detection-integration/203-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/service.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/replay/core.inc
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/tests/sequence_detection_integration.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reuse the normal deposit lane for sequence findings</name>
  <files>
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/detection/pipeline.rs
  </files>
  <action>Attach the configured sequence detector during service construction, evaluate it after the single-event detector pass, and persist partial or full findings through the same signed pheromone helper.</action>
  <done>Sequence findings and deposits share the normal runtime attribution, signing, and substrate persistence path, with partial matches staying lower confidence than full matches.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Align replay with the live sequence path</name>
  <files>
    crates/swarm-runtime/src/replay/core.inc
    crates/swarm-runtime/tests/sequence_detection_integration.rs
  </files>
  <action>Make offline replay build the same configured sequence detector as the live service and prove partial or full sequence findings persist through replay bundles, investigations, and incidents.</action>
  <done>The offline replay harness and the live runtime now agree on sequence-detection behavior, and focused tests prove partial plus full matches reuse the normal replay and correlation lanes.</done>
</task>

</tasks>

<verification>
Verify that partial and full sequence findings produce signed pheromone
deposits with the normal strategy-scoped agent identity and that replay
continues to persist the resulting bundles, investigations, and incidents.
</verification>
