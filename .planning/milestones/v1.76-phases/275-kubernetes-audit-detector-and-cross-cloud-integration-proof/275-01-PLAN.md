---
phase: 275-kubernetes-audit-detector-and-cross-cloud-integration-proof
plan: 01
type: execute
depends_on:
  - 274-cloudtrail-detector
files_modified:
  - crates/swarm-whisker/src/kubernetes_audit.rs
  - crates/swarm-whisker/src/detector.rs
  - crates/swarm-whisker/src/lib.rs
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-runtime/src/detector_factory.rs
  - crates/swarm-runtime/tests/cloud_signal_integration.rs
  - crates/swarm-runtime/tests/critical_path_integration.rs
autonomous: true
requirements:
  - CLOUDDET-02
  - CLOUDDET-03
---

<objective>
Add one bounded `KubernetesAuditDetector` and close the milestone with a repo-owned proof that the
CloudTrail and Kubernetes bridge-plus-detector families compose on the same shipped runtime path.
</objective>

<tasks>

<task>
  <name>Implement Kubernetes audit escalation and escape detection</name>
  <done>`KubernetesAuditDetector` now detects privileged role bindings, wildcard RBAC permissions, impersonation, and privileged pod or host-escape indicators from normalized audit telemetry.</done>
</task>

<task>
  <name>Wire Kubernetes audit profiles into runtime detector construction</name>
  <done>The runtime config and detector factory now validate `kubernetes_audit` profile overrides and construct the detector through the normal strategy path.</done>
</task>

<task>
  <name>Prove both cloud lanes compose through the shared runtime</name>
  <done>The new cross-cloud integration test proves CloudTrail and Kubernetes audit bridge inputs normalize, evaluate, and deposit signed findings on one shared runtime and bridge-health surface.</done>
</task>

</tasks>
