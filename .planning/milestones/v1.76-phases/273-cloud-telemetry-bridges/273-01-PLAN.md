---
phase: 273-cloud-telemetry-bridges
plan: 01
type: execute
depends_on:
  - 272-stix-taxii-consumer-and-threat-intel-enrichment
files_modified:
  - crates/swarm-core/src/telemetry.rs
  - crates/swarm-core/src/lib.rs
  - crates/swarm-core/src/config/bridges.rs
  - crates/swarm-core/src/config/validation.rs
  - crates/swarm-ingest-json/src/lib.rs
  - crates/swarm-ingest-json/src/cloudtrail.rs
  - crates/swarm-ingest-json/src/kubernetes_audit.rs
  - crates/swarm-runtime/src/bridge_runtime.rs
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/tests/cloud_signal_integration.rs
autonomous: true
requirements:
  - CLOUDBR-01
  - CLOUDBR-02
  - CLOUDBR-03
---

<objective>
Extend the shared JSON bridge family with CloudTrail and Kubernetes audit variants that normalize
cloud telemetry into the existing `TelemetryPayload` contract and report health on the same
runtime bridge surface as the shipped host-log adapters.
</objective>

<tasks>

<task>
  <name>Add bounded CloudTrail and Kubernetes audit payload types</name>
  <done>`TelemetryPayload` now carries explicit `CloudTrail` and `KubernetesAudit` variants with the stable fields later detector evidence requires.</done>
</task>

<task>
  <name>Implement cloud JSON bridge adapters on the shared ingest seam</name>
  <done>`swarm-ingest-json` now ships `CloudTrailBridge` and `KubernetesAuditBridge` that parse raw AWS and Kubernetes audit JSON into normalized telemetry events and fail closed on missing required fields.</done>
</task>

<task>
  <name>Register cloud bridges on the runtime bridge-health path</name>
  <done>The runtime bridge registry and readiness probing now build, validate, and report both cloud bridge variants through `runtime.telemetry_sources` and the existing bridge health surface.</done>
</task>

</tasks>
