---
phase: 272-stix-taxii-consumer-and-threat-intel-enrichment
plan: 01
type: execute
depends_on:
  - 271-quickstart-command-and-end-to-end-validation
files_modified:
  - Cargo.toml
  - crates/swarm-core/src/pheromone.rs
  - crates/swarm-core/src/config/runtime.rs
  - crates/swarm-core/src/config/validation.rs
  - crates/swarm-ingest-taxii/Cargo.toml
  - crates/swarm-ingest-taxii/src/lib.rs
  - crates/swarm-runtime/Cargo.toml
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/src/threat_intel_runtime.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
  - crates/swarm-runtime/src/ingest/mod.rs
  - crates/swarm-runtime/src/ingest/health.rs
  - crates/swarm-runtime/src/detection/pipeline.rs
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/src/http/core.inc
autonomous: true
requirements:
  - THREATINTEL-01
  - THREATINTEL-02
  - THREATINTEL-03
---

<objective>
Add one bounded STIX/TAXII 2.1 ingestion lane that stores normalized indicators on the existing
threat-intel substrate, enriches matching findings on the shipped detection pipeline, and exposes
feed health on the normal runtime status surface.
</objective>

<tasks>

<task>
  <name>Build the repo-owned TAXII consumer crate and indicator normalization path</name>
  <done>`swarm-ingest-taxii` now polls TAXII collection URLs, parses IPv4/domain/file-hash/URL STIX indicators, and maps them into `ThreatIntelEntry` with TTL, confidence, source attribution, and indicator IDs.</done>
</task>

<task>
  <name>Wire feed workers into the runtime health surface</name>
  <done>`swarm-runtime` now starts configured threat-intel feed workers from `runtime.threat_intel_feeds`, records poll timestamps plus ingest and error counters, and exposes the aggregated report on the existing readiness and health surfaces.</done>
</task>

<task>
  <name>Enrich matching findings on the shipped detection path</name>
  <done>The runtime detection pipeline now matches process, DNS, network, CloudTrail, and Kubernetes indicator candidates against persisted threat-intel entries and adds IOC value, source, STIX indicator ID, and confidence-boost evidence to the signed finding envelope.</done>
</task>

</tasks>
