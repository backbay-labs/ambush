---
phase: 274-cloudtrail-detector
plan: 01
type: execute
depends_on:
  - 273-cloud-telemetry-bridges
files_modified:
  - crates/swarm-core/src/config/detection.rs
  - crates/swarm-whisker/src/cloudtrail.rs
  - crates/swarm-whisker/src/detector.rs
  - crates/swarm-whisker/src/lib.rs
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-runtime/src/detector_factory.rs
  - crates/swarm-runtime/tests/cloud_signal_integration.rs
  - crates/swarm-runtime/tests/critical_path_integration.rs
autonomous: true
requirements:
  - CLOUDDET-01
---

<objective>
Add one repo-owned `CloudTrailDetector` on the normal detector seam that covers bounded IAM
abuse, resource-hijacking, and credential-compromise patterns and proves the resulting findings
through the signed runtime critical path.
</objective>

<tasks>

<task>
  <name>Implement bounded CloudTrail detection patterns</name>
  <done>`CloudTrailDetector` now detects unusual-principal access-key creation, console logins without MFA from new IPs, privileged role assumption, mining-shaped instance launches, large-instance launches from unusual principals, and unusual secret or parameter reads.</done>
</task>

<task>
  <name>Wire CloudTrail profiles into runtime config and detector construction</name>
  <done>The runtime config and detector factory now validate `cloudtrail` profile overrides and construct the detector through the existing `DetectionStrategy` path.</done>
</task>

<task>
  <name>Prove the signed runtime path carries cloud evidence</name>
  <done>Repo-owned runtime integration proof now shows CloudTrail findings persist through the critical path with AWS account ID, principal ARN, event name, MITRE ATT&CK metadata, and signed pheromone deposits.</done>
</task>

</tasks>
