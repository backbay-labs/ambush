# Plan 26-01

1. Add a strategy-memory runtime module with durable record, history, and file-backed store types.
2. Build idempotent memory-ingest paths from completed canary and production-promotion artifacts.
3. Add `swarmctl` commands for memory ingest, lookup, and strategy history.
4. Cover memory ingestion and history behavior with unit tests.
