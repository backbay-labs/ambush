# Plan 27-01

1. Add a deterministic scoring model over strategy-memory records with explicit outcome, recency, and context weighting.
2. Blend live-memory score and replay-fitness fallback when the live sample is below the minimum threshold.
3. Persist score breakdowns with per-memory explanations for operator inspection.
4. Cover fallback and live-memory scoring behavior with unit tests.

