---
phase: 192-startup-binary-and-ruleset-attestation
plan: 01
type: execute
wave: 1
depends_on:
  - v1.55 milestone complete
files_modified:
  - crates/swarm-runtime/src/bin/swarm_detect.rs
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-runtime/src/ingest/health.rs
  - docs/CONFIGURATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ATTEST-01
must_haves:
  truths:
    - "Startup attestation must happen on the real runtime entrypoint, not in an optional sidecar tool."
    - "Live-response mode must fail closed when binary or ruleset attestation does not verify."
    - "Operators need a readable runtime status surface for attestation results once the gate exists."
  artifacts:
    - path: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      provides: "Startup-time binary and ruleset attestation enforcement."
    - path: "crates/swarm-runtime/src/ingest/health.rs"
      provides: "Attestation visibility on runtime startup and readiness surfaces."
    - path: "docs/CONFIGURATION.md"
      provides: "Operator guidance for the attestation contract and fail-closed behavior."
---

<objective>
Complete Phase 192 by adding startup attestation for the runtime binary and the
repo-owned ruleset set, and by refusing live-response mode when that
verification fails.

Purpose: close the runtime self-protection gap at process start before later
phases add signed config verification and ongoing anti-tamper monitoring.
Output: startup attestation enforcement, surfaced status, and updated operator
configuration guidance.
</objective>

<context>
@.planning/phases/192-startup-binary-and-ruleset-attestation/192-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/bin/swarm_detect.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/health.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/docs/CONFIGURATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add runtime binary and ruleset attestation at startup</name>
  <files>
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-runtime/src/config.rs
  </files>
  <action>Compute and verify the runtime binary and repo-owned ruleset attestation inputs during startup, and fail closed for live-response mode when verification fails.</action>
  <done>The runtime enforces startup attestation on the real entrypoint and refuses unsafe live-response startup.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Surface attestation state to operators</name>
  <files>
    crates/swarm-runtime/src/ingest/health.rs
    docs/CONFIGURATION.md
  </files>
  <action>Expose the current attestation result on startup or readiness surfaces and document how operators provision and interpret the new contract.</action>
  <done>Operators can see whether attestation passed and understand the fail-closed startup behavior.</done>
</task>

</tasks>

<verification>
Verify that startup attestation passes on the known-good repo artifacts, that
tampered or missing attestation inputs fail closed for live-response mode, and
that the runtime surfaces expose the resulting state clearly.
</verification>
