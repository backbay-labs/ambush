---
phase: 194-runtime-anti-tamper-monitoring
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 193
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/anti_tamper.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
  - crates/swarm-runtime/src/runtime_events.rs
  - crates/swarm-runtime/src/ingest/mod.rs
  - crates/swarm-runtime/src/ingest/health.rs
  - crates/swarm-runtime/src/ingest/platform_api.rs
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - ATTEST-03
must_haves:
  truths:
    - "Runtime tamper checks must never create an operator bypass around the existing startup and readiness surfaces."
    - "Live-response mode may fail closed on tamper, but unsupported platforms must report that state explicitly instead of pretending coverage exists."
    - "Tamper detection must be durable enough for operators to inspect after startup, not only ephemeral logs."
  artifacts:
    - path: "crates/swarm-runtime/src/anti_tamper.rs"
      provides: "Runtime anti-tamper probe and monitor."
    - path: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      provides: "Startup admission and background monitor lifecycle."
    - path: "crates/swarm-runtime/src/ingest/health.rs"
      provides: "Operator-visible anti-tamper status."
---

<objective>
Complete Phase 194 by adding structured runtime anti-tamper monitoring for
debugger attachment and unexpected library loads, exposing that state through the
existing runtime health and status surfaces, and optionally failing closed for
live-response mode.

Purpose: extend the startup integrity contract into continuous runtime
self-monitoring.
Output: repo-owned anti-tamper config, Linux probe implementation, structured
runtime alerts, and surfaced operator status.
</objective>

<context>
@.planning/phases/194-runtime-anti-tamper-monitoring/194-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/bin/swarm_detect.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/mod.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/health.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/platform_api.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add anti-tamper probe and startup admission</name>
  <files>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/anti_tamper.rs
    crates/swarm-runtime/src/bin/swarm_detect.rs
  </files>
  <action>Add runtime anti-tamper config, implement a Linux probe for debugger and shared-library drift, evaluate it before serve-mode startup, and optionally fail closed for live-response mode.</action>
  <done>The runtime has one anti-tamper report before serve startup and a background monitor after startup.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Surface anti-tamper state and alerts</name>
  <files>
    crates/swarm-runtime/src/runtime_events.rs
    crates/swarm-runtime/src/ingest/mod.rs
    crates/swarm-runtime/src/ingest/health.rs
    crates/swarm-runtime/src/ingest/platform_api.rs
    docs/CONFIGURATION.md
  </files>
  <action>Persist the latest anti-tamper report in runtime state, emit a structured tamper alert event, surface the latest report on health and platform status routes, and document the operator contract.</action>
  <done>Operators can inspect anti-tamper state through the normal runtime surfaces and the docs explain the platform and mode-specific behavior.</done>
</task>

</tasks>

<verification>
Verify that the runtime rejects fail-closed live-response startup on tamper,
publishes structured anti-tamper state on operator-visible status surfaces, and
correctly reports supported versus unsupported probe behavior.
</verification>
