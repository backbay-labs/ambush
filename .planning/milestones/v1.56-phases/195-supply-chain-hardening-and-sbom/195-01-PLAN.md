---
phase: 195-supply-chain-hardening-and-sbom
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 194
files_modified:
  - Cargo.toml
  - Cargo.lock
  - deny.toml
  - tools/check-supply-chain.sh
  - tools/generate-sbom.sh
  - .github/workflows/ci.yml
  - .github/workflows/release-sbom.yml
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - ATTEST-04
must_haves:
  truths:
    - "Dependency policy must be enforced through one repo-owned path shared by CI and local verification."
    - "First-party workspace wiring must not force the repo to weaken wildcard dependency policy for third-party crates."
    - "SBOM generation must not leave generated artifacts dirty inside the workspace tree after the script finishes."
  artifacts:
    - path: "tools/check-supply-chain.sh"
      provides: "Shared dependency-policy gate."
    - path: "tools/generate-sbom.sh"
      provides: "Repo-owned SBOM generation."
    - path: ".github/workflows/release-sbom.yml"
      provides: "Release artifact publication."
---

<objective>
Complete Phase 195 by hardening dependency policy, adding a repo-owned CI supply
chain gate, and generating one CycloneDX SBOM per workspace crate for release
artifacts.

Purpose: close the milestone's build-time integrity gap and align release
artifacts with the startup attestation contract.
Output: stricter dependency policy, shared supply-chain scripts, CI enforcement,
and release SBOM publication.
</objective>

<context>
@.planning/phases/195-supply-chain-hardening-and-sbom/195-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/Cargo.toml
@/Users/connor/Medica/backbay/standalone/swarm-team-six/deny.toml
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.github/workflows/ci.yml
@/Users/connor/Medica/backbay/standalone/swarm-team-six/docs/CONFIGURATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tighten dependency policy and CI enforcement</name>
  <files>
    Cargo.toml
    Cargo.lock
    deny.toml
    tools/check-supply-chain.sh
    .github/workflows/ci.yml
  </files>
  <action>Pin first-party workspace dependency versions, tighten `deny.toml`, add the repo-owned `cargo deny` plus `cargo audit` gate script, and run it in CI.</action>
  <done>The repo now has one shared supply-chain verification path that passes locally and in CI.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Generate and publish release SBOM artifacts</name>
  <files>
    tools/generate-sbom.sh
    .github/workflows/release-sbom.yml
    docs/CONFIGURATION.md
  </files>
  <action>Add a repo-owned CycloneDX generation script, publish the resulting files through a release workflow, and document the operator-facing SBOM workflow.</action>
  <done>Release automation emits per-crate SBOM artifacts and the docs explain how operators should use them.</done>
</task>

</tasks>

<verification>
Verify that the shared supply-chain gate passes, CI uses the same repo-owned
script, and SBOM generation produces one artifact per workspace crate without
leaving generated JSON inside the repo tree.
</verification>
