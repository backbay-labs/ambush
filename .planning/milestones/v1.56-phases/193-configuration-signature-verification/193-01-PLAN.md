---
phase: 193-configuration-signature-verification
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 192
files_modified:
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
  - crates/swarm-runtime/src/ingest/mod.rs
  - docs/CONFIGURATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ATTEST-02
must_haves:
  truths:
    - "Config signature verification must happen before unsigned config bytes become trusted runtime state."
    - "Unsigned or tampered config files must fail closed on startup and on full config reload."
    - "The trust root for config verification must not live inside the unsigned config file itself."
  artifacts:
    - path: "crates/swarm-runtime/src/config.rs"
      provides: "Signed-config verification on load and reload."
    - path: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      provides: "Fail-closed startup admission for signed configs."
    - path: "docs/CONFIGURATION.md"
      provides: "Operator guidance for provisioning config signatures."
---

<objective>
Complete Phase 193 by requiring signed runtime config files at startup and on
full reload, rejecting unsigned or tampered configs with a structured startup
error.

Purpose: extend the startup trust chain from Phase 192's binary and repo-owned
ruleset attestation to the mutable runtime config file itself.
Output: signed config admission, fail-closed reload behavior, and documented
signing guidance.
</objective>

<context>
@.planning/phases/193-configuration-signature-verification/193-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/bin/swarm_detect.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/mod.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/docs/CONFIGURATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Require signed config files on startup</name>
  <files>
    crates/swarm-runtime/src/config.rs
    crates/swarm-runtime/src/bin/swarm_detect.rs
  </files>
  <action>Add detached-signature verification for the loaded config file before startup proceeds, and fail closed with a structured startup error when the signature is missing, invalid, or mismatched.</action>
  <done>`swarm_detect` only admits signed trusted config files at startup.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Keep reload and operator guidance aligned</name>
  <files>
    crates/swarm-runtime/src/ingest/mod.rs
    docs/CONFIGURATION.md
  </files>
  <action>Apply the same signature gate to full config reloads, keep secret-only reload behavior bounded, and document the config-signing contract operators must provision.</action>
  <done>Reloaded config files remain signed, secret-only refresh stays scoped, and the operator docs explain the config signature contract.</done>
</task>

</tasks>

<verification>
Verify that signed config files load successfully, unsigned or tampered config
files fail closed both on startup and on full reload, and the operator docs
describe how to provision the signature sidecar safely.
</verification>
