---
phase: 211-degradation-transition-tests
plan: 01
type: execute
wave: 1
depends_on:
  - phase-210
files_modified:
  - crates/swarm-runtime/src/ingest/tests.rs
  - crates/swarm-runtime/tests
autonomous: true
requirements:
  - ISOLATE-04
must_haves:
  truths:
    - "Scenario proof must validate the shipped degradation state machine, not bypass it with private-only assertions."
    - "Each required transition must fail closed and surface the expected operator-visible degradation level."
    - "Failure injection stays bounded to repo-owned test seams rather than requiring uncontrolled external infrastructure."
  artifacts:
    - path: "crates/swarm-runtime/src/ingest/tests.rs"
      provides: "Repo-owned degradation transition proof on the shipped health and ingest surfaces."
    - path: "crates/swarm-runtime/tests"
      provides: "Any broader runtime integration coverage needed for substrate or write-path transition proof."
---

<objective>
Prove the degradation state machine behaves correctly under bounded substrate,
write-path, and heap-pressure failure scenarios.
</objective>

<context>
@.planning/phases/211-degradation-transition-tests/211-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/tests.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/mod.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/health.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add bounded failure fixtures for each required transition</name>
  <files>
    crates/swarm-runtime/src/ingest/tests.rs
    crates/swarm-runtime/tests
  </files>
  <action>Introduce repo-owned fixtures that force substrate-unreachable, replay-store/write-path failure, and heap-pressure drain conditions through the existing runtime seams.</action>
  <done>Each required failure mode can be triggered deterministically inside the repo-owned test harness without manual infrastructure intervention.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Prove operator-visible degradation outcomes end to end</name>
  <files>
    crates/swarm-runtime/src/ingest/tests.rs
    crates/swarm-runtime/tests
  </files>
  <action>Assert that the forced failure scenarios surface the expected degradation level and fail-closed behavior through the shipped health, ingest, or status contracts.</action>
  <done>The repo owns explicit proof for NATS-down to detect-only, write-path failure to read-only, and heap-pressure to emergency-drain transitions.</done>
</task>

</tasks>

<verification>
Verify that bounded failure injection drives the shipped degradation contract to
the expected levels and that the operator-visible surfaces report those
transitions with fail-closed behavior.
</verification>
