---
phase: 210-degradation-mode-state-machine
plan: 01
type: execute
wave: 1
depends_on:
  - phase-209
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/ingest/mod.rs
  - crates/swarm-runtime/src/ingest/health.rs
  - crates/swarm-runtime/src/control.rs
autonomous: true
requirements:
  - ISOLATE-03
must_haves:
  truths:
    - "Degradation decisions must stay runtime-owned and bounded; one unhealthy component cannot silently force undefined runtime behavior."
    - "Each degradation level must define what the runtime may still do, especially around detection, response, and operator visibility."
    - "Transitions must be observable through the existing repo-owned status or health surfaces with explicit reason and current level."
  artifacts:
    - path: "crates/swarm-core/src/config.rs"
      provides: "Explicit degradation-level configuration and transition policy contract."
    - path: "crates/swarm-runtime/src/ingest/mod.rs"
      provides: "Runtime-owned evaluation and state updates for degradation transitions."
    - path: "crates/swarm-runtime/src/ingest/health.rs"
      provides: "Health and readiness surfaces that expose current degradation level and reason."
    - path: "crates/swarm-runtime/src/control.rs"
      provides: "Operator-facing status output for the shared degradation state machine."
---

<objective>
Define explicit runtime degradation levels and bounded health-driven transitions
between them.
</objective>

<context>
@.planning/phases/210-degradation-mode-state-machine/210-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/mod.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/health.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/control.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Define bounded degradation levels and policy</name>
  <files>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/ingest/mod.rs
  </files>
  <action>Introduce the explicit runtime degradation-level model plus bounded transition policy driven by existing runtime health signals.</action>
  <done>The runtime can derive one current degradation level with explicit transition reasons instead of relying on implicit scattered checks.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Surface degradation state through repo-owned health and status paths</name>
  <files>
    crates/swarm-runtime/src/ingest/health.rs
    crates/swarm-runtime/src/control.rs
    crates/swarm-runtime/src/ingest/mod.rs
  </files>
  <action>Expose the current degradation level, trigger reason, and bounded allowed-behavior summary through existing readiness and operator status surfaces.</action>
  <done>Operators can read the active degradation state and why the runtime transitioned there through the shipped health and status contracts.</done>
</task>

</tasks>

<verification>
Verify that the runtime derives explicit degradation levels from bounded health
signals, transitions deterministically, and surfaces the resulting level and
reason through existing repo-owned status or health endpoints.
</verification>
