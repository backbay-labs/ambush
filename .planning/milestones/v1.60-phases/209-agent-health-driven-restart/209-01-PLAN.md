---
phase: 209-agent-health-driven-restart
plan: 01
type: execute
wave: 1
depends_on:
  - phase-208
files_modified:
  - crates/swarm-runtime/src/dispatcher.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
autonomous: true
requirements:
  - ISOLATE-02
must_haves:
  truths:
    - "Restart decisions must stay agent-scoped; one failing agent cannot force a full dispatcher or process restart."
    - "Restart must recreate the agent through the same runtime-owned dependency and identity wiring used at startup."
    - "Health reporting must stay honest during restart so operators can distinguish recovered agents from still-failing ones."
  artifacts:
    - path: "crates/swarm-runtime/src/dispatcher.rs"
      provides: "Agent-scoped restart trigger and registry replacement flow."
    - path: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      provides: "Reusable runtime-owned constructors for restarted agents."
---

<objective>
Restart only the failing agent when repeated failures or failed health state
cross the configured lifecycle boundary.
</objective>

<context>
@.planning/phases/209-agent-health-driven-restart/209-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/dispatcher.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/bin/swarm_detect.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Introduce agent-scoped restart wiring</name>
  <files>
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/bin/swarm_detect.rs
  </files>
  <action>Refactor runtime agent construction into reusable runtime-owned factories or restart handles so the dispatcher can replace one failing agent without reinitializing the full process.</action>
  <done>The runtime has one reusable construction path for each registered agent role and can swap a single agent instance back into the dispatcher registry.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Trigger targeted restart from repeated failure state</name>
  <files>
    crates/swarm-runtime/src/dispatcher.rs
  </files>
  <action>Detect repeated degraded or failed health for one agent, restart only that agent, and preserve honest health or logging state for the rest of the swarm.</action>
  <done>Persistent agent failure triggers individual restart while healthy peers remain registered and continue ticking.</done>
</task>

</tasks>

<verification>
Verify that repeated failure for one agent triggers only that agent's restart,
healthy peers continue running, and the dispatcher plus process stay alive.
</verification>
