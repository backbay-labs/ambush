---
phase: 208-per-agent-panic-boundaries
plan: 01
type: execute
wave: 1
depends_on:
  - milestone-v1.59
files_modified:
  - crates/swarm-runtime/src/dispatcher.rs
  - crates/swarm-runtime/src/lib.rs
autonomous: true
requirements:
  - ISOLATE-01
must_haves:
  truths:
    - "Per-agent panic containment must fail closed at the dispatcher boundary; a panicking tick cannot terminate the shared dispatcher loop."
    - "Panics must be attributable to the specific agent role or boundary so later health-driven restart logic has honest input."
    - "This phase adds containment only; restart orchestration and degradation-state transitions stay in later phases."
  artifacts:
    - path: "crates/swarm-runtime/src/dispatcher.rs"
      provides: "Dispatcher-owned catch_unwind boundary and degraded-health handling for panicking agents."
    - path: "crates/swarm-runtime/src/lib.rs"
      provides: "Runtime-owned panic boundary classification reusable by later restart and health surfaces."
---

<objective>
Contain each runtime agent tick within its own panic boundary so one agent
panic does not terminate the shared dispatcher or runtime process.
</objective>

<context>
@.planning/phases/208-per-agent-panic-boundaries/208-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/dispatcher.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add dispatcher-owned panic containment per agent tick</name>
  <files>
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/lib.rs
  </files>
  <action>Wrap each registered agent tick in a runtime-owned panic boundary, convert panic payloads into attributable boundary failures, and preserve the current timeout plus degraded-health behavior for non-panicking agents.</action>
  <done>A panic in one agent marks only that agent degraded and the dispatcher continues ticking other agents instead of unwinding the shared task.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Prove panics are isolated and attributable</name>
  <files>
    crates/swarm-runtime/src/dispatcher.rs
  </files>
  <action>Add focused dispatcher proof that a panicking agent neither crashes the dispatcher loop nor prevents healthy agents from continuing to tick, while preserving role-attributed failure context for later restart work.</action>
  <done>The repo has deterministic tests proving per-agent panic containment without yet implementing restart loops or global degradation transitions.</done>
</task>

</tasks>

<verification>
Verify that a deliberately panicking agent is isolated to its own degraded
health state, healthy agents continue to tick, and the dispatcher task stays
alive.
</verification>
