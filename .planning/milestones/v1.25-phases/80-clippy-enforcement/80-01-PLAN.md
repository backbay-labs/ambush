---
phase: 80-clippy-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/swarm-core/Cargo.toml
  - crates/swarm-whisker/Cargo.toml
  - crates/swarm-bridge/Cargo.toml
  - crates/swarm-consensus/Cargo.toml
  - crates/swarm-guard/Cargo.toml
  - crates/swarm-crypto/Cargo.toml
  - crates/swarm-spine/Cargo.toml
  - crates/swarm-pheromone/Cargo.toml
  - crates/swarm-policy/Cargo.toml
  - crates/swarm-response/Cargo.toml
  - crates/swarm-runtime/Cargo.toml
  - crates/swarm-crypto/src/canonical.rs
  - crates/swarm-crypto/src/hashing.rs
  - crates/swarm-crypto/src/lib.rs
  - crates/swarm-crypto/src/merkle.rs
  - crates/swarm-crypto/src/signing.rs
  - crates/swarm-spine/src/chain.rs
  - crates/swarm-spine/src/checkpoint.rs
  - crates/swarm-spine/src/envelope.rs
  - crates/swarm-spine/src/incident.rs
  - crates/swarm-spine/src/investigation.rs
  - crates/swarm-spine/src/store.rs
  - crates/swarm-pheromone/src/substrate.rs
  - crates/swarm-policy/src/static_gate.rs
  - crates/swarm-response/src/adapters.rs
autonomous: true
requirements:
  - OPS-30

must_haves:
  truths:
    - "Workspace Cargo.toml declares [workspace.lints.clippy] with unwrap_used = deny and expect_used = deny"
    - "Every crate Cargo.toml inherits workspace lints via [lints] workspace = true"
    - "All non-runtime crates (crypto, spine, pheromone, policy, response, guard, core, whisker, bridge, consensus) compile cleanly under the deny lints"
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace-level clippy lint deny rules"
      contains: "[workspace.lints.clippy]"
    - path: "crates/swarm-crypto/Cargo.toml"
      provides: "Workspace lint inheritance"
      contains: "[lints]"
    - path: "crates/swarm-spine/Cargo.toml"
      provides: "Workspace lint inheritance"
      contains: "[lints]"
  key_links:
    - from: "crates/*/Cargo.toml"
      to: "Cargo.toml [workspace.lints.clippy]"
      via: "[lints] workspace = true"
      pattern: "workspace\\s*=\\s*true"
---

<objective>
Set up workspace-level clippy lint denial for `unwrap_used` and `expect_used`, wire all crate Cargo.toml files to inherit workspace lints, and fix all non-runtime crates so they compile cleanly under the new rules.

Purpose: Establishes the lint infrastructure that makes `unwrap()`/`expect()` a compile-deny across the workspace, and clears all crates except swarm-runtime (which has the bulk of production violations and gets its own plan).

Output: Workspace lint config, all 11 crate Cargo.toml files updated, and 10 non-runtime crates passing `cargo clippy` under the deny rules.
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/80-clippy-enforcement/80-CONTEXT.md
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace lint config and wire all crate Cargo.toml files</name>
  <files>Cargo.toml, crates/swarm-core/Cargo.toml, crates/swarm-whisker/Cargo.toml, crates/swarm-bridge/Cargo.toml, crates/swarm-consensus/Cargo.toml, crates/swarm-guard/Cargo.toml, crates/swarm-crypto/Cargo.toml, crates/swarm-spine/Cargo.toml, crates/swarm-pheromone/Cargo.toml, crates/swarm-policy/Cargo.toml, crates/swarm-response/Cargo.toml, crates/swarm-runtime/Cargo.toml</files>
  <action>
1. Add to the workspace `Cargo.toml` (after the existing `[workspace.dependencies]` section):

```toml
[workspace.lints.clippy]
unwrap_used = "deny"
expect_used = "deny"
```

2. Add to EVERY crate `Cargo.toml` (all 11 members: swarm-core, swarm-whisker, swarm-bridge, swarm-consensus, swarm-guard, swarm-crypto, swarm-spine, swarm-pheromone, swarm-policy, swarm-response, swarm-runtime):

```toml
[lints]
workspace = true
```

This goes at the end of each crate Cargo.toml, after all other sections.

Do NOT change any existing sections in any Cargo.toml. Only append the new `[lints]` section.
  </action>
  <verify>
    <automated>grep -q 'unwrap_used.*=.*"deny"' Cargo.toml && grep -q 'expect_used.*=.*"deny"' Cargo.toml && for c in crates/swarm-*/Cargo.toml; do grep -q 'workspace = true' "$c" || (echo "MISSING: $c" && exit 1); done && echo "PASS"</automated>
  </verify>
  <done>Workspace Cargo.toml has [workspace.lints.clippy] with both deny rules. All 11 crate Cargo.toml files have [lints] workspace = true.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all non-runtime crates to compile cleanly under deny lints</name>
  <files>crates/swarm-crypto/src/canonical.rs, crates/swarm-crypto/src/hashing.rs, crates/swarm-crypto/src/lib.rs, crates/swarm-crypto/src/merkle.rs, crates/swarm-crypto/src/signing.rs, crates/swarm-spine/src/chain.rs, crates/swarm-spine/src/checkpoint.rs, crates/swarm-spine/src/envelope.rs, crates/swarm-spine/src/incident.rs, crates/swarm-spine/src/investigation.rs, crates/swarm-spine/src/store.rs, crates/swarm-pheromone/src/substrate.rs, crates/swarm-policy/src/static_gate.rs, crates/swarm-response/src/adapters.rs</files>
  <action>
All non-runtime crate violations are in `#[cfg(test)]` modules. No production code changes are needed for these crates.

For each file that has a `#[cfg(test)]` module containing `unwrap()` or `expect()` calls, add the allow attribute ABOVE the `#[cfg(test)]` line:

```rust
#[allow(clippy::unwrap_used, clippy::expect_used)]
#[cfg(test)]
mod tests {
    ...
}
```

**Files to annotate (all violations are test-only):**

swarm-crypto (33 test violations, 0 prod):
- `src/canonical.rs` — 8 test violations
- `src/hashing.rs` — 4 test violations
- `src/lib.rs` — 3 test violations
- `src/merkle.rs` — 12 test violations
- `src/signing.rs` — 6 test violations

swarm-spine (79 test violations, 0 prod):
- `src/chain.rs` — 22 test violations
- `src/checkpoint.rs` — 13 test violations
- `src/envelope.rs` — 13 test violations (note: may be listed as 12 in raw grep; use clippy as truth)
- `src/incident.rs` — 9 test violations
- `src/investigation.rs` — 11 test violations
- `src/store.rs` — 11 test violations

swarm-pheromone (23 test violations, 0 prod):
- `src/substrate.rs` — 23 test violations

swarm-policy (4 test violations, 0 prod):
- `src/static_gate.rs` — 4 test violations

swarm-response (2 test violations, 0 prod):
- `src/adapters.rs` — 2 test violations

**Crates that already have zero violations (no source changes needed):**
- swarm-core, swarm-whisker, swarm-bridge, swarm-consensus, swarm-guard

After annotating, verify each crate compiles cleanly:
```bash
cargo clippy -p swarm-crypto --all-targets -- -D warnings
cargo clippy -p swarm-spine --all-targets -- -D warnings
cargo clippy -p swarm-pheromone --all-targets -- -D warnings
cargo clippy -p swarm-policy --all-targets -- -D warnings
cargo clippy -p swarm-response --all-targets -- -D warnings
cargo clippy -p swarm-core --all-targets -- -D warnings
cargo clippy -p swarm-whisker --all-targets -- -D warnings
cargo clippy -p swarm-guard --all-targets -- -D warnings
```
  </action>
  <verify>
    <automated>cargo clippy -p swarm-crypto -p swarm-spine -p swarm-pheromone -p swarm-policy -p swarm-response -p swarm-core -p swarm-whisker -p swarm-guard -p swarm-bridge -p swarm-consensus --all-targets -- -D warnings 2>&1 | grep -c "error" | xargs test 0 -eq && echo "PASS"</automated>
  </verify>
  <done>All 10 non-runtime crates compile with zero clippy errors under the workspace deny rules. Test modules have #[allow] annotations. No production code was changed in these crates.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep 'unwrap_used.*deny' Cargo.toml` returns the deny rule
2. `grep 'expect_used.*deny' Cargo.toml` returns the deny rule
3. All 11 crate Cargo.toml files contain `[lints] workspace = true`
4. `cargo clippy -p swarm-crypto -p swarm-spine -p swarm-pheromone -p swarm-policy -p swarm-response -p swarm-core -p swarm-whisker -p swarm-guard -p swarm-bridge -p swarm-consensus --all-targets -- -D warnings` produces zero errors
</verification>

<success_criteria>
- Workspace Cargo.toml has `[workspace.lints.clippy]` section denying both `unwrap_used` and `expect_used`
- All 11 crate Cargo.toml files inherit workspace lints
- All 10 non-runtime crates pass clippy with zero violations
- No production code behavior is changed (only test module annotations added)
</success_criteria>

<output>
After completion, create `.planning/phases/80-clippy-enforcement/80-01-SUMMARY.md`
</output>
