---
phase: 218-multi-telemetry-behavioral-extension
plan: 01
type: execute
wave: 1
depends_on:
  - phase-217
files_modified:
  - crates/swarm-core/src/pheromone.rs
  - crates/swarm-whisker/src/behavioral_anomaly.rs
  - crates/swarm-runtime/src/config.rs
autonomous: true
requirements:
  - ANOMALY-03
must_haves:
  truths:
    - "Behavioral anomaly learning must extend beyond process starts to the shipped network, DNS, authentication, file-oriented, and memory-oriented telemetry families."
    - "The broadened detector must keep using the shared restart-safe behavioral baseline snapshot seam instead of introducing detector-local persistence."
    - "Operators must still receive clear telemetry-family attribution and bounded evidence after the behavioral detector broadens."
  artifacts:
    - path: "crates/swarm-core/src/pheromone.rs"
      provides: "Shared restart-safe behavioral baseline schema needed for non-process telemetry learning."
    - path: "crates/swarm-whisker/src/behavioral_anomaly.rs"
      provides: "Multi-telemetry behavioral anomaly evaluation, scoring, and evidence attribution."
    - path: "crates/swarm-runtime/src/config.rs"
      provides: "Repo-owned config merge and validation for any new behavioral breadth tuning bounds."
---

<objective>
Extend learned behavioral anomaly scoring to the other shipped telemetry
families without introducing a second persistence seam.
</objective>

<context>
@.planning/phases/218-multi-telemetry-behavioral-extension/218-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/pheromone.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-whisker/src/behavioral_anomaly.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/config.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend the shared behavioral baseline seam for non-process telemetry</name>
  <files>
    crates/swarm-core/src/pheromone.rs
    crates/swarm-whisker/src/behavioral_anomaly.rs
  </files>
  <action>Add the bounded shared baseline state needed for network, DNS, authentication, file-oriented, and memory-oriented telemetry so the behavioral detector can hydrate and persist that learning through the existing restart-safe snapshot contract.</action>
  <done>Restart-safe behavioral baseline snapshots can carry the learned state required for the widened telemetry families without introducing a second detector-local store.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add multi-telemetry behavioral evaluation and bounded evidence</name>
  <files>
    crates/swarm-whisker/src/behavioral_anomaly.rs
    crates/swarm-runtime/src/config.rs
  </files>
  <action>Extend `BehavioralAnomalyDetector` beyond process starts to the shipped non-process telemetry families, keep the explicit deviation-scoring model bounded, and preserve clear per-family evidence and config validation.</action>
  <done>Behavioral anomalies can be emitted for the widened telemetry families with clear evidence attribution and repo-owned config validation for any new tuning bounds.</done>
</task>

</tasks>

<verification>
Verify that the shared behavioral anomaly path now evaluates the widened
telemetry families and that the learned baseline state for those families still
survives restart through the existing snapshot seam.
</verification>
