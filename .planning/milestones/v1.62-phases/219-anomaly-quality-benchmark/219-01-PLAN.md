---
phase: 219-anomaly-quality-benchmark
plan: 01
type: execute
wave: 1
depends_on:
  - phase-218
files_modified:
  - crates/swarm-runtime/examples
  - docs/benchmarks
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - ANOMALY-04
must_haves:
  truths:
    - "The repo must ship one repeatable labeled-telemetry benchmark for the widened behavioral anomaly detector, not just ad hoc tests."
    - "The benchmark must measure both catch-rate and false-positive behavior against labeled telemetry, and the resulting artifact must be checked in."
    - "Any improvement or lack of improvement claims must be grounded in the actual widened detector state shipped at the end of Phase 218."
  artifacts:
    - path: "crates/swarm-runtime/examples"
      provides: "Repo-owned runnable benchmark entrypoint or replay harness for behavioral anomaly quality."
    - path: "docs/benchmarks"
      provides: "Checked-in benchmark artifact that records the measured false-positive and catch-rate outcome."
---

<objective>
Measure the quality of the widened behavioral anomaly detector against labeled
telemetry and check in the resulting benchmark artifact.
</objective>

<context>
@.planning/phases/219-anomaly-quality-benchmark/219-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-whisker/src/behavioral_anomaly.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build a repeatable labeled-telemetry benchmark path for behavioral anomaly quality</name>
  <files>
    crates/swarm-runtime/examples
  </files>
  <action>Create or adapt a repo-owned runnable benchmark or replay path that evaluates the widened behavioral anomaly detector against labeled telemetry and records catch-rate plus false-positive metrics.</action>
  <done>The repo can run one repeatable behavioral anomaly quality benchmark against labeled telemetry without manual bookkeeping.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Check in the measured benchmark artifact</name>
  <files>
    docs/benchmarks
  </files>
  <action>Run the benchmark, record the measured outcome honestly, and check in the resulting artifact so later work can compare against the same reference run.</action>
  <done>A checked-in benchmark artifact records the measured catch-rate and false-positive behavior for the widened behavioral anomaly detector.</done>
</task>

</tasks>

<verification>
Verify that the repo-owned benchmark path runs against labeled telemetry and
that the checked-in artifact reflects the measured outcome for the widened
behavioral anomaly detector.
</verification>
