---
phase: 216-online-distribution-learning
plan: 01
type: execute
wave: 1
depends_on:
  - phase-215
files_modified:
  - crates/swarm-whisker/src/behavioral_anomaly.rs
  - crates/swarm-core/src/pheromone.rs
  - crates/swarm-runtime/src/config.rs
autonomous: true
requirements:
  - ANOMALY-01
must_haves:
  truths:
    - "Behavioral anomaly confidence must come from learned online distribution state, not the current fixed signal-count arithmetic."
    - "Learned distribution state must remain durable and restart-safe through the existing behavioral baseline snapshot seam."
    - "The phase must stay bounded to the existing behavioral process-start detector instead of widening into multi-telemetry anomaly scoring early."
  artifacts:
    - path: "crates/swarm-whisker/src/behavioral_anomaly.rs"
      provides: "Online per-entity distribution tracking and learned confidence derivation for behavioral process-start anomalies."
    - path: "crates/swarm-core/src/pheromone.rs"
      provides: "Shared durable snapshot types that persist the learned online-distribution state across restart."
    - path: "crates/swarm-runtime/src/config.rs"
      provides: "Repo-owned profile validation or defaults for any new online-learning knobs added to the behavioral anomaly detector."
---

<objective>
Replace fixed behavioral anomaly confidence arithmetic with learned per-entity
online distributions.
</objective>

<context>
@.planning/phases/216-online-distribution-learning/216-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-whisker/src/behavioral_anomaly.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/pheromone.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/config.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add restart-safe online distribution state to behavioral baselines</name>
  <files>
    crates/swarm-whisker/src/behavioral_anomaly.rs
    crates/swarm-core/src/pheromone.rs
  </files>
  <action>Extend the behavioral anomaly detector and the shared baseline snapshot schema so host, identity, and peer-group scopes can persist the online statistics needed for learned anomaly confidence across restart.</action>
  <done>The behavioral detector can hydrate, update, and re-snapshot learned per-entity online distribution state without introducing a second persistence seam.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace fixed confidence arithmetic with learned per-entity scoring</name>
  <files>
    crates/swarm-whisker/src/behavioral_anomaly.rs
    crates/swarm-runtime/src/config.rs
  </files>
  <action>Use the new online statistics to derive behavioral anomaly confidence from learned per-entity distributions, keep the profile fail-closed, and add focused proof that the detector no longer relies on fixed signal-count arithmetic.</action>
  <done>`BehavioralAnomalyDetector` emits confidence from learned online distributions with focused regression proof and repo-owned profile validation.</done>
</task>

</tasks>

<verification>
Verify that behavioral anomaly confidence now comes from durable learned
per-entity online distributions and survives detector restart through the
existing baseline snapshot contract.
</verification>
