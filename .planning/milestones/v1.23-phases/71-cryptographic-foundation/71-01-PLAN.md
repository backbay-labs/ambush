---
phase: 71-cryptographic-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/swarm-crypto/Cargo.toml
  - crates/swarm-crypto/src/error.rs
  - crates/swarm-crypto/src/hashing.rs
  - crates/swarm-crypto/src/canonical.rs
autonomous: true
requirements: [CRYPTO-02, CRYPTO-04]

must_haves:
  truths:
    - "SHA-256 of b'hello' produces the known test vector 2cf24dba5fb0a30e26e83b2ac5b9e29e1b161e5c1fa7425e73043362938b9824"
    - "Hash type supports from_bytes, from_hex, to_hex, to_hex_prefixed, zero, serde round-trip"
    - "Canonical JSON serialization sorts object keys by UTF-16 code units per RFC 8785"
    - "Canonical JSON number formatting uses ryu for deterministic shortest representation and handles JCS edge cases (negative zero, exponential notation thresholds)"
    - "Canonical JSON string escaping handles control characters, backslash, and quote per JCS spec"
  artifacts:
    - path: "crates/swarm-crypto/src/error.rs"
      provides: "CryptoError enum with all hush-core variants minus TPM-specific ones"
      contains: "pub enum Error"
    - path: "crates/swarm-crypto/src/hashing.rs"
      provides: "Hash type and sha256/sha256_hex functions"
      exports: ["Hash", "sha256", "sha256_hex"]
    - path: "crates/swarm-crypto/src/canonical.rs"
      provides: "RFC 8785 canonical JSON serialization"
      exports: ["canonicalize"]
  key_links:
    - from: "crates/swarm-crypto/src/canonical.rs"
      to: "crates/swarm-crypto/src/error.rs"
      via: "use crate::error::{Error, Result}"
      pattern: "crate::error"
    - from: "crates/swarm-crypto/src/hashing.rs"
      to: "crates/swarm-crypto/src/error.rs"
      via: "use crate::error::{Error, Result}"
      pattern: "crate::error"
---

<objective>
Port the foundational crypto modules from hush-core into swarm-crypto: error types, SHA-256 hashing with the Hash value type, and RFC 8785 canonical JSON serialization.

Purpose: These modules are the foundation that signing (Plan 02) and merkle (Plan 02) depend on. Error types define the shared error surface. The Hash type is used by merkle trees. Canonical JSON is used by signing for deterministic payload hashing.

Output: Three new source files in swarm-crypto/src/ plus updated Cargo.toml with new workspace dependencies (hex, ryu, rand_core).
</objective>

<execution_context>
@/Users/connor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-cryptographic-foundation/71-CONTEXT.md

Source files to port from:
@vendor/reference/clawdstrike/libs/hush-core/src/error.rs
@vendor/reference/clawdstrike/libs/hush-core/src/hashing.rs
@vendor/reference/clawdstrike/libs/hush-core/src/canonical.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace dependencies and port error and hashing modules</name>
  <files>Cargo.toml, crates/swarm-crypto/Cargo.toml, crates/swarm-crypto/src/error.rs, crates/swarm-crypto/src/hashing.rs</files>
  <action>
1. Add workspace dependencies to root `Cargo.toml` under `[workspace.dependencies]`:
   - `hex = "0.4"` (used by hashing and signing for hex encode/decode)
   - `ryu = "1"` (used by canonical JSON for deterministic float formatting)
   - `rand_core = { version = "0.6", features = ["getrandom"] }` (used by signing for key generation via OsRng)

2. Update `crates/swarm-crypto/Cargo.toml` to add new deps:
   - Add `hex.workspace = true`
   - Add `ryu.workspace = true`
   - Add `rand_core.workspace = true`

3. Create `crates/swarm-crypto/src/error.rs` by adapting `vendor/reference/clawdstrike/libs/hush-core/src/error.rs`:
   - Copy the Error enum and Result type alias
   - REMOVE: `ReceiptVerificationFailed`, `InvalidReceiptVersion`, `UnsupportedReceiptVersion` variants (receipt.rs not being ported)
   - REMOVE: `IoError` variant (not needed for crypto-only crate)
   - REMOVE: `TpmError` variant (TPM not being ported)
   - KEEP: `InvalidSignature`, `InvalidPublicKey`, `InvalidPrivateKey`, `InvalidHex`, `InvalidHashLength`, `MerkleProofFailed`, `EmptyTree`, `InvalidProofIndex`, `JsonError`
   - KEEP: the `From<serde_json::Error>` impl
   - REMOVE: the `From<std::io::Error>` impl
   - Keep `#[non_exhaustive]` on the enum

4. Create `crates/swarm-crypto/src/hashing.rs` by adapting `vendor/reference/clawdstrike/libs/hush-core/src/hashing.rs`:
   - Copy the `Hash` struct with its serde transparent wrapper, `hash_serde` module, all impl methods (`from_bytes`, `from_hex`, `to_hex`, `to_hex_prefixed`, `zero`, `as_bytes`), `AsRef<[u8]>`, `Display`
   - Copy `sha256` and `sha256_hex` functions
   - REMOVE: `keccak256` and `keccak256_hex` functions (not in requirements, avoids sha3 dep)
   - REMOVE: the `use sha3::Keccak256` import
   - Use `hex` crate for hex encoding/decoding (matches hush-core's approach) instead of hand-rolled nibble code
   - Copy all tests except keccak-related ones. Include the SHA-256 known test vector test, hex round-trip test, serde round-trip test, and concat_hashes test.

Note: The `sha256_hex` function in hush-core returns `0x`-prefixed hex. The existing swarm-crypto `sha256_hex` returns unprefixed hex. Plan 02 will handle the backward-compat re-export that preserves the old unprefixed behavior for downstream callers.
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/swarm-team-six && cargo check -p swarm-crypto 2>&1 | tail -5</automated>
  </verify>
  <done>error.rs exports Error enum and Result type. hashing.rs exports Hash, sha256, sha256_hex. Both files compile. SHA-256 of b"hello" matches known vector in test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port RFC 8785 canonical JSON module</name>
  <files>crates/swarm-crypto/src/canonical.rs</files>
  <behavior>
    - Test: JCS number edge cases -- 1.0 renders as "1", 0.0 and -0.0 both render as "0", 1e21 uses exponential, 1e20 uses decimal, 1e-6 uses decimal, 1e-7 uses exponential
    - Test: Object keys sort by UTF-16 code units (not byte order) -- numeric string keys "10" sorts before "2"
    - Test: Surrogate-pair keys sort correctly (U+E000 vs U+10437)
    - Test: Control characters use JCS escape shortcuts (\b, \f, \n, \r, \t) and \u00xx for others
    - Test: Nested objects and arrays serialize correctly
    - Test: String escaping handles backslash, quote, and control chars
  </behavior>
  <action>
Create `crates/swarm-crypto/src/canonical.rs` by copying `vendor/reference/clawdstrike/libs/hush-core/src/canonical.rs` with these adaptations:

1. Change `use crate::error::{Error, Result}` to reference the local error module
2. Copy ALL functions: `canonicalize`, `cmp_utf16_code_units`, `canonicalize_number`, `canonicalize_f64`, `parse_to_scientific_parts`, `render_decimal`, `trim_decimal`, `escape_json_string`
3. Copy ALL tests verbatim: `jcs_vector_b_numbers`, `jcs_vector_a_unicode_and_controls`, `jcs_vector_c_escape_shortcuts`, `jcs_vector_d_numeric_string_keys`, `sorted_keys`, `sorted_keys_use_utf16_code_units`, `nested_objects`, `arrays`
4. Do NOT simplify the float formatting -- the ryu-based approach is the whole point of RFC 8785 compliance

The module does NOT need to be wired into lib.rs yet -- Plan 02 handles the full lib.rs rewrite.
  </action>
  <verify>
    <automated>cd /Users/connor/Medica/backbay/standalone/swarm-team-six && cargo test -p swarm-crypto -- canonical 2>&1 | tail -20</automated>
  </verify>
  <done>All 8 canonical JSON tests pass including JCS number vectors, UTF-16 key sorting, control character escaping, and nested structure serialization.</done>
</task>

</tasks>

<verification>
```bash
# All new modules compile
cargo check -p swarm-crypto

# Hashing tests pass with known vectors
cargo test -p swarm-crypto -- hashing

# Canonical JSON tests pass including JCS vectors
cargo test -p swarm-crypto -- canonical

# Error module compiles (no standalone tests needed -- it is a type-only module)
cargo check -p swarm-crypto
```
</verification>

<success_criteria>
1. `cargo test -p swarm-crypto -- hashing` passes with SHA-256 known test vector matching
2. `cargo test -p swarm-crypto -- canonical` passes with all 8 JCS test vectors
3. `cargo check -p swarm-crypto` compiles clean with no warnings
4. New workspace deps (hex, ryu, rand_core) are declared and resolve correctly
</success_criteria>

<output>
After completion, create `.planning/phases/71-cryptographic-foundation/71-01-SUMMARY.md`
</output>
