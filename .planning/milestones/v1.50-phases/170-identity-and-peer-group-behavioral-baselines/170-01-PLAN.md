---
phase: 170-identity-and-peer-group-behavioral-baselines
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 169
files_modified:
  - crates/swarm-whisker/src/behavioral_anomaly.rs
  - crates/swarm-runtime/src/detection/pipeline.rs
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-core/src/pheromone.rs
  - crates/swarm-pheromone/src/substrate.rs
autonomous: true
requirements:
  - BEHAV-01
  - BEHAV-02
must_haves:
  truths:
    - "Host, identity, and peer-group baselines must be tracked independently."
    - "Baseline persistence, decay, and recovery must remain restart-safe and scope-aware."
    - "Behavioral findings must explain which scope triggered the anomaly."
  artifacts:
    - path: "crates/swarm-whisker/src/behavioral_anomaly.rs"
      provides: "Multi-scope behavioral baseline logic and evidence shaping."
    - path: "crates/swarm-runtime/src/detection/pipeline.rs"
      provides: "Runtime hydration and persistence of scope-specific baselines."
    - path: "crates/swarm-core/src/pheromone.rs"
      provides: "Durable baseline snapshot shape that supports multiple scopes."
---

<objective>
Complete Phase 170 by extending behavioral anomaly detection into identity and peer-group scopes with durable scope-aware persistence, decay, and readable evidence.

Purpose: give the async and critical lanes richer anomaly context without abandoning the shipped bounded detector architecture.
Output: multi-scope behavioral baselines, restart-safe persistence, and scope-specific anomaly evidence.
</objective>

<context>
@.planning/phases/170-identity-and-peer-group-behavioral-baselines/170-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-whisker/src/behavioral_anomaly.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/detection/pipeline.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/pheromone.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend behavioral learning to identity and peer-group scope</name>
  <files>
    crates/swarm-whisker/src/behavioral_anomaly.rs
    crates/swarm-runtime/src/config.rs
  </files>
  <action>Add repo-owned thresholds and learning rules for identity and peer-group baselines while preserving the existing host baseline behavior.</action>
  <done>The detector can evaluate anomalies separately across host, identity, and peer-group scopes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist and reload scope-aware baselines</name>
  <files>
    crates/swarm-runtime/src/detection/pipeline.rs
    crates/swarm-core/src/pheromone.rs
    crates/swarm-pheromone/src/substrate.rs
  </files>
  <action>Extend the durable baseline snapshot contract so each scope persists, decays, and reloads independently across restarts.</action>
  <done>Behavioral baselines remain restart-safe and scope-aware without regressing the current per-host path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose scope-specific evidence and regression tests</name>
  <files>
    crates/swarm-whisker/src/behavioral_anomaly.rs
    crates/swarm-runtime/src/detection/pipeline.rs
  </files>
  <action>Attach readable scope attribution to findings and prove via focused tests that host, identity, and peer-group anomalies persist and recover independently.</action>
  <done>Operators and downstream correlation can see which baseline scope triggered a finding, and regression coverage pins the behavior.</done>
</task>

</tasks>

<verification>
Keep the phase baseline-focused. Operator-visible async queue and correlation dashboards belong to the next phase.
</verification>
