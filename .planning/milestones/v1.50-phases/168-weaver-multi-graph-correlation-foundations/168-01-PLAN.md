---
phase: 168-weaver-multi-graph-correlation-foundations
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 167
files_modified:
  - crates/swarm-runtime/src/correlation.rs
  - crates/swarm-runtime/src/weaver_agent.rs
  - crates/swarm-spine/src/incident.rs
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs
autonomous: true
requirements:
  - ASYNC-01
  - ASYNC-02
must_haves:
  truths:
    - "Weaver must traverse temporal, causal, entity, and semantic dimensions using repo-owned rules."
    - "Correlated incidents must carry explainable evidence chains and confidence scores instead of opaque summaries."
    - "The async correlation lane must stay bounded and off the hot path."
  artifacts:
    - path: "crates/swarm-runtime/src/correlation.rs"
      provides: "Multi-graph traversal, scoring, and evidence-chain assembly."
    - path: "crates/swarm-runtime/src/weaver_agent.rs"
      provides: "Runtime wiring that turns completed investigations into richer correlated incidents."
    - path: "crates/swarm-spine/src/incident.rs"
      provides: "Durable incident shape for explainable multi-graph outputs."
---

<objective>
Complete Phase 168 by extending Weaver correlation into a first-class multi-graph async lane with explainable evidence chains and bounded confidence scoring.

Purpose: improve incident quality before scheduling, behavioral-depth, and operator-surface work expands the rest of the async lane.
Output: richer correlation engine, durable explainable incident artifacts, and integration coverage for multi-graph traversal.
</objective>

<context>
@.planning/phases/168-weaver-multi-graph-correlation-foundations/168-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/correlation.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/weaver_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-spine/src/incident.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add multi-graph correlation traversal and scoring</name>
  <files>
    crates/swarm-runtime/src/correlation.rs
    crates/swarm-runtime/src/weaver_agent.rs
  </files>
  <action>Extend the current correlation engine with explicit temporal, causal, entity, and semantic traversal rules plus bounded confidence scoring rooted in repo-owned heuristics.</action>
  <done>Weaver can correlate across the four graph dimensions without introducing a parallel correlation path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist explainable evidence chains</name>
  <files>
    crates/swarm-spine/src/incident.rs
    crates/swarm-runtime/src/service.rs
  </files>
  <action>Attach readable evidence chains, attribution metadata, and confidence details to durable correlated incidents and any surfaced artifact views that already expose them.</action>
  <done>Operators and downstream code can inspect why an incident was stitched together instead of only seeing a summary label.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove bounded multi-graph correlation end to end</name>
  <files>
    crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs
    crates/swarm-runtime/src/weaver_agent.rs
  </files>
  <action>Add focused integration coverage showing completed investigation bundles produce explainable multi-graph incidents without changing hot-path detection behavior.</action>
  <done>The new correlation depth is exercised through the shipped async lane and remains bounded to async processing.</done>
</task>

</tasks>

<verification>
Keep the phase correlation-focused. Scheduling, queue fairness, and operator-facing async surfaces belong to later phases in the milestone.
</verification>
