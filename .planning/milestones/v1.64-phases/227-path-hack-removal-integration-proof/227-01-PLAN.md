---
phase: 227-path-hack-removal-integration-proof
plan: 01
type: execute
wave: 1
depends_on:
  - 226
files_modified:
  - .planning/phases/227-path-hack-removal-integration-proof
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - PATHFIX-04
must_haves:
  truths:
    - "The affected crates must still build after the path-hack removal."
    - "The refactor must retain the runtime's existing library-test coverage."
    - "Production targets for the affected crates must stay clippy-clean."
  artifacts:
    - path: ".planning/phases/227-path-hack-removal-integration-proof"
      provides: "Summary and verification proof for the final build/test/lint gates."
---

<objective>
Produce the final build/test/lint proof for the path-hack removal.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build proof</name>
  <files>
    crates/swarm-runtime
    crates/swarm-evolution
  </files>
  <action>Run targeted cargo checks for the affected crates after the refactor.</action>
  <done>The affected crates build successfully through the new module boundary.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Test proof</name>
  <files>
    crates/swarm-runtime
    crates/swarm-evolution
  </files>
  <action>Run the affected crates' library tests to prove the refactor preserved behavior.</action>
  <done>The runtime library test suite passes and the compatibility crate remains healthy.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Lint proof</name>
  <files>
    crates/swarm-runtime
    crates/swarm-evolution
  </files>
  <action>Run clippy on the affected crates' production targets with `-D warnings`.</action>
  <done>The affected crates' library/bin targets are clippy-clean after the path-hack removal.</done>
</task>

</tasks>

<verification>
Verify the affected crates with `cargo check`, `cargo test --lib`, and
production-target `cargo clippy -D warnings`.
</verification>
