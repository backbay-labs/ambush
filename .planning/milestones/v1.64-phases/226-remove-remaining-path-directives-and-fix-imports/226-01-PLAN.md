---
phase: 226-remove-remaining-path-directives-and-fix-imports
plan: 01
type: execute
wave: 1
depends_on:
  - 225
files_modified:
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-evolution/src/lib.rs
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - PATHFIX-03
must_haves:
  truths:
    - "There must be no surviving runtime `#[path]` bridge back into `swarm-evolution`."
    - "The runtime/evolution surface has to compile through normal crate and module paths after the source move."
  artifacts:
    - path: "crates/swarm-runtime/src/lib.rs"
      provides: "Normal module declarations with no cross-crate source inclusion."
---

<objective>
Confirm that the path directives are gone and the workspace imports are now
normal Rust module edges.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove any remaining path directives</name>
  <files>
    crates/swarm-runtime/src/lib.rs
  </files>
  <action>Confirm the runtime crate no longer uses `#[path = "../../swarm-evolution/..."]` for the former evolution modules.</action>
  <done>No runtime source file includes modules from `swarm-evolution/src/` via `#[path]`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Validate import normalization</name>
  <files>
    crates/swarm-runtime/src
    crates/swarm-evolution/src/lib.rs
  </files>
  <action>Verify the runtime and compatibility crate surfaces compile through ordinary `pub mod` and `pub use` module paths.</action>
  <done>The affected crates build without relying on source inclusion to bridge the boundary.</done>
</task>

</tasks>

<verification>
Verify that no `#[path = "../../swarm-evolution/..."]` declarations remain and
that the affected crates compile through normal module paths.
</verification>
