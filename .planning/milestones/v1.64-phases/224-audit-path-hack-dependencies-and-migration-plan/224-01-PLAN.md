---
phase: 224-audit-path-hack-dependencies-and-migration-plan
plan: 01
type: execute
wave: 1
depends_on:
  - v1.63
files_modified:
  - .planning/phases/224-audit-path-hack-dependencies-and-migration-plan
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - PATHFIX-01
must_haves:
  truths:
    - "The audit must identify every active `#[path]` bridge and the runtime/evolution dependency cycle it is hiding."
    - "The output must give Phase 225 a concrete bridge extraction strategy instead of just restating that the path hacks are bad."
    - "Phase 224 is documentation and planning only; it should not tangle the actual refactor into the audit phase."
  artifacts:
    - path: ".planning/phases/224-audit-path-hack-dependencies-and-migration-plan"
      provides: "Context, plan, summary, and verification artifacts describing the current hack surface and the migration plan."
---

<objective>
Produce the path-hack audit and migration plan that will drive the refactor in
Phases 225-227.
</objective>

<context>
@.planning/phases/224-audit-path-hack-dependencies-and-migration-plan/224-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Inventory the path-hack surface and dependency cycle</name>
  <files>
    crates/swarm-runtime/src/lib.rs
    crates/swarm-evolution/src
  </files>
  <action>Catalog all ten `#[path]` imports in `swarm-runtime`, the runtime modules that consume them, and the runtime-owned modules that `swarm-evolution` currently imports back through its crate surface.</action>
  <done>The audit identifies both sides of the boundary clearly enough that the later refactor can be scoped without rediscovery.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Define the migration strategy for Phases 225-227</name>
  <files>
    .planning/phases/224-audit-path-hack-dependencies-and-migration-plan
    .planning/ROADMAP.md
  </files>
  <action>Turn the audit into a concrete staged plan that breaks the crate cycle first, then swaps runtime over to normal `swarm-evolution` re-exports, then proves build/test/tooling health.</action>
  <done>The phase summary calls out the recommended bridge extraction order, the key risks, and the exact handoff to the next three phases.</done>
</task>

</tasks>

<verification>
Verify that the audit captured the ten runtime path hacks, the reverse
`swarm-evolution -> swarm-runtime` dependency surface, and a baseline
`cargo check -p swarm-runtime` result for later comparison.
</verification>
