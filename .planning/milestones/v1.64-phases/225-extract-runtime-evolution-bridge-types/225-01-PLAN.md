---
phase: 225-extract-runtime-evolution-bridge-types
plan: 01
type: execute
wave: 1
depends_on:
  - 224
files_modified:
  - crates/swarm-runtime/src
  - crates/swarm-evolution/src/lib.rs
  - .planning/phases/225-extract-runtime-evolution-bridge-types
autonomous: true
requirements:
  - PATHFIX-02
must_haves:
  truths:
    - "The runtime/evolution cycle has to be removed with real crate boundaries, not another indirection that keeps `#[path]` debt alive."
    - "The moved modules already compile under the runtime crate root today; the refactor should exploit that instead of rewriting their internals unnecessarily."
    - "`swarm-evolution` must stay buildable as a compatibility facade so the workspace still has a normal crate surface after the source move."
  artifacts:
    - path: "crates/swarm-runtime/src"
      provides: "Runtime-owned source for the former path-hacked evolution modules."
    - path: "crates/swarm-evolution/src/lib.rs"
      provides: "Compatibility re-export facade over the runtime-owned modules."
---

<objective>
Break the runtime/evolution source-inclusion cycle by making `swarm-runtime`
the real owner of the moved modules and turning `swarm-evolution` into a thin
compatibility crate.
</objective>

<context>
@.planning/phases/225-extract-runtime-evolution-bridge-types/225-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Move the path-hacked source into swarm-runtime</name>
  <files>
    crates/swarm-runtime/src
    crates/swarm-evolution/src
  </files>
  <action>Move the ten path-hacked files plus their `evolution/` and `mutation/` support trees into `crates/swarm-runtime/src/`, then replace the `#[path]` declarations with normal module declarations.</action>
  <done>`swarm-runtime` owns the source tree directly and no longer relies on `#[path]` to compile those modules from another crate directory.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Preserve the swarm-evolution crate surface as re-exports</name>
  <files>
    crates/swarm-evolution/src/lib.rs
  </files>
  <action>Replace the current runtime-wrapper plus source-module mix with a thin `pub use swarm_runtime::{...};` compatibility facade over the moved modules and shared runtime helpers.</action>
  <done>`swarm-evolution` still builds and exposes the expected modules without owning duplicate source files.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Prove the bridge extraction compiles</name>
  <files>
    crates/swarm-runtime
    crates/swarm-evolution
  </files>
  <action>Run targeted cargo checks for the affected crates after the move and capture any import or module-path fallout for Phase 226.</action>
  <done>The phase ends with both crates compiling through normal Rust module ownership rather than runtime path inclusion.</done>
</task>

</tasks>

<verification>
Verify that `crates/swarm-runtime/src/lib.rs` no longer contains the ten
`#[path]` module declarations, `swarm-evolution` builds as a facade, and the
affected crates pass targeted `cargo check`.
</verification>
