---
phase: 214-composable-playbook-yaml-schema
plan: 01
type: execute
wave: 1
depends_on:
  - phase-213
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/pounce_agent.rs
  - crates/swarm-runtime/tests/pounceagent_integration.rs
autonomous: true
requirements:
  - RESPONSE-03
must_haves:
  truths:
    - "Composable playbooks must stay repo-owned YAML and deterministic; Phase 214 should not introduce a general-purpose scripting surface."
    - "Conditional branching must remain bounded to operator-readable runtime context such as threat class, severity, confidence, or equivalent rule inputs."
    - "Composed playbooks must continue to execute through the existing approval-aware runtime response path."
  artifacts:
    - path: "crates/swarm-core/src/config.rs"
      provides: "Typed config schema and validation for multi-step conditional response playbooks."
    - path: "crates/swarm-runtime/src/pounce_agent.rs"
      provides: "Runtime execution of composed playbook steps through the existing guarded action lane."
    - path: "crates/swarm-runtime/tests/pounceagent_integration.rs"
      provides: "Focused proof that repo-owned YAML playbooks can branch and emit ordered runtime actions."
---

<objective>
Add a repo-owned YAML schema for multi-step conditional response playbooks so
operators can compose bounded action sequences without hard-coding them in Rust.
</objective>

<context>
@.planning/phases/214-composable-playbook-yaml-schema/214-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/pounce_agent.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend the response playbook config into a typed composable YAML schema</name>
  <files>
    crates/swarm-core/src/config.rs
  </files>
  <action>Add repo-owned config and validation for multi-step response playbooks with bounded conditional branching over runtime context.</action>
  <done>Operators can express composed response sequences in YAML, and invalid or ambiguous playbooks fail validation cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Route composed playbooks through the existing guarded response lane</name>
  <files>
    crates/swarm-runtime/src/pounce_agent.rs
    crates/swarm-runtime/tests/pounceagent_integration.rs
  </files>
  <action>Teach the runtime playbook executor to evaluate composed YAML playbooks and emit ordered response actions without bypassing policy, approval, or governance.</action>
  <done>Repo-owned tests prove conditional playbooks branch deterministically and still execute through the existing response request path.</done>
</task>

</tasks>

<verification>
Verify that repo-owned YAML playbooks can express bounded multi-step branching
and that the runtime executes the resulting actions through the existing
approval-aware response path.
</verification>
