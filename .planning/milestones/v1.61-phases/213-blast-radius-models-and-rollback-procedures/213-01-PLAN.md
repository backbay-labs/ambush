---
phase: 213-blast-radius-models-and-rollback-procedures
plan: 01
type: execute
wave: 1
depends_on:
  - phase-212
files_modified:
  - crates/swarm-core/src/types.rs
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime
autonomous: true
requirements:
  - RESPONSE-02
must_haves:
  truths:
    - "Blast-radius and rollback metadata must be typed and shared, not duplicated across preview and rehearsal paths."
    - "Every supported response action must define bounded impact semantics or explicitly declare the lack of rollback."
    - "Phase 213 should complete typed action metadata, not full playbook composition or UI preview orchestration."
  artifacts:
    - path: "crates/swarm-core/src/types.rs"
      provides: "Typed blast-radius and rollback contract coverage for the expanded response catalog."
    - path: "crates/swarm-runtime/src/service.rs"
      provides: "Runtime-owned preview builder that covers the full supported response catalog."
    - path: "crates/swarm-runtime"
      provides: "Focused proof that typed preview metadata exists for the expanded action set."
---

<objective>
Add typed blast-radius and rollback contracts for the expanded response action
catalog so later rehearsal and playbook preview work can build on one shared
semantic surface.
</objective>

<context>
@.planning/phases/213-blast-radius-models-and-rollback-procedures/213-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/types.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/service.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend typed blast-radius and rollback coverage for all response actions</name>
  <files>
    crates/swarm-core/src/types.rs
    crates/swarm-runtime/src/service.rs
  </files>
  <action>Replace the temporary unsupported-preview behavior for the new actions with typed blast-radius and rollback semantics on the shared preview contract.</action>
  <done>Every supported response action has an explicit typed impact and rollback description through the existing rehearsal-preview seam.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Prove the runtime surfaces the typed metadata for the expanded catalog</name>
  <files>
    crates/swarm-runtime
  </files>
  <action>Add focused proof that the runtime can build typed rehearsal or preview metadata for the newly added response actions.</action>
  <done>The expanded response catalog no longer fails closed at the preview seam, and the typed metadata is covered by repo-owned tests.</done>
</task>

</tasks>

<verification>
Verify that every supported response action now has a typed blast-radius and
rollback contract through the shared runtime preview seam.
</verification>
