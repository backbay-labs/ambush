---
phase: 215-playbook-dry-run-and-preview
plan: 01
type: execute
wave: 1
depends_on:
  - phase-214
files_modified:
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-cli/src/core.inc
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - RESPONSE-04
must_haves:
  truths:
    - "Preview must reuse the same typed blast-radius and rollback metadata already used by rehearsal and live execution; no heuristic-only preview contract."
    - "Preview must remain dry-run and operator-facing; it must not create live executor side effects or governance state transitions."
    - "Conditional playbook evaluation must honor the repo-owned branching semantics added in Phase 214."
  artifacts:
    - path: "crates/swarm-runtime/src/service.rs"
      provides: "Shared builder that materializes one full playbook preview from typed action metadata."
    - path: "crates/swarm-runtime/src/control.rs"
      provides: "Operator-visible control-plane preview report for one matched composed playbook."
    - path: "crates/swarm-cli/src/core.inc"
      provides: "Repo-owned CLI entrypoint for dry-run playbook preview."
    - path: "docs/CONFIGURATION.md"
      provides: "Operator-facing preview contract and usage guidance."
---

<objective>
Preview projected blast radius and approval requirements for composed response
playbooks before any live execution.
</objective>

<context>
@.planning/phases/215-playbook-dry-run-and-preview/215-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/service.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/control.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-cli/src/core.inc
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Materialize one full dry-run preview from the repo-owned playbook contract</name>
  <files>
    crates/swarm-runtime/src/service.rs
  </files>
  <action>Compose the Phase 213 typed rehearsal metadata across the ordered Phase 214 playbook actions so one matched playbook can render projected blast radius, rollback expectations, and approval requirements without executing live actions.</action>
  <done>The runtime can build a deterministic preview report for a matched playbook using the same typed action metadata as rehearsal and live execution.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Expose playbook preview through the repo-owned operator surface</name>
  <files>
    crates/swarm-runtime/src/control.rs
    crates/swarm-cli/src/core.inc
    docs/CONFIGURATION.md
  </files>
  <action>Add a bounded operator-facing preview entrypoint and document how operators trigger it against the checked-in config.</action>
  <done>Operators can invoke one repo-owned preview surface and receive the same dry-run playbook report without touching live executors.</done>
</task>

</tasks>

<verification>
Verify that one matched composed playbook can be previewed without live side
effects and that the preview surfaces the same typed blast-radius, rollback,
and approval semantics that would govern the live path.
</verification>
