---
phase: 212-response-action-adapter-expansion
plan: 01
type: execute
wave: 1
depends_on:
  - phase-211
files_modified:
  - crates/swarm-core/src/types.rs
  - crates/swarm-response
  - crates/swarm-runtime
autonomous: true
requirements:
  - RESPONSE-01
must_haves:
  truths:
    - "Action expansion must reuse the existing typed response seam instead of introducing a parallel playbook-only execution contract."
    - "Each newly supported action must fail closed when the configured adapter cannot execute it."
    - "Phase 212 should land concrete action coverage, not blast-radius or playbook composition work that belongs to later phases."
  artifacts:
    - path: "crates/swarm-core/src/types.rs"
      provides: "Expanded typed response-action catalog."
    - path: "crates/swarm-response"
      provides: "Adapter execution support for the expanded action set."
    - path: "crates/swarm-runtime"
      provides: "Runtime wiring and proof that the new action library routes through the normal approval and execution lane."
---

<objective>
Expand the response adapter library to materially broader concrete action
coverage while preserving the existing typed and fail-closed execution contract.
</objective>

<context>
@.planning/phases/212-response-action-adapter-expansion/212-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/types.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-response
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expand the typed response-action catalog</name>
  <files>
    crates/swarm-core/src/types.rs
    crates/swarm-response
  </files>
  <action>Add the broader concrete response actions required by v1.61 and wire them through the shared adapter contract.</action>
  <done>The codebase owns materially broader typed action coverage beyond the current minimal set.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Prove runtime routing for the expanded action set</name>
  <files>
    crates/swarm-response
    crates/swarm-runtime
  </files>
  <action>Verify that the expanded actions route through the existing approval and execution lane with fail-closed behavior on unsupported adapters.</action>
  <done>The runtime and adapter layer can execute or explicitly reject the expanded response-action catalog through repo-owned proof.</done>
</task>

</tasks>

<verification>
Verify that the response adapter library now supports materially broader typed
action coverage and that the runtime routes those actions through the normal
fail-closed approval and execution seam.
</verification>
