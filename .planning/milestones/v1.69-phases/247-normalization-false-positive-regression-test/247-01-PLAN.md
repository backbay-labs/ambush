---
phase: 247-normalization-false-positive-regression-test
plan: 01
type: execute
wave: 1
depends_on:
  - 246-evasion-catch-rate-improvement-measurement
files_modified:
  - crates/swarm-runtime/src/evasion_coverage.rs
  - scenarios/command-line-deobfuscation-benign.yaml
  - scenario-suites/command-line-deobfuscation-v1.yaml
  - .planning/phases/247-normalization-false-positive-regression-test
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - DEOBF-04
must_haves:
  truths:
    - "The benign proof must run from repo-owned scenarios, not ad hoc shell snippets."
    - "Regression means normalization-enabled must not exceed the baseline false-positive count."
    - "The final milestone closeout must name both the detection gain and the benign safety result."
  artifacts:
    - path: ".planning/phases/247-normalization-false-positive-regression-test"
      provides: "Context, plan, summary, and verification artifacts for Phase 247."
---

<objective>
Add benign controls to the command-line normalization corpus and prove the new
normalization seam introduces zero false-positive regression across the affected
detector family.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add benign command-line normalization controls</name>
  <files>
    scenarios/command-line-deobfuscation-benign.yaml
    scenario-suites/command-line-deobfuscation-v1.yaml
  </files>
  <action>Add benign command lines that exercise caret, env-var, Unicode, and shell-surface inputs without representing malicious behavior.</action>
  <done>The command-line normalization suite includes benign controls suitable for repeatable regression checking.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add baseline-vs-normalized false-positive comparison</name>
  <files>
    crates/swarm-runtime/src/evasion_coverage.rs
  </files>
  <action>Evaluate the benign controls with normalization disabled and enabled across the command-line detector family, then persist the per-detector false-positive deltas.</action>
  <done>The runtime exposes a repo-owned benign regression surface for the command-line normalization seam.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Prove zero false-positive regression</name>
  <files>
    crates/swarm-runtime/src/evasion_coverage.rs
  </files>
  <action>Add focused runtime tests asserting baseline and normalized benign false positives both remain zero across the affected detector family.</action>
  <done>Focused `swarm-runtime` tests prove the command-line normalization seam does not increase benign false positives.</done>
</task>

</tasks>

<verification>
Verify with focused `swarm-runtime` benign-regression tests for the
command-line normalization suite.
</verification>
