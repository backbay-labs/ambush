---
phase: 244-caret-and-environment-variable-normalization
plan: 01
type: execute
wave: 1
depends_on:
  - 243-cross-detector-fitness-improvement-proof
files_modified:
  - crates/swarm-whisker/src/command_line.rs
  - crates/swarm-whisker/src/detector.rs
  - crates/swarm-whisker/src/suspicious_scripting.rs
  - crates/swarm-whisker/src/fileless_execution.rs
  - crates/swarm-whisker/src/lateral_movement.rs
  - crates/swarm-whisker/src/supply_chain.rs
  - crates/swarm-whisker/src/lib.rs
  - Cargo.toml
  - crates/swarm-whisker/Cargo.toml
  - .planning/phases/244-caret-and-environment-variable-normalization
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - DEOBF-01
must_haves:
  truths:
    - "Caret stripping and env-var expansion must happen once in a shared detector seam."
    - "The detector evidence must preserve raw-to-normalized command lineage."
    - "Already-normalized command lines must keep their prior detector behavior."
  artifacts:
    - path: ".planning/phases/244-caret-and-environment-variable-normalization"
      provides: "Context, plan, summary, and verification artifacts for Phase 244."
---

<objective>
Add a shared command-line normalization seam for caret removal and bounded
environment-variable expansion, then route the command-line detector families
through that seam without changing their raw event evidence.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Introduce shared command-line normalization and profile controls</name>
  <files>
    crates/swarm-whisker/src/command_line.rs
    crates/swarm-whisker/src/lib.rs
    Cargo.toml
    crates/swarm-whisker/Cargo.toml
  </files>
  <action>Add a shared normalizer with auditable transforms plus a defaulted profile seam that detectors can disable for baseline comparison.</action>
  <done>The repo exposes one reusable command-line normalization helper and profile instead of duplicating raw string transforms per detector.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Route command-line detectors through the shared seam</name>
  <files>
    crates/swarm-whisker/src/detector.rs
    crates/swarm-whisker/src/suspicious_scripting.rs
    crates/swarm-whisker/src/fileless_execution.rs
    crates/swarm-whisker/src/lateral_movement.rs
    crates/swarm-whisker/src/supply_chain.rs
  </files>
  <action>Replace per-detector raw lowercase matching with shared normalized matching while keeping raw command lines in the evidence payloads.</action>
  <done>The command-line detector families consume normalized `match_text` and expose raw plus normalized lineage in their evidence.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Prove caret and env-var normalization works without regressions</name>
  <files>
    crates/swarm-whisker/src/command_line.rs
    crates/swarm-whisker/src/suspicious_scripting.rs
  </files>
  <action>Add focused tests covering caret stripping and `%ComSpec%` / `$env:` expansion against the shared normalizer and scripting detector path.</action>
  <done>Focused unit tests prove the seam handles caret and env-var obfuscation while already-normalized inputs still behave normally.</done>
</task>

</tasks>

<verification>
Verify with focused `swarm-whisker` command-line and suspicious-scripting tests,
then run the crate test slice.
</verification>
