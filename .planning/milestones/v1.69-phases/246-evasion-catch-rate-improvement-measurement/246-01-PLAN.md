---
phase: 246-evasion-catch-rate-improvement-measurement
plan: 01
type: execute
wave: 1
depends_on:
  - 245-unicode-homoglyph-and-encoding-normalization
files_modified:
  - crates/swarm-runtime/src/evasion_coverage.rs
  - scenario-suites/command-line-deobfuscation-v1.yaml
  - scenarios/command-line-deobfuscation-execution.yaml
  - scenarios/command-line-deobfuscation-defense-evasion.yaml
  - rulesets/evasion/command-line-deobfuscation-catalog.yaml
  - .planning/phases/246-evasion-catch-rate-improvement-measurement
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - DEOBF-03
must_haves:
  truths:
    - "The comparison must isolate normalization alone by toggling the new profile seam, not by swapping detector logic."
    - "The benchmark proof must live on repo-owned corpus and report structures."
    - "The result must identify which detector lanes improved and which scenarios were used."
  artifacts:
    - path: ".planning/phases/246-evasion-catch-rate-improvement-measurement"
      provides: "Context, plan, summary, and verification artifacts for Phase 246."
---

<objective>
Add a repo-owned command-line deobfuscation adversarial corpus and a runtime
comparison helper that proves normalization improves execution and
defense-evasion catch rate by at least 15%.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add a tracked command-line deobfuscation adversarial corpus</name>
  <files>
    scenario-suites/command-line-deobfuscation-v1.yaml
    scenarios/command-line-deobfuscation-execution.yaml
    scenarios/command-line-deobfuscation-defense-evasion.yaml
    rulesets/evasion/command-line-deobfuscation-catalog.yaml
  </files>
  <action>Add repo-owned execution and defense-evasion scenarios whose command-line indicators only become visible after normalization.</action>
  <done>The repo contains a named suite and catalog that can be replayed repeatedly for normalization measurement.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add baseline-vs-normalized benchmark helpers</name>
  <files>
    crates/swarm-runtime/src/evasion_coverage.rs
  </files>
  <action>Build baseline and normalized configs from the same runtime settings, evaluate both against the new suite, and persist per-detector threat-class catch-rate deltas.</action>
  <done>The runtime exposes a repo-owned command-line normalization benchmark surface with named detectors and scenarios.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Prove the benchmark threshold</name>
  <files>
    crates/swarm-runtime/src/evasion_coverage.rs
  </files>
  <action>Add focused runtime tests asserting at least 15% improvement for the targeted execution and defense-evasion lanes.</action>
  <done>Focused `swarm-runtime` tests prove the normalization benchmark exceeds the required catch-rate threshold.</done>
</task>

</tasks>

<verification>
Verify with focused `swarm-runtime` evasion-coverage tests for the normalization
benchmark.
</verification>
