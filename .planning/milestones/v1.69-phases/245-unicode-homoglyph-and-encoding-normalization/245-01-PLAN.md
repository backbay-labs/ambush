---
phase: 245-unicode-homoglyph-and-encoding-normalization
plan: 01
type: execute
wave: 1
depends_on:
  - 244-caret-and-environment-variable-normalization
files_modified:
  - crates/swarm-whisker/src/command_line.rs
  - crates/swarm-whisker/src/detector.rs
  - crates/swarm-whisker/src/suspicious_scripting.rs
  - crates/swarm-whisker/src/fileless_execution.rs
  - crates/swarm-whisker/src/lateral_movement.rs
  - crates/swarm-whisker/src/supply_chain.rs
  - .planning/phases/245-unicode-homoglyph-and-encoding-normalization
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - DEOBF-02
must_haves:
  truths:
    - "Unicode normalization must stay bounded and explainable."
    - "Encoded-command decoding must preserve raw lineage while surfacing decoded indicators to detectors."
    - "Phase 245 must compose with the Phase 244 shared normalizer instead of introducing a second seam."
  artifacts:
    - path: ".planning/phases/245-unicode-homoglyph-and-encoding-normalization"
      provides: "Context, plan, summary, and verification artifacts for Phase 245."
---

<objective>
Extend the shared command-line normalizer with bounded homoglyph folding and
encoded-argument decoding, then prove the affected detector families can match
the normalized content.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add bounded homoglyph and encoded-argument normalization</name>
  <files>
    crates/swarm-whisker/src/command_line.rs
  </files>
  <action>Add fullwidth and common confusable-character folding plus bounded PowerShell encoded-argument and `FromBase64String` decoding support.</action>
  <done>The shared normalizer produces explainable transform labels and decoded command segments for the supported obfuscation forms.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Ensure the command-line detector lanes consume the extended normalization seam</name>
  <files>
    crates/swarm-whisker/src/detector.rs
    crates/swarm-whisker/src/suspicious_scripting.rs
    crates/swarm-whisker/src/fileless_execution.rs
    crates/swarm-whisker/src/lateral_movement.rs
    crates/swarm-whisker/src/supply_chain.rs
  </files>
  <action>Keep detector logic on normalized `match_text` so decoded payload indicators and folded homoglyphs influence matching without rewriting each detector separately.</action>
  <done>Execution, defense-evasion, remote-admin, and signed-binary-abuse lanes all see the normalized command content through one helper.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add focused Unicode and decoding tests</name>
  <files>
    crates/swarm-whisker/src/command_line.rs
    crates/swarm-whisker/src/fileless_execution.rs
    crates/swarm-whisker/src/suspicious_scripting.rs
    crates/swarm-whisker/src/lateral_movement.rs
    crates/swarm-whisker/src/supply_chain.rs
  </files>
  <action>Add unit tests covering fullwidth/confusable indicators, encoded PowerShell payload decode, and detector matching that depends on the decoded or folded content.</action>
  <done>The repo has direct tests proving the Unicode and encoded-command seam works across the affected detectors.</done>
</task>

</tasks>

<verification>
Verify with focused `swarm-whisker` Unicode/decoding tests and the full crate
library test slice.
</verification>
