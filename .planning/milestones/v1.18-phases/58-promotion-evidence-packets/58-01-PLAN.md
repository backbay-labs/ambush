# Plan 58-01

1. Add durable promotion evidence packet types and storage above the existing production-promotion artifact.
2. Assemble packet attachments from signed evidence bundle summaries plus latest verification status for promotion, canary, verification, and shadow artifacts.
3. Wire `swarmctl` and authenticated HTTP lookup for persisted promotion evidence packets.
4. Verify both blocked and ready packet assembly paths without introducing approval or rollout automation.
