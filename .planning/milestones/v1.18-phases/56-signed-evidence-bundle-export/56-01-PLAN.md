# Plan 56-01

1. Implement minimal repo-owned crypto primitives for canonical JSON bytes, SHA-256 digests, and Ed25519 detached signatures.
2. Add a runtime evidence module with signed bundle types, file-backed bundle storage, and export harnesses for persisted runtime and rollout artifacts.
3. Wire `swarmctl` commands for evidence export, lookup, and listing with config-backed result directories and signing-key env defaults.
4. Document the new evidence export flow and verify it with focused tests plus workspace checks.
