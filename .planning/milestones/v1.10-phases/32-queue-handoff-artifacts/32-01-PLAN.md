# Plan 32-01

1. Add durable handoff packet types and a file-backed store with stable handoff IDs.
2. Build a handoff harness that assembles packets from accepted proposals plus passed shadow evidence.
3. Add `swarmctl` commands to create and reload handoff packets.
4. Cover handoff persistence and stable-ID reload behavior with unit tests.
