# Plan 34-01

1. Add `swarmctl` commands to inspect handoff packets and launch canary from a stable handoff ID.
2. Persist launch metadata and resulting canary-run references on the handoff artifact.
3. Ensure launch remains operator-triggered and never starts automatically from queue review alone.
4. Document the queue-to-canary handoff workflow and verify it end to end.
