---
phase: 127-integration-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 127-01
files_modified:
  - crates/swarm-runtime/tests/dispatch_integration.rs
autonomous: true
requirements:
  - DEESC-01
  - DEESC-02
must_haves:
  truths:
    - "A burst-decay-burst sequence does not retrigger routed autonomous response before cooldown-driven de-escalation resets the session."
    - "The full workspace test suite stays green after all v1.39 hardening changes."
    - "The full workspace clippy gate stays green with warnings denied."
  artifacts:
    - path: "crates/swarm-runtime/tests/dispatch_integration.rs"
      provides: "Shared-mode-state proof that cooldown prevents premature re-trigger."
  key_links:
    - from: "crates/swarm-runtime/src/escalation.rs"
      to: "crates/swarm-runtime/src/dispatcher.rs"
      via: "shared `SwarmModeState` governing when PounceAgent session dedupe resets"
      pattern: "with_shared_mode_state|with_mode_state"
    - from: "crates/swarm-runtime/src/pounce_agent.rs"
      to: "crates/swarm-runtime/tests/dispatch_integration.rs"
      via: "same-session handled-action dedupe surviving quiet periods before cooldown reset"
      pattern: "current_session"
---

<objective>
Prove cooldown-driven session reset behavior and close the milestone with workspace-level validation.

Purpose: Turn the remaining de-escalation pitfall into a dispatcher-backed integration proof, then finish v1.39 with green workspace tests and lint.
Output: burst-decay-burst routed proof, green `cargo test --workspace`, and green `cargo clippy --workspace -- -D warnings`.
</objective>

<context>
@.planning/phases/127-integration-hardening/127-CONTEXT.md
@.planning/phases/127-integration-hardening/127-RESEARCH.md
@.planning/phases/127-integration-hardening/127-VALIDATION.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/escalation.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/pounce_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/tests/dispatch_integration.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cooldown re-trigger proof on the routed path</name>
  <read_first>
    crates/swarm-runtime/src/escalation.rs
    crates/swarm-runtime/src/pounce_agent.rs
    crates/swarm-runtime/tests/dispatch_integration.rs
  </read_first>
  <files>
    crates/swarm-runtime/tests/dispatch_integration.rs
  </files>
  <action>Add the exact test `burst_decay_burst_does_not_retrigger_pounceagent_before_cooldown_reset`. Share one `SwarmModeState` between `ConcentrationMonitor` and `AgentDispatcher`, prove the first burst routes exactly one response, prove a second burst before cooldown completion routes none, and optionally prove a post-deescalation burst can route again once the session resets.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test dispatch_integration burst_decay_burst_does_not_retrigger_pounceagent_before_cooldown_reset -- --exact</automated>
  </verify>
  <done>Cooldown resistance is proven at the dispatcher-backed routed-execution layer instead of only in the concentration monitor.</done>
</task>

<task type="auto">
  <name>Task 2: Run milestone-wide verification and lint gates</name>
  <read_first>
    .planning/phases/127-integration-hardening/127-VALIDATION.md
  </read_first>
  <files>
    .planning/phases/127-integration-hardening/127-VERIFICATION.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <action>Run `cargo test --workspace` and `cargo clippy --workspace -- -D warnings`. If both pass, write the Phase 127 verification report and update roadmap/state for milestone closeout readiness.</action>
  <verify>
    <automated>cargo test --workspace</automated>
    <automated>cargo clippy --workspace -- -D warnings</automated>
  </verify>
  <done>Phase 127 closes with green workspace tests, green lint, and updated planning artifacts.</done>
</task>

</tasks>

<verification>
After the exact cooldown test passes, run `cargo test -p swarm-runtime --test dispatch_integration`, then the full workspace test and clippy gates.
</verification>

<output>
After completion, create `.planning/phases/127-integration-hardening/127-02-SUMMARY.md`
</output>
