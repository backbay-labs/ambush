---
phase: 126-tomagent-governance
plan: 03
type: execute
wave: 3
depends_on:
  - 126-01
  - 126-02
files_modified:
  - crates/swarm-runtime/src/tom_agent.rs
  - crates/swarm-runtime/src/pounce_agent.rs
  - crates/swarm-runtime/tests/pounceagent_integration.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
autonomous: true
requirements:
  - TOM-02
must_haves:
  truths:
    - "`GovernancePolicy` is a shared `Arc` updated by TomAgent and read by PounceAgent."
    - "`GovernancePolicy::can_act()` vetoes destructive actions synchronously inside `PounceAgent::tick()`."
    - "Non-destructive actions still flow through the existing autonomous response path."
  artifacts:
    - path: "crates/swarm-runtime/src/tom_agent.rs"
      provides: "Shared governance policy state and veto semantics."
    - path: "crates/swarm-runtime/src/pounce_agent.rs"
      provides: "Synchronous governance check before autonomous response emission."
    - path: "crates/swarm-runtime/tests/pounceagent_integration.rs"
      provides: "Proof that PounceAgent emits governance veto intent instead of `RequestResponse` when blocked."
  key_links:
    - from: "crates/swarm-runtime/src/tom_agent.rs"
      to: "crates/swarm-runtime/src/pounce_agent.rs"
      via: "`Arc<GovernancePolicy>` shared ref"
      pattern: "can_act"
---

<objective>
Insert synchronous governance veto into the autonomous response decision point.

Purpose: Deliver the real TOM-02 safety property by preventing destructive actions before the dispatcher ever routes them toward execution.
Output: Shared governance policy state, destructive-action veto semantics, and PounceAgent integration tests proving veto happens in tick-time.
</objective>

<context>
@.planning/phases/126-tomagent-governance/126-CONTEXT.md
@.planning/phases/126-tomagent-governance/126-RESEARCH.md
@.planning/phases/126-tomagent-governance/126-VALIDATION.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/pounce_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/tests/pounceagent_integration.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed governance policy and PounceAgent veto tests</name>
  <read_first>
    crates/swarm-runtime/src/pounce_agent.rs
    crates/swarm-runtime/tests/pounceagent_integration.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/tom_agent.rs
    crates/swarm-runtime/tests/pounceagent_integration.rs
  </files>
  <action>Add exact tests `governance_policy_vetoes_destructive_actions_when_swarm_is_unhealthy` and `pounceagent_emits_governance_veto_for_destructive_action`. The PounceAgent test must prove the agent does not emit `RequestResponse` when governance blocks a destructive action.</action>
  <verify>
    <automated>rg -n "governance_policy_vetoes_destructive_actions_when_swarm_is_unhealthy|pounceagent_emits_governance_veto_for_destructive_action" crates/swarm-runtime/src/tom_agent.rs crates/swarm-runtime/tests/pounceagent_integration.rs</automated>
  </verify>
  <done>The veto semantics are pinned to the correct synchronous insertion point.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement shared governance policy and synchronous veto</name>
  <read_first>
    crates/swarm-runtime/src/tom_agent.rs
    crates/swarm-runtime/src/pounce_agent.rs
    crates/swarm-runtime/src/bin/swarm_detect.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/tom_agent.rs
    crates/swarm-runtime/src/pounce_agent.rs
    crates/swarm-runtime/tests/pounceagent_integration.rs
    crates/swarm-runtime/src/bin/swarm_detect.rs
  </files>
  <action>Implement `GovernancePolicy`, share it between TomAgent and PounceAgent, and teach `PounceAgent::tick()` to emit governance-veto intent for destructive blocked actions while allowing non-destructive actions to continue through the normal path.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test pounceagent_integration pounceagent_emits_governance_veto_for_destructive_action -- --exact</automated>
  </verify>
  <done>Destructive PounceAgent actions are synchronously blocked by governance before dispatcher routing would call the canonical runtime execution path.</done>
</task>

</tasks>

<verification>
Run the exact PounceAgent veto test first, then the full `pounceagent_integration` suite to ensure the governance seam preserves existing playbook and dedupe behavior.
</verification>

<output>
After completion, create `.planning/phases/126-tomagent-governance/126-03-SUMMARY.md`
</output>
