---
phase: 126-tomagent-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/swarm-core/src/agent.rs
  - crates/swarm-core/src/types.rs
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/config.rs
  - rulesets/default.yaml
autonomous: true
requirements:
  - TOM-01
  - TOM-02
must_haves:
  truths:
    - "Runtime config exposes a governance degraded-tick threshold that loads from the repository ruleset."
    - "`SwarmEnvironment` exposes agent-health summaries to agents each tick."
    - "Lifecycle and governance-veto actions have explicit target/request contracts suitable for TomAgent and PounceAgent."
  artifacts:
    - path: "crates/swarm-core/src/agent.rs"
      provides: "Cross-agent health-summary environment contract."
    - path: "crates/swarm-core/src/types.rs"
      provides: "Targeted lifecycle and governance-veto action types."
    - path: "rulesets/default.yaml"
      provides: "Repository-owned governance threshold."
  key_links:
    - from: "rulesets/default.yaml"
      to: "crates/swarm-core/src/config.rs"
      via: "repository config deserialization"
      pattern: "governance_degraded_tick_threshold"
---

<objective>
Establish the core contracts TomAgent governance needs before runtime behavior changes land.

Purpose: Make governance a repository-owned runtime concern and widen the agent/runtime contracts exactly once before implementation fans out.
Output: Config support for the degraded-to-failed threshold plus core agent/action contracts for health summaries, targeted lifecycle actions, and governance-veto recording.
</objective>

<context>
@.planning/phases/126-tomagent-governance/126-CONTEXT.md
@.planning/phases/126-tomagent-governance/126-RESEARCH.md
@.planning/phases/126-tomagent-governance/126-VALIDATION.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/types.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/rulesets/default.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add governance config and shared agent contracts</name>
  <read_first>
    crates/swarm-core/src/agent.rs
    crates/swarm-core/src/types.rs
    crates/swarm-core/src/config.rs
    rulesets/default.yaml
  </read_first>
  <files>
    crates/swarm-core/src/agent.rs
    crates/swarm-core/src/types.rs
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/config.rs
    rulesets/default.yaml
  </files>
  <behavior>
    - Runtime config exposes a validated governance degraded-tick threshold.
    - `SwarmEnvironment` includes agent-health summaries.
    - `SwarmAction::RoleShift` and `SwarmAction::HealthReport` can target explicit agents, and a governance-veto action/request type exists for the later routing plans.
  </behavior>
  <action>Add the governance threshold to runtime config with validation and repository defaults, move `AgentHealthEntry` into `swarm-core`, widen `SwarmEnvironment`, and extend the core action contract for targeted lifecycle actions plus governance-veto intent.</action>
  <verify>
    <automated>cargo test -p swarm-runtime config::tests::loads_repository_ruleset -- --exact</automated>
  </verify>
  <done>Core contracts and repo config are ready for TomAgent and veto implementation without ad hoc runtime-local types.</done>
</task>

</tasks>

<verification>
Run the repository config-load test first, then use `cargo check -p swarm-runtime` to catch constructor fallout from the widened core contracts.
</verification>

<output>
After completion, create `.planning/phases/126-tomagent-governance/126-01-SUMMARY.md`
</output>
