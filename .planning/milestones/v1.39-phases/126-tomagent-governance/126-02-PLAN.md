---
phase: 126-tomagent-governance
plan: 02
type: execute
wave: 2
depends_on:
  - 126-01
files_modified:
  - crates/swarm-runtime/src/dispatcher.rs
  - crates/swarm-runtime/src/tom_agent.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
autonomous: true
requirements:
  - TOM-01
must_haves:
  truths:
    - "TomAgent monitors dispatcher-provided health summaries each tick."
    - "A degraded agent is targeted with a role shift to `AgentRole::Tom`."
    - "An agent that stays degraded past the configured threshold is targeted with `HealthReport { status: Failed }`."
    - "Serve mode registers TomAgent into the live dispatcher."
  artifacts:
    - path: "crates/swarm-runtime/src/tom_agent.rs"
      provides: "TomAgent implementation and lifecycle decision tests."
    - path: "crates/swarm-runtime/src/dispatcher.rs"
      provides: "Targeted lifecycle action application."
    - path: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      provides: "Serve-mode TomAgent registration."
  key_links:
    - from: "crates/swarm-runtime/src/dispatcher.rs"
      to: "crates/swarm-runtime/src/tom_agent.rs"
      via: "targeted role/health action handling"
      pattern: "target_agent_id"
---

<objective>
Implement TomAgent’s health-monitoring and lifecycle-management behavior.

Purpose: Deliver TOM-01 by turning dispatcher health snapshots into governance actions under a deterministic threshold.
Output: TomAgent, targeted dispatcher lifecycle routing, serve-mode registration, and unit coverage for degraded and failed transitions.
</objective>

<context>
@.planning/phases/126-tomagent-governance/126-CONTEXT.md
@.planning/phases/126-tomagent-governance/126-VALIDATION.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/dispatcher.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/bin/swarm_detect.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed targeted dispatcher and TomAgent tests</name>
  <read_first>
    crates/swarm-runtime/src/dispatcher.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/tom_agent.rs
  </files>
  <action>Add exact tests for targeted dispatcher lifecycle handling and TomAgent health transitions: `dispatcher_applies_targeted_role_shift_from_tom_agent`, `dispatcher_applies_targeted_failed_health_report_from_tom_agent`, `tom_agent_shifts_degraded_agents_to_tom_role`, and `tom_agent_marks_agents_failed_after_threshold`.</action>
  <verify>
    <automated>rg -n "dispatcher_applies_targeted_role_shift_from_tom_agent|dispatcher_applies_targeted_failed_health_report_from_tom_agent|tom_agent_shifts_degraded_agents_to_tom_role|tom_agent_marks_agents_failed_after_threshold" crates/swarm-runtime/src/dispatcher.rs crates/swarm-runtime/src/tom_agent.rs</automated>
  </verify>
  <done>The lifecycle behavior is pinned down before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TomAgent lifecycle monitoring and serve-mode wiring</name>
  <read_first>
    crates/swarm-runtime/src/tom_agent.rs
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/bin/swarm_detect.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/tom_agent.rs
    crates/swarm-runtime/src/bin/swarm_detect.rs
  </files>
  <action>Implement TomAgent as a `SwarmAgent` with `AgentRole::Tom`, teach the dispatcher to apply targeted role and health actions, and register TomAgent in serve mode with the configured governance threshold.</action>
  <verify>
    <automated>cargo test -p swarm-runtime tom_agent::tests::tom_agent_shifts_degraded_agents_to_tom_role -- --exact</automated>
  </verify>
  <done>TomAgent observes health summaries, targets degraded agents for role shift, targets long-degraded agents as failed, and runs in the live dispatcher.</done>
</task>

</tasks>

<verification>
Run the exact TomAgent test first, then `cargo test -p swarm-runtime dispatcher::tests::dispatcher_applies_targeted_role_shift_from_tom_agent -- --exact`, then `cargo check -p swarm-runtime`.
</verification>

<output>
After completion, create `.planning/phases/126-tomagent-governance/126-02-SUMMARY.md`
</output>
