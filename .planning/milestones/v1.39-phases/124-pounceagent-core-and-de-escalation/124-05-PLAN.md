---
phase: 124-pounceagent-core-and-de-escalation
plan: 05
type: execute
wave: 2
depends_on:
  - 124-01
files_modified:
  - crates/swarm-runtime/src/detection/pipeline.rs
  - crates/swarm-runtime/src/stalker_agent.rs
  - crates/swarm-runtime/src/whisker_agent.rs
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/src/evidence.rs
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime/src/http/core.inc
  - crates/swarm-runtime/src/strategy.rs
  - crates/swarm-runtime/src/canary.rs
  - crates/swarm-runtime/src/promotion.rs
autonomous: true
requirements:
  - POUNCE-03
  - DEESC-02
must_haves:
  truths:
    - "Phase 124 behavior plans start from a runtime baseline that already understands the expanded pheromone config shape."
    - "Config-backed cooldown and playbook behavior can be exercised across owned runtime surfaces without hidden fixture regressions."
  artifacts:
    - path: "crates/swarm-runtime/src/detection/pipeline.rs"
      provides: "Updated runtime helper fixture using the expanded `PheromoneConfig`."
    - path: "crates/swarm-runtime/src/control.rs"
      provides: "Config-bearing runtime control fixture aligned to the new pheromone shape."
    - path: "crates/swarm-runtime/src/canary.rs"
      provides: "Repo-owned runtime harness fixture aligned to the new pheromone shape."
    - path: "crates/swarm-runtime/src/promotion.rs"
      provides: "Promotion harness fixture aligned to the new pheromone shape."
  key_links:
    - from: "crates/swarm-runtime/src/detection/pipeline.rs"
      to: "crates/swarm-core/src/config.rs"
      via: "owned `PheromoneConfig` literal"
      pattern: "PheromoneConfig \\{"
    - from: "crates/swarm-runtime/src/promotion.rs"
      to: "crates/swarm-core/src/config.rs"
      via: "repo-owned runtime fixture"
      pattern: "PheromoneConfig \\{"
---

<objective>
Normalize owned `swarm-runtime` config literals to the expanded Phase 124 pheromone shape after the seam lands.

Purpose: Keep the foundational config plan small while still giving downstream behavior plans a compile-clean runtime baseline.
Output: Mechanical `PheromoneConfig` literal updates across owned runtime modules with a crate-level compile check.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-CONTEXT.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-RESEARCH.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-VALIDATION.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/detection/pipeline.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/control.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/canary.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/promotion.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize core runtime library fixtures to the expanded pheromone shape</name>
  <read_first>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/detection/pipeline.rs
    crates/swarm-runtime/src/stalker_agent.rs
    crates/swarm-runtime/src/whisker_agent.rs
    crates/swarm-runtime/src/control.rs
    crates/swarm-runtime/src/evidence.rs
    crates/swarm-runtime/src/service.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/detection/pipeline.rs
    crates/swarm-runtime/src/stalker_agent.rs
    crates/swarm-runtime/src/whisker_agent.rs
    crates/swarm-runtime/src/control.rs
    crates/swarm-runtime/src/evidence.rs
    crates/swarm-runtime/src/service.rs
  </files>
  <action>Update the owned `PheromoneConfig { ... }` literals and helper fixtures in the listed core `swarm-runtime` library files so the new Phase 124 cooldown/playbook config shape compiles across the main runtime surface. Keep this slice mechanical: fix struct literals, default builders, and import fallout only. Do not implement de-escalation behavior, PounceAgent behavior, dispatcher routing, or test-specific assertions here; those stay in the later requirement-owning plans.</action>
  <verify>
    <automated>cargo check -p swarm-runtime --lib</automated>
  </verify>
  <done>The main owned `swarm-runtime` library fixtures understand the expanded `PheromoneConfig` shape, giving downstream behavior plans a green baseline instead of missing-field churn.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize operational runtime harness fixtures to the expanded pheromone shape</name>
  <read_first>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/http/core.inc
    crates/swarm-runtime/src/strategy.rs
    crates/swarm-runtime/src/canary.rs
    crates/swarm-runtime/src/promotion.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/http/core.inc
    crates/swarm-runtime/src/strategy.rs
    crates/swarm-runtime/src/canary.rs
    crates/swarm-runtime/src/promotion.rs
  </files>
  <action>Update the remaining owned operational and harness-side `PheromoneConfig { ... }` literals in the listed runtime files so the expanded config shape is consistent across HTTP/control-adjacent and rollout-related runtime surfaces. Keep this task mechanical and confined to constructor/default-shape updates; do not mix in behavior or routing changes.</action>
  <verify>
    <automated>cargo check -p swarm-runtime --lib</automated>
  </verify>
  <done>The remaining owned operational fixtures compile against the expanded config shape, so Phase 124 behavior work can rely on a consistent runtime baseline across both core and rollout-oriented surfaces.</done>
</task>

</tasks>

<verification>
Run `cargo check -p swarm-runtime --lib` to prove the owned runtime library surface compiles cleanly after the mechanical literal migration.
</verification>

<success_criteria>
The Phase 124 config seam no longer leaves hidden `PheromoneConfig` literal breakage across owned `swarm-runtime` modules.
</success_criteria>

<output>
After completion, create `.planning/phases/124-pounceagent-core-and-de-escalation/124-05-SUMMARY.md`
</output>
