---
phase: 124-pounceagent-core-and-de-escalation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-policy/src/static_gate.rs
  - rulesets/default.yaml
autonomous: true
requirements:
  - POUNCE-03
  - DEESC-02
must_haves:
  truths:
    - "The repository default ruleset loads through the normal `load_config()` path with Phase 124 cooldown and response-playbook data present."
    - "Operators have a repo-owned default cooldown value and response-playbook examples in `rulesets/default.yaml` to inspect and edit."
    - "Policy and runtime code can share one response-action scope helper instead of drifting into separate dedupe semantics."
  artifacts:
    - path: "crates/swarm-core/src/config.rs"
      provides: "Response playbook contracts and de-escalation cooldown config."
    - path: "crates/swarm-policy/src/static_gate.rs"
      provides: "Public response-action scope helper shared by policy and PounceAgent."
    - path: "rulesets/default.yaml"
      provides: "Repo-owned default playbook and cooldown examples."
  key_links:
    - from: "crates/swarm-core/src/config.rs"
      to: "rulesets/default.yaml"
      via: "ResponsePlaybookConfig serialization and defaults"
      pattern: "response_playbook"
    - from: "crates/swarm-policy/src/static_gate.rs"
      to: "crates/swarm-runtime/src/pounce_agent.rs"
      via: "scope_for_response_action"
      pattern: "scope_for_response_action"
---

<objective>
Land the shared config and policy-helper seams that every later Phase 124 slice builds on.

Purpose: Separate repo-owned playbook/cooldown setup from later behavior changes so PounceAgent and de-escalation implementation do not reopen core config churn.
Output: Stable config contracts, one shared scope helper, and default ruleset coverage proven through the normal runtime config loader.
</objective>

<execution_context>
@/Users/connor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/connor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-CONTEXT.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-RESEARCH.md
@.planning/phases/124-pounceagent-core-and-de-escalation/124-VALIDATION.md
@.planning/phases/122-cross-strategy-pheromone-signals-and-rollout-scoping/122-01-SUMMARY.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-policy/src/static_gate.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/rulesets/default.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared Phase 124 config contracts and the policy-owned scope helper</name>
  <read_first>
    crates/swarm-core/src/config.rs
    crates/swarm-policy/src/static_gate.rs
    rulesets/default.yaml
    .planning/phases/124-pounceagent-core-and-de-escalation/124-CONTEXT.md
  </read_first>
  <files>
    crates/swarm-core/src/config.rs
    crates/swarm-policy/src/static_gate.rs
    rulesets/default.yaml
  </files>
  <action>Add `deescalation_cooldown_secs: i64` to `PheromoneConfig` with repo-owned defaults and serde support, and add `ResponsePlaybookConfig` plus any supporting config structs required to map `(ThreatClass, Severity, confidence range)` to ordered `ResponseAction` sequences. Update `rulesets/default.yaml` with a concrete cooldown value and playbook examples. Expose a reusable public helper in `crates/swarm-policy/src/static_gate.rs` for deriving action scope so policy and PounceAgent share one scope model. Prove the updated default ruleset still deserializes through `swarm_runtime::config::load_config()`; do not mix runtime-wide constructor churn into this plan, and do not add YAML policy-rule evaluation or TomAgent seams because those are deferred.</action>
  <verify>
    <automated>cargo check -p swarm-core -p swarm-policy && cargo test -p swarm-runtime config::tests::loads_repository_ruleset -- --exact</automated>
  </verify>
  <done>The config contracts exist in `swarm-core`, the shared scope helper is public in `swarm-policy`, and the default ruleset documents the new cooldown/playbook seams while still loading through the repository runtime config path.</done>
</task>

</tasks>

<verification>
Run `cargo check -p swarm-core -p swarm-policy` and `cargo test -p swarm-runtime config::tests::loads_repository_ruleset -- --exact` to prove the config seam is live and the repository ruleset still deserializes through the normal loader.
</verification>

<success_criteria>
Phase 124 has stable playbook/cooldown config contracts, one shared action-scope helper, and a repository ruleset that still loads cleanly through the runtime config entrypoint.
</success_criteria>

<output>
After completion, create `.planning/phases/124-pounceagent-core-and-de-escalation/124-01-SUMMARY.md`
</output>
