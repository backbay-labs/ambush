---
phase: 125-configurable-policy-rules-and-audit-trail
plan: 04
type: execute
wave: 4
depends_on:
  - 125-02
  - 125-03
files_modified:
  - crates/swarm-response/src/lib.rs
  - crates/swarm-spine/src/lib.rs
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/tests/dispatch_integration.rs
autonomous: true
requirements:
  - POLICY-04
must_haves:
  truths:
    - "Runtime structured policy logs include the decisive rule name and reason."
    - "`AuditTrail.policy` persists the decisive rule name and reason for every policy verdict."
    - "Successful `ResponseReceipt` values expose policy audit metadata in a dedicated audit payload."
  artifacts:
    - path: "crates/swarm-runtime/src/lib.rs"
      provides: "Policy rule attribution in runtime logs, audit trails, and returned receipts."
    - path: "crates/swarm-response/src/lib.rs"
      provides: "Explicit receipt audit payload for policy metadata."
    - path: "crates/swarm-runtime/tests/dispatch_integration.rs"
      provides: "Automated proof that rule attribution reaches audit trails and successful receipts."
  key_links:
    - from: "crates/swarm-policy/src/lib.rs"
      to: "crates/swarm-runtime/src/lib.rs"
      via: "PolicyDecision rule attribution"
      pattern: "rule_name"
    - from: "crates/swarm-runtime/src/lib.rs"
      to: "crates/swarm-response/src/lib.rs"
      via: "receipt policy audit injection"
      pattern: "audit"
---

<objective>
Persist policy attribution all the way through runtime audit output and successful receipts.

Purpose: Make policy decisions explainable and auditable after the fact, not just during live evaluation.
Output: Rule-attributed policy decisions in runtime logs, audit trails, and `ResponseReceipt.audit.policy`, with integration coverage proving the plumbing.
</objective>

<context>
@.planning/phases/125-configurable-policy-rules-and-audit-trail/125-CONTEXT.md
@.planning/phases/125-configurable-policy-rules-and-audit-trail/125-VALIDATION.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-response/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-spine/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/tests/dispatch_integration.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend runtime integration coverage for policy attribution</name>
  <read_first>
    crates/swarm-runtime/tests/dispatch_integration.rs
    crates/swarm-runtime/src/lib.rs
  </read_first>
  <files>
    crates/swarm-runtime/tests/dispatch_integration.rs
  </files>
  <action>Add the exact tests `audit_trail_records_rule_name_and_reason` and `successful_receipts_embed_policy_audit` to `crates/swarm-runtime/tests/dispatch_integration.rs`. The first should prove the decisive rule name and reason persist on `AuditTrail.policy`; the second should prove successful receipts return a dedicated audit payload containing policy verdict metadata.</action>
  <verify>
    <automated>rg -n "audit_trail_records_rule_name_and_reason|successful_receipts_embed_policy_audit" crates/swarm-runtime/tests/dispatch_integration.rs</automated>
  </verify>
  <done>The runtime integration suite contains exact proof points for audit-trail and receipt policy attribution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inject rule attribution into runtime logs, audit trails, and receipts</name>
  <read_first>
    crates/swarm-response/src/lib.rs
    crates/swarm-spine/src/lib.rs
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/tests/dispatch_integration.rs
  </read_first>
  <files>
    crates/swarm-response/src/lib.rs
    crates/swarm-spine/src/lib.rs
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/tests/dispatch_integration.rs
  </files>
  <action>Extend `PolicyDecision`, `PolicyRecord`, and `ResponseReceipt` so decisive rule attribution survives the canonical runtime path. Update `SwarmRuntime` logging and audit construction to emit the decisive rule name and reason, and attach policy audit metadata to successful receipts without overwriting adapter-specific details.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --test dispatch_integration audit_trail_records_rule_name_and_reason -- --exact</automated>
  </verify>
  <done>Policy rule attribution is now observable in logs, audit trails, and successful response receipts.</done>
</task>

</tasks>

<verification>
Run the two exact dispatch integration tests first, then the full dispatch integration suite to confirm attribution works alongside the Phase 124 routed-execution invariants.
</verification>

<output>
After completion, create `.planning/phases/125-configurable-policy-rules-and-audit-trail/125-04-SUMMARY.md`
</output>
