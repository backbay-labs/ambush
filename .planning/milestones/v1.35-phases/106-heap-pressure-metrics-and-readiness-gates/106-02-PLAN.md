---
phase: 106-heap-pressure-metrics-and-readiness-gates
plan: 02
type: execute
wave: 3
depends_on: [106-01]
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/ingest.rs
autonomous: true
requirements: [K8S-05]

must_haves:
  truths:
    - "`/readyz` returns HTTP 503 when heap pressure exceeds `max_heap_pressure`"
    - "The readiness payload explains when heap pressure is the reason for degradation"
    - "`/livez` and `/startupz` remain separate from the heap-pressure readiness decision"
  artifacts:
    - path: "crates/swarm-runtime/src/ingest.rs"
      provides: "heap-gated readiness response"
      contains: "max_heap_pressure"
  key_links:
    - from: "crates/swarm-core/src/config.rs"
      to: "crates/swarm-runtime/src/ingest.rs"
      via: "configured readiness threshold"
      pattern: "max_heap_pressure"
---

<objective>
Use the live heap-pressure signal to fail readiness before the process crosses its memory safety budget.

Purpose: Kubernetes should shed load from a pressured runtime before the kernel does it forcibly.

Output: heap-aware readiness logic, health payload details, and regression tests for degraded `/readyz`.
</objective>
