---
phase: 105-schema-versioning-and-secret-resolution
plan: 01
type: execute
wave: 2
depends_on: [104-01, 104-02]
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/config.rs
  - rulesets/default.yaml
autonomous: true
requirements: [K8S-04]

must_haves:
  truths:
    - "`SwarmConfig` requires an explicit `schema_version`"
    - "The loader rejects future or unrecognized schema versions fail closed"
    - "Supported older schema versions are migrated forward with structured migration-step logging"
  artifacts:
    - path: "crates/swarm-runtime/src/config.rs"
      provides: "schema-aware parse and migration logic"
      contains: "CURRENT_SCHEMA_VERSION"
    - path: "rulesets/default.yaml"
      provides: "repo-owned config updated to the current schema"
      contains: "schema_version"
  key_links:
    - from: "crates/swarm-core/src/config.rs"
      to: "crates/swarm-runtime/src/config.rs"
      via: "top-level schema version validation plus migration defaults"
      pattern: "schema_version"
---

<objective>
Introduce explicit schema versioning for runtime config and make the loader responsible for backward-compatible migration and fail-closed rejection.

Purpose: Production deployment needs a stable, inspectable config contract instead of best-effort deserialize behavior.

Output: `schema_version`, supported migration logic, updated repo config, and focused config-loader tests.
</objective>
