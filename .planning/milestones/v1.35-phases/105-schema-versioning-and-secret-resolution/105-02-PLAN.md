---
phase: 105-schema-versioning-and-secret-resolution
plan: 02
type: execute
wave: 2
depends_on: [105-01]
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
  - crates/swarm-response/src/config.rs
  - crates/swarm-response/src/dispatch.rs
  - crates/swarm-response/src/http_edr.rs
  - crates/swarm-response/src/webhook.rs
autonomous: true
requirements: [K8S-03]

must_haves:
  truths:
    - "A shared `SwarmSecretProvider` resolves `@secret:` references for HTTP EDR and webhook adapters"
    - "Mounted-file secrets and environment variables are both supported"
    - "Secret-directory watch events trigger runtime reload without process restart"
  artifacts:
    - path: "crates/swarm-runtime/src/config.rs"
      provides: "shared secret provider plus response-adapter secret resolution"
      contains: "SwarmSecretProvider"
    - path: "crates/swarm-response/src/webhook.rs"
      provides: "webhook auth-token support"
      contains: "AUTHORIZATION"
  key_links:
    - from: "crates/swarm-runtime/src/config.rs"
      to: "crates/swarm-response/src/dispatch.rs"
      via: "resolved adapter config reaches adapter construction"
      pattern: "resolve_response_adapter_secrets"
    - from: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      to: "crates/swarm-runtime/src/config.rs"
      via: "secret-dir watch drives reload_from_disk"
      pattern: "secret_dir"
---

<objective>
Resolve adapter secrets through a repo-owned provider so Kubernetes-mounted secrets and environment-backed credentials can be rotated safely.

Purpose: Inline static tokens are not acceptable for production response adapters.

Output: `@secret:` support for HTTP EDR and webhook configs, webhook auth headers, secret reload watch integration, and adapter/config tests.
</objective>
