---
phase: 104-drain-control-and-startup-probe-surface
plan: 02
type: execute
wave: 1
depends_on: [104-01]
files_modified:
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
autonomous: true
requirements: [K8S-02]

must_haves:
  truths:
    - "A dedicated `/startupz` endpoint exists alongside `/readyz` and `/livez`"
    - "`/startupz` stays focused on startup-only invariants instead of steady-state health drift"
    - "Lifecycle tests prove the startup probe remains distinct from readiness"
  artifacts:
    - path: "crates/swarm-runtime/src/ingest.rs"
      provides: "startup probe handler and startup-only health payload"
      contains: "startupz_handler"
  key_links:
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      via: "serve router now exposes startup semantics"
      pattern: "/startupz"
---

<objective>
Split startup probing from readiness so Kubernetes can gate initial boot on schema compatibility, substrate readiness, and telemetry-source presence without conflating that with later runtime degradation.

Purpose: Startup probes and readiness probes answer different operational questions and should not share one contract.

Output: `/startupz`, startup-focused JSON payloads, and probe-specific verification coverage.
</objective>
