# Plan 45-01

1. Add selection listing and review-state filtering on top of persisted selection records.
2. Record accept, defer, and reject decisions with durable operator reasons and decision history.
3. Expose result, list, and decision commands through `swarmctl`.
4. Verify review-state transitions and filtered listing with runtime tests and the real CLI flow.
