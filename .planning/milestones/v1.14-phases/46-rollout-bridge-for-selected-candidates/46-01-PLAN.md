# Plan 46-01

1. Add durable ranked-candidate bridge artifacts above accepted selections.
2. Revalidate preserved manifest and lineage digests before minting a queue-ready bridge result.
3. Expose bridge creation and reload through `swarmctl` and document the operator workflow.
4. Verify both the blocked fail-closed path and the successful bridge into handoff and canary.
