---
phase: 145-agent-key-persistence-and-identity-derivation
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 144
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-core/src/types.rs
  - crates/swarm-core/src/pheromone.rs
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-runtime/src/agent_identity.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
  - crates/swarm-runtime/src/whisker_agent.rs
  - crates/swarm-runtime/src/stalker_agent.rs
  - crates/swarm-runtime/src/weaver_agent.rs
  - crates/swarm-runtime/src/tom_agent.rs
  - crates/swarm-runtime/src/pounce_agent.rs
  - crates/swarm-runtime/src/kitten_agent.rs
  - crates/swarm-runtime/src/sphinx_agent.rs
  - crates/swarm-runtime/src/detection/pipeline.rs
  - rulesets/default.yaml
autonomous: true
requirements:
  - IDENTITY-01
must_haves:
  truths:
    - "Serve-mode agent constructors currently generate ephemeral Ed25519 keys in memory, so restart-safe identity requires a new shared persistence seam rather than one-off fixes per agent."
    - "Pheromone deposits and response audits already carry agent identifiers, so the least risky way to satisfy the requirement is to thread stable identities through those existing contracts."
    - "Later identity-registry and key-rotation phases need a deterministic file-backed key layout from this phase instead of ad hoc role-local files."
  artifacts:
    - path: "crates/swarm-runtime/src/agent_identity.rs"
      provides: "File-backed Ed25519 key load-or-create store and stable identity derivation."
    - path: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      provides: "Serve-mode registration wired to persisted agent identities."
    - path: "crates/swarm-core/src/pheromone.rs"
      provides: "Signed deposit contract carries stable identity and role metadata."
  key_links:
    - from: "crates/swarm-runtime/src/agent_identity.rs"
      to: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      via: "persisted serve-mode agent bootstrap"
      pattern: "load or create one keypair per agent slot"
    - from: "crates/swarm-runtime/src/detection/pipeline.rs"
      to: "crates/swarm-pheromone/src/substrate.rs"
      via: "canonical deposit signing payload"
      pattern: "identity metadata is signed and verified"
    - from: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      to: "crates/swarm-runtime/src/lib.rs"
      via: "identity-based AgentId propagation"
      pattern: "receipts and audits inherit stable requested_by identities"
---

<objective>
Complete Phase 145 by giving runtime agents persistent Ed25519 identities that survive restart and flow into the signed pheromone plus audited response surfaces.

Purpose: establish the durable identity seam required for later registry, rotation, and distributed-governance work.
Output: repo-owned key-dir config, file-backed agent key persistence, stable `swarm:ed25519:<hex>` identities in serve mode, and signed deposits carrying explicit identity metadata.
</objective>

<context>
@.planning/phases/145-agent-key-persistence-and-identity-derivation/145-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/pheromone.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/bin/swarm_detect.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/detection/pipeline.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repo-owned identity config and persisted key store</name>
  <read_first>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/config.rs
  </read_first>
  <files>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/config.rs
    crates/swarm-runtime/src/agent_identity.rs
    rulesets/default.yaml
  </files>
  <action>Add a repo-owned `identity.agent_key_dir` config surface and implement a file-backed Ed25519 load-or-create store that persists one raw seed per agent slot relative to the active config file.</action>
  <verify>
    <automated>cargo test -p swarm-core config::tests:: -- --nocapture</automated>
  </verify>
  <done>Runtime config validates the key-dir field and the new store deterministically reloads the same public identity after restart.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire persisted identities into serve-mode agents and signed deposits</name>
  <read_first>
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-runtime/src/detection/pipeline.rs
    crates/swarm-runtime/src/stalker_agent.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-runtime/src/whisker_agent.rs
    crates/swarm-runtime/src/stalker_agent.rs
    crates/swarm-runtime/src/weaver_agent.rs
    crates/swarm-runtime/src/tom_agent.rs
    crates/swarm-runtime/src/pounce_agent.rs
    crates/swarm-runtime/src/kitten_agent.rs
    crates/swarm-runtime/src/sphinx_agent.rs
    crates/swarm-runtime/src/detection/pipeline.rs
    crates/swarm-core/src/pheromone.rs
    crates/swarm-core/src/types.rs
  </files>
  <action>Bootstrap persisted identities in `swarm_detect --serve`, use the derived `swarm:ed25519:<hex>` string as the serve-mode agent ID, and extend signed deposits with explicit identity plus role metadata so downstream agents no longer rely on name prefixes.</action>
  <verify>
    <automated>cargo test -p swarm-runtime whisker_agent::tests:: -- --nocapture</automated>
    <automated>cargo test -p swarm-runtime stalker_agent::tests:: -- --nocapture</automated>
  </verify>
  <done>Restart-safe agent identities flow through serve-mode registration, deposits, and request/audit surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove restart stability and audit propagation</name>
  <read_first>
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-runtime/tests/multi_agent_pipeline_integration.rs
    crates/swarm-runtime/tests/dispatch_integration.rs
  </files>
  <action>Add focused verification that the persisted key store reloads the same identity across restarts and that the stable identity propagates into the existing request-response audit chain in serve mode.</action>
  <verify>
    <automated>cargo check -p swarm-runtime --tests -j 1 --message-format short</automated>
  </verify>
  <done>Phase 145 has proof for persistence across restart and identity propagation through the audited runtime path.</done>
</task>

</tasks>

<verification>
Land the config and store seam first, then wire serve-mode agent bootstrap, then add focused restart and audit-propagation tests. Keep the phase bounded to identity persistence and derivation; registry admission and key rotation stay out of scope until Phase 146.
</verification>
