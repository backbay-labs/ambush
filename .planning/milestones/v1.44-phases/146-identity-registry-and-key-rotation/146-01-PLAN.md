---
phase: 146-identity-registry-and-key-rotation
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 145
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/agent_identity.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
  - crates/swarm-runtime/src/dispatcher.rs
  - crates/swarm-runtime/src/tom_agent.rs
  - crates/swarm-runtime/src/pounce_agent.rs
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/src/bin/swarmctl.rs
  - rulesets/default.yaml
autonomous: true
requirements:
  - IDENTITY-02
  - IDENTITY-03
must_haves:
  truths:
    - "Phase 145 solved persistence and stable identity derivation, but there is still no durable notion of which identities are legitimate runtime participants."
    - "This phase should fail closed for governance participation without overreaching into later consensus-backed multi-instance registry synchronization."
    - "Historical signed artifacts remain valuable after rotation only if old keys and continuity proofs are retained durably."
  artifacts:
    - path: "crates/swarm-runtime/src/agent_identity.rs"
      provides: "file-backed identity registry, continuity proofs, and retired-key retention"
    - path: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      provides: "startup admission of persisted identities before agent registration"
    - path: "crates/swarm-runtime/src/bin/swarmctl.rs"
      provides: "operator-facing key-rotation entry point"
  key_links:
    - from: "crates/swarm-runtime/src/agent_identity.rs"
      to: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      via: "startup registry admission"
      pattern: "persisted serve identities are admitted or rejected before governance participation"
    - from: "crates/swarm-runtime/src/bin/swarmctl.rs"
      to: "crates/swarm-runtime/src/agent_identity.rs"
      via: "continuity-proof rotation workflow"
      pattern: "new keypair plus signed handoff plus retired-key retention"
    - from: "crates/swarm-runtime/src/dispatcher.rs"
      to: "crates/swarm-runtime/src/tom_agent.rs"
      via: "governance fail-closed boundary"
      pattern: "unknown identities cannot participate in governance actions"
---

<objective>
Complete Phase 146 by adding a durable identity registry, admitting persisted serve-mode identities on startup, and implementing safe key rotation with continuity proofs and retired-key retention.

Purpose: turn the Phase 145 persistent-key seam into a governed identity lifecycle usable by later consensus and substrate-admission work.
Output: registry-backed admission state, continuity-proof rotation flow, and fail-closed governance participation for unknown identities.
</objective>

<context>
@.planning/phases/146-identity-registry-and-key-rotation/146-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/agent_identity.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/bin/swarm_detect.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/dispatcher.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/bin/swarmctl.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add registry config and durable identity registry store</name>
  <read_first>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/agent_identity.rs
  </read_first>
  <files>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/agent_identity.rs
    rulesets/default.yaml
  </files>
  <action>Add repo-owned registry paths and implement a file-backed `AgentIdentityRegistry` that records active identities, retired keys, and continuity proofs per role / slot.</action>
  <verify>
    <automated>cargo test -p swarm-core config::tests:: -- --nocapture</automated>
    <automated>cargo test -p swarm-runtime agent_identity::tests:: -- --nocapture</automated>
  </verify>
  <done>Registry artifacts persist durably, restart cleanly, and retain enough retired-key state to verify historical artifacts after rotation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce startup admission for governance participants</name>
  <read_first>
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/tom_agent.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/tom_agent.rs
    crates/swarm-runtime/src/pounce_agent.rs
  </files>
  <action>Admit persisted serve-mode identities into the registry on startup and fail closed for governance participation when the acting identity is not registered.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --bin swarm_detect tests:: -- --nocapture</automated>
    <automated>cargo test -p swarm-runtime dispatcher::tests:: -- --nocapture</automated>
  </verify>
  <done>Unknown identities are logged and blocked from governance participation without preventing non-governance runtime startup flows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement continuity-proof rotation flow</name>
  <read_first>
    crates/swarm-runtime/src/agent_identity.rs
    crates/swarm-runtime/src/bin/swarmctl.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/agent_identity.rs
    crates/swarm-runtime/src/bin/swarmctl.rs
  </files>
  <action>Add a repo-owned rotation entry point that generates a new keypair, signs a handoff with the old key, persists the continuity proof, and retains the retired key with an `active_until` timestamp.</action>
  <verify>
    <automated>cargo test -p swarm-runtime agent_identity::tests:: -- --nocapture</automated>
    <automated>cargo check -p swarm-runtime --tests -j 1 --message-format short</automated>
  </verify>
  <done>Rotation leaves one active key, durable retired-key history, and a verifiable continuity proof linking old and new identities.</done>
</task>

</tasks>

<verification>
Land the registry store first, then wire serve-mode admission, then add the operator-facing rotation flow. Keep substrate-wide unregistered-deposit rejection out of scope until the distributed-governance milestone.
</verification>
