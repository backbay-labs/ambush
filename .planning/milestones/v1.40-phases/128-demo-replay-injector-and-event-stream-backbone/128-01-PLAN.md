---
phase: 128-demo-replay-injector-and-event-stream-backbone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/128-demo-replay-injector-and-event-stream-backbone/128-CONTEXT.md
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/Cargo.toml
  - crates/swarm-runtime/src/bin/swarm_detect.rs
  - crates/swarm-runtime/src/dispatcher.rs
  - crates/swarm-runtime/src/escalation.rs
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/src/runtime_events.rs
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
must_haves:
  truths:
    - "Replay injection is disabled unless runtime.demo_mode is explicitly enabled."
    - "Demo replay loads the existing repo-owned replay scenario manifest format and injects each event into the real runtime path with configurable pacing."
    - "The runtime exposes a typed SSE stream with event-type filtering over one shared broadcaster."
    - "Ingest, dispatcher, and escalation producers all publish through the same event backbone so downstream demo consumers see one coherent stream."
  artifacts:
    - path: "crates/swarm-core/src/config.rs"
      provides: "Repo-owned demo-mode runtime gate with validation-safe defaults."
    - path: "crates/swarm-runtime/src/runtime_events.rs"
      provides: "Typed runtime event taxonomy and shared broadcaster primitives."
    - path: "crates/swarm-runtime/src/ingest.rs"
      provides: "Demo replay endpoint, SSE stream endpoint, and endpoint-level verification."
    - path: "crates/swarm-runtime/src/dispatcher.rs"
      provides: "Typed action/routing events published from the dispatcher convergence point."
    - path: "crates/swarm-runtime/src/escalation.rs"
      provides: "Escalation and mode-transition events published from the monitor."
  key_links:
    - from: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      to: "crates/swarm-runtime/src/ingest.rs"
      via: "shared RuntimeEventBroadcaster wired into serve-mode state"
      pattern: "with_runtime_events"
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-runtime/src/replay/core.inc"
      via: "repo-owned replay scenario loading and step injection"
      pattern: "load_scenario_manifest"
    - from: "crates/swarm-runtime/src/dispatcher.rs"
      to: "crates/swarm-runtime/src/runtime_events.rs"
      via: "typed agent-action and routed-request publication"
      pattern: "publish"
    - from: "crates/swarm-runtime/src/escalation.rs"
      to: "crates/swarm-runtime/src/runtime_events.rs"
      via: "escalation threshold and mode-transition publication"
      pattern: "publish"
---

<objective>
Build the runtime backbone for the v1.40 live demo.

Purpose: Let operators drive the real swarm from repo-owned replay scenarios and observe typed live runtime events over one coherent stream.
Output: `demo_mode` runtime gate, shared runtime event bus, `/v1/demo/replay`, `/v1/events/stream`, and tests proving replay injection and filtered stream emission.
</objective>

<context>
@.planning/phases/128-demo-replay-injector-and-event-stream-backbone/128-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/REQUIREMENTS.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/dispatcher.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/escalation.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/replay/core.inc
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the repo-owned demo-mode gate and runtime event backbone</name>
  <read_first>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/escalation.rs
  </read_first>
  <files>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/escalation.rs
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/src/runtime_events.rs
  </files>
  <action>Add `runtime.demo_mode` with a default-false validation-safe config path, define the typed runtime event model and broadcaster, and wire that broadcaster into serve mode, dispatcher routing, and concentration-monitor transitions.</action>
  <verify>
    <automated>cargo test -p swarm-runtime ingest::tests::demo_replay_endpoint_rejects_when_demo_mode_disabled -- --exact</automated>
  </verify>
  <done>The runtime has one shared event backbone and the demo-only entrypoint is safely gated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose replay injection and filtered SSE over the runtime HTTP surface</name>
  <read_first>
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/replay/core.inc
  </read_first>
  <files>
    crates/swarm-runtime/Cargo.toml
    crates/swarm-runtime/src/ingest.rs
  </files>
  <action>Implement `POST /v1/demo/replay` using the existing replay-scenario loader and configurable pacing into the real runtime path, then implement `GET /v1/events/stream` as typed SSE with event-type filtering backed by the shared broadcaster. Add focused endpoint tests for demo-mode gating, replay injection, and filtered stream output.</action>
  <verify>
    <automated>cargo test -p swarm-runtime ingest::tests::events_stream_filters_typed_runtime_events -- --exact</automated>
  </verify>
  <done>The operator surface can drive the live swarm from a scenario file and stream typed filtered events.</done>
</task>

</tasks>

<verification>
Run the new exact ingest tests first, then `cargo test -p swarm-runtime ingest::tests:: -- --nocapture` and `cargo check -p swarm-runtime` to confirm the demo backbone compiles cleanly against the existing dirty workspace.
</verification>

<output>
After completion, create `.planning/phases/128-demo-replay-injector-and-event-stream-backbone/128-01-SUMMARY.md`
</output>
