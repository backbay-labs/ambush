---
phase: 130-approval-in-the-loop-demo-and-signed-proof-export
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 128
files_modified:
  - .planning/phases/130-approval-in-the-loop-demo-and-signed-proof-export/130-CONTEXT.md
  - crates/swarm-runtime/Cargo.toml
  - crates/swarm-runtime/src/bin/swarm_detect.rs
  - crates/swarm-runtime/src/cli/core.inc
  - crates/swarm-runtime/src/http/core.inc
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/lib.rs
autonomous: true
requirements:
  - DEMO-04
  - DEMO-05
must_haves:
  truths:
    - "Demo replay pauses on `RequireHuman` with a durable approval target instead of executing immediately."
    - "The operator approval vote endpoint can complete quorum, export a signed receipt pack, and resume the paused runtime action through the real runtime path."
    - "The exported demo proof package contains the signed approval chain, Merkle proofs, final correlated incident, and full timeline for one run."
    - "Resumed execution reuses canonical runtime authorization and adapter wiring rather than a demo-only side path."
  artifacts:
    - path: "crates/swarm-runtime/src/ingest.rs"
      provides: "Paused demo approval tracking, runtime resume endpoint, proof export endpoint, and focused runtime tests."
    - path: "crates/swarm-runtime/src/lib.rs"
      provides: "Audited human-approved execution path through the canonical runtime."
    - path: "crates/swarm-runtime/src/http/core.inc"
      provides: "Operator vote endpoint bridge that resumes paused demo approvals after signed quorum."
    - path: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      provides: "Serve-mode approval harness wiring for the demo approval path."
  key_links:
    - from: "crates/swarm-runtime/src/http/core.inc"
      to: "crates/swarm-runtime/src/ingest.rs"
      via: "runtime HTTP callback after approval quorum produces a signed receipt pack"
      pattern: "resume_demo_approval"
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-runtime/src/lib.rs"
      via: "paused human-gated action resumes through human-approved audited execution"
      pattern: "audit_authorize_and_execute_human_approved_instrumented"
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-runtime/src/approval.rs"
      via: "receipt-pack verification and approval-chain proof composition"
      pattern: "verify_receipt_pack"
---

<objective>
Build the approval-in-the-loop demo path and signed proof export for v1.40.

Purpose: Turn a `RequireHuman` runtime decision into a resumable, operator-approved, auditable demo flow.
Output: paused demo approval state, operator-driven resume path, `GET /v1/demo/proof`, and runtime/operator tests proving signed end-to-end approval execution.
</objective>

<context>
@.planning/phases/130-approval-in-the-loop-demo-and-signed-proof-export/130-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/REQUIREMENTS.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/approval.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/http/core.inc
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist paused demo approvals and export proof packages from the runtime surface</name>
  <read_first>
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/approval.rs
    crates/swarm-runtime/src/lib.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/Cargo.toml
  </files>
  <action>Add demo-run approval tracking, a runtime resume endpoint, and `GET /v1/demo/proof`; verify receipt packs before resuming and build a deterministic JSON proof package with signed approval artifacts, Merkle proofs, incident context, and timeline state.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --lib human_gated_demo_replay_can_resume_and_export_proof -- --nocapture</automated>
    <automated>cargo test -p swarm-runtime --lib human_approved_live_runtime_executes_human_gated_action -- --nocapture</automated>
  </verify>
  <done>The runtime can pause a demo action on `RequireHuman`, resume it after signed approval, and export one proof package for the run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bridge operator approval votes into runtime resume</name>
  <read_first>
    crates/swarm-runtime/src/http/core.inc
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-runtime/src/cli/core.inc
  </read_first>
  <files>
    crates/swarm-runtime/src/http/core.inc
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-runtime/src/cli/core.inc
  </files>
  <action>Wire serve mode with the approval harness, then update the operator approval vote endpoint so quorum automatically creates the verdict, exports the signed receipt pack, and resumes the paused demo action over the runtime HTTP surface.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --lib approval_vote_endpoint_resumes_demo_runtime_and_proof_export -- --nocapture</automated>
  </verify>
  <done>The authenticated operator vote path closes the human gate and resumes the real runtime action with signed approval evidence.</done>
</task>

</tasks>

<verification>
Run the focused approval and proof tests first, then `cargo test -p swarm-runtime` on the formatted tree to confirm the demo approval path does not regress the rest of the runtime package.
</verification>

<output>
After completion, create `.planning/phases/130-approval-in-the-loop-demo-and-signed-proof-export/130-01-SUMMARY.md`
</output>
