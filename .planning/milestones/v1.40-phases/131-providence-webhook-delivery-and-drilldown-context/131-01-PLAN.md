---
phase: 131-providence-webhook-delivery-and-drilldown-context
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 128
  - Phase 129
  - Phase 130
files_modified:
  - .planning/phases/131-providence-webhook-delivery-and-drilldown-context/131-CONTEXT.md
  - crates/swarm-core/src/config.rs
  - crates/swarm-response/src/notification.rs
  - crates/swarm-runtime/src/http/core.inc
  - crates/swarm-runtime/src/ingest.rs
autonomous: true
requirements:
  - DEMO-06
must_haves:
  truths:
    - "A repo-owned `providence_webhook` notification channel delivers Providence-shaped finding payloads through the existing notification router."
    - "Providence payloads include absolute drilldown links back to Swarm replay, investigation, incident, and review surfaces."
    - "Providence payloads include current swarm mode, active agent count, and bridge health summary from the live runtime."
    - "Existing generic notification channels keep their prior payload shape and dead-letter replay behavior."
  artifacts:
    - path: "crates/swarm-core/src/config.rs"
      provides: "Repo-owned `operator.public_base_url` config and validation."
    - path: "crates/swarm-response/src/notification.rs"
      provides: "Channel-specific notification payload builder support without forking the delivery lane."
    - path: "crates/swarm-runtime/src/ingest.rs"
      provides: "Providence payload shaping with runtime context and focused runtime tests."
    - path: "crates/swarm-runtime/src/http/core.inc"
      provides: "Operator-surface fixture updates for absolute public-link configuration."
  key_links:
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-response/src/notification.rs"
      via: "runtime-installed payload builder for the `providence_webhook` channel"
      pattern: "set_payload_builder"
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-core/src/config.rs"
      via: "absolute operator and runtime URLs used to build Providence drilldown links"
      pattern: "public_base_url"
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-runtime/src/http/core.inc"
      via: "hunt-based links into replay, investigation, incident, and review pages"
      pattern: "/v1/operator/"
---

<objective>
Build Providence webhook delivery and drilldown context for the v1.40 demo.

Purpose: Push live Swarm findings into Providence with enough runtime context and stable links to support the external demo story.
Output: repo-owned public operator URL config, Providence-shaped outbound notification payloads, live runtime context enrichment, and end-to-end tests proving the webhook contract.
</objective>

<context>
@.planning/phases/131-providence-webhook-delivery-and-drilldown-context/131-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/REQUIREMENTS.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-response/src/notification.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/http/core.inc
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repo-owned public operator URL config and notification payload builder support</name>
  <read_first>
    crates/swarm-core/src/config.rs
    crates/swarm-response/src/notification.rs
  </read_first>
  <files>
    crates/swarm-core/src/config.rs
    crates/swarm-response/src/notification.rs
  </files>
  <action>Add `operator.public_base_url` with validation, then extend `NotificationRouter` so the runtime can inject a channel-specific payload builder while preserving existing delivery, dead-letter, and replay behavior.</action>
  <verify>
    <automated>cargo test -p swarm-core config::tests::operator_surface_requires_http_public_base_url_when_enabled -- --exact</automated>
    <automated>cargo test -p swarm-response notification::tests::router_uses_channel_specific_payload_builder_when_present -- --exact</automated>
  </verify>
  <done>The runtime can opt a channel into Providence-shaped payloads without changing the generic notification path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enrich the Providence webhook with runtime context and absolute drilldown links</name>
  <read_first>
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/http/core.inc
  </read_first>
  <files>
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/http/core.inc
  </files>
  <action>Install the payload builder from `IngestState`, map severity and threat class into Providence incident fields, and emit absolute replay, investigation, incident, review, and dashboard links plus live runtime mode, active-agent, and bridge-health context.</action>
  <verify>
    <automated>cargo test -p swarm-runtime ingest::tests::providence_webhook_payload_includes_runtime_context_and_links -- --exact</automated>
    <automated>cargo test -p swarm-runtime --lib demo_dashboard_snapshot_endpoint_reports_live_runtime_state -- --exact</automated>
  </verify>
  <done>Providence receives the demo-ready incident payload with drilldown links and runtime status context.</done>
</task>

</tasks>

<verification>
Run the focused config, notification, and runtime tests first, then `cargo test -p swarm-runtime` to ensure the Providence webhook path does not regress the broader runtime package.
</verification>

<output>
After completion, create `.planning/phases/131-providence-webhook-delivery-and-drilldown-context/131-01-SUMMARY.md`
</output>
