---
phase: 198-measured-evolution-benchmark
plan: 01
type: execute
wave: 1
depends_on:
  - 197 complete
files_modified:
  - crates/swarm-evolution/src/mutation.rs
  - crates/swarm-runtime/src/kitten_agent.rs
  - crates/swarm-runtime/src/evolution_status.rs
  - docs/benchmarks/
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - AUTOEVO-03
must_haves:
  truths:
    - "The repo must be able to run a bounded multi-generation autonomous evolution benchmark end to end."
    - "Benchmark output must persist generation-over-generation measured fitness deltas with reproducible lineage."
    - "Phase 198 stops at benchmark execution and raw reporting; improvement publication remains Phase 199."
  artifacts:
    - path: "crates/swarm-evolution/src/mutation.rs"
      provides: "Durable generation benchmark records derived from Phase 197 measured fitness."
    - path: "crates/swarm-runtime/src/kitten_agent.rs"
      provides: "Runtime-owned orchestration or harness reuse for bounded multi-generation autonomous benchmark execution."
    - path: "docs/benchmarks/"
      provides: "Repo-owned benchmark artifact with measured generation-over-generation deltas."
---

<objective>
Complete Phase 198 by running a bounded multi-generation autonomous evolution
benchmark that reuses the existing mutation, validation, and measured-fitness
seams, then persist generation-over-generation deltas in a reproducible
repo-owned artifact.

Purpose: move `v1.57` from one-generation measured evaluation into benchmarked
autonomous improvement over several generations.
Output: durable benchmark records and checked-in benchmark results for the
bounded multi-generation loop.
</objective>

<context>
@.planning/phases/198-measured-evolution-benchmark/198-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/mutation.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/kitten_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/evolution_status.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Persist bounded multi-generation benchmark state</name>
  <files>
    crates/swarm-evolution/src/mutation.rs
  </files>
  <action>Add durable generation benchmark artifacts that aggregate Phase 197 measured autonomous fitness across N bounded generations with lineage and delta tracking.</action>
  <done>One benchmark run now persists reproducible generation-over-generation measured fitness records.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Reuse the runtime evolution loop for benchmark execution</name>
  <files>
    crates/swarm-runtime/src/kitten_agent.rs
    crates/swarm-runtime/src/evolution_status.rs
  </files>
  <action>Wire a bounded benchmark harness or runtime-owned orchestration path that can execute several autonomous generations end to end and surface the resulting benchmark state.</action>
  <done>The repo can run a measured multi-generation autonomous benchmark without widening the existing proposal or rollout contracts.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Check in benchmark output</name>
  <files>
    docs/benchmarks/
  </files>
  <action>Record the benchmark host assumptions, generation count, and measured deltas in one repo-owned artifact that future regressions can compare against.</action>
  <done>A checked-in benchmark artifact captures raw generation-over-generation measured fitness deltas with enough context to rerun later.</done>
</task>

</tasks>

<verification>
Verify that the repo can execute a bounded N-generation autonomous evolution
benchmark end to end, that each generation persists measured-fitness deltas
derived from the Phase 197 autonomous fitness artifacts, and that the checked-in
benchmark output is reproducible enough to compare future runs without yet
publishing improvement claims.
</verification>
