---
phase: 196-algorithmic-parameter-variant-generation
plan: 01
type: execute
wave: 1
depends_on:
  - v1.56 complete
files_modified:
  - crates/swarm-evolution/src/mutation.rs
  - crates/swarm-evolution/src/strategy.rs
  - crates/swarm-runtime/src/dispatcher.rs
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - AUTOEVO-01
must_haves:
  truths:
    - "Kitten must generate variants algorithmically from existing genomes; operators should not have to hand-author experiment specs."
    - "Generated variants must stay bounded and attributable so later safety and benchmark phases can replay exactly what was proposed."
    - "Phase 196 stops at candidate generation; measured fitness still belongs to later phases."
  artifacts:
    - path: "crates/swarm-evolution/src/mutation.rs"
      provides: "Bounded perturbation and crossover candidate generation."
    - path: "crates/swarm-evolution/src/strategy.rs"
      provides: "Genome-level variant representation compatible with existing proposal routing."
---

<objective>
Complete Phase 196 by adding repo-owned algorithmic parameter variant generation
to Kitten so it can create bounded mutation and crossover candidates from
top-performing genomes without operator-authored experiment specs.

Purpose: start `v1.57` by replacing manual experiment shaping with automatic,
replayable candidate generation.
Output: bounded autonomous variant generation that feeds the existing proposal
and safety pipeline.
</objective>

<context>
@.planning/phases/196-algorithmic-parameter-variant-generation/196-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/mutation.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/strategy.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/dispatcher.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add bounded mutation and crossover generation</name>
  <files>
    crates/swarm-evolution/src/mutation.rs
    crates/swarm-evolution/src/strategy.rs
  </files>
  <action>Generate bounded random perturbation and crossover variants from top-performing genomes, preserving enough lineage to replay or inspect later.</action>
  <done>Kitten can create autonomous parameter variants without operator-authored experiment specs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Keep proposal routing compatible</name>
  <files>
    crates/swarm-runtime/src/dispatcher.rs
  </files>
  <action>Ensure the new autonomous variants still flow through the existing typed proposal-routing, safety, and review boundaries without widening the runtime contract.</action>
  <done>Algorithmically generated variants enter the same proposal lane as existing Kitten candidates.</done>
</task>

</tasks>

<verification>
Verify that Kitten can generate bounded autonomous parameter variants from prior
genomes, that those candidates retain replayable lineage, and that the runtime
continues to route them through the existing proposal boundary.
</verification>
