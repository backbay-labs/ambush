---
phase: 95-stalkeragent-and-weaveragent-pipeline
plan: 01
type: execute
wave: 1
depends_on: [94-01, 94-02]
files_modified:
  - crates/swarm-runtime/src/stalker_agent.rs
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
  - crates/swarm-runtime/src/lib.rs
autonomous: true
requirements: [MULTI-04]

must_haves:
  truths:
    - "`StalkerAgent` consumes WhiskerAgent pheromones and submits the matching replay bundle into the existing investigation pipeline"
    - "Completed investigations cause `StalkerAgent` to deposit investigation-result pheromones back into the substrate"
    - "Serve mode registers `StalkerAgent` when investigation is enabled"
  artifacts:
    - path: "crates/swarm-runtime/src/stalker_agent.rs"
      provides: "StalkerAgent implementation over replay-store and InvestigationCoordinator"
      contains: "pub struct StalkerAgent"
    - path: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      provides: "live registration of StalkerAgent beside WhiskerAgent"
      contains: "StalkerAgent::new"
  key_links:
    - from: "crates/swarm-runtime/src/stalker_agent.rs"
      to: "ConfiguredReplayBundleStore"
      via: "load replay bundle by hunt id derived from Whisker pheromone indicator.event_id"
      pattern: "load_by_hunt_id"
    - from: "crates/swarm-runtime/src/stalker_agent.rs"
      to: "InvestigationCoordinator"
      via: "submit replay bundles and publish completed investigation results"
      pattern: "submit"
---

<objective>
Implement `StalkerAgent` so the live swarm can turn Whisker detections into persisted investigation work and second-stage investigation pheromones.

Purpose: Detection pheromones are only the first half of the multi-agent pipeline. This plan binds the existing replay/investigation stack into the dispatcher model so agents can claim hunts, queue investigations, and publish investigation-result pheromones without inventing a second async review system.

Output: `stalker_agent.rs`, live serve-mode registration when investigation is enabled, and focused tests proving detection pheromones become submitted/completed investigation outputs.
</objective>
