---
phase: 94-agent-registry-and-role-shift-runtime
plan: 02
type: execute
wave: 1
depends_on: [94-01]
files_modified:
  - crates/swarm-core/src/agent.rs
  - crates/swarm-runtime/src/dispatcher.rs
  - crates/swarm-runtime/src/detection/metrics.rs
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
autonomous: true
requirements: [MULTI-03, MULTI-06]

must_haves:
  truths:
    - "Each dispatcher tick builds a SwarmEnvironment snapshot with recent pheromones, current mode, and recent peer findings"
    - "Peer findings are visible to agents as a read-only view refreshed once per tick"
    - "Agent spawn, tick completion, health transitions, and role shifts emit structured logs with agent_id fields"
    - "Prometheus exposes agent_ticks_total, agent_role_shifts_total, and agent_health_transitions_total partitioned by role on the existing /metrics surface"
  artifacts:
    - path: "crates/swarm-core/src/agent.rs"
      provides: "Expanded SwarmEnvironment and peer-finding snapshot type"
      contains: "peer_findings"
    - path: "crates/swarm-runtime/src/detection/metrics.rs"
      provides: "Agent lifecycle counters registered in the shared Prometheus registry"
      contains: "agent_ticks_total"
    - path: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      provides: "Dispatcher wired to the shared metrics registry used by /metrics"
      contains: "with_metrics"
  key_links:
    - from: "crates/swarm-runtime/src/dispatcher.rs"
      to: "crates/swarm-core/src/agent.rs"
      via: "SwarmEnvironment.peer_findings built from recent SwarmAction outputs"
      pattern: "peer_findings"
    - from: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      to: "crates/swarm-runtime/src/detection/metrics.rs"
      via: "dispatcher shares the runtime Prometheus registry"
      pattern: "current_prometheus_metrics"
---

<objective>
Make the new registry operational by enriching the per-tick environment and instrumenting agent lifecycle behavior on the shared runtime metrics surface.

Purpose: A registry alone is not enough for multi-agent coordination. Agents need visibility into peer work and operators need durable telemetry on the new lifecycle events. This plan adds the read-only peer-finding snapshot plus the structured logging and Prometheus counters that make the swarm runtime observable.

Output: Expanded `SwarmEnvironment`, dispatcher-side peer-finding tracking, shared-lifecycle metrics on `/metrics`, and tests proving the environment and counters are populated correctly.
</objective>
