# Phase 65 Plan

1. Extend review-session refs so the workbench can resolve `promotion_review`, `canary_run`, and `production_promotion` stable IDs through the existing evidence and promotion-packet stores.
2. Compute lane summaries, subject refs, freshness markers, and unresolved gaps from the resolved governance-prep, canary, and production artifacts.
3. Wire the same lane-aware session model through both `swarmctl` and the authenticated local review surface.
