---
phase: 149-providence-contract-and-service-auth
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 148
files_modified:
  - crates/swarm-crypto/src/hashing.rs
  - crates/swarm-crypto/src/lib.rs
  - crates/swarm-core/src/config.rs
  - crates/swarm-core/src/lib.rs
  - crates/swarm-core/src/types.rs
  - crates/swarm-response/Cargo.toml
  - crates/swarm-response/src/notification.rs
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-runtime/src/http/core.inc
  - crates/swarm-runtime/src/ingest.rs
  - docs/CONFIGURATION.md
  - rulesets/default.yaml
autonomous: true
requirements:
  - PROVAUTH-01
  - PROVAUTH-02
must_haves:
  truths:
    - "The v1.40 Providence payload already exists, so Phase 149 should harden and type that seam instead of replacing it with a new transport before lifecycle sync lands."
    - "Bearer auth is already first-class on notification channels, so the missing auth primitive is request signing, not another outbound client stack."
    - "Future Providence lifecycle and feedback phases need one shared schema contract and one secret-resolution path rather than per-phase bespoke structs."
  artifacts:
    - path: "crates/swarm-core/src/types.rs"
      provides: "Shared Providence webhook contract and CreateIncidentBody mapping."
    - path: "crates/swarm-response/src/notification.rs"
      provides: "Canonical JSON webhook delivery with optional HMAC-SHA256 request signing."
    - path: "crates/swarm-runtime/src/ingest.rs"
      provides: "Providence payload mapping plus integration proof for bearer and signature headers."
  key_links:
    - from: "crates/swarm-core/src/config.rs"
      to: "crates/swarm-runtime/src/config.rs"
      via: "notification-channel secret resolution"
      pattern: "auth token and HMAC secret both resolve from @secret references"
    - from: "crates/swarm-response/src/notification.rs"
      to: "crates/swarm-runtime/src/ingest.rs"
      via: "providence_webhook delivery"
      pattern: "typed contract is signed and delivered over the existing channel"
---

<objective>
Complete Phase 149 by shipping a typed Providence webhook contract and authenticated service-to-service delivery.

Purpose: give Swarm and Providence one shared payload shape and one signed transport seam before lifecycle state sync and feedback ingestion build on top of it.
Output: `SwarmProvidenceWebhookContract`, canonical JSON HMAC signing for notification channels, Providence bearer-token config via `@secret:`, and focused proof that the live Providence webhook path carries the new contract and auth headers.
</objective>

<context>
@.planning/phases/149-providence-contract-and-service-auth/149-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/types.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-response/src/notification.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the shared Providence contract and signing primitive</name>
  <files>
    crates/swarm-crypto/src/hashing.rs
    crates/swarm-crypto/src/lib.rs
    crates/swarm-core/src/types.rs
    crates/swarm-core/src/lib.rs
  </files>
  <action>Add a typed `SwarmProvidenceWebhookContract` plus `CreateIncidentBody` mapping, and expose an HMAC-SHA256 helper suitable for service-to-service webhook signing.</action>
  <verify>
    <automated>cargo test -p swarm-crypto hashing::tests::test_hmac_sha256_matches_known_vector -- --exact</automated>
  </verify>
  <done>The Providence webhook contract lives in shared types and the HMAC helper has a known-vector proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire notification-channel request signing and secret resolution</name>
  <files>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/config.rs
    crates/swarm-response/src/notification.rs
    docs/CONFIGURATION.md
    rulesets/default.yaml
  </files>
  <action>Extend notification channels with optional HMAC signing, resolve the shared secret through the existing `@secret:` path, and document the Providence bearer-plus-signature configuration shape.</action>
  <verify>
    <automated>cargo test -p swarm-core config::tests::notification_request_signature_requires_non_empty_secret -- --exact</automated>
    <automated>cargo test -p swarm-runtime config::tests::notification_request_signature_secret_reference_is_resolved -- --exact</automated>
    <automated>cargo test -p swarm-response notification::tests::router_signs_notifications_with_hmac_header -- --exact</automated>
  </verify>
  <done>Notification channels can sign canonical JSON bodies and both bearer and HMAC secrets resolve from repo-owned config.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Map the live Providence payload onto the shared contract</name>
  <files>
    crates/swarm-runtime/src/ingest.rs
  </files>
  <action>Replace the ad hoc Providence payload builder with the shared contract, emit `schema_version`, map the outbound envelope to `create_incident`, and prove the live channel sends both auth headers and the typed payload.</action>
  <verify>
    <automated>cargo test -p swarm-runtime ingest::tests::providence_webhook_payload_includes_runtime_context_and_links -- --exact</automated>
  </verify>
  <done>The shipped `providence_webhook` path now emits the shared contract with canonical auth headers and operator drilldown context.</done>
</task>

</tasks>

<verification>
Keep the phase bounded to contract and auth. Reuse the existing `providence_webhook` transport, prove the header semantics directly, and defer incident lifecycle state, feedback ingestion, and widget embedding to later phases.
</verification>
