---
phase: 152-embeddable-dashboard-widget-and-context-tokens
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 149
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/providence.rs
  - crates/swarm-runtime/src/ingest.rs
  - rulesets/default.yaml
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - PROVDASH-01
  - PROVDASH-02
  - PROVDASH-03
must_haves:
  truths:
    - "Phase 128 and Phase 129 already provide the runtime event and dashboard data paths; Phase 152 should scope and package those surfaces rather than clone them."
    - "Providence drilldown links need one shared scope contract across widget, SSE, and platform reads; mismatched scope logic would be a security defect."
    - "Context-token auth must stay read-only and narrowly bounded to scoped GET routes."
  artifacts:
    - path: "crates/swarm-runtime/src/providence.rs"
      provides: "Scoped Providence link generation and context-token minting."
    - path: "crates/swarm-runtime/src/ingest.rs"
      provides: "Widget HTML, scoped dashboard/SSE filtering, and read-only token validation."
    - path: "crates/swarm-core/src/config.rs"
      provides: "Repo-owned embed-origin and token-TTL policy."
  key_links:
    - from: "crates/swarm-runtime/src/providence.rs"
      to: "crates/swarm-runtime/src/ingest.rs"
      via: "ProvidenceContextScope and context_token query params"
      pattern: "Providence links and runtime validation share one scope contract"
    - from: "crates/swarm-core/src/config.rs"
      to: "docs/CONFIGURATION.md"
      via: "operator_surface.allowed_embed_origins and widget_token_ttl_secs"
      pattern: "repo-owned policy is documented and shipped in default config"
---

<objective>
Complete Phase 152 by giving Providence a minimal live widget and bounded read-only deep links.

Purpose: let Providence embed live Swarm context and open scoped drilldowns without sharing full operator credentials.
Output: `/v1/demo/widget`, scoped dashboard plus SSE filtering, short-lived signed context tokens, and token-backed read access for scoped platform findings and incidents.
</objective>

<context>
@.planning/phases/152-embeddable-dashboard-widget-and-context-tokens/152-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/providence.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repo-owned widget and context-token policy</name>
  <files>
    crates/swarm-core/src/config.rs
    rulesets/default.yaml
    docs/CONFIGURATION.md
  </files>
  <action>Add `operator_surface.allowed_embed_origins` and `operator_surface.widget_token_ttl_secs`, validate them fail closed, and document the shipped defaults.</action>
  <verify>
    <automated>cargo test -p swarm-core config::tests::operator_surface_requires_positive_widget_token_ttl_when_enabled -- --exact</automated>
    <automated>cargo test -p swarm-core config::tests::operator_surface_rejects_invalid_embed_origin -- --exact</automated>
  </verify>
  <done>Widget embed policy and token lifetime are repo-owned configuration instead of code-only defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serve a minimal scoped Providence widget from the runtime</name>
  <files>
    crates/swarm-runtime/src/ingest.rs
  </files>
  <action>Add `/v1/demo/widget`, reuse the demo dashboard plus SSE surfaces, and scope the visible activity, concentrations, and timeline to the requested hunt or strategy context.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --lib ingest::tests::demo_widget_endpoint_sets_embed_headers_and_renders_scoped_context -- --exact</automated>
    <automated>cargo test -p swarm-runtime --lib ingest::tests::events_stream_filters_scoped_runtime_events_for_widget_context -- --exact</automated>
  </verify>
  <done>The widget renders without operator auth, emits the correct embed headers, and only streams scoped runtime context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mint and validate short-lived read-only Providence context tokens</name>
  <files>
    crates/swarm-runtime/src/providence.rs
    crates/swarm-runtime/src/ingest.rs
  </files>
  <action>Generate short-lived Ed25519-signed tokens for Providence links and accept them as a narrow alternative auth path for scoped `GET /v2/api/findings` and `GET /v2/api/incidents` reads.</action>
  <verify>
    <automated>cargo test -p swarm-runtime --lib ingest::tests::platform_api_read_routes_accept_context_token_for_scoped_queries -- --exact</automated>
    <automated>cargo test -p swarm-runtime --lib ingest::tests::providence_webhook_payload_includes_runtime_context_and_links -- --exact</automated>
  </verify>
  <done>Providence links carry signed scoped context, and the runtime validates those tokens without widening write access.</done>
</task>

</tasks>

<verification>
Land shared scope and token minting first, then widget rendering, then platform read validation. Any scope mismatch must fail closed.
</verification>
