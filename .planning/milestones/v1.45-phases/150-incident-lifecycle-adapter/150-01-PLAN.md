---
phase: 150-incident-lifecycle-adapter
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 149
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-response/src/notification.rs
  - crates/swarm-spine/src/incident.rs
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - PROVBI-01
  - PROVBI-02
  - PROVBI-03
must_haves:
  truths:
    - "Phase 149 already proves the Providence contract and auth seam, so Phase 150 should build a stateful adapter on top of that seam instead of revisiting payload shape."
    - "Incident lifecycle requires persistent external IDs linked to Swarm incidents; dead-letter alone is not enough."
    - "Providence health has to surface on `/healthz` and `/readyz`, so the adapter must report reachability and auth state, not just fire requests."
  artifacts:
    - path: "crates/swarm-runtime/src/ingest.rs"
      provides: "Integration seam where runtime mode transitions and Providence delivery already intersect."
    - path: "crates/swarm-spine/src/incident.rs"
      provides: "Persistent incident reference storage."
    - path: "crates/swarm-runtime/src/service.rs"
      provides: "Health-surface integration point for configured adapters."
  key_links:
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-spine/src/incident.rs"
      via: "mode transition to external reference persistence"
      pattern: "Providence incident ID is durable and tied to Swarm incident state"
    - from: "crates/swarm-runtime/src/service.rs"
      to: "crates/swarm-runtime/src/http/core.inc"
      via: "health reporting"
      pattern: "configured Providence lifecycle health appears on readiness surfaces"
---

<objective>
Complete Phase 150 by shipping a Providence incident lifecycle adapter that can create, update, and resolve incidents with retry, idempotency, durable external references, and surfaced health.

Purpose: move Providence from one-shot notification delivery to tracked incident synchronization.
Output: stateful Providence incident adapter, persisted incident IDs on Swarm incidents, retry plus dead-letter handling, and health / readiness reporting for the Providence integration.
</objective>

<context>
@.planning/phases/150-incident-lifecycle-adapter/150-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-spine/src/incident.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/service.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a stateful Providence incident adapter and durable external references</name>
  <files>
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-spine/src/incident.rs
  </files>
  <action>Build `ProvidenceIncidentAdapter` to create, update, and resolve Providence incidents on Swarm mode changes and persist the Providence incident ID as an external reference linked to the Swarm incident record.</action>
  <verify>
    <automated>cargo test -p swarm-runtime providence::tests:: -- --nocapture</automated>
  </verify>
  <done>Swarm can create, update, and resolve one Providence incident per durable incident key without losing the external reference across restart.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add retry, idempotent create-by-key, and dead-letter behavior</name>
  <files>
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-response/src/notification.rs
  </files>
  <action>Add create-by-key idempotency plus bounded retry and dead-letter handling consistent with the existing notification-router resilience path.</action>
  <verify>
    <automated>cargo test -p swarm-runtime providence::tests::adapter_retries_and_dead_letters_failures -- --exact</automated>
  </verify>
  <done>Repeated creates do not duplicate incidents, and failed Providence API calls are retried then dead-lettered deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface Providence adapter health on readiness probes</name>
  <files>
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/http/core.inc
  </files>
  <action>Report Providence integration readiness and authentication state on `/healthz` and `/readyz` whenever the incident adapter is configured.</action>
  <verify>
    <automated>cargo test -p swarm-runtime service::tests:: -- --nocapture</automated>
  </verify>
  <done>Operators can see when Providence is configured, authenticated, degraded, or unavailable from the existing health surfaces.</done>
</task>

</tasks>

<verification>
Build the stateful adapter first, then add resilience, then wire health surfaces. Keep the phase scoped to outbound lifecycle sync; inbound Providence feedback remains Phase 151.
</verification>
