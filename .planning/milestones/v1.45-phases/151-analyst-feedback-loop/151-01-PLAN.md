---
phase: 151-analyst-feedback-loop
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 150
files_modified:
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/providence.rs
  - crates/swarm-runtime/src/kitten_agent.rs
  - crates/swarm-spine/src/lib.rs
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - PROVFB-01
  - PROVFB-02
  - PROVFB-03
must_haves:
  truths:
    - "Phase 150 already owns the outbound Providence identity, HMAC, and incident-key seam; inbound feedback should attach to that seam instead of creating a second Providence identity model."
    - "Dismiss feedback is high-signal negative supervision, so persistence and replayability matter more than optimistic direct Kitten mutation."
    - "Feedback actions must leave a durable audit trail; HTTP success without persisted evidence is not enough."
  artifacts:
    - path: "crates/swarm-runtime/src/ingest.rs"
      provides: "Inbound HTTP endpoint and request verification seam for Providence feedback."
    - path: "crates/swarm-runtime/src/providence.rs"
      provides: "Stable incident-key and external-reference ownership from Phase 150."
    - path: "crates/swarm-runtime/src/kitten_agent.rs"
      provides: "Landing zone for negative fitness signals or pending fallback."
  key_links:
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-runtime/src/providence.rs"
      via: "incident_id / finding_id resolution"
      pattern: "Providence feedback addresses the same incidents created by the outbound adapter"
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-runtime/src/kitten_agent.rs"
      via: "dismiss -> negative fitness signal or pending persistence"
      pattern: "feedback survives when Kitten is unavailable"
---

<objective>
Complete Phase 151 by accepting signed Providence analyst actions and translating them into durable runtime feedback.

Purpose: make Providence analyst triage change Swarm behavior instead of stopping at outbound incident lifecycle.
Output: `POST /v1/providence/feedback`, HMAC verification, substrate + audit persistence for confirm/dismiss/investigate, and Kitten negative-signal wiring with bounded fallback.
</objective>

<context>
@.planning/phases/151-analyst-feedback-loop/151-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/providence.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/kitten_agent.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add signed Providence feedback intake and durable audit persistence</name>
  <files>
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-spine/src/lib.rs
  </files>
  <action>Expose `POST /v1/providence/feedback`, verify the Providence HMAC header, resolve the addressed incident / finding, and persist the full analyst action as a durable audit entry.</action>
  <verify>
    <automated>cargo test -p swarm-runtime providence_feedback::tests::signed_feedback_endpoint_persists_audit_entry -- --exact</automated>
  </verify>
  <done>Providence feedback is authenticated, replayable, and linked to the affected runtime artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Translate confirm / dismiss / investigate into runtime side effects</name>
  <files>
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/providence.rs
  </files>
  <action>Map `confirm`, `dismiss`, and `investigate` into substrate operations and investigation requests while reusing the Phase 150 incident identity seam.</action>
  <verify>
    <automated>cargo test -p swarm-runtime providence_feedback::tests::feedback_actions_translate_into_runtime_side_effects -- --exact</automated>
  </verify>
  <done>Every supported feedback action changes Swarm state deterministically and durably.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire dismiss feedback into Kitten negative fitness with durable fallback</name>
  <files>
    crates/swarm-runtime/src/kitten_agent.rs
    crates/swarm-runtime/src/ingest.rs
  </files>
  <action>Forward dismiss feedback to Kitten as a negative fitness signal when the evolution lane is deployed, and persist pending feedback when it is not.</action>
  <verify>
    <automated>cargo test -p swarm-runtime providence_feedback::tests::dismiss_feedback_reaches_kitten_or_pending_fallback -- --exact</automated>
  </verify>
  <done>False-positive dismissals become usable negative supervision without blocking on Kitten availability.</done>
</task>

</tasks>

<verification>
Land the signed HTTP seam first, then side effects, then the Kitten hookup. Keep the phase scoped to explicit analyst actions; full state reconciliation remains out of scope.
</verification>
