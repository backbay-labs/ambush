---
phase: 114-supply-chain-detector-and-profile-support
plan: 01
type: execute
wave: 1
depends_on: [112-01, 112-02]
files_modified:
  - crates/swarm-whisker/src/supply_chain.rs
  - crates/swarm-whisker/src/lib.rs
autonomous: true
requirements: [PERSIST-02, PERSIST-03, PERSIST-04]

must_haves:
  truths:
    - "`SupplyChainDetector` implements `DetectionStrategy` over `ProcessStart` and `FilePersistence` events"
    - "`SupplyChainProfile` validates trusted paths, signers, loader pairs, and thresholds"
    - "Findings emit `ThreatClass::SupplyChain` plus ATT&CK-tagged evidence"
  artifacts:
    - path: "crates/swarm-whisker/src/supply_chain.rs"
      provides: "supply-chain heuristics for trusted-path execution and DLL side-loading"
      contains: "SupplyChainDetector"
  key_links:
    - from: "crates/swarm-whisker/src/supply_chain.rs"
      to: "crates/swarm-core/src/pheromone.rs"
      via: "new threat class is surfaced directly in detector findings"
      pattern: "SupplyChain"
---

<objective>
Implement the profile and heuristics for the supply-chain detector.

Purpose: The runtime needs a dedicated strategy family for trusted-path abuse and DLL side-loading rather than treating those behaviors as generic execution.

Output: `SupplyChainProfile` plus a `SupplyChainDetector` that produces ATT&CK-tagged supply-chain findings.
</objective>
