---
phase: 112-telemetry-persistence-payloads-and-detector-contracts
plan: 02
type: execute
wave: 2
depends_on: [112-01]
files_modified:
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/src/canary.rs
  - crates/swarm-runtime/src/promotion.rs
  - crates/swarm-runtime/src/replay/core.inc
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime/src/escalation.rs
autonomous: true
requirements: [PERSIST-03, PERSIST-04]

must_haves:
  truths:
    - "Control, replay, canary, and promotion surfaces can route the new detector families"
    - "Runtime threat-class label helpers and metrics code recognize `SupplyChain`"
    - "Config validation can parse the new profile override payloads"
  artifacts:
    - path: "crates/swarm-runtime/src/control.rs"
      provides: "live-runtime detector selection for new strategies"
      contains: "supported_detector"
    - path: "crates/swarm-runtime/src/replay/core.inc"
      provides: "replay and candidate-manifest support for the new strategies"
      contains: "DetectorCandidateManifest"
  key_links:
    - from: "crates/swarm-runtime/src/config.rs"
      to: "crates/swarm-runtime/src/control.rs"
      via: "profile resolution feeds live detector construction"
      pattern: "_profile"
    - from: "crates/swarm-runtime/src/replay/core.inc"
      to: "crates/swarm-runtime/src/promotion.rs"
      via: "candidate manifests must stay aligned across replay and promotion"
      pattern: "strategy_id"
---

<objective>
Wire the shared contracts through every runtime detector-construction surface.

Purpose: Adding a detector only to the live path would leave replay, canary, and promotion drifting from the supported strategy set.

Output: Runtime profile helpers plus supported-detector routing that can construct `persistence` and `supply_chain` strategies everywhere the runtime already works with detector families.
</objective>
