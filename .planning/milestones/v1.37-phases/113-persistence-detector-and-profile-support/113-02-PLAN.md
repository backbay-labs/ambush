---
phase: 113-persistence-detector-and-profile-support
plan: 02
type: execute
wave: 2
depends_on: [113-01]
files_modified:
  - crates/swarm-whisker/src/persistence.rs
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/tests
autonomous: true
requirements: [PERSIST-01]

must_haves:
  truths:
    - "Focused tests prove suspicious persistence artifacts trigger and benign neighbors stay silent"
    - "Live runtime detector selection can construct the `persistence` strategy"
  artifacts:
    - path: "crates/swarm-whisker/src/persistence.rs"
      provides: "unit tests for persistence heuristics"
      contains: "mod tests"
---

<objective>
Prove the persistence detector and runtime-facing strategy selection.

Purpose: The milestone needs concrete evidence that the detector catches the intended footholds without widening false positives across neighboring registry or file activity.

Output: Focused detector tests plus runtime smoke coverage for `strategy: persistence`.
</objective>
