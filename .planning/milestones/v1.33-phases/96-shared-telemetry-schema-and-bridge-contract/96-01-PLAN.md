---
phase: 96-shared-telemetry-schema-and-bridge-contract
plan: 01
type: execute
wave: 1
depends_on: [95-01, 95-02]
files_modified:
  - crates/swarm-core/src/lib.rs
  - crates/swarm-core/src/telemetry.rs
  - crates/swarm-whisker/src/detector.rs
  - crates/swarm-whisker/src/lib.rs
  - crates/swarm-core/Cargo.toml
autonomous: true
requirements: [BRIDGE-01]

must_haves:
  truths:
    - "`TelemetryEvent` and its payload variants live in `swarm-core` so bridge crates can share one normalized schema without a dependency cycle"
    - "`swarm-core` defines a reusable `TelemetryBridge` contract plus shared bridge health and bridge error types"
    - "Existing detector and runtime code continue to compile through compatibility re-exports from `swarm-whisker`"
  artifacts:
    - path: "crates/swarm-core/src/telemetry.rs"
      provides: "shared telemetry schema plus bridge contract"
      contains: "pub trait TelemetryBridge"
    - path: "crates/swarm-whisker/src/detector.rs"
      provides: "detector compatibility over shared telemetry types"
      contains: "pub use swarm_core::telemetry"
  key_links:
    - from: "crates/swarm-core/src/telemetry.rs"
      to: "crates/swarm-whisker/src/detector.rs"
      via: "shared TelemetryEvent / TelemetryPayload reuse"
      pattern: "TelemetryEvent"
    - from: "crates/swarm-core/src/telemetry.rs"
      to: "bridge crates"
      via: "polling-based bridge contract and shared health model"
      pattern: "pub trait TelemetryBridge"
---

<objective>
Move the normalized telemetry schema into `swarm-core` and define the bridge contract that future bridge crates and runtime orchestration will share.

Purpose: `TelemetryBridge` cannot live in `swarm-core` while `TelemetryEvent` stays in `swarm-whisker`. This plan resolves that crate-boundary problem first so later runtime bridge work builds on a stable shared contract instead of per-crate duplication.

Output: shared telemetry/bridge types in `swarm-core` and compatibility re-exports that keep detector/runtime callers compiling.
</objective>
