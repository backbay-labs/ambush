---
phase: 96-shared-telemetry-schema-and-bridge-contract
plan: 02
type: execute
wave: 1
depends_on: [96-01]
files_modified:
  - crates/swarm-ingest-tetragon/Cargo.toml
  - crates/swarm-ingest-tetragon/src/bridge.rs
  - crates/swarm-ingest-tetragon/src/error.rs
  - crates/swarm-ingest-tetragon/src/lib.rs
  - crates/swarm-ingest-tetragon/src/mapper.rs
autonomous: true
requirements: [BRIDGE-02]

must_haves:
  truths:
    - "`TetragonBridge` implements the shared `TelemetryBridge` trait"
    - "The primary Tetragon bridge contract no longer requires a direct `Sender<TelemetryEvent>` input"
    - "Reconnect backoff and `ProcessExec` mapping behavior remain covered by tests after the trait port"
  artifacts:
    - path: "crates/swarm-ingest-tetragon/src/bridge.rs"
      provides: "trait-based Tetragon bridge with polling and bridge health"
      contains: "impl TelemetryBridge for TetragonBridge"
    - path: "crates/swarm-ingest-tetragon/src/bridge.rs"
      provides: "compatibility polling or send loop helpers built on the trait"
      contains: "pub async fn poll"
  key_links:
    - from: "crates/swarm-ingest-tetragon/src/bridge.rs"
      to: "crates/swarm-core/src/telemetry.rs"
      via: "shared TelemetryBridge and BridgeHealth contract"
      pattern: "TelemetryBridge"
    - from: "crates/swarm-ingest-tetragon/src/bridge.rs"
      to: "crates/swarm-ingest-tetragon/src/mapper.rs"
      via: "ProcessExec mapping preserved under trait polling"
      pattern: "map_process_exec"
---

<objective>
Refactor `TetragonBridge` onto the shared telemetry bridge contract without losing its reconnect behavior or event mapping coverage.

Purpose: The first bridge implementation proves the new contract is real. Porting Tetragon now validates the trait shape before the runtime starts managing multiple bridge instances in later phases.

Output: a trait-based `TetragonBridge`, retained reconnect/backoff coverage, and passing bridge tests under the new polling model.
</objective>
