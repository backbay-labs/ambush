---
phase: 98-runtime-bridge-registry-and-health-surface
plan: 01
type: execute
wave: 1
depends_on: [97-01, 97-02]
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/Cargo.toml
  - crates/swarm-runtime/src/bin/swarm_detect.rs
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/lib.rs
autonomous: true
requirements: [BRIDGE-05]

must_haves:
  truths:
    - "Serve mode constructs bridge instances from `runtime.telemetry_sources[*].bridge` and polls only configured bridges"
    - "Bridge-produced `TelemetryEvent` values flow into the same channel already consumed by `WhiskerAgent`"
    - "The runtime config can express Tetragon, CloudTrail, and generic JSON bridge instances by name"
  artifacts:
    - path: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      provides: "bridge worker startup integrated with serve mode"
      contains: "telemetry_tx"
    - path: "crates/swarm-runtime/src/ingest.rs"
      provides: "shared bridge health snapshot owned by runtime state"
      contains: "bridge"
  key_links:
    - from: "crates/swarm-core/src/config.rs"
      to: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      via: "named bridge config variants instantiated by the runtime"
      pattern: "TelemetryBridgeConfig"
    - from: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      to: "crates/swarm-runtime/src/whisker_agent.rs"
      via: "shared telemetry channel into live detection"
      pattern: "telemetry_tx"
---

<objective>
Build the runtime bridge registry and feed configured bridge output into the live detection lane.

Purpose: Bridge crates are only valuable if serve mode can instantiate them from repo config and feed their normalized output into the same live dispatcher path already used by HTTP ingest.

Output: named bridge config variants, runtime bridge-worker startup, and bridge events flowing into `WhiskerAgent` through the shared telemetry channel.
</objective>
