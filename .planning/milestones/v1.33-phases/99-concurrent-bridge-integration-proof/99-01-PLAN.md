---
phase: 99-concurrent-bridge-integration-proof
plan: 01
type: execute
wave: 1
depends_on: [98-01, 98-02]
files_modified:
  - crates/swarm-runtime/tests/bridge_registry_integration.rs
autonomous: true
requirements: [BRIDGE-06]

must_haves:
  truths:
    - "At least two configured bridge instances run concurrently against the same runtime input channel"
    - "Both bridge-produced events are consumed by the live Whisker detection path"
    - "The proof remains bounded and deterministic without external bridge dependencies"
  artifacts:
    - path: "crates/swarm-runtime/tests/bridge_registry_integration.rs"
      provides: "concurrent multi-bridge runtime harness"
      contains: "BridgeRuntimeRegistry"
  key_links:
    - from: "crates/swarm-runtime/src/bridge_runtime.rs"
      to: "crates/swarm-runtime/tests/bridge_registry_integration.rs"
      via: "shared worker spawn and bridge health surface"
      pattern: "BridgeRuntimeRegistry"
    - from: "crates/swarm-runtime/src/whisker_agent.rs"
      to: "crates/swarm-runtime/tests/bridge_registry_integration.rs"
      via: "shared telemetry channel drained into detect-and-deposit"
      pattern: "WhiskerAgent"
---

<objective>
Create a deterministic integration harness that starts two real bridge workers and feeds their normalized events into the shipped Whisker detection lane.

Purpose: The bridge architecture needs proof that concurrency works in the real runtime seam, not just in isolated bridge or metrics tests.

Output: a bounded integration test that starts two bridge workers, drains their output through `WhiskerAgent`, and proves both sources reached the substrate.
</objective>
