---
phase: 243-cross-detector-fitness-improvement-proof
plan: 01
type: execute
wave: 1
depends_on:
  - 242-multi-detector-evolution-benchmark
files_modified:
  - crates/swarm-runtime/src/kitten_agent.rs
autonomous: true
requirements:
  - GENOME-04
must_haves:
  truths:
    - "The milestone closes only when at least one non-process-tree detector shows measured improvement above baseline."
    - "The proof must use the same persisted benchmark report surface introduced in Phase 242."
    - "The final claim must stay bounded to what is actually demonstrated."
  artifacts:
    - path: ".planning/phases/243-cross-detector-fitness-improvement-proof"
      provides: "Context, plan, summary, and verification artifacts for Phase 243."
---

<objective>
Use the shared measured benchmark harness to prove that a non-process-tree
detector outperforms its conservative baseline on measured autonomous fitness.
</objective>

<context>
@.planning/phases/243-cross-detector-fitness-improvement-proof/243-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/kitten_agent.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add a bounded non-process-tree improvement proof</name>
  <files>
    crates/swarm-runtime/src/kitten_agent.rs
  </files>
  <action>Stage a conservative non-process-tree detector baseline, run one bounded measured benchmark generation, and assert that the persisted leader metrics exceed the baseline metrics on the same report surface.</action>
  <done>At least one non-process-tree detector has an explicit executable proof of autonomous fitness improvement above baseline.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Record the bounded milestone claim</name>
  <files>
    .planning/phases/243-cross-detector-fitness-improvement-proof
  </files>
  <action>Capture which detector improved, what metric moved, and which detectors remain benchmarkable but unproven for this milestone.</action>
  <done>Milestone closeout stays faithful to the measured proof instead of over-claiming detector breadth.</done>
</task>

</tasks>

<verification>
Verify with the dedicated non-process-tree improvement test and retain the
shared multi-detector benchmark coverage as supporting evidence.
</verification>
