---
phase: 241-fileless-and-dns-detector-genomes
plan: 01
type: execute
wave: 1
depends_on:
  - 240-behavioral-anomaly-genome-and-mutation-operators
files_modified:
  - crates/swarm-runtime/src/mutation/autonomous.rs
  - crates/swarm-runtime/src/mutation/test_support.rs
  - crates/swarm-runtime/src/mutation/tests_core.rs
autonomous: true
requirements:
  - GENOME-02
must_haves:
  truths:
    - "Fileless execution and DNS exfiltration must use the same typed genome seam as behavioral anomaly instead of creating another legacy side path."
    - "Autonomous recipes must stay bounded and replayable so the resulting genomes remain explainable."
    - "The new detector support must compose with the existing materialization and lineage flow."
  artifacts:
    - path: ".planning/phases/241-fileless-and-dns-detector-genomes"
      provides: "Context, plan, summary, and verification artifacts for Phase 241."
---

<objective>
Extend typed mutation-genome support to fileless execution and DNS exfiltration
detectors with bounded autonomous seed-control, perturbation, and crossover
recipes.
</objective>

<context>
@.planning/phases/241-fileless-and-dns-detector-genomes/241-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/mutation/autonomous.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/mutation/test_support.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-whisker/src/fileless_execution.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-whisker/src/dns_exfiltration.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add typed fixture and profile support for fileless and DNS genomes</name>
  <files>
    crates/swarm-runtime/src/mutation/test_support.rs
    crates/swarm-runtime/src/mutation/tests_core.rs
  </files>
  <action>Expose typed fileless-execution and DNS-exfiltration fixtures through the mutation test support layer so the new genome lane can be materialized and asserted directly.</action>
  <done>Focused tests can load typed fileless and DNS detector candidates through the mutation pipeline.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement bounded autonomous recipes for fileless and DNS detectors</name>
  <files>
    crates/swarm-runtime/src/mutation/autonomous.rs
  </files>
  <action>Teach autonomous generation to emit seed-control, bounded-perturbation, and crossover variants for fileless-execution and DNS-exfiltration parent genomes.</action>
  <done>The mutation lane can derive replayable fileless and DNS variants from compatible typed parents.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Prove typed candidate materialization and autonomous generation</name>
  <files>
    crates/swarm-runtime/src/mutation/tests_core.rs
  </files>
  <action>Add focused tests proving typed fileless and DNS targets materialize and that autonomous generation emits bounded variants for both detector families.</action>
  <done>Phase 241 has focused red-green evidence that both new detector families participate in the typed mutation lane.</done>
</task>

</tasks>

<verification>
Verify with targeted fileless and DNS typed-genome mutation tests plus a
crate-local `cargo check` on `swarm-runtime`.
</verification>
