---
phase: 242-multi-detector-evolution-benchmark
plan: 01
type: execute
wave: 1
depends_on:
  - 241-fileless-and-dns-detector-genomes
files_modified:
  - crates/swarm-runtime/src/kitten_agent.rs
  - crates/swarm-runtime/src/mutation/harness.rs
autonomous: true
requirements:
  - GENOME-03
must_haves:
  truths:
    - "The benchmark harness must evaluate process-tree, behavioral-anomaly, fileless-execution, and DNS-exfiltration detectors through one shared loop."
    - "Benchmark measurement must not depend on queue-readiness because blocked validation results still carry measured fitness."
    - "Benchmark reports must keep comparable baseline and generation metrics for later proof and regression work."
  artifacts:
    - path: ".planning/phases/242-multi-detector-evolution-benchmark"
      provides: "Context, plan, summary, and verification artifacts for Phase 242."
---

<objective>
Generalize the bounded measured benchmark harness so every supported detector
family can run through the same evolution loop and persist comparable baseline
and generation metrics.
</objective>

<context>
@.planning/phases/242-multi-detector-evolution-benchmark/242-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/kitten_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/mutation/harness.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Generalize benchmark detector input construction</name>
  <files>
    crates/swarm-runtime/src/kitten_agent.rs
  </files>
  <action>Teach the benchmark harness to build detector-specific evasion-pressure input for behavioral anomaly, fileless execution, and DNS exfiltration in addition to the existing process-tree baseline.</action>
  <done>One benchmark entry point can configure detector-specific staged measurement across all supported detector families.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Preserve measured fitness for blocked benchmark candidates</name>
  <files>
    crates/swarm-runtime/src/mutation/harness.rs
    crates/swarm-runtime/src/kitten_agent.rs
  </files>
  <action>Split benchmark population refresh from live proposal refresh so the benchmark can retain blocked validation results and their measured autonomous fitness without changing proposal-ready selection behavior.</action>
  <done>Generation summaries can resolve a leader and mean fitness even when benchmark candidates are not queue-ready.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Prove cross-detector benchmark coverage</name>
  <files>
    crates/swarm-runtime/src/kitten_agent.rs
  </files>
  <action>Add staged benchmark tests covering process-tree continuity plus behavioral, fileless, and DNS breadth through one bounded benchmark run.</action>
  <done>The benchmark harness persists generation results for every currently supported detector genome family.</done>
</task>

</tasks>

<verification>
Verify with a `swarm-runtime` crate check plus the measured benchmark tests for
generation deltas, conservative-seed improvement, and non-process-tree detector
breadth.
</verification>
