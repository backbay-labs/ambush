---
phase: 190-hot-path-criterion-benchmarks
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 189
files_modified:
  - crates/swarm-runtime/Cargo.toml
  - crates/swarm-runtime/benches/hot_path.rs
  - docs/benchmarks/fast-detection.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - JTEST-03
must_haves:
  truths:
    - "Criterion benchmarks must measure the repo-owned hot path directly instead of shelling out to one-off example binaries."
    - "The benchmark fixture must be reproducible and bounded so later runs compare like-for-like latency percentiles."
    - "Phase 190 measures p50, p95, and p99 latency for the hot path; sustained throughput and readiness shedding stay in Phase 191."
  artifacts:
    - path: "crates/swarm-runtime/Cargo.toml"
      provides: "Criterion wiring for repo-owned runtime benchmarks."
    - path: "crates/swarm-runtime/benches/hot_path.rs"
      provides: "A runnable Criterion benchmark for ingest->detect->deposit->escalate latency."
    - path: "docs/benchmarks/fast-detection.md"
      provides: "Recorded baseline data and workload contract for future comparison."
---

<objective>
Complete Phase 190 by adding a repo-owned Criterion benchmark for the bounded
runtime hot path and recording the first percentile baseline in the docs.

Purpose: replace ad hoc manual timing with a first-class benchmark entrypoint
that future runtime changes can compare against directly.
Output: Criterion wiring in `swarm-runtime`, a reproducible hot-path benchmark,
and updated benchmark documentation with p50/p95/p99 context.
</objective>

<context>
@.planning/phases/190-hot-path-criterion-benchmarks/190-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/Cargo.toml
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/examples/fast_detection_bench.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/examples/end_to_end_ingest_bench.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/docs/benchmarks/fast-detection.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/docs/benchmarks/end-to-end-ingest.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Criterion wiring and a repo-owned hot-path fixture</name>
  <files>
    crates/swarm-runtime/Cargo.toml
    crates/swarm-runtime/benches/hot_path.rs
  </files>
  <action>Add `criterion` benchmark wiring to `swarm-runtime` and build a reproducible benchmark fixture that measures the bounded ingest -> detect -> deposit -> escalate slice with stable synthetic telemetry and substrate setup.</action>
  <done>The repo can run a dedicated Criterion benchmark for the runtime hot path and produce p50, p95, and p99 latency data without relying on example-only timing code.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Record the baseline and invocation contract</name>
  <files>
    docs/benchmarks/fast-detection.md
  </files>
  <action>Update the benchmark documentation with the exact benchmark command, workload shape, backend assumptions, and measured percentile outputs so future runs can compare against the same contract.</action>
  <done>The repo contains a checked-in baseline describing how the Criterion benchmark was run and what percentile results it produced.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Keep latency benchmarking separate from throughput work</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <action>Advance the planning docs so Phase 190 closes the Criterion latency baseline while Phase 191 continues to own sustained throughput and readiness-shedding measurements.</action>
  <done>The planning documents preserve a clean separation between percentile latency baselines and the later sustained load envelope work.</done>
</task>

</tasks>

<verification>
Verify that the new Criterion benchmark runs through `cargo bench` with a
stable fixture, emits percentile data for the hot path, and that the recorded
documentation matches the shipped benchmark invocation and scope.
</verification>
