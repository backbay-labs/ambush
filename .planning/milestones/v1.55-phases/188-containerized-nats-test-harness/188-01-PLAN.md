---
phase: 188-containerized-nats-test-harness
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 187
files_modified:
  - docker-compose.yml
  - .github/workflows/ci.yml
  - tools/with-nats-jetstream.sh
  - crates/swarm-pheromone/tests/jetstream.rs
  - crates/swarm-pheromone/tests/multi_instance.rs
autonomous: true
requirements:
  - JTEST-01
must_haves:
  truths:
    - "The repo must own one deterministic JetStream bring-up path instead of relying on a manually started external NATS server."
    - "The harness must be reusable by later pheromone, runtime, and benchmark phases through stable environment and teardown semantics."
    - "CI must exercise the harness at least once so JetStream test infrastructure stops being purely aspirational."
  artifacts:
    - path: "tools/with-nats-jetstream.sh"
      provides: "Repo-owned compose-backed JetStream harness with health wait and cleanup."
    - path: ".github/workflows/ci.yml"
      provides: "Shared CI entrypoint that can boot the harness and run JetStream-backed verification."
    - path: "crates/swarm-pheromone/tests/jetstream.rs"
      provides: "Harness-backed JetStream smoke coverage anchored to the existing substrate test surface."
---

<objective>
Complete Phase 188 by adding a reusable containerized JetStream test harness and
proving that CI can boot it and run at least one real JetStream-backed
verification path.

Purpose: remove the manual NATS prerequisite that currently keeps JetStream
tests ignored and out of the normal repo workflow.
Output: compose-backed harness, CI bootstrap, and one reusable JetStream smoke
path ready for the full substrate migration in Phase 189.
</objective>

<context>
@.planning/phases/188-containerized-nats-test-harness/188-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/docker-compose.yml
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.github/workflows/ci.yml
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-pheromone/tests/jetstream.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-pheromone/tests/multi_instance.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add a repo-owned JetStream harness entrypoint</name>
  <files>
    docker-compose.yml
    tools/with-nats-jetstream.sh
  </files>
  <action>Add one compose-backed wrapper that starts the repo-owned JetStream container, waits for health, exports `NATS_URL`, runs a supplied command, and always tears the environment down cleanly.</action>
  <done>Local and CI callers can run JetStream-backed commands through one deterministic harness instead of manual container setup.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Prove CI can boot and consume the harness</name>
  <files>
    .github/workflows/ci.yml
    crates/swarm-pheromone/tests/jetstream.rs
    crates/swarm-pheromone/tests/multi_instance.rs
  </files>
  <action>Wire one CI path through the new harness and convert at least one existing ignored JetStream-backed verification path into a harness-consumed smoke test that can run non-interactively.</action>
  <done>CI demonstrates that the repo can start JetStream and run real backend coverage without a pre-provisioned external NATS server.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Leave a clean handoff for the full suite migration</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <action>Record the shipped harness contract and make explicit that Phase 189 owns the identical-assertion substrate-suite migration across the remaining JetStream tests.</action>
  <done>The roadmap and requirement mapping clearly distinguish harness bootstrap from the broader JetStream assertion migration.</done>
</task>

</tasks>

<verification>
Verify that the new harness can bootstrap JetStream deterministically, that CI
can invoke it, and that at least one real JetStream-backed test or smoke path
passes through that harness without manual setup.
</verification>
