---
phase: 191-sustained-throughput-load-test
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 190
files_modified:
  - crates/swarm-runtime/examples/end_to_end_ingest_bench.rs
  - docs/benchmarks/end-to-end-ingest.md
  - docs/CONFIGURATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/MILESTONES.md
  - .planning/STATE.md
autonomous: true
requirements:
  - JTEST-04
must_haves:
  truths:
    - "The sustained-throughput artifact must exercise the real HTTP ingest path, not only the detector microbenchmark."
    - "The load test must capture the first readiness-shedding transition instead of only reporting a best-case throughput number."
    - "The measured host profile and heap-budget assumptions must be explicit so later reruns compare like-for-like."
  artifacts:
    - path: "crates/swarm-runtime/examples/end_to_end_ingest_bench.rs"
      provides: "A shipped load-test mode that ramps concurrency until `/readyz` sheds."
    - path: "docs/benchmarks/end-to-end-ingest.md"
      provides: "Recorded steady-state and readiness-shed benchmark baselines."
    - path: "docs/CONFIGURATION.md"
      provides: "Operator-facing sizing and alert guidance tied to the measured envelope."
---

<objective>
Complete Phase 191 by turning the existing end-to-end ingest benchmark into a
repeatable load-test artifact that captures the highest stable accepted-event
rate before readiness shedding.

Purpose: give operators one repo-owned command that measures both steady-state
ingest behavior and the first `/readyz` shed threshold on the real runtime
surface.
Output: a ramp-to-shed benchmark mode, measured reference numbers, and updated
operator documentation that explains how to rerun the ceiling on deployment
hardware.
</objective>

<context>
@.planning/phases/191-sustained-throughput-load-test/191-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/examples/end_to_end_ingest_bench.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/health.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/detection/metrics.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/docs/benchmarks/end-to-end-ingest.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/docs/CONFIGURATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend the benchmark with a monotonic ramp-to-shed mode</name>
  <files>
    crates/swarm-runtime/examples/end_to_end_ingest_bench.rs
  </files>
  <action>Keep the fixed steady-state benchmark and add a ramp mode that doubles request concurrency, polls `/readyz` during the run, records per-stage throughput plus latency, and stops at the first readiness-shedding transition.</action>
  <done>The repo can run one shipped benchmark command that reports the highest stable accepted-event rate before `/readyz` turns unhealthy.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Publish the measured host-profile and shed-threshold artifact</name>
  <files>
    docs/benchmarks/end-to-end-ingest.md
    docs/CONFIGURATION.md
  </files>
  <action>Record the fixed-profile and ramp-to-shed commands, the measured reference-host numbers, the threshold used to make readiness shedding observable, and the rerun guidance for JetStream or deployment-specific memory budgets.</action>
  <done>Operators can read one repo-owned artifact that explains the steady-state envelope, the highest stable rate before shed, and how to re-measure the ceiling on their own host.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Advance the planning docs and close the milestone</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    .planning/MILESTONES.md
    .planning/STATE.md
  </files>
  <action>Mark JTEST-04 complete, close `v1.55`, and queue the first `v1.56` phase with fresh context and a plan.</action>
  <done>The planning documents show `v1.55` shipped, `JTEST-04` satisfied, and `v1.56` active with Phase 192 queued next.</done>
</task>

</tasks>

<verification>
Verify that the example compiles, the fixed mode still reports the steady-state
ingest envelope, the ramp mode reports the first `/readyz` shed stage, and the
checked-in docs match the measured host-profile contract.
</verification>
