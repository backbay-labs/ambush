---
phase: 189-jetstream-substrate-test-migration
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 188
files_modified:
  - .github/workflows/ci.yml
  - tools/with-nats-jetstream.sh
  - crates/swarm-pheromone/tests/jetstream.rs
  - crates/swarm-pheromone/tests/multi_instance.rs
  - crates/swarm-pheromone/src/substrate.rs
autonomous: true
requirements:
  - JTEST-02
must_haves:
  truths:
    - "JetStream must satisfy the same substrate semantics already asserted against the in-memory and local-journal backends."
    - "The expanded JetStream suite must run through the Phase 188 harness rather than through ad hoc external NATS setup."
    - "Backend-specific failures must stay attributable: test output should distinguish real JetStream regressions from harness or fixture issues."
  artifacts:
    - path: "crates/swarm-pheromone/tests/jetstream.rs"
      provides: "JetStream-backed substrate contract coverage beyond a single smoke test."
    - path: "crates/swarm-pheromone/tests/multi_instance.rs"
      provides: "Shared-backend source-diversity and cross-instance visibility assertions on JetStream."
    - path: ".github/workflows/ci.yml"
      provides: "CI execution of the expanded JetStream substrate suite through the repo-owned harness."
---

<objective>
Complete Phase 189 by migrating the real substrate assertion matrix onto the
JetStream backend using the shipped Phase 188 harness.

Purpose: turn the JetStream backend from smoke-tested infrastructure into a
backend with meaningful semantic parity coverage.
Output: expanded harness-backed JetStream tests, CI execution for that suite,
and a clear backend contract that matches the in-memory substrate behavior.
</objective>

<context>
@.planning/phases/189-jetstream-substrate-test-migration/189-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/tools/with-nats-jetstream.sh
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-pheromone/tests/jetstream.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-pheromone/tests/multi_instance.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-pheromone/src/substrate.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expand JetStream contract coverage to match existing substrate semantics</name>
  <files>
    crates/swarm-pheromone/tests/jetstream.rs
    crates/swarm-pheromone/tests/multi_instance.rs
    crates/swarm-pheromone/src/substrate.rs
  </files>
  <action>Port the existing in-memory and local-journal substrate assertions that define replay, filtering, source-diversity, concentration decay, garbage collection, and signed-deposit rejection into harness-backed JetStream tests, sharing helpers where practical.</action>
  <done>JetStream tests assert the same core substrate semantics as the current repo-owned backend contract instead of only verifying container reachability and reconnect smoke paths.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run the expanded JetStream suite through the repo-owned harness in CI</name>
  <files>
    .github/workflows/ci.yml
    tools/with-nats-jetstream.sh
  </files>
  <action>Update CI to invoke the broader JetStream suite through the existing harness without introducing a second orchestration path.</action>
  <done>The repository runs meaningful JetStream substrate verification automatically in CI through the same entrypoint local developers use.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Preserve a clean boundary for later benchmark phases</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <action>Record that Phase 189 closes semantic substrate parity, while performance measurement still belongs to the later benchmark and load phases.</action>
  <done>The roadmap and requirements keep semantic backend parity distinct from latency and throughput measurement work.</done>
</task>

</tasks>

<verification>
Verify that the expanded JetStream tests pass through the Phase 188 harness,
that they cover the intended substrate semantics, and that CI uses the same
harness-backed path for real backend verification.
</verification>
