---
phase: 222-pheromone-wire-format-versioning
plan: 01
type: execute
wave: 1
depends_on:
  - phase-221
files_modified:
  - crates/swarm-core/src
  - crates/swarm-pheromone/src
  - crates/swarm-pheromone/tests
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - DECOMP-03
must_haves:
  truths:
    - "Pheromone deposits must carry an explicit schema version instead of relying on implicit JSON shape."
    - "The substrate must accept only the current and immediately previous deposit version with bounded migration and fail closed for unsupported versions."
    - "Phase 222 stays scoped to pheromone wire format and migration; operator/API response version negotiation remains Phase 223."
  artifacts:
    - path: "crates/swarm-core/src/pheromone.rs"
      provides: "A versioned shared deposit contract."
    - path: "crates/swarm-pheromone/src"
      provides: "Version-aware signing, validation, and substrate hydration paths."
---

<objective>
Add explicit versioning and bounded migration to pheromone deposit wire payloads
without breaking the current substrate contract for supported versions.
</objective>

<context>
@.planning/phases/222-pheromone-wire-format-versioning/222-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/pheromone.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-pheromone/src/substrate.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-pheromone/src/jetstream.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Introduce an explicit versioned deposit contract</name>
  <files>
    crates/swarm-core/src
    crates/swarm-pheromone/src
  </files>
  <action>Add an explicit schema version to pheromone deposits and align the signing and validation payloads on that shared versioned contract.</action>
  <done>Deposits no longer rely on implicit field shape, and the shared contract used by emitters and validators includes explicit version metadata.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add bounded current-plus-previous migration proof</name>
  <files>
    crates/swarm-pheromone/src
    crates/swarm-pheromone/tests
  </files>
  <action>Teach the substrate hydration path to accept the current and immediately previous deposit version with explicit migration, and add focused tests for supported and unsupported versions.</action>
  <done>Supported current and previous deposits round-trip through the substrate, while unsupported versions fail closed with explicit errors.</done>
</task>

</tasks>

<verification>
Verify that the versioned pheromone deposit contract still compiles through the
shared `swarm-core` and substrate surfaces, and that focused tests prove current
and previous version compatibility while unsupported versions are rejected.
</verification>
