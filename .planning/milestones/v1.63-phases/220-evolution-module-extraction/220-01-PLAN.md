---
phase: 220-evolution-module-extraction
plan: 01
type: execute
wave: 1
depends_on:
  - phase-219
files_modified:
  - crates/swarm-evolution/src
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - DECOMP-01
must_haves:
  truths:
    - "`evolution.rs` must be decomposed into focused submodules without silently changing the current runtime behavior."
    - "The public `swarm-evolution` crate surface used by runtime callers must stay stable while internals move behind explicit module boundaries."
    - "Phase 220 stays scoped to `evolution.rs`; the separate `mutation.rs` breakup is Phase 221."
  artifacts:
    - path: "crates/swarm-evolution/src"
      provides: "A focused `evolution/` module tree with no extracted file exceeding the phase size target."
---

<objective>
Split the oversized `swarm-evolution` evolution module into focused internal
submodules while preserving the crate's existing behavior and public contract.
</objective>

<context>
@.planning/phases/220-evolution-module-extraction/220-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/evolution.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract focused evolution submodules</name>
  <files>
    crates/swarm-evolution/src
  </files>
  <action>Move coherent sections of `evolution.rs` into focused sibling modules under an `evolution/` tree and keep the top-level module as a thin composition surface.</action>
  <done>No extracted file in the `evolution` module tree exceeds the milestone size target, and the split reflects coherent responsibilities instead of arbitrary line-count slicing.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Preserve the crate contract and verify the refactor</name>
  <files>
    crates/swarm-evolution/src
  </files>
  <action>Keep the current crate-level imports and runtime callers compiling, tighten internal visibility where practical, and verify the refactor through focused crate checks or tests.</action>
  <done>`swarm-evolution` still compiles and the refactor remains behavior-preserving from the perspective of existing crate consumers.</done>
</task>

</tasks>

<verification>
Verify that `swarm-evolution` still builds through the current crate surface and
that the extracted `evolution` module tree satisfies the size-boundary goal for
this phase.
</verification>
