---
phase: 223-api-response-schema-migration
plan: 01
type: execute
wave: 1
depends_on:
  - phase-222
files_modified:
  - crates/swarm-runtime/src
  - crates/swarm-cli/src
  - docs/CONFIGURATION.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - DECOMP-04
must_haves:
  truths:
    - "Operator-facing API envelopes must carry explicit schema version metadata instead of relying on implicit JSON shape."
    - "Breaking response changes must go through one bounded negotiation path rather than landing as silent field drift."
    - "Existing repo-owned CLI and operator clients must retain one compatibility lane while newer schema versions are introduced."
  artifacts:
    - path: "crates/swarm-runtime/src"
      provides: "Versioned response envelopes plus bounded negotiation on operator-facing runtime APIs."
    - path: "crates/swarm-cli/src"
      provides: "CLI compatibility with the negotiated response schema contract."
---

<objective>
Introduce explicit schema versions and bounded negotiation for operator-facing
API response envelopes without forking the current runtime or CLI control path.
</objective>

<context>
@.planning/phases/223-api-response-schema-migration/223-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/control.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/platform_api.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-cli/src/core.inc
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Introduce a shared versioned API envelope contract</name>
  <files>
    crates/swarm-runtime/src
    crates/swarm-cli/src
  </files>
  <action>Add explicit schema version metadata and one bounded negotiation seam for operator-facing JSON responses so version selection is centralized instead of ad hoc per endpoint.</action>
  <done>Runtime responses and CLI consumers rely on one shared versioned envelope contract rather than implicit payload shape.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate shipped operator surfaces onto the negotiated envelope</name>
  <files>
    crates/swarm-runtime/src
    docs/CONFIGURATION.md
  </files>
  <action>Adopt the versioned envelope on the current operator and platform status/review surfaces, keep one bounded compatibility path for existing clients, and add focused proof that unsupported schema requests fail closed.</action>
  <done>Current operator-facing APIs expose explicit schema versions, compatibility remains bounded for existing clients, and unsupported negotiated versions fail closed with explicit errors.</done>
</task>

</tasks>

<verification>
Verify that the versioned response envelope compiles through the runtime and CLI
surfaces, and that focused tests prove explicit schema metadata, compatibility
handling, and fail-closed negotiation for unsupported versions.
</verification>
