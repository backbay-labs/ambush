---
milestone: v1.28
audited: 2026-04-05T06:02:03Z
status: passed
scores:
  requirements: 4/4
  phases: 2/2
  integration: 4/4
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - JetStream concentration, listing, and GC still scan current KV keys client-side, which is acceptable for current small volumes but not yet server-side optimized
nyquist:
  compliant_phases: []
  partial_phases: []
  missing_phases:
    - 86-nats-jetstream-pheromone-backend
    - 87-multi-instance-coordination-and-cleanup
  overall: missing
---

# Milestone v1.28 Audit

## Summary

`v1.28 Durable Substrate And Multi-Instance Coordination` achieved its definition of done. The repo now supports a durable JetStream-backed pheromone substrate selected from repo-owned config, proves cross-instance pheromone aggregation and `min_sources_for_escalation` behavior against a shared NATS bucket, and removes the dead Python/PyO3 bridge surface from the live workspace.

## Requirements Coverage

| Requirement | Phase | Verification | Final Status | Evidence |
|-------------|-------|--------------|--------------|----------|
| SUB-01 | 86 | passed | satisfied | `JetStreamPheromoneSubstrate` now persists deposits in JetStream KV, and live-NATS restart tests confirm reconnect-safe recovery. |
| SUB-02 | 87 | passed | satisfied | Two substrate instances pointed at the same JetStream bucket now observe each other's deposits and shared recent-deposit queries. |
| SUB-03 | 87 | passed | satisfied | Multi-instance tests prove `distinct_sources` remains keyed to unique `agent_id` values and `exceeds_threshold` only flips after a second source contributes. |
| CLEAN-01 | 87 | passed | satisfied | `crates/swarm-bridge/`, `kernel/`, and `pyproject.toml` were removed, and canonical docs were updated to match the live repo layout. |

## Phase Verification

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 86 NATS JetStream Pheromone Backend | passed | 4/4 | Added config-selected JetStream durability, lazy KV bootstrap, restart-safe persistence, and GC/concentration verification. |
| 87 Multi-Instance Coordination And Cleanup | passed | 8/8 | Added live multi-instance aggregation tests, enforced distinct-source semantics, and removed the dead Python/PyO3 surface without workspace regressions. |

## Cross-Phase Integration

| Flow | Status | Evidence |
|------|--------|----------|
| `pheromone.backend.kind: jet_stream` -> `ConfiguredPheromoneSubstrate::JetStream` -> existing runtime/service wiring | ✓ VERIFIED | `swarm-core` config, `swarm-pheromone` selector wiring, and runtime bootstrap all build cleanly without an async constructor refactor. |
| JetStream KV deposit -> reconnect/reopen -> durable deposit recovery | ✓ VERIFIED | Live JetStream integration tests passed against a temporary NATS server on `127.0.0.1:4223`. |
| shared JetStream bucket -> cross-instance concentration -> `min_sources_for_escalation` gating | ✓ VERIFIED | The multi-instance integration suite passed live and proved source diversity semantics across two substrate instances. |
| legacy artifact removal -> repo docs -> workspace health | ✓ VERIFIED | Cleanup removed `swarm-bridge`, `kernel`, and `pyproject.toml`, updated canonical docs, and preserved green `build`, `clippy`, and `test` runs. |

## End-to-End Flows

| Flow | Status | Evidence |
|------|--------|----------|
| configure and build a durable JetStream substrate from repo-owned config | ✓ VERIFIED | `cargo test -p swarm-core` and `cargo test -p swarm-pheromone` both passed with the new `jet_stream` backend configuration path. |
| deposit into JetStream, reconnect to the same bucket, and recover persisted deposits | ✓ VERIFIED | `deposits_survive_reconnect_with_shared_bucket` passed live. |
| evaporated deposits do not contribute to concentration and can be garbage-collected from JetStream | ✓ VERIFIED | `gc_removes_evaporated_entries_and_preserves_fresh_concentration` passed live. |
| two instances share pheromone state and require two distinct sources before escalation | ✓ VERIFIED | `cross_instance_deposit_visibility`, `cross_instance_query_deposits`, `single_instance_no_inflation`, and `escalation_requires_min_sources` all passed live. |
| remove the dead Python/PyO3 surface and keep the Rust workspace green | ✓ VERIFIED | Filesystem checks plus `cargo fmt --all -- --check`, `cargo build --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo test --workspace` all passed post-cleanup. |

## Notes

- The compose-managed NATS sidecar intentionally remains internal-only, so live host verification used a short-lived JetStream container published on `127.0.0.1:4223`.
- Historical docs that discuss the old Python control-plane design remain in the repo as reference material, but the live filesystem and canonical docs now reflect the Rust-only workspace.
- Nyquist validation artifacts are still absent for phases 86 and 87 and remain follow-up work.

## Verdict

Milestone `v1.28` passes audit and is ready for archival.
