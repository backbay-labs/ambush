# Phase 36 Context

Once selection pressure is explicit, the runtime needs a durable draft artifact that packages that evidence into a candidate proposal shape without promoting it into the reviewed queue automatically.
