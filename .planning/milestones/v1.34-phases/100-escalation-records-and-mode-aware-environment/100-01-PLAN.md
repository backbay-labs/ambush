---
phase: 100-escalation-records-and-mode-aware-environment
plan: 01
type: execute
wave: 1
depends_on: [99-01, 99-02]
files_modified:
  - crates/swarm-core/src/pheromone.rs
  - crates/swarm-core/src/lib.rs
  - crates/swarm-pheromone/src/substrate.rs
  - crates/swarm-pheromone/src/jetstream.rs
autonomous: true
requirements: [SUBSTRATE-01]

must_haves:
  truths:
    - "The shared substrate contract can persist and query `EscalationRecord` entries by timestamp"
    - "In-memory, local-journal, and JetStream substrate backends all implement the escalation-record path"
    - "Escalation history persistence does not alter existing pheromone deposit query semantics"
  artifacts:
    - path: "crates/swarm-pheromone/src/substrate.rs"
      provides: "shared escalation-record trait contract plus in-memory and local-journal persistence"
      contains: "query_escalations"
    - path: "crates/swarm-pheromone/src/jetstream.rs"
      provides: "durable JetStream escalation-record persistence"
      contains: "EscalationRecord"
  key_links:
    - from: "crates/swarm-core/src/pheromone.rs"
      to: "crates/swarm-pheromone/src/substrate.rs"
      via: "shared durable escalation record shape"
      pattern: "EscalationRecord"
    - from: "crates/swarm-pheromone/src/substrate.rs"
      to: "crates/swarm-pheromone/src/jetstream.rs"
      via: "aligned backend persistence/query behavior"
      pattern: "query_escalations"
---

<objective>
Add durable escalation-history storage to the shared substrate contract and implement it across every current substrate backend.

Purpose: Future operator flows and threat-intel escalation proof need substrate-owned escalation history instead of process-local runtime state only.

Output: `EscalationRecord` types, substrate persistence/query methods, and backend tests proving in-memory, local-journal, and JetStream support the same escalation-history contract.
</objective>
