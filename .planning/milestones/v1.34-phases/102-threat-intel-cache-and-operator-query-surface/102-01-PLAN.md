---
phase: 102-threat-intel-cache-and-operator-query-surface
plan: 01
type: execute
wave: 1
depends_on: [101-01, 101-02]
files_modified:
  - crates/swarm-core/src/pheromone.rs
  - crates/swarm-core/src/lib.rs
  - crates/swarm-pheromone/src/substrate.rs
  - crates/swarm-pheromone/src/jetstream.rs
autonomous: true
requirements: [SUBSTRATE-04]

must_haves:
  truths:
    - "The substrate stores durable threat-intel entries with indicator type, value, confidence, and expiration time"
    - "Exact lookup by indicator type plus value ignores expired entries fail closed"
    - "In-memory, local-journal, and JetStream backends all implement the same threat-intel persistence contract"
  artifacts:
    - path: "crates/swarm-core/src/pheromone.rs"
      provides: "shared durable threat-intel record types"
      contains: "ThreatIntelEntry"
    - path: "crates/swarm-pheromone/src/substrate.rs"
      provides: "threat-intel store and lookup primitives for in-memory and local-journal backends"
      contains: "query_threat_intel_entry"
    - path: "crates/swarm-pheromone/src/jetstream.rs"
      provides: "JetStream threat-intel persistence and exact lookup"
      contains: "ThreatIntelEntry"
  key_links:
    - from: "crates/swarm-core/src/pheromone.rs"
      to: "crates/swarm-pheromone/src/substrate.rs"
      via: "shared threat-intel record contract"
      pattern: "ThreatIntelEntry"
    - from: "crates/swarm-pheromone/src/substrate.rs"
      to: "crates/swarm-pheromone/src/jetstream.rs"
      via: "aligned backend threat-intel persistence behavior"
      pattern: "store_threat_intel_entry"
---

<objective>
Add durable threat-intel entry storage and expiration-aware exact lookup primitives to the substrate contract across every current backend.

Purpose: Later detector enrichment needs substrate-owned operator intelligence, but phase 102 first has to establish the record shape and exact lookup behavior safely.

Output: `ThreatIntelEntry` types, substrate storage/query methods, and backend tests covering persistence plus fail-closed TTL handling.
</objective>
