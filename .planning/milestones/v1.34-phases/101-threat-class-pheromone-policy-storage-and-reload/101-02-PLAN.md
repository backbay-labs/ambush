---
phase: 101-threat-class-pheromone-policy-storage-and-reload
plan: 02
type: execute
wave: 1
depends_on: [101-01]
files_modified:
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/src/http/core.inc
  - crates/swarm-runtime/tests/escalation_integration.rs
  - crates/swarm-runtime/src/http/core.inc
autonomous: true
requirements: [SUBSTRATE-03]

must_haves:
  truths:
    - "Authenticated operator endpoints can list and upsert threat-class pheromone policy records"
    - "Operator-written threat-class policy becomes visible to the runtime without process restart"
    - "Verification covers backend persistence, runtime override resolution, and operator-surface behavior"
  artifacts:
    - path: "crates/swarm-runtime/src/control.rs"
      provides: "operator control helpers for threat-class pheromone policy"
      contains: "ThreatClassConfig"
    - path: "crates/swarm-runtime/src/http/core.inc"
      provides: "authenticated operator API routes for policy list and upsert"
      contains: "/v1/operator/pheromone"
    - path: "crates/swarm-runtime/tests/escalation_integration.rs"
      provides: "runtime proof that threat-class overrides affect live escalation behavior"
      contains: "ThreatClassConfig"
  key_links:
    - from: "crates/swarm-runtime/src/control.rs"
      to: "crates/swarm-runtime/src/http/core.inc"
      via: "operator-authenticated threat-class policy management"
      pattern: "store_threat_class_config"
    - from: "crates/swarm-runtime/src/http/core.inc"
      to: "crates/swarm-runtime/tests/escalation_integration.rs"
      via: "operator-managed substrate updates reflected in live runtime behavior"
      pattern: "query_threat_class_configs"
---

<objective>
Expose authenticated operator management for threat-class pheromone policy and prove that operator-written policy changes are honored by the runtime without restart.

Purpose: The milestone requirement is not met until operators can manage threat-class overrides through the existing API boundary and verification proves those writes affect live runtime behavior.

Output: operator policy list/upsert routes, control-plane helpers, and runtime/integration coverage showing operator-managed substrate state changes take effect immediately.
</objective>
