---
phase: 101-threat-class-pheromone-policy-storage-and-reload
plan: 01
type: execute
wave: 1
depends_on: [100-01, 100-02]
files_modified:
  - crates/swarm-core/src/pheromone.rs
  - crates/swarm-core/src/lib.rs
  - crates/swarm-pheromone/src/substrate.rs
  - crates/swarm-pheromone/src/jetstream.rs
  - crates/swarm-runtime/src/detection/pipeline.rs
  - crates/swarm-runtime/src/escalation.rs
  - crates/swarm-runtime/src/stalker_agent.rs
autonomous: true
requirements: [SUBSTRATE-03]

must_haves:
  truths:
    - "The substrate stores and queries durable `ThreatClassConfig` records across every backend"
    - "Live deposit paths resolve per-threat-class half-life overrides while preserving `PheromoneConfig` fallback behavior"
    - "Live escalation evaluation resolves per-threat-class evaporation, alert, and incident thresholds without breaking current global defaults"
  artifacts:
    - path: "crates/swarm-pheromone/src/substrate.rs"
      provides: "shared threat-class policy storage and resolution helpers for in-memory and local-journal backends"
      contains: "ThreatClassConfig"
    - path: "crates/swarm-pheromone/src/jetstream.rs"
      provides: "JetStream persistence and lookup for threat-class policy records"
      contains: "query_threat_class_config"
    - path: "crates/swarm-runtime/src/detection/pipeline.rs"
      provides: "runtime deposit path that resolves substrate-backed half-life overrides"
      contains: "detect_and_deposit"
  key_links:
    - from: "crates/swarm-core/src/pheromone.rs"
      to: "crates/swarm-pheromone/src/substrate.rs"
      via: "shared durable threat-class policy record"
      pattern: "ThreatClassConfig"
    - from: "crates/swarm-pheromone/src/substrate.rs"
      to: "crates/swarm-runtime/src/escalation.rs"
      via: "live concentration and threshold resolution against stored overrides"
      pattern: "query_threat_class_config"
---

<objective>
Add substrate-owned threat-class pheromone policy records and wire the live runtime to resolve those overrides during deposit and escalation work.

Purpose: Phase 101 needs one durable source of truth for per-threat-class pheromone behavior so later operator and threat-intel work can tune live runtime semantics without changing repo defaults.

Output: `ThreatClassConfig` records, backend persistence/query support, and runtime deposit/escalation paths that honor stored overrides while falling back cleanly to `PheromoneConfig`.
</objective>
