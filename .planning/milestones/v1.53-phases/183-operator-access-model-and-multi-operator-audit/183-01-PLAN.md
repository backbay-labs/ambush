---
phase: 183-operator-access-model-and-multi-operator-audit
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 182
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/http/core.inc
  - crates/swarm-runtime/src/ingest/platform_api.rs
  - docs/CONFIGURATION.md
  - deploy/helm/swarm-team-six/values-production.yaml
autonomous: true
requirements:
  - ACCESS-01
  - ACCESS-02
  - ACCESS-03
must_haves:
  truths:
    - "Multiple operators must authenticate as distinct principals with explicit read, rehearse, approve, or maintenance scope instead of sharing one runtime-wide bearer token."
    - "Operator-triggered approvals and related actions must be attributable end to end in the normal audit lane."
    - "The supported deployment profile must document how operator identities, secrets, and non-loopback access are meant to be wired."
  artifacts:
    - path: "crates/swarm-core/src/config.rs"
      provides: "Scoped multi-operator auth configuration surface."
    - path: "crates/swarm-runtime/src/http/core.inc"
      provides: "Operator-surface auth and attributable approval enforcement."
    - path: "crates/swarm-runtime/src/ingest/platform_api.rs"
      provides: "Scoped platform API auth alignment with the operator model."
    - path: "docs/CONFIGURATION.md"
      provides: "Supported operator access and deployment contract."
---

<objective>
Complete Phase 183 by turning the operator surface from one shared-secret local
shell into a supported multi-operator access model with explicit scopes,
attributable approvals, and a documented deployment pattern.

Purpose: close the last `v1.53` gap so the repo ships a real operator model
instead of saying production is supported while keeping operator access disabled.
Output: scoped operator auth, attributable approval lineage, and deployment
guidance for operators.
</objective>

<context>
@.planning/phases/183-operator-access-model-and-multi-operator-audit/183-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/http/core.inc
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/platform_api.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/docs/CONFIGURATION.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/deploy/helm/swarm-team-six/values-production.yaml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Expand the operator auth config to distinct scoped principals</name>
  <files>
    crates/swarm-core/src/config.rs
    deploy/helm/swarm-team-six/values-production.yaml
  </files>
  <action>Replace the single operator bearer-token shape with a repo-owned multi-principal model that can express scoped read, rehearse, approve, and maintenance access.</action>
  <done>Config can describe multiple operator principals and the supported profile documents how those secrets are mounted or referenced.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Enforce scoped permissions across operator and platform surfaces</name>
  <files>
    crates/swarm-runtime/src/http/core.inc
    crates/swarm-runtime/src/ingest/platform_api.rs
  </files>
  <action>Thread authenticated operator identity and scope through the runtime request context, then restrict read, rehearse, approve, and maintenance routes accordingly.</action>
  <done>Multiple operators can authenticate concurrently with least-privilege access over the shipped surfaces.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Make approvals and operator actions attributable</name>
  <files>
    crates/swarm-runtime/src/http/core.inc
    docs/CONFIGURATION.md
  </files>
  <action>Record the authenticated operator identity on approval and related operator actions, and document the supported reference architecture for operator adoption.</action>
  <done>Approval lineage and operator-triggered actions are attributable end to end, and the docs explain the supported deployment pattern.</done>
</task>

</tasks>

<verification>
Verify that distinct operator principals can authenticate with different scopes,
that forbidden actions fail closed, and that approval or maintenance outputs
retain attributable operator identity in the normal audit lane.
</verification>

