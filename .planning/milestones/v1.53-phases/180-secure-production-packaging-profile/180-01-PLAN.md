---
phase: 180-secure-production-packaging-profile
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 179
files_modified:
  - deploy/helm/swarm-team-six/values.yaml
  - deploy/helm/swarm-team-six/values-production.yaml
  - deploy/helm/swarm-team-six/templates/_helpers.tpl
  - deploy/helm/swarm-team-six/templates/deployment.yaml
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - PROD-01
must_haves:
  truths:
    - "The repo must ship one explicit supported production profile instead of implying that the bootstrap Helm values are production-ready."
    - "Runtime-owned durable paths must resolve into one declared state root instead of falling back to config-relative defaults."
    - "The packaged profile must make dependency, secret, TLS, and runtime boundaries clear enough for later recovery and access milestones."
  artifacts:
    - path: "deploy/helm/swarm-team-six/values-production.yaml"
      provides: "Supported secure production deployment profile."
    - path: "deploy/helm/swarm-team-six/templates/_helpers.tpl"
      provides: "Derived state-root, secret, and TLS config wiring for the packaged profile."
    - path: "deploy/helm/swarm-team-six/templates/deployment.yaml"
      provides: "Non-root, read-only, explicit-mount production pod hardening."
    - path: "docs/CONFIGURATION.md"
      provides: "Operator-readable production topology and state-root contract."
---

<objective>
Complete Phase 180 by shipping one secure production Helm profile with explicit runtime and dependency state boundaries.

Purpose: replace the current ad hoc bootstrap chart posture with a supported deployment contract that later recovery drills and operator-access work can treat as the baseline.
Output: hardened Helm packaging, a checked-in production values profile, and documentation for the supported topology and state roots.
</objective>

<context>
@.planning/phases/180-secure-production-packaging-profile/180-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/deploy/helm/swarm-team-six/values.yaml
@/Users/connor/Medica/backbay/standalone/swarm-team-six/deploy/helm/swarm-team-six/templates/_helpers.tpl
@/Users/connor/Medica/backbay/standalone/swarm-team-six/deploy/helm/swarm-team-six/templates/deployment.yaml
@/Users/connor/Medica/backbay/standalone/swarm-team-six/docs/CONFIGURATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Define the supported production packaging profile</name>
  <files>
    deploy/helm/swarm-team-six/values.yaml
    deploy/helm/swarm-team-six/values-production.yaml
  </files>
  <action>Add a repo-owned production values profile that turns on the supported dependency layout and hardening defaults instead of relying on the bootstrap values as production guidance.</action>
  <done>The chart ships one explicit profile operators can install as the supported production baseline.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Make runtime-owned state roots explicit in the rendered config</name>
  <files>
    deploy/helm/swarm-team-six/templates/_helpers.tpl
  </files>
  <action>Derive the runtime's durable local paths, identity roots, secret root, and optional TLS paths from the packaged state-root and mount layout.</action>
  <done>Rendered config no longer depends on config-relative defaults for production runtime state.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Harden the runtime pod and document the topology contract</name>
  <files>
    deploy/helm/swarm-team-six/templates/deployment.yaml
    docs/CONFIGURATION.md
  </files>
  <action>Add the pod-level hardening and mount points needed by the supported profile, then document the exact runtime, secret, TLS, and dependency boundaries for later milestones.</action>
  <done>The supported profile is secure by default and documented as the production baseline.</done>
</task>

</tasks>

<verification>
Render the chart with the production profile and validate the rendered config through the shipped CLI so the supported packaging path is both syntactically correct and accepted by runtime validation.
</verification>
