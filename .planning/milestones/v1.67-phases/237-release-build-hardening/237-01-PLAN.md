---
phase: 237-release-build-hardening
plan: 01
type: execute
wave: 1
depends_on:
  - 236-secret-zeroization-with-zeroize-crate
files_modified:
  - Cargo.toml
  - tools
  - crates/swarm-runtime/src/config.rs
  - .planning/phases/237-release-build-hardening
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - ZERO-02
must_haves:
  truths:
    - "Release hardening must be explicit at the workspace profile level rather than assumed from Cargo defaults."
    - "The shipped runtime binaries must compile in release mode with `panic = \"abort\"` and `overflow-checks = true`."
    - "The panic-strategy tradeoff against existing `catch_unwind` usage must be documented clearly in the phase artifacts."
  artifacts:
    - path: ".planning/phases/237-release-build-hardening"
      provides: "Context, plan, summary, and verification artifacts for Phase 237."
---

<objective>
Make the release profile explicit and security-oriented by enabling overflow
checks and forcing release panics to abort, then prove the shipped runtime
binaries build with that exact compiler configuration.
</objective>

<context>
@.planning/phases/237-release-build-hardening/237-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/Cargo.toml
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/bin/swarm_detect.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/bin/swarmctl.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add explicit release hardening defaults</name>
  <files>
    Cargo.toml
  </files>
  <action>Add a workspace-level release profile with explicit panic and overflow-check settings aligned to ZERO-02.</action>
  <done>The workspace no longer relies on implicit release defaults for panic strategy or overflow checking.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add a repo-owned release-profile proof</name>
  <files>
    tools
    crates/swarm-runtime/src/config.rs
  </files>
  <action>Add a small verification helper for the shipped runtime binaries and clean any incidental build noise encountered while producing the release proof.</action>
  <done>The repo has a repeatable command that proves the effective release cfg for `swarm_detect` and `swarmctl`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build-proof the hardened runtime binaries</name>
  <files>
    Cargo.toml
    tools
  </files>
  <action>Run release-mode verification and build commands to prove the shipped binaries still compile with the hardened profile.</action>
  <done>Release-profile hardening is both configured and verified on the shipped operator/runtime binaries.</done>
</task>

</tasks>

<verification>
Verify with release-profile inspection and release builds for the shipped
`swarm-runtime` binaries.
</verification>
