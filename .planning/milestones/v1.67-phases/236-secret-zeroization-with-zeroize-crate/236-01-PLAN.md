---
phase: 236-secret-zeroization-with-zeroize-crate
plan: 01
type: execute
wave: 1
depends_on:
  - v1.66
files_modified:
  - Cargo.toml
  - crates/swarm-core/src/config
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-runtime/src/http/core.inc
  - crates/swarm-runtime/src/ingest/platform_api.rs
  - crates/swarm-runtime/src/providence.rs
  - crates/swarm-response/src/http_edr.rs
  - crates/swarm-response/src/notification.rs
  - crates/swarm-response/src/siem.rs
  - crates/swarm-response/src/webhook.rs
  - .planning/phases/236-secret-zeroization-with-zeroize-crate
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - ZERO-01
must_haves:
  truths:
    - "Resolved plaintext secrets must stop flowing through raw heap `String` values where a shared zeroizing wrapper can be used instead."
    - "The runtime secret resolver must zeroize temporary file/env buffers after handing the secret into the long-lived holder."
    - "Outbound adapters plus operator/platform auth must keep working after the secret wrapper conversion."
  artifacts:
    - path: ".planning/phases/236-secret-zeroization-with-zeroize-crate"
      provides: "Context, plan, summary, and verification artifacts for Phase 236."
---

<objective>
Add a shared zeroizing secret wrapper and route resolved outbound and auth
secrets through it so plaintext is scrubbed on drop instead of being carried as
raw `String` values.
</objective>

<context>
@.planning/phases/236-secret-zeroization-with-zeroize-crate/236-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/http/core.inc
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/platform_api.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add the shared zeroizing secret wrapper</name>
  <files>
    Cargo.toml
    crates/swarm-core/src/config
  </files>
  <action>Introduce a serde-transparent `SecretString` type backed by `zeroize` and convert the shared secret-bearing config fields to use it.</action>
  <done>Secret-bearing config no longer exposes raw plaintext `String` storage for outbound auth tokens or request-signature secrets.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Zeroize runtime resolution and auth-state seams</name>
  <files>
    crates/swarm-runtime/src/config.rs
    crates/swarm-runtime/src/http/core.inc
    crates/swarm-runtime/src/ingest/platform_api.rs
  </files>
  <action>Zeroize temporary resolution buffers and store operator/platform expected tokens with the shared wrapper instead of raw strings.</action>
  <done>`@secret:` resolution, operator auth, and platform bearer/context-token state all use zeroizing secret holders.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Preserve outbound adapter behavior and prove it</name>
  <files>
    crates/swarm-response/src/http_edr.rs
    crates/swarm-response/src/webhook.rs
    crates/swarm-response/src/siem.rs
    crates/swarm-response/src/notification.rs
    crates/swarm-runtime/src/providence.rs
  </files>
  <action>Update secret consumers to borrow from the wrapper cleanly and add focused tests for zeroization plus resolved secret reload behavior.</action>
  <done>The adapters still compile and tests prove the shared secret wrapper plus runtime resolution seams work as intended.</done>
</task>

</tasks>

<verification>
Verify with targeted config and auth tests plus crate checks for
`swarm-core`, `swarm-response`, and `swarm-runtime`.
</verification>
