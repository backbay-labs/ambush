---
phase: 238-rotatable-token-lifecycle
plan: 01
type: execute
wave: 1
depends_on:
  - 237-release-build-hardening
files_modified:
  - crates/swarm-core/src/config/operator.rs
  - crates/swarm-core/src/config/validation.rs
  - crates/swarm-core/src/config/tests.rs
  - crates/swarm-runtime/src/http/core.inc
  - crates/swarm-runtime/src/ingest/platform_api.rs
  - crates/swarm-runtime/src/service/types.rs
  - crates/swarm-runtime/src/service/runtime_service.rs
  - crates/swarm-runtime/src/ingest/tests.rs
  - crates/swarm-cli/Cargo.toml
  - .planning/phases/238-rotatable-token-lifecycle
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - TOKEN-01
must_haves:
  truths:
    - "Token expiry metadata must live in repo-owned config rather than hidden runtime state."
    - "Operator and platform bearer auth must observe rotated env-backed tokens without process restart."
    - "Expired tokens must fail closed with clear request or status context."
  artifacts:
    - path: ".planning/phases/238-rotatable-token-lifecycle"
      provides: "Context, plan, summary, and verification artifacts for Phase 238."
---

<objective>
Make bearer-token lifecycle explicit and restart-free by carrying expiry metadata
through config, re-reading env-backed bearer material on each protected request,
and surfacing expired-token context in the shipped status responses.
</objective>

<context>
@.planning/phases/238-rotatable-token-lifecycle/238-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config/operator.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/http/core.inc
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/platform_api.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend config with token lifecycle metadata</name>
  <files>
    crates/swarm-core/src/config/operator.rs
    crates/swarm-core/src/config/validation.rs
    crates/swarm-core/src/config/tests.rs
  </files>
  <action>Add optional expiry metadata to operator principals and validate the new fields.</action>
  <done>Repo-owned config can express bearer-token expiry without breaking the existing auth contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Make operator and platform bearer auth restart-free</name>
  <files>
    crates/swarm-runtime/src/http/core.inc
    crates/swarm-runtime/src/ingest/platform_api.rs
  </files>
  <action>Replace startup-snapshotted bearer plaintext with per-request env reads for operator and platform bearer auth.</action>
  <done>Rotated env-backed tokens are observed on the next protected request and expired tokens fail closed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Surface lifecycle status and prove behavior</name>
  <files>
    crates/swarm-runtime/src/service/types.rs
    crates/swarm-runtime/src/service/runtime_service.rs
    crates/swarm-runtime/src/ingest/tests.rs
    crates/swarm-cli/Cargo.toml
  </files>
  <action>Add operator-visible status context for bearer lifecycle and verify rotation plus expiry behavior through focused tests and build checks.</action>
  <done>Status surfaces expose bearer expiry metadata and focused tests prove rotation, expiry rejection, and auth regressions remain fail closed.</done>
</task>

</tasks>

<verification>
Verify with focused config-validation, operator-surface, and platform-surface
tests covering expiry validation, live token rotation, expired-token rejection,
and the unchanged protected-route auth contract.
</verification>
