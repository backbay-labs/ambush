---
phase: 239-http-rate-limiting
plan: 01
type: execute
wave: 1
depends_on:
  - 238-rotatable-token-lifecycle
files_modified:
  - crates/swarm-core/src/config/defaults.rs
  - crates/swarm-core/src/config/operator.rs
  - crates/swarm-core/src/config/validation.rs
  - crates/swarm-core/src/config/tests.rs
  - crates/swarm-runtime/src/http/mod.rs
  - crates/swarm-runtime/src/http/rate_limit.rs
  - crates/swarm-runtime/src/http/core.inc
  - crates/swarm-runtime/src/ingest/mod.rs
  - crates/swarm-runtime/src/ingest/platform_api.rs
  - crates/swarm-runtime/src/ingest/tests.rs
  - crates/swarm-runtime/src/service/types.rs
  - crates/swarm-runtime/src/service/runtime_service.rs
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/src/providence.rs
  - crates/swarm-cli/src/core.inc
  - .planning/phases/239-http-rate-limiting
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - TOKEN-02
must_haves:
  truths:
    - "Operator and platform surfaces need the same limiter semantics but independent repo-owned thresholds."
    - "Rate-limit violations must fail closed with 429 semantics and bounded retry guidance."
    - "Recent violations must be visible through shipped status surfaces, not just logs."
  artifacts:
    - path: ".planning/phases/239-http-rate-limiting"
      provides: "Context, plan, summary, and verification artifacts for Phase 239."
---

<objective>
Enforce repo-owned per-source burst and sustained request limits on the shipped
operator and platform HTTP surfaces, surface recent violations through runtime
status, and prove the protected request paths allow burst traffic, reject
sustained abuse, and recover after refill.
</objective>

<context>
@.planning/phases/239-http-rate-limiting/239-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config/operator.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/http/core.inc
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/platform_api.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add repo-owned HTTP rate-limit config</name>
  <files>
    crates/swarm-core/src/config/defaults.rs
    crates/swarm-core/src/config/operator.rs
    crates/swarm-core/src/config/validation.rs
    crates/swarm-core/src/config/tests.rs
    crates/swarm-cli/src/core.inc
  </files>
  <action>Add shared burst/sustained rate-limit config for operator and platform surfaces, validate it, and expose it in generated config templates.</action>
  <done>Repo-owned config can express rate limits for both protected HTTP surfaces and rejects nonsensical thresholds.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Enforce one reusable limiter across operator and platform routes</name>
  <files>
    crates/swarm-runtime/src/http/mod.rs
    crates/swarm-runtime/src/http/rate_limit.rs
    crates/swarm-runtime/src/http/core.inc
    crates/swarm-runtime/src/ingest/mod.rs
    crates/swarm-runtime/src/ingest/platform_api.rs
  </files>
  <action>Implement a bounded in-memory per-source limiter and wire it into the protected middleware seams with `429` plus `Retry-After` behavior.</action>
  <done>Operator and platform routes reject burst or sustained abuse using the same limiter semantics while keeping surface-specific config.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Surface audit context and prove behavior</name>
  <files>
    crates/swarm-runtime/src/service/types.rs
    crates/swarm-runtime/src/service/runtime_service.rs
    crates/swarm-runtime/src/control.rs
    crates/swarm-runtime/src/ingest/tests.rs
    crates/swarm-runtime/src/providence.rs
  </files>
  <action>Expose recent limiter violations through shipped status payloads and add focused route tests for burst allowance, sustained rejection, and refill recovery.</action>
  <done>Operators can see recent HTTP limiter violations in status output and tests prove the protected request paths enforce the intended behavior.</done>
</task>

</tasks>

<verification>
Verify with config-validation tests, focused operator/platform route tests, and
an isolated workspace `cargo check` covering the new config and runtime seams.
</verification>
