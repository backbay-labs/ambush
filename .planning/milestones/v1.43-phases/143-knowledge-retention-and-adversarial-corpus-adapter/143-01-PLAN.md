---
phase: 143-knowledge-retention-and-adversarial-corpus-adapter
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 142
files_modified:
  - crates/swarm-core/src/config.rs
  - rulesets/default.yaml
  - crates/swarm-runtime/src/sphinx_agent.rs
  - crates/swarm-runtime/src/red_swarm.rs
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/src/replay/core.inc
autonomous: true
requirements:
  - SPHINX-05
  - HELLCAT-01
must_haves:
  truths:
    - "Phase 142 already proved the memory query loop, so Phase 143 should bound the graph and add corpus generation without dragging Kitten fitness into the same patch."
    - "The repo already owns adversarial suite manifests, so the new adapter should reuse that layer rather than inventing a second scenario format."
    - "Retention and adversarial generation must both stay deterministic and Rust-native."
  artifacts:
    - path: "crates/swarm-core/src/config.rs"
      provides: "Repo-owned retention and adapter configuration."
    - path: "crates/swarm-runtime/src/sphinx_agent.rs"
      provides: "TTL-style garbage collection over the durable knowledge graph."
    - path: "crates/swarm-runtime/src/red_swarm.rs"
      provides: "Rust-native adversarial sequence adapter plus mock implementation."
  key_links:
    - from: "crates/swarm-runtime/src/sphinx_agent.rs"
      to: "crates/swarm-core/src/config.rs"
      via: "knowledge_retention_days"
      pattern: "bounded durable memory"
    - from: "crates/swarm-runtime/src/red_swarm.rs"
      to: "crates/swarm-runtime/src/replay/core.inc"
      via: "scenario-suites manifest reuse"
      pattern: "Rust-native adversarial corpus generation"
---

<objective>
Complete Phase 143 by adding retention-safe Sphinx graph garbage collection and a Rust-native adversarial corpus adapter that can later feed the evolution loop.

Purpose: keep durable memory bounded and establish the red-side corpus seam before Phase 144 integrates adversarial pressure into Kitten fitness and episode logging.
Output: memory retention config plus GC, `RedSwarmAdapter` and `MockRedSwarm`, and focused tests proving deterministic suite-driven sequence generation.
</objective>

<context>
@.planning/phases/143-knowledge-retention-and-adversarial-corpus-adapter/143-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/sphinx_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/replay/core.inc
@/Users/connor/Medica/backbay/standalone/swarm-team-six/scenario-suites/hellcat-office-v1.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add retention-safe knowledge-graph garbage collection</name>
  <read_first>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/sphinx_agent.rs
  </read_first>
  <files>
    crates/swarm-core/src/config.rs
    rulesets/default.yaml
    crates/swarm-runtime/src/sphinx_agent.rs
  </files>
  <action>Add `knowledge_retention_days` to the repo-owned memory config and make Sphinx prune stale nodes, edges, and processed-observation metadata on tick using the same fail-safe durability model as the rest of the graph store.</action>
  <verify>
    <automated>cargo test -p swarm-core config::tests::memory_requires_positive_retention_days_when_enabled -- --exact</automated>
    <automated>cargo test -p swarm-runtime sphinx_agent::tests:: -- --nocapture</automated>
  </verify>
  <done>The graph stays bounded by configurable TTL and restart-safe persistence still works after GC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define a Rust-native adversarial corpus adapter</name>
  <read_first>
    crates/swarm-runtime/src/replay/core.inc
    scenario-suites/hellcat-office-v1.yaml
  </read_first>
  <files>
    crates/swarm-runtime/src/red_swarm.rs
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/src/replay/core.inc
  </files>
  <action>Create a `RedSwarmAdapter` trait plus a deterministic default implementation that reads repo-owned suite manifests and produces parameterized adversarial telemetry sequences without invoking Python, alongside a `MockRedSwarm` fixture adapter for tests.</action>
  <verify>
    <automated>cargo test -p swarm-runtime red_swarm::tests:: -- --nocapture</automated>
  </verify>
  <done>The runtime owns one deterministic adversarial-sequence seam that later phases can call directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove suite-driven generation stays deterministic and Rust-native</name>
  <read_first>
    crates/swarm-runtime/src/red_swarm.rs
    crates/swarm-runtime/src/replay/core.inc
  </read_first>
  <files>
    crates/swarm-runtime/src/red_swarm.rs
    scenario-suites/hellcat-office-v1.yaml
  </files>
  <action>Add focused tests showing the default adapter can materialize a stable adversarial sequence from `scenario-suites/hellcat-office-v1.yaml` and that `MockRedSwarm` can inject static deterministic sequences for later fitness integration work.</action>
  <verify>
    <automated>cargo check -p swarm-runtime --tests -j 1 --message-format short</automated>
  </verify>
  <done>The repo has a verified Rust-native adversarial corpus adapter and a deterministic test seam for future red-blue evaluation phases.</done>
</task>

</tasks>

<verification>
Land the retention config and GC first, then add the adapter surface, then prove deterministic suite-driven generation plus mockability. Keep the phase bounded: memory retention and corpus generation only, no phase-144 fitness or episode wiring here.
</verification>
