---
phase: 142-stigmergic-memory-queries-and-fitness-retrieval
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 141
files_modified:
  - crates/swarm-runtime/src/sphinx_agent.rs
  - crates/swarm-runtime/src/kitten_agent.rs
  - crates/swarm-runtime/src/dispatcher.rs
  - crates/swarm-core/src/types.rs
autonomous: true
requirements:
  - SPHINX-03
  - SPHINX-04
must_haves:
  truths:
    - "Phase 141 already established the durable graph, so Phase 142 should add read semantics and scoring reuse rather than changing the graph foundation again."
    - "Indirect query and answer pheromones must stay auditable through the existing substrate-driven interaction model."
    - "Kitten must keep replay-only fitness as a deterministic fallback whenever memory retrieval is unavailable or too sparse."
  artifacts:
    - path: "crates/swarm-runtime/src/sphinx_agent.rs"
      provides: "Query and answer pheromone handling over the durable graph."
    - path: "crates/swarm-runtime/src/kitten_agent.rs"
      provides: "Memory-aware Q-value retrieval and fallback fitness integration."
    - path: "crates/swarm-core/src/types.rs"
      provides: "Any shared pheromone payload contracts required for auditable query and answer semantics."
  key_links:
    - from: "crates/swarm-runtime/src/kitten_agent.rs"
      to: "crates/swarm-runtime/src/sphinx_agent.rs"
      via: "query and answer pheromone deposits"
      pattern: "stigmergic memory retrieval"
    - from: "crates/swarm-runtime/src/kitten_agent.rs"
      to: "crates/swarm-evolution/src/strategy.rs"
      via: "Q-value scoring and replay fallback composition"
      pattern: "fitness integration"
---

<objective>
Complete Phase 142 by making Sphinx memory queryable through the pheromone substrate and by letting Kitten incorporate memory-backed retrieval into candidate fitness without losing replay-only fallback behavior.

Purpose: turn the Phase 141 graph foundation into a real swarm-usable memory surface.
Output: auditable query and answer pheromone semantics, Sphinx query handling, Kitten retrieval integration, and focused tests proving the indirect memory loop works.
</objective>

<context>
@.planning/phases/142-stigmergic-memory-queries-and-fitness-retrieval/142-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/sphinx_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/kitten_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/dispatcher.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/strategy.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define query and answer pheromone semantics for Sphinx memory</name>
  <read_first>
    crates/swarm-core/src/types.rs
    crates/swarm-runtime/src/sphinx_agent.rs
  </read_first>
  <files>
    crates/swarm-core/src/types.rs
    crates/swarm-runtime/src/sphinx_agent.rs
  </files>
  <action>Define an auditable query and answer payload contract that lets agents ask Sphinx for memory-backed context through the existing pheromone substrate rather than a direct request path.</action>
  <verify>
    <automated>cargo test -p swarm-runtime sphinx_agent::tests:: -- --nocapture</automated>
  </verify>
  <done>Sphinx can recognize query pheromones, resolve them against the durable graph, and emit answer pheromones with stable payloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate memory-backed Q-value retrieval into Kitten fitness</name>
  <read_first>
    crates/swarm-runtime/src/kitten_agent.rs
    crates/swarm-evolution/src/strategy.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/kitten_agent.rs
    crates/swarm-runtime/src/sphinx_agent.rs
  </files>
  <action>Teach Kitten to request memory context through the new pheromone contract, compute a Q-value style retrieval contribution from Sphinx answers, and fall back cleanly to replay-only fitness when memory is unavailable.</action>
  <verify>
    <automated>cargo test -p swarm-runtime kitten_agent::tests:: -- --nocapture</automated>
  </verify>
  <done>Kitten can enrich candidate fitness with Sphinx-backed memory retrieval without introducing a direct memory RPC dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove indirect memory interaction and fallback behavior</name>
  <read_first>
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/sphinx_agent.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/sphinx_agent.rs
    crates/swarm-runtime/src/kitten_agent.rs
  </files>
  <action>Add focused tests showing query and answer pheromones flow through the dispatcher-owned runtime model and that Kitten still behaves deterministically when Sphinx is disabled or sparse.</action>
  <verify>
    <automated>cargo check -p swarm-runtime --tests -j 1 --message-format short</automated>
  </verify>
  <done>The indirect memory loop is proven without breaking the existing substrate-driven swarm interaction model.</done>
</task>

</tasks>

<verification>
Land the query contract first, then integrate Kitten retrieval, then verify indirect query/answer flow plus replay-only fallback. Keep the new memory semantics auditable and aligned with the existing pheromone-driven runtime.
</verification>
