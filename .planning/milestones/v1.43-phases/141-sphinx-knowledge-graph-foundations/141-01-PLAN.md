---
phase: 141-sphinx-knowledge-graph-foundations
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 140
files_modified:
  - crates/swarm-core/src/config.rs
  - rulesets/default.yaml
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
  - crates/swarm-runtime/src/dispatcher.rs
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime/src/sphinx_agent.rs
autonomous: true
requirements:
  - SPHINX-01
  - SPHINX-02
must_haves:
  truths:
    - "`AgentRole::Sphinx` already exists, so Phase 141 should land a real runtime-owned agent rather than inventing a parallel role system."
    - "The knowledge graph must be a durable typed store with explicit node and edge schemas; the pheromone substrate remains the short-lived coordination layer, not the memory database."
    - "Phase 141 owns graph foundations and lifecycle wiring only; query pheromones and Kitten memory retrieval are Phase 142 work."
  artifacts:
    - path: "crates/swarm-runtime/src/sphinx_agent.rs"
      provides: "Runtime-owned Sphinx agent implementation and durable graph lifecycle."
    - path: "crates/swarm-core/src/config.rs"
      provides: "Repo-owned knowledge-graph configuration surface."
    - path: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      provides: "Serve-mode registration for Sphinx."
  key_links:
    - from: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      to: "crates/swarm-runtime/src/sphinx_agent.rs"
      via: "serve-mode dispatcher registration"
      pattern: "AgentRole::Sphinx"
    - from: "crates/swarm-runtime/src/sphinx_agent.rs"
      to: "crates/swarm-evolution/src/strategy.rs"
      via: "durable file-backed memory patterns reused for graph storage"
      pattern: "strategy memory"
---

<objective>
Complete Phase 141 by landing the durable memory substrate and the first runtime-owned Sphinx agent.

Purpose: give the swarm persistent typed memory before adding pheromone-query access and evolution-time retrieval in later phases.
Output: repo-owned memory config, a typed file-backed knowledge graph store, a runtime-owned `SphinxAgent`, serve-mode registration, and focused tests for persistence and lifecycle behavior.
</objective>

<context>
@.planning/phases/141-sphinx-knowledge-graph-foundations/141-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/dispatcher.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/bin/swarm_detect.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/service.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/strategy.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repo-owned memory configuration and typed graph schema</name>
  <read_first>
    crates/swarm-core/src/config.rs
    rulesets/default.yaml
  </read_first>
  <files>
    crates/swarm-core/src/config.rs
    rulesets/default.yaml
  </files>
  <action>Add a minimal repo-owned config surface for Sphinx and define the typed node, edge, and persistence schema needed for a durable knowledge graph store.</action>
  <verify>
    <automated>cargo test -p swarm-core config::tests -- --exact</automated>
  </verify>
  <done>The repo can load and validate Sphinx memory settings and typed graph schema without hidden defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `FileKnowledgeGraphStore` and `SphinxAgent` lifecycle</name>
  <read_first>
    crates/swarm-core/src/agent.rs
    crates/swarm-evolution/src/strategy.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/sphinx_agent.rs
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/src/service.rs
  </files>
  <action>Implement a runtime-owned Sphinx agent that loads, persists, and updates a file-backed typed knowledge graph without relying on the short-lived pheromone substrate as the backing store.</action>
  <verify>
    <automated>cargo test -p swarm-runtime sphinx_agent::tests -- --exact</automated>
  </verify>
  <done>Sphinx can boot with durable graph state, preserve updates, and survive restart with the same typed knowledge model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register Sphinx in serve mode and prove startup wiring</name>
  <read_first>
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-runtime/src/dispatcher.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-runtime/src/dispatcher.rs
  </files>
  <action>Register Sphinx in serve mode when memory is enabled, preserve the current dispatcher behavior, and add focused tests proving the new agent starts cleanly with the rest of the runtime.</action>
  <verify>
    <automated>cargo test -p swarm-runtime dispatcher::tests -- --exact</automated>
    <automated>cargo check -p swarm-runtime --tests -j 1 --message-format short</automated>
  </verify>
  <done>The runtime can boot with Sphinx enabled and expose the new memory substrate without regressing the current agent registry.</done>
</task>

</tasks>

<verification>
Land the config and graph schema first, then implement the durable store and Sphinx agent, then wire serve-mode startup. Finish with focused runtime verification because Phase 141 crosses config, persistence, and the agent registry.
</verification>
