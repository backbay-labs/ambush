---
phase: 184-runtime-unwrap-audit-and-error-types
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 183
files_modified:
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime/src/serve.rs
  - crates/swarm-runtime/src/ingest/mod.rs
  - crates/swarm-runtime/src/http/core.inc
autonomous: true
requirements:
  - PANIC-01
  - PANIC-02
must_haves:
  truths:
    - "Non-test unwrap and expect sites in `swarm-runtime` must be inventoried by module and runtime path instead of remaining implicit debt."
    - "Typed error boundaries must be explicit at the runtime module seams that still rely on panic-driven or string-only failure handling."
    - "Deferred cleanup for later phases must be recorded clearly rather than hidden behind a partial first-pass audit."
  artifacts:
    - path: "crates/swarm-runtime/src/lib.rs"
      provides: "Top-level runtime error boundary and audit anchor."
    - path: "crates/swarm-runtime/src/service.rs"
      provides: "Service-layer typed error boundary inventory."
    - path: "crates/swarm-runtime/src/serve.rs"
      provides: "Serve/bootstrap panic-site inventory and boundary contract."
    - path: "crates/swarm-runtime/src/ingest/mod.rs"
      provides: "Ingest build and request-path error seam inventory."
    - path: "crates/swarm-runtime/src/http/core.inc"
      provides: "Operator HTTP boundary inventory for follow-on panic-free conversion."
---

<objective>
Complete Phase 184 by producing a repo-owned audit of non-test panic sites in
`swarm-runtime` and normalizing the typed error seams that later phases will use
to remove those sites safely.

Purpose: establish a credible panic-eradication map before making broad runtime
edits.
Output: audited panic-site inventory, normalized error-boundary definitions, and
clear follow-on conversion targets for phases 185 and 186.
</objective>

<context>
@.planning/phases/184-runtime-unwrap-audit-and-error-types/184-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/service.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/serve.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/mod.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/http/core.inc
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit live unwrap and expect sites in swarm-runtime</name>
  <files>
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/serve.rs
    crates/swarm-runtime/src/ingest/mod.rs
    crates/swarm-runtime/src/http/core.inc
  </files>
  <action>Inventory non-test panic sites, classify them by module and runtime path, and separate inline-test debt from live code that matters for the milestone.</action>
  <done>The repo has an explicit map of non-test unwrap/expect usage in the runtime entrypoints and shared module seams.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Normalize typed error boundaries for the first runtime seams</name>
  <files>
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/ingest/mod.rs
    crates/swarm-runtime/src/http/core.inc
  </files>
  <action>Define or refine typed error enums and conversions at the runtime seams that still depend on panic-driven or string-only behavior.</action>
  <done>The first tranche of runtime module boundaries uses explicit typed errors suitable for follow-on conversion work.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record the remaining conversion map for phases 185 and 186</name>
  <files>
    crates/swarm-runtime/src/serve.rs
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/src/http/core.inc
  </files>
  <action>Document which panic sites remain after the boundary pass, why they remain, and which later phase owns each cluster of cleanup work.</action>
  <done>Phase 185 and 186 can execute against an explicit, repo-owned panic-eradication map instead of rediscovering the same debt.</done>
</task>

</tasks>

<verification>
Verify that the runtime panic-site inventory covers the live entrypoints and
shared seams, and that the typed error boundary work compiles cleanly so later
phases can convert panic sites without reopening the audit.
</verification>
