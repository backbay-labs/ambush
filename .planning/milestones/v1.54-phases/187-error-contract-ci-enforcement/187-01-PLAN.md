---
phase: 187-error-contract-ci-enforcement
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 186
files_modified:
  - .github/workflows/ci.yml
  - tools/check-runtime-panic-contract.sh
  - crates/swarm-runtime/tests/ingest_integration.rs
  - crates/swarm-runtime/tests/critical_path_integration.rs
autonomous: true
requirements:
  - PANIC-04
must_haves:
  truths:
    - "CI must fail when new non-test `unwrap()` or `expect()` sites appear in `swarm-runtime` without an explicit allowed pattern."
    - "The repo must prove representative malformed-input runtime paths return errors instead of panicking."
    - "The allowed exception pattern, if any, must be explicit and auditable instead of implicit reviewer knowledge."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Repo-owned CI enforcement for the runtime panic contract."
    - path: "tools/check-runtime-panic-contract.sh"
      provides: "Deterministic scan for non-test runtime `unwrap()` and `expect()` debt."
    - path: "crates/swarm-runtime/tests/ingest_integration.rs"
      provides: "Integration proof that malformed runtime ingress stays non-panicking."
---

<objective>
Complete Phase 187 by making the runtime panic-eradication contract enforceable
in CI and by adding representative integration proof that malformed input stays
fail-closed.

Purpose: close `v1.54` with a durable repo rule instead of a one-time cleanup.
Output: CI panic-contract gate, documented allowed exception pattern, and
integration coverage for non-panicking malformed-input behavior.
</objective>

<context>
@.planning/phases/187-error-contract-ci-enforcement/187-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.github/workflows/ci.yml
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/tests/ingest_integration.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/tests/critical_path_integration.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add a repo-owned runtime panic-contract checker</name>
  <files>
    .github/workflows/ci.yml
    tools/check-runtime-panic-contract.sh
  </files>
  <action>Add a deterministic scan over non-test `swarm-runtime` sources that fails on unjustified `unwrap()` and `expect()` use, then wire it into CI.</action>
  <done>Pull requests cannot introduce new unjustified runtime panic sites without failing the shared CI workflow.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Prove malformed runtime paths stay non-panicking in integration tests</name>
  <files>
    crates/swarm-runtime/tests/ingest_integration.rs
    crates/swarm-runtime/tests/critical_path_integration.rs
  </files>
  <action>Add representative malformed-input or failing-runtime integration assertions that return errors or rejected envelopes instead of crashing the process.</action>
  <done>Representative runtime integration paths prove the panic-free contract at a broader scope than the phase-local unit tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document the allowed exception rule and close the milestone</name>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
  </files>
  <action>Record the enforcement rule, any permitted exception pattern, and the completed requirement mapping so `v1.54` can be audited and closed cleanly.</action>
  <done>The repo can explain how the panic contract is enforced and Phase 187 can close `PANIC-04` without ambiguity.</done>
</task>

</tasks>

<verification>
Verify that CI now enforces the runtime panic contract, that representative
malformed-input integration tests pass without panic, and that the milestone
documentation reflects the new enforcement rule.
</verification>
