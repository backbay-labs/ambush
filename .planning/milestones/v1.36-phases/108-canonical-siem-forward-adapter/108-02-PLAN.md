---
phase: 108-canonical-siem-forward-adapter
plan: 02
type: execute
wave: 1
depends_on: [108-01]
files_modified:
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/tests/dispatch_integration.rs
autonomous: true
requirements: [SIEM-01]

must_haves:
  truths:
    - "The live runtime can forward findings through the configured SIEM adapter without requiring a response action"
    - "Forwarding uses the canonical `swarm_finding` payload across all transport variants"
    - "Focused tests prove runtime-driven SIEM forwarding and resilient adapter wiring"
  artifacts:
    - path: "crates/swarm-runtime/src/service.rs"
      provides: "runtime-owned SIEM forwarder integration"
      contains: "forward_finding"
  key_links:
    - from: "crates/swarm-runtime/src/service.rs"
      to: "crates/swarm-response/src/siem.rs"
      via: "runtime finding delivery uses the adapter contract"
      pattern: "SiemForwardAdapter"
---

<objective>
Wire the SIEM forward adapter into the live detection lane and prove that canonical finding delivery happens from real runtime processing.

Purpose: A shipping adapter is not enough if the runtime never emits findings into it.

Output: runtime SIEM integration plus focused tests proving canonical finding forwarding from the hot path.
</objective>
