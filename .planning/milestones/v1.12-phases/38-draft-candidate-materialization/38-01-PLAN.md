# Plan 38-01

1. Add durable materialization report and store types to the drafting module.
2. Implement draft materialization from one base experiment plus explicit profile overrides.
3. Add `swarmctl` commands to create and reload materialization artifacts by stable ID.
4. Cover materialization persistence and render output with runtime tests.
