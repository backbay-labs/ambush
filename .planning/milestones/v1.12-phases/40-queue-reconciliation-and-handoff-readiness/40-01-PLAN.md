# Plan 40-01

1. Add durable queue-reconciliation report and store types to the drafting module.
2. Implement in-place reconciliation that replaces the placeholder draft block on the original queue proposal with refreshed evidence state.
3. Add `swarmctl` commands to reconcile and reload reconciliation artifacts by stable ID.
4. Verify the accepted reconciled proposal can feed the existing handoff path without duplicate rollout state.
