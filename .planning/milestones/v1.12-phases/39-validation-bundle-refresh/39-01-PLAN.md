# Plan 39-01

1. Add durable validation-bundle report and store types to the drafting module.
2. Implement one refresh flow that evaluates experiment, verification, proof, shadow, and advisory scorecard artifacts from a materialized candidate.
3. Make validation fail closed on manifest drift or inconsistent refreshed evidence while still persisting blocked bundles.
4. Add `swarmctl` commands to create and reload validation bundles by stable ID.
