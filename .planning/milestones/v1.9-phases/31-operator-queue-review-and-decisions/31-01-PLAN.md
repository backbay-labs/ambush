# Plan 31-01

1. Add `swarmctl` commands for proof creation, proposal creation, queue list, queue result, and operator decisions.
2. Persist decision history and enforce a simple review-state machine that never mutates live rollout state.
3. Document the proof and queue workflow plus artifact directories in `docs/CONFIGURATION.md`.
4. Verify the CLI-backed queue flow through unit tests and end-to-end command checks.
