---
milestone: v1.25
audited: 2026-04-05T03:06:02Z
status: passed
scores:
  requirements: 5/5
  phases: 3/3
  integration: 5/5
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - swarm-detect still processes repo-owned scenario fixtures rather than a live telemetry ingress
  - strict clippy enforcement relies on scoped test and example allowances instead of rewriting every assertion-heavy test module
nyquist:
  compliant_phases: []
  partial_phases: []
  missing_phases:
    - 78-service-extraction-and-detection-binary
    - 79-metrics-and-integration-tests
    - 80-clippy-enforcement
  overall: missing
---

# Milestone v1.25 Audit

## Summary

`v1.25 Operational Hardening And Service Extraction` achieved its definition of done. The repo now supports a standalone `swarm-detect` hot-path binary, shared ruleset and scenario loading, Prometheus critical-path metrics, end-to-end integration coverage for detect-to-receipt flows, and workspace-wide denial of production `unwrap`/`expect` usage.

## Requirements Coverage

| Requirement | Phase | Verification | Final Status | Evidence |
|-------------|-------|--------------|--------------|----------|
| OPS-26 | 78 | passed | satisfied | `swarm-detect` now builds and runs independently of the operator workbench CLI. |
| OPS-27 | 78 | passed | satisfied | Rulesets and scenario fixtures are loaded through shared runtime config and replay helpers instead of only through `swarmctl`. |
| OPS-28 | 79 | passed | satisfied | RuntimeService now records Prometheus histogram metrics for detection, policy, and response stages and exposes them via `/metrics`. |
| OPS-29 | 79 | passed | satisfied | Integration tests now cover detect-to-receipt happy path, benign no-op, scenario-fixture replay, and policy-deny behavior in `cargo test --workspace`. |
| OPS-30 | 80 | passed | satisfied | Workspace clippy denial for `unwrap_used` and `expect_used` is active across all crates and enforced by the existing CI clippy step. |

## Phase Verification

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 78 Service Extraction And Detection Binary | passed | 5/5 | Standalone binary, shared detector/scenario loading, runtime-mode handling, and per-event reporting all landed. |
| 79 Metrics And Integration Tests | passed | 4/4 | Prometheus metrics, `/metrics` exposure, and critical-path integration coverage are all in place. |
| 80 Clippy Enforcement | passed | 4/4 | Workspace lint denial, crate lint inheritance, runtime refactors, and full validation all passed. |

## Cross-Phase Integration

| Flow | Status | Evidence |
|------|--------|----------|
| shared detector factory and scenario loading -> standalone `swarm-detect` service binary | ✓ VERIFIED | Phase 78 exposed the detector/scenario helpers and the new binary now consumes them directly. |
| runtime hot path -> Prometheus registry -> `/metrics` operator endpoint | ✓ VERIFIED | Phase 79 attached Prometheus observers in `RuntimeService` and exposed the shared registry in `operator_http.rs`. |
| scenario fixtures -> standalone binary -> replayable per-event summaries | ✓ VERIFIED | `swarm-detect` processes `scenarios/*.yaml` and reports findings, deposits, policy verdicts, and response kinds. |
| public runtime API -> critical path integration tests -> workspace test suite | ✓ VERIFIED | `critical_path_integration.rs` uses public runtime APIs and runs under `cargo test --workspace`. |
| workspace clippy policy -> runtime refactors -> CI validation path | ✓ VERIFIED | Phase 80 cleaned the remaining production violations and `.github/workflows/ci.yml` continues to run the clippy/build/test gates. |

## End-to-End Flows

| Flow | Status | Evidence |
|------|--------|----------|
| run `swarm-detect` against repo-owned rulesets and scenario fixtures | ✓ VERIFIED | `cargo run -p swarm-runtime --bin swarm_detect -- --config rulesets/default.yaml --scenarios-dir scenarios/` passed and processed the shipped scenario corpus. |
| process a suspicious telemetry event from detection through persisted receipt | ✓ VERIFIED | `full_critical_path_detect_to_receipt` passed in the integration suite. |
| process a suspicious event where policy denies response but the audit artifact still persists | ✓ VERIFIED | `policy_deny_produces_bundle_with_skipped_response` passed in the integration suite. |
| scrape Prometheus metrics from the local operator surface | ✓ VERIFIED | The `/metrics` route test passed with OpenMetrics content and all three histogram families present. |
| enforce panic-free production code under full workspace CI validation | ✓ VERIFIED | `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo build --workspace --all-targets`, and `cargo test --workspace` all passed. |

## Notes

- The standalone service extraction stays single-node and fixture-driven; real telemetry ingress and deployment packaging remain future milestone work.
- The lint hardening deliberately preserved test ergonomics by using scoped module-level allowances for assertion-heavy test suites and the benchmark example.
- Nyquist validation files remain absent for phases 78-80 and are tracked only as future follow-up work.

## Verdict

Milestone `v1.25` passes audit and is ready for archival.
