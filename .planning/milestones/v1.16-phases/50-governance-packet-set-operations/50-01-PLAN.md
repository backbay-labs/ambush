# Plan 50-01

1. Add file-backed governance packet-set reports, records, indexes, and a runtime harness above persisted governance-ready packets.
2. Preserve source packet, portfolio, cohort, ranking, validation, proof, advisory, and rollout-lineage context on each packet-set entry.
3. Add `swarmctl` commands to create, reload, list, and split packet-set artifacts by stable ID.
4. Cover merge, split, parent-lineage, and cohort-filter behavior with runtime tests.
