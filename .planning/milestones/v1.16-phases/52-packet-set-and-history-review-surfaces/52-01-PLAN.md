# Plan 52-01

1. Wire packet-set and history result/list commands into `swarmctl` with dedicated results-dir globals.
2. Add human-readable renderers for packet sets, packet-set lists, history reports, and history lists.
3. Update operator documentation to describe the new packet-set and history commands plus their fail-closed behavior.
4. Verify the new surfaces through workspace checks and one real temp-dir CLI flow.
