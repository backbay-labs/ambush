---
phase: 230-service-module-decomposition
plan: 01
type: execute
wave: 1
depends_on:
  - 229
files_modified:
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime/src/service
  - .planning/phases/230-service-module-decomposition
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - SVCMOD-01
must_haves:
  truths:
    - "`swarm_runtime::service::{...}` must remain the stable public import path after the split."
    - "No extracted service source file may exceed the 2000-line boundary claimed by the roadmap."
    - "The decomposition must separate lifecycle, orchestration, preview/status helpers, and tests into clearer ownership boundaries without changing shipped behavior."
  artifacts:
    - path: ".planning/phases/230-service-module-decomposition"
      provides: "Context, plan, summary, and verification artifacts for the service module refactor."
---

<objective>
Decompose `crates/swarm-runtime/src/service.rs` into focused lifecycle,
orchestration, preview/status, and test modules without changing the shipped
runtime surface.
</objective>

<context>
@.planning/phases/230-service-module-decomposition/230-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/service.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Define the internal service module boundaries</name>
  <files>
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/service
  </files>
  <action>Split the monolith into focused internal files for shared types and metrics, preview/enrichment helpers, `RuntimeService`, configured stack construction, operator status helpers, and tests.</action>
  <done>The service module tree is easier to navigate than the original monolith and no extracted file exceeds 2000 lines.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Preserve the shipped service surface</name>
  <files>
    crates/swarm-runtime/src/service
    crates/swarm-runtime/src/lib.rs
  </files>
  <action>Keep `swarm_runtime::service::{ConfiguredRuntimeStack, EventExecutionContext, RuntimeService, ...}` stable through root-module re-exports and shared imports.</action>
  <done>Existing internal and downstream callers keep compiling without broad call-site churn.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Prove the runtime still builds with the new structure</name>
  <files>
    crates/swarm-runtime
  </files>
  <action>Run targeted cargo checks and service-focused tests after the split.</action>
  <done>The refactor compiles cleanly and the runtime service coverage still passes on the decomposed module tree.</done>
</task>

</tasks>

<verification>
Verify the split with file-size checks, `cargo check -p swarm-runtime`, and
targeted service-focused tests.
</verification>
