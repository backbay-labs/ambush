---
phase: 231-runtime-service-arc-reduction
plan: 01
type: execute
wave: 1
depends_on:
  - 230
files_modified:
  - crates/swarm-runtime/src/service/runtime_service.rs
  - crates/swarm-runtime/src/ingest/mod.rs
  - crates/swarm-runtime/src/ingest/demo.rs
  - .planning/phases/231-runtime-service-arc-reduction
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - .planning/MILESTONES.md
autonomous: true
requirements:
  - SVCMOD-02
must_haves:
  truths:
    - "Request-facing paths must stop loading the full configured runtime stack when they only need audited execution."
    - "Runtime reload must keep any narrowed request-runtime handle synchronized with the rebuilt stack."
    - "The refactor must preserve human-approved demo replay, request routing, and service behavior while reducing wide shared-state cloning."
  artifacts:
    - path: ".planning/phases/231-runtime-service-arc-reduction"
      provides: "Context, plan, summary, and verification artifacts for the request-runtime narrowing refactor."
---

<objective>
Narrow `RuntimeService` ownership so request-response and human-approved demo
execution paths clone only the shared execution runtime instead of the full
configured runtime stack.
</objective>

<context>
@.planning/phases/231-runtime-service-arc-reduction/231-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/service/runtime_service.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/mod.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/demo.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Narrow the owned runtime handle inside RuntimeService</name>
  <files>
    crates/swarm-runtime/src/service/runtime_service.rs
  </files>
  <action>Store the runtime as an explicit shared handle and expose a bounded accessor so callers that only need audited execution stop reaching through the full service/stack graph.</action>
  <done>`RuntimeService` has a narrower execution-runtime ownership model that is explicit in the type shape.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Route request-facing ingest paths through the narrowed runtime handle</name>
  <files>
    crates/swarm-runtime/src/ingest/mod.rs
    crates/swarm-runtime/src/ingest/demo.rs
  </files>
  <action>Add a separately swapped request-runtime handle in ingest state and update the request router plus human-approved demo replay paths to use it instead of cloning the full configured stack.</action>
  <done>Request routing and human-approved demo execution no longer load the whole runtime stack just to reach audited execution.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Prove request-path behavior still works</name>
  <files>
    crates/swarm-runtime
  </files>
  <action>Run build, service-focused tests, request-path demo tests, and production-target clippy after the narrowing change.</action>
  <done>The narrowed ownership model is build/test/clippy proven without behavioral regression.</done>
</task>

</tasks>

<verification>
Verify with `cargo check -p swarm-runtime`, `cargo test -p swarm-runtime --lib service::`,
request-path demo tests, and `cargo clippy -p swarm-runtime --lib --bins -- -D warnings`.
</verification>
