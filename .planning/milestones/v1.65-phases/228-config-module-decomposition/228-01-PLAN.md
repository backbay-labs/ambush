---
phase: 228-config-module-decomposition
plan: 01
type: execute
wave: 1
depends_on:
  - v1.64
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-core/src/config
  - .planning/phases/228-config-module-decomposition
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - CFGEXT-01
must_haves:
  truths:
    - "`swarm_core::config` must remain the stable public module path after the split."
    - "No extracted config file may exceed the 2000-line boundary claimed by the roadmap."
    - "The split must preserve the shipped validation and serde-default behavior instead of creating a cosmetic wrapper around the same monolith."
  artifacts:
    - path: ".planning/phases/228-config-module-decomposition"
      provides: "Context, plan, summary, and verification artifacts for the config module refactor."
---

<objective>
Decompose `swarm-core/src/config.rs` into focused sub-modules without changing
the shipped config contract.
</objective>

<context>
@.planning/phases/228-config-module-decomposition/228-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/lib.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Define the config module boundaries</name>
  <files>
    crates/swarm-core/src/config.rs
    crates/swarm-core/src/config
  </files>
  <action>Split the monolithic config file into focused domain modules for runtime, bridges, pheromone/policy, response delivery, storage/rollout, evolution, deception or memory, operator surfaces, validation, defaults, and tests.</action>
  <done>The resulting config module tree has small focused files and keeps `swarm_core::config::{...}` stable for downstream callers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Preserve validation and default behavior</name>
  <files>
    crates/swarm-core/src/config
  </files>
  <action>Keep the existing serde defaults, domain validation helpers, and `SwarmConfig::validate()` behavior intact while moving code into domain-owned modules.</action>
  <done>The extracted module tree still expresses the same config validation contract and default-value behavior as the monolith.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Prove the split builds cleanly</name>
  <files>
    crates/swarm-core
    crates/swarm-runtime
  </files>
  <action>Run targeted cargo checks and tests that cover the config surface after the refactor.</action>
  <done>The split compiles cleanly and the targeted config tests continue to pass.</done>
</task>

</tasks>

<verification>
Verify the extracted module tree with file-size checks plus targeted
`cargo test -p swarm-core config --lib` and `cargo check -p swarm-runtime`.
</verification>
