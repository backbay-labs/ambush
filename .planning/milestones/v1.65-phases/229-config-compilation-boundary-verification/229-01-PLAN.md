---
phase: 229-config-compilation-boundary-verification
plan: 01
type: execute
wave: 1
depends_on:
  - 228
files_modified:
  - tools/measure-config-rebuild-scope.sh
  - .planning/phases/229-config-compilation-boundary-verification
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - CFGEXT-02
must_haves:
  truths:
    - "The verification method must be repeatable from the repo, not just a one-off shell note."
    - "The measured rebuild set must be compared against the actual `swarm-core` reverse dependency set."
    - "The artifact must make the remaining rebuild fanout explicit for future config crate extraction."
  artifacts:
    - path: "tools/measure-config-rebuild-scope.sh"
      provides: "Repeatable workspace rebuild-scope measurement for config-only edits."
    - path: ".planning/phases/229-config-compilation-boundary-verification"
      provides: "Context, plan, summary, and verification artifacts for rebuild-boundary proof."
---

<objective>
Prove the Phase 228 config split now rebuilds only the crates that actually
depend on `swarm-core` config state, and document the remaining crate fanout.
</objective>

<context>
@.planning/phases/229-config-compilation-boundary-verification/229-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/tools/measure-config-rebuild-scope.sh
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Define the rebuild-scope measurement</name>
  <files>
    tools/measure-config-rebuild-scope.sh
  </files>
  <action>Add a repeatable repo-owned measurement that warms the workspace, touches one config module, reruns a workspace check, and captures the rebuilt crate set.</action>
  <done>The repo has a concrete, repeatable command for measuring config-only rebuild fanout.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Compare the measured rebuild set with actual reverse dependencies</name>
  <files>
    tools/measure-config-rebuild-scope.sh
    .planning/phases/229-config-compilation-boundary-verification
  </files>
  <action>Measure the rebuilt crates after a representative config touch and compare them with the `swarm-core` reverse dependency set plus total workspace membership.</action>
  <done>The phase artifact records which crates rebuild, which remain untouched, and whether the rebuilt set matches the true `swarm-core` dependency fanout.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Document the remaining crate-boundary limitation</name>
  <files>
    .planning/phases/229-config-compilation-boundary-verification
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <action>Summarize the measured fanout and make explicit that any remaining breadth is still driven by the `swarm-core` crate boundary rather than the former `config.rs` file shape.</action>
  <done>The verification artifact gives future extraction work a clear baseline and Phase 229 can be marked complete.</done>
</task>

</tasks>

<verification>
Verify the phase with `tools/measure-config-rebuild-scope.sh`,
`cargo tree --workspace --invert swarm-core --depth 2`, and the resulting
rebuild-set artifact.
</verification>
