---
phase: 176-providence-callback-and-incident-reconciliation
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 175
files_modified:
  - crates/swarm-core/src/lib.rs
  - crates/swarm-core/src/types.rs
  - crates/swarm-runtime/src/ingest/mod.rs
  - crates/swarm-runtime/src/ingest/platform_api.rs
  - crates/swarm-runtime/src/ingest/providence_handlers.rs
  - crates/swarm-runtime/src/ingest/tests.rs
  - crates/swarm-runtime/src/providence.rs
  - crates/swarm-spine/src/incident.rs
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - PROVREC-01
  - PROVREC-02
must_haves:
  truths:
    - "Phase 150 already proved outbound Providence lifecycle sync and durable external references, so Phase 176 must extend that seam rather than inventing a second incident integration."
    - "Lifecycle drift only matters if it survives restart and reaches operator-visible reads; logging alone does not satisfy reconciliation."
    - "If reconciliation detects disagreement, the outbound adapter cannot immediately erase that disagreement on the next tick, or operators never get a stable review state."
  artifacts:
    - path: "crates/swarm-runtime/src/providence.rs"
      provides: "Outbound incident sync loop, incident key derivation, and Providence adapter state."
    - path: "crates/swarm-runtime/src/ingest/providence_handlers.rs"
      provides: "Signed Providence inbound-handler and incident lookup patterns."
    - path: "crates/swarm-spine/src/incident.rs"
      provides: "Durable incident metadata store for external references and audit trails."
  key_links:
    - from: "crates/swarm-runtime/src/ingest/providence_handlers.rs"
      to: "crates/swarm-spine/src/incident.rs"
      via: "signed callback payload persistence"
      pattern: "callback outcome becomes durable incident reconciliation state"
    - from: "crates/swarm-runtime/src/providence.rs"
      to: "crates/swarm-runtime/src/ingest/platform_api.rs"
      via: "latest reconciliation snapshot"
      pattern: "operator-visible incident summary shows which side is ahead"
---

<objective>
Complete Phase 176 by shipping authenticated Providence callbacks, durable incident reconciliation state, and explicit drift visibility on existing operator incident reads.

Purpose: move Providence from outbound-only lifecycle sync to bidirectional lifecycle reconciliation with bounded operator review semantics.
Output: signed callback endpoint, durable reconciliation snapshot plus callback audit, outbound adapter drift gating, and surfaced reconciliation status on incident summaries.
</objective>

<context>
@.planning/phases/176-providence-callback-and-incident-reconciliation/176-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/providence.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/providence_handlers.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-spine/src/incident.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/platform_api.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend shared incident persistence with Providence reconciliation state</name>
  <files>
    crates/swarm-core/src/types.rs
    crates/swarm-core/src/lib.rs
    crates/swarm-spine/src/incident.rs
  </files>
  <action>Add shared Providence callback and reconciliation types, persist the latest reconciliation snapshot plus callback audit on correlated incidents, and provide store helpers to resolve incidents by Providence external reference or update reconciliation state.</action>
  <verify>
    <automated>cargo test -p swarm-spine incident::tests:: -- --nocapture</automated>
  </verify>
  <done>Incident records can durably store Providence reconciliation state and reload it across restart.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add authenticated callback intake and bounded adapter reconciliation behavior</name>
  <files>
    crates/swarm-runtime/src/providence.rs
    crates/swarm-runtime/src/ingest/mod.rs
    crates/swarm-runtime/src/ingest/providence_handlers.rs
    crates/swarm-runtime/src/ingest/tests.rs
  </files>
  <action>Implement a signed `/v1/providence/callback` endpoint that resolves the target incident, persists reconciliation outcomes, refreshes remote identifiers, and prevents the adapter from blindly overwriting drift that now requires review.</action>
  <verify>
    <automated>cargo test -p swarm-runtime providence::tests:: ingest::tests::providence_callback -- --nocapture</automated>
  </verify>
  <done>Providence callbacks reconcile open/update/resolve state against durable incidents and preserve lifecycle disagreement as explicit review-required state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface reconciliation status through existing incident reads</name>
  <files>
    crates/swarm-runtime/src/ingest/platform_api.rs
    crates/swarm-runtime/src/ingest/tests.rs
    docs/CONFIGURATION.md
  </files>
  <action>Expose the latest Providence reconciliation summary on the existing incidents API and document the callback contract and operator-visible drift semantics.</action>
  <verify>
    <automated>cargo test -p swarm-runtime ingest::tests::platform_incidents -- --nocapture</automated>
  </verify>
  <done>Operators can query incidents and see whether Providence and Swarm are in sync or which side is ahead.</done>
</task>

</tasks>

<verification>
Build the durable model first, then wire the callback handler, then surface the status. Keep the phase scoped to lifecycle reconciliation only; analyst feedback and review UX stay in later phases.
</verification>
