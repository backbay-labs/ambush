---
phase: 254-benchmark-regression-gate
plan: 01
type: execute
depends_on:
  - 253-jetstream-integration-tests-in-ci
files_modified:
  - .github/workflows/ci.yml
  - tools/check-hot-path-regression.sh
  - docs/benchmarks/fast-detection-baseline.json
  - docs/benchmarks/fast-detection.md
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - CIHARD-03
---

<objective>
Add a dedicated CI benchmark gate that runs the repo-owned `hot_path` Criterion
bench, reads a tracked baseline JSON, and fails when p99 latency regresses
beyond the configured threshold.
</objective>

<tasks>

<task>
  <name>Add the benchmark job and parser</name>
  <done>The CI workflow now runs `tools/check-hot-path-regression.sh`, uploads the benchmark log artifact, and the script extracts p50/p95/p99/throughput from the bench output.</done>
</task>

<task>
  <name>Track and refresh the baseline in-repo</name>
  <done>The benchmark baseline now lives in `docs/benchmarks/fast-detection-baseline.json` and the benchmark document explains how and when to refresh it.</done>
</task>

<task>
  <name>Keep the helper portable</name>
  <done>The benchmark script no longer relies on `mapfile`, so the same repo-owned gate runs on local macOS Bash 3 and CI Linux shells.</done>
</task>

</tasks>
