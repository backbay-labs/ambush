---
phase: 255-release-pipeline-and-container-publishing
plan: 01
type: execute
depends_on:
  - 254-benchmark-regression-gate
files_modified:
  - .github/workflows/release.yml
  - .github/workflows/release-sbom.yml
  - tools/generate-changelog.sh
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - RELEASE-01
  - RELEASE-02
---

<objective>
Replace the limited release automation with a tagged repo-owned workflow that
generates a changelog, emits SBOM artifacts, publishes multi-arch GHCR images,
signs them, and attaches provenance plus GitHub release artifacts.
</objective>

<tasks>

<task>
  <name>Promote tagged release automation</name>
  <done>A new `release.yml` now runs on version tags and replaces the old SBOM-only workflow.</done>
</task>

<task>
  <name>Add repo-owned changelog generation</name>
  <done>`tools/generate-changelog.sh` groups conventional commits into a Markdown changelog for the tag being released.</done>
</task>

<task>
  <name>Publish hardened release artifacts</name>
  <done>The tagged release path now builds multi-arch GHCR images, emits SBOM artifacts, signs the image digest with cosign keyless flow, publishes provenance attestation, and creates a GitHub release.</done>
</task>

</tasks>
