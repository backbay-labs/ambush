---
phase: 253-jetstream-integration-tests-in-ci
plan: 01
type: execute
depends_on:
  - 252-ci-job-parallelization
files_modified:
  - .github/workflows/ci.yml
  - tools/with-nats-jetstream.sh
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - CIHARD-02
---

<objective>
Wire the existing JetStream substrate proof into CI through a repo-owned NATS
container harness and make failures actionable enough to diagnose quickly.
</objective>

<tasks>

<task>
  <name>Add a dedicated CI JetStream job</name>
  <done>The CI workflow now runs the ignored `swarm-pheromone` JetStream and multi-instance integration suites under a dedicated `jetstream` job.</done>
</task>

<task>
  <name>Preserve repo-owned container orchestration</name>
  <done>The job reuses `tools/with-nats-jetstream.sh` instead of introducing an out-of-band CI-only harness.</done>
</task>

<task>
  <name>Improve failure output</name>
  <done>The harness now emits `docker compose ps` and NATS logs on failure before teardown so CI failures distinguish harness and service faults from test regressions.</done>
</task>

</tasks>
