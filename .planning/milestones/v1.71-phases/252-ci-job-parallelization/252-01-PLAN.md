---
phase: 252-ci-job-parallelization
plan: 01
type: execute
depends_on:
  - 251-bridge-health-and-metrics-integration
files_modified:
  - .github/workflows/ci.yml
  - docs/CONFIGURATION.md
  - crates/swarm-response/src/config.rs
  - crates/swarm-response/src/dispatch.rs
  - crates/swarm-response/src/http_edr.rs
  - crates/swarm-response/src/notification.rs
  - crates/swarm-response/src/siem.rs
  - crates/swarm-runtime/tests/critical_path_integration.rs
  - crates/swarm-runtime/tests/ingest_integration.rs
  - crates/swarm-runtime/tests/operational_hardening_integration.rs
  - crates/swarm-runtime/examples/evolution_benchmark.rs
  - crates/swarm-runtime/src/ingest/mod.rs
  - crates/swarm-runtime/src/ingest/tests.rs
  - crates/swarm-runtime/src/evolution_status.rs
  - crates/swarm-runtime/src/http/core.inc
  - crates/swarm-runtime/src/startup_attestation.rs
  - rulesets/attestation.json
autonomous: true
requirements:
  - CIHARD-01
---

<objective>
Replace the monolithic CI path with parallel fmt, panic-contract, build, clippy,
test, and supply-chain jobs that reuse cargo caches and still expose one
operator-readable repo health signal.
</objective>

<tasks>

<task>
  <name>Decompose the workflow graph</name>
  <done>The CI workflow now runs separate fmt, panic-contract, build, clippy, test, jetstream, benchmark, and supply-chain jobs with explicit dependency edges.</done>
</task>

<task>
  <name>Reuse shared build artifacts</name>
  <done>The new workflow restores a shared cargo-home cache and a shared `target/ci` cache keyed by commit SHA across the heavy jobs.</done>
</task>

<task>
  <name>Stabilize the repo-owned test proof</name>
  <done>The workspace test lane now runs deterministically with `--test-threads=1`, and the required fixture/signer/readiness drift fixes were landed so the lane stays green.</done>
</task>

</tasks>
