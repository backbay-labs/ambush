---
phase: 251-bridge-health-and-metrics-integration
plan: 01
type: execute
depends_on:
  - 250-auditd-bridge
files_modified:
  - crates/swarm-runtime/src/bridge_runtime.rs
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/tests/bridge_registry_integration.rs
autonomous: true
requirements:
  - TELBR-04
---

<objective>
Prove the new Windows Event Log, Sysmon, and auditd bridges all surface through
the existing runtime health/metrics and detector pipeline without any adapter-
specific special casing.
</objective>

<tasks>

<task>
  <name>Wire registry and readiness seams</name>
  <done>`BridgeRuntimeRegistry` and the operator readiness probe both understand the new bridge variants through the same generic surface as the earlier bridges.</done>
</task>

<task>
  <name>Add fleet-level integration proof</name>
  <done>One runtime integration test runs the three host-log bridges together, checks bridge health and metrics, and proves existing detectors consume the normalized events end to end.</done>
</task>

</tasks>
