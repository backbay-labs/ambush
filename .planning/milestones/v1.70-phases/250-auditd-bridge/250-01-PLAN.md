---
phase: 250-auditd-bridge
plan: 01
type: execute
depends_on:
  - 249-sysmon-bridge
files_modified:
  - crates/swarm-core/src/config/bridges.rs
  - crates/swarm-core/src/config/tests.rs
  - crates/swarm-ingest-json/src/lib.rs
  - crates/swarm-ingest-json/src/auditd.rs
  - crates/swarm-runtime/src/bridge_runtime.rs
  - crates/swarm-runtime/src/control.rs
autonomous: true
requirements:
  - TELBR-03
---

<objective>
Ship an auditd bridge that maps representative syscall and authentication
records into the shared telemetry schema without adding detector-specific hacks.
</objective>

<tasks>

<task>
  <name>Add config/runtime registration for auditd</name>
  <done>The runtime can deserialize, validate, and construct `auditd` bridge-backed telemetry sources.</done>
</task>

<task>
  <name>Implement auditd normalization</name>
  <done>Representative auditd auth, execve, connect, and file-write records map into shared telemetry payloads.</done>
</task>

<task>
  <name>Prove detector-ready mappings</name>
  <done>Focused bridge tests lock the payload shape that the existing auth, execution, network, and persistence detectors already consume.</done>
</task>

</tasks>
