---
phase: 248-windows-event-log-bridge
plan: 01
type: execute
depends_on:
  - 247-normalization-false-positive-regression-test
files_modified:
  - crates/swarm-core/src/config/bridges.rs
  - crates/swarm-core/src/config/tests.rs
  - crates/swarm-ingest-json/src/lib.rs
  - crates/swarm-ingest-json/src/host_common.rs
  - crates/swarm-ingest-json/src/windows_event_log.rs
  - crates/swarm-runtime/src/bridge_runtime.rs
  - crates/swarm-runtime/src/control.rs
autonomous: true
requirements:
  - TELBR-01
---

<objective>
Ship a repo-configured Windows Event Log bridge that reuses the existing bridge
runtime and maps representative Security log records into the shared telemetry
schema.
</objective>

<tasks>

<task>
  <name>Add config and runtime registration for Windows Event Log bridges</name>
  <done>`TelemetryBridgeConfig` can deserialize and validate `windows_event_log` sources and the runtime bridge registry can construct them.</done>
</task>

<task>
  <name>Implement Windows Event Log normalization</name>
  <done>`WindowsEventLogBridge` maps logon and process-create records into shared auth and process payloads while skipping unsupported event IDs.</done>
</task>

<task>
  <name>Prove representative mappings</name>
  <done>Focused bridge tests prove failed RDP-style logons and process-create records normalize correctly.</done>
</task>

</tasks>
