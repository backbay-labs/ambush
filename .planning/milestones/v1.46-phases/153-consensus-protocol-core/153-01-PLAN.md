---
phase: 153-consensus-protocol-core
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 152
files_modified:
  - crates/swarm-consensus/Cargo.toml
  - crates/swarm-consensus/src/lib.rs
autonomous: true
requirements:
  - CONSENSUS-01
  - CONSENSUS-02
must_haves:
  truths:
    - "The consensus crate already exists as the intended boundary, so Phase 153 should fill that crate rather than grow a temporary protocol engine inside swarm-runtime."
    - "Phase 153 is protocol core only; signatures, equivocation handling, registry admission, and governance execution routing are explicit next-phase concerns."
    - "Every node must derive the same proposer from local state alone, which means proposer rotation has to depend only on the previous commit hash and stable committee identity ordering."
  artifacts:
    - path: "crates/swarm-consensus/src/lib.rs"
      provides: "Committee rotation, Tendermint-style round state machine, timeout-driven round advance, and in-process proof harness."
    - path: "crates/swarm-consensus/Cargo.toml"
      provides: "Consensus crate dependencies for hashing, transport subjects, and async test execution."
  key_links:
    - from: "crates/swarm-runtime/src/agent_identity.rs"
      to: "crates/swarm-consensus/src/lib.rs"
      via: "AgentId-based committee membership"
      pattern: "committee rotation hashes the prior commit and stable agent identities"
    - from: "crates/swarm-pheromone/src/jetstream.rs"
      to: "crates/swarm-consensus/src/lib.rs"
      via: "JetStream transport seam"
      pattern: "consensus messages publish over stable swarm consensus subjects"
---

<objective>
Complete Phase 153 by shipping the reusable consensus protocol core in `swarm-consensus`.

Purpose: give the next governance phase a tested BFT round engine and deterministic committee selector instead of embedding consensus policy directly into TomAgent.
Output: a Tendermint-style propose / prevote / precommit engine with timeout-driven round advance, deterministic proposer rotation from prior commits plus agent identities, JetStream subject helpers, and an in-process 3-node proof that commits 10 sequential proposals.
</objective>

<context>
@.planning/phases/153-consensus-protocol-core/153-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-consensus/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/tom_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/agent_identity.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-pheromone/src/jetstream.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the deterministic committee and message contract</name>
  <files>
    crates/swarm-consensus/Cargo.toml
    crates/swarm-consensus/src/lib.rs
  </files>
  <action>Add the reusable committee, proposer rotation, message, subject-layout, and threshold types that let every node derive the same round leader from the previous commit hash and committee identities.</action>
  <verify>
    <automated>cargo test -p swarm-consensus tests::committee_rotation_depends_on_previous_commit_hash_and_agent_ids -- --exact</automated>
  </verify>
  <done>The crate exposes deterministic committee rotation and stable JetStream subject helpers with focused proof coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the round engine with timeout-driven view change</name>
  <files>
    crates/swarm-consensus/src/lib.rs
  </files>
  <action>Implement the propose, prevote, and precommit state machine plus round timeouts so a node can advance to the next proposer when the current round stalls.</action>
  <verify>
    <automated>cargo test -p swarm-consensus tests::timeout_advances_to_the_next_round_and_proposer -- --exact</automated>
  </verify>
  <done>The protocol advances rounds deterministically on timeout and can continue toward commit without external coordination logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove sequential consensus with an in-process three-node harness</name>
  <files>
    crates/swarm-consensus/src/lib.rs
  </files>
  <action>Add a small in-process harness that fans messages across three nodes and prove they commit 10 sequential proposals without Byzantine behavior.</action>
  <verify>
    <automated>cargo test -p swarm-consensus tests::three_node_committee_reaches_consensus_for_ten_sequential_proposals -- --exact</automated>
  </verify>
  <done>A three-node in-process committee commits ten proposals in order and produces stable commit hashes for proposer rotation.</done>
</task>

</tasks>

<verification>
Keep the phase bounded to reusable protocol core. Avoid pulling governance policy, runtime admission, or signature validation into this change set even if the protocol API leaves clear seams for those next-phase concerns.
</verification>
