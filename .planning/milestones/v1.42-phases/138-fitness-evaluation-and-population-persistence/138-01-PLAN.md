---
phase: 138-fitness-evaluation-and-population-persistence
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 137
files_modified:
  - crates/swarm-core/src/config.rs
  - rulesets/default.yaml
  - crates/swarm-runtime/src/config.rs
  - crates/swarm-evolution/src/mutation.rs
  - crates/swarm-runtime/src/kitten_agent.rs
autonomous: true
requirements:
  - KITTEN-02
  - KITTEN-05
must_haves:
  truths:
    - "Kitten fitness must be derived from repo-owned replay, verification, proof, and advisory artifacts rather than an ad hoc side channel."
    - "Population and generation state must survive process restart, so the implementation needs a durable store instead of in-memory-only bookkeeping."
    - "A configurable `max_proposals_per_hour` throttle must cap downstream proposal volume before Phase 139 safety and canary routing are added."
  artifacts:
    - path: "crates/swarm-core/src/config.rs"
      provides: "Repo-owned evolution config for fitness weights, population persistence, and proposal throttling."
    - path: "crates/swarm-evolution/src/mutation.rs"
      provides: "Candidate scoring and durable population update primitives reused by the runtime."
    - path: "crates/swarm-runtime/src/kitten_agent.rs"
      provides: "Runtime-owned restore, update, and throttle logic for the persisted Kitten population."
  key_links:
    - from: "crates/swarm-runtime/src/kitten_agent.rs"
      to: "crates/swarm-evolution/src/mutation.rs"
      via: "population updates derived from validated mutation batches"
      pattern: "rank_candidates"
    - from: "crates/swarm-core/src/config.rs"
      to: "rulesets/default.yaml"
      via: "repo-owned evolution fitness and throttle defaults"
      pattern: "evolution"
---

<objective>
Complete Phase 138 by making Kitten’s candidate evaluation durable and restart-safe.

Purpose: turn the Phase 137 mutation loop into a real evolutionary subsystem with replay-corpus fitness, persisted population state, and proposal-rate control.
Output: repo-owned fitness config, a durable population and generation store, runtime restore/update logic, and focused tests for restart persistence and proposal throttling.
</objective>

<context>
@.planning/phases/138-fitness-evaluation-and-population-persistence/138-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/kitten_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/mutation.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/strategy.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/selection.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend repo-owned evolution config for fitness and throttling</name>
  <read_first>
    crates/swarm-core/src/config.rs
    rulesets/default.yaml
  </read_first>
  <files>
    crates/swarm-core/src/config.rs
    rulesets/default.yaml
    crates/swarm-runtime/src/config.rs
  </files>
  <action>Add evolution fitness weights, persistence sizing knobs, and `max_proposals_per_hour` to the repo-owned config surface with fail-closed validation and sensible defaults.</action>
  <verify>
    <automated>cargo test -p swarm-core config::tests -- --exact</automated>
  </verify>
  <done>The runtime can load and validate population, fitness, and throttle settings from YAML.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist a replay-scored Kitten population</name>
  <read_first>
    crates/swarm-evolution/src/mutation.rs
    crates/swarm-evolution/src/strategy.rs
    crates/swarm-runtime/src/kitten_agent.rs
  </read_first>
  <files>
    crates/swarm-evolution/src/lib.rs
    crates/swarm-evolution/src/mutation.rs
    crates/swarm-runtime/src/kitten_agent.rs
  </files>
  <action>Add durable population and generation state, derive multi-objective candidate fitness from the existing validation, proof, advisory, and replay artifacts, and use Pareto tournament selection to choose survivors and proposal candidates.</action>
  <verify>
    <automated>cargo test -p swarm-evolution mutation::tests -- --exact</automated>
    <automated>cargo test -p swarm-runtime kitten_agent::tests -- --exact</automated>
  </verify>
  <done>Kitten can persist, restore, and evolve a scored candidate population across restarts without losing generation progress.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce proposal throttling in the runtime loop</name>
  <read_first>
    crates/swarm-runtime/src/kitten_agent.rs
    crates/swarm-runtime/src/dispatcher.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/kitten_agent.rs
    crates/swarm-runtime/src/dispatcher.rs
  </files>
  <action>Track proposal timestamps in the persisted Kitten state and block new proposals when the configured hourly budget is exhausted, while preserving the validated population for later selection once the throttle window clears.</action>
  <verify>
    <automated>cargo test -p swarm-runtime kitten_agent::tests -- --exact</automated>
    <automated>cargo check -p swarm-core -p swarm-runtime -p swarm-evolution --tests -j 1 --message-format short</automated>
  </verify>
  <done>The runtime cannot flood downstream canary review with more than the configured hourly proposal budget, and restart does not reset the throttle history unexpectedly.</done>
</task>

</tasks>

<verification>
Land config first, then the durable population store plus fitness computation, then the runtime throttle and restart behavior. Finish with a combined `swarm-core`, `swarm-runtime`, and `swarm-evolution` verification pass because the phase crosses all three crates.
</verification>
