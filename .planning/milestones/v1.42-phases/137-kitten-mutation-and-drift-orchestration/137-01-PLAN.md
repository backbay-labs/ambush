---
phase: 137-kitten-mutation-and-drift-orchestration
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 136
files_modified:
  - crates/swarm-core/src/config.rs
  - rulesets/default.yaml
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/src/bin/swarm_detect.rs
  - crates/swarm-runtime/src/dispatcher.rs
  - crates/swarm-runtime/src/kitten_agent.rs
autonomous: true
requirements:
  - KITTEN-01
  - KITTEN-03
must_haves:
  truths:
    - "`KittenAgent` runs as a real `SwarmAgent` and advances a bounded multi-tick state machine instead of doing unbounded replay work in one dispatcher tick."
    - "Concept drift is repo-configured and must require minimum observations plus cooldown enforcement before mutation work starts."
    - "Phase 137 does not depend on dispatcher routing for `ProposeStrategy`; it only needs to reach a durable internal proposing state and preserve readiness for the later safety/canary phases."
  artifacts:
    - path: "crates/swarm-runtime/src/kitten_agent.rs"
      provides: "Runtime-owned Kitten agent implementation plus drift detector wiring."
    - path: "crates/swarm-core/src/config.rs"
      provides: "Repo-owned evolution and concept-drift configuration."
    - path: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      provides: "Serve-mode Kitten registration."
  key_links:
    - from: "crates/swarm-runtime/src/bin/swarm_detect.rs"
      to: "crates/swarm-runtime/src/kitten_agent.rs"
      via: "serve-mode dispatcher registration"
      pattern: "AgentRole::Kitten"
    - from: "crates/swarm-runtime/src/kitten_agent.rs"
      to: "crates/swarm-evolution/"
      via: "reuse extracted evolution harnesses rather than duplicating stores"
      pattern: "DefaultEvolution"
---

<objective>
Complete Phase 137 by landing the first runtime-owned evolution agent and the drift trigger that activates it.

Purpose: make evolution a real runtime subsystem without violating the current dispatcher timeout budget or depending on Phase 139 canary-routing work.
Output: repo-owned evolution config, a bounded `KittenAgent`, a concept-drift detector, serve-mode registration, and focused tests proving the state machine advances safely.
</objective>

<context>
@.planning/phases/137-kitten-mutation-and-drift-orchestration/137-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/types.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/dispatcher.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/bin/swarm_detect.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/drafting.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/mutation.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/strategy.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repo-owned evolution and drift configuration</name>
  <read_first>
    crates/swarm-core/src/config.rs
    rulesets/default.yaml
  </read_first>
  <files>
    crates/swarm-core/src/config.rs
    rulesets/default.yaml
    crates/swarm-runtime/src/config.rs
  </files>
  <action>Add a minimal `evolution` config block with enablement, drift thresholds, minimum observations, and cooldown fields; validate it fail-closed and wire defaults into the repo-owned config surface.</action>
  <verify>
    <automated>cargo test -p swarm-core config::tests -- --exact</automated>
    <automated>cargo test -p swarm-runtime config::tests -- --exact</automated>
  </verify>
  <done>The runtime can load and validate evolution settings from YAML without hard-coded thresholds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `ConceptDriftDetector` and bounded `KittenAgent` state transitions</name>
  <read_first>
    crates/swarm-core/src/agent.rs
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-evolution/src/mutation.rs
    crates/swarm-evolution/src/strategy.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/kitten_agent.rs
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/src/dispatcher.rs
  </files>
  <action>Implement the drift detector and a runtime-owned Kitten agent that advances one state-machine step per tick, reuses extracted evolution harnesses where useful, and preserves intermediate mutation/evaluation context without blocking a dispatcher tick.</action>
  <verify>
    <automated>cargo test -p swarm-runtime kitten_agent::tests -- --exact</automated>
    <automated>cargo test -p swarm-runtime dispatcher::tests::dispatcher_*kitten* -- --exact</automated>
  </verify>
  <done>Kitten can idle, trigger on drift, progress through bounded states, and remain healthy under the existing dispatcher timeout budget.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register Kitten in serve mode and prove activation semantics</name>
  <read_first>
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-runtime/src/dispatcher.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/bin/swarm_detect.rs
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/ingest.rs
  </files>
  <action>Wire Kitten into the serve-mode dispatcher when evolution is enabled, preserve existing agent startup behavior, and add focused tests proving drift-triggered activation and cooldown behavior without changing the live-response routing semantics.</action>
  <verify>
    <automated>cargo test -p swarm-runtime kitten_agent::tests -- --exact</automated>
    <automated>cargo test -p swarm-runtime -j 1 --message-format short</automated>
  </verify>
  <done>Serve mode can run a configured Kitten agent and the runtime test suite proves its activation and timeout-safe orchestration behavior.</done>
</task>

</tasks>

<verification>
Land config validation first, then Kitten state-machine tests, then serve-mode wiring. Finish with a package-level `swarm-runtime` verification pass because Kitten touches dispatcher and runtime startup paths directly.
</verification>
