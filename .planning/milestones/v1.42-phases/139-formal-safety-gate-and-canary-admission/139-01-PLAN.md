---
phase: 139-formal-safety-gate-and-canary-admission
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 138
files_modified:
  - crates/swarm-core/src/config.rs
  - rulesets/default.yaml
  - crates/swarm-evolution/src/evolution.rs
  - crates/swarm-evolution/src/selection.rs
  - crates/swarm-evolution/src/canary.rs
  - crates/swarm-runtime/src/kitten_agent.rs
  - crates/swarm-runtime/src/dispatcher.rs
autonomous: true
requirements:
  - KITTEN-04
  - SAFETY-01
  - SAFETY-02
  - SAFETY-03
must_haves:
  truths:
    - "Phase 139 should reuse the proof, selection, and canary harnesses already shipped in `swarm-evolution` instead of inventing a second admission lane."
    - "Safety verdicts must remain deterministic and repo-owned, with explicit failure counterexamples preserved when a candidate is rejected."
    - "Kitten must hand candidates into an asynchronous admission workflow; blocking the tick loop on proof or canary work would regress the bounded-state-machine contract."
  artifacts:
    - path: "crates/swarm-evolution/src/evolution.rs"
      provides: "Durable formal safety artifacts, proof persistence, and queue-to-canary handoff packets."
    - path: "crates/swarm-evolution/src/selection.rs"
      provides: "Admission-state transitions and deterministic acceptance criteria for canary launch."
    - path: "crates/swarm-runtime/src/kitten_agent.rs"
      provides: "Runtime wiring that submits durable population winners into the formal safety lane."
  key_links:
    - from: "crates/swarm-runtime/src/kitten_agent.rs"
      to: "crates/swarm-evolution/src/selection.rs"
      via: "proof-backed selection and acceptance for canary"
      pattern: "AcceptedForCanary"
    - from: "crates/swarm-evolution/src/selection.rs"
      to: "crates/swarm-evolution/src/canary.rs"
      via: "handoff packets launching deterministic canary runs"
      pattern: "launch_canary"
---

<objective>
Complete Phase 139 by converting Kitten proposals into real safety-gated canary admissions.

Purpose: move beyond peer-visible `ProposeStrategy` warnings and into deterministic proof-backed admission backed by repo-owned invariants and durable failure records.
Output: repo-owned safety config and invariant files, asynchronous formal safety verification plus proof persistence, and a runtime handoff path that launches canary admission only for accepted candidates.
</objective>

<context>
@.planning/phases/139-formal-safety-gate-and-canary-admission/139-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/kitten_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/dispatcher.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/evolution.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/selection.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/canary.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repo-owned formal safety configuration and invariant sources</name>
  <read_first>
    crates/swarm-core/src/config.rs
    rulesets/default.yaml
  </read_first>
  <files>
    crates/swarm-core/src/config.rs
    rulesets/default.yaml
    rulesets/safety/
  </files>
  <action>Add repo-owned configuration for formal safety verification, invariant bundle locations, and optional proof-system toggles, then seed the first deterministic YAML invariants under `rulesets/safety/`.</action>
  <verify>
    <automated>cargo test -p swarm-core config::tests -- --exact</automated>
  </verify>
  <done>The runtime can load deterministic safety-gate settings and repo-owned invariants without hidden defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire proof-backed selection into asynchronous canary admission</name>
  <read_first>
    crates/swarm-evolution/src/evolution.rs
    crates/swarm-evolution/src/selection.rs
    crates/swarm-evolution/src/canary.rs
  </read_first>
  <files>
    crates/swarm-evolution/src/evolution.rs
    crates/swarm-evolution/src/selection.rs
    crates/swarm-evolution/src/canary.rs
  </files>
  <action>Use the existing proof, selection, and canary harnesses to verify evolved candidates against deterministic invariants, persist signed proof or counterexample artifacts, and launch canary handoff packets only when admission reaches `accepted_for_canary`.</action>
  <verify>
    <automated>cargo test -p swarm-evolution selection::tests -- --exact</automated>
    <automated>cargo test -p swarm-evolution evolution::tests -- --exact</automated>
  </verify>
  <done>Passing candidates produce durable safety artifacts and can enter canary admission; failing candidates persist explicit rejection evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace warning-only runtime proposals with safety-gated handoff</name>
  <read_first>
    crates/swarm-runtime/src/kitten_agent.rs
    crates/swarm-runtime/src/dispatcher.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/kitten_agent.rs
    crates/swarm-runtime/src/dispatcher.rs
  </files>
  <action>Submit persisted Kitten population winners into the formal safety lane, keep the runtime tick loop bounded, and preserve durable failure records when safety or canary admission rejects a candidate.</action>
  <verify>
    <automated>cargo test -p swarm-runtime dispatcher::tests -- --exact</automated>
    <automated>cargo test -p swarm-runtime kitten_agent::tests -- --exact</automated>
    <automated>cargo check -p swarm-core -p swarm-runtime -p swarm-evolution --tests -j 1 --message-format short</automated>
  </verify>
  <done>`KittenAgent` no longer stops at warning-only `ProposeStrategy`; accepted candidates move into canary admission through the shipped runtime lane.</done>
</task>

</tasks>

<verification>
Land the config and invariant surface first, then prove the selection and canary harness path, then wire the runtime submission flow. Finish with a cross-crate verification pass because Phase 139 spans shared config, evolution storage, and runtime orchestration.
</verification>
