---
phase: 140-evolution-observability-and-operator-status
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 139
files_modified:
  - crates/swarm-runtime/src/runtime_events.rs
  - crates/swarm-runtime/src/dispatcher.rs
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/kitten_agent.rs
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-cli/src/core.inc
  - crates/swarm-evolution/src/canary.rs
  - crates/swarm-evolution/src/mutation.rs
  - crates/swarm-evolution/src/selection.rs
autonomous: true
requirements:
  - EVOLVE-OBS-01
  - EVOLVE-OBS-02
must_haves:
  truths:
    - "Phase 140 should summarize the already-persisted evolution artifacts instead of inventing a separate observability-only state store."
    - "Evolution metrics must flow through the existing runtime SSE broadcaster so operators see them through the same event channel used for demo and runtime status."
    - "`swarmctl evolution status` must report the same durable generation, population, verification, and canary-admission facts that the runtime uses internally."
  artifacts:
    - path: "crates/swarm-runtime/src/runtime_events.rs"
      provides: "Stable SSE event kind and payload for evolution metrics."
    - path: "crates/swarm-runtime/src/control.rs"
      provides: "Operator-facing rendering of current evolution status."
    - path: "crates/swarm-cli/src/core.inc"
      provides: "Repo-owned `swarmctl evolution status` entrypoint."
  key_links:
    - from: "crates/swarm-runtime/src/kitten_agent.rs"
      to: "crates/swarm-runtime/src/runtime_events.rs"
      via: "proposal-time evolution metrics on the existing broadcaster"
      pattern: "ProposeStrategy"
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-evolution/src/selection.rs"
      via: "admission outcome metrics derived from durable selection and canary state"
      pattern: "AcceptedForCanary"
---

<objective>
Complete Phase 140 by exposing evolution metrics through SSE and a runtime-facing CLI summary.

Purpose: make the shipped Kitten, safety, and canary lanes inspectable without forcing operators to read raw files from the evolution result directories.
Output: durable evolution status summaries, runtime SSE events for evolution metrics, and a `swarmctl evolution status` command aligned with the same persisted artifacts.
</objective>

<context>
@.planning/phases/140-evolution-observability-and-operator-status/140-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/runtime_events.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/dispatcher.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/kitten_agent.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/service.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/control.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-cli/src/core.inc
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/mutation.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/selection.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/canary.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a durable evolution status summary from shipped artifacts</name>
  <read_first>
    crates/swarm-evolution/src/mutation.rs
    crates/swarm-evolution/src/selection.rs
    crates/swarm-evolution/src/canary.rs
  </read_first>
  <files>
    crates/swarm-evolution/src/mutation.rs
    crates/swarm-evolution/src/selection.rs
    crates/swarm-evolution/src/canary.rs
    crates/swarm-runtime/src/service.rs
  </files>
  <action>Derive a stable evolution status snapshot from the durable population, selection, and canary artifacts so generation count, population size, diversity, latest fitness, verification pass rate, and canary admission rate can be read without scraping raw JSON by hand.</action>
  <verify>
    <automated>cargo test -p swarm-runtime service::tests -- --exact</automated>
  </verify>
  <done>The runtime can compute one evolution status summary from persisted artifacts without adding a sidecar cache.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit evolution metrics on the existing SSE broadcaster</name>
  <read_first>
    crates/swarm-runtime/src/runtime_events.rs
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/ingest.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/runtime_events.rs
    crates/swarm-runtime/src/dispatcher.rs
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/kitten_agent.rs
  </files>
  <action>Add a stable evolution runtime event and publish it when Kitten produces a proposal-ready population snapshot and when the admission lane updates the selection or canary outcome.</action>
  <verify>
    <automated>cargo test -p swarm-runtime runtime_events::tests -- --exact</automated>
    <automated>cargo test -p swarm-runtime ingest::tests -- --exact</automated>
  </verify>
  <done>Operators subscribed to `/v1/events/stream` can observe evolution metrics and admission outcomes without reading local files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add `swarmctl evolution status` and extend operator status rendering</name>
  <read_first>
    crates/swarm-runtime/src/control.rs
    crates/swarm-cli/src/core.inc
  </read_first>
  <files>
    crates/swarm-runtime/src/control.rs
    crates/swarm-cli/src/core.inc
  </files>
  <action>Expose the durable evolution summary in the operator control surface and add a concise `swarmctl evolution status` command that reports generation, population, drift-related state, latest fitness, and verification or canary progress.</action>
  <verify>
    <automated>cargo test -p swarm-runtime control::tests -- --exact</automated>
    <automated>cargo test -p swarm-cli tests:: -- --nocapture</automated>
    <automated>cargo check -p swarm-runtime -p swarm-cli -j 1 --message-format short</automated>
  </verify>
  <done>Operators can inspect evolution state through one CLI command and the existing control-plane output without dropping to raw JSON stores.</done>
</task>

</tasks>

<verification>
Land the durable status summary first, then wire the runtime event emission, then expose the control and CLI surfaces. Finish with focused runtime and CLI verification because Phase 140 crosses persisted artifacts, SSE serialization, and command parsing.
</verification>
