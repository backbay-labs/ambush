---
phase: 136-crate-extraction-and-trace-instrumentation
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 135
files_modified:
  - Cargo.toml
  - crates/swarm-runtime/src/lib.rs
  - crates/swarm-runtime/src/bin/swarmctl.rs
  - crates/swarm-runtime/src/cli/core.inc
  - crates/swarm-runtime/src/ingest.rs
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-whisker/src/composite.rs
  - crates/swarm-policy/src/configurable_gate.rs
  - crates/swarm-response/src/dispatcher.rs
autonomous: true
requirements:
  - HARD-02
  - HARD-04
must_haves:
  truths:
    - "`swarm-evolution` owns the current evolution subsystem and `swarm-cli` owns the current CLI surface without breaking the shipped `swarm_detect` and `swarmctl` binaries."
    - "The required critical-path functions emit structured tracing spans carrying the existing correlation ID as `trace_id`."
    - "OTLP export stays optional and defaults to the current stdout JSON tracing path when not configured."
  artifacts:
    - path: "crates/swarm-evolution/"
      provides: "Extracted evolution subsystem with stable public harness interfaces."
    - path: "crates/swarm-cli/"
      provides: "Extracted CLI args, dispatch, and tracing bootstrap."
    - path: "crates/swarm-runtime/src/*.rs"
      provides: "Runtime re-exports plus hot-path tracing instrumentation."
  key_links:
    - from: "crates/swarm-runtime/src/bin/swarmctl.rs"
      to: "crates/swarm-cli/"
      via: "binary wrapper delegates into extracted CLI crate"
      pattern: "run(cli)"
    - from: "crates/swarm-runtime/src/lib.rs"
      to: "crates/swarm-evolution/"
      via: "public harness types remain reachable after extraction"
      pattern: "pub use"
---

<objective>
Complete Phase 136 by splitting ownership cleanly and landing critical-path tracing without destabilizing the runtime entrypoints.

Purpose: reduce `swarm-runtime` monolith pressure while making the hot path observable through stable trace IDs and optional OTLP export.
Output: extracted `swarm-evolution` and `swarm-cli` crates, runtime-compatible re-exports, instrumented hot-path functions, and preserved binaries.
</objective>

<context>
@.planning/phases/136-crate-extraction-and-trace-instrumentation/136-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/Cargo.toml
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/lib.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/bin/swarmctl.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/cli/core.inc
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/drafting.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/mutation.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/evolution.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/selection.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/portfolio.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/governance_prep.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/canary.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/promotion.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/strategy.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/evidence.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract `swarm-evolution` and preserve runtime-facing interfaces</name>
  <read_first>
    crates/swarm-runtime/src/lib.rs
    crates/swarm-runtime/src/drafting.rs
    crates/swarm-runtime/src/mutation.rs
    crates/swarm-runtime/src/evolution.rs
    crates/swarm-runtime/src/selection.rs
    crates/swarm-runtime/src/portfolio.rs
    crates/swarm-runtime/src/governance_prep.rs
    crates/swarm-runtime/src/canary.rs
    crates/swarm-runtime/src/promotion.rs
    crates/swarm-runtime/src/strategy.rs
    crates/swarm-runtime/src/evidence.rs
  </read_first>
  <files>
    Cargo.toml
    crates/swarm-evolution/**
    crates/swarm-runtime/src/lib.rs
  </files>
  <action>Create the new workspace crate, move the evolution-owned modules into it with minimal behavior changes, and keep the existing runtime call sites stable through explicit re-exports.</action>
  <verify>
    <automated>cargo check -p swarm-evolution -p swarm-runtime -j 1</automated>
  </verify>
  <done>The evolution subsystem is no longer owned by `swarm-runtime`, and existing runtime callers still compile through the preserved public surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract the CLI crate and preserve `swarmctl` behavior</name>
  <read_first>
    crates/swarm-runtime/src/bin/swarmctl.rs
    crates/swarm-runtime/src/cli/core.inc
  </read_first>
  <files>
    Cargo.toml
    crates/swarm-cli/**
    crates/swarm-runtime/src/bin/swarmctl.rs
  </files>
  <action>Move the CLI args and dispatch implementation into `swarm-cli`, keep `swarmctl` as a thin wrapper, and preserve the current command surface and output shapes.</action>
  <verify>
    <automated>cargo check -p swarm-cli -p swarm-runtime -j 1</automated>
  </verify>
  <done>The CLI implementation lives in its own crate and the existing `swarmctl` binary still builds and routes commands correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Instrument the required hot path and add optional OTLP export</name>
  <read_first>
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/service.rs
    crates/swarm-whisker/src/composite.rs
    crates/swarm-policy/src/configurable_gate.rs
    crates/swarm-response/src/dispatcher.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-runtime/src/service.rs
    crates/swarm-whisker/src/composite.rs
    crates/swarm-policy/src/configurable_gate.rs
    crates/swarm-response/src/dispatcher.rs
    crates/swarm-cli/**
  </files>
  <action>Add structured tracing spans carrying `trace_id=<correlation_id>` to the required critical-path functions and route optional OTLP export through a new CLI flag while keeping stdout JSON as the default sink.</action>
  <verify>
    <automated>cargo test -p swarm-runtime -p swarm-cli -j 1</automated>
  </verify>
  <done>The hot path emits stable trace metadata, and OTLP export remains opt-in rather than changing default runtime behavior.</done>
</task>

</tasks>

<verification>
Complete the extraction incrementally with `cargo check` on the new crates after each move, then finish with focused runtime and CLI test coverage once the tracing and OTLP path are wired.
</verification>
