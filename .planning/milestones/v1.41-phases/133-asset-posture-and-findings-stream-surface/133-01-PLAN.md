---
phase: 133-asset-posture-and-findings-stream-surface
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 132
files_modified:
  - .planning/phases/133-asset-posture-and-findings-stream-surface/133-CONTEXT.md
  - crates/swarm-pheromone/src/substrate.rs
  - crates/swarm-whisker/src/stream.rs
  - crates/swarm-runtime/src/detection/pipeline.rs
  - crates/swarm-runtime/src/service.rs
  - crates/swarm-runtime/src/stalker_agent.rs
  - crates/swarm-runtime/src/runtime_events.rs
  - crates/swarm-runtime/src/ingest.rs
autonomous: true
requirements:
  - API-02
  - API-03
must_haves:
  truths:
    - "`GET /v2/api/assets/{host_id}/posture` returns per-threat-class concentrations, active investigations, escalation level, and recent findings for the requested host through the Phase 132 platform API contract."
    - "`GET /v2/api/stream/findings` uses the existing runtime-event broadcaster and emits canonical `SwarmFindingEnvelope` payloads as SSE `finding` events."
    - "Host posture uses substrate-backed host filtering instead of inventing a side cache or bypassing persisted deposits."
    - "Both new surfaces stay behind the Phase 132 platform API key middleware and do not change health or ingest auth boundaries."
  artifacts:
    - path: "crates/swarm-pheromone/src/substrate.rs"
      provides: "Host-filtered deposit queries."
    - path: "crates/swarm-runtime/src/runtime_events.rs"
      provides: "First-class runtime finding events."
    - path: "crates/swarm-runtime/src/ingest.rs"
      provides: "Platform host posture endpoint, findings SSE endpoint, event publication, and focused HTTP tests."
  key_links:
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-pheromone/src/substrate.rs"
      via: "host-filtered deposit query powers posture concentration summaries"
      pattern: "DepositQuery"
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-runtime/src/runtime_events.rs"
      via: "platform findings SSE subscribes to finding runtime events"
      pattern: "RuntimeEvent::Finding"
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-spine/src/investigation.rs"
      via: "active host investigations derive from persisted investigation records"
      pattern: "host_id"
---

<objective>
Expose host posture and live findings on the versioned platform API.

Purpose: make `/v2/api/*` usable for operator-facing and downstream asset workflows instead of leaving it as list-only plumbing.
Output: host-filtered substrate queries, asset posture read model, canonical finding runtime events, and authenticated findings SSE.
</objective>

<context>
@.planning/phases/133-asset-posture-and-findings-stream-surface/133-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/REQUIREMENTS.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-pheromone/src/substrate.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/runtime_events.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-spine/src/investigation.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-spine/src/store.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add substrate-backed host filtering and runtime finding events</name>
  <read_first>
    crates/swarm-pheromone/src/substrate.rs
    crates/swarm-runtime/src/detection/pipeline.rs
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/runtime_events.rs
  </read_first>
  <files>
    crates/swarm-pheromone/src/substrate.rs
    crates/swarm-whisker/src/stream.rs
    crates/swarm-runtime/src/detection/pipeline.rs
    crates/swarm-runtime/src/service.rs
    crates/swarm-runtime/src/stalker_agent.rs
    crates/swarm-runtime/src/runtime_events.rs
    crates/swarm-runtime/src/ingest.rs
  </files>
  <action>Extend deposit queries with `host_id` filtering, ensure new deposits carry host identity, add a first-class runtime `finding` event, and publish those events when ingest or demo replay produces findings.</action>
  <verify>
    <automated>cargo test -p swarm-pheromone substrate::tests::query_deposits_filters_by_host_id -- --exact</automated>
    <automated>cargo test -p swarm-runtime ingest::tests::process_runtime_event_publishes_finding_runtime_events -- --exact</automated>
  </verify>
  <done>The runtime can identify host-scoped deposits and broadcast canonical findings as they happen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose asset posture and platform findings SSE</name>
  <read_first>
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-spine/src/investigation.rs
    crates/swarm-spine/src/store.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/ingest.rs
  </files>
  <action>Add `/v2/api/assets/{host_id}/posture` and `/v2/api/stream/findings`, both behind the existing platform API auth, and prove the posture payload plus SSE behavior with focused tests.</action>
  <verify>
    <automated>cargo test -p swarm-runtime ingest::tests::platform_asset_posture_endpoint_returns_host_filtered_posture -- --exact</automated>
    <automated>cargo test -p swarm-runtime ingest::tests::platform_findings_stream_endpoint_emits_canonical_finding_events -- --exact</automated>
  </verify>
  <done>The platform API now exposes both host posture and live findings without breaking the Phase 132 contract.</done>
</task>

</tasks>

<verification>
Run the new substrate test plus the focused ingest tests, then rerun the existing Phase 132 platform test slice to catch route regressions.
</verification>

<output>
After completion, create `.planning/phases/133-asset-posture-and-findings-stream-surface/133-01-SUMMARY.md`
</output>
