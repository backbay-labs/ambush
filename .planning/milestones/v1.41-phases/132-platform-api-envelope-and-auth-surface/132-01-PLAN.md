---
phase: 132-platform-api-envelope-and-auth-surface
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 131
files_modified:
  - .planning/phases/132-platform-api-envelope-and-auth-surface/132-CONTEXT.md
  - crates/swarm-core/src/config.rs
  - crates/swarm-runtime/src/ingest.rs
autonomous: true
requirements:
  - API-01
  - API-04
must_haves:
  truths:
    - "The detect server exposes `/v2/api/findings`, `/v2/api/incidents`, and `/v2/api/runtime/status` behind one `{ data, cursor }` response envelope."
    - "Platform API pagination defaults to `page_size=50`, caps requests at `200`, and supports repo-owned query filters on findings and incidents."
    - "Only `/v2/api/*` is protected by the new platform API key middleware; `/v1/ingest/events`, demo routes, and health probes remain outside that gate."
    - "Configured SHA-256 key hashes resolve a `read` principal that is attached to request context for downstream handler logging."
  artifacts:
    - path: "crates/swarm-core/src/config.rs"
      provides: "Repo-owned `platform_api` config and validation for hashed read keys."
    - path: "crates/swarm-runtime/src/ingest.rs"
      provides: "Platform API route group, envelope types, cursor pagination, API-key middleware, and focused integration tests."
  key_links:
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-core/src/config.rs"
      via: "platform API key config and request pagination defaults"
      pattern: "platform_api"
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-spine/src/store.rs"
      via: "recent replay bundles become platform finding summaries"
      pattern: "recent"
    - from: "crates/swarm-runtime/src/ingest.rs"
      to: "crates/swarm-spine/src/incident.rs"
      via: "recent incidents become platform incident summaries"
      pattern: "recent"
---

<objective>
Build the first versioned platform read API on the detect server.

Purpose: turn the current demo/runtime state into a stable, authenticated read surface that external consumers can page through without touching the operator contract.
Output: `platform_api` config, scoped API-key middleware, `/v2/api/*` list routes, and tests proving auth boundaries plus filterable pagination behavior.
</objective>

<context>
@.planning/phases/132-platform-api-envelope-and-auth-surface/132-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/REQUIREMENTS.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-spine/src/store.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-spine/src/incident.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add platform API config and scoped key validation</name>
  <read_first>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/ingest.rs
  </read_first>
  <files>
    crates/swarm-core/src/config.rs
    crates/swarm-runtime/src/ingest.rs
  </files>
  <action>Add a repo-owned `platform_api` config section for hashed keys and read scope metadata, then wire detect-server middleware that validates an inbound `x-api-key`, resolves the configured principal, and attaches it to request extensions.</action>
  <verify>
    <automated>cargo test -p swarm-core config::tests::platform_api_rejects_invalid_key_hash -- --exact</automated>
    <automated>cargo test -p swarm-runtime ingest::tests::platform_api_routes_require_valid_api_key_but_health_and_ingest_do_not -- --exact</automated>
  </verify>
  <done>`/v2/api/*` has a distinct auth gate that does not leak onto existing operator or ingest surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose paginated findings, incidents, and runtime status</name>
  <read_first>
    crates/swarm-runtime/src/ingest.rs
    crates/swarm-spine/src/store.rs
    crates/swarm-spine/src/incident.rs
  </read_first>
  <files>
    crates/swarm-runtime/src/ingest.rs
  </files>
  <action>Add the `/v2/api/*` route group with one shared envelope, findings and incidents filters plus cursor pagination, and a runtime-status summary backed by the live detect state.</action>
  <verify>
    <automated>cargo test -p swarm-runtime ingest::tests::platform_findings_endpoint_returns_filtered_cursor_paginated_envelope -- --exact</automated>
    <automated>cargo test -p swarm-runtime ingest::tests::platform_incidents_endpoint_returns_filtered_cursor_paginated_envelope -- --exact</automated>
    <automated>cargo test -p swarm-runtime ingest::tests::platform_runtime_status_endpoint_returns_live_status_envelope -- --exact</automated>
  </verify>
  <done>The platform API serves stable versioned reads with the pagination and auth contract required by Phase 132.</done>
</task>

</tasks>

<verification>
Run the focused config and ingest tests first, then a broader `cargo test -p swarm-runtime ingest::tests` slice if the targeted platform tests stay green.
</verification>

<output>
After completion, create `.planning/phases/132-platform-api-envelope-and-auth-surface/132-01-SUMMARY.md`
</output>
