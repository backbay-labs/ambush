---
phase: 134-helm-deployment-and-config-cli-experience
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 132
files_modified:
  - .planning/phases/134-helm-deployment-and-config-cli-experience/134-CONTEXT.md
  - crates/swarm-runtime/src/cli/core.inc
  - docs/CONFIGURATION.md
  - deploy/helm/swarm-team-six/Chart.yaml
  - deploy/helm/swarm-team-six/values.yaml
  - deploy/helm/swarm-team-six/templates/_helpers.tpl
  - deploy/helm/swarm-team-six/templates/configmap.yaml
  - deploy/helm/swarm-team-six/templates/secret.yaml
  - deploy/helm/swarm-team-six/templates/service.yaml
  - deploy/helm/swarm-team-six/templates/deployment.yaml
  - deploy/helm/swarm-team-six/templates/pdb.yaml
autonomous: true
requirements:
  - HELM-01a
  - HELM-01b
  - HELM-02
  - CLI-01
  - CLI-02
must_haves:
  truths:
    - "`swarmctl validate --config <path>` reuses the existing config loader, exits non-zero on invalid config or failed optional endpoint probes, and emits structured JSON when `--json` is passed."
    - "`swarmctl init --mode detect_only|live_response` writes a complete `rulesets/custom.yaml` template with inline comments and valid defaults for the selected mode."
    - "A new repo-owned Helm chart deploys `swarm_detect --serve` with ConfigMap-mounted config, Secret-backed `@secret:` files, configurable resources, probe wiring, and a PodDisruptionBudget."
    - "The chart parameterizes runtime mode, strategies, substrate backend, response adapter, SIEM forwarding, notification channels, and optional JetStream-backed NATS deployment."
  artifacts:
    - path: "crates/swarm-runtime/src/cli/core.inc"
      provides: "New init and validate workflows plus focused CLI tests."
    - path: "deploy/helm/swarm-team-six/values.yaml"
      provides: "The repo-owned deployment contract for current runtime surfaces."
    - path: "deploy/helm/swarm-team-six/templates/deployment.yaml"
      provides: "Mounted config, secret files, command args, resources, and probe wiring."
  key_links:
    - from: "crates/swarm-runtime/src/cli/core.inc"
      to: "crates/swarm-runtime/src/config.rs"
      via: "validate command reuses load_config() and outbound secret resolution"
      pattern: "load_config"
    - from: "deploy/helm/swarm-team-six/templates/configmap.yaml"
      to: "rulesets/default.yaml"
      via: "chart-generated config mirrors the repository-owned runtime schema"
      pattern: "schema_version"
    - from: "deploy/helm/swarm-team-six/templates/deployment.yaml"
      to: "crates/swarm-runtime/src/ingest.rs"
      via: "probe wiring targets the existing startup, readiness, liveness, and prestop handlers"
      pattern: "/startupz"
---

<objective>
Make the runtime deployable and operator-friendly with one chart and two bootstrapping CLI flows.

Purpose: remove the gap between a demo-ready runtime and a repeatable deployment target.
Output: `swarmctl validate`, `swarmctl init`, and a Helm chart tree that can render a complete `swarm_detect --serve` deployment.
</objective>

<context>
@.planning/phases/134-helm-deployment-and-config-cli-experience/134-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/REQUIREMENTS.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/cli/core.inc
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/rulesets/default.yaml
@/Users/connor/Medica/backbay/standalone/swarm-team-six/Dockerfile
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repo-owned `swarmctl validate` and `swarmctl init` flows</name>
  <read_first>
    crates/swarm-runtime/src/cli/core.inc
    crates/swarm-runtime/src/config.rs
    crates/swarm-core/src/config.rs
    rulesets/default.yaml
  </read_first>
  <files>
    crates/swarm-runtime/src/cli/core.inc
    docs/CONFIGURATION.md
  </files>
  <action>Route `validate` and `init` before the existing config-dependent CLI bootstrap, reuse the runtime config loader, add optional TCP endpoint probes, emit structured JSON reports, and write a documented `rulesets/custom.yaml` template for detect-only and live-response modes.</action>
  <verify>
    <automated>cargo test -p swarm-runtime cli::dispatch::tests::validate_command_reports_structured_json_and_endpoint_failures -- --exact</automated>
    <automated>cargo test -p swarm-runtime cli::dispatch::tests::init_command_writes_live_response_template_to_default_output -- --exact</automated>
  </verify>
  <done>The CLI can bootstrap and validate deployment config without relying on pre-existing operator artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship the base Helm chart for `swarm_detect --serve`</name>
  <read_first>
    Dockerfile
    rulesets/default.yaml
    docs/CONFIGURATION.md
  </read_first>
  <files>
    deploy/helm/swarm-team-six/Chart.yaml
    deploy/helm/swarm-team-six/values.yaml
    deploy/helm/swarm-team-six/templates/_helpers.tpl
    deploy/helm/swarm-team-six/templates/configmap.yaml
    deploy/helm/swarm-team-six/templates/secret.yaml
    deploy/helm/swarm-team-six/templates/service.yaml
    deploy/helm/swarm-team-six/templates/deployment.yaml
    deploy/helm/swarm-team-six/templates/pdb.yaml
  </files>
  <action>Create a repo-owned Helm chart that renders the runtime config, mounts secrets into `runtime.secret_dir`, deploys `swarm_detect --serve`, wires the existing probe surface and PreStop hook, exposes resources and service settings, and includes an optional NATS dependency for JetStream-backed substrate mode.</action>
  <verify>
    <automated>helm template swarm-team-six deploy/helm/swarm-team-six</automated>
  </verify>
  <done>The repo contains a renderable deployment chart that matches the current runtime contract.</done>
</task>

</tasks>

<verification>
Run the new focused CLI tests, format the workspace, and render the chart with `helm template` if Helm is available in the environment.
</verification>

<output>
After completion, create `.planning/phases/134-helm-deployment-and-config-cli-experience/134-01-SUMMARY.md`
</output>
