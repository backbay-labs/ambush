# Plan 47-01

1. Add file-backed portfolio artifacts and a runtime harness above ranked-candidate selections and mutation rankings.
2. Preserve ranking, selection, cohort, mutation-batch, validation-batch, and rollout-lineage context on each portfolio entry.
3. Add `swarmctl` commands to create, reload, and filter portfolio artifacts by stable ID.
4. Cover multi-entry assembly and reload behavior with runtime tests.
