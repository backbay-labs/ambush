---
phase: 173-counterexample-harvest-and-replay-regeneration
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 172
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-evolution/src/evolution.rs
  - crates/swarm-evolution/src/mutation.rs
  - rulesets/default.yaml
autonomous: true
requirements:
  - COUNTER-01
  - COUNTER-02
must_haves:
  truths:
    - "Solver counterexamples and evasion misses must become durable replayable cases with lineage."
    - "Harvested cases must feed existing mutation ranking and proposal fitness instead of living in a side store only."
    - "The replayable case format must stay repo-owned and deterministic."
  artifacts:
    - path: "crates/swarm-evolution/src/evolution.rs"
      provides: "Counterexample and evasion-gap harvest with lineage."
    - path: "crates/swarm-evolution/src/mutation.rs"
      provides: "Mutation ranking or fitness integration for harvested assurance cases."
    - path: "crates/swarm-core/src/config.rs"
      provides: "Repo-owned harvest path and bounded regeneration controls."
---

<objective>
Complete Phase 173 by harvesting assurance failures into durable replayable cases and feeding those cases into mutation ranking and proposal fitness.

Purpose: close the loop between assurance failure and future candidate improvement.
Output: persisted replayable assurance cases, lineage back to proof and strategy artifacts, and mutation/ranking summaries that consume harvested evidence.
</objective>

<context>
@.planning/phases/173-counterexample-harvest-and-replay-regeneration/173-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/evolution.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/mutation.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist replayable assurance cases with lineage</name>
  <files>
    crates/swarm-core/src/config.rs
    crates/swarm-evolution/src/evolution.rs
    rulesets/default.yaml
  </files>
  <action>Write durable assurance-case artifacts for solver counterexamples and evasion misses, including strategy, proof, detector, and source lineage.</action>
  <done>Failed assurance checks now generate stable replay-case artifacts instead of only blocking a proposal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regenerate replay-friendly case manifests</name>
  <files>
    crates/swarm-evolution/src/evolution.rs
  </files>
  <action>Emit deterministic replay or scenario payloads from harvested assurance cases so they can be consumed by the existing replay and review lane.</action>
  <done>Each harvested case can be traced back to one proof or coverage failure and replayed as durable evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Feed harvested evidence into mutation ranking and fitness</name>
  <files>
    crates/swarm-evolution/src/mutation.rs
  </files>
  <action>Attach harvested assurance-case counts and lineage to mutation ranking or population fitness so future variants can see what they need to close.</action>
  <done>Mutation artifacts and review summaries now reflect harvested assurance evidence instead of only raw replay and evasion pressure.</done>
</task>

</tasks>

<verification>
Keep this phase harvest-focused. Fail-closed rollout transitions and operator waivers belong to Phases 174 and 175.
</verification>
