---
phase: 172-assurance-policy-and-gate-inputs
plan: 01
type: execute
wave: 1
depends_on:
  - Phase 171
files_modified:
  - crates/swarm-core/src/config.rs
  - crates/swarm-evolution/src/evolution.rs
  - crates/swarm-runtime/src/evolution_status.rs
  - rulesets/default.yaml
autonomous: true
requirements:
  - ASSURE-01
  - ASSURE-02
must_haves:
  truths:
    - "Evasion coverage floors and solver outcomes must feed one shared repo-owned assurance policy."
    - "Assurance policy must explain why a candidate is blocked instead of only marking it unsafe."
    - "The new policy must reuse the shipped proof, queue, and status lanes instead of forking them."
  artifacts:
    - path: "crates/swarm-core/src/config.rs"
      provides: "Repo-owned assurance policy configuration."
    - path: "crates/swarm-evolution/src/evolution.rs"
      provides: "Shared assurance evaluation and proposal blocking lineage."
    - path: "crates/swarm-runtime/src/evolution_status.rs"
      provides: "Operator-visible assurance summary from durable artifacts."
---

<objective>
Complete Phase 172 by adding one repo-owned assurance policy that evaluates evasion coverage floors and solver proof outcomes before a candidate remains queue-eligible.

Purpose: convert existing robustness artifacts into first-class gate inputs without yet changing downstream canary and promotion state machines.
Output: assurance policy config, durable assurance summaries on proposals, and shared status surfacing for the latest assurance state.
</objective>

<context>
@.planning/phases/172-assurance-policy-and-gate-inputs/172-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-core/src/config.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-evolution/src/evolution.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/evolution_status.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repo-owned assurance policy config</name>
  <files>
    crates/swarm-core/src/config.rs
    rulesets/default.yaml
  </files>
  <action>Add assurance configuration covering per-detector or global evasion coverage floors plus accepted solver-proof outcomes.</action>
  <done>The assurance policy is operator-readable, validated at config load, and available from the existing `evolution` config tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Evaluate assurance in the proof-backed queue path</name>
  <files>
    crates/swarm-evolution/src/evolution.rs
  </files>
  <action>Attach one shared assurance evaluation to queue proposals so coverage shortfalls, solver counterexamples, timeouts, or disabled proof states can block a candidate with explicit lineage.</action>
  <done>Queue proposals now persist assurance summary and assurance-derived blocking reasons through the existing proposal artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Surface assurance status through the shared evolution status lane</name>
  <files>
    crates/swarm-runtime/src/evolution_status.rs
  </files>
  <action>Expose the latest assurance policy outcome and blocked-gap counts through the existing evolution status report.</action>
  <done>Operators can see whether the latest candidate is blocked by assurance policy without opening raw queue artifacts.</done>
</task>

</tasks>

<verification>
Keep this phase policy-focused. Harvesting replay cases and rollout fail-closed enforcement belong to later phases in the milestone.
</verification>
