# Phase 69 Plan

1. Add a durable imported-capsule artifact with verification checks, trust status, and remote signer metadata.
2. Verify imported capsules against canonical payload, digest, detached signature, and local trust expectations.
3. Expose imported-capsule lookup through `swarmctl` and the authenticated review surface.
