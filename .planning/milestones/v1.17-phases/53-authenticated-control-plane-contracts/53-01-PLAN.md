# Plan 53-01

1. Extend runtime config with an operator-surface section that captures local bind address and bearer-token env configuration plus semantic validation.
2. Add the minimal HTTP stack and router contracts for a local authenticated operator surface, including auth extraction and structured API errors.
3. Wire `swarmctl` with a `serve` entry point that starts the authenticated surface from the same repo-owned config and results-dir globals already used by the CLI.
4. Verify the contracts with config validation tests and router auth tests.
