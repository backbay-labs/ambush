---
milestone: v1.37
audited: 2026-04-07T21:15:12Z
status: passed
scores:
  requirements: 5/5
  phases: 4/4
  integration: 5/5
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - persistence coverage is heuristic and path-based today; the runtime does not yet correlate signer trust or package provenance with persistent footholds beyond the shipped indicators
  - supply-chain detection can use signer metadata when present, but current Tetragon ingestion does not populate signer fields, so richer provenance depends on later bridge enrichment
  - the generic JSON bridge now supports persistence payloads, but the repo still lacks a larger scenario corpus for persistence and supply-chain replay beyond the milestone integration proof
nyquist:
  compliant_phases: []
  partial_phases: []
  missing_phases:
    - 112-telemetry-persistence-payloads-and-detector-contracts
    - 113-persistence-detector-and-profile-support
    - 114-supply-chain-detector-and-profile-support
    - 115-persistence-and-supply-chain-integration-proof
  overall: missing
---

# Milestone v1.37 Audit

## Summary

`v1.37 Persistence And Supply Chain Detection` achieved its definition of done. The shared telemetry schema now models persistence-specific payloads and optional signer metadata, the runtime can construct `persistence` and `supply_chain` strategies across live and replay-owned surfaces, and the shipped detector families produce ATT&CK-tagged findings with non-zero pheromone deposits through runtime-facing integration proof.

## Requirements Coverage

| Requirement | Phase | Verification | Final Status | Evidence |
|-------------|-------|--------------|--------------|----------|
| PERSIST-01 | 113 | passed | satisfied | `PersistenceDetector` now evaluates `RegistryPersistence` and `FilePersistence` payloads for run keys, cron, systemd timers, and scheduled-task footholds. |
| PERSIST-02 | 114 | passed | satisfied | `SupplyChainDetector` now evaluates trusted-path signature abuse, DLL side-loading, and signed-binary abuse from normalized telemetry. |
| PERSIST-03 | 112, 114 | passed | satisfied | Shared telemetry and pheromone contracts now include `ThreatClass::SupplyChain`, and both detector families emit `mitre_technique_id` evidence. |
| PERSIST-04 | 112, 113, 114 | passed | satisfied | `PersistenceProfile` and `SupplyChainProfile` are now repo-owned config surfaces with fail-closed validation. |
| PERSIST-05 | 115 | passed | satisfied | Runtime integration tests now drive persistence and supply-chain telemetry through config-selected detectors into non-zero pheromone deposits. |

## Phase Verification

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 112 Telemetry Persistence Payloads And Detector Contracts | passed | 5/5 | Added shared persistence payloads, optional process signer metadata, `ThreatClass::SupplyChain`, and runtime-owned detector profile/config support. |
| 113 Persistence Detector And Profile Support | passed | 5/5 | Added `PersistenceDetector` plus focused heuristics and regression coverage for run keys, cron, systemd timers, and scheduled tasks. |
| 114 Supply Chain Detector And Profile Support | passed | 5/5 | Added `SupplyChainDetector` plus focused heuristics and regression coverage for trusted-path abuse, DLL side-loading, and signed-binary abuse. |
| 115 Persistence And Supply Chain Integration Proof | passed | 5/5 | Added runtime integration proof, strategy-factory coverage, docs, verification, and milestone closeout evidence. |

## Cross-Phase Integration

| Flow | Status | Evidence |
|------|--------|----------|
| shared telemetry contract -> detector construction across live, replay, canary, and promotion | ✓ VERIFIED | Runtime config, `supported_detector`, replay manifests, canary, and promotion surfaces now all construct `persistence` and `supply_chain` detectors from the same repo-owned config. |
| generic JSON or mapped telemetry -> persistence payload normalization | ✓ VERIFIED | The generic JSON bridge now maps `registry_persistence` and `file_persistence` payload kinds through validated JSON Pointer fields into shared telemetry. |
| process signer metadata -> supply-chain heuristics | ✓ VERIFIED | `ProcessStartEvent` now carries optional signer metadata and the supply-chain detector consumes it for unsigned trusted-path execution checks. |
| detector findings -> ATT&CK-tagged evidence -> pheromone deposits | ✓ VERIFIED | Both new detector families emit `mitre_technique_id` evidence and the runtime integration proof confirms those findings deposit into the substrate with non-zero confidence. |
| docs/config examples -> runtime strategy selection | ✓ VERIFIED | The default ruleset, configuration reference, README, and runtime strategy smoke test now all reflect `persistence` and `supply_chain` as shipped choices. |

## End-to-End Flows

| Flow | Status | Evidence |
|------|--------|----------|
| detect registry-run persistence and deposit a persistence pheromone | ✓ VERIFIED | The new runtime integration test constructs `RegistryPersistence` telemetry and proves `strategy: persistence` emits one `ThreatClass::Persistence` finding and deposit. |
| detect unsigned trusted-path execution and deposit a supply-chain pheromone | ✓ VERIFIED | The new runtime integration test constructs signedness-aware `ProcessStart` telemetry and proves `strategy: supply_chain` emits one `ThreatClass::SupplyChain` finding and deposit. |
| select new detectors from repo-owned config without breaking existing strategies | ✓ VERIFIED | The critical-path integration suite now includes `persistence` and `supply_chain` in the supported strategy matrix. |
| preserve ATT&CK evidence through runtime handling | ✓ VERIFIED | Integration assertions prove `mitre_technique_id` survives from detection output to the deposited finding evidence. |
| keep the workspace green after broad telemetry/runtime changes | ✓ VERIFIED | `cargo test --workspace`, strict workspace clippy, and `cargo build --workspace` all passed after the milestone landed. |

## Notes

- Verification remained green through full workspace tests, strict clippy across all crates and tests, and a final workspace build.
- The milestone intentionally expands normalized telemetry and strategy construction first so later fileless or provenance-focused detectors can build on stable contracts.
- Nyquist validation artifacts are still absent for phases 112-115 and remain follow-up work.

## Verdict

Milestone `v1.37` passes audit and is ready for archival.
