---
phase: 258-inbound-soar-verdict-sync
plan: 01
type: execute
depends_on:
  - 257-generated-python-client
files_modified:
  - crates/swarm-core/src/types.rs
  - crates/swarm-runtime/src/ingest/mod.rs
  - crates/swarm-runtime/src/ingest/soar_verdict_handlers.rs
  - crates/swarm-runtime/src/ingest/providence_handlers.rs
  - crates/swarm-runtime/src/ingest/tests.rs
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - SOARSYNC-01
---

<objective>
Accept bounded Splunk SOAR, Sentinel SOAR, and Chronicle SOAR verdicts on one signed
runtime-owned ingress and route them onto the existing false-positive and evolution
feedback path instead of creating a disconnected side channel.
</objective>

<tasks>

<task>
  <name>Define one inbound verdict contract</name>
  <done>The core types now carry a normalized SOAR verdict request for the supported source systems.</done>
</task>

<task>
  <name>Wire one signed ingress surface</name>
  <done>The detect router now serves `POST /v1/soar/verdicts` using a dedicated signed webhook configuration.</done>
</task>

<task>
  <name>Reuse the existing feedback path</name>
  <done>Accepted SOAR verdicts now resolve the existing feedback target and call the Providence-backed substrate, investigation, false-positive, and Kitten routing logic.</done>
</task>

</tasks>
