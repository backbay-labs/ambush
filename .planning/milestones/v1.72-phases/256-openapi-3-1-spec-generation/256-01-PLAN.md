---
phase: 256-openapi-3-1-spec-generation
plan: 01
type: execute
depends_on:
  - 255-release-pipeline-and-container-publishing
files_modified:
  - crates/swarm-runtime/src/bin/generate_platform_openapi.rs
  - docs/openapi/v2-platform-openapi.json
  - tools/generate-platform-openapi.sh
  - tools/check-platform-openapi.sh
autonomous: true
requirements:
  - APISPEC-01
---

<objective>
Generate one repo-owned OpenAPI 3.1 contract for the shipped authenticated `/v2/api/`
surface and validate it from the emitted artifact instead of maintaining a second
handwritten API description.
</objective>

<tasks>

<task>
  <name>Define one generator-backed source of truth</name>
  <done>The runtime now ships a `generate_platform_openapi` binary that emits the checked-in platform contract from repo-owned code.</done>
</task>

<task>
  <name>Cover the shipped `/v2/api/` surface</name>
  <done>The emitted spec covers findings, incidents, evasion coverage, asset posture, runtime status, and findings stream endpoints with auth and error envelopes.</done>
</task>

<task>
  <name>Validate the emitted document</name>
  <done>Repo-owned helper scripts now regenerate and OpenAPI-validate the checked-in `docs/openapi/v2-platform-openapi.json` artifact.</done>
</task>

</tasks>
