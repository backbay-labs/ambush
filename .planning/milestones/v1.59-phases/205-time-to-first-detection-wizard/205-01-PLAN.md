---
phase: 205-time-to-first-detection-wizard
plan: 01
type: execute
wave: 1
depends_on:
  - phase-204
files_modified:
  - crates/swarm-cli/src/core.inc
  - crates/swarm-runtime/src/ingest/demo.rs
  - crates/swarm-runtime/src/control.rs
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - ONBOARD-02
must_haves:
  truths:
    - "The wizard must reuse the Phase 204 readiness contract instead of bypassing it."
    - "Synthetic telemetry injection, first detection, approval, and proof export must stay in one bounded operator-visible flow."
    - "Onboarding state must persist enough context for operators to resume or inspect the first-run sequence."
  artifacts:
    - path: "crates/swarm-runtime/src/ingest/demo.rs"
      provides: "Guided first-run runtime flow and synthetic telemetry execution."
    - path: "crates/swarm-cli/src/core.inc"
      provides: "Operator-facing CLI entrypoint for the first-run wizard."
---

<objective>
Add the repo-owned time-to-first-detection wizard so operators can move from a
passing readiness report to a guided synthetic detection, approval, and proof
export flow within one bounded first-run path.
</objective>

<context>
@.planning/phases/205-time-to-first-detection-wizard/205-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/demo.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-cli/src/core.inc
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Define the first-run wizard state machine</name>
  <files>
    crates/swarm-runtime/src/ingest/demo.rs
    crates/swarm-runtime/src/control.rs
  </files>
  <action>Compose readiness, synthetic telemetry injection, first detection visibility, approval gating, and proof export into one bounded wizard state machine with durable step context.</action>
  <done>The runtime can create and advance one first-run wizard flow without relying on ad hoc manual orchestration.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Surface the wizard through swarmctl and docs</name>
  <files>
    crates/swarm-cli/src/core.inc
    docs/CONFIGURATION.md
  </files>
  <action>Add the operator-facing CLI command and documentation so the first-run wizard is discoverable and uses the readiness diagnostic as its entry gate.</action>
  <done>`swarmctl` exposes the wizard and the docs explain the required readiness precondition plus the expected detection, approval, and proof sequence.</done>
</task>

</tasks>

<verification>
Verify that the first-run wizard refuses to start when readiness is blocked and
that a passing run yields a first detection, approval artifact, and proof
export through the existing repo-owned runtime paths.
</verification>
