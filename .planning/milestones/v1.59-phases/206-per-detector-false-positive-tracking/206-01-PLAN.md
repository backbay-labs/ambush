---
phase: 206-per-detector-false-positive-tracking
plan: 01
type: execute
wave: 1
depends_on:
  - phase-205
files_modified:
  - crates/swarm-runtime/src/ingest/providence_handlers.rs
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/src/ingest/platform_api.rs
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - ONBOARD-03
must_haves:
  truths:
    - "False-positive tracking must reuse the existing analyst feedback ingress instead of introducing a second feedback contract."
    - "Measurements must stay bounded and attributable per detector and per host."
    - "Operators must be able to inspect the measured FP state through repo-owned CLI and API surfaces."
  artifacts:
    - path: "crates/swarm-runtime/src/ingest/providence_handlers.rs"
      provides: "Durable false-positive measurement updates driven by signed analyst feedback."
    - path: "crates/swarm-runtime/src/control.rs"
      provides: "Repo-owned operator report for per-detector and per-host FP measurements."
---

<objective>
Persist and surface measured false-positive rates per detector and per host
from analyst feedback without widening the existing Providence feedback
contract.
</objective>

<context>
@.planning/phases/206-per-detector-false-positive-tracking/206-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/providence_handlers.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/control.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Persist bounded FP measurements from analyst feedback</name>
  <files>
    crates/swarm-runtime/src/ingest/providence_handlers.rs
  </files>
  <action>Derive per-detector and per-host false-positive measurements from the existing signed analyst feedback flow and store them in a bounded runtime-owned artifact that later phases can reuse.</action>
  <done>Dismiss or false-positive feedback updates durable per-detector and per-host measurement state instead of only appending audit text.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Expose FP measurements to operators</name>
  <files>
    crates/swarm-runtime/src/control.rs
    crates/swarm-runtime/src/ingest/platform_api.rs
    docs/CONFIGURATION.md
  </files>
  <action>Add repo-owned CLI and API summaries for the measured false-positive state and document how operators inspect it.</action>
  <done>Operators can inspect per-detector and per-host false-positive measurements through `swarmctl` and the platform API without reading raw incident feedback history.</done>
</task>

</tasks>

<verification>
Verify that signed analyst feedback updates durable per-detector and per-host
false-positive measurements and that the new repo-owned CLI or API surfaces can
retrieve those measurements.
</verification>
