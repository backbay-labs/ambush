---
phase: 204-readiness-diagnostic-and-guided-setup
plan: 01
type: execute
wave: 1
depends_on:
  - v1.58 complete
files_modified:
  - crates/swarm-cli/src/core.inc
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/src/ingest/health.rs
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - ONBOARD-01
must_haves:
  truths:
    - "The readiness diagnostic must prove operational readiness, not just config parse success."
    - "Telemetry source connectivity, detector activation, and substrate health all need explicit operator-visible verdicts."
    - "The output should compose with later guided-first-run work rather than hard-code the Phase 205 flow."
  artifacts:
    - path: "crates/swarm-cli/src/core.inc"
      provides: "Operator-facing readiness command surface."
    - path: "crates/swarm-runtime/src/control.rs"
      provides: "Runtime-side readiness aggregation from existing health and config seams."
---

<objective>
Start Phase 204 by adding a readiness diagnostic that validates telemetry
connectivity, detector activation, and substrate health before onboarding
declares the runtime ready for a guided first run.
</objective>

<context>
@.planning/phases/204-readiness-diagnostic-and-guided-setup/204-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-cli/src/core.inc
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/control.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Define the readiness diagnostic contract</name>
  <files>
    crates/swarm-runtime/src/control.rs
    crates/swarm-runtime/src/ingest/health.rs
  </files>
  <action>Aggregate existing runtime and ingest health into one explicit readiness verdict covering telemetry sources, detector activation, and substrate state.</action>
  <done>Operators can inspect one bounded readiness report that explains why the runtime is or is not ready for first-run onboarding.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Surface readiness through swarmctl</name>
  <files>
    crates/swarm-cli/src/core.inc
    docs/CONFIGURATION.md
  </files>
  <action>Add a repo-owned CLI entrypoint and documentation for the readiness diagnostic so onboarding can depend on one shared operator command.</action>
  <done>`swarmctl` exposes the readiness diagnostic and the docs explain what each readiness input means before the guided walkthrough exists.</done>
</task>

</tasks>

<verification>
Verify that the readiness diagnostic fails clearly when telemetry, detector
activation, or substrate health are missing and that it reports ready only
when all required onboarding inputs are satisfied.
</verification>
