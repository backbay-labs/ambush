---
phase: 207-alert-tuning-recommendations
plan: 01
type: execute
wave: 1
depends_on:
  - phase-206
files_modified:
  - crates/swarm-runtime/src/control.rs
  - crates/swarm-runtime/src/ingest/platform_api.rs
  - crates/swarm-runtime/src/alert_tuning.rs
  - docs/CONFIGURATION.md
autonomous: true
requirements:
  - ONBOARD-04
must_haves:
  truths:
    - "Recommendations must derive from the measured false-positive state gathered in Phase 206 instead of from raw feedback audit payloads."
    - "Outputs must remain advisory; this phase does not write runtime config or exclusion lists automatically."
    - "Operators must be able to inspect the recommendation set through repo-owned CLI and API surfaces."
  artifacts:
    - path: "crates/swarm-runtime/src/alert_tuning.rs"
      provides: "Bounded recommendation derivation from detector and host false-positive measurements."
    - path: "crates/swarm-runtime/src/control.rs"
      provides: "Repo-owned operator summary for alert-tuning recommendations."
---

<objective>
Generate concrete detector-tuning recommendations from the measured
false-positive patterns gathered in Phase 206.
</objective>

<context>
@.planning/phases/207-alert-tuning-recommendations/207-CONTEXT.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/.planning/ROADMAP.md
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/control.rs
@/Users/connor/Medica/backbay/standalone/swarm-team-six/crates/swarm-runtime/src/ingest/platform_api.rs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Derive bounded tuning recommendations from measured FP state</name>
  <files>
    crates/swarm-runtime/src/alert_tuning.rs
  </files>
  <action>Turn the Phase 206 detector and host rollups into concrete advisory recommendations such as host exclusions, threshold review, or detector-specific investigation, with enough supporting context that operators can understand why each recommendation was emitted.</action>
  <done>The runtime can produce bounded recommendation artifacts from measured false-positive patterns without rescanning raw feedback payloads.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Expose alert-tuning recommendations to operators</name>
  <files>
    crates/swarm-runtime/src/control.rs
    crates/swarm-runtime/src/ingest/platform_api.rs
    docs/CONFIGURATION.md
  </files>
  <action>Publish the recommendation set through repo-owned control and platform read surfaces, and document how operators inspect and interpret that advice.</action>
  <done>Operators can retrieve concrete alert-tuning recommendations through `swarmctl` and the platform API instead of reading raw false-positive counters alone.</done>
</task>

</tasks>

<verification>
Verify that measured false-positive fixtures produce concrete bounded
recommendations with supporting context and that the new repo-owned CLI or API
surfaces can retrieve them.
</verification>
