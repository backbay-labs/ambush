name: mutation_materialized_office_baseline_control_kitten_evasion_bounded_perturbation_1_1776039150464
description: Materialized from mutation spec `evolution_mutation_spec:autonomous:office_baseline_control_kitten_evasion:1776039150293` variant `bounded-perturbation-1` using base experiment `office_baseline_control`
corpus:
  suite: ../scenario-suites/hellcat-office-v1.yaml
verification:
  corpus: ../verifications/office-detector-safety-v1.yaml
candidate:
  strategy: suspicious_process_tree
  strategy_id: office_baseline_control_kitten_evasion_bounded_perturbation_1
  description: Autonomous bounded perturbation from suspicious_process_tree
  profile:
    suspicious_parents:
    - winword
    - excel
    - outlook
    - acrord32
    - teams
    suspicious_children:
    - powershell
    - pwsh
    - cmd
    - sh
    - bash
    - curl
    - wget
    high_confidence_threshold: 0.84
    medium_confidence_threshold: 0.64
lineage:
  parent_strategy_id: suspicious_process_tree
  mutation: autonomous_bounded_perturbation
  rationale: seed a proposal-ready candidate with evasion pressure | apply a bounded threshold perturbation to top population candidate `suspicious_process_tree` while targeting measured evasion gaps (T1204.002, T1055)
gates:
  require_known_bad_coverage: true
  max_false_positive_delta: 0
  max_detect_latency_delta_us: 10000
