//! Core substrate operations.

// TODO: Implement
// - PheromoneSubstrate struct (NATS JetStream backed)
// - deposit(pheromone: PheromoneDeposit) -> Result<()>
// - query_concentration(threat_class, time_window) -> PheromoneConcentration
// - subscribe(threat_class, severity) -> Stream<PheromoneDeposit>
// - gc_evaporated(threshold) -> usize (returns count removed)
