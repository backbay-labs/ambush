[package]
name = "swarm-agents"
description = "Swarm agent role implementations scheduled by the runtime composition root"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
async-trait.workspace = true
ed25519-dalek.workspace = true
hex.workspace = true
rand_core.workspace = true
serde.workspace = true
serde_json.workspace = true
swarm-consensus.workspace = true
swarm-core.workspace = true
swarm-crypto.workspace = true
swarm-pheromone.workspace = true
swarm-policy.workspace = true
swarm-runtime.workspace = true
swarm-spine.workspace = true
swarm-whisker.workspace = true
thiserror.workspace = true
tokio.workspace = true
tracing.workspace = true

[target.'cfg(unix)'.dependencies]
libc.workspace = true

[dev-dependencies]
# Test-only collaborator: stalker's tests drive a full policy/response round
# trip. It does not appear in this crate's non-test code. (`swarm-policy` used
# to sit here too; `pounce_agent` now names it in production, so it moved up.)
swarm-response.workspace = true

[lints]
workspace = true
